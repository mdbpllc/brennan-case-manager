# The Firm-Obligations Hands-On Sitting and the Post-`CCS-1` Walk — Typed Session Capture (2026-09-12)

**Status:** RAW CAPTURE — typed design session (Cowork, Fable 5.1), design side. Not canonical, not a design doc, not in the build queue. The RULING-class record of the same sitting is `docs/specs/firm-obligations-hands-on-rulings-2026-09-12.md` (filed by the `#156` packet); this capture is the transcript-level record behind it.
**Canonical repo path:** `docs/record/firm-obligations-hands-on-2026-09-12/walkthrough-capture-2026-09-12.md` (EVIDENCE, `CAP-2`) — this file and that one are byte-identical.
**TRANSIT — this file is not a resident of project knowledge. It leaves for the LEGAL AUTHORITY ARCHIVE project or for Michael's own machine — his choice, never the repo — once BOTH conditions hold: (1) its session-log entry is filed at HEAD, and (2) every document its routing table names exists at its canonical repo path. VERIFY both by full-text read at HEAD before relocating — RAG absence is not evidence of absence. RELOCATED, NEVER DELETED.**
**Mode:** WRAP. The sitting is complete; nothing was left mid-stream. The next design act on this thread is the fix build (`FOS-2`, a Code session Michael fires), then the migration by his hand.
**Also still pending from before this thread:** the two `H12-v` asks; the DECISION 9A acquisitions; the DECISION 8 drafting act; `Q-STAT-7`; `Q-API-18`'s facts; `Q-API-19`; the Wave 0 acts; the `#137` Voice2 pair's and the `#155` pair's TRANSIT; go-live, his day.

## PART 0 — ORIGIN AND PREMISE

The firm-obligations slice (`FOS-1`) was built by a Code session on 2026-09-11/12 (pushed; local ref `5781420`), fixture-only in demo mode, one migration written and not run. Every list at HEAD — BUILD-STATE's NEXT ACTS, the build entry's "Next", the CC-1 hands-on queue — named THE FIRM-OBLIGATIONS HANDS-ON SITTING as the next act, with THE POST-`CCS-1` WALK (called at `#150`) still open beside it. CC-1(c) says a hands-on session runs long and wide because the expensive part is the mode switch. So this sitting took both walks in order and anything ripe beside them. Michael ran the app himself in his own Chrome (`npm run dev:demo` → `http://localhost:5175`, demo mode, the `FOD-21` fixture) and ruled from what he saw; every question went through the answer widget; every ruling was written into a running ledger in his words within the exchange it was made. The ledger is reproduced whole in PART 3.

## PART 1 — WHAT WAS SETTLED (all CONFIRMED unless marked)

**Part One — firm obligations.** The §8-against-`FOD-4` collision: "(A)" — settings editable any time on a not-past-due row; no code change; spec edit. The card, register, four forms and catalog: confirmed as built (the override keeps "due date"; double activation stays allowed; "Needs attention" unseen and still PROVISIONAL). Outlook: the reminder as his composite — hard items only, 30 days before target by default (or the lead if shorter), a per-obligation field raised by hand — after his own words about case deadlines (quoted in PART 3); Done keeps the event retitled and kills its reminder; no staleness fallback. `FO-6`: a firm-wide calendar page "later" (🟡). Leads: stand as shipped. All thirty-three `FOD-` defaults ruled. The nine handed-back design questions: column (B1) yes; CHECK (B2) tighten; RPC (C6) build now; retry the orphaned Outlook delete (C7); one act one line (C8/C14); C15 closed by consequence. Fix routing: draft the slice tonight; one slice, two groups.

**Part Two — post-`CCS-1`.** The split address, the Medical/Forms surfaces, `SD-3` (instrument-wide), the `R8` strings: confirmed as built. Six text acts (`R1`'s route line and tier-3 panel line, `R10`'s pronoun line, `R15`'s title strings, `SD-12`, `SD-11`) NOT reached — PROVISIONAL by name; `SD-14`/`HS-4` held. The seven proposed: `D-18` no episode sentence; `D-8` no sentence; per-paragraph regenerate "later" (🟡); `AS-Q14` (b); `AS-Q15`, `AS-Q16` as default; `AS-Q17` pause still fires, then designate. THIRD TRANCHE 8 (print once), 10 (Code tokenizes the master), 11 (`case_providers` list). `CAP-2a`: edit rows carry `—` — a convention amended; trigger 3; v33 delivered.

**`FOS-2`** — the fix slice, two groups, RULED YES ("Yes", 23:55 CDT) after two adversarial lanes and with `FXD-9` inside the question.

**Adjacent:** `Q-API-14` and `Q-API-3`'s filing half held again; `Q-API-18`/`Q-API-19` facts not now; `Q-STAT-7` not read.

**PROPOSED, unruled (Claude's, carried as defaults in the fix slice):** `FXD-1`–`FXD-11` — reported by number at the build, none approved by silence; `FXD-9` alone was put inside `FOS-2`.

## PART 2 — WHAT WAS NOT COVERED

No Outlook sign-in, so nothing seen live in his Outlook (DECISION 7's subject and body lines stay PROVISIONAL); Wave 0 (b) not offered. The `#137` Voice2 pair's TRANSIT condition (2) not verified (no Chrome docs-API read). `Q-STAT-7` not put. No weekend default (DECISION 8's act). The six `CCS-1` text acts above. The trigger-7 monthly review pass — offered in the record, not taken.

## PART 3 — THE RUNNING LEDGER, VERBATIM (Michael's picks in bold quotes; Claude's option text as such)

# RUNNING LEDGER — THE FIRM-OBLIGATIONS HANDS-ON SITTING — 2026-09-12 (Central; DT-1)

Typed design session, Cowork, Fable 5.1 per the environment. Opened 08:02 CDT 2026-09-12.
Every ruling below is written in Michael's words within the exchange it was made. Claude's option text is marked as such.

## Start-of-session facts (verified from staged copies; the bridge VM shell could not mount the folder — twice — so no git command ran on his machine)
- HEAD: `.git/refs/heads/master` = `.git/refs/remotes/origin/master` = `57814201bf0cda142c616701f6437e479b01ba7e` (5781420). LOCAL READ, not a QR-3 pass.
- Instructions field: v32 in force by a live read (project_info, 08:02 CDT).
- Knowledge meter: 1,690,163 / 2,000,000 = 84.5% at open; FIFTEEN docs. Q-CAP-5(a) did not fire.
- Live log top entry: `## 2026-09-12 — CODE SESSION (Opus 5, fresh, fired from …PROMPT-firm-obligations-slice-build-session.md…` — the FO build; 53,975 B incl. CAP-4 tail; read whole. Unindexed in the head file (batch-96 edition, 186 rows) — by design (TOC-4).
- `docs/spec-feedback.md` `## 2026-09-12` section read whole (parts A, B, C).
- BUILD-STATE at `e046906` (as committed in 5781420) read whole; 99,994 B per its banner.
- Head file §1's nine entries each confirmed present in the live log by heading; §2 186 rows.
- `package.json`: `dev:demo` = `vite --mode demo --port 5175` — so the demo URL is http://localhost:5175 (README's `npm run dev` → 5173 would reach SUPABASE mode on his machine, whose `.env` is filled). Entra redirect is `localhost:5173` — Outlook connect from 5175 will not work; the build session used 5173 by a temporary launch.json edit.

## Rulings (in order)
- 08:0x CDT — MIGRATION STATUS, his pick: **"Not run"**. `db/migrations/2026-09-10-firm-obligations.sql` stays written and not run; the ruled order sitting → migration → his real activations holds.
- 08:07 CDT — DEMO APP: `npm run dev:demo` → http://localhost:5175; his pick: **"Up — Demo mode, Obligations in the nav"**. Demo mode, fixture, today Sat 2026-09-12 driving the states.
- ITEM 1 — §8 against `FOD-4` (what may unlight a lit occurrence). First widget answered: *"I need to to explain this a bit more to me. I am not sure how to answer here."* — explained with the bar-dues example (lead 30→5 on Sep 20, three outcomes A/B/C). RULED, his pick: **"(A) Yes — settings are editable any time"** — keep what's built: a lead/rule/override edit on a not-yet-due row may unlight it; only a past-due row is protected from being moved later. SPEC EDIT: slice §8 first DO-NOT and §7 item 21 amended to name `FOD-4`'s edits; spec §12 `FOD-4` unchanged in substance. NO FIX for Code.
- (Clock note, DT-1: the item-1 pick arrived at ~22:04 CDT 2026-09-12 after a pause since ~08:10 CDT; the container's UTC date has rolled to 09-13 — the sitting stays stamped 2026-09-12 Central until midnight Central.)
- ITEM 2 — THE `/cases` CARD, his pick: **"Confirm as built"** — position below the statute-worklist card (`FOD-27`), title "Firm obligations — N due · M overdue", three lines then "and K more", "Open the register", "About these dates" (`FOD-26`, `FOD-24`), the hard-only horizon (hard AND (overdue OR (lit AND target within 14 days))) — CONFIRMED. Part C items 2, 9, 10 ACCEPTED as built: "1 day" at N=1; no day count on an `unknown` weekend row; a hard past-date item stays on the card. SPEC EDIT: `FOD-14` and `FOD-26` reworded to what the build does. Text acts under this group → CONFIRMED.
- ITEM 3 — THE REGISTER, his pick: **"Confirm as built"** — nav label "Obligations" and page title "Firm obligations" (DECISION 0), the count line "N open · M overdue", the Overdue pin hard-first-then-most-overdue (`FOM-9`), twelve month groups by target month and "Nothing falls due this month" (`FOM-14`, `FOM-3`), Later, Inactive collapsed (`FOM-5`), the retired badge "retired — stays until done" (`FOD-33`), the five strong-line states incl. the `FOD-1` note under `unknown` (`FOD-25`), "Needs attention" (not seen on screen — stays PROVISIONAL). Text acts in this group → CONFIRMED except "Needs attention" (unseen).
- ITEM 4a — ACTIVATION + EDIT FORMS, his pick: **"Confirm both, override label stays "due date""** — the rule-kind and weekend-rule labels, `FOD-30`'s typed rule date with the template date as a hint (part B item 8), `FOM-4`'s "last period completed" and the day-precision refusal (part C item 4), the edit form's "A missed period" — Stays owed / Collapses (DECISION 2), "The real due date for …" / "Set this date" — CONFIRMED; part C item 13 CLOSED: the override keeps "due date" wording. Text acts in this group → CONFIRMED.
- ITEM 4b — CLOSE FORM, RE-ACTIVATION, DOUBLE ACTIVATION, his pick: **"Confirm all three as built"** — the close form (date defaulting today, note, filed at) and `FOD-18`'s reasons, Not applicable on the eight conditional rows only — CONFIRMED; part C item 5's three re-activation readings RULED as built; part C item 11 CLOSED: double activation stays allowed, no warning, no block. Text acts in this group → CONFIRMED.
- ITEM 5 — THE CATALOG, his pick: **"Confirm as built"** — all 35 offered (DECISION 9B), "suggested", "Applies if:", "Can lapse per period" / "Can lapse for a period (offers Not applicable)", "Source:", "Lead … · weight …", "Nothing here is active until you activate it with your own date.", the BOI note row — CONFIRMED. No weekend default taken (DECISION 8's act; PF-1 there). Text acts in this group → CONFIRMED.
- ITEM 6(b) — THE OUTLOOK REMINDER (his own `FOM-8` question). Widget 1 answered outside the set: *"Let's talk this one through some more to tailor it. I do not want all of the events to carry a reminder."* Two levers put (WHICH events remind; WHEN). His answer, verbatim: **"Most deadlines in a case need to remind at most 30 days out, except for important deadlines that we need more time to think about, like expert designation deadlines."** — NOTE: stated about CASE deadlines (FC-7's matter-scoped track, the deadline engine, unbuilt); carried as a standing DIRECTION for that design, not a build item. Re-asked narrower for the firm module below.
- ITEM 6(b) RULED — his word on the composite: **"That looks correct."** The composite (Claude's translation, confirmed): (1) the calendar event on "MDBP Firm" for every occurrence, as built; (2) a reminder rings on HARD items only — routine items get the event, no reminder (`isReminderOn: false`); (3) the reminder fires 30 days before the target by default, or at the lead if the lead is shorter than 30 — no longer at the full lead; (4) a per-obligation "Outlook reminder days" field at activation and in Edit…, defaulting to 30, raised by hand on the long-think rows. Register/card lighting stays at the full lead. → FIX for the next Code session (domain + both adapters + forms + `toGraphFirmEvent`; a new column on `firm_obligations` — the migration is UNRUN, so it can take the column before it runs). DECISION 7 amended in this limb; `FOD-29`'s minutes rule now measured from the reminder day, not `lightsOn`. His case-deadline sentence carried as a DIRECTION for the FC-7 deadline engine.
- ITEM 6(a) — DONE → EVENT, his pick: **"Keep it, retitled, but kill the reminder"** — the event stays on "MDBP Firm" retitled "Done — …" (`FOD-22` kept); Done sets `isReminderOn: false` so an early Done never rings; Undo restores the reminder with the subject. FIX for Code (slice §3 item 7's literal `true`-on-done and the reverted lane deviation are superseded by this ruling). Part B item 10 CLOSED.
- ITEM 6(c) — STALE REMINDER / FALLBACK, his pick: **"Register and card are enough"** — no second-event fallback, no late-push adjustment; the `FOM-8` limitation (part B item 14) RECORDED AND CLOSED; `FO-2`'s surfaces are the register and the card. NO FIX.
- ITEM 6(d) — the empty "MDBP Firm" calendar in his Outlook: HIS HAND to keep or delete, not a ruling (put as information only).
- ITEM 7 — `FO-6`, his pick: **"Firm-wide calendar page, later"** — a month/agenda view of firm occurrences inside the app is WANTED, as a separate later slice, not a fix; nothing firm on a case's Calendar tab (`FOD-13` stands). `FO-6` STAYS ⬜ (🟡 ruled-in-shape), the answer annotated add-only. Not built; not authorized.
- ITEM 7 (leads) — widget 1: *"Explain this a bit more for me."* — explained (lead = register/card window; examples of shipped values). His pick: **"Stand as shipped"** — every template's drafted lead stands as a starting point; per-row adjustment at activation. `FOD-2`'s 30-day custom default stands. Spec §11 item 5 CLOSED.
- ITEM 8 — THE `FOD-` DEFAULTS. Screen-ruled under items 2–5 and 7: FOD-1, 2, 4, 5, 9, 12, 14, 15, 16, 17, 18, 21, 24, 25, 26, 27, 28, 30, 31, 32, 33 (as built / as accepted). Amended tonight by item 6: FOD-22 (Done keeps the event, reminder OFF), FOD-29 (minutes run from the reminder day; hard-only). List-only eleven — FOD-3, 6, 7, 8, 10, 11, 13, 19, 20, 23, 29-as-amended — his pick: **"All eleven stand"**. All thirty-three now RULED (none left PROVISIONAL as a default).
- ITEM 9, first group — his picks: **B1 "Add the column (Recommended)"** — `materialized_from` (+ a `touched` flag) on `firm_obligation_occurrences`, in the unrun migration and `db/schema.sql`; Undo's test reads the column. FIX. **B2 "Tighten it (Recommended)"** — `outcome is not distinct from 'not-applicable'`. FIX. **C6 "Build the RPC functions now"** — one Postgres function per act in the unrun migration, the Supabase adapter calls them; the failed-activation log question is subsumed (an RPC act is atomic — no half-state to log). FIX (larger; whether the fix list is authorized whole as a slice is put at the end of Part One).
- ITEM 9, second group — his picks: **C7 "Retry on next sync (Recommended)"** — the orphaned event id is kept and the sync drain deletes it when Outlook is next connected. FIX. **C8 "One act, one line (Recommended)"** — folds C14: one adapter act `activateFromInactive` (edit + re-activate), one review-log line, one RPC function. FIX; §7 item 16 stands. **C15 "Not needed once RPC lands (Recommended)"** — closed by consequence of C6. C14 CLOSED by C8. Part C item 12 (same-name calendar guard) — recorded as built, no ruling needed (a guard, not a behaviour).
- ITEM 10 — part C item 16: no ruling; the kickoff-prompt DO-NOT correction to §8's words rides the packet as a spec-edit line.
- PART ONE CLOSED. FIX ROUTING, his pick: **"Draft the fix slice tonight; put FOS-2 to me (Recommended)"** — Claude drafts `docs/specs/firm-obligations-fix-slice.md` + a kickoff prompt from the ledger, RR-1 + adversarial audit, `FOS-2` put whole before the zip closes. (Drafted after Part Two and §5, so it carries every later ruling.)

## PART TWO — THE POST-`CCS-1` WALK (opened ~23:25 CDT)
- P2 SCREEN 1 — SPLIT ADDRESS SURFACE, his pick: **"Confirm as built"** — two fields, "Split by rule — confirm or edit" + one-click Confirm (`SD-6`), legacy field hidden (`SD-5`), formatted phones, new-party locations group with no legacy box. CONFIRMED; the mark's wording CONFIRMED.
- P2 SCREEN 2 — MEDICAL/FORMS, his pick: **"Confirm all as built"** — `SD-7`, `SD-8`, `SD-10` behaviours RULED; text acts `SD-1` (three panel lines), `SD-16` (Generate label), `SD-17` (line 7), `SD-18` (R6's two flag lines) CONFIRMED; the rendered block (selected campus, formatted phone, the custodian literal, mid-level name+credential) CONFIRMED; `SD-9` suffix set CONFIRMED; `SD-2`, `SD-4`, `SD-13`, `SD-19` (behaviours the screen exercised) RULED.
- P2 SCREEN 3 — his picks: **`SD-3` "Instrument-wide stands"** (veto declined; one formatter, every phone token). **R8 strings "Confirm as built"** — the subtitle, the corrected unknown-token warning, `SD-15`'s note format CONFIRMED. Third-tranche item 9 (the 14-token registry stub): NOT widened — stays as recorded, his call later.
- P2 REMAINING TEXT ACTS — his pick: **"Confirm all but ones I haven't reached"**, then on which were reached: **none**. So `R1`'s route line + tier-3 panel line, `R10`'s pronoun line, `R15`'s title strings, `SD-12` README paragraph, `SD-11` sign-in sentence — ALL STAY PROVISIONAL (not reached). `SD-14`/`HS-4`: **"Didn't reach it — hold"** — PROVISIONAL, `HS-4` stays ⬜.
- P2 PROPOSED ITEMS (1/3) — his picks: **D-18 "Change the form"** (re-asked narrower below). **D-8 "Don't add the sentence"** — the block reads the selected facility only; no "Currently practicing at …" sentence; the mismatch panel line (§8.2 line 17) stays. RULED; FIX for Code (remove the sentence path; D-8 amended). **Per-paragraph regenerate "Yes, later slice"** — wanted, separately authorized; row stays open (🟡) with that answer.
- P2 **D-18 re-asked, his pick: "No episode sentence at all"** — §9.11's two approved sentences placed whole with `{facility_name}`, nothing else; the writer returns NOTHING for the custodian-only shape (the `care_episode_clause` part retired). RULED; FIX for Code (D-18 amended; the custodian-only shape's part contract). 
- P2 PROPOSED ITEMS (3/3) — his picks: **AS-Q14 "(b) Re-designate the facility whole"** — re-selecting an already-designated facility regenerates its whole paragraph in the supplement; the default becomes the rule; (a) not built. CLOSED. **AS-Q15 "Stand as default"** — no rider, off the block, panel line 14. CLOSED. **AS-Q16 "Stand as default"** — not rendered; panel line 15. CLOSED. **AS-Q17 "Designate under the treating paragraph"** — the option bundled two limbs (no pause on the marker; designated under the treating paragraph's causation sentence) — re-asked narrower on the pause limb below.
- P2 **AS-Q17 re-asked, his pick: "Pause still fires, then designate"** — the §5.1 hard pause fires on the marker (as built); once cleared, the individual IS designated in the treating paragraph under its causation sentence and its LEAD (NOT excluded, NOT hand-drafted). D-65's exclusion limb for marked individuals amended. RULED; FIX for Code. CLOSED.
- P2 THIRD TRANCHE (spec-feedback "THIRD TRANCHE — recorded by the `CCS-1` build session, 2026-09-07") — his picks: **item 8 "Fix — print it once"** — at N=0 the custodian line is suppressed when the top line already is it. FIX (served block; `FE-18`/§17.6 amended in this one point). **item 10 "Have Code tokenize the master"** — a Code session is AUTHORIZED to edit the master `.docx` at the two heading spots only, carrying `{instrument_title}` and `{footer_title}`; D-61/R15 completes. FIX (his document; edit scoped to those two spots). **item 11 "The Providers section's list (case_providers)"** — the new-bill form's picker reads `case_providers` facilities. FIX. Item 9 (token registry stub) stays recorded, not widened (ruled at screen 3). Item 12 is a walk note, no ruling.
- P2 **`CAP-2` EXEMPTION WORDING, his pick: "Edit rows carry — (Recommended)"** — batch 89's reading becomes the rule: the class is a BIRTH rule for new files; an edit-in-place row inherits the edited file's class and carries `—`. CONVENTION AMENDED → TRIGGER 3 FIRES; instructions v33 delivered this sitting. `CAP-2a`.
- PART TWO CLOSED.

## §5 ADJACENT (offered, not pressed)
- his picks: **"Keep both held (Recommended)"** — `Q-API-14` and `Q-API-3`'s filing half stay HELD (CC-1(b)). **"Not now — stay open"** — `Q-API-18`'s two facts and `Q-API-19` stay his. **"Not yet — stays open"** — `Q-STAT-7` not put. Wave 0 (b) (Outlook-defect captures) NOT offered — he did not sign in to Outlook this sitting (no throwaway events created; nothing to delete).

## `FOS-2` — THE FIX SLICE AUTHORIZATION
- Slice and prompt delivered as files BEFORE the question (`firm-obligations-fix-slice.md` 38,298 B; `PROMPT-firm-obligations-fix-slice-build-session.md` 13,700 B, both as delivered; final sizes in CHECKSUMS.txt). Two adversarial read-only lanes ran on the first draft (lane A fidelity-to-ledger: 2 HIGH / 7 MEDIUM / 7 LOW; lane B HEAD facts: 2 HIGH / 7 MEDIUM / 6 LOW); every HIGH and MEDIUM fixed; `FXD-9` (the reminder pre-fill vs. cap reading) put in the question itself.
- **`FOS-2` RULED YES, verbatim "Yes"** — both groups, as written, `FXD-1`–`FXD-11` as named, `FXD-9` included. (Clock: see line below.)
- FOS-2 ruled at 23:55 CDT on 2026-09-12 (Central).

## CLOSE-OUT (unprompted; packaging after midnight Central on the `#153`/`#155` precedent — the sitting stays stamped 2026-09-12)
- RR-1: the fix slice, the prompt, the rulings record, the audit record, v33, the manifest and this capture re-read against every later act (`FOS-2`'s YES; the fix routing; `CAP-2a`). One correction made in the re-read: the build slice's §12 append had listed the act-confirmation notices and refusal sentences as CONFIRMED though he cancelled the forms rather than firing the acts — moved to PROVISIONAL (the conservative reading, flagged).
- PF-1 did not fire (no legal characterization, no registry entry, no weekend default). Two discretionary adversarial lanes ran on the fix slice; record filed as EVIDENCE.
- Packet: batch 97, delivered by `device_commit_files` into `C:\Users\Brennan\brennan-case-manager\inbox\` (the VM shell could not mount the folder); size and sha256 stated from the container in the closing report. v33 also delivered to `Claude outputs\` on the checkout.
- Project knowledge: `claude_Firm_Obligations_Hands_On_Sitting_Capture_2026-09-12.md` (byte-identical to the EVIDENCE walkthrough capture, which embeds this ledger whole) and `claude_Handoff_Session_Log_2026-09-12.md`, both with TRANSIT lines.


## PART 4 — CROSS-CUTTING PATTERNS

- **CC-1(a) held twice where it mattered:** the reminder question came back outside the set ("I do not want all of the events to carry a reminder"; then his case-deadline sentence), and the composite was built from his words and put back for a yes; `AS-Q17`'s bundled option was split and the pause limb re-asked.
- **Explain-with-an-example unlocked two rulings** (item 1's bar-dues row; the leads) — his standing preference for rendered examples, applied to text.
- **An audit caught a reading before it bound:** `min(reminderDays, leadDays)` would have silently defeated the field he ruled the composite for; rewritten as a pre-fill and put inside the authorization.
- **The bridge VM shell did not mount, twice, and the sitting ran anyway** on staged copies — the 2026-09-10 note's second exhibit; `package.json` read this way caught the 5175-vs-5173 port fact before the command went out.

## PART 5 — OPEN ITEMS

| ID | Item | Status |
|---|---|---|
| v33 | Paste instructions v33 | His hand |
| `FOS-2` | Fire the fix build (fresh Opus Code session) | RULED YES; his hand |
| MIG | Run the firm-obligations migration AFTER the fix build amends it (ten checks) | His hand |
| ACT | His real activations | After MIG |
| `FO-6` | Firm-wide calendar page — a later slice | 🟡 |
| REGEN | Per-paragraph regenerate — a later slice; ID his to mint | 🟡 |
| `HS-4` / TXT | `SD-14` and the six PROVISIONAL `CCS-1` text acts | A later hands-on sitting |
| `Q-API-14`, `Q-API-3` | Held again | CC-1(b) |
| CAL | The empty "MDBP Firm" calendar | His hand |
| OUT | `Claude outputs\` (v33; the `#155` pre-audit copies) | His hand |
| T-7 | Monthly instructions review, due by month's end | Offered |
| TRANSIT | `#137` and `#155` pairs, eligible once batch 97 lands | A later sitting with his Chrome |

## PART 6 — WHAT MUST HAPPEN NEXT

1. The queue runs the `#156` packet on `mdb-pllc` (batch 97); Sync; paste v33.
2. `/usage`; a fresh Opus Code session; "run docs/prompts/PROMPT-firm-obligations-fix-slice-build-session.md".
3. The migration by his hand, after the fix build; then his real activations.
4. In parallel: the `H12-v` asks; the DECISION 9A acquisitions; the DECISION 8 drafting act.
5. A later sitting with his Chrome: the two TRANSIT-eligible pairs; the six `CCS-1` text acts and `SD-14`.
