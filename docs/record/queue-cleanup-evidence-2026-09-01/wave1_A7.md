# WAVE-1 — verifier A7 report — HANDS-ON candidates ACCEPTED at `#139`, R11's two rows, FO-7, Q-WF-4

Read-only. Files as staged from HEAD `7f02131` (2026-09-01). Line numbers below are STAGED-copy line numbers (the register `docs/specs/attorney-review-queue.md` = REG; the live log `docs/record/session-log.md` = LOG; the closed archive = ARCH; `docs/specs/BUILD-STATE.md` = BS). Nothing under `docs/` was modified.

## A7-0 — Slice-wide findings that every block below relies on (stated once)

**(a) The acceptance sentence, `#139`, LOG L335–340, heading `## 2026-08-31 (#139) — (Typed design session, Cowork, Fable 5 — the model per the` (verbatim; the file's soft line breaks joined with single spaces):**

> **THE CC-1 HANDS-ON QUEUE — "Accept all of them onto the queue" — TWENTY-ONE:** `#137`'s four (`DA-1`, `DA-3`, `DA-4`, `FO-6`); the 2026-08-24 audit's twelve (`CL2-AC-1`, `CL2-CHECK-1`, `FE-§11.4`, the bill-label pre-fill, `CR-7`, `CR-CONSTRAINT`, `Q-FE6-5`, `Q-FE4-1`, `Q-FE5-3`, `Q-IN2-7`, `Q-IN1-1`, `Q-IN3-6`); and tonight's five — the stop's shape; ND-7(c); Q5; Q10; the rider's supervisor beneath a collective paragraph — each carried with its full question at the REQ-CAPTURE's §18.F. CC-1(c) was discharged unprompted: the queue is worth an hour, needs the product in front of him, and is a SEPARATE sitting (screenshot-driven walkthroughs are Fable's per §7.2).

Whose words: the quoted fragment "Accept all of them onto the queue" is MICHAEL's (the entry's convention — quoted fragments after a bold label are his picks; `#139` L243–244 names the only three rulings that were selections among Claude-written descriptions, and this is not among them). The rest of the sentence is the entry's (Claude's) record. The same entry's header line (L235) reads *"five items HELD for hands-on by name and the CC-1 HANDS-ON QUEUE ACCEPTED WHOLE — twenty-one items, no longer empty"*, and L247 reads *"The CC-1 hands-on queue was FORMALLY EMPTY at open, sixteen proposed; it is TWENTY-ONE ACCEPTED at close."* An acceptance onto the CC-1 queue is a CC-1(c) act; it answers no question. Nothing in `#139` rules any of the twelve.

**(b) The twelve rows are textually UNCHANGED since the audits' HEAD `edea20b` (batch 82) — established from the three runner lines, each a Code statement that names its own check, and corroborated by line arithmetic:**
- Runner 83 (LOG L384): *"**every other line in the 636 KB file was verified byte-identical.**"* (only the `SK-v2` row and the header changed).
- Runner 84 (LOG L210): *"the row grew by exactly 697 bytes, the sentence plus one joining space, and not one other line of the 1,282-line file changed."* (only the `RECON-1` row and the header changed).
- Runner 85 (LOG L40): the `FE-D1A-1` row *"inserted CONTIGUOUSLY after the `Placeholder discipline` row"* (REG L215) plus the header sentence; *"ONE ROW ADDED — `FE-D1A-1` — AND NO ROW CLOSED"* (REG header).
- Arithmetic: the file is now 1,283 lines (was 1,282 at batch 84); rows ABOVE L215 keep the line numbers the audits cite (hardened §5.2 cites L86 → still L86; the audit's "lines 84–88" TRCP sign-offs → still L84–88), rows BELOW shift by exactly +1 (hardened §5.2's L1012 → L1013). The audit's §6 "Row" numbers are marked-row ORDINALS: 91/92/112/75/80 (above the insertion) match today's ordinals exactly; 379/382/396/400/408/419/486 (below it) are today's 380/383/397/401/409/420/487 — a uniform +1, consistent with one insertion and no other row movement.
- I could NOT byte-compare against the batch-82 copy (not staged) — the "unchanged" claim rests on those three runner statements plus the arithmetic; stated as such.

**(c) REQ-CAPTURE §18.F — UNVERIFIABLE-HERE.** `docs/specs/REQ-CAPTURE_disclosures-expert-designation_2026-08-20.md` is not staged. What the staged record says: `#139` L339 *"each carried with its full question at the REQ-CAPTURE's §18.F"*; BS L152 *"**each carried with its full question at the REQ-CAPTURE's §18.F; it needs the running product in front of you and is a SEPARATE sitting, worth about an hour**"*; `#140` L147–149 *"the accepted queue stays at twenty-one until he rules item by item at the hands-on sitting."* That §18.F actually holds the twelve rows' text — and whether it holds it VERBATIM or as a label — cannot be confirmed from the staged files.

**(d) ⛔ A REGISTER-HEADER DEFECT THAT TOUCHES ALL TWELVE (runner statement, not a ruling; mint/repair nothing).** The batch-84 reconcile sentence in the REG Status header (L3, the sentence beginning `Reconciled again to session-log #139 on 2026-08-31`) ends: *"**the CC-1 hands-on queue is TWENTY-ONE ACCEPTED** as of 2026-08-31 — `#137`'s four (`DA-1`, `DA-3`, `DA-4`, `FO-6`), the 2026-08-24 audit's twelve, and the sitting's own five (the hard stop's shape, ND-7(c), Q5, Q10, the rider's supervisor beneath a collective paragraph) — **none of them a row in this register, all of them Michael's**, and the sitting they need is a separate one with the running product in front of him.)"* **"None of them a row in this register" is FALSE for twelve of the twenty-one** — every one of the audit's twelve IS a row (L170, L171, L201, L1283, L127, L132, L964, L945, L953, L918, L924, L938; `grep -c -F` on each ID/head = 1 or more, positive). True of the nine others (`DA-1/3/4`, `FO-6`, the five held items — `grep -c -F "DA-3"` on the register body returns header-only hits). Consequence for the sheet: any annotation placed on the twelve rows will contradict the header sentence as it stands; a later add-only reconcile sentence naming the error is the house pattern (QR-6(b); the header cannot be edited in place). Reported, not repaired.

**(e) What the hardening did with this class:** NOTHING. The hardened file's §1 table hardens only the three closure classes (RULED-BUT-OPEN 27 / SUPERSEDED 21 / DUPLICATE 10 = 58); the HANDS-ON class (12) is not hardened, and none of the twelve IDs appears anywhere in the hardened file (flattened `grep` for each of the twelve = 0 against a firing control `FE-7` = 4). "Hardened verdict" for the twelve below therefore reads **not hardened (non-closure class)**.

**(f) Since the hardening, for all twelve:** the ONLY events are `#139`'s acceptance (LOG L335–340), runner 84's carry (LOG L228), runner 85's carry (LOG L52 — *"TWENTY-ONE accepted items plus the FOURTEEN this slice PROPOSES"*), `#140` L66 (*"the CC-1 hands-on queue at TWENTY-ONE accepted items, unchanged from `#139`"*), BS L152, and the REG header sentence in (d). No ruling on any limb of any of the twelve — every hit in LOG for each ID in the first ~600 lines is one of those carry lines (per-ID line lists in each block).

**(g) The standard annotation proposed for the twelve (substance; the sheet words it):** *"ANNOTATED add-only 2026-09-01, NOT CLOSED — ON THE CC-1 HANDS-ON QUEUE, ACCEPTED 2026-08-31 (#139, Michael: "Accept all of them onto the queue"). Held for the separate hands-on sitting per CC-1(b)/(c); full question text also carried at the REQ-CAPTURE's §18.F per #139 (not verified here). Ripeness per audit §6: <per row>."* Nothing in it mints, closes, or moves a glyph. CC-1(b): *"HANDS-ON QUESTIONS WAIT FOR THE PRODUCT … SAY SO AND HOLD IT rather than pressing for an answer"* — so none of the twelve closes here, whatever the sitting later decides.

---

### CL2-AC-1 (L170)

1. **Row read whole (1,653 chars, one physical line).** No sentence ends in "?"; the open ask is stated declaratively: *"**UNRULED, doc-on-screen required:** link-removal behavior (persist/orphan/prompt), mixed-posture cases, backfill vs. new-links-only. **Not closed until the edges are ruled.**"* Status on its face: *"**DIRECTION-CONFIRMED 2026-08-08** (was PROPOSED, unruled): auto-create on PI client-role link is the policy."*
2. **Every mention.** REG: 8 line-hits (L170 the row; L1279 the italic cross-reference *"consolidated 2026-08-08 into the CL2-AC-1 entry"*; the rest header/sweep prose). LOG: 3 (L228 runner-84 carry; L336 `#139` acceptance; L8878 `#65`'s consolidation-precedent mention). ARCH: 22 (ID issued `#34` L2784 *"**CL2-AC-1 ISSUED**"*; `#38` L2492 *"**CL2-AC-1 → DIRECTION-CONFIRMED** (Michael chose (b)…"*; `#39` L2385–2398 the dedup consolidation; runner 6 L70 the third-location conflict). BS: 2 (L152 the hands-on list; L154 *"**CL2-AC-1 is DIRECTION-CONFIRMED**"*). Collision report: `CL2-` clear.
3. **Decisive text.** `#139` L335–340 (A7-0(a)). Nothing later. Audit §6 row 91: *"Three unruled edges (link-removal behavior, mixed-posture cases, backfill) on the **built and walked** CL-2 dimension. He hit the gap himself in a live walkthrough."* — ripe NOW per audit ("The first four are ripe **now**").
4. **Whose words.** Acceptance = Michael ("Accept all of them onto the queue"). The direction-confirmation = Michael's option (b) at `#38`. The three edges: UNRULED — nobody's words answer them.
5. **Since the hardening:** A7-0(f) only; BS L154 restates direction-confirmed. Nothing rules an edge.
6. **DISCONFIRM.** Strongest case against ANNOTATE-KEEP: (i) "direction-confirmed" could be read as a ruling making the row closable — rejected by the row's own *"Not closed until the edges are ruled"* and by CC-1(b) (recommendation-led rulings stand, but this one was expressly withdrawn to direction-confirmed at Michael's choice). (ii) The header sentence says the twenty-one are "none of them a row" — a defect (A7-0(d)), not evidence of closure. (iii) Unique text: `impossible real-world state` 1 · `link-removal behavior (persist/orphan/prompt)` 1 · `backfill vs. new-links-only` 1 (control `Michael` 226) — the edges exist nowhere else in the register; anything but keep would destroy them. No later entry reopens or narrows; no deferral in his words beyond the hold itself.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Stays ⬜; annotation per A7-0(g), ripeness "ripe now (CL-2 built and walked; audit §6)". CC-1(b) holds it; nothing closes.
8. **Row first line (exact, occurs exactly once — `exact-line-count=1`, substring count 1):**
   - ⬜ **CL2-AC-1 — Auto-create the client record when a party is linked with the Client role on a PI case — a PI client with no damages-scope record is an impossible real-world state.** *(ID issued 2026-08-07 for the 2026-07-28 walkthrough capture, so the question cannot die the K-6/K-7 death.)* **Michael's own framing, from the CL-2 live walkthrough (moved here 2026-08-08, not shrunk):** linking a party with the Client role on a PI case and having no damages-scope record is an **impossible real-world state** — a PI client without damages isn't a client. Michael: don't make me build it by hand; assigning the Client role on PI case types should auto-create the damages-scope record, posture defaulting from case type, ready to populate. Data model stays parallel (D-CL2-8); **UI workflow only**. Brushes directly against deferred **CL2-CHECK-1** — "auto-create on PI" and "flag Client-role-without-record" are two answers to the same gap, and Michael fell into that gap himself on 2026-07-28. **DIRECTION-CONFIRMED 2026-08-08** (was PROPOSED, unruled): auto-create on PI client-role link is the policy. **UNRULED, doc-on-screen required:** link-removal behavior (persist/orphan/prompt), mixed-posture cases, backfill vs. new-links-only. **Not closed until the edges are ruled.** *(The morning's "yes" was withdrawn to direction-confirmed by Michael's own choice of option (b) after Claude flagged its own bad elicitation — `rulings-capture-2026-08-08.md` Part 5. **Consolidated here 2026-08-08** from the "Captured 2026-07-28" block per the CL2-AC-1 dedup ruling — one register, one home per question; see `rulings-capture-2026-08-08b.md` Part 2.)*

### CL2-CHECK-1 (L171)

1. **Row read whole.** Interrogative: *"Adopt later or drop?"* Status on its face: *"**DEFERRED 2026-07-28 — do not build.** … **RE-AFFIRMED 2026-08-13 (#66): KEEP PARKED** — the deferred-do-not-build posture stands; revisit after CD-1's flags prove out."*
2. **Every mention.** REG: 3 (L171 the row; L170's cross-reference *"Brushes directly against deferred **CL2-CHECK-1**"*; header). LOG: 3 (L228; L336; L9091 `#66` *"- CL2-CHECK-1 RULED: KEEP PARKED (deferred-do-not-build stands; revisit after CD-1's flags prove out)."*). ARCH: 4 (L3157 the CL2-AC-1 brush; L3206 *"**CL2-CHECK-1 still explicitly deferred**"*; L3359 table *"DEFERRED 2026-07-28"*; L3399 *"Fable's proposed"*). BS: 1 (L152).
3. **Decisive text.** `#139` acceptance (A7-0(a)); `#66` LOG L9091 (heading `## 2026-08-13 (#66) — RULING RUN: ~20 open queue items ruled one by one`). Audit §6 row 92: *"Deferred at `#66` with the revisit trigger being product experience — *"revisit after CD-1's flags prove out."* CD-1 is built."* Audit §1 finding 2: *"CD-1 built `#61` and completed `#113`"*.
4. **Whose words.** "KEEP PARKED" is recorded as RULED at `#66` — the log's phrasing, not marked as a verbatim quotation of his. Acceptance = Michael's word at `#139`.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** This row is ALSO a MICHAEL-IN-WORDS hold ("KEEP PARKED", `#66`) — the strongest alternative disposition. Two readings: (i) the hands-on acceptance is exactly the *"revisit after CD-1's flags prove out"* trigger the row names, so ANNOTATE-KEEP is consistent with the hold; (ii) someone could read "accepted onto the hands-on queue" as un-parking it — it does not; the parked posture stands until he rules at the sitting. Unique text: `advisory client-role` 1 · `revisit after CD-1's flags prove out` 1 · `Adopt later or drop?` 1 — the question lives only here (the ARCH table L3359 carries an abbreviated copy, closed file).
7. **PROPOSED: ANNOTATE-KEEP — HIGH** (with the annotation stating expressly that `#66`'s KEEP PARKED posture is unchanged and the sitting is the row's own revisit trigger). Ripeness: ripe now (audit §6). Alternative label if the sheet prefers: MICHAEL-IN-WORDS — same action, same glyph.
8. **Row first line (exact, once):**
   - ⬜ **CL2-CHECK-1** — advisory client-role ↔ client-record consistency check (flag/review-log, never auto-fix). **DEFERRED 2026-07-28 — do not build.** Adopt later or drop? **RE-AFFIRMED 2026-08-13 (#66): KEEP PARKED** — the deferred-do-not-build posture stands; revisit after CD-1's flags prove out.

### FE-§11.4 (L201)

1. **Row read whole.** No "?" — a banked item: *"**FE-§11.4 — In-app template editor UX pass.** FE-D1 builds a **MINIMAL** editor (edit → new `template_version`, no styling UI) because §1's Michael-owns-the-wording principle requires *some* editor; **the UX pass itself stays open as a later item.**"*
2. **Every mention.** REG: 1 (the row). LOG: 2 (L228; L336) — plus the batch-filing confirmation at `#65` L9213 *"FE-§11.1/.2/.4"* (rows confirmed in place). ARCH: 0 for the literal `FE-§11.4` (flattened also 0; control `CR-1–CR-11` L231 fires; the §63 ruling is recorded at ARCH L286 *"the MINIMAL TEMPLATE EDITOR ruled IN"*). BS: 1 (L152). Collision report: `§11.4` 0.
3. **Decisive text.** `#139` acceptance. Audit §6 row 112: *"The minimal template editor **exists** (fixture mode); the UX pass is exactly this shape."* — ripe now.
4. **Whose words.** Acceptance = Michael's. The minimal editor IN = `#63` group ruling (his). The UX pass = open, nobody's ruling.
5. **Since the hardening:** A7-0(f) only. BS says nothing about a template editor (`template editor` 0 in BS) — whether the fixture-mode editor exists at HEAD is a BUILD-STATE/src fact the audit asserted; UNVERIFIABLE-HERE beyond the audit's sentence.
6. **DISCONFIRM.** Could this be RECLASSIFY-ACT (a UX pass is work, not a ruling)? It is a banked design item awaiting a scoping ruling with the product in front of him — the audit's hands-on reading is the better fit and Michael accepted it as such. Unique text: `In-app template editor UX pass` 1 · `the UX pass itself stays open as a later item` 1 · `no styling UI` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: ripe now.
8. **Row first line (exact, once):**
   - ⬜ **FE-§11.4 — In-app template editor UX pass.** FE-D1 builds a **MINIMAL** editor (edit → new `template_version`, no styling UI) because §1's Michael-owns-the-wording principle requires *some* editor; **the UX pass itself stays open as a later item.** *(Registered 2026-08-12 (#63), same commissioned check — no row existed.)*

### Bill label default row (L1283) — no ID

1. **Row read whole.** No "?" at all. The ask is the capture itself: *"**Bill label should default to the provider name**, editable only to disambiguate ("ProCare — initial eval" vs "— follow-up"). **Code answered the blocking question 2026-07-28: the label is NOT load-bearing** — display-only in all five uses, and the report generator already prefers the provider (`providerName ?? bill.label`). Safe to pre-fill; not built, as it sits outside CL-2's six pieces."* Its section header (L1277): *"**PROPOSED and unruled — captured, not built.** IDs are issued by a design session, not here;"* (soft-wrapped log quotes above and below are joined with single spaces where the file breaks a line)
2. **Every mention.** REG: 1 (`Bill label` — the row). LOG: `Bill label` 0; the item is carried as *"the bill-label pre-fill"* (L228, L336) — flattened `bill-label` 2, both carries. ARCH: 1 (L3166 *"3. **Bill label should default to the provider name.** Code answered the blocking question:"* — the 2026-07-28 capture). BS: 1 (L152 *"the bill-label pre-fill"*). Audit §6 row 486 (ordinal; now 487): *"The medical spine is built and walked; confirm the pre-fill at the bill screen."*
3. **Decisive text.** `#139` acceptance. No ruling anywhere adopts or rejects the default.
4. **Whose words.** The capture is Michael's 2026-07-28 walkthrough ask (recorded); "Code answered the blocking question" is a Code statement (not load-bearing = a code fact); acceptance = Michael's word.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** (i) Could this be SUPERSEDED because Code found the label not load-bearing? No — that finding removed a BLOCKER; the default itself remains unadopted and unbuilt. (ii) Could it be ANSWERED by any medical-tab ruling since? None found (BS `bill.label|bill label|bill-label` = 1, the carry only). (iii) It has no ID — the annotation must not mint one (IDs are issued by a design session per the section header; ID-DL-1 pattern). Unique text: `Bill label should default to the provider name` 1 · `providerName ?? bill.label` 1 · `ProCare` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: ripe now (medical spine built and walked). Name it in the annotation as `#139` names it ("the bill-label pre-fill") — no ID minted.
8. **Row first line (exact, once — it is the register's LAST line):**
   - ⬜ **Bill label should default to the provider name**, editable only to disambiguate ("ProCare — initial eval" vs "— follow-up"). **Code answered the blocking question 2026-07-28: the label is NOT load-bearing** — display-only in all five uses, and the report generator already prefers the provider (`providerName ?? bill.label`). Safe to pre-fill; not built, as it sits outside CL-2's six pieces.

### CR-7 (L127)

1. **Row read whole.** Requirement + riding question: *"**Question riding here (§5 Q5): manual entry at court, OCR-assisted import of the DA charge table, or both — and can the mapping print as a front-page column without crowding the handwriting space?** Priority: now."*
2. **Every mention.** REG: 2 (L127 the row; L119's series note mentions the `CR-3` collision only — `CR-7` itself: the row plus header). LOG: 2 (L228; L337). ARCH: literal `CR-7` 0 (flattened 0) — the series was filed as a range: ARCH L231 *"**CR-1–CR-11 plus the binding black-and-white print constraint**, each with full text per QR-1"* (control fires). BS: 1 (L152). Collision report: `CR` series — only `CR-3` collides (the TxDOT crash-report form); `CR-7` clear.
3. **Decisive text.** `#139` acceptance. Audit §6 row 75: *"The layout half (does a three-way mapping print without crowding handwriting space?) is only answerable on a rendered worksheet."*
4. **Whose words.** The requirement = Michael's Uvalde REQ-CAPTURE, ID ruled `#62`; the riding question = the capture's §5 Q5 (unruled); acceptance = his word at `#139`.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** The hands-on acceptance covers the RIDING QUESTION (layout/print half; the audit named only that half). The requirement half (three-way mapping, priority now; the manual-vs-OCR limb of Q5) is not a hands-on question and is not answered — the annotation must be scoped so the row is not read as "whole item parked on the sitting". Unique text: `Three-way number mapping` 1 · `DA's charge-table` 1 · `without crowding the handwriting space` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH**, annotation scoped: "the §5 Q5 riding question (print/layout half) is on the CC-1 hands-on queue, accepted #139; the requirement itself and the manual-vs-OCR limb are unchanged." Ripeness: not ripe (no worksheet renderer built — BS has nothing for the criminal worksheet; audit: "The rest wait on slices that do not exist").
8. **Row first line (exact, once):**
   - ⬜ **CR-7 — Three-way number mapping: defense cause number ↔ docket entry number ↔ DA file/incident number.** Stored per cause, printed on the worksheet, optionally imported from the DA's charge-table document. Live evidence: the DA's own charge-table printout hand-annotated — indicted rows circled, numbered, and mapped to docket entries — because the worksheet carries only cause and docket numbers while the DA's office talks in its own file numbers. **Question riding here (§5 Q5): manual entry at court, OCR-assisted import of the DA charge table, or both — and can the mapping print as a front-page column without crowding the handwriting space?** Priority: now.

### CR-CONSTRAINT (L132)

1. **Row read whole.** A binding design constraint with a riding question: *"**Question riding here (§5 Q4): which black-and-white-safe typographic conventions should carry banner and flag semantics?** FE-10's format profiles are the civil-side sibling — **cross-referenced, never merged.**"*
2. **Every mention.** REG: 1 (the row). LOG: 3 (L228; L337; L9212 `#65` *"the B&W CR-CONSTRAINT"* confirmed in place). ARCH: literal 0; the range sentence at L231 carries it as *"the binding black-and-white print constraint"*. BS: 1 (L152). Collision report: 0.
3. **Decisive text.** `#139` acceptance. Audit §6 row 80: *"Which B&W-safe typographic conventions carry banner semantics — judged on printed output."*
4. **Whose words.** The constraint itself is Michael's (prints B&W only — the capture); the riding question is §5 Q4, unruled; acceptance = his word.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** Same shape as CR-7: the CONSTRAINT is binding law already ("This governs ALL worksheet rendering") and is not a question; only the riding Q4 is a hands-on question. An annotation that reads as parking the constraint would be wrong. Unique text: `survive BLACK-AND-WHITE printing` 1 · `never color-coded meaning` 1 · `black-and-white-safe typographic conventions` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH**, scoped to the §5 Q4 riding question; the constraint is unchanged and in force. Ripeness: not ripe (printed worksheet output does not exist).
8. **Row first line (exact, once):**
   - ⬜ **CR-CONSTRAINT (no ID — BINDING DESIGN CONSTRAINT, not a feature): all worksheet semantics must survive BLACK-AND-WHITE printing.** Borders, shading density, weight, and icons — **never color-coded meaning.** Michael prints in black and white only; the highlighting observed on one worksheet was a one-time conversation aid and was expressly **not** a feature request. **This governs ALL worksheet rendering, including every CR item above.** **Question riding here (§5 Q4): which black-and-white-safe typographic conventions should carry banner and flag semantics?** FE-10's format profiles are the civil-side sibling — **cross-referenced, never merged.**

### Q-FE6-5 (L964)

1. **Row read whole.** *"**Does the packaging mode belong to the case, the wave, or the individual render?**"* (plus the practical test: *"whether a later supplemental instrument in the same case may be served in a different mode than the original, and whether the engine should let that happen silently."*)
2. **Every mention.** REG: 1 (the row; the Task-8 note at L943 governs the series: *"deliberately NOT minted … routes the series decision to **`ID-DL-1`**"*). LOG: 2 (L228; L337). ARCH: 0 (series minted 2026-08-15 at `#81`, after the archive cutoff; control `Q-WF` 0 in ARCH for the same reason; `D-CL2-3` = 15 fires). BS: 1 (L152).
3. **Decisive text.** `#139` acceptance. Audit §6 row 419: *"Packaging mode as case/wave/per-render is a how-the-firm-serves question; the FE-D1 render path is now a substrate to look at."* Audit §6 paragraph: *"The first four are ripe **now** … The rest wait on slices that do not exist"* — this row is in "the rest" (the slice file's "(audit: not yet ripe)" reads the paragraph, not the table row).
4. **Whose words.** Question = the FE-6 spec's §6 (Claude, PROPOSED); acceptance = Michael's word. No ruling.
5. **Since the hardening:** A7-0(f) only. `#140`'s amendment slice concerns disclosures paragraphs, not instrument packaging — no hit for `Q-FE6-5` or `packaging mode` in `#140` (checked: `packaging` 0 in LOG L54–199).
6. **DISCONFIRM.** Could `form-engine.md` §13.3's ruled words *"packaging is a render-time choice"* (2 hits in REG) already answer it? The row itself cites them as ONE of three readings and asks which — the ruling's words do not settle case vs wave vs render. Unique: `Does the packaging mode belong to the case, the wave, or the individual render?` 1 · `supplemental instrument in the same case may be served in a different mode` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: NOT ripe (discovery slice / separate-mode render unauthorized) — annotation says so; ID stays packet-local (ID-DL-1).
8. **Row first line (exact, once):**
   - ⬜ **Q-FE6-5.** **Does the packaging mode belong to the case, the wave, or the individual render?** A case-level setting matches how a firm actually serves; a per-render choice matches §13.3's words (*"packaging is a render-time choice"*); a per-wave setting sits between. The practical test is whether a later supplemental instrument in the same case may be served in a different mode than the original, and whether the engine should let that happen silently.

### Q-FE4-1 (L945)

1. **Row read whole.** *"**What makes an edit *material*?** … **Does the engine classify materiality itself, present the choice to the attorney at save time, or treat every edit as material and accept version inflation as the cost of never guessing wrong?**"*
2. **Every mention.** REG: 2 (L945 the row; L943 series note). LOG: 3 (L228; L337; L7595 the batch-36 filing *"merges (Q-FE4-1..6, Q-FE5-1..9, Q-FE6-1..7, full text per QR-1)"*). ARCH: 0 (post-cutoff series; control as above). BS: 1 (L152).
3. **Decisive text.** `#139` acceptance. Audit §6 row 400: *"What makes a definitions-set edit "material" — a save-time UX call."*
4. **Whose words.** Question = FE-4 spec §6 (Claude, PROPOSED); acceptance = Michael's word. No ruling.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** `form-engine.md` §13.1's ruled sentence "a material edit creates a new version" is the PREMISE of the question, not its answer. Unique: `What makes an edit *material*?` 1 · `version inflation` 1 · `substantive narrowing of the ESI definition` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: NOT ripe (no definitions-set editor exists; FE-4's home unruled — Q-FE4-2).
8. **Row first line (exact, once):**
   - ⬜ **Q-FE4-1.** §13.1 says "a material edit creates a new version." **What makes an edit *material*?** A typographical correction to a standard definition, a substantive narrowing of the ESI definition, and the addition of a newly-identified vehicle are three different kinds of change, and only the last two obviously deserve a version. **Does the engine classify materiality itself, present the choice to the attorney at save time, or treat every edit as material and accept version inflation as the cost of never guessing wrong?**

### Q-FE5-3 (L953)

1. **Row read whole.** *"**Does that change the escalation threshold, argue for a ratio-based or absolute-margin condition instead of bare crossing, or does it confirm the ruled design**"* (the Swepi 19-vs-98 datum against §13.2's rule; the row notes *"The spec records this as evidence about firing frequency, expressly NOT as an argument that the ruling is wrong."*)
2. **Every mention.** REG: 2 (L953 the row; L967 FE5-LOOK-3 *"if Q-FE5-3 turns on how courts actually apply the test"*). LOG: 3 (L228; L337; L7542 `#81` *"**Q-FE5-3, Michael's.**"*). ARCH: 0. BS: 1 (L152).
3. **Decisive text.** `#139` acceptance; `#81` L7542 (heading `## 2026-08-15 (#81) — CHAT-DISPATCH TASK 8`). Audit §6 row 408: *"The Swepi 98-vs-19 datum against the ruled escalation threshold; the trade is **warning fatigue in use**."*
4. **Whose words.** Question = FE-5 spec §7; "Michael's" = the entry's attribution of ownership, not a ruling; acceptance = his word.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** The escalation rule is RULED (§13.2, 2026-08-11) and CC-1(b) says recommendation-led rulings stand — so is the row already answered by "the ruled design stands"? No: the row asks whether the datum CHANGES the threshold, which only he can say; the spec's own caveat keeps the ruling intact meanwhile. FE5-LOOK-2 (a citator on *Swepi*) is a separate Michael look, shared subject — not a duplicate. Unique: `facial subpart count of 98` 1 · `ratio-based or absolute-margin` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: NOT ripe (no interrogatory budget built — BS L83: *"no discovery-level field on `cases`" (FE-5's hard prerequisite)*).
8. **Row first line (exact, once):**
   - ⬜ **Q-FE5-3.** *In re Swepi L.P.* is the only empirical datum on record about facial-vs-legal subpart counts, and it comes from the Fourth Court of Appeals: **19 numbered interrogatories, a facial subpart count of 98 asserted by the objecting party, and a holding that none of the nineteen had multiple discrete subparts.** Under §13.2's escalation rule (worst-case 98 > cap 25 while numbered 19 ≤ 25) **the engine would have warned**, and on the holding the warning would have been wrong. **Does that change the escalation threshold, argue for a ratio-based or absolute-margin condition instead of bare crossing, or does it confirm the ruled design** — on the reasoning that a warning which prompts one attorney look is cheap next to a recharacterization fight, and that the passive display was correct the whole time? The recorded warning-fatigue risk is the thing being traded. *(The spec records this as evidence about firing frequency, expressly NOT as an argument that the ruling is wrong.)*

### Q-IN2-7 (L918)

1. **Row read whole.** *"**Should the review surface additionally mark high-confidence identifiers that no independent check has corroborated, so "confident" and "corroborated" are not displayed as the same thing — without disturbing the ruled queue order?**"*
2. **Every mention.** REG: 1 (the row; series note L910 — packet-local, `IN-` minting is Michael's). LOG: 2 (L228; L337). ARCH: 0. BS: 1 (L152).
3. **Decisive text.** `#139` acceptance. Audit §6 row 379: *"Distinguishing "confident" from "corroborated" on the review surface. *(Far from ripe — GPU-gated.)*"*
4. **Whose words.** Question = IN-2 spec §10 (Claude, PROPOSED); acceptance = Michael's word; the "ruled queue order" (§7.1 low-confidence-first) is the premise, ruled.
5. **Since the hardening:** A7-0(f) only. IN-2's extraction half remains GPU-arm gated (BS L83 IN-2 facts unchanged; T3 unauthorized).
6. **DISCONFIRM.** The audit itself said this one *"should be marked "hands-on, not yet ripe" rather than queued"* — Michael accepted ALL twelve regardless ("Accept all of them onto the queue"), so queue membership is his word and ripeness is a fact to carry, not a reason to un-accept. Unique: `one transposed character` 1 · `"confident" and "corroborated"` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: far from ripe (GPU-gated; T3 UNAUTHORIZED/KICK-1) — say so in the annotation.
8. **Row first line (exact, once):**
   - ⬜ **Q-IN2-7.** §7.1 orders the review queue low-confidence-first, which is ruled. But class-A coded values fail loudly (membership test) while class-B identifiers fail silently — a VIN with one transposed character passes every check the system has and flows into definitions, requests, and subpoenas looking correct (spec §7.4). **Should the review surface additionally mark high-confidence identifiers that no independent check has corroborated, so "confident" and "corroborated" are not displayed as the same thing — without disturbing the ruled queue order?**

### Q-IN1-1 (L924)

1. **Row read whole.** *"Which, and is answer ingestion its own step or a mode of a general document-intake step that does not exist yet?"* — preceded by the quoted queue question *"Is answer ingestion a distinct pipeline step, and where in the UI do suggestions surface?"* and three surfaces (a) /inbox pattern, (b) case-detail tab/card, (c) inside the drafting flow.
2. **Every mention.** REG: 2 (L924 the row; the IN-1 row L250 carries the original question sentence — `Is answer ingestion a distinct pipeline step` = 2 hits, L250 and L924; that is the row QUOTING IN-1, a shared question stated in two places, not a duplicate row: IN-1 is the requirement, Q-IN1-1 the spec's surface question). LOG: 2 (L228; L337). ARCH: 0. BS: 1 (L152).
3. **Decisive text.** `#139` acceptance. Audit §6 row 382: *"Where answer-mining suggestions surface: inbox / case-detail card / inside drafting."*
4. **Whose words.** Question = IN-1 spec §7 (Claude, PROPOSED) quoting the queue's own (Michael-facing) question; acceptance = his word.
5. **Since the hardening:** A7-0(f) only.
6. **DISCONFIRM.** Because the first sentence also lives in the IN-1 row (L250), someone could call this a duplicate and merge — it is not: the three-surface analysis and the "own step vs mode of a general intake step" limb exist ONLY here (`confirm-to-file` 1 · `general document-intake step that does not exist yet` 1). Shared question, distinct row.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: not ripe (IN-1 unauthorized; its spec PROPOSED).
8. **Row first line (exact, once):**
   - ⬜ **Q-IN1-1.** The queue's own question, unanswered: *"Is answer ingestion a distinct pipeline step, and where in the UI do suggestions surface?"* Three surfaces exist to build on and they are not equivalent. (a) The **/inbox pattern** — staging items with ranked suggestions and confirm-to-file, which already has the suggest/dismiss/tuning-log machinery but is transcript-shaped and case-agnostic at entry. (b) A **case-detail tab or card**, alongside the roster-flag card's Mark-handled affordance, which is case-scoped by construction but adds a surface. (c) **Inside the drafting flow**, where hooks appear only when a set is being built, which guarantees they are seen in context but means an answer filed today surfaces nothing until someone drafts. Which, and is answer ingestion its own step or a mode of a general document-intake step that does not exist yet?

### Q-IN3-6 (L938)

1. **Row read whole.** *"**What happens to a set held for a defendant who never appears?** … **Should a hold carry a review date, a staleness prompt, or an expiry?**"*
2. **Every mention.** REG: 1 (the row; series note L922). LOG: 2 (L228; L337). ARCH: 0. BS: 1 (L152).
3. **Decisive text.** `#139` acceptance. Audit §6 row 396: *"Hold review date vs staleness prompt vs expiry — prompt shapes. *(Not ripe; discovery slice unauthorized.)*"*
4. **Whose words.** Question = IN-3 spec §8 (Claude, PROPOSED); acceptance = Michael's word.
5. **Since the hardening:** A7-0(f) only. `#140`'s slice concerns disclosures; no held-set/IN-3 ruling in it (`Q-IN3` 0 in LOG L54–199).
6. **DISCONFIRM.** Q-IN3-7/-8 (freeze vs editable; fresh render on release) share the HOLD subject — shared blocker (discovery slice), not duplicates; each asks a different limb. Unique: `a defendant who never appears` 1 · `review date, a staleness prompt, or an expiry` 1 · `served by publication months ago` 1.
7. **PROPOSED: ANNOTATE-KEEP — HIGH.** Ripeness: not ripe (discovery slice unauthorized).
8. **Row first line (exact, once):**
   - ⬜ **Q-IN3-6.** **What happens to a set held for a defendant who never appears?** Nothing on the record answers this and the failure is silent by construction: a finished instrument sits in a state nobody is prompted about, aimed at a party who may have been non-suited, defaulted, or served by publication months ago. **Should a hold carry a review date, a staleness prompt, or an expiry?** The record already has the adjacent failure class on file — a hand-filled certificate date going *"silently stale"* across five documents (IN-4's origin) — and this is the same class one level up.

---

### TRCP 195.2(a) and (b) (L86) — R11's unblock, row 1 of 2

1. **Row read whole (155 chars).** *"**TRCP 195.2(a) and (b)** — the most expensive thing on this list to miss. **Are the 90-day and 60-day offsets from the discovery-period end correct?**"* Section: `## 3 — TRCP DEADLINE SKELETON (none touched today)` — five sign-off rows from 2026-07-26.
2. **Every mention.** REG `195.2`: 2 line-hits (L86; L1013) + header (flattened 3). LOG `195.2`: 25 line-hits, every one since `#127` (2026-08-20) carrying *"no TRCP 195.2 deadline computed, displayed or proposed"* or *"the TRCP 195.2 verification (R11's gate…)"*; decisive ones below. ARCH: 1 (the original 2026-07-26 queue compile). BS: 2 (L2 — `form-engine.md` *"§2 step 7 states the TRCP 195.2 rule UNVERIFIED with NO date computed"*; L125 — *"**Rule 195.2's 30-day floor is GONE**"* stated flatly as the deadline memo's finding). Audit (2026-08-24): `195.2` 0, `R11` 1 (§8 defect 2 — no-row list); the row is in §7's *"Registry / verification work (≈45 rows) … the TRCP sign-offs at lines 84–88"* = LIVE. Hardened §5.2: *"**`R11` itself has no row. Its unblock has two, both classified LIVE:** … **Line 86** … **Closing those two rows discharges the item that `#135` and `#137` both name as the cheapest move available in the whole disclosures thread.**"* Collision report L329/L381: `R11` = the live disclosures requirement, *"header-only"*, no queue row.
3. **Decisive sentences, character-exact.**
   - `#127` LOG L1673 (heading `## 2026-08-20 (#127) — THE DISCLOSURES REQ-CAPTURE RECONCILED`): *"**`R11` IS GATED ON MICHAEL, AND IT IS THE CHEAPEST UNBLOCK IN THE CAPTURE.** The FE-D1 build computed no designation deadline because CLAUDE.md's registry rule bars an UNVERIFIED proposition from driving a computed legal outcome, and **TRCP 195.2 has no registry entry** — only a playbook row carrying an unresolved 90/60-vs-60/90 conflict flag. *"A proposal for confirmation is still a computed date."* **Michael's verification is the gate.**"*
   - `#138` LOG L520–521: *"**`R11`'s unblock — "the cheapest unblock in the disclosures thread" — ALREADY HAS TWO REGISTER ROWS**, both classified LIVE, and nobody connected them."*
   - `#139` LOG L331–332 (the HD-10 ruling): *"**`HD-10` "Close it by pointing at them"** — the two register rows ARE the verification act; no new item; **R11 stays GATED and nothing was computed, displayed or proposed**"*
   - `#139` LOG L367–368 (Still open): *"the TRCP 195.2 verification (R11's gate; its two register rows)"*; runner 84 L228 the same words; runner 85 L52 *"**R11 (the TRCP 195.2 verification)**"*; `#140` L194 *"the TRCP 195.2 verification (R11's gate)"*.
   - `#140` L103–104 (AS-Q12): *"§2 step 7 amended (rule stated, UNVERIFIED, no date computed — R11 gated)"*.
4. **Whose words.** "Close it by pointing at them" = MICHAEL (quoted pick at `#139`). What it closes: `HD-10` — the disclosures-side item (its text is in REQ-CAPTURE §18, NOT staged; the collision report says numerals 4–10 of the `HD` series *"have no disclosures text in the repo at all"* — so HD-10's own question is UNVERIFIABLE-HERE; its subject is established only by `#139`'s sentence: whether R11's verification needs a new item). The gloss *"the two register rows ARE the verification act; no new item"* is the entry's record of what his pick means. The verification itself is an ACT reserved to him by registry discipline ("ONLY Michael verifies") and by `#127` ("Michael's verification is the gate"); no session can perform it.
5. **Since the hardening:** `#139` HD-10 CONNECTED the two rows to R11 in his words (what §5.2 said nobody had done); `#140`/batch 85 restated the 195.2 rule UNVERIFIED in `form-engine.md` §2 step 7 with no date computed; every carry line still names the verification as open. No verification happened; no registry entry for 195.2 exists on the staged record (`#127`; nothing later creates one — `195.2` hits in LOG L1–600 are all "not computed"/"verification" carries).
6. **DISCONFIRM.** (i) The hardened §5.2 says *"Closing those two rows discharges…"* — read carelessly, that invites CLOSE. It does not: the rows CLOSE ONLY BY HIS VERIFICATION; `#139` expressly kept *"R11 stays GATED"* and pointed at the rows as the PLACE of the act, not as done. (ii) Could the row be SUPERSEDED by [DL-memo Q4] (the floor is gone; 90/60 fixed dates)? No — L86 asks whether the 90/60 offsets are correct (the July 2026 text's fixed-date formulation), L1013 asks whether the 2021 floor is gone; both must be verified and the `#127` flag names an *"unresolved 90/60-vs-60/90 conflict"* only L86 resolves. Shared blocker (his verification), two limbs, not a duplicate. (iii) The row carries no cite to a registry entry, and `#127` says none exists — verification here attaches to a QUEUE row rather than a registry entry; whether Michael's verification should instead create/verify a registry entry is a sequencing point the sheet may want to name (ROUTE-C's "verification attaches to WORDING"). Unique text: `the most expensive thing on this list to miss` 1 · `Are the 90-day and 60-day offsets from the discovery-period end correct?` 1.
7. **PROPOSED: RECLASSIFY-ACT — HIGH** (Michael's VERIFICATION act; not a ruling). Stays ⬜ with an add-only annotation: "R11's gate — the disclosures designation-deadline computation is barred until this verification (#127); ruled at #139 HD-10, his words "Close it by pointing at them": this row and [DL-memo Q4] ARE the verification act; no new item; nothing computed until verified." Destroys nothing.
8. **Row first line (exact, once):**
   - ⬜ **TRCP 195.2(a) and (b)** — the most expensive thing on this list to miss. **Are the 90-day and 60-day offsets from the discovery-period end correct?**

### [DL-memo Q4] (L1013) — R11's unblock, row 2 of 2

1. **Row read whole.** *"**Do you verify that the floor is gone? A designation function built on the 2021 formulation would compute a LATER date than the current rule allows.**"* (premise: *"Rule 195.2's "later of … 30 days after the request is served" floor is GONE from the July 2026 text … 90 days (party seeking affirmative relief) and 60 days (all others) before the end of the discovery period."*; *"**Cross-links the TRCP 195.2(a)/(b) sign-off row already on this queue.**"*)
2. **Every mention.** REG `DL-memo Q4`: 1 (the row). LOG: 0 literal (flattened 0; control `DL-memo` in BS L125 fires: *"all five ARE filed as `[DL-memo Q1]`–`[DL-memo Q5]` and all five still ⬜"*); the SUBJECT is carried as the 195.2 verification (see L86 block). ARCH: 0 (entered 2026-08-14 at `#75`, after the archive cutoff). BS: L125 *"**The skeleton was NOT edited; resolving it is your verification act.** Also open: … **Rule 195.2's 30-day floor is GONE**"* — BS states the memo's finding flatly; that is a carried runner statement of a PROPOSED finding, not a verification. Audit: `DL-memo Q4` 0; the family is in §7 *"`[DL-memo Q1]`…`[Q5]`"* = LIVE. Hardened §5.2 names it as row 2 of R11's unblock (cited as L1012, now L1013).
3. **Decisive sentences.** Same set as L86 (items 3 there): `#127` L1673; `#138` L520–521; `#139` L331–332 HD-10; the carries. ID-DL-1 (REG L1009) governs the `[DL-memo]` labels — packet-local, minting is his.
4. **Whose words.** The question is the deadline memo's (`#75`, Claude, PROPOSED); "Do you verify" addresses Michael expressly; `#139`'s pick is his; BS L125's "your verification act" is the runner's characterization, consistent.
5. **Since the hardening:** as for L86. Additionally `form-engine.md` §2 step 7 now *"states the TRCP 195.2 rule UNVERIFIED with NO date computed"* (BS L2; batch 85) — the design text now depends on this verification explicitly.
6. **DISCONFIRM.** (i) BS L125's flat *"Rule 195.2's 30-day floor is GONE"* could be mistaken for a settled fact — it is the memo's PROPOSED reading carried by a runner; SOURCING: *"Retrieval is not verification … ONLY MICHAEL VERIFIES."* (ii) Could this be a DUPLICATE of L86? Different limb (floor vs offsets), same act — "shared blocker", per the brief's rule. (iii) The row's second sentence is a consequence statement, not a question; nothing else to close. Unique: `floor is GONE from the July 2026 text` 1 · `Do you verify that the floor is gone?` 1 · `would compute a LATER date than the current rule allows` 1.
7. **PROPOSED: RECLASSIFY-ACT — HIGH** (Michael's verification act), same annotation as L86, cross-naming each other and R11. Destroys nothing.
8. **Row first line (exact, once):**
   - ⬜ **[DL-memo Q4] — Rule 195.2's "later of … 30 days after the request is served" floor is GONE from the July 2026 text.** The rule now states expert designation as fixed dates — 90 days (party seeking affirmative relief) and 60 days (all others) before the end of the discovery period. The 2021 text carried the floor. **Do you verify that the floor is gone? A designation function built on the 2021 formulation would compute a LATER date than the current rule allows.** Entered 2026-08-14. **Cross-links the TRCP 195.2(a)/(b) sign-off row already on this queue.**

---

### FO-7 (NO ROW) — hardened §5.4 "answerable from HEAD right now"

1. **No row.** `grep -c -F "FO-7"` REG = 1, and that hit is the Status header (L3): *"`FO-7` (whether a research memo and a future-module staging satisfy `FO-3`'s QuickBooks condition)"* inside the batch-82 reconcile sentence's *"WHAT IS GENUINELY OPEN AND HAS NO ROW ANYWHERE"* list, which ends *"**Minting any of them is Michael's act, not a runner's.**"* The register body has no FO-7 row (positive control: `DA-3` likewise header-only). Definition on the record — runner 82, LOG L604: *"**`FO-3` second limb / `FO-7`** the QuickBooks hook, **CONDITIONAL and UNVERIFIED**, whether a research memo and a future-module staging satisfy the condition being his call"*; runner 82 L598: *"`FO-6`, `FO-7`, `DA-1` and `DA-4` sit beside it. **None has a queue row anywhere.**"* Origin ruling — `#137` LOG L657–674 (heading `## 2026-08-22 (#137) — (Voice design session finished in typed mode, Opus 5: the disclosures`): *"CONDITIONAL, NOT RULED: IF a QuickBooks integration is genuinely in the build or spec'd, THEN the firm-obligation record carries whatever hook makes the expense side trivial rather than staying deliberately ignorant of it — and THE CONDITION IS VERIFIED AT HEAD BEFORE ANYONE BUILDS ON IT."* … *"Reconcile against the QBO integration already staged as a future module (link-don't-rebuild, read-only first) rather than building alongside it."*
2. **Every mention.** REG: 1 (header). LOG: 2 (L598, L604 — both runner 82). ARCH: 0 (post-cutoff). BS: 1 (L164: *"with `FO-6`, `FO-7`, `DA-1` and `DA-4` beside it, and `FO-1`/`FO-2`/`FO-3` and `BR-1`–`BR-5` living in log entries alone."*). Audit §8 defect 2 (L242–243) lists `FO-7` among the no-row items; collision report L25 *"FO-6/FO-7 defined nowhere"* and L372 *"header-only"*.
3. **The two HEAD facts the hardening states — verified as far as the staged files allow:**
   - **FACT 1 — "NOT in the build":** BS L83 carries, verbatim: *"AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — none of the 37 tables is a money table."* with its basis: *"*(Basis stated: word-bounded over `db/` these are exactly zero. Widened to `db/` + `supabase/`, two stop being literally zero — `message` ×3, every one a JavaScript `error.message`, and `case_links` ×4, every one a COMMENT saying it is not built."* **CONFIRMED that BUILD-STATE at HEAD `7f02131` states it** (the hardening quoted it accurately). The underlying `db/` sweep is UNVERIFIABLE-HERE (`db/` not staged); note the line is tagged `(#87)` — a 2026-08-15 measurement carried through the 126th refresh; BS L96 independently states *"**NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**"*.
   - **FACT 2 — "But SPEC'D" (the memo exists):** the file `docs/specs/qbo-integration-research-memo-2026-08-15.md` is NOT staged, and no staged inventory lists `docs/specs/` at HEAD (the superseded-specs survey has 0 `qbo` hits). Existence is CORROBORATED, not confirmed: runner 42 LOG L6480–6481 *"**The staged memo landed VERBATIM and matches its stated counts exactly — 580 non-blank / 721 raw**, 51,316 bytes, copied byte-for-byte rather than retyped."*; `#87` L6531 *"**One staged file:** `docs/specs/qbo-integration-research-memo-2026-08-15.md`"*; REG L854 *"from `docs/specs/qbo-integration-research-memo-2026-08-15.md` §11 and §12 (CHAT-DISPATCH Task 13)"* with rows `Q-QBO-1`…`-8` (L855–863) and `QBO-LOOK-1`…`-4`; BS L77 records the file was EDITED at HEAD on 2026-08-19 (*"`qbo-integration-research-memo` … NOW REPAIRED"*). Presence at `7f02131` specifically: UNVERIFIABLE-HERE (a `git ls-tree` is the decisive check; not available).
   - **What the memo IS, on the record:** *"PROPOSED — RESEARCH ONLY"* (REG L854); BS L96 *"RESEARCH/INPUTS ONLY"*. Whether a research memo is "spec'd" in `FO-3`'s sense is exactly the question runner 82 records as *"his call"*.
4. **Whose words.** `FO-3`'s split is Michael's (*"Yeah. So it's right."* confirming the split, `#137` L664–665); the condition text is the entry's record of his ruling; "his call" on whether the memo satisfies it is runner 82's statement of the packet's own rule. Hardened §5.4's "answerable from HEAD" is a Claude finding about the two FACTS, not a ruling on the CONDITION.
5. **Since the hardening:** nothing on FO-7 or FO-3 — no hit in LOG L1–600 beyond the two runner-82 lines (which pre-date the hardening); BS L164 carries the no-row list unchanged; `#139`/`#140` do not touch QuickBooks (`QuickBooks`/`QBO` 0 in LOG L20–370).
6. **DISCONFIRM the hardening's framing.** "Answerable from HEAD" is true of the two FACTS and false of the QUESTION: the record puts *"whether a research memo and a future-module staging satisfy the condition"* to Michael in terms. And the two facts point opposite ways (not in the build / but a research memo exists), so nothing can be discharged mechanically. Also: "staged as a future module" (`#137`'s phrase) has no BS referent — `future module` 0, `link-don` 0 in BS; the only "future-module" text in BS is the unrelated `future-modules-capture-2026-07-28.md` (L132, L164) — so half of `FO-3`'s premise ("already staged as a future module") is itself unverified on the staged record.
7. **PROPOSED: none — NO ROW; REPORT ONLY (mint nothing).** For the sheet: the FO-3 second-limb condition remains CONDITIONAL and UNVERIFIED with both facts stated; whether they satisfy it is Michael's; if a row is ever wanted its minting is his act (runner 82; `#137` ID-series caution). Confidence in the two fact-checks: FACT 1 HIGH (BS at HEAD states it verbatim); FACT 2 MED (existence corroborated three ways, file not staged). Destroys nothing.
8. **No row line.** Header sentence (L3) is the only carrier; it is a rewritten-wholesale header, cite by quoted sentence, not by line.

---

### Q-WF-4 (L900)

1. **Row read whole (2,423 chars).** The question, verbatim: *"**Does this application acquire a server-side identity, and of what shape (deployed Supabase Edge Function, a process on the GPU machine, something else)? This question is upstream of all seven WF rows, it is not in the queue under any ID, and no WF gate names it.**"* Restated in the `#94` annotation the row quotes: *"The question itself — does this application acquire a server-side identity, and of what shape — remains open and Michael's."* The row's own limbs, as it frames them: (i) WF-2–WF-8 — *"Every WF pipeline is a background watcher measured in days and weeks … Graph webhooks require a publicly reachable HTTPS endpoint answering in 3–10 seconds, and even delta polling needs a process running when no browser is open — which means app-only credentials, i.e. a client secret or certificate this registration deliberately does not have."*; (ii) `Q-QBO-3` — *"what is irreducible is the confidential-client exchange and the rotating-token home"*; (iii) `Q-COM-4` — *"needs a scheduled runner to beat a 30-day retention window."*
2. **Every mention.** REG: 6 line-hits (L900 the row; L826 `Q-COM-4`; L858 `Q-QBO-3`; L273 the WF annotation note; header ×2 — flattened 14, the extra ones inside the header's long sentences). LOG: 14 line-hits, all located: L84 (`#140` AS-Q1 — below); L522 (`#138`: *"`Q-WF-4` (an unnamed upstream node two other rows hang on)"*); L5443 (runner 49: *"`Q-WF-4` now names all three consumers at his word "Amend the row," its substance untouched"*); L5506–5508 (`#94`: *"**Q-WF-4's row is ANNOTATED with its three consumers** … The identity question itself remains open and Michael's."*); L6222 (`#89`: *"**Recorded as a fact about that question; Q-WF-4 was NOT amended.**"*); L6400 (`#88`: *"**`Q-WF-4` DOES NOT ACQUIRE A THIRD CONSUMER — stated because #87 asked.**"*); L6514, L6524, L6560, L6614 (`#87`: *"**HEADLINE 3 — QBO LANDS ON `Q-WF-4`, FROM A DIFFERENT DIRECTION AND HARDER.**"*); L6888 (`#85`, origin: *"**THE HARDEST GATE IS NOT ON THE RECORD AT ALL (Q-WF-4).**"*); L5254/L5364/L5444 open-items carries. ARCH: 0 (flattened `Q-WF` 0 — the ID was minted 2026-08-15 at `#85`, after the archive's 2026-08-12 cutoff; control `D-CL2-3` = 15 fires). BS: 1 (L130: *"**THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** … **ITS ROW NAMES ALL THREE CONSUMERS (#94)**"* — note the internal contradiction: "on no row at all" is `#85`-era text carried after the row existed; the same bullet then cites "its row"). 2026-08-24 audit: `Q-WF-4` 0 (control `Q-WF-6` 1, `Q-QBO-3` 1) — confirms hardened §5.5 *"**`Q-WF-4`** (the server-side-identity question `Q-QBO-3` and now `FO-3` both hang on — an unnamed upstream node)"*. Collision report: 0 (packet-local `Q-WF-` namespace, ID-DL-1).
3. **Decisive text — is there a ruling? NO.** Every Michael word on this row is procedural: *"Amend the row"* (`#94`, add the consumers) — and the annotation records in the same breath that *"The identity question itself remains open and Michael's."* No entry in LOG or ARCH contains a ruling on server-side identity for the application: grep `server-side identity` LOG = hits only in the `#85`/`#87`/`#89`/`#94` passages above and the runner carries; ARCH = 0 (control `D-CL2-3` 15). **The AS-Q1 ruling, `#140` LOG L82–85 (heading `## 2026-08-31 (#140) — (Typed design session, Cowork, Fable 5 per the environment; DEVICE BRIDGE GRANTED`), character-exact (soft breaks joined):** *"`AS-Q1` "Yes — server-side function + secret; fixture writer only now; note CLAUDE.md" — the model call runs in a server-side function holding the credential as a Supabase secret (the LegiScan pattern; `Q-WF-4`'s first instance); this slice ships the writer INTERFACE, a FIXTURE writer in both modes and the function's SHAPE (a stub that refuses without its one vendor-neutral secret); no vendor SDK, key or endpoint until `H12-v`."* Related: `#140` L159–160 *"CLAUDE.md's data-hygiene bullet takes a conforming note (`AS-Q1`)"* (CLAUDE.md not staged — UNVERIFIABLE-HERE).
4. **Whose words.** *"Yes — server-side function + secret; fixture writer only now; note CLAUDE.md"* is MICHAEL's selection (`#140` L81: *"his selections, verbatim; the option texts are Claude's and are never quoted as his"*; all eighteen are RULED-BY-SELECTION). The gloss — including *"`Q-WF-4`'s first instance"* — is the entry's (Claude's) characterization. "Amend the row" (`#94`) is his; the row's question is the WF spec's (`#85`, Claude) and the row itself says the answer is his.
5. **Since the hardening:** ONE thing — AS-Q1 (`#140`), plus batch 85's landing of the slice and the CLAUDE.md note. The row L900 itself is unchanged (A7-0(b)). `#138` (L522) named it as an unnamed upstream node. Nothing else.
6. **Which limb, if any, does AS-Q1 answer?** Read against the row's own words:
   - **Limb "does this application acquire a server-side identity?" — NOT answered.** AS-Q1 establishes a server-side EXECUTION CONTEXT holding a static vendor secret for ONE consumer — the disclosures model call, which is user-initiated and synchronous ("fixture writer only now"; the function's SHAPE, a stub). Q-WF-4's "identity" means an app-only credential on the Entra registration (client secret/certificate) able to act against Graph with no browser open, a publicly reachable webhook endpoint, a scheduled runner, or a confidential-client exchange with a rotating-token home. AS-Q1 supplies none of those: the `#140` entry itself calls it *"the LegiScan pattern"* — and the row's own `#87` annotation says of that pattern *"gate 4's LegiScan comparator is a *static* key rotated by hand, and does not transfer."* The entry's own label is *"`Q-WF-4`'s first instance"*, i.e. a first instance of the pattern the question is about — not a ruling on the question, and not framed as one anywhere (`#140`'s "WHAT DID NOT HAPPEN" and BS L130 keep Q-WF-4 open; `#140`'s still-open list does not name Q-WF-4 either way).
   - **Limb "of what shape (deployed Supabase Edge Function, a process on the GPU machine, something else)?" — PARTLY answered by precedent, for one consumer only.** AS-Q1 picks "server-side function + secret" with the credential *"as a Supabase secret"* — the Edge-Function shape from the row's own option list — for the disclosures call. It does NOT decide the shape for any of the three named consumers (WF-2–WF-8 need a process running with no browser open — a request-driven function does not supply a watcher or a webhook endpoint without more; QBO needs a durable WRITABLE home for a rotating token, which a static secret is not; COM-4 needs a scheduled runner). So: shape precedent established for synchronous secret-holding calls; identity question untouched; the three consumers' requirements untouched.
   - **What AS-Q1 does add to Q-WF-4's record:** a FOURTH consumer of the server-side question in a weaker form (the disclosures model call), already decided for itself; and a concrete first instance of "deployed Supabase function + secret" in the design (not yet built — `FE-D1A-1` OPEN).
   **DISCONFIRM my own reading:** one could say AS-Q1 answers limb (i) with "yes, for this call" — but "a server-side function holding a secret" (outbound API key) is not "a server-side identity" in the row's sense (an app-only principal / confidential client), and the entry chose the words "first instance", not "ruled". Second attempt: BS L130's "ON NO ROW AT ALL" could mislead a reader into thinking the question was never registered — it is registered at L900 since batch 40 (2026-08-15). Unique text: `Does this application acquire a server-side identity` 1 · `rotating-token home` 1 · `confidential-client exchange` 1 (`no server-side identity` 3 — L273, L858, L900, each a different row's premise).
7. **PROPOSED: ANNOTATE-KEEP — MED.** LIVE and unruled, exactly as hardened §5.5 asked to verify; annotation substance: "ANNOTATED add-only 2026-09-01, NOT CLOSED — a FOURTH, weaker consumer decided for itself at #140 AS-Q1 (Michael: "Yes — server-side function + secret; fixture writer only now; note CLAUDE.md"): the disclosures model call runs in a server-side function holding a static vendor secret (the LegiScan pattern), which #140 records as Q-WF-4's FIRST INSTANCE. It establishes a shape precedent for synchronous secret-holding calls only; it does not decide whether the application acquires a server-side identity, and supplies neither the app-only credential, the always-on process/webhook endpoint, the scheduled runner, nor the rotating-token home the three named consumers require. The question remains open and Michael's." Why MED not HIGH: this row's last amendment was done at Michael's word ("Amend the row", `#94`) after `#89` recorded a fact *without* amending — the sheet may prefer KEEP with the AS-Q1 fact stated in the sheet only, leaving the row to his word. Either way the glyph stays ⬜ and nothing is destroyed.
8. **Row first line (exact, once):**
   - ⬜ **Q-WF-4.** The single auth surface is a single-tenant public-client browser SPA using MSAL with PKCE, holding one delegated scope (`Calendars.ReadWrite`), with **per-browser tokens**, a `localhost:5173` redirect, and no server-side identity. Every WF pipeline is a background watcher measured in days and weeks (*"stuck for weeks," "weeks-long stalls," "10+ days"*), Graph webhooks require a publicly reachable HTTPS endpoint answering in 3–10 seconds, and even delta polling needs a process running when no browser is open — which means app-only credentials, i.e. a client secret or certificate this registration deliberately does not have. **Does this application acquire a server-side identity, and of what shape (deployed Supabase Edge Function, a process on the GPU machine, something else)? This question is upstream of all seven WF rows, it is not in the queue under any ID, and no WF gate names it.** **ANNOTATED add-only 2026-08-15 (#87), NOT CLOSED — A SECOND CONSUMER NOW STACKS BEHIND IT.** The Task 13 QBO memo reaches this same question from a different direction and, on one axis, harder: the QuickBooks token exchange **requires a client secret** (a confidential client) and the refresh token **rotates**, so custody needs a **durable, writable home for a value that changes** — where gate 4's LegiScan comparator is a *static* key rotated by hand, and does not transfer. **One thing is easier than the WF case and is recorded so the question is not over-costed:** QBO's read path can be user-initiated, so **no daemon, no webhook endpoint and no app-only credential are strictly required** — what is irreducible is the confidential-client exchange and the rotating-token home. **Whether QBO folds in here or gets its own question is `Q-QBO-3`, and it is yours.** **ANNOTATED add-only 2026-08-16 (#94), NOT CLOSED — THE ROW NOW NAMES ALL THREE CONSUMERS, at Michael's ruling "Amend the row."** Full text per QR-1: **"`Q-WF-4` now has THREE consumers on the record and the row must say so: WF-2–WF-8 (every pipeline a background watcher measured in weeks); #87's `Q-QBO-3`; and #89's `Q-COM-4`, the most demanding — a communications log wants everything continuously, and even the metadata-only `callRecords` version needs a scheduled runner to beat a 30-day retention window. The question itself — does this application acquire a server-side identity, and of what shape — remains open and Michael's."**

---

## Could not process / caveats
- None unprocessed. Caveats: REQ-CAPTURE §18.F content, `db/` sweep, the QBO memo file, `CLAUDE.md`'s AS-Q1 note, and the batch-82 register copy are NOT staged — every statement about them above is marked UNVERIFIABLE-HERE or "runner statement".

## SUMMARY

| ID | audit class | hardened verdict | PROPOSED | confidence | destroys-unique-text? (Y/N) |
|---|---|---|---|---|---|
| CL2-AC-1 (L170) | HANDS-ON candidate (§6 row 91, ripe now) | not hardened (non-closure class); `#139` ACCEPTED | ANNOTATE-KEEP — "on the CC-1 hands-on queue, accepted #139"; ripe now | HIGH | N |
| CL2-CHECK-1 (L171) | HANDS-ON candidate (§6 row 92, ripe now) | not hardened; `#139` ACCEPTED; also `#66` KEEP PARKED | ANNOTATE-KEEP (parked posture unchanged; sitting = the row's own revisit trigger) | HIGH | N |
| FE-§11.4 (L201) | HANDS-ON candidate (§6 row 112, ripe now) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; ripe now | HIGH | N |
| Bill label default (L1283, no ID) | HANDS-ON candidate (§6 row 486, ripe now) | not hardened; `#139` ACCEPTED as "the bill-label pre-fill" | ANNOTATE-KEEP; no ID minted | HIGH | N |
| CR-7 (L127) | HANDS-ON candidate (§6 row 75; not ripe) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP, scoped to the §5 Q5 riding question (print/layout half) | HIGH | N |
| CR-CONSTRAINT (L132) | HANDS-ON candidate (§6 row 80; not ripe) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP, scoped to the §5 Q4 riding question; constraint in force | HIGH | N |
| Q-FE6-5 (L964) | HANDS-ON candidate (§6 row 419; "the rest… not ripe") | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; not ripe; ID packet-local (ID-DL-1) | HIGH | N |
| Q-FE4-1 (L945) | HANDS-ON candidate (§6 row 400; not ripe) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; not ripe | HIGH | N |
| Q-FE5-3 (L953) | HANDS-ON candidate (§6 row 408; not ripe) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; not ripe (no budget built) | HIGH | N |
| Q-IN2-7 (L918) | HANDS-ON candidate (§6 row 379, "far from ripe — GPU-gated") | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; far from ripe | HIGH | N |
| Q-IN1-1 (L924) | HANDS-ON candidate (§6 row 382; not ripe) | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; shares its first sentence with IN-1 (L250) — shared question, not a duplicate | HIGH | N |
| Q-IN3-6 (L938) | HANDS-ON candidate (§6 row 396, "not ripe; discovery slice unauthorized") | not hardened; `#139` ACCEPTED | ANNOTATE-KEEP; not ripe | HIGH | N |
| TRCP 195.2(a)/(b) (L86) | LIVE (§7 "TRCP sign-offs at lines 84–88") | §5.2: R11's unblock row 1 of 2 — CONFIRMED; `#139` HD-10 "Close it by pointing at them" connects it | RECLASSIFY-ACT (Michael's VERIFICATION act; R11's gate) + annotation; stays ⬜ until he verifies | HIGH | N |
| [DL-memo Q4] (L1013) | LIVE (§7 "[DL-memo Q1]…[Q5]") | §5.2: R11's unblock row 2 of 2 — CONFIRMED (cited L1012, now L1013) | RECLASSIFY-ACT (same act, different limb — shared blocker, not duplicate) | HIGH | N |
| FO-7 (NO ROW) | no-row item (§8 defect 2) | §5.4 "answerable from HEAD": FACT 1 (BS zero-hit line) CONFIRMED at HEAD; FACT 2 (memo exists) CORROBORATED, file not staged | none — REPORT ONLY; mint nothing; whether the facts SATISFY FO-3's condition is Michael's (runner 82 "his call") | FACT 1 HIGH / FACT 2 MED | N |
| Q-WF-4 (L900) | named nowhere in the audit | §5.5: unnamed upstream node — CONFIRMED (audit 0 hits vs controls 1) | ANNOTATE-KEEP — LIVE/unruled; AS-Q1 (#140) is a first INSTANCE (shape precedent for one synchronous consumer), answers neither limb | MED | N |

**Standing defects reported, not repaired (runner/Code statements, not rulings):** (1) REG header, batch-84 sentence: *"none of them a row in this register"* — false for the audit's twelve. (2) BS L130: *"ON NO ROW AT ALL (Q-WF-4)"* contradicted by its own next sentence (*"ITS ROW NAMES ALL THREE CONSUMERS"*). (3) BS L125's flat *"Rule 195.2's 30-day floor is GONE"* is a PROPOSED memo finding carried as if settled; the verification is Michael's (BS's own words two clauses earlier: *"resolving it is your verification act"*).
