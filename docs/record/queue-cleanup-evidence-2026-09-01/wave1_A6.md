# WAVE-1 — verifier A6 — the ten DUPLICATE candidates + the H43 enumeration

**Read-only. Nothing under `/home/claude/work/ev/docs/` was modified. Staged at HEAD `7f02131`, 2026-09-01.**

**Task.** The hardened audit (`attorney-review-queue-audit-HARDENED-2026-08-25.md` §4) says of the 2026-08-24 audit's ten DUPLICATE candidates: *"not one is a true duplicate."* Each pair below was tested in BOTH directions: (i) the strongest case that the pair IS one question in two rows (so the hardened verdict is wrong and a fold is safe), and (ii) the strongest case against the disposition I propose. For each pair I list (a) the limbs the rows share, (b) the limbs unique to each, and (c) the distinctive phrases with `grep` counts across the register (line-anchored and on a flattened copy) against a positive control.

**Corrections to the slice's line pointers, found on reading.** The `Q-COM-10` UMBRELLA row is at **L832** (4,403 bytes on the line; rows.tsv says 4,404 with its newline), NOT near L763 — L763–L768 are the six sub-rows `-A`…`-F` (`-F` is at L768, not L767). `Q-DES-6` is at **L774** (L773 is `Q-DES-5`). `Q-T19-1` is at **L609** (L608 is the ✅ `TASK-19` row). The audit's own numbers (186, 223, 230, 255, 311, 348, 401, 407, 415, 426) are ROW ORDINALS, not line numbers.

**Method notes that apply to every block.**
- Count table columns: `live` = `docs/record/session-log.md` line-anchored; `liveF` = same file flattened (`tr '\n' ' '`); `arch` / `archF` = the closed archive; `BS` = `BUILD-STATE.md`; `reg` / `regF` = the register; `toc` = `session-log-toc.md`. Positive control `QR-1`: live 74/80, arch 45/47, BS 2, reg 54/102, toc 4 — fires in every file. **Every one of the ten IDs was minted after the archive's 2026-08-13 cutoff, so an archive count of 0 is expected for all of them, not evidence of anything.**
- "Since the hardening" = the first ~605 lines of the live log (runner 85 · `#140` · runner 84 · `#139` · runner 83 · `#138` · top of runner 82) plus BUILD-STATE at HEAD. In that region the ONLY hits for any of the ten IDs are inside `#138`'s own summary of the hardening it filed (L505–L517: `WESTLAW-5` L507, `Q-RE-8`/`Q-STAT-5` L508, `H43` L516–517). `FE-4`/`FE-5`/`FE-6`/`FE-7`, `privilege_tier`, `fe-d1-build-slice`, `TRAP 77`, `WP-1`, `conferral`, `TDRPC 1.04` all return **0** in that region (control `PF-1` = 6, `FE-D1` = 22). **Nothing in `#139`, `#140` or runners 83–85 touches any of the ten.** v28's restoration of PF-1 and the SOURCING fourth channel bears on none of them (the fourth channel is `Q-RE-9`'s subject, already ✅; it says nothing about registry placement).
- Not staged, therefore UNVERIFIABLE-HERE wherever named below: `fc-adjudication-record-2026-08-18.md`, `task-19-signoff-record-2026-08-17.md`, `bexar-local-rules-and-efiling-2026-08-15.md`, `case-heartbeat-design.md`, `fe-d1-build-slice.md`, `fe-d1-amendment-slice.md` (NEW at HEAD per runner 85 — 141,468 B), `criminal-opinions-read-2026-08.md`, `costs-and-privilege-authority-read-2026-08-18.md`.
- Row-identity strings (item 8) are copied by program from the staged register, never retyped; each was checked with `grep -c -F -x` and every one occurs exactly **once**.
- SUMMARY column *destroys-unique-text?* answers: **would the 2026-08-24 audit's fold (closing this row into the survivor it named) delete interrogative text that exists nowhere else in the register?** My own proposals are framed so that no text is deleted (the register's closure practice keeps the row text — `#106`'s runner line: *"the original question preserved above it per QR-1"*).

---

### FC13-Q-5 (L432)

**1. Row, whole (682 B, one line; section "FC-13 entry drafts and the `Q-WS3-5` second pass … ADDED 2026-08-19 (#112)").** Interrogative, verbatim: *"**Where does the TDRPC 1.04(f) entry live?** There is no practice-of-law registry file and the registry carries not one professional-conduct proposition. Candidates: **(a)** a new file, e.g. `legal-rule-registry-practice-and-professional-conduct.md`; **(b)** into the enforcement-and-pleading file despite its subject line; **(c)** hold it staged until `Q-STAT-5` is ruled."* Tail: *"**No session should pick this — naming a home decides part of the cases-vs-practice boundary you expressly left undecided at #108.** Note that four research passes already stack behind `Q-STAT-5`'s one ruling, so this may be the cheapest moment to take it. — **OPEN — Michael**"*

**2. Locate.** `FC13-Q-5`: live 0/0 · arch 0/0 · BS 0/0 · reg **1**/1 (its own row only) · toc 0. Zero-controls: `FC13-Q` live **5** (the series is named; the `-5` member never is), `FC-13` live 19, BS `FC13-Q` 1 (L20: *"`FC13-Q-1..6` ask whether they insert at all and where"*). The ID exists in exactly one place in the whole staged corpus: this row.

**3. Decisive text.**
- `session-log.md` — `## 2026-08-18 (#108) — T-26 RUN LIVE: THREE GROUP A ENTRIES VERIFIED AND TWO CONFORMED VERBATIM AT` (L3952) — L4001–4005: *"FC-13 RULED AND CLOSED, both limbs: five entries OPEN — Prop. Code ch. 55, CPRC ch. 146 incl. the 2025 (c-1) amendment, TRCP 47(c) bands, §18.001/§41.0105, and TDRPC 1.04(f), the registry's FIRST practice-of-law proposition. Drafting authorized, Opus lane, entering UNVERIFIED, under the verbatim direction; the plain open was chosen over the offered boundary-ruling variant, so the cases-vs-practice boundary stays undecided."*
- `BUILD-STATE.md` L20: *"**You chose the plain open over the boundary-ruling variant, so the cases-vs-practice boundary stays UNDECIDED**"*; same line: *"six texts now exist at `docs/specs/fc13-entry-drafts-2026-08-18.md`, DRAFTED AND NOT INSERTED"*.
- Register `Q-STAT-5` row (L1003) names the stack as exactly four: *"Privacy (`Q-WF-6`), insurance (#78), client property / trust accounting (`Q-QBO-6`) and now professional conduct"* — `FC13-Q-5` is not among them (`Q-STAT-5` row contains `FC13`: 0). `BUILD-STATE.md` L116: *"**FOUR research passes still stack behind `Q-STAT-5`'s one ruling.**"*

**4. Whose words.** The row is the `#112` Opus-run's (Claude), carried by the runner. The `#108` sentence is a Fable design session's compressed record of Michael's choice — *"the plain open was chosen over the offered boundary-ruling variant"* — his verbatim words live in `fc-adjudication-record-2026-08-18.md` (UNVERIFIABLE-HERE). No ruling on `FC13-Q-5` exists anywhere in the staged files; no one has spoken on where the entry lives.

**5. Since the hardening.** Nothing. `FC13-Q-5` = 0 in the first 605 log lines; BUILD-STATE at HEAD still carries `FC13-Q-1..6` as open placement questions (L20).

**6. DISCONFIRM — both directions.**
- *Case that it IS a duplicate of `Q-RE-8` (the audit's claim):* both rows turn on the same unmade choice — practice-of-law propositions get a fifth file or go into an existing one. Option (a) ≈ `Q-RE-8`'s "fifth registry file"; option (b) ≈ "join an existing one". One ruling on `Q-RE-8` in the "fifth file" or "join existing" direction would answer `FC13-Q-5`. The row itself says naming a home "decides part of the cases-vs-practice boundary" — i.e., it concedes it is an instance of the boundary question.
- *Why that fails:* (i) **Neither row names the other** — `Q-RE-8` in the FC13-Q-5 row: 0; `FC13` in the Q-RE-8 row: 0 (and `FC13-Q-5` is not in `Q-STAT-5`'s named stack of four). (ii) `Q-RE-8`'s third option — *"stay out of the registry entirely"* — cannot answer `FC13-Q-5`, because `#108` already ruled TDRPC 1.04(f) IN as a registry entry (*"the registry's FIRST practice-of-law proposition"*, *"five entries OPEN"*); `FC13-Q-5` asks only WHERE a ruled-in, already-drafted entry lands, and adds option **(c) hold** which `Q-RE-8` does not carry. (iii) `FC13-Q-5` is a per-entry placement act attached to a specific drafted text (`fc13-entry-drafts-2026-08-18.md`); `Q-RE-8` is the layer/boundary question over fourteen zero-hit terms. Shared: the BLOCKER (fifth-file placement) — **shared blocker, not duplicate.**
- *Case against my proposal (KEEP as its own row):* a reader could say the row is redundant with `Q-STAT-5`'s stack and one sitting collapses all of them (the audit's §1 finding 1). True as a SITTING plan — false as a duplication finding: collapsing rows before the ruling deletes the (c)-hold option and the entry-specific drafted-text anchor.
- **(a) Shared limbs:** fifth-file-vs-existing-file placement; the cases-vs-practice boundary as the thing that decides it. **(b) Unique to FC13-Q-5:** option (c) hold-until-`Q-STAT-5`; the specific drafted entry (TDRPC 1.04(f) at `fc13-entry-drafts…`); the candidate filename; the "cheapest moment" observation; the *"you expressly left undecided at #108"* pointer. **Unique to Q-RE-8:** the "stay out entirely" option; the boundary interrogative; the fourteen-term zero-hit finding; "the four passes unblock together". **(c) Phrase counts (register line/flat; live):** `practice-of-law registry file` 1/1; 1 · `legal-rule-registry-practice-and-professional-conduct` 1/1; 0 · `naming a home decides` 1/1; 1 · `cheapest moment` 1/1; 0 · `boundary-ruling variant` 1/1; 1 (BS 1) · control `QR-1` 54/102; 74.

**7. PROPOSED: `KEEP` — HIGH.** The audit's DUPLICATE call was wrong: neither row names the other, `Q-RE-8`'s "stay out" limb is inapplicable to an entry `#108` ruled in, and `FC13-Q-5` carries a hold option and an entry-specific anchor found nowhere else. Sitting note (not a disposition): rule it in the same sitting as `Q-RE-8`/`Q-STAT-5`, since Michael's own `#108` choice — the plain open over the boundary ruling — is the reason all of them are still open; his verbatim words for that choice are in the unstaged FC record.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **`FC13-Q-5`** — **Where does the TDRPC 1.04(f) entry live?** There is no practice-of-law registry file and the registry carries not one professional-conduct proposition. Candidates: **(a)** a new file, e.g. `legal-rule-registry-practice-and-professional-conduct.md`; **(b)** into the enforcement-and-pleading file despite its subject line; **(c)** hold it staged until `Q-STAT-5` is ruled. **No session should pick this — naming a home decides part of the cases-vs-practice boundary you expressly left undecided at #108.** Note that four research passes already stack behind `Q-STAT-5`'s one ruling, so this may be the cheapest moment to take it. — **OPEN — Michael**
```

---

### V5-IDS (L606)

**1. Row, whole (2,139 B, one line).** Interrogative, verbatim: *"**V5-IDS — NEW 2026-08-17 (#96). Yours, one word, reversible: do the split entries take derived IDs (19a/19b, 20a/20b, 21a/21b), or does the backlog renumber 19–24 with everything after shifting by three?**"* Finding carried in the row: *"**RUNNER-FOUND: the precedent supports the NAMING, not the FILE SHAPE** — #66 produced one `##` container with two `###` sub-entries, while this split produced six top-level `##` entries, so the two registry files are now shaped two different ways."* Cross-link: *"**ANNOTATED 2026-08-17 (#97), ADD-ONLY (`T19-IDS-XREF`): `Q-T19-1` is this question's other half** … **ONE ANSWER SHOULD GOVERN BOTH ROWS**"*. Deferral: *"**ANNOTATED 2026-08-17 (#98), ADD-ONLY — DEFERRED AT THE WALK BY MICHAEL'S WORD (`D-6`, `task-19-signoff-record-2026-08-17.md` §4), NOT ANSWERED.**"*

Partner `Q-T19-1` (L609, 1,707 B): *"**THE QUESTION: do the three take 35–37, do they take derived IDs on the #66 precedent, or does the registry renumber?** This is **`V5-IDS`'s other half and one answer should govern both**"* — and the identical `#98` deferral paragraph.

**2. Locate.** `V5-IDS`: live 9/10 · arch 0/0 · BS 2/2 · reg 5/8 · toc 0. `Q-T19-1`: live 5/7 · arch 0/0 · BS 2/2 · reg 7/11 · toc 0. Cross-naming verified both ways: `Q-T19-1` in the V5-IDS row 1; `V5-IDS` in the Q-T19-1 row 1.

**3. Decisive text.**
- `session-log.md` — `## 2026-08-17 (#98) — TASK 19 SIGN-OFF WALK EXECUTED (CHAT-DISPATCH v2 Task B): 24 of 40` (L5098); header L5099–5100: *"(design session, Fable 5, Cowork, typed; Michael live throughout — every act below is his word, item by item)"*; L5107–5108: *"work-product WP-1/2/3 (labels packet-local — numbering DEFERRED, D-6)"*; L5119–5122: *"**SIX HOUSEKEEPING RULINGS** (record §4): … Q-T19-1 numbering DEFERRED."*
- `session-log.md` — `## 2026-08-17 (#97) — TASK 19 SIGN-OFF WORKLIST STAGED` (L5157) — L5204–5205: *"**Nothing was invented** — they are labelled `WP-1/WP-2/WP-3`, packet-local. **This is `V5-IDS`'s other half and one answer should govern both.**"*
- `session-log.md` — `## 2026-08-16 (#96) — V-EXEC EXECUTED IN PART` (L5257) — L5287–5289: *"**AND THE SPLIT'S ID SCHEME WAS A PACKET-ADDED CHOICE, ROUTED RATHER THAN ASSUMED (`V5-IDS`).** #73 ruled the split and named no ID scheme. Derived IDs — **19a/19b, 20a/20b, 21a/21b** — were executed on the file family's own precedent, the #66 split of the criminal file's former entry 4"*.
- `BUILD-STATE.md` L111: *"**THE THREE WORK-PRODUCT ENTRIES ARE VERIFIED AND STILL HAVE NO NUMBER (`REGISTRY-V` CLOSED, `Q-T19-1` OPEN).** … **Labelled `WP-1/2/3`, packet-local; numbering DEFERRED (`D-6`)**"*; L151: *"**`V5-IDS`** — derived IDs were executed on the #66 precedent, **but that precedent supports the NAMING and not the FILE SHAPE**"*; L118: *"it remains **the numbering authority `V5-IDS` and `Q-T19-1` rest on**"*.

**4. Whose words.** The `#98` deferral is Michael's act (the entry states every act is his word) — but note precisely what `#98`'s own list names: *"Q-T19-1 numbering DEFERRED"*. `V5-IDS` is not named in `#98`'s six housekeeping rulings; the *"DEFERRED AT THE WALK BY MICHAEL'S WORD"* sentence on the V5-IDS row is the runner's extension of `D-6` to the "other half". His verbatim deferral words are in the unstaged sign-off record §4 (UNVERIFIABLE-HERE). Nothing else on either row is a ruling: the derived-ID execution was a packet-added choice (Opus, `#96`), the "one answer should govern both" is the `#97` session's, and the file-shape finding is runner-found.

**5. Since the hardening.** Nothing. Both IDs 0 in the first 605 log lines; BUILD-STATE at HEAD still says *"`Q-T19-1` OPEN"* and *"numbering DEFERRED (`D-6`)"*.

**6. DISCONFIRM — both directions.**
- *Case that it IS one question in two rows:* both rows say so in terms — *"other half"*, *"ONE ANSWER SHOULD GOVERN BOTH ROWS"*, *"one answer should govern both"* — and the policy question (does the registry number by derived IDs, by position, or renumber?) is single. A ruling "derived IDs on the #66 precedent" or "renumber" answers both.
- *Why that fails as a DUPLICATE finding:* (i) the option sets differ — `V5-IDS` = {derived (executed), renumber 19–24}; `Q-T19-1` = {35–37, derived, renumber}. "35–37" answers only `Q-T19-1`; "derived" is already executed on `V5-IDS` but has no obvious meaning for three entries that are not splits of anything. (ii) Different entries in different states: six split entries (19b/21b VERIFIED, the rest not) vs three WP entries, all VERIFIED with NO number of either kind. (iii) `V5-IDS` carries a FILE-SHAPE finding — one `##` container with `###` sub-entries (#66) vs six top-level `##` entries — that survives ANY answer to the ID question and is stated nowhere else in the register (`shaped two different ways` reg 1/1, live 0; `supports the NAMING, not the FILE SHAPE` reg 1/1, live 0). "Other half" is the rows' own word; "same question" is not.
- *Case against my proposal (ANNOTATE-KEEP):* one could say the two should simply be consolidated into one row carrying all limbs, with `Q-T19-1` surviving (the audit's fold). Under this brief's vocabulary a fold is `CLOSE`, which requires a ruling in Michael's words — there is none; there is a deferral. And `D-6`'s named subject is `Q-T19-1`, so `V5-IDS`'s "deferred by Michael's word" is itself a runner inference the sheet should not harden.
- **(a) Shared:** the numbering-scheme policy (derived vs renumber); the #66 precedent; the workbook as numbering authority; "one answer governs both"; the `#98` deferral paragraph (identical text on both rows). **(b) Unique to V5-IDS:** the executed derived-ID choice and its one-word reversibility; the file-shape divergence; the "entry numbering is the workbook's and is not positional" reasoning. **Unique to Q-T19-1:** the 35–37 option; the collision-with-criminal-file / out-of-file-order finding; the three WP entries' VERIFIED-without-number state. **(c) Phrase counts (reg line/flat; live):** `shaped two different ways` 1/1; 0 · `supports the NAMING, not the FILE SHAPE` 1/1; 0 · `do the three take 35` 1/1; 0 · `collides with the criminal entries` 1/1; 0 · `renumber 19` 1/1; 0 · `DEFERRED AT THE WALK BY MICHAEL` 2/2; 0 (both rows) · `numbering DEFERRED` 0/0; live 2 (`#98` ×2) · control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a duplicate); the disposition itself MED.** Annotation substance: *"Not a duplicate of `Q-T19-1`; the two are one POLICY question with two instances and different option sets, to be put TOGETHER in one sitting. `#98`'s `D-6` names `Q-T19-1`; this row's deferral is by extension. The file-shape finding is its own limb and survives any ID answer."* Only Michael's word closes either (he deferred `Q-T19-1` at the walk); nothing has moved since 2026-08-17.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **V5-IDS — NEW 2026-08-17 (#96). Yours, one word, reversible: do the split entries take derived IDs (19a/19b, 20a/20b, 21a/21b), or does the backlog renumber 19–24 with everything after shifting by three?** Executed as **derived IDs** on the file family's own precedent — at #66 you split the criminal file's former entry 4 into **4a and 4b** rather than renumbering. The reason is sharper than tidiness: **entry numbering is the workbook's and is not positional in the registry files**, so renumbering would silently change what "entry 23", "entry 27", "entries 1–3" and `Q-STAT-6`'s eighteen-entry list refer to across the log, BUILD-STATE, this queue, the workbook, the citator pass and the statute pass. **#73 ruled the split and named no ID scheme, so this was a packet-added choice** (QR-6(e)) **and is yours.** One word reverses it; renaming entries is a doc edit. **RUNNER-FOUND: the precedent supports the NAMING, not the FILE SHAPE** — #66 produced one `##` container with two `###` sub-entries, while this split produced six top-level `##` entries, so the two registry files are now shaped two different ways. **ANNOTATED 2026-08-17 (#97), ADD-ONLY (`T19-IDS-XREF`): `Q-T19-1` is this question's other half** — the three TRCP work-product entries added at #94 carry a number in **neither** numbering system, and the #97 worklist labels them `WP-1/WP-2/WP-3`, packet-local. **ONE ANSWER SHOULD GOVERN BOTH ROWS; ruling this one in isolation splits the identifier scheme across the two files a second way.** Nothing above was changed. **ANNOTATED 2026-08-17 (#98), ADD-ONLY — DEFERRED AT THE WALK BY MICHAEL'S WORD (`D-6`, `task-19-signoff-record-2026-08-17.md` §4), NOT ANSWERED.** The three work-product entries were signed off under the packet-local labels **WP-1/WP-2/WP-3** and carry no registry-wide number in either file. **Deferring the number did not block their verification** — all three now read `VERIFIED — Michael, 2026-08-17` — so the identifier question is now attached to VERIFIED entries rather than unverified ones. **One answer should still govern both this row and its other half.**
```

---

### WESTLAW-5 (L613)

**1. Row, whole (4,064 B, one line).** Interrogative/act, verbatim: *"**WESTLAW-5 — NEW 2026-08-17 (#98). Your hand, and it is the single largest unblocking act left in the registry backlog: five named Westlaw pulls into `Documents\Knowledge Repo\Opinions\`.**"* Then: *"**ANNOTATED 2026-08-17 (#99), ADD-ONLY — SATISFIED AS TO ALL FIVE NAMED PULLS AND OVER-DELIVERED, AND STILL NOT CLOSED.**"* … *"**THE ROW DOES NOT CLOSE: its consumers 30 and 31 still need your cite supply.**"* … `#102`: *"**the five named Westlaw pulls this row is actually about were not touched, and the cite supply for entries 30 and 31 is still your act.**"*

Partners: `Q-RL6-1` (L757, 4,102 B) — entry 31's cite menu, now *"**do you want (a) *Hurlburt* cited on entry 4b, (b) *Pharr* cited on entry 4a/31, (c) both, or (d) neither pending your own read?** … **Your selection is unchanged and still yours; only the menu shrank.** — **STILL OPEN — your act**"*; `Q-RL6-2` (L758, 361 B) — *"**Do you want a separate targeted search, or does entry 30 stay cite-less?**"*

**2. Locate.** `WESTLAW-5`: live 7/8 · arch 0/0 · BS **0/0** · reg 5/8 · toc 0. `Q-RL6-1`: live 13/14 · BS 2/3 · reg 4/7. `Q-RL6-2`: live 2/2 · BS 1/1 · reg 2/2. Cross-naming is ONE-WAY: the WESTLAW-5 row names `Q-RL6-` four times; neither `Q-RL6-1` nor `Q-RL6-2` names `WESTLAW-5` (0 and 0). BUILD-STATE at HEAD does not carry `WESTLAW-5` at all but does carry the residual as `Q-RL6-1`/`Q-RL6-2` (L151: *"the cite supply for entries 30 and 31 (`Q-RL6-1`, `Q-RL6-2`)"*). Note: `id-collision-report.md` L5 lists `WESTLAW-5` among prefixes it treats as *non-IDs* — a labelling curiosity, not a finding about this row.

**3. Decisive text.**
- `session-log.md` — `## 2026-08-17 (#99) — CHAT-DISPATCH v3 TASKS T-20 THROUGH T-25 EXECUTED IN ONE PASS: the` (L4897) — L4909–4911: *"**`WESTLAW-5` IS SATISFIED AND THEN SOME — AND IT PAID FOR THE WHOLE SESSION.** `Documents\Knowledge Repo\Opinions\` holds all five named pulls plus *De Anda*, plus **nine criminal-side opinions the dispatch never named.**"*; L4918–4920: *"**20b (*Collins*) IS LOCATED.** … cause **02-14-00294-CV**, Jan. 19, 2017, MEMORANDUM OPINION, **pet. denied Jan. 26, 2018**"*; L4935: *"PDF — Nov. 30, 2017, Lang, J. **What FOLLOWS from a memorandum designation is Michael's**"*.
- Runner 72 line (L2701, 2026-08-19): *"`Q-RL6-1` / `Q-RL6-2` (cite supply for entries 30 and 31 — the authority read stages, it does not select)"*.

**4. Whose words.** `#99` is *"(design session, Cowork, Opus 5; Michael not live after the initial paste)"* — the "SATISFIED" statement is Opus's reading of Michael's folder, and *"THE ROW DOES NOT CLOSE"* is that session's (carried by the runner), not Michael's. Michael has never spoken to this row's closure. The act itself — the pulls — was Michael's hand, and the folder read is the evidence it happened.

**5. Since the hardening.** Nothing except `#138`'s summary of the hardening (L507: *"`WESTLAW-5` holds two V-9 provenance facts"*). BUILD-STATE at HEAD has no `WESTLAW-5` string; it carries 30/31 under `Q-RL6-1`/`-2`.

**6. DISCONFIRM — both directions.**
- *Case that it IS a duplicate of `Q-RL6-1`/`-2`:* the only thing still open on `WESTLAW-5` — *"its consumers 30 and 31 still need your cite supply"* — is exactly `Q-RL6-1` (entry 31) and `Q-RL6-2` (entry 30). Everything else on the row is done. On the open-limb test, the three rows collapse to two.
- *Why that fails as a DUPLICATE finding:* (i) **Category error, confirmed:** `WESTLAW-5` asks for an ACT ("Your hand … five named Westlaw pulls"); `Q-RL6-1`/`-2` are cite-SELECTION menus ("do you want (a)…(d)"). Satisfying the pulls does not select a cite, and selecting a cite would not have made the pulls. (ii) The row is the register's ONLY home for three V-9 provenance facts: entry 17's designation — `Lang, J` reg **1**/1 (this row), live 1 (`#99`); entry 20b's *"pet. denied Jan. 26, 2018"* reg 1/1, live 1; entry 16's *"pet. denied," which no public source could reach* — `no public source could reach` reg **1**/1, live **0**: that sentence exists in the register alone. Folding the row into `Q-RL6-1`/`-2` (whose subjects are entries 30/31, not 16/17/20b) would strand those facts. (iii) The audit's stated reason — *"the five pulls are done and over-delivered"* — is a SUPERSEDED/satisfied argument, not a duplication one; the hardened audit is right about that. **Shared blocker (30/31 cite supply), not shared question.**
- *Case against my proposal (CLOSE-SPLIT):* the closure basis is an act performed and verified by an Opus folder read, not a ruling in Michael's words, and the row's own authors kept it open twice (`#99`, `#102`) on the 30/31 residual. Someone could insist only Michael's nod closes an act row addressed to "your hand". Also `Hurlburt` — the optional companion named in the row — was NOT in the folder; if Michael later wants it pulled, this row is where that was recorded (though `Q-RL6-1` now also carries *Hurlburt* as verified-this-batch and option (a)).
- **(a) Shared:** the entry-30/31 cite-supply residual; the *Hurlburt* candidate; the nine unnamed opinions (`RL-6` → `T-27`). **(b) Unique to WESTLAW-5:** the five named pulls and the sibling-appeal warning (02-09-00305-CV); 20b LOCATED with cause/date/pet.-denied; 24's majority identified (Devine, J., 5–4); 17's designation with author; 16's pet. denied; the folder-as-gate rule for T-21; HK-5 no-sweep note. **Unique to Q-RL6-1:** the four-option menu, the *Pharr*/*Hurlburt* route, the *Whaley* "Do Not Publish" finding. **Unique to Q-RL6-2:** the targeted-search-or-stay-cite-less question for entry 30. **(c) Phrase counts (reg line/flat; live):** `Lang, J` 1/1; 1 · `pet. denied Jan. 26, 2018` 1/1; 1 · `no public source could reach` 1/1; **0** · `consumers 30 and 31` 1/1; 1 · `five named Westlaw pulls` 1/2; 1 · `02-14-00294-CV` 2/2; 1 · control `QR-1` 54/102; 74.

**7. PROPOSED: `CLOSE-SPLIT` — MED.** The act limb (the five pulls) is SATISFIED — by Michael's own hand, evidenced by the `#99` folder read — and the surviving limb (cite supply for entries 30 and 31) needs NO re-mint because it already lives at `Q-RL6-1` and `Q-RL6-2`; the closing annotation should cross-reference them and the row's text (the V-9 provenance facts) stays in place. MED because the closure rests on an act-done rather than a ruling-in-words and the row was twice deliberately held open by its authors; if the sheet wants a ruling basis, the alternative is `RECLASSIFY-ACT` (act performed; residual act rowed elsewhere). Either way: **NOT a duplicate**, and the audit's fold would destroy register-unique text.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **WESTLAW-5 — NEW 2026-08-17 (#98). Your hand, and it is the single largest unblocking act left in the registry backlog: five named Westlaw pulls into `Documents\Knowledge Repo\Opinions\`.** The five, named at the walk: ***Collins v. Kappa Sigma*** (2017 WL 218286 — **beware the 2010 sibling appeal in the same litigation, No. 02-09-00305-CV**; it is not this opinion); ***Allstate v. Irwin***, 627 S.W.3d 263; ***In re Ochoa***, 2004 WL 1192444; ***In re Sting Soccer***, 2017 WL 5897454; ***LaPorte v. State***, 840 S.W.2d 412 (optionally ***Hurlburt***, 506 S.W.3d 199); and optionally the ***De Anda*** history printout. **What each unblocks:** *Collins* → entry **20b**, which is NOT LOCATED in FLP or on the public web and has no citing graph at all; *Irwin* → entry **24**, the UDJA availability holding, now the sole blocker on that proposition since entry 23 verified — and the `V-9` class, where CLAUDE.md's binding majority-opinion rule cannot run, so a paginated copy is the only path to a positive majority identification; *Ochoa* → entry **18**, and it is **the one possible cite UPGRADE in the set** (a reporter-parallel cite), which is your act alone; *Sting Soccer* → entry **17**'s memorandum-vs-published designation; *LaPorte* → entry **31**'s “single criminal action” definition, which is cite-less and awaits your supply. **The folder is the gate: CHAT-DISPATCH v3 T-21 fires only if it exists, and SKIPS with a note if it is absent or empty.** Per HK-5 nothing on your machine is swept — only the named path is read, and only when you grant it. Source: `task-19-signoff-record-2026-08-17.md` and the #98 packet §7. **ANNOTATED 2026-08-17 (#99), ADD-ONLY — SATISFIED AS TO ALL FIVE NAMED PULLS AND OVER-DELIVERED, AND STILL NOT CLOSED.** `Documents\Knowledge Repo\Opinions\` holds *Collins*, *Irwin*, *Ochoa*, *Sting Soccer* and *LaPorte*, plus *De Anda*, **plus nine unnamed criminal-side opinions that are their own question at `RL-6`**. *Hurlburt*, the optional *LaPorte* companion, is **not** there. **WHAT IT BOUGHT: 20b LOCATED OUTRIGHT** — cause **02-14-00294-CV**, Jan. 19, 2017, MEMORANDUM OPINION, **pet. denied Jan. 26, 2018**, with the 2010 sibling **02-09-00305-CV** positively distinguished by the copy's own Prior History — **24's majority identified** (Devine, J., 5–4), **17's designation in the court's own word** (MEMORANDUM OPINION, Nov. 30, 2017, Lang, J.), and **16's "pet. denied," which no public source could reach.** **THE ROW DOES NOT CLOSE: its consumers 30 and 31 still need your cite supply.** **And locating is not verifying — every entry named here is still UNVERIFIED.** **ANNOTATED add-only 2026-08-18 (#100), NOT CLOSED — `RL-6`'s ruling stages the raw material and expressly does NOT supply the cites.** The nine unnamed criminal opinions get a FULL READ (CHAT-DISPATCH v4 **T-27**), staged as CANDIDATES per opinion; **the cite supply for entries 30 and 31 remains your act on those staged findings — the read stages, never selects.** Nothing was read this batch. **ANNOTATED ADD-ONLY 2026-08-17 (#102), NOT CLOSED — the staging half RAN and the pull half did not move.** CHAT-DISPATCH v4 **T-27** read all nine unnamed criminal opinions in full and staged them as candidates; **the five named Westlaw pulls this row is actually about were not touched, and the cite supply for entries 30 and 31 is still your act.** What the read returned is worth knowing before you spend a pull: **eight of the nine never mention art. 102.073**, and the ninth (*Middleton v. State*, 634 S.W.3d 46) touches it only in footnotes 36–37 as the State's parallel-statute argument, answered *"even assuming the State is correct"* and **expressly not decided** — suggestive dicta, not an adoption (`Q-RL6-1`). Entry 30 got **nothing** (`Q-RL6-2`). The read also produced a finding nobody asked for: ***LaPorte* is half-overruled** (`Q-RL6-3`), and **four of the nine are author-unidentifiable under V-9** (`Q-RL6-4`). Full record: `docs/specs/criminal-opinions-read-2026-08.md`.
```

---

### Q-RL6-5 (L761)

**1. Row, whole (1,166 B, one line).** Interrogative, verbatim: *"**Five of the nine are marked "DO NOT PUBLISH."** Registry entries built on unpublished CCA opinions carry a citation limit under the appellate rules. **Do you want a standing rule on whether unpublished opinions may support a registry entry at all**, or is that judged per entry?"* Amendment: *"**FOUR, not five, are "DO NOT PUBLISH"** — *Schmitt*, *Bailey*, *Simmons 2014*, *Simmons 2015* … **all nine are Court of Criminal Appeals opinions, so TRAP 77 governs, not TRAP 47 — and TRAP 77.3 reads *"Unpublished opinions have no precedential value and must not be cited as authority by counsel or by a court."* That is a BAR, not a limit** … **The standing-rule-or-per-entry question is unchanged; see `Q-DES-6`, which supersedes this row's framing.**"*

Partner `Q-DES-6` (L774, 912 B): *"**NEW 2026-08-18 (#103), and it supersedes `Q-RL6-5`'s framing.** … **Do you want a STANDING RULE that no unpublished CCA opinion may support a registry entry — and if so, does it bar the opinion as a cite, as support, or both?** Note the practical consequence before answering: **it reaches four staged candidates**, and `Q-RL6-5` asks the same question in weaker terms."*

**2. Locate.** `Q-RL6-5`: live 2/2 · arch 0/0 · BS 1/1 · reg 3/4 · toc 0. `Q-DES-6`: live 4/4 · arch 0/0 · BS 1/1 · reg 4/4 · toc 0. Cross-naming both ways: 1 and 1.

**3. Decisive text.**
- `BUILD-STATE.md` L135: *"**AND TRAP 77, NOT 47, GOVERNS THE CCA — 77.3 BARS citing unpublished opinions as authority** (`Q-DES-6`)"*; L136: *"**And FOUR, not five, are "DO NOT PUBLISH"** (`Q-RL6-5`)"* — both carried as open findings, no ruling.
- Register `Q-RL6-1` row (L757), runner annotation at the fifty-eighth invocation: *"it reads **"Do Not Publish — TEX. R. APP. P. 47.2 (b)"** (see `Q-AUTH-2`). **Under TRAP 47.7(a) that leaves *Whaley* with no precedential value**"* — a court-of-appeals (Dallas) criminal opinion, i.e. the TRAP 47 branch, live in this register.
- `session-log.md` — `## 2026-08-18 (#103) — CORRECTION: an adversarial audit of the five CHAT-DISPATCH v4 research` (L4372) — the session that authored both the `Q-RL6-5` amendment and `Q-DES-6`; its `#103` queue merge (L4363) entered `Q-DES-6` and amended `Q-RL6-5` in place.

**4. Whose words.** Both rows and the word *"supersedes"* are the `#103` Opus correction session's — Claude's framing, not Michael's. No ruling on either row exists in the staged files; the audit's own evidence for "duplicate" is that word, which its taxonomy files under SUPERSEDED (§3), not DUPLICATE (§4) — the hardened audit's point, confirmed.

**5. Since the hardening.** Nothing. Both IDs 0 in the first 605 lines; `unpublished` and `TRAP 77` 0 there; BUILD-STATE at HEAD carries both findings unchanged.

**6. DISCONFIRM — both directions.**
- *Case that it IS one question in two rows (the closest of the ten — agreed):* the interrogative core is the same — a standing rule on unpublished opinions supporting registry entries — the TRAP 77.3 text is quoted in both, both reach the same four staged candidates, and each row says the other is the same question ("supersedes this row's framing" / "asks the same question in weaker terms"). A ruling on `Q-DES-6` in either direction would, in practice, be applied to `Q-RL6-5`.
- *Why that still fails as a clean DUPLICATE:* (i) `Q-RL6-5` carries the **per-entry alternative** — *"or is that judged per entry?"* — which `Q-DES-6` drops (`judged per entry` reg 1/1, live 0: this row only). A ruling "no standing rule; per entry" is an answer to `Q-RL6-5` that `Q-DES-6`'s yes/no-plus-scope framing does not offer. (ii) **Scope:** `Q-DES-6`'s proposed rule is CCA-only (*"no unpublished CCA opinion"*); `Q-RL6-5`'s interrogative sentence says *"unpublished opinions"* without the CCA qualifier (the qualifier sits in its premise sentence). The non-CCA branch is live in this register: *Whaley* (Dallas COA) carries *"Do Not Publish — TEX. R. APP. P. 47.2 (b)"* and *"Under TRAP 47.7(a) … no precedential value"* (`Q-RL6-1`). A CCA-only standing rule leaves that branch unruled. (iii) `Q-DES-6` adds a limb `Q-RL6-5` lacks — *"as a cite, as support, or both"* (reg 1/1). (iv) The count correction (four named opinions) is `Q-RL6-5`'s alone (`FOUR, not five` reg 1/1). So: overlapping on the core, with one unique limb on each side.
- *Case against my proposal (ANNOTATE-KEEP):* both rows already cross-reference each other, so an annotation adds little; a reader could argue for consolidating into ONE row carrying three limbs (standing vs per-entry; CCA-only vs all unpublished; cite/support/both). That is a defensible work order — but it is a merge before any ruling, which this brief's vocabulary reaches only through `CLOSE`, which needs Michael's words. There are none.
- **(a) Shared:** standing rule on unpublished opinions supporting registry entries; TRAP 77.3 quoted; "four staged candidates"; mutual cross-reference. **(b) Unique to Q-RL6-5:** per-entry alternative; the four opinions named; the count correction; the original TRAP-47-era "citation limit" framing (now corrected). **Unique to Q-DES-6:** CCA-only scoping; the cite/support/both limb; the TRAP 47 vs 77 explanation as the row's headline. **(c) Phrase counts (reg line/flat; live):** `judged per entry` 1/1; 0 · `standing rule on whether unpublished` 1/1; 0 · `STANDING RULE that no unpublished CCA` 1/1; 0 · `as a cite, as support, or both` 1/1; 0 · `supersedes this row` 1/1; 0 · `asks the same question in weaker terms` 1/1; 0 · `FOUR, not five` 1/1; 0 · `must not be cited as authority` 3/3; 1 · `TRAP 47.7(a)` 2/3; 3 · control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a duplicate); the disposition MED.** Annotation substance: *"Rule with `Q-DES-6` as ONE question in three limbs: (1) standing rule or per-entry; (2) CCA-only (TRAP 77.3 bar) or all unpublished criminal opinions (TRAP 47.7(a) — the *Whaley* branch at `Q-RL6-1`); (3) if a rule, bar as cite, as support, or both."* Nothing on either row is ruled; "supersedes" is Claude's word.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **`Q-RL6-5`** — **Five of the nine are marked "DO NOT PUBLISH."** Registry entries built on unpublished CCA opinions carry a citation limit under the appellate rules. **Do you want a standing rule on whether unpublished opinions may support a registry entry at all**, or is that judged per entry? Source: doc 5 §7. **AMENDED 2026-08-18 (#103) — THE COUNT AND THE CONSTRAINT WERE BOTH WRONG, AND THE CORRECTION MAKES THIS ROW HARDER, NOT SOFTER.** **FOUR, not five, are "DO NOT PUBLISH"** — *Schmitt*, *Bailey*, *Simmons 2014*, *Simmons 2015*, re-derived per file (the memo's own §5 table already said four; the error propagated to four places). **And "citation limit" understates it: all nine are Court of Criminal Appeals opinions, so TRAP 77 governs, not TRAP 47 — and TRAP 77.3 reads *"Unpublished opinions have no precedential value and must not be cited as authority by counsel or by a court."* That is a BAR, not a limit**, and it reaches four staged candidates. **The standing-rule-or-per-entry question is unchanged; see `Q-DES-6`, which supersedes this row's framing.** Source: doc 4 §4, §6. — **OPEN — count and constraint corrected**
```

---

### Q-COM-10 — the UMBRELLA (L832; sub-rows `-A`…`-F` at L763–L768)

**1. Row, whole (4,403 B, one line).** Interrogatives, verbatim: *"**`Q-COM-10` — The two `privilege_tier` vocabularies still disagree, a comment calls one of them "the shared system-wide set," and a communications log would be the third consumer. Which vocabulary is correct?**"* … *"**Which set is authoritative, and does the answer come before a third consumer picks one by implementation?**"* Closure condition, in the row: *"**It closes only when he adopts the drafted unified value list** — including whether `transcripts`' `'privileged'` conforms to `'attorney-client'`, and the fourth value's exact token"* … *"**This row still closes only when you adopt a list, and it now has six sub-questions (`Q-COM-10-A`–`F`) rather than one.**"* Last annotation on the row: `#102` (2026-08-17). **The row does NOT record `Q-COM-10-A`'s `#105` ruling** (`#105` in the umbrella: 0) — the audit was right about that one fact.

Sub-rows (read whole): `-A` L763 ✅ RULED `#105` (purpose-scoped + precedence; Option 2); `-B` L764 ('privileged' → 'attorney-client', a preference not a cure); `-C` L765 (other privileges — physician-patient, spousal, clergy, mediation); `-D` L766 (one canonical `PrivilegeTier` type or two); `-E` L767 (the fourth value's token); `-F` L768 (the false `db/schema.sql:514` comment). **No sub-row names the bare umbrella ID** (`Q-COM-10\`` in each of L763–L768: 0); the umbrella names `-A` and the range `-A`–`F`.

**2. Locate.** `Q-COM-10` (any form): live **28**/32 · arch 0/0 · BS 5/9 · reg 13/32 · toc 1. The bare umbrella ID is the record's working name for the vocabulary question — e.g. the FE-D1 build session (Code, L1761): *"(`Q-COM-10` stays open, its convergence shape ruled but unexecuted)"*; `#94`'s ruling (L5478): *"(Q-COM-10 stays unruled and is not settled by implementation)"*; `#100` (L4837–4839); BUILD-STATE L79, L132, L151.

**3. Decisive text.**
- `session-log.md` — `## 2026-08-18 (#100) — FABLE ADJUDICATION SESSION: 24 items put, 24 ruled, zero deferrals —` (L4802; *"Michael LIVE throughout — every act is his word, item by item"*) — L4836–4839: *"`WS-4` — the fourth privilege_tier state enters BOTH vocabularies, **ruling Q-COM-10's direction toward one shared vocabulary** — the unified list is drafted and put to him before execution (T-30); Q-COM-10 closes only at that adoption."*
- `session-log.md` — `## 2026-08-18 (#105) — FABLE SPEND-DOWN ADJUDICATION: two items put, four limbs ruled — Q-AUDIT-1` (L4183) — L4205–4213: *"**Q-COM-10-A RULED, BOTH LIMBS.** (a) `privilege_tier` is **NOT exhaustive** … (pick verbatim: **"Purpose-scoped + precedence"**) … (b) **Option 2 — three values + a `witness_statement` boolean** (pick verbatim: **"Option 2: 3 values + boolean"**) … Q-COM-10-B through -F are unblocked (Opus work)."*
- `BUILD-STATE.md` L151: *"**`Q-COM-10`**, which closes the moment you adopt the unified `privilege_tier` list — **`Q-COM-10-A` IS RULED AND CLOSED (#105), so `B`–`F` are unblocked**"*; L132: *"`Q-COM-10` and `Q-COM-12` REMAIN YOURS"*.

**4. Whose words.** `#100`'s direction ruling (one shared vocabulary via `WS-4`) and `#105`'s `-A` ruling are Michael's, with his picks quoted verbatim. *"Q-COM-10 closes only at that adoption"* is the `#100` Fable session's statement of the closure condition, consistent with the row's own text. Nothing says the umbrella is closed or absorbed; `-B`…`-F` are OPEN.

**5. Since the hardening.** Nothing. `Q-COM-10` 0 and `privilege_tier` 0 in the first 605 log lines; BUILD-STATE at HEAD (L79, L132, L135, L151) carries the umbrella as open with `-A` ruled and `B`–`F` unblocked.

**6. DISCONFIRM — both directions.**
- *Case that the umbrella IS a duplicate of its sub-rows:* every concrete decision the umbrella says it closes on — 'privileged' → 'attorney-client' (`-B`), the token (`-E`), the type home (`-D`), the comment (`-F`), breadth (`-C`), exhaustiveness/shape (`-A`, ruled) — has its own row; the umbrella's "which set is authoritative" is answered by the sum of those. It is 4.4 KB of history plus one sentence of question, and it is stale (no `#105` note).
- *Why that fails:* (i) **The umbrella carries an interrogative no sub-row has** — *"does the answer come before a third consumer picks one by implementation?"* — a sequencing question about the communications log: `third consumer` reg 2 lines / 4 flat, and the only other line is `Q-COM-4` (L826), about `Q-WF-4`'s third consumer — a different subject; `picks one by implementation` reg 1/1, live 0; `Which set is authoritative` 1/1; `Which vocabulary is correct` 1/1. (ii) The umbrella is the record's ADDRESS for the question: 28 live-log lines, the FE-D1 build entry, `#94`'s ruling text and BUILD-STATE all say "`Q-COM-10` stays open / closes at adoption" by that name — closing it into six children orphans every one of those pointers. (iii) The audit's own §7 lists *"`Q-COM-10`'s list adoption"* as LIVE (L205–206) while its §4 calls the same row a duplicate — the audit contradicts itself. (iv) Parent/child is not duplication: the umbrella's closure is a FUNCTION of the children (its own words: *"closes only when he adopts the drafted unified value list"*).
- *Case against my proposal (ANNOTATE-KEEP):* a reader could say the "third consumer" sequencing limb is moot because the answer DID come first — `WS-4`/`-A` were ruled before any communications log exists, and the FE-D1 engine (a consumer) *"copied that table's vocabulary, renamed nothing, and reconciled nothing"* rather than picking one. That is a fair reading and belongs IN the annotation, not in a closure: the list is not adopted, and FE-D1 filing every document *unclassified* (L1761) shows the sequencing risk is live, not spent.
- **(a) Shared:** the two CHECK lists and the false `schema.sql:514` comment (`-F`); the four homes incl. the two TypeScript unions (`-D`); the fourth value (`-E`); the 'privileged'→'attorney-client' mapping (`-B`); the mutual-exclusivity finding (`-A`). **(b) Unique to the umbrella:** the "third consumer / before implementation" sequencing interrogative; the `#94` not-settled-by-`Q-COM-11` note; the `#99` 194.2(9) "loses the duty" edge; the `#100` direction ruling and closure condition; the `#102` T-30 outcome. **Unique to sub-rows:** each one's specific decision and, for `-A`, the `#105` ruling. **(c) Phrase counts:** above; control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH.** Annotation substance (dated): *"`Q-COM-10-A` RULED 2026-08-18 (#105) — purpose-scoped + precedence; Option 2, three values + `witness_statement` boolean; `-B`…`-F` UNBLOCKED, Opus lane; the authored migration must be redrafted to Option 2. This umbrella closes when the unified list is adopted (`#100`'s own condition), not before; the 'third consumer' sequencing limb stays until then — FE-D1 (2026-08-20) copied `generated_documents`' vocabulary and reconciled nothing."* The audit's "adds nothing they do not carry" is false on the grep.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **`Q-COM-10` — The two `privilege_tier` vocabularies still disagree, a comment calls one of them "the shared system-wide set," and a communications log would be the third consumer. Which vocabulary is correct?** `generated_documents` allows `('attorney-client','work-product','non-privileged')`; `transcripts` allows `('privileged','work-product','non-privileged')`; `db/schema.sql:514` says *"Privilege vocabulary is the shared system-wide set."* They do not map cleanly in either direction — `'privileged'` → `'attorney-client'` narrows and may be wrong, the reverse widens and loses which privilege. #85 flagged this; nothing has moved. **Which set is authoritative, and does the answer come before a third consumer picks one by implementation?** **ANNOTATED add-only 2026-08-16 (#94), NOT CLOSED, AND EXPRESSLY NOT SETTLED BY `Q-COM-11`'s RULING** — the ruling dropped both defaults and left both CHECK lists exactly as they were, because a NULL asserts nothing in either vocabulary. *(**RUNNER-FOUND, forty-ninth invocation: the divergence is not only in the schema — it is duplicated in TypeScript, and the two copies are separate declarations that can drift independently.** `src/domain/billing.ts:267` declares `export type PrivilegeTier = 'attorney-client' | 'work-product' | 'non-privileged'` and `src/domain/transcripts.ts:37` declares `export type PrivilegeTier = 'privileged' | 'work-product' | 'non-privileged'` — **the same type name, exported twice, with different members**, matching their respective CHECK lists. So the vocabulary question has **four** homes, not two, and whichever way it is ruled the fix touches `src/` as well as `db/`. Nothing was changed: reconciling them is this row.)* **ANNOTATED 2026-08-17 (#99), ADD-ONLY, NOT CLOSED — it gains a substantive edge it did not have as a consistency problem.** The T-22 memo finds a state **neither vocabulary can express**: a witness statement is not privileged, is not protected as work product, and is **owed as a Rule 194.2(9) initial disclosure without a request**. Classifying it `'non-privileged'` records the conclusion and **loses the duty — and the duty is the part that gets a firm sanctioned.** Rides `WS-4`. **NO SCHEMA ACT WAS PROPOSED AND NONE WAS TAKEN:** all four homes — both CHECKs and both TypeScript unions — are untouched. **ANNOTATED add-only 2026-08-18 (#100), NOT CLOSED — but its DIRECTION is now RULED.** `WS-4` puts the fourth state ("witness statement — owed as an initial disclosure") into **BOTH** vocabularies, and Michael's ruling thereby directs this row toward **ONE SHARED vocabulary**. **It closes only when he adopts the drafted unified value list** — including whether `transcripts`' `'privileged'` conforms to `'attorney-client'`, and the fourth value's exact token — which is drafted design-side at CHAT-DISPATCH v4 **T-30** together with an **authored-UNRUN** migration and the matching TypeScript union edits, staged as a proposal. **Nothing executes until he adopts the list; the migration is his to run. No CHECK, union or migration was touched this batch.** **ANNOTATED ADD-ONLY 2026-08-17 (#102), NOT CLOSED — the list is DRAFTED, and drafting it broke the ruled answer.** CHAT-DISPATCH v4 **T-30** executed and produced `docs/specs/privilege-tier-unified-vocabulary-proposal-2026-08-17.md`. **The finding that matters: the fourth `privilege_tier` value `WS-4` ruled in is NOT MUTUALLY EXCLUSIVE with the other three.** `privilege_tier` holds ONE value, but the T-28 authority (*Fontenot*/*ExxonMobil*) holds a witness statement inside a confidential attorney-client communication **is** privileged — so a client-interview recording is **both**, and a four-value enum **forces a false choice with opposite consequences**. Put as `Q-COM-10-A` with two options (four values in one column, or three plus a `witness_statement boolean`); **`WS-4`'s direction is satisfied either way.** The migration is **authored, UNRUN and NOT AUTHORIZED**, and unlike the 2026-08-16 precedent it **ABORTS at Step 0** rather than backfilling if any row holds `'privileged'`, because rewriting a privilege tier is a re-characterization and is yours. **The `src/` half is specified IN WORDS and deliberately NOT drafted** — see `Q-PR3-1`. **This row still closes only when you adopt a list, and it now has six sub-questions (`Q-COM-10-A`–`F`) rather than one.**
```

---

### Q-RE-8 (L878)

**1. Row, whole (1,050 B, one line).** Interrogatives, verbatim: *"**`Q-RE-8` — Does the professional-conduct layer become the fifth registry file, join an existing one, or stay out of the registry entirely?**"* … *"**Is that scope the intended one, in which case these propositions live somewhere else and the boundary should be written down; or is the registry the home for any proposition the software relies on, in which case a fifth file is overdue and the four passes unblock together?**"*

The "stack" the audit names: `Q-STAT-5` (L1003, 2,936 B): *"**Do you want them ruled in as registry entries now (which starts a fifth registry file, since none of the four covers insurance), held as design input until the UIM/UDJA module is actually scoped, or narrowed to the handful that CD-3 and CL-3 actually touch?** The two deadline candidates — **C-19's 61-day pre-suit notice** and **C-20's two-year limitations, extendable 180 days** — are the ones with a build consequence independent of the registry."*; `Q-QBO-6` (L861): *"**Do you want a retrieval pass run against a named authority for that layer; if so does it land in an existing registry file, in the fifth file `Q-STAT-5` already asks about, or nowhere until a money module exists to rely on it?**"* … *"**The client-property / trust retrieval this row asks for is still NOT RUN**"*; `Q-FE5-7` (L957): *"**Which of the twelve propositions in §2 become registry entries?**"*.

**2. Locate.** `Q-RE-8`: live 3/3 · arch 0/0 · BS **0/0** · reg 6/6 · toc 0. `Q-STAT-5`: live 10/10 · BS 1/1 · reg 8/12 · toc 1. `Q-QBO-6`: live 2/2 · BS 1/1 · reg 6/8. `Q-FE5-7`: live 4/4 · BS 0 · reg 4/5. Cross-naming: `Q-STAT-5` in the Q-RE-8 row 1; `Q-RE-8` in the Q-STAT-5 row 1 (the `#88` annotation). BUILD-STATE at HEAD names the shape without the ID: L116 *"(4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** — thirteen terms, all zero"*.

**3. Decisive text.**
- `BUILD-STATE.md` L116: *"**THE SAME ABSENCE HAS APPEARED FIVE TIMES.** (1) **NOT ONE PRIVACY PROPOSITION** (`Q-WF-6`) · (2) #78's insurance narrowing · (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (`Q-QBO-6`) · (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** — thirteen terms, all zero · (5) work product — **now VERIFIED, and still only three entries.** **The registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** **FOUR research passes still stack behind `Q-STAT-5`'s one ruling.**"*
- `session-log.md` `#108` (L4001–4005), quoted at FC13-Q-5 §3: TDRPC 1.04(f) ruled OPEN as *"the registry's FIRST practice-of-law proposition"*; *"the plain open was chosen over the offered boundary-ruling variant, so the cases-vs-practice boundary stays undecided."*
- `session-log.md` — `## 2026-08-16 (#88) — CHAT-DISPATCH TASK 14 + QR-5 RULED` (L6308) — L6310–6311: *"Task 14: Michael did not participate and made no rulings — all PROPOSED."* (the row's origin).

**4. Whose words.** The row is `#88`'s (Opus, Michael absent) — PROPOSED. The one Michael act that bears on it is the `#108` choice of the plain open over the boundary ruling — recorded by a Fable session; verbatim in the unstaged FC record. No ruling answers either interrogative.

**5. Since the hardening.** Nothing but `#138`'s summary (L508: *"`Q-RE-8`/`Q-STAT-5` share a BLOCKER, not a question"*). BUILD-STATE at HEAD: four passes still stack; boundary UNDECIDED (L20, L116).

**6. DISCONFIRM — both directions.**
- *Case that it IS a duplicate of the `Q-STAT-5` stack:* every row in the stack waits on one decision — fifth file for practice law, or not — and `Q-RE-8` states that decision in its widest form. One ruling there ("fifth file: yes/no; boundary: X") lets a runner execute all of `Q-STAT-5`, `Q-QBO-6`, `Q-FE5-7`, `FC13-Q-5`. The audit's "keep ONE ruling point" is administratively attractive.
- *Why that fails:* (i) `Q-STAT-5` asks how far the INSURANCE line goes (ruled in now / held / narrowed to CD-3/CL-3) and carries **C-19/C-20's deadline consequence independent of the registry** — a placement ruling does not answer it (`61-day pre-suit notice` and `extendable 180 days` live in that row alone). (ii) `Q-QBO-6` asks whether a RETRIEVAL PASS runs — an un-run act (*"still NOT RUN"*) that no placement ruling performs. (iii) `Q-FE5-7` asks WHICH of twelve specific propositions enter — a per-proposition selection. (iv) `Q-RE-8`'s boundary interrogative is unique to it: `Is that scope the intended one` 1/1; `boundary should be written down` 1/1; `home for any proposition` 1/1; `stay out of the registry entirely` 1/1; `four passes unblock together` 1/1 — all live 0. The shared sentence (*"the registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE"*, reg 4 lines) is an OBSERVATION carried onto four rows as annotation, not the question. **Shared blocker, not shared question — confirmed.**
- *Case against my proposal (ANNOTATE-KEEP):* the sheet could treat Michael's `#108` choice as `MICHAEL-IN-WORDS` — he was offered the boundary ruling and declined it, so only his word reopens it. That is the stronger label for the SITTING; I keep ANNOTATE-KEEP because the row does not yet record `#108` at all, and one of its three options ("stay out entirely") has been partly overtaken by `#108`'s ruling that a TDRPC proposition IS a registry entry. Recording that is the value; the label is secondary.
- **(a) Shared (with the stack):** the fifth-file placement blocker; the CASES-vs-PRACTICE observation. **(b) Unique to Q-RE-8:** the three-way layer question incl. "stay out entirely"; the boundary interrogative; fourteen zero-hit terms; "the four passes unblock together". **Unique to each stack member:** stated in (iii)–(iv) above. **(c) Phrase counts (reg line/flat; live):** as listed; `fifth registry file` 3/3; 1 · `fifth file` 3/3; 0 · `CASES and nothing about the law of` 4/4; 0 (BS 1) · control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a duplicate); disposition MED.** Annotation substance (dated): *"`#108` (2026-08-18): TDRPC 1.04(f) ruled OPEN as a registry entry — the first practice-of-law proposition — so 'stay out of the registry entirely' is foreclosed for that proposition; the boundary-ruling variant was offered and Michael chose the plain open, so the scope limb stays expressly UNDECIDED by his choice. `FC13-Q-5` is the per-entry placement instance; `Q-STAT-5`/`Q-QBO-6`/`Q-FE5-7` share the blocker, not the question."*

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **`Q-RE-8` — Does the professional-conduct layer become the fifth registry file, join an existing one, or stay out of the registry entirely?** Fourteen fee-sharing, referral and disciplinary terms return zero across all four registry files, and every non-zero adjacent hit was read and is unrelated. **That is the FOURTH absence of this shape** — after privacy (`Q-WF-6`), insurance (`#78`) and client property / trust accounting (`Q-QBO-6`) — and the four together describe a scope: **the registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE.** All four now stack behind `Q-STAT-5`. **Is that scope the intended one, in which case these propositions live somewhere else and the boundary should be written down; or is the registry the home for any proposition the software relies on, in which case a fifth file is overdue and the four passes unblock together?** *(That may be the right scope — but nobody chose it, and three of the four were found by accident while looking for something else.)*
```

---

### Q-FE4-2 (L946)

**1. Row, whole (711 B, one line; section "THE TASK 8 SPECS' TWENTY-TWO QUESTIONS AND THREE LOOKS — ADDED 2026-08-15, THIRTY-SIXTH queue-runner invocation").** Interrogatives, verbatim: *"**Is FE-4 IN FE-D1 — disclosures under TRCP 194 do not conventionally carry a definitions block, which argues out — or is its home the discovery slice alongside the §13 item model? Should the four already-ruled items get explicit dispositions so the FE series reads uniformly?** *(Runner note: the same question is asked from the FE-5 and FE-6 sides as Q-FE5-2 and Q-FE6-1; one ruling closes all three. `fe-d1-build-slice.md` was NOT edited — the packet's §6 bars it.)*"*

**2. Locate.** `Q-FE4-2`: live 2/2 · arch 0/0 · BS 0/0 · reg 1/1 · toc 0. `Q-FE5-2`: live 2/2 · reg 2/2. `Q-FE6-1`: live 3/3 · reg 2/2. `Q-FE7`: **0 everywhere** (register, live log, BUILD-STATE) — FE-7 has no disposition row (confirmed). Cross-naming is ONE-WAY: Q-FE4-2 names both siblings (2); neither sibling names `Q-FE4-2` (0, 0). The three IDs are packet-local (never minted — the section note: *"deliberately NOT minted … routes the series decision to `ID-DL-1`"*).

**3. Decisive text.**
- `session-log.md` — `## 2026-08-15 — QUEUE-RUNNER batch (runner line; THIRTY-SIXTH invocation)` (L7426) — L7461–7462: *"`fe-d1-build-slice.md` untouched — **no FE-4/5/6/7 disposition added**; whether the four ruled items get explicit ones is Michael's (Q-FE4-2 / Q-FE5-2 / Q-FE6-1)."*
- `session-log.md` — `## 2026-08-15 (#81) — CHAT-DISPATCH TASK 8: form-engine specs FE-4, FE-5, FE-6` (L7481; *"design session, Opus 5"*) — L7572–7577: *"`fe-d1-build-slice.md` dispositions **FE-8 through FE-17** with named homes and separately parks FE-2 — the four already-ruled items appear in **neither** list. FE-6's is nearly implicit (*"The §13 item model itself — slice 2's core; nothing in FE-D1 creates items,"* …), but it is still not an FE-6 row. **Likely because #63 scoped the unruled items and left the ruled ones alone — stated as a reading, not an error.** Q-FE4-2 / Q-FE5-2 / Q-FE6-1."*

**4. Whose words.** *"one ruling closes all three"* is the RUNNER's note (Code) — Claude's prediction of how a ruling would land, not a ruling. `#81` (Opus) says the question is *"Michael's"*. No ruling exists.

**5. Since the hardening.** No log mention (`FE-4`/`FE-5`/`FE-6`/`FE-7`, `slice 2`, `discovery slice` all 0 in the first 605 lines; `FE-D1` 22). **One thing the audits could not see and I cannot verify:** runner 85 (2026-09-01) filed `docs/specs/fe-d1-amendment-slice.md` (141,468 B) at HEAD. Whether it carries an IN/OUT list naming FE-4/5/6/7 — which would change the rows' premise *"appear in neither list"* — is UNVERIFIABLE-HERE (not staged). The sheet should have that file read before ruling this trio.

**6. DISCONFIRM — both directions.**
- *Case that the three ARE one question:* the second sentence is verbatim-identical in Q-FE4-2 and Q-FE5-2 (*"should the four already-ruled items get explicit dispositions so the FE series reads uniformly"* — reg **2** lines; `already-ruled items get explicit dispositions` reg **3** lines, all three rows) and near-identical in Q-FE6-1. A single ruling — "yes, all four get explicit dispositions: FE-4 →, FE-5 →, FE-6 →, FE-7 →; 'slice 2' = the discovery slice" — closes all three at once. That is exactly what the runner predicted.
- *Why that fails as a DUPLICATE finding:* the first limb differs per row and each carries a rationale found nowhere else — Q-FE4-2: is FE-4 IN FE-D1, with the TRCP-194-no-definitions-block argument (`do not conventionally carry a definitions block` 1/1); Q-FE5-2: is FE-5's home the discovery slice because that is where the item table it counts goes (`which is where the item table it counts is going` 1/1); Q-FE6-1: explicit-or-inferred (`explicit or inferred` 1/1) plus the naming question (`is "slice 2" the same thing as` 1/1). The audit's "Q-FE5-2 = verbatim restatement" is false — different item, different rationale, and Q-FE5-2 does not even ask the IN-FE-D1 question. One SITTING closes all three; that is not the same as one QUESTION.
- *Case against my proposal:* a consolidation into one row "FE-4/5/6/7 dispositions" carrying all four per-item limbs plus the naming limb would be cleaner and lose nothing — a fair work order for Michael, but it is a merge before a ruling (`CLOSE` requires his words) and this brief bars merging rows in my head.
- **(a) Shared:** the premise (FE-8–17 dispositioned; FE-4/5/6/7 in neither list); the "explicit dispositions so the series reads uniformly" limb. **(b) Unique to Q-FE4-2:** IN-FE-D1 vs discovery-slice for FE-4; the definitions-block argument; the runner cross-reference. **Unique to Q-FE5-2:** FE-5's home tied to the item table. **Unique to Q-FE6-1:** explicit-vs-inferred; the "slice 2" = "discovery slice" naming question. **Absent everywhere:** any FE-7 question. **(c) Phrase counts:** as above; `one ruling closes all three` 1/1; live 0 · `in neither list` 2/2; 0 · control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a duplicate).** Annotation substance: *"Rule together with `Q-FE5-2` and `Q-FE6-1` as one sitting item with four per-item limbs (FE-4, FE-5, FE-6, and FE-7 — which has no row) plus the 'slice 2' naming limb; before ruling, read `fe-d1-amendment-slice.md` (HEAD, 2026-09-01) for any IN/OUT list that already names them."*

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **Q-FE4-2.** FE-4 has no FE-D1 disposition on the record. `fe-d1-build-slice.md` dispositions FE-8 through FE-17 in or out with named homes, and FE-2 is separately parked, but FE-4 (and FE-5, FE-6, FE-7) appear in neither list. **Is FE-4 IN FE-D1 — disclosures under TRCP 194 do not conventionally carry a definitions block, which argues out — or is its home the discovery slice alongside the §13 item model? Should the four already-ruled items get explicit dispositions so the FE series reads uniformly?** *(Runner note: the same question is asked from the FE-5 and FE-6 sides as Q-FE5-2 and Q-FE6-1; one ruling closes all three. `fe-d1-build-slice.md` was NOT edited — the packet's §6 bars it.)*
```

---

### Q-FE5-2 (L952)

**1. Row, whole (396 B, one line).** Interrogative, verbatim: *"**Is FE-5's home the discovery slice — which is where the item table it counts is going — and should the four already-ruled items get explicit dispositions so the FE series reads uniformly?**"*

**2. Locate.** `Q-FE5-2`: live 2/2 (L7462, L7577 — both the Q-FE4-2/Q-FE5-2/Q-FE6-1 triplet) · arch 0/0 · BS 0/0 · reg 2/2 (its own row + Q-FE4-2's runner note) · toc 0. Does not name `Q-FE4-2` (0).

**3. Decisive text.** Same two passages as Q-FE4-2 §3 (runner 36 L7461–7462; `#81` L7572–7577). No passage addresses FE-5's home separately.

**4. Whose words.** Runner (Code) and `#81` (Opus): *"Michael's"*. No ruling.

**5. Since the hardening.** Nothing in the log; the `fe-d1-amendment-slice.md` caveat from Q-FE4-2 §5 applies.

**6. DISCONFIRM.** *For duplication:* the second limb is verbatim Q-FE4-2's, and "FE-5's home the discovery slice" is the same shape as Q-FE4-2's "or is its home the discovery slice". *Against:* Q-FE5-2 asks only about FE-5, does not ask the IN-FE-D1 question at all, and grounds FE-5's home in the item table it counts — a rationale with no counterpart in Q-FE4-2 (`which is where the item table it counts is going` reg 1/1; live 0). It is the FE-5 instance, not a restatement. *Against my proposal:* the row could simply be folded into a consolidated four-item row (see Q-FE4-2 §6); same objection — no ruling yet. **(a) Shared:** the explicit-dispositions limb (verbatim). **(b) Unique:** FE-5 / item-table rationale. **(c) Counts:** `so the FE series reads uniformly` 2/2 (this row + Q-FE4-2); `already-ruled items get explicit dispositions` 3/3; control as above.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a verbatim restatement).** Annotation: a back-reference — *"one of three per-item disposition questions (`Q-FE4-2`, `Q-FE5-2`, `Q-FE6-1`); rule together; FE-7 has no row."*

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **Q-FE5-2.** FE-5 has no FE-D1 disposition on the record. `fe-d1-build-slice.md` dispositions FE-8 through FE-17 in or out with named homes, but FE-4, FE-5, FE-6 and FE-7 appear in neither list. **Is FE-5's home the discovery slice — which is where the item table it counts is going — and should the four already-ruled items get explicit dispositions so the FE series reads uniformly?**
```

---

### Q-FE6-1 (L960)

**1. Row, whole (600 B, one line).** Interrogatives, verbatim: *"**Is FE-6's disposition explicit or inferred?** `fe-d1-build-slice.md` says *"The §13 item model itself — slice 2's core; nothing in FE-D1 creates items,"* and FE-9, FE-11 and FE-17 are all homed at "the discovery slice." Read together the record places FE-6 at the discovery slice — but there is no FE-6 row in either the IN or the OUT list … **Should the four already-ruled items get explicit dispositions, and is "slice 2" the same thing as "the discovery slice" (the two names are used in the same document and never equated)?**"*

**2. Locate.** `Q-FE6-1`: live 3/3 (L7462, L7577, L7595 — the merge line) · arch 0/0 · BS 0/0 · reg 2/2 · toc 0. Does not name `Q-FE4-2` (0).

**3. Decisive text.** Runner 36 L7461–7462 and `#81` L7572–7577 as above — `#81` itself reads FE-6's disposition as *"nearly implicit"* and *"still not an FE-6 row"* — a session's reading, expressly *"not an error"*.

**4. Whose words.** Runner and `#81` (Opus). No ruling. The `fe-d1-build-slice.md` sentence the row quotes is a spec's text, not a ruling.

**5. Since the hardening.** Nothing in the log; the `fe-d1-amendment-slice.md` caveat applies with more force here — an amendment slice is exactly where "slice 2" vs "the discovery slice" might have been equated or a FE-6 row added. UNVERIFIABLE-HERE.

**6. DISCONFIRM.** *For duplication:* the "explicit dispositions" limb is shared, and "explicit or inferred" is just the FE-6 way of asking "is FE-6 in the OUT list". *Against:* the row asks about the STATE OF THE RECORD (explicit vs inferred), not about a destination, and it alone carries the naming question — `is "slice 2" the same thing as` reg 1/1, live 0; `explicit or inferred` 1/1, live 0. The audit itself conceded this "extra limb" and proposed folding it into Q-FE4-2 — which is a merge, not a duplicate finding. *Against my proposal:* as for the siblings. **(a) Shared:** the explicit-dispositions limb. **(b) Unique:** explicit-vs-inferred; the slice-2/discovery-slice naming question; the FE-9/11/17 corroboration. **(c) Counts:** above.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH.** Annotation: back-reference to the trio; flag that the naming limb ("slice 2" = "the discovery slice"?) is the one a bridge read of `fe-d1-amendment-slice.md` might already answer.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **Q-FE6-1.** **Is FE-6's disposition explicit or inferred?** `fe-d1-build-slice.md` says *"The §13 item model itself — slice 2's core; nothing in FE-D1 creates items,"* and FE-9, FE-11 and FE-17 are all homed at "the discovery slice." Read together the record places FE-6 at the discovery slice — but there is no FE-6 row in either the IN or the OUT list, and the same is true of FE-4, FE-5 and FE-7. **Should the four already-ruled items get explicit dispositions, and is "slice 2" the same thing as "the discovery slice" (the two names are used in the same document and never equated)?**
```

---

### [Task 7 memo Q4] (L975)

**1. Row, whole (729 B, one line; section "THE TASK 7 MEMO'S SIX QUESTIONS — ADDED 2026-08-15, thirty-fifth queue-runner invocation, with NO DURABLE IDs").** Interrogative, verbatim: *"**[Task 7 memo Q4] — The rule text bearing on H43 is now supplied; the question is not answered.** H43 asked whether a Bexar party whose opponent will not confer is genuinely trapped. Local Rule 4.E supplies an express alternative certificate — *"has made reasonable efforts to confer … but has been unable to do so"* — while reserving the court's discretion to strike the setting; the 225th requires three documented attempts; the 285th bars any setting until a certificate of conference is filed. **Does that change your answer to H43, and does the heartbeat's Bexar conferral gate become a soft gate with a two-branch certificate rather than the hard block it is currently modeled as — differently per court?**"*

**2. Locate.** `Task 7 memo Q4`: live 0/0 · arch 0/0 · BS 0/0 · reg 1/1 · toc 0. Zero-control: `Task 7` live (the `#80` entry names "CHAT-DISPATCH TASK 7"); the six questions are packet-local and the log refers to them as *"packet-local Q1–Q6"* (L7645). `H43` in this row: 3.

**3. Decisive text.** See H43 block below — the same passages govern both. The row's own first sentence is decisive for the duplication question: *"the question is not answered."*

**4. Whose words.** `#80` (Opus, 2026-08-15) authored the memo's §11 text; the runner carried it verbatim. No ruling. Michael has no recorded answer to H43 in any staged file (the collision report's own status: *"(queue row, OPEN)"*).

**5. Since the hardening.** Nothing (the row's ID 0; `conferral` 0; `225th`/`285th` 0 in the first 605 lines; H43 only in `#138`'s hardening summary).

**6. DISCONFIRM — both directions.**
- *Case that Q4 IS a duplicate of H43:* Q4's first limb — *"Does that change your answer to H43"* — IS H43, restated with the rule text attached; there is no separate "answer to H43" to change. The H43 row is a bare cross-link that says nothing beyond the heartbeat question and *"see … Q4 above"*. On the interrogative alone, one ruling on H43 answers Q4's first limb, and the sheet could carry Q4's rule-text paragraph under H43 without loss — the audit's proposal.
- *Why that fails as a clean DUPLICATE:* Q4's SECOND limb has no counterpart anywhere — *"does the heartbeat's Bexar conferral gate become a soft gate with a two-branch certificate rather than the hard block it is currently modeled as — differently per court?"* — `soft gate` 1/1, `two-branch certificate` 1/1, `hard block` 1/1, `differently per court` 1/1, `three documented attempts` 1/1, `Local Rule 4.E` 1/1, `reasonable efforts to confer` 1/1 — all live **0**. That limb is the only one with a build consequence (the heartbeat's gate model), and it is not answered by an answer to H43 (a "yes, you can set unilaterally under LR 4.E" ruling still leaves how the gate is modelled per court open). The rows are H43 (the practice question) and Q4 (the practice question + the design question); overlapping, not identical.
- *Strongest partial-answer case, tested:* FC-8 (`#106`) ruled *"court-keyed deadline PROFILE + structured geography on `cases`"* — *"the key is the COURT, not the county"* (BUILD-STATE L126). That supplies the "differently per court" ARCHITECTURE Q4's second limb presupposes, so one could argue that sub-clause is answered YES. It does not touch soft-vs-hard gate or the two-branch certificate, and it is a DEADLINE-profile ruling; reading it onto the conferral gate is inference, not a ruling.
- *Case against my proposal (ANNOTATE-KEEP):* a consolidation with H43 as the ID and Q4 as the text is the tidier end state; but that is a merge, and nothing here is ruled.
- **(a) Shared:** the H43 question (can a hearing be set unilaterally in Bexar without conferral); the "Task 7 supplies rule text, does not answer" statement (both rows say it). **(b) Unique to Q4:** LR 4.E's alternative certificate; the 225th/285th variance; the soft-gate / two-branch / per-court design limb. **Unique to H43:** the cross-link identity (heartbeat register keeps H43; `#80` first-row provenance). **(c) Counts:** above; `expressly does not answer it` 1/1 (H43 row); `the question is not answered` 1/1 (Q4 row); control `QR-1` 54/102; 74.

**7. PROPOSED: `ANNOTATE-KEEP` — HIGH (not a duplicate).** Annotation substance: *"Rule with `H43` as one sitting item — H43 is the practice question; this row adds the rule text and the gate-model limb. FC-8 (`#106`) ruled the deadline profile COURT-keyed ('the key is the COURT, not the county'), which supplies the per-court architecture but rules neither H43 nor the soft-gate/two-branch question."*

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **[Task 7 memo Q4] — The rule text bearing on H43 is now supplied; the question is not answered.** H43 asked whether a Bexar party whose opponent will not confer is genuinely trapped. Local Rule 4.E supplies an express alternative certificate — *"has made reasonable efforts to confer … but has been unable to do so"* — while reserving the court's discretion to strike the setting; the 225th requires three documented attempts; the 285th bars any setting until a certificate of conference is filed. **Does that change your answer to H43, and does the heartbeat's Bexar conferral gate become a soft gate with a two-branch certificate rather than the hard block it is currently modeled as — differently per court?**
```

---

### H43 (L978) — the H43-α / H43-β enumeration

**1. Row, whole (504 B on the line; rows.tsv's 1,859 B includes the unmarked "Two concrete looks" bullet at L979).** Verbatim: *"**H43 — Can a hearing be set unilaterally in Bexar if the opponent will not confer? CARRIED AND OPEN; given a row here for the FIRST time, 2026-08-15 (#80).** It lives in the heartbeat register (H1–H83) and had **no row in this file at all** — the same gap the Q-3 sync-picker item had at #46 — so it is registered here **cross-linked, not renamed**; the heartbeat register keeps H43. **Task 7 supplies the rule text and expressly does not answer it** — see the memo's §5.2 and Q4 above."*

**2. Locate.** `H43`: live 6/8 · arch 0/0 · BS 1/1 · reg 3/6 · toc 1. Every hit read: register L3 (Status header: *"**H43 given a row here for the first time**, cross-linked to the heartbeat register rather than renamed"*), L975 (Q4), L978 (this row); live L516–517 (`#138`), L2698 & L2701 (runner 72), L7645–7647 (`#80`), L7731 (runner 35); BUILD-STATE L126; toc L445 (runner 35's abstract). **`H43` occurs NOWHERE in `#106` (L4102–L4159) — line-anchored and flattened, 0.**

**3. Decisive text.**
- `session-log.md` — `## 2026-08-18 (#106) — FABLE FC-BLOCK ADJUDICATION: fifteen FC items put one at a time, THIRTEEN` (L4102) — L4121–4123, the whole of FC-8's compressed ruling: *"FC-8 court-keyed deadline PROFILE + structured geography on `cases` when a schema act is next authorized (no migration authorized)"*. Nothing about conferral, hearings, settings, or certificates.
- `BUILD-STATE.md` L126 (the `#80` memo summary): *"**A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**) — **which FC-8's court-keyed profile answers by ruling: the key is the COURT, not the county.**"* — this is the ONLY place in the staged corpus where the `H43` tag sits on the profile-key proposition.
- `session-log.md` — `## 2026-08-15 (#80) — CHAT-DISPATCH TASK 7: Bexar local rules + eFileTexas` (L7666) — L7697–7700, the source of both: *"**THE VARIANCE IS THE FINDING, NOT A CAVEAT.** The 225th and 285th District Courts **have elected out of the Presiding and Monitoring Court dockets** — two scheduling regimes inside one county, so a court-profile keyed to COUNTY is wrong for Bexar civil on its face. Conferral is discretionary-strike under LR 4.E, three-documented-attempts in the 225th, and an absolute bar on setting in the 285th."*
- Runner 72 (L2701, 2026-08-19, carrying `#117`'s §7): *"`H43`, **flagged not renamed** (the FC record attaches it to the court-keyed proposition while the queue's `H43` row asks whether a Bexar hearing can be set unilaterally — one ID, two propositions, already in the record)"*.
- `id-collision-report.md` L278: *"| `H43` | heartbeat capture f: *"Can a hearing be set unilaterally in Bexar if they will not confer …"* (queue row, OPEN) | — | `deadline-engine-spec.md`, `bexar-local-rules…md` cite the same H43 → same subject, not a collision | — |"* — the report's status column for H43 is OPEN; it enumerates no second sense.
- Hardened §4.1 (L162–165): H43-α OPEN; H43-β *"RULED at `#106` via FC-8"*; *"**`FC-8` does not touch H43-α** — the string `H43` appears nowhere in `#106`"*.

**4. Whose words.** FC-8 is Michael's ruling (`#106`: *"Michael's word per item, verbatim"* in the unstaged FC record). The `(H43)` tag on the profile-key proposition is BUILD-STATE's (a runner rewrite summarising `#80`); "one ID, two propositions" is `#117`'s packet §7 (Opus, *"adjudicates nothing"*) carried by runner 72; "same subject, not a collision" is the collision report's (Claude, 2026-08-24/25). None of those is a ruling. **No ruling in the staged files answers H43-α.**

**5. Since the hardening.** Only `#138`'s restatement (L516–517: *"the `H43` warning was right while the companion collision report is WRONG — it cleared `H43` as not a collision when the live log had already recorded "one ID, two propositions.""*). BUILD-STATE at HEAD (L126) unchanged.

**6. Verification of the enumeration, adversarially.**
- **Confirmed:** the register's H43 row asks a conferral/setting question; FC-8 as recorded in `#106` rules a deadline-profile key and geography; the `H43` string does not appear in `#106`; nothing in any staged file rules H43-α. **Closing the register's H43 row on "FC-8 ruled it at `#106`" would destroy an open item — CONFIRMED.**
- **Where the hardened audit overstates:** whether there are "two propositions on one ID" or "one subject with a ruled design consequence" depends on the unstaged FC record (*"the FC record attaches it to the court-keyed proposition"* — UNVERIFIABLE-HERE). In the staged corpus the `H43` tag on the profile-key sentence appears exactly once (BUILD-STATE L126), inside a summary of the same `#80` paragraph that carries the conferral finding — i.e. β is a design consequence derived from the α research, labelled with α's ID. The collision report's "same subject" is therefore not simply WRONG; it is a different description of the same fact pattern. **The operative conclusion is identical under either description**, which is what the sheet needs.
- **Strongest case the other way (FC-8 DOES reach the row):** Q4's *"differently per court"* clause is answered in architecture by *"the key is the COURT, not the county"*. That reaches Q4's second limb at most partially and reaches H43-α not at all.
- **Ancillary:** `case-heartbeat-design.md` (the heartbeat register) is not staged — H43's heartbeat-side text is known only through the collision report's quotation (*"…if they will not confer …"*). UNVERIFIABLE-HERE beyond that.

**7. PROPOSED: `KEEP` — HIGH.** H43-α is open; no ruling touches it; the audit's DUPLICATE pairing with Q4 is an overlap (Q4 = H43 + rule text + a gate-model limb), and its warning *"`FC-8` (`#106`) ruled the *other* `H43` proposition"* is the correct caution. Sitting note: rule H43 and Q4 together.

**8. Row first line (exact; `grep -c -F -x` = 1):**
```
- ⬜ **H43 — Can a hearing be set unilaterally in Bexar if the opponent will not confer? CARRIED AND OPEN; given a row here for the FIRST time, 2026-08-15 (#80).** It lives in the heartbeat register (H1–H83) and had **no row in this file at all** — the same gap the Q-3 sync-picker item had at #46 — so it is registered here **cross-linked, not renamed**; the heartbeat register keeps H43. **Task 7 supplies the rule text and expressly does not answer it** — see the memo's §5.2 and Q4 above.
```

---

## SUMMARY

| ID | audit class | hardened verdict | PROPOSED | confidence | destroys-unique-text? (Y/N) |
|---|---|---|---|---|---|
| `FC13-Q-5` (L432) | DUPLICATE of `Q-RE-8` | OVERLAPPING | `KEEP` | HIGH | **Y** — option (c) hold, the entry-specific anchor, the candidate filename (reg 1/1 each) |
| `V5-IDS` (L606) | DUPLICATE of `Q-T19-1` | OVERLAPPING | `ANNOTATE-KEEP` (rule with `Q-T19-1` as one policy question, two instances) | HIGH not-dup / MED disposition | **Y** — the file-shape finding (`shaped two different ways` reg 1/1, live 0) unless carried |
| `WESTLAW-5` (L613) | DUPLICATE of `Q-RL6-1/-2` | ⛔ DIFFERENT-QUESTIONS | `CLOSE-SPLIT` (act limb satisfied at `#99`; residual 30/31 already rowed at `Q-RL6-1`/`-2`, cross-reference, no re-mint; row text retained) | MED | **Y** — `no public source could reach` reg 1/1 live 0; `Lang, J` reg 1/1 |
| `Q-RL6-5` (L761) | DUPLICATE of `Q-DES-6` | OVERLAPPING (closest) | `ANNOTATE-KEEP` (one question, three limbs, with `Q-DES-6`) | HIGH not-dup / MED disposition | **Y** — `judged per entry` reg 1/1; the non-CCA (TRAP 47.7(a), *Whaley*) branch |
| `Q-COM-10` umbrella (L832) | DUPLICATE of `-A`…`-F` | OVERLAPPING (parent/child) | `ANNOTATE-KEEP` (record `-A` ruled `#105`; closes at list adoption per `#100`) | HIGH | **Y** — `picks one by implementation` reg 1/1; the umbrella is the log's address (28 lines) |
| `Q-RE-8` (L878) | DUPLICATE of the `Q-STAT-5` stack | ⛔ DIFFERENT-QUESTIONS | `ANNOTATE-KEEP` (record `#108`: TDRPC 1.04(f) ruled IN; boundary ruling offered and declined) | HIGH not-dup / MED disposition | **Y** — the boundary interrogative (`Is that scope the intended one`, `boundary should be written down` reg 1/1, live 0) |
| `Q-FE4-2` (L946) | DUPLICATE (survivor) | OVERLAPPING (1 of 3 limbs) | `ANNOTATE-KEEP` (rule the trio together; FE-7 has no row; read `fe-d1-amendment-slice.md` first) | HIGH | N for this row as survivor; **Y** for the siblings' limbs if folded uncarried |
| `Q-FE5-2` (L952) | DUPLICATE | OVERLAPPING (not verbatim) | `ANNOTATE-KEEP` (back-reference) | HIGH | **Y** — `which is where the item table it counts is going` reg 1/1 |
| `Q-FE6-1` (L960) | DUPLICATE | OVERLAPPING | `ANNOTATE-KEEP` (back-reference; naming limb flagged) | HIGH | **Y** — `explicit or inferred`, `is "slice 2" the same thing as` reg 1/1 |
| `[Task 7 memo Q4]` (L975) | DUPLICATE of `H43` | OVERLAPPING | `ANNOTATE-KEEP` (rule with `H43`; FC-8 supplies per-court architecture only) | HIGH | **Y** — `soft gate`, `two-branch certificate`, `differently per court`, `Local Rule 4.E` reg 1/1, live 0 |
| `H43` (L978) | (enumeration check) | H43-α OPEN / H43-β RULED via FC-8 | `KEEP` — closing on FC-8 would destroy an open item: CONFIRMED | HIGH | **Y** — the only register row carrying the heartbeat question by its ID |

**Net finding.** The hardened audit's headline — *"not one is a true duplicate"* — SURVIVES on the text at HEAD for all ten pairs; for every pair the audit's fold would delete at least one interrogative phrase that occurs exactly once in the register (control `QR-1` fires 54/102 times). Two of the hardened verdicts could be stated more carefully: (i) `V5-IDS` — `#98`'s deferral names `Q-T19-1`, not `V5-IDS`; the "deferred by Michael's word" on V5-IDS is by extension; (ii) `H43` — whether β is a "second proposition" or a "ruled consequence of the same subject" turns on the unstaged FC record, but the operative conclusion (FC-8 does not rule the register's row) holds either way. Nothing since the hardening (`#139`, `#140`, runners 83–85, v28) touches any of the eleven; the one thing the audits could not see and I could not read is `fe-d1-amendment-slice.md` at HEAD, which bears on the FE trio's premise.

**Could not fully process:** none of the eleven. UNVERIFIABLE-HERE items (not staged): the verbatim words of Michael's `D-6` deferral (`task-19-signoff-record-2026-08-17.md` §4); his verbatim FC-8 and FC-13 words and the FC record's use of `H43` (`fc-adjudication-record-2026-08-18.md`); the heartbeat register's own H43 text (`case-heartbeat-design.md`); `fe-d1-build-slice.md` and `fe-d1-amendment-slice.md` IN/OUT lists.
