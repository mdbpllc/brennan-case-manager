# VERIFY-V1 — mechanical verification of the cleanup adjudication sheet
**Scope:** header, §0, §1, §2 Blocks A–D of `/home/claude/work/sheet/attorney-review-queue-cleanup-adjudication-2026-09-01.md`.
**Verifier:** Opus 5, read-only, mechanical. **Adjudicates nothing** — no disposition is assessed for correctness, only whether the sheet's evidence is what the sources say.
**Sources (all staged, byte-identical from HEAD `7f02131`):** `REG` = `docs/specs/attorney-review-queue.md` (sha256 `5427b901…3cd15d`, 1,283 lines) · `LOG` = `docs/record/session-log.md` (9,278 lines) · `ARCH` = `docs/archive/session-log-archive-2026-07-21_2026-08-12.md` (6,090 lines) · `TOC` = `docs/record/session-log-toc.md` · `BS` = `docs/specs/BUILD-STATE.md` · `AUD` = the 2026-08-24 audit · `HARD` = the 2026-08-25 hardening · `COLL` = `id-collision-report.md` · `A1`–`A8` = the eight wave-1 verifier reports at `out/`.
**Method:** every fragment located by NFKC-normalized, emphasis-stripped, whitespace-flattened substring search across all eleven sources at once (harness at `scratchpad/find.py`); zero results re-run line-anchored with `grep -c` plus a firing positive control. A line-join bug in the first harness build (double space at wrapped-line boundaries) was found and fixed before any NOT-FOUND was recorded; every zero below was re-run after the fix.
**Non-staged sources:** the project-instructions field (v28) is readable in this session but is not a staged repo file; fragments sourced to it are marked `VERBATIM-NON-STAGED`. No `.git` directory was staged, so SHA/remote claims are `UNVERIFIABLE-HERE`.

---

## Table

| # | Location in sheet | Fragment / claim | Verdict | Detail |
|---|---|---|---|---|
| 1 | Header | `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01.md` (canonical path) | N/A | Prospective path for an unfiled file; nothing to verify. |
| 2 | Header | Pinned to HEAD `7f02131f215605b3062a8351c2b7ec96f46c004c` (batch 85) | UNVERIFIABLE-HERE / MATCH (batch) | No `.git` staged, so the SHA cannot be checked. "batch 85" MATCHES: the EIGHTY-FIFTH invocation runner line is the newest entry in the staged log, `LOG:20`, dated 2026-09-01. (Its own Step 0 gate records the *pre*-batch remote at `ed1ddf6…`, consistent with `7f02131` being the batch-85 commit.) |
| 3 | Header | `origin/master` == `HEAD` by `git ls-remote`; `inbox/` empty of zips | UNVERIFIABLE-HERE | Session-runtime facts; no repo metadata staged. |
| 4 | Header | **487 marked rows = 356 `⬜` + 126 `✅` + 5 `🟡`**, counted with the sheet's own stated regex | MATCH | Re-ran the sheet's own regex on the staged REG: 487 / 356 / 126 / 5. Exact on all four figures. (Contrast HARD §7 at `edea20b`: "486 marked rows = 357 ⬜ + 124 ✅ + 5 🟡" — different HEAD, no conflict.) |
| 5 | Header | "register line numbers below are AT `7f02131`" | MATCH | All 42 in-scope `L`-anchors resolve to the named row in the staged REG: 59, 71, 112, 133, 172, 185, 186, 187, 188, 213, 214, 228, 229, 282, 288, 388, 391, 400, 409, 410, 467, 472, 473, 518, 605, 606, 607, 613, 650, 667, 684, 745, 767, 777, 809, 840–842, 882, 888, 990, 1001, 1004, 1248, 1249. Zero drift. |
| 6 | §0.1 | "11-agent Fable fleet + Opus consolidation, at `edea20b`" | MATCH | AUD header: *"the **eleven-agent** audit fleet ran on **Fable 5** … this consolidation is **Opus 5**"*; Source line `edea20b8e7ee082903293562d2a24c13c0eea881`. *(Note, not a sheet defect: HARD §0 says "nine auditors"; the two source documents disagree and the sheet follows the audit's own words.)* |
| 7 | §0.1 | "classified all 362 then-open rows" | MATCH | AUD §1: *"362 open rows (357 ⬜ + 5 🟡) were read individually."* |
| 8 | §0.1 | "found 108 not open rulings" | MATCH | AUD §1: *"**108 of 362 rows — 30% — are not open rulings.**"* |
| 9 | §0.1 | "**UNTRACKED at HEAD — it was never filed**" | MATCH | HARD §0: *"`git ls-files` returns nothing for it. No commit contains it… **It was never filed.**"* |
| 10 | §0.1 | "(Michael ruled DO NOTHING, batch 83)" | MATCH | `LOG:387`, inside the EIGHTY-THIRD invocation (heading `LOG:371`): *"He was asked in session and ruled **DO NOTHING**."* |
| 11 | §0.2 | "(Opus, five checkers, at `edea20b`)" | MATCH | HARD §7: *"Five independent checkers"*; header *"**Opus 5, execution class**"*, source `edea20b…`. |
| 12 | §0.2 | "of the 58 closure-class candidates, **19 verify clean, 39 need Michael's eye**" | MATCH | HARD §1 table: **TOTAL closure-class 58 / 19 / 39**. Independently: 27 + 21 + 10 = 58. |
| 13 | §0.2 | "11 would destroy an open question if acted on as written" | MATCH | HARD §1: *"**eleven of them would destroy an open question if acted on as written.**"* |
| 14 | §0.2 | "not one of the ten "duplicates" is one question in two rows" | VERBATIM | HARD `L57`: *"**NOT ONE OF THE TEN "DUPLICATES" IS ONE QUESTION IN TWO ROWS.**"* |
| 15 | §0.2 | *"No adversarial verify pass ran on THIS file either… the adjudicative half is still owed and is Fable-class."* | VERBATIM | `HARD:250`. Source: *"**No adversarial verify pass ran on THIS file either.** It is the evidentiary half; the adjudicative half is still owed and is Fable-class."* The `…` elides ". It is the evidentiary half;" — elision marked, nothing reversed. |
| 16 | §0.3 | "eight read-only verifiers, all eight returned content" | MATCH | Eight files present at `out/wave1_A1..A8.md`, 56,314–101,116 B, all substantive. |
| 17 | §0.3 | "the `#104` dead-fleet check passed" | MATCH | Established record terminology, used identically at `LOG:76` (inside `#140`): *"and returned (the `#104` dead-fleet caveat did not apply)"*; origin at `LOG:3677`: *"where a dead fleet returns an empty finding list wearing the costume of a clean bill of health."* |
| 18 | §0.3 | "every one of the 108 candidates re-verified at `7f02131`" | MATCH (derived) | Annex slice inventory reconciles to the audit's classes: A1 (10, incl. the unclassified `FE-8`) + A2 (9) + A3 (9) = the 27 RULED-BUT-OPEN; A4 (11) + A5 (10) = the 21 SUPERSEDED; A6 = the 10 DUPLICATE; A7 covers the 12 HANDS-ON; A8 = the 38 ACTION rows. 27+21+10+12+38 = 108. |
| 19 | §0.3 | "Wave 2 (**the ~247 open rows** no closure-class audit tested)" | **MISMATCH** | See full report below. |
| 20 | §0.3 | "launched on Fable and DIED AT THE WEEKLY LIMIT — eight agents, zero output; rerouted the same hour to an Opus session per §7.2" | UNVERIFIABLE-HERE | Session-runtime event; no staged artefact records it. |
| 21 | §0.3 | "Three batches landed between the hardening and this sheet (`#138`–`#140`, runners 83–85)" | MATCH | `LOG:394` `#138` (2026-08-25), `LOG:230` `#139` (2026-08-31), `LOG:54` `#140` (2026-08-31); runner lines `LOG:371` (83rd, 2026-08-31), `LOG:200` (84th, 2026-08-31), `LOG:20` (85th, 2026-09-01). |
| 22 | §0.3 | "the hardening's own headline PREMISE-INTACT finding" (v23-paste row) | MATCH | HARD §3, row "468 — v23 paste": *"**⛔ PREMISE-INTACT.** The most consequential reversal in this file."* |
| 23 | §0.3 | "which v28 (pasted 2026-08-31) has since dissolved" | MATCH | `LOG:64` (`#140`) + `BS:142` + `BS:152` — see rows 60–63. |
| 24 | §0 (annexes) | "OUTSIDE the design-side sync by the standing `/docs/record/` exclusion (TC-4's rule)" | MATCH | TC-4 / the `docs/record/` exclusion is standing law per the project instructions and `#132` (`LOG:1709`, "THE THIN CONSTITUTION EXECUTED: the live log LEAVES the sync for `docs/record/`"). |
| 25 | §1 | "(the `SK-v2` / `RECON-1` closure shape, batches 83/84)" | MATCH | `LOG:384` (batch 83): *"**ROW 3 — `SK-v2` CLOSED** … the leading `⬜` flipped to `✅`"*; `LOG:212` (batch 84): `RECON-1` *"needed closing by hand"*. Both rows now `✅` in REG (`L1242`, `L474`). |
| 26 | A-1 `FE-7` | "What does the queue look like pre-engine, and what does adoption look like?" | VERBATIM | `REG:188`, inside the FE-7 row's own bold question. |
| 27 | A-1 | *"all four candidates ENTER §13.4 … Entering ≠ adopting; FE-7 guardrails hold"* — ruled `#63` | VERBATIM | `ARCH:303–304`, inside `#63` (heading `ARCH:268`). Ellipsis elides the four candidate names. Attribution to `#63` correct. |
| 28 | A-1 | "ruled **#53**" | MATCH | `ARCH:1192–1195` (`#53`, heading `ARCH:1171`): *"FE-7 RULED: pre-engine distillation queue is a live table in form-engine.md §13.4…"* |
| 29 | A-1 | "Guardrail text survives **verbatim** in the ✅ row at L213" | **MISMATCH** | See full report below. |
| 30 | A-1 | "Zero distillation activity since." | MATCH | `distill` (case-insensitive): LOG **0**, BS **0**; REG only at rows 188/197/202/213. Positive control `FE-7` in LOG = 2. |
| 31 | A-2 `DL-INPUT` | *"all rulings Michael's … by widget or direct words"* — `#66` | VERBATIM | `LOG:9022`, in the `#66` heading block (`LOG:9021`). Ellipsis elides ", 2026-08-13 Central,". |
| 32 | A-2 | *"the deadline model is PER-(CASE, PARTY) — each party's response clock computes from its own service date."* | VERBATIM | `LOG:9095–9096` inside `#66`: *"DL-INPUT RULED: the deadline model is PER-(CASE, PARTY) — each party's response clock / computes from its own service date."* Also carried at `REG:288`. |
| 33 | A-2 | "`#75`'s refinement is PROPOSED elaboration, not contradiction" | MATCH | `LOG:8326` inside `#75` (heading `LOG:8318`-region): *"THE MODEL, answering what DL-INPUT left open: the chain is per-defendant at both ends and case-wide in [the middle]"*; HARD `L79` states it as PROPOSED and unruled. |
| 34 | A-2 | "the pass's open questions live at `[DL-memo Q1]–[Q5]`/`ID-DL-1`" | MATCH | `REG:1010–1014` are `[DL-memo Q1]`…`[Q5]`; `REG:1009` is `ID-DL-1`. All six are `⬜` rows. |
| 35 | A-2 | Closure text "ruled ≠ authorized: no deadline engine is in the build queue." | MATCH (sheet-authored, fact checks out) | Proposed closure wording, not a source quote; the operative fact is verbatim at `BS:125`: *"**No deadline engine is in the build queue.**"* |
| 36 | A-3 `H22` | "**ruled #66**: verification interleaves BY BUILD URGENCY" | MATCH | `LOG:9049` inside `#66`: *"H22 RULED: registry verification interleaves BY BUILD URGENCY — whatever a ruled slice needs is verified first, regardless of series."* |
| 37 | A-3 | "the OTHER `H22` (disclosures, warn-never-block, `#130`)" | MATCH | `LOG:1466` inside `#130` (heading `LOG:1450`): *"**H22 — CONFIRMED.** The practice-name check … **warns and never blocks**."* |
| 38 | A-3 | "is now `HD-22` **and rowless**" | MATCH | `REG:313–314` records the ruled renumber (`H1`→`HD-1` … `H22`→`HD-22`). No register row carries an `HD-` ID (row index scan: zero); `HD-22` appears only in REG prose at `L3`/`L314`. |
| 39 | A-4 OAA | *"DEMOGRAPHICS RULED … CAPTURES BOTH … Cause-number-pending reminder RULED YES"* — `#66` | VERBATIM | `LOG:9041–9044` inside `#66`. Both ellipses elide only intervening clauses. |
| 40 | A-4 | the row's own text says *"the question CLOSES."* | VERBATIM | `REG:133`, tail: *"…Annotated add-only on the spec-feedback entry the same batch; the question CLOSES."* |
| 41 | A-5 `PF-2` | *"Adopt as standing"* — ruled `#126` | VERBATIM | `LOG:1810` inside `#126` (heading `LOG:1790`): *"**The `#118` proposal — "Adopt as standing."**"* |
| 42 | A-5 | Runner 79's own words: *"one word closes it in the next batch."* | VERBATIM | `LOG:1784`, inside the SEVENTY-NINTH invocation runner line (heading `LOG:1771`). Attribution to runner 79 correct. |
| 43 | A-5 | "now the RE-SWEEP AFTER FIX convention in instructions v24+/v28" | MATCH / VERBATIM-NON-STAGED | v24 shipped the same day as `#126` (heading `LOG:1790`: "v24 ships the same day trigger 3 fired"); the convention is present in the live v28 instructions field ("RE-SWEEP AFTER FIX (ruled 2026-08-20…)"). Instructions field is not a staged repo file. |
| 44 | A-6 `V-5` | *"V-5 — SPLIT ALL THREE two-case entries, one entry per case"* — ruled `#73` | VERBATIM | `LOG:8526` inside `#73` (heading `LOG:8511`): *"**V-5 — SPLIT ALL THREE two-case entries**, one entry per case."* |
| 45 | A-6 | "EXECUTED (51st invocation; entries 19a/19b/20a/20b/21a/21b exist)" | MATCH | `LOG:5238` heading: *"FIFTY-FIRST invocation … executed V-5, V-6 and V-7 under eleven separate rulings"*; `LOG:5246`: *"Result: 30 → 33 `##` entries."* All six IDs named at `REG:604/606/607/611`, `LOG:5107/5147/5189/5288`, `BS:105–110`. *(Registry files unstaged: their in-file existence is UNVERIFIABLE-HERE; the record names them consistently.)* |
| 46 | A-6 | "The audit quoted a runner heading, not the ruling — the hardening caught that" | MATCH | `AUD:78` quotes *"executed V-5, V-6 and V-7 under eleven separate rulings"* — which is the 51st-invocation **heading** at `LOG:5238`. HARD `L97`: *"The quoted sentence is a **queue-runner batch HEADING** from the 51st invocation, not a ruling and not from `#73`."* |
| 47 | A-6 | "Residue has rows: `V5-IDS` (L606), `V5-ATTRIB` (L607-area)" | MATCH | `REG:606` = `V5-IDS` ⬜; `REG:607` = `V5-ATTRIB` ⬜. |
| 48 | A-7 `V-6` | *"BOTH STAY, REWORDED to state their operative tests"* — ruled `#73` | VERBATIM | `LOG:8533` inside `#73`: *"**V-6 — BOTH STAY, REWORDED to state their operative tests.**"* |
| 49 | A-7 | "entries 33/34 VERIFIED 2026-08-17" | MATCH | `LOG:5106–5107` inside `#98` (heading `LOG:5098`): *"**24 ENTRIES VERIFIED** (verified-by Michael, 2026-08-17): … criminal 28, **33, 34**…"* *(BS `L108`'s "verified 08-17: 5, 6, 11, 27" is a different partition — the Q-STAT-6 divergence dispositions — and lists 33/34 under "Flags CURED"; no conflict.)* |
| 50 | A-7 | "Residue at `Q-T19-4`" | MATCH | `REG:612` is the `Q-T19-4` ⬜ row (27.18(a) exclusivity), *"Raised at the walk, expressly NOT ruled."* |
| 51 | A-8 `T-26` | *"T-26 RAN, LIVE, AND CLOSED … ENTRIES 13, 2 AND 29 VERIFIED — his word, dated 2026-08-18"* | VERBATIM | `LOG:3968–3970` inside `#108` (heading `LOG:3952`). Ellipsis elides the Group A entry list and source note. |
| 52 | A-8 | "successor act had its own row, closed #110" | MATCH | `REG:746`: *"**CLOSED 2026-08-18 (#110) — COMPLETED, ALL FIVE VERIFIED…**"*; corroborated at `REG:3` ("Reconciled again to session-log #110 … the sixty-third batch's ID-less successor row CLOSED"). |
| 53 | A-8 | The hardening: "the cleanest close in the set." | VERBATIM | `HARD:83`: *"**The cleanest close in the set.**"* |
| 54 | A-9 `Q-AUTH-1` | *"BOTH READ IN FULL, BOTH POSITIVELY IDENTIFIED UNDER V-9's SECOND LIMB"* — EXECUTED `#117` | VERBATIM | `LOG:2843–2844` inside `#117` (heading `LOG:2705`): *"**`Q-AUTH-1` IS EXECUTED: *EX PARTE PHARR* AND *HURLBURT v. STATE*, BOTH READ IN FULL, BOTH POSITIVELY IDENTIFIED UNDER V-9's SECOND LIMB**."* |
| 55 | A-9 | "authorized #106" · "Residue rowed at `Q-RL6-1`/`-2`" · "`Q-PH-1..5` rowless BY his ruling" | MATCH | `#106` = `LOG:4102`. `Q-RL6-1` at `REG:757`/`REG:778` and named in the WESTLAW-5 row `REG:613`. `LOG:2696`: *"PACKET-LOCAL QUESTIONS TOOK NO DURABLE IDs AND NO ROWS, **ON HIS RULING**"* — naming `Q-PH-1`–`Q-PH-5`. |
| 56 | A-10 Fam. Code | *"I am not going to be practicing family law… Delete it."* — ruled `#9` | VERBATIM | `ARCH:4516–4517` inside `#9` (heading `ARCH:4480`). The `…` is the source's own. |
| 57 | A-10 | "The row has been struck-through-but-⬜ since July." | MATCH | `REG:59`: `- ⬜ ~~**Tex. Fam. Code …**~~ — **REMOVED FROM THE QUEUE 2026-07-26.**` Glyph `⬜`, strike-through present, July date. |
| 58 | A-11 carry | The carry's own condition ("rides whichever session next amends form-engine.md") | **PARAPHRASE** | See full report below. |
| 59 | A-11 | *"REQ-10 FOLD EXECUTED … placeholder discipline enters form-engine.md as new §14"* — executed `#63` | VERBATIM | `ARCH:301–302` inside `#63`. Ellipsis elides "(the carry confirmed 08-12 closes):". |
| 60 | A-11 | BUILD-STATE's stale "CONFIRMED AS CARRY" line | MATCH | `BS:154`: *"**REQ-10 placeholder discipline: CONFIRMED AS CARRY.**"* One occurrence in BS. |
| 61 | A-12 `WS-P1` | *"STAYS OPEN until a design session verifies it from the live config"* | VERBATIM | `REG:388`, the row's own closing condition. |
| 62 | A-12 | "met at #123: the sync filters, read via the API after his click, carry `/docs/reference/`: "exclude"" | MATCH (note) | `LOG:2134` inside `#123` (heading `LOG:2130`): *"sync filters, read via the API after his **change**, now carry `/docs/reference/": "exclude"`"*. The only quoted element — `"exclude"` — is verbatim. Unquoted prose says "click" where the source says "change"; substantively the same act (a picker click), so recorded as a note rather than a defect. |
| 63 | A-13 `O-8` | *"~~OPEN~~ CLOSED 2026-08-19: Michael upgraded the database the same night."* | VERBATIM | `REG:409`, in the row's own tail. |
| 64 | A-14 `O-9` | "row reads MOOT (*"answered by never having to be decided"*)" | **PARAPHRASE** | See full report below. |
| 65 | A-15 `TC-OPEN-5` | "#133 correction 2 records the mis-ordered click and the 72.6% landing" | MATCH | `LOG:1258` ("correction 2") and `LOG:1266`/`1269` ("72.6%"), both inside `#133` (heading `LOG:1225`). |
| 66 | A-16 `TC-OPEN-6` | "done 2026-08-21; closure records #133 correction 4 (destination = his machine, not ARCHIVE)" | MATCH | `LOG:1307` ("correction 4") inside `#133`; `LOG:1316`: *"makes instructions v26's start-of-session rule 4 wrong where it says relocated captures…"* |
| 67 | A-17 `TC-OPEN-7` | "The v26 paste — done and superseded (v27, then v28 in force per `#140`)" | MATCH | `LOG:1386`: *"v26 the same day, and **v27** is now pending"*; `LOG:341–347` (`#139`) treats v27 as in force; `LOG:64` (`#140`) records v28 in force. |
| 68 | A-18 `RE-LOOK-3` | "#106: six documents acquired mid-session; `FC-14` ruled the channel" | MATCH | `LOG:4131–4136` inside `#106`: *"**FC-14 / trigger #3: SOURCING gains a FOURTH NAMED CHANNEL** … Michael acquired **six documents** by hand mid-session."* Corroborated at `BS:144`. |
| 69 | A-18 | "`#112` read the TDRPC over the bridge" | MATCH | `LOG:3637` and `LOG:3688` inside `#112` (heading `LOG:3625`): *"TDRPC 1.04(f) from the State Bar PDF, FC-14's fourth channel"*; *"glued-`AA` measured 0 in both the TRCP and TDRPC PDFs."* |
| 70 | A-19 migration | "run clean, THIRD, 2026-08-19 (#113)" | MATCH | `#113` = `LOG:3446`-region heading *"EXECUTION SESSION: all three pending live migrations RUN AND VERIFIED"*; row is `REG:1249` (`2026-08-16-privilege-tier-no-default.sql`). |
| 71 | A-20 v23 row | `#139`: trigger #6 fired on the v23 loss; Michael's "Yes" to v28 | VERBATIM/MATCH | `LOG:236–237` (`#139` heading): *"instructions trigger #6 fired on the v23 loss and v28 drafted"*; `LOG:345`: *"Michael: **"Yes"** to a v28 draft. **v28 was assembled tonight from the verbatim fragments at**…"* |
| 72 | A-20 | `#140`: "the v28 instructions IN FORCE (read live in the project-instructions field)" | **PARAPHRASE** | See full report below. |
| 73 | A-20 | BUILD-STATE at HEAD: "~~PASTE v28~~ — REPORTED DONE" | VERBATIM | `BS:152`: *"**~~PASTE v28~~ — REPORTED DONE: `#140`'s design session read the instructions field live on 2026-08-31…**"* |
| 74 | A-20 | "THE HAND ITEM IS THEREFORE OFF THE LIST." | VERBATIM | `BS:142`. |
| 75 | A-20 | names BUILD-STATE's **one** stale contrary line ("rides the v23 paste") | VERBATIM + MATCH (count) | `BS:144`: *"**SOURCING itself is NOT amended in any repo file: the convention lives in the project instructions and the amendment rides the v23 paste.**"* Exactly **one** occurrence in BS (a second, separate occurrence sits in REG at `L879`, inside a CLOSED row — the sheet's count is scoped to BUILD-STATE and is right). |
| 76 | Block A close | "from the SUPERSEDED set … **none beyond the rows above**" | MATCH | HARD §3 "Clean — 11" = Family Code · placeholder carry · WS-P1 · O-8 · O-9 · TC-OPEN-5/-6/-7 · RE-LOOK-3 · SK-v2 · the 2026-08-16 migration. Ten are A-10…A-19; the eleventh, `SK-v2`, is already `✅` at `REG:1242` (closed batch 83) and so is not an open row. |
| 77 | B-1 `FE-4` | *"definitions are case-level versioned objects — immutable versions … prospective propagation"* (§13.1) — `#53` | VERBATIM | `ARCH:1179–1180` inside `#53`. Ellipsis elides "every rendered instrument stamps the version used (the house stamping pattern);". "§13.1" matches *"Full design: form-engine.md §13.1."* |
| 78 | B-1 | "The record's "ruled ≠ built, row stays ⬜" posture" | **PARAPHRASE** | See full report below. |
| 79 | B-1 | "is **runner 36's and #81's (Claude), never his** — wave 1 re-attributed both sentences (annex A1 §S-2; the hardening had swapped the cites)" | MATCH (annex spot-checked against primary) | A1 §S-2 exists and says exactly this. Spot-check confirms it against the primary source: *"THE FE ROWS STAY OPEN … a spec is not a closure"* is at `LOG:7465–7466`, inside the **THIRTY-SIXTH invocation** runner line (heading `LOG:7425`); *"They are open because the **BUILD** is gated, not because the design question is unanswered"* is at `LOG:7491–7492`, inside **`#81`** (heading `LOG:7481`). HARD §2 had them the other way round (attributing the first to `#81` and the second to runner 35). Both are Claude/runner text; neither is Michael's. |
| 80 | B-1 | "Residue all rowed (`Q-FE4-1..6`; build-home at `Q-FE4-2`)" | MATCH | `REG:945–950` = `Q-FE4-1` … `Q-FE4-6`, all `⬜`; `REG:946` (`Q-FE4-2`) is the FE-D1-disposition/home row. |
| 81 | B-1 | the register's #114 scope line ("the complete inventory of open RULINGS") | VERBATIM | `REG:11` (also `REG:3`): *"…would otherwise take it as the complete inventory of open items: **it is the complete inventory of open RULINGS.**"* The line names `#114` and is dated "ruled 2026-08-19". |
| 82 | B-2 `FE-6` | "#53 (§13.3: standalone instruments REPEAT the definitions block; separate-mode layout derived; one COS per document)" | MATCH (faithful paraphrase, not presented as a quote) | `ARCH:1187–1191` (`#53`): *"FE-6 RULED: packaging is a render-time choice from one item model; each standalone instrument **REPEATS the definitions block in full** … **separate-mode layout derived from the evidenced combined layout, one COS per document**, verification rides interrogatories only. §13.3."* No quote marks in the sheet, and each of the three clauses is present. |
| 83 | B-2 | "seven `Q-FE6-1..7` rows hold every residue" | MATCH | `REG:960–966` = `Q-FE6-1` … `Q-FE6-7`, seven rows, all `⬜`. |
| 84 | B-2 | no item table exists (BS: "NO ITEM TABLE") | VERBATIM | `BS:99`: *"**NO ITEM TABLE, NO FE-9/11/13/14/16/17.**"* |
| 85 | B-3 `CD-1` | *"`parties` IS the contact directory"* — `#51` | VERBATIM | `ARCH:1378` inside `#51` (heading `ARCH:1371`-region, *"CD-1 SCHEMA SESSION: every CD-1 question RULED"*). |
| 86 | B-3 | "built #61, **COMPLETE #113**" | MATCH | `ARCH:387` = `#61` *"CD-1 DIRECTORY BUILD"*; `BS:29`: *"**ITEM 7 RAN 2026-08-19 AND IS VERIFIED — CD-1 IS COMPLETE (#113).**"* |
| 87 | B-3 | *"This entry stays open as the living-spec pointer"* exists ONLY in the register row — not in `#51` (wave 1: 0 hits both logs) | VERBATIM + MATCH (count) | `REG:228` carries it. Flattened search of both logs for "stays open as the living" = **0 / 0**; positive control `CD-1` = 74 (LOG) / 97 (ARCH). The one `living-spec` hit in LOG (`L6696`, `#86`) is a different sentence about the deferred ladder pass. |
| 88 | B-3 | "the hardening's "part of the same #51 ruling"" | VERBATIM | `HARD:95`: *"But the row's open status **is part of the same `#51` ruling**."* |
| 89 | B-3 | "Living-spec revisits demonstrably take their own IDs (`CD-3`…`CD-13`, `CD-14`)" | MATCH (note) | `CD-3`…`CD-13` are register rows at `REG:231, 235–244`. `CD-14` is an ID minted prospectively at `#140` (`LOG:107–108`, *"CD-14 are MINTED by the act of authorization"*) and has no register row yet — the sheet's claim is about IDs, not rows, and holds. |
| 90 | B-4 `P-COM-*` | "**#94** (entries created), **VERIFIED by him #98** (2026-08-17)" | MATCH | `LOG:5106–5107` (`#98`): *"24 ENTRIES VERIFIED (verified-by Michael, 2026-08-17): … work-product **WP-1/2/3**"*. HARD `L127`: *"What `#94` created were entries labelled **`WP-1/WP-2/WP-3`**."* |
| 91 | B-4 | "(zero `P-COM-2` hits in either log — hardening confirmed)" | **MISMATCH** | See full report below. |
| 92 | B-4 | "*(Registry files unstaged — the WP↔P-COM wording identity is UNVERIFIABLE-HERE…)*" | MATCH (self-flag correct) | No `legal-rule-registry-*.md` is in the staged set; the sheet's own UNVERIFIABLE-HERE marking is accurate. |
| 93 | B-5 `TOC-5` | "never ruled by name — superseded by events (TC-3/TC-4 at #132…)" | MATCH | `LOG:1709` (`#132`): *"**THE THIN CONSTITUTION EXECUTED:** the live log LEAVES the sync for `docs/record/`, the index SPLITS…"*. HARD `L119`: *"**`TOC-5` is never named in `#132` or later.**"* |
| 94 | B-5 | The toc's own footer still says "remains Michael's and unruled" | VERBATIM | `TOC:517` (file is 525 lines, so the closing note): *"**`TOC-5`** — whether a file this size should sync into project knowledge at all — **remains Michael's and unruled**."* |
| 95 | B-6 `Q-PR3-3` | "#113: CD-1 ran first; backfill step (c) matched ZERO rows; *"the failure did not occur"*" | VERBATIM | `LOG:3452–3453` inside `#113`: *"**Backfill step (c) matched zero rows, so no row took that derivation and the failure did not occur.**"* (Sheet's unquoted "ZERO" vs source "zero" — case only.) |
| 96 | B-6 | *"Whether anything remains in it is Michael's; the row is annotated, not closed"* — the `#113` session's sentence | VERBATIM | `LOG:3453–3454`. The sentence sits in the session's own narration with no attribution to Michael, so the sheet's "the #113 session's sentence, not his words" is supported by the surrounding text. *(HARD §3.1 files it under "deferred-in-words"; that is a reading of what the sentence reserves, not a claim that Michael spoke it. The disagreement is adjudicative and outside this pass.)* |
| 97 | B-6 | "Residue = `Q-PR3-2` (its own row)" | MATCH | `REG:887` = `Q-PR3-2` ⬜. |
| 98 | B-7 `Q-STAT-6` | BS: *"Q-STAT-6's EIGHTEEN ARE NOW NONE LIVE, AND THE ROW STILL STAYS OPEN"* | VERBATIM | `BS:108`. |
| 99 | B-7 | "the general residue … is `READ-A`'s and the `#108` direction's territory" | MATCH | `REG:605` (`READ-A` ⬜) and `LOG:3947` / `BS:108` (the `#108` verbatim-drafting direction). |
| 100 | B-8 `Q-STAT-3` | "premise dissolved (#107 retired the capture…)" | MATCH | `LOG:4048` inside `#107` (heading `LOG:4041`): *"RULED (Michael): retire all session captures dated ≤ 2026-08-16 (42 docs)…"* — the row's capture is `claude_Authority_Corpus_and_eCFR_Method_**2026-08-14**.md` (`REG:1001`), inside that range. |
| 101 | B-8 | "The capture is RELOCATED, not deleted — bridge-reachable on his machine (v27/v28 rule 4)" | MATCH | `LOG:4050–4051`: *"All 44 zipped losslessly with a manifest … byte-verified against the pre-deletion inventory"*; destination corrected to his machine at `#133` correction 4 (`LOG:1307`-region). |
| 102 | B-9 email row | "**ruled #63** *as recorded constraints, not buildable items*" | VERBATIM | `REG:282`, the row's own parenthetical: *"(RULED 2026-08-12 as recorded constraints, not buildable items)"*. HARD `L106` offset-verifies it inside `#63`. |
| 103 | B-9 | "Three of four constraints exist nowhere else in the staged record" | MATCH | Flattened counts across REG/LOG/ARCH/BS/TOC — "highest-value function" 1/0/0/0/0; "envelope number, claim number, document name" 1/0/0/0/0; "hard ignore layer" 1/0/0/0/0; the fourth ("NEVER templated automation" / "human routing") also occurs once in ARCH. Exactly three of four are register-unique. Control `dedupe` = 10/13/7. |
| 104 | B-9 | "the `#68` precedent" (glyph-less record line) | UNVERIFIABLE-HERE | `#68` is below the live log's floor and not in the staged archive's index range for this shape; no staged file states the precedent in those terms. Not contradicted — simply not locatable in the staged set. |
| 105 | C-1 `FE-5` | *"escalates to a warning ONLY when worst-case crosses the cap **while the numbered count does not**"* — `#53` | VERBATIM | `ARCH:1184–1185` inside `#53`. Bold is the sheet's emphasis. |
| 106 | C-1 | "the clause the 08-24 audit truncated, as the hardening caught" | MATCH | `AUD:67` stops at *"crosses the cap"*; `HARD:86`: *"**Quote truncated at the load-bearing point.**"* |
| 107 | C-1 | `#81` scope findings: pair-keyed cap accumulating across waves; Level 3 has no cap, so `cap = f(level)` is structurally wrong | MATCH | `REG:186` carries all three findings verbatim; `cap = f(level)` at `REG:186`, `LOG:7520` (`#81`), `HARD:86`. |
| 108 | C-1 | "(`Q-FE5-1`, `Q-FE5-9` already have rows and stay.)" | MATCH | Both are `Q-FE5-*` rows in the `REG:951–959` block. |
| 109 | C-2 `CD-2` | "#51 (both layers: roster-as-data; typed directional edges), built #61, migration #113" | MATCH (sheet's own labels, not quotes) | Neither phrase is a source string (0 hits anywhere) and neither is presented as one. The `#51` CD-2 ruling and the `#61`/`#113` build-and-migration chain check out (rows 85–86 above). |
| 110 | C-2 | "Limb (e), PROPOSED at #64 and never put … (spec-level act, CD-3 precedent)" | MATCH | `REG:229`: *"**(e) ADDED 2026-08-12 (#64): carrier-ID and financial-responsibility TYPE codes from the CR-3 code sheet are directory-vocabulary CANDIDATES** — PROPOSED only… **Adding directory vocabulary is a spec-level act on the living spec (the CD-3 precedent), so nothing was added.**"* |
| 111 | C-2 | "Limb (b) (verification-officer tracking, "someday")" | VERBATIM | `REG:229`: *"(b) *verification-officer tracking* (deficiency capture REQ-11, priority **someday**)"*. |
| 112 | C-3 `V-7` | **#73** (*"Rejected: V7-23-CITE — entry 23's cite stays § 37.001 et seq."*) | **MISMATCH (cite)** | See full report below. |
| 113 | C-3 | "executed; entry 23 VERIFIED" | MATCH | `LOG:5107` (`#98`): *"UIM/pleading **23**, 25, 26, 27"* among the 24 verified 2026-08-17. |
| 114 | C-3 | "The only open home of: *"entry 24's proposition has never been located inside the Devine opinion"*" | VERBATIM + MATCH (uniqueness) | Phrase occurs in three register rows: `V-7` (row at `REG:684`, text at `L700`, glyph `🟡` = open), `V-9` (row `REG:714`, `✅`), `WB2-2` (row `REG:795`, `✅`). `V-7` is the only non-closed row carrying it. |
| 115 | C-4 `OPEN-5` | limb (a) ruled **#82** (A-2, anchor form) | MATCH | `LOG:7387` inside `#82` (heading `LOG:7374`): *"**A-2 RULED: option (a), the anchor form (AUD-5 class).**"* |
| 116 | C-4 | and **#94** (*"Yes — ride the v9 amendment"*) | VERBATIM | `LOG:5473–5474` inside `#94` (heading `LOG:5447`): *"OPEN-5(a) RIDES THE SAME AMENDMENT by Michael's word ("Yes — ride the v9 amendment")"*. Also at `REG:990`. |
| 117 | C-4 | "the five stale pointers were removed by the 37th invocation" | MATCH | `LOG:7338–7339` (THIRTY-SEVENTH invocation, heading `LOG:7317`): *"Removed: **AUD-1, AUD-5, AUD-6, AUD-7, AUD-8, AUD-9**."* The "five" is the `AUD-5..9` set the row itself names; `AUD-1` is a separate, sixth pointer outside that set. |
| 118 | C-4 | #94's own words: *"OPEN-5(b)… REMAIN OPEN"* | VERBATIM | `LOG:5475` inside `#94`: *"**OPEN-5(b) and the AUD-5..9 one-word apply were deliberately NOT put and REMAIN OPEN.**"* Ellipsis elides "and the AUD-5..9 one-word apply were deliberately NOT put and". |
| 119 | C-4 | limb (b) "absent from v28" | MATCH / VERBATIM-NON-STAGED | The live v28 instructions' start-of-session steps (1–6) contain no instructions-version comparison. Instructions field is not a staged repo file; the sheet marks this class of read as attributed. |
| 120 | C-5 `Q-COM-10-E` | "`Q-COM-10-A` ruled #105 (Option 2: THREE values plus a `witness_statement` boolean — there is no fourth value)" | MATCH | `LOG:4208–4210` inside `#105` (heading `LOG:4183`): *"(b) **Option 2 — three values + a `witness_statement` boolean** (pick verbatim: **"Option 2: 3 values + boolean"**)"*. |
| 121 | C-5 | "(`witness_statement` is Claude's option text at #105)" | MATCH | Michael's verbatim pick at `LOG:4209` is *"Option 2: 3 values + boolean"* — the token does not appear in his words; it is in the option description Claude wrote. |
| 122 | C-5 | "#105's runner expressly declined to close the row — "governs ≠ answers"" | **PARAPHRASE** | See full report below. |
| 123 | C-6 `WESTLAW-5` | "the five Westlaw pulls **done and over-delivered (#99)**" | MATCH | "over-delivered" occurs at `REG:613` (the row) and `LOG:4891`; `LOG:4891` sits inside `#99`. |
| 124 | C-6 | entry 17's *"MEMORANDUM OPINION, Nov. 30, 2017, Lang, J."* | VERBATIM | `REG:613` — one occurrence in the register, in the `WESTLAW-5` row (matching HARD `L60`: *"`Lang, J` occurs **once in the whole 635 KB file**"*). |
| 125 | C-6 | entry 16's *"pet. denied"* that no public source could reach | VERBATIM | `REG:613` carries both the phrase and the row's *"no public source could reach"*. |
| 126 | C-6 | "the residual cite supply for entries 30/31 already lives at `Q-RL6-1`/`-2`" | MATCH | `REG:757` (`Q-RL6-1`) and the `Q-RL6-*` block; `BS:109`: *"**30 and 31 are cite-less and their cites are yours.**"* |
| 127 | D-1 `WS-P4` | **"Defer."** (register annotation #108) | VERBATIM | `REG:391`: *"**ANNOTATED 2026-08-18 (#108), ADD-ONLY — his word: "Defer."**"* |
| 128 | D-1 | log #108 *"WS-P4 deferred"* | VERBATIM | `LOG:4008` inside `#108` (heading `LOG:3952`). |
| 129 | D-1 | "**v28's trigger-4 history still omits 2026-08-18** (read from the v28 text in this session, labelled a non-staged observation)" | MATCH / VERBATIM-NON-STAGED | The live v28 trigger-4 history names 2026-08-07, 2026-08-20 and 2026-08-21 only; 2026-08-18 (the `#107` 44-file retirement) is absent. The sheet's own non-staged labelling is accurate. |
| 130 | D-2 `READ-A` | "deferred #95" | MATCH | `REG:605`: *"**READ-A — NEW 2026-08-16 (#95). Michael's, expressly DEFERRED rather than answered.**"* |
| 131 | D-2 | v28's ROUTE-C bullet carries *"Reading A's SCOPE remains expressly DEFERRED … silence is not a ruling on the rest."* | VERBATIM-NON-STAGED | Verbatim in the live v28 instructions ROUTE-C bullet: *"Reading A's SCOPE remains expressly DEFERRED (put #93, deferred #95): the conservative default governs — it covers entries 1–3 only — and silence is not a ruling on the rest."* Not present in any staged repo file (0 hits REG/LOG/ARCH/BS); corroborated for v27 at `HARD:120`. |
| 132 | D-2 | The audit's superseding ground ("the #108 direction now governs every entry") | **PARAPHRASE** | See full report below. |
| 133 | D-2 | "FALSE at HEAD three ways (BS: *"a ruled DIRECTION, not a binding convention … applying it to any EXISTING entry is a per-entry ROUTE-C act and also yours"*)" | VERBATIM | `BS:108` carries both clauses; the ellipsis elides intervening text. Also at `LOG:3947` and `HARD:120`. |
| 134 | D-2 | "Stake re-stated at today's numbers: backlog 47, 12 unverified." | MATCH | `BS:102` heading: *"The registry — FOUR files, **backlog 47**, THIRTY-FIVE VERIFIED"* (47 − 35 = 12); stated directly at `LOG:3833`: *"backlog 47 entries, 35 VERIFIED / **12 UNVERIFIED**."* |
| 135 | D-3 `PR-3` | **"execution HELD"** (#34) | VERBATIM | `ARCH:2754` inside `#34` (heading `ARCH:2730`): *"**PR-3 — direction CONFIRMED per V17, execution HELD.**"* Also echoed in `#35` (`ARCH:2658`, `2710`) and at `REG:72`. |
| 136 | D-3 | "**he deferred pending Domser**" | MATCH | `REG:73`: *"the gating design pass is now **DEFERRED pending the Domser matter**."* |
| 137 | D-3 | "Eleven children exist" | MATCH | `PL-1`…`PL-4` at `REG:296–299` (4) + `Q-PR3-1`…`Q-PR3-7` at `REG:886–892` (7) = 11. Consistent with HARD `L90` ("All 11 children exist"). |
| 138 | D-3 | "`D-CL1-3` (L172) and `O-2` (L400) gate on PR-3 BY NAME" | VERBATIM/MATCH | `REG:172`: *"**D-CL1-3 is gated on PR-3 alone.**"* `REG:400`: *"**Rides the PR-3 ruling already in this queue.**"* |
| 139 | D-3 | "the unblock condition exists nowhere else" | MATCH (register-scoped) | The condition is at `REG:73` (*"the **letters-issued arc capture** from the Domser matter, plus a rescheduled pass. **Checkpoints are at LETTERS and CLOSING.**"*). "Checkpoints are at LETTERS and CLOSING" = 1 REG / 0 LOG / 0 ARCH / 0 BS. "letters-issued arc capture" = 1 REG / 1 LOG (`L6696`, `#86` — a report of the same hold, not a second home) / 0 ARCH / 0 BS. Control `Domser` = 8/2/9. |
| 140 | D-3 | "(hardening's PARTIAL + wave-1 confirmed: destroys-unique-text = Y)" | MATCH | HARD §2 rates `PR-3` **PARTIAL** and states the orphaning of `D-CL1-3`; A3 records the same. |
| 141 | D-3 | "annotated with the #113 events (backfill limb SPENT; row ANNOTATED NOT CLOSED)" | MATCH | `LOG:3453`: *"The backfill limb of `Q-PR3-3` is spent… the row is annotated, not closed."* |

---

## Verdict counts

| Verdict | Count |
|---|---|
| **VERBATIM** — located in the named source, character-exact modulo marked ellipsis and emphasis markup | **46** |
| **MATCH** — numeric or entry-cite claim re-run against the staged files and confirmed | **76** |
| **VERBATIM-NON-STAGED** — verbatim in the live project-instructions v28 field, which is not a staged repo file (the sheet labels each of these as an attributed non-staged read) | **4** |
| **PARAPHRASE** | **6** |
| **MISMATCH** | **4** |
| **UNVERIFIABLE-HERE** | **3** (+1 partially confirmed: the batch-85 half of the HEAD pin) |
| **N/A** — not a source claim | **1** |
| **NOT-FOUND** | **0** |
| **Total rows** | **141** |

One VERBATIM row (#79) is a **VERBATIM-VIA-ANNEX** chain: the sheet cites annex A1 §S-2, and that annex's cites were spot-checked against the primary source and confirmed.

No fragment in scope failed to be located somewhere in the staged set. Every zero result was re-run flattened with a firing positive control before being resolved.

---

## Full report — every PARAPHRASE / NOT-FOUND / MISMATCH row

### MISMATCH 1 — §0.3, "Wave 2 (**the ~247 open rows** no closure-class audit tested)"
**True values.** The register at the sheet's own pinned HEAD carries **361 open rows** (356 `⬜` + 5 `🟡`, the sheet's own header figures). 361 − 108 candidates = **253**. The other natural derivation is the audit's own LIVE class, **254** (`AUD` §1 table). **~247 matches neither**, and no derivation is stated on the sheet or in any annex. The sheet hedges with "~", and §6 repeats the figure as "the ~247 LIVE rows"; the substantive impact is nil, but the number as printed is not reproducible from the staged files.

### MISMATCH 2 — A-1 `FE-7`, "Guardrail text survives **verbatim** in the ✅ row at L213"
**What the sources say.** FE-7's guardrail text at `REG:188` reads: *"Guardrails (in from birth): distillation output is client-clean BY CONSTRUCTION — the no-real-client-data rule is why distillation is a deliberate separate step, never "save as template" — and distilled templates are PROPOSED artifacts until Michael adopts them as house standard."*
`REG:213` (the `✅` row) reads: *"**Entering is not adopting:** each is `queued`, none distilled, none format-authoritative, and **FE-7's guardrails hold** — adoption is a separate explicit ruling of yours, and adoption is what flips a template's FE-12 provenance to format-authoritative."*
**Delta.** The `✅` row **restates** the guardrails in different words; it does not reproduce FE-7's guardrail sentence. Even the narrowest reading fails a literal test: `#63`'s *"FE-7 guardrails hold"* appears at `L213` as *"FE-7's guardrails hold"* (possessive added), and *"Entering ≠ adopting"* appears as *"Entering is not adopting"*. The substance genuinely survives — the disposition's premise (nothing unique is destroyed by the glyph flip) is unaffected — but the word **"verbatim"** overstates what `L213` carries. The annex is more careful: A1 §7 says only "the guardrails **survive** in the ✅ row at L213".

### MISMATCH 3 — B-4 `P-COM-2/-3/-4`, "(zero `P-COM-2` hits in either log — hardening confirmed)"
**True value at `7f02131`: live log = 1, archive = 0.** The single hit is `LOG:511`, inside `#138` (heading `LOG:394`, the 2026-08-25 design session that filed the hardening's findings): *"**`P-COM-2`/`-3`/`-4` return ZERO in both logs.**"*
**Why it matters and why it does not.** The hardening's own count was taken at `edea20b`, before `#138` was written; the finding then entered the log and became its own counter-example. The substantive point stands unchanged — no ruling anywhere names `P-COM-2`, and the only hit is a report *of the zero*. But the sheet asserts character-exact re-verification at `7f02131`, and at that HEAD the flat statement "zero … in either log" is one hit off. Controls: `P-COM-1` = 3 LOG / 0 ARCH; `P-COM-` (any) = 6 LOG / 0 ARCH; `WP-1` = 5 LOG / 0 ARCH. Flattened re-run gives the same figures.

### MISMATCH 4 — C-3 `V-7`, cite: **#73** for *"Rejected: V7-23-CITE — entry 23's cite stays § 37.001 et seq."*
**The quote is verbatim — in a different entry than the one named.** It sits at `LOG:5245`, inside the **FIFTY-FIRST invocation queue-runner line** (heading `LOG:5238`, 2026-08-17): *"**ELEVEN ITEMS WERE PUT TO MICHAEL AT THE STEP 1 STOP AND ALL ELEVEN WERE RULED** … **Rejected: `V7-23-CITE`** — entry 23's cite **stays `§ 37.001 et seq.`** No open row was entered for it: it is **ruled, not awaiting a ruling.**"*
**`#73` does not contain it.** `#73` spans `LOG:8511–8561`; a scan of that range returns **no** occurrence of `V7-23` and **no** occurrence of `Rejected`. `#73`'s actual V-7 ruling is `LOG:8539–8542`: *"**V-7 — NARROW ENTRY 23** to what CPRC § 37.001 et seq. actually provides; the availability holding lives on the *Irwin* entry, cross-referenced."*
**Provenance of the error.** The mis-cite is inherited from `AUD:80`, which pairs the same quote with "`#73`, executed"; `HARD` rated that row PARTIAL on other grounds and did not catch the cite. Two things keep this from being fatal to the disposition: the sentence **is** a ruling of Michael's (put at the Step 1 STOP, one at a time per ROUTE-C), and the substance — V7-23-CITE rejected, entry 23's cite unchanged — is corroborated at `REG:604` and `LOG:5315`. But the sheet flags exactly this defect class at A-6 ("The audit quoted a runner heading, not the ruling — the hardening caught that") while reproducing a sibling of it here. The correct cite is the 51st-invocation runner line at `LOG:5245`, ruling `#73`-adjacent but not `#73`.

### PARAPHRASE 1 — A-11, The carry's own condition ("rides whichever session next amends form-engine.md")
**Actual text, `REG:214`:** *"**CONFIRMED AS CARRY by Michael 2026-08-12:** it rides whichever **future** session next amends `form-engine.md` — likely the engine's slice-scoping session."*
**Delta:** the word **"future"** is dropped from inside the quotation marks, with no ellipsis. Meaning unchanged.

### PARAPHRASE 2 — A-14 `O-9`, "row reads MOOT (*"answered by never having to be decided"*)"
**The quoted phrase is not the row's.** `REG:410` reads: *"— ~~OPEN~~ **MOOT 2026-08-19: `O-8` was answered by upgrading, so both halves landed together and the split never had to be decided.**"*
The quoted words are verbatim at `LOG:3741`, inside the **SIXTY-FIFTH invocation** runner line (heading `LOG:3715`): *"**Both halves landed together, so `O-9` — whether the version-independent F-18 CHECK could be split off — was answered by never having to be decided.**"*
**Delta:** the parenthetical is presented as the row's text ("row reads MOOT (…)"); it is the runner line's. The row does read MOOT, and its own words say the same thing, so the disposition is untouched — only the attribution of the quoted string is wrong.

### PARAPHRASE 3 — A-20, `#140`: "the v28 instructions IN FORCE (read live in the project-instructions field)"
**Actual text, `LOG:64`:** *"…index lock, the v28 instructions IN FORCE (read live in the project-instructions field — BUILD-STATE's "whether the paste…"*
**Delta:** the sheet closes the parenthesis where the source continues with an em-dash and further text. A truncation presented as a complete parenthetical, with a closing `)` the source does not have at that point. No meaning is altered.

### PARAPHRASE 4 — B-1 `FE-4`, The record's "ruled ≠ built, row stays ⬜" posture
**Not a source string.** Zero hits for the phrase as written in REG, LOG, ARCH, BS, TOC, AUD, HARD or COLL. Its components exist separately: *"Ruled ≠ built"* is the **wave-1 annex's own shorthand** (A1 §S-1: *"'Ruled ≠ built' survives"*; A1 §7: *"a Claude/runner 'ruled ≠ built' convention"*), and the glyph half is `BS:127`: *"**All three rows stay ⬜.**"* / `REG:186`: *"**The row stays ⬜.**"*
**Delta:** a composite label rendered in quotation marks as though it were a quoted posture. The attribution the sentence carries (runner 36 and `#81`, both Claude) is separately correct and independently verified — see table row 79.

### PARAPHRASE 5 — C-5 `Q-COM-10-E`, "#105's runner expressly declined to close the row — "governs ≠ answers""
**"governs ≠ answers" is the hardening's phrase, not the runner's.** It occurs at `HARD:125` (*"**Governs ≠ answers.**"*) and nowhere in either log (0 hits both, flattened; control `none closed` = 9 LOG hits). The runner sentence the hardening was characterising is at `LOG:4177`, inside `#105`: *"`Q-COM-10-B`–`F` were annotated add-only as UNBLOCKED; **none closed**."*
**Delta:** the sheet places the hardening's own epigram immediately after "#105's runner expressly declined", where it reads as the runner's words. The underlying claim (the `#105` runner declined to close the row) is verbatim-supported at `LOG:4177`.

### PARAPHRASE 6 — D-2 `READ-A`, The audit's superseding ground ("the #108 direction now governs every entry")
**Actual text, `AUD:111`:** *"the `#108` **verbatim-drafting** direction now governs every entry and the row's stake (Q-STAT-6's eighteen) is spent."*
**Delta:** **"verbatim-drafting"** is dropped from inside the quotation marks with no ellipsis. `HARD:120` quotes the same sentence with the words intact. Meaning unchanged; the modifier is the part that names *which* `#108` direction is meant.

---

## Two notes that are not findings against the sheet

1. **Annex cite typo, spot-checked.** A1's decisive-text block cites the `#63` DISTILLATION ruling at "ARCH L1303–1304". The text is in fact at `ARCH:303–304`; `ARCH:1303–1304` holds the FE-2 re-parking and SK-v2 authoring lines. The annex's own `#63` heading cite (`ARCH:268`) is right and is inconsistent with its body cite, so this reads as a stray leading digit. The quoted sentence itself is verbatim and correctly attributed to `#63`; the sheet does not carry the line number, so nothing propagates. A1's other ARCH cites checked in this pass (`L1192–1195` for `#53`, `L1147–1149` for runner 15) are correct.
2. **A disagreement between the two source audits, which the sheet resolves silently in the audit's favour.** `AUD`'s header says the fleet was **eleven** agents; `HARD:22` says *"On 2026-08-24 **nine** auditors classified 362 open rows."* The sheet's §0.1 says "11-agent Fable fleet", following the audit's own words. Recorded so the discrepancy is visible; it is not the sheet's error.
