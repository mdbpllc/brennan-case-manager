# RUNNING LEDGER — 2026-09-03 (Central), the continuation of the 2026-09-02 late sitting's chat — typed, Cowork, Fable 5 per the environment
# Every ruling in Michael's words, within the exchange it was made. The 2026-09-02 portion of this ledger is filed at docs/record/fe-d1a-1-authorization-2026-09-02/ruling-ledger-2026-09-02-late.md.

## BATCH 88 RUNNER REPORT — pasted by Michael into this design chat (2026-09-03, after the packet ran to the commit hold)
- Runner (Code, Opus per §7.2) reports: identity + Step 0 gate re-checked; §4.6 evidence filed, hashes match; three-act merge done, closed rows conserved at 130; BUILD-STATE rewritten under both caps (99,857 B / 150 non-blank; CAP-4 bound — eleven displaced originals in the runner line); log prepended; TOC regenerated (batch 87's stale census/arithmetic/edition repaired forward); head file regenerated with the 200 KB ceiling BINDING — two whole entries dropped, head reaches back to #144; held at the commit pending adversarial verification. HEAD and origin still cae00ba.
- Runner's flagged RULING (Michael's): the register Status paragraph's batch-87 descriptive block (split counts) is now falsified by the three flips; §4.4 default "leave as it stands" was followed and flagged; strict CAP-3 §3.3 says intro + current sentence only.
- Runner's operational finding: `git checkout` re-materialized three LF record files as CRLF (core.autocrlf=true, no .gitattributes); sizes grew by exactly the line count; converted back, verified byte-identical. Candidate operational note for the next instructions revision (not a trigger).
- Design-side observation (Claude, PROPOSED): CAP-4 displacement text in the runner line inflated the head file past its ceiling → head lost two entries (three design entries, not TC-2's four). BUILD-STATE will displace every batch from here. Proposal: displaced text → EVIDENCE file under docs/record/<batch-slug>/, runner line carries a pointer — a CAP-4 amendment (trigger 3 if ruled).

### RULINGS on the batch-88 report — 2026-09-03 Central (Michael's picks verbatim; option texts Claude's; alternatives named)
- STATUS BLOCK: "Move it to the closed register now (Recommended)" — the synced Status paragraph keeps intro + the CURRENT reconcile sentence only (strict CAP-3 §3.3, an APPLICATION of the ruled convention, not a change — no trigger 3); the batch-87 descriptive block moves VERBATIM to the closed register's RECONCILE HISTORY beside batch 87's superseded sentence; executed in THIS batch (88) as a packet-added act on Michael's in-session authorization (QR-6(e)); the runner line names it. Alternatives not taken: keep-and-refresh each batch; leave and queue a row.
- CAP-4 AMENDMENT PROPOSAL (displaced BUILD-STATE text → EVIDENCE file under docs/record/, runner line carries a pointer): "Carry it to the next design packet (Recommended)" — PROPOSED, unruled; the next design session puts it with alternatives and the measured numbers (eleven originals; head file −2 entries, reaching #144). Not taken: mint the row now; drop it.

## BUILD SESSION REPORT — pasted by Michael (the FE-D1 amendment slice, first tranche; Opus, Code) — verified over the bridge
- HEAD aa132f2 = local origin ref (bridge read); batch 88 landed at 7d57421 (its Status-block act executed: 'first batch under' 0 in synced / 1 in closed; FE-D1A-1 ✅ in the closed register, line 78; synced Status paragraph = intro + #146 sentence only). Four build commits 71539f6 → f01efd1 → 93be13f → aa132f2, file lists as reported; NO edit to docs/prompts/ (the prompt still carries the "generator does not exist → stop" line); NO edit to the head file (correct — the runner's); BUILD-STATE 99,943 B / 145 non-blank; head file 195,956 B; inbox: no zips (only the loose 08-31 folder); no index.lock.
- Build entry at the top of the live log (unnumbered, unindexed by design until the next runner batch); spec-feedback.md carries the nine findings (2026-09-03 section).
- Landed: §13 items 1–4 (+ generator half of 5): generator scripts/generate-form-text.mjs (never existed before — the record's "sliced out by program" was false); fixed-sentence table + drift test; D-45 vocabulary + SQL↔TS guard; migration 2026-09-03-fe-d1-amendment.sql WRITTEN NOT RUN (D-37 gate; 10 checks); rename in code + probe 46 + store v13→v14 + regression test; Medical tab click-verified in demo. Trio 452/452 / build 0 / lint 0.
- NOT landed: items 5–14 (R17, chronology/readers, extraction, ParagraphWriter/fixture/Edge stub, assembly/LEAD/shapes/rider/split, tiers/panel, Forms-tab rebuild, D-35 fixtures, walk). 3 of 30 invariants ship.
- The WINDOW IS OPEN from aa132f2: the app selects facility_party_id; the live DB keeps provider_party_id until MIG-1 + the amendment file run (Michael's hand, same sitting, before any live matter).
- Triage of the nine findings (Claude, design side): (1) bold LEAD vs already-bold body → Michael's, appearance of the served document → hands-on-shaped; build default = LEAD as its own bold run, body as the master supplies. (2) block + one narrative → build approach clone-per-paragraph within §12.3 = a default taken; slice §6.2 needs a conformance note (packet). (3) NO seed path for the template bank in live mode → predates the slice; design question → new queue row PROPOSED. (4) §9.3 one {s} for noun and verb → text defect in approved §9; fix = second token; Michael's naming/ruling. (5) midlevel_short_name vs midlevel_name → both needed; no ruling. (6) slice cites REQ-CAPTURE 2nd edition; 3rd at HEAD → one-line authority correction (packet). (7) prompt Step 2 stop contradicts slice §6.4/§13.2 → prompt correction (packet, docs/prompts) + CONTINUATION header. (8) rlsProbe comments → cosmetic; continuation fixes the second in passing. (9) adapter method names still Provider → cosmetic; Michael's.
- OWED CORRECTION ENTRY (design side): the FE-D1 build entry (2026-08-20) / variants.ts header asserted "sliced out by program"; no generator was ever committed (git log --all -- scripts/). Required fields to be filled in the next packet.
- CAP-4 note: the build displaced five BUILD-STATE paragraphs into its OWN log entry ("stands in for the runner line") — second consecutive entry carrying displaced prose; strengthens the carried CAP-4 amendment proposal.

## MIG-1 RUN — Michael's hand, 2026-09-03 (Central), the window-closing sitting
- Order confirmed: (0) STEP 0 counts (medical_bills / code_mappings / provider_billing_profiles) written down BEFORE either file; (1) MIG-1 = db/migrations/2026-08-20-fe-d1-form-engine.sql pasted ALONE, unchanged, into an empty SQL buffer on the live Supabase project, after a backup; its eight verification checks answered in words; (2) then the amendment file, alone; its ten checks answered in words.
- MIG-1's own expectations (from the file at HEAD): check 1 four non-null names; 2 four true; 3 four FALSE (a true = STOP, live exposure); 4 four rows, RLS true, policies = 1 each; 5 → 7; 6 INSERT 0 1 then ROLLBACK (vacuous if `cases` is empty — say so); 7 both null; 8 → 41.
- Answers as Michael reports them: (to be filled)
- MIG-1 RAN with success on the live project (Michael's word, 2026-09-03). First attempt pasted the file's PATH, not its contents — Postgres syntax error at "db", nothing ran; the file was then pasted whole.
- STEP 0 counts NOT taken before MIG-1. Verified from the file at HEAD: MIG-1 never mentions medical_bills, code_mappings or provider_billing_profiles (it creates the four form_* tables and alters generated_documents), so counts taken AFTER MIG-1 and BEFORE the amendment file serve check 7 identically. Recorded as taken between the two files, with this reason.
- STEP 0 counts (taken between MIG-1 and the amendment file), Michael: "Count 0" — recorded as medical_bills 0 / code_mappings 0 / provider_billing_profiles 0 (all three; to be confirmed if any differs). Consequence: the amendment's check 7 (row-count conservation) is satisfied trivially (0 = 0) and proves nothing about data — the rename is proven by the catalog checks (check 6), not by rows. The live project carries no bill data yet, consistent with go-live not having happened.
- MIG-1 checks, answered from the combined read-only grid (screenshot pasted by Michael, 2026-09-03): 1 = form_format_profiles, form_templates, form_template_versions, form_token_definitions (four names, no NULL) ✓ · 2 = true, true, true, true ✓ · 3 = false, false, false, false ✓ · 4 = first two entries visible (form_format_profiles rls=true policies=1; form_template_versions rls=true polic…) — CELL TRUNCATED in the editor; the other two entries re-run as four rows (below) · 5 = 7 ✓ · 7 = NULL, NULL ✓ · 8 = 41 ✓ (41 = the 2026-08-20 gate-3 run's 37 + four). Check 6 pending: vacuous if `cases` is empty.
- MIG-1 check 4 re-run as four rows, Michael: "True and one on four rows" — four form_* tables, rls=true, policies=1 each ✓. Seven of eight checks pass; check 6 waits on `select count(*) from cases`.
- Check 6: Michael reports "Success. No rows returned." — the editor's message for a statement that returns no rows; it does not distinguish INSERT 0 1 (constraint widened) from INSERT 0 0 (cases empty → vacuous). NOT recorded as passed; the `cases` count decides which it was.
- `select count(*) from cases` = 1 → the check-6 block inserted one probe row and rolled it back with no constraint error: check 6 PASSES (real, not vacuous). MIG-1: ALL EIGHT CHECKS PASS. MIG-1 is RUN on the live project, 2026-09-03, Michael's hand.
- AMENDMENT: the combined read-only check statement failed at parse time — relation "facility_billing_profiles" does not exist (42P01). The check query itself executed nothing. Meaning: the rename has NOT happened on the live project — either the amendment file has not been run yet, or it was run and did not apply. Diagnostic put to Michael: to_regclass on provider_billing_profiles / case_providers + the public table count (41 = nothing applied; 46 = tables created).
- Michael clarifies: he pasted the CHECK statement, not the amendment file — the file has NOT been run yet. State: MIG-1 run; amendment pending. Nothing applied by the checks.
- AMENDMENT FILE db/migrations/2026-09-03-fe-d1-amendment.sql RAN with success on the live project (Michael's word, 2026-09-03 ~11:35 CDT) — the gate passed (check 1). Both migrations have now run in one sitting, in order, before any live matter: THE §5 WINDOW IS CLOSED. Checks 2–10 pending.
- `select count(*) from parties` = 2 → check 8 is NOT vacuous; both check-8 blocks to be run (first must succeed; second must raise the CHECK violation). Combined grid (2–7, 10) and check 9 still awaited.
- AMENDMENT combined grid (Michael pasted the table): 2a five names ✓ · 2b 1 ✓ · 3 true×5 ✓ · 4 false×5 ✓ · 5 five entries rls=true pol=1 ✓ · 6a NULL / facility_billing_profiles ✓ · 6b case_providers, code_mappings, facility_billing_profiles, medical_bills ✓ · 6c NONE ✓ · 6d = 47 / 11 / 3 against an expected 0 / 0 / 0 ✗ AS WRITTEN · 7 0/0/0 ✓ (trivial) · 10 46 ✓.
- 6d ANALYSIS (Claude): the file's own check — names LIKE '%provider%' → EXPECT ZERO — is unsatisfiable as written, because THIS FILE creates case_providers, case_provider_individuals and case_provider_visits (and generated_document_paragraphs.case_provider_id), whose auto-named pkeys/fkeys/CHECKs/policies/indexes all contain "provider". A verification-TEXT defect in the migration's comment block, not (yet) evidence of a schema defect. Decisive check: the same three catalogs filtered to names NOT attached to the four new tables — expect zero. Finding for the record (spec-feedback / correction to the migration's comment block by packet).
- 6d DECISIVE RESULT: 27 rows, ALL constraints in Supabase's own `auth` schema (custom_oauth_providers ×20, identities, saml_providers ×3, saml_relay_states, sso_domains, sso_providers) — none in `public`; ZERO indexes and ZERO policies returned. So in the public schema nothing on the three renamed tables still says "provider": CHECK 6 PASSES IN SUBSTANCE. Two verification-TEXT defects in the file's check 6 (third limb): (i) '%provider%' catches the file's own case_provider* tables; (ii) the pg_constraint limb has no schema filter (pg_indexes/pg_policies do) and catches Supabase's auth schema. Correction to the comment block owed by packet; executable statements unaffected.
- CHECK 8 PART ONE FAILED (a real defect): inserting 'renders-care-at' violated CHECK "contact_edges_type_check" (23514). Row detail shows effective_from/effective_to present → the ADD COLUMNs took. CAUSE, verified at HEAD: the CD-1 migration (db/migrations/2026-08-12-cd1-contact-directory.sql lines 188–190) NAMED the CHECK `contact_edges_type_check`; the amendment file (line 430) dropped `contact_edges_edge_type_check` "by its auto-generated name" with IF EXISTS — a silent no-op — then ADDED a second CHECK under that name including the new value. Live state: TWO CHECKs on contact_edges — the old (19 values) still rejects 'renders-care-at'; the new (20 values) accepts it. db/schema.sql carries no `contact_edges_type_check` (its CHECK is inline/unnamed → auto-name on a fresh project), so schema.sql and the live database DIVERGE in constraint naming; the build read schema.sql. FAILURE CLASS: drop-by-guessed-name (the file's own D-7 reasoning for the unique key, not applied to the CHECK) + a command that cannot disconfirm (IF EXISTS) treated as done (QR-6(a) class). Same class as the provider→facility auto-name concern the same file handled correctly. Actor: the FE-D1 amendment build session (Opus 5, per its entry). STOP per the file's own rule; fix = a follow-up migration dropping the old CHECK by catalog lookup, Code's act under FE-D1A-1, Michael's hand to run. Live impact now: none (no code path inserts a renders-care-at edge yet). Check 8 part two: MOOT (the old CHECK rejects any new value, so exactness cannot be tested until the fix runs).
- DIAGNOSIS CONFIRMED on the live project (Michael pasted pg_get_constraintdef): THREE CHECKs on contact_edges — `contact_edges_edge_type_check` (20 values incl. 'renders-care-at'; the amendment's), `contact_edges_not_self`, and the OLD `contact_edges_type_check` (19 values, no 'renders-care-at'; CD-1's). Exactly the predicted state. The fix is one drop of the old constraint; check 8 (both halves) re-runs after it.
- CHECK 9 PASSES: both new families ('fixed-sentence', 'writer-instructions') accepted inside a rolled-back transaction — "Success. No rows returned." (a family CHECK violation would have errored).
- AMENDMENT MIGRATION TALLY: 1 ✓ (gate) · 2 ✓ · 3 ✓ · 4 ✓ · 5 ✓ · 6 ✓ in substance (two verification-text defects owed) · 7 ✓ (trivial, 0/0/0) · 8 ✗ FAILED — old CD-1 CHECK `contact_edges_type_check` still standing; fix pending; both halves re-run after · 9 ✓ · 10 ✓ (46). NINE OF TEN; the tenth waits on the fix.

### RULINGS — 2026-09-03 (~12:00 CDT), Michael's picks verbatim (option texts Claude's; alternatives named)
- OLD CHECK REMOVAL: "Follow-up migration file (Recommended)" — the continuation build writes db/migrations/2026-09-03-fe-d1-amendment-fix.sql (drop `contact_edges_type_check` by CATALOG LOOKUP, folded into schema.sql); Michael runs it by hand; check 8 (both halves) re-run after. Not taken: the one-line drop now.
- §9.3 `{s}`: "Second token, build names it (Recommended)" — `{s}` keeps the noun's plural s; a NEW verb token (name the build's, e.g. `{verb_s}`) carries the verb's singular s; §9.3's approved wording otherwise untouched; recorded as his ruling; the edit to form-engine.md §9.3 is applied by the continuation under this ruling, then regenerated and drift-tested. Not taken: hold.
- BOLD LEAD: "Hold for the hands-on sitting (Recommended)" — CC-1(b); build default meanwhile: the LEAD as its own bold run, the body exactly as the master supplies it, reported as a default taken. Not taken: body roman now; keep body bold with no LEAD emphasis.
- LIVE-MODE SEED: "Queue a register row (Recommended)" — a new ⬜ row with the full question and three options (seed migration / app-side first-run seed from the generated constants / one-shot script); the continuation stays fixture-only as authorized. Not taken: rule app-side now; rule seed migration now.
- (Adapter method rename — PROPOSED as a continuation default: rename the four `*Provider*` DataAdapter methods to Facility for consistency; veto available.)

## PACKAGING — 2026-09-03 (~12:15 CDT)
- Entry ordinal measured at HEAD aa132f2: newest design entry #146 → this sitting's entry is #147. IDs minted in the packet (register rows, full question text): FE-SEED-1 (live-mode template-bank seed), CAP-OPEN-4 (CAP-4 amendment proposal), HD-23 (the bold LEAD, held for hands-on). Six exact-string work orders; every OLD string verified to occur exactly once at HEAD aa132f2 over the bridge; no HD- row remains in the synced register, so HD-23 goes at the end of the Form engine section.
- PF-1 did not fire (no legal characterization, no registry entry). RR-1: figure sweep run across manifest, entry, capture, handoff, run record, ledger.
