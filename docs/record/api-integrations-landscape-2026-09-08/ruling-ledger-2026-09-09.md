# RUNNING LEDGER — API-integrations ruling sitting (typed, Cowork, Fable 5.1) — opened 2026-09-09 Central (DT-1; container clock not used)
# Practice (2026-08-31): every ruling written here in Michael's words within the exchange it was made. Nothing here is a filing.
# HEAD 44409ef unchanged; inbox/ empty; bridge on the checkout. Instruction that opened the sitting: "If there is anything that you want to rule on, bring it to me one thing at a time."

## Items put, in order

### Item 1 — FACT: is Microsoft's HIPAA BAA in force on the tenant? (put 00:46 CDT)
- Michael's answer (his words, verbatim opening): "I can show you the BAA, but it is subject to this NDA with Microsoft: Microsoft Non-Disclosure Agreement for Compliance Materials …" — followed by the NDA's full text (the click-through NDA that gates Service Trust Portal materials).
- What this establishes: Michael has ACCESS to the BAA document on Microsoft's Service Trust Portal, behind the Compliance Materials NDA. The NDA's own terms (§1(a)–(c): no disclosure to a third party, no reproduction or summary; §2: no duplication, no giving any part to anyone else) mean the document is NOT to be pasted into this chat. Claude asked him not to, and does not need its text.
- Open, re-put narrower: "in force" under Microsoft's own page turns on (i) a commercial Microsoft 365 subscription under the Microsoft Customer Agreement / DPA and (ii) the firm being a covered entity or business associate — the BAA applies BY DEFAULT through the DPA on those facts, with no separate signature step (Microsoft's page; UNVERIFIED as a legal reading — his to confirm).
- NOT RULED. No decision taken on this item.
- Re-put narrower (subscription type + covered status). Michael's pick, verbatim option label: "Covered-status is undecided". Meaning recorded: whether the firm is a HIPAA covered entity or business associate is OPEN and his; decision 1 proceeds on the POLICY footing (BAA-for-PHI stays the firm's rule by choice; Supabase's own condition on PHI applies regardless). Subscription type NOT STATED (the pick did not answer it) — remains a fact to read at admin.microsoft.com → Billing → Your products; not assumed.
- Status: item 1 = FACT, partly answered; no ruling. Named look survives: the subscription type. The legal question is recorded as Michael's, open.
- POST-ITEM NOTE (mid-turn): Michael pasted the text of Microsoft's HIPAA BAA into the chat after Claude asked him not to. **The document's text is NOT recorded here, will not be carried into any capture, handoff, packet, or project-knowledge file, and is not summarized in any record** — the Compliance Materials NDA he pasted bars reproduction and summary. NO fact is taken from it for this sitting — not its structure, not its opening, not its terms; the public offering page (the BAA applies on the covered-entity/business-associate condition through an agreement incorporating the DPA) is the only source cited, and it was read independently. In-force status for the tenant therefore stands as: CONDITIONAL on Michael's covered-status determination (open, his) and on the subscription being under an agreement that incorporates the DPA (not stated). Nothing further from the document is used.

### Item 2 — DECISION 1: where PHI lives (put ~00:50 CDT)
- Put with the price ($599 Team floor; $100 PITR per 7 days; $15 compute; add-on unpublished — ≈$714 before the add-on; Pro $25) and on the POLICY footing.
- Michael's pick, verbatim option label: **"Pointer + split store (Recommended)"**. Option text as put (Claude's words, selected by him): "Chronology files live in the matter's OneDrive folder; the five PHI tables move to a Postgres inside Microsoft's tenant (Azure Database for PostgreSQL); rows are a rebuildable index over the files; Supabase keeps the case core and pointers at $25. Needs the AS-Q4 re-ruling as its own act (next item) and a build slice later. Cost: two databases, no full-text search in Supabase, a discipline built into the fields."
- STATUS: **RULED BY SELECTION** (the pick was of the recommended option; the option text is Claude's, the selection is his — CC-1(a) noted). Consequences NOT yet ruled: the `AS-Q4` re-ruling (its own act, next); the runner shape (decision 2); the build slice (a later authorization).

### Item 3 — DECISION 1a: the AS-Q4 re-ruling, its own act (put ~00:58 CDT; walk given at his request "Run me through this a bit more."; re-put after the walk)
- The ruled text in front of him, quoted from the REQ-CAPTURE §14.1 (RULED 2026-08-31 late): "(i) Extracted text per version in the database; bytes not retained; file store at gate 7."
- Replacement wording as put: "Extracted text per version as a text file in the matter's OneDrive folder (per client, as AS-Q10 has it); the `case_chronology_versions` row keeps version number, drive item id, content hash, `readable`, `char_count`, `removed_at`; everything else in AS-Q4 stands." The original-file, removal and who-writes forks expressly reserved for separate items.
- Michael's pick, verbatim option label: **"Adopt the one-limb change"**.
- STATUS: **RULED — `AS-Q4` AMENDED, ONE LIMB** (the storage location of the extracted text; all other limbs of AS-Q4 stand: versioned text records, readability flag never sent, removal, bytes of the original not retained pending item 4, file store at gate 7 unchanged, one chronology set per client). Applies to the built `case_chronology_versions` table as a later build slice (schema change: `extracted_text` leaves; `drive_item_id` + `content_hash` arrive) — NOT authorized to build by this ruling.

### Item 4 — Fork 1 of 3: the original file (put ~01:02 CDT)
- Michael's pick, verbatim option label: **"Keep it beside the text (Recommended)"**. Option text as put: "The dropped file is saved into the same OneDrive chronology folder next to its extracted-text file; if the drop is a file already in the mirror, the app points at that item instead of writing a copy. 'Bytes not retained' now reads 'bytes not retained by the app's database'."
- STATUS: **RULED BY SELECTION.** AS-Q4's "bytes not retained" limb is re-read as "not retained by the app's database"; the original lives in OneDrive beside the text; point-don't-copy when the file is already in the mirror. Context put with the question (his own earlier statement, from memory, that the chronology vendor's attribution has been wrong once): the original is what he checks against.

### Item 5 — Fork 2 of 3: removal of a mis-dropped version (put ~01:04 CDT)
- Michael's pick, verbatim option label: **"Move to a removed folder (Recommended)"**. Option text as put: "The app moves both files out of the matter's chronology folder into a firm-level `_removed` folder that is not under any matter, keeps the pointer on the row so the act is auditable, and you delete from there by hand. PHI leaves the wrong matter at once; nothing is destroyed by the app."
- STATUS: **RULED BY SELECTION.** D-60's row behaviour stands; the file half is a MOVE to a firm-level `_removed` folder (outside any matter), pointer kept, deletion his hand.

### Item 6 — Fork 3 of 3: who writes the files and runs the extractor (put ~01:06 CDT)
- Michael's pick, verbatim option label: **"Browser now, P1 later (Recommended)"**. Option text as put: "(A) is the shape for the next slice — it exists today, minus the OneDrive write — and (B) is the destination when the P1 service is authorized; the row and file layout are the same either way, so nothing is rebuilt at the switch."
- STATUS: **RULED BY SELECTION.** Consequence: the delegated `Files.ReadWrite` consent on the SPA registration becomes NECESSARY for the chronology path (not only for PDF preview) — that consent is its own item (decision 3a). T3/KICK-1 unchanged.

### Item 7 — DECISION 2: the server-side identity, Q-WF-4 (put ~01:08 CDT)
- Michael's pick, verbatim option label: **"Composite as read (Recommended)"**. Option text as put: "(i) now; (ii) for the PHI-free jobs the day the two functions deploy; (iv) as the interim for the known-sender mail pipelines; (iii) as the destination runner for anything that touches PHI. Decided now, built in Wave 3; each build is its own authorization."
- STATUS: **RULED BY SELECTION — `Q-WF-4` ANSWERED IN SHAPE**: delegated-only now; Supabase scheduled runner (ack-and-enqueue) for Class-0 jobs; Power Automate flows (pointers only) as the interim for known-sender mail; an Azure runner (Functions + Managed Identity + Key Vault; Graph → Event Hubs) as the destination for PHI-touching pipelines. NOTHING BUILT OR AUTHORIZED by this ruling; each build is its own act. Consumers of Q-WF-4 on the register (WF-2–WF-8, Q-QBO-3, Q-COM-4) now have a shape to reconcile against.

### Item 8 — DECISION 3a: delegated `Files.ReadWrite` on the SPA registration (put ~01:10 CDT)
- Michael's pick, verbatim option label: **"In, for all three uses (Recommended)"**. Option text as put: "Chronology files, the removal move, and PDF preview. Consent is your act when the slice that needs it is built; the folder convention (where matter folders live in OneDrive) is a fact you supply then."
- STATUS: **RULED BY SELECTION.** Scope ruled IN for three named uses: (1) chronology text + original written to the matter's OneDrive folder at the drop; (2) the `_removed` move; (3) PDF preview via Graph conversion (`Q-API-3`'s preview half — the FILING half stays behind the fidelity test). The consent click remains HIS act at build time; the OneDrive matter-folder convention is a fact he supplies then (H5 — never swept).

### Item 9 — DECISION 3b: delegated `Mail.Send`, user-initiated (put ~01:12 CDT)
- Michael's pick, verbatim option label: **"In, user-initiated (Recommended)"**. Option text as put: "Delegated `Mail.Send` on the SPA registration; every send is your click; no app-only sending until the Azure runner is built and separately authorized. The rule 'client-naming notifications go through Graph, never Postmark' is adopted with it."
- STATUS: **RULED BY SELECTION.** Two things ruled: the scope (delegated, user-initiated only; consent his act at build time) and the RULE that any notification or message that could name a client or provider goes through Graph from his own mailbox, never through Postmark (`Q-API-5` closed). App-only `Mail.Send` is NOT ruled — its own act with the Azure runner.

### Item 10 — DECISION 4: Wave 0 acts (put ~01:14 CDT; multi-select)
- Michael's picks, verbatim option labels: **"(a) Deploy the two functions"**, **"(b) Outlook defect captures"**, **"(c) CloudLex export rehearsal"**, **"(d) Backup vendor choice"**.
- STATUS: **RULED — AUTHORIZED, four acts:** (a) deploy `statute-fetch` and `legiscan-poller` per `statute-cache-setup.md`, one manual invocation of the poller read before cron is trusted — a Code session with his Supabase sign-in; (b) the two Outlook-defect evidence captures — his hand, his browser; (c) the CloudLex export rehearsal onto the existing OneDrive tree, importers built against the real shapes — his hand on the portal, a Code session for the importers; (d) the backup vendor choice — a one-page comparison (Backblaze B2 vs AWS S3/Glacier/AWS Backup) against `#137`'s requirements, then his pick.
- (e) the Exchange transport-rule ignore list was described in the question but could not be a tickable option (widget holds four) — NOT RULED; put next as its own item.

### Item 11 — DECISION 4(e): the Exchange transport-rule ignore list (put ~01:16 CDT)
- Michael's pick, verbatim option label: **"Yes, but later"**. Option text as put: "Authorized in principle; the sender list waits for the mail-intake slice so it is drafted once against real pipelines."
- STATUS: **RULED — authorized in principle, deferred to the mail-intake slice**; sender list is his hand (H5) when that slice is drafted; nothing drafted now.

### Item 12 — DECISION 5: the closed-for-now list (put ~01:18 CDT)
- Michael's pick, verbatim option label: **"Adopt the list as put (Recommended)"**. The list as put, each with its trigger: SMS/CPaaS (COM-LOOK-4 answered + a signed BAA + his written finding that photos must enter the log automatically); Teams chat ingest (the paralegal hire); Bookings (the hire); the e-signature API (a signed BAA naming the SKU + a volume finding); LawPay links and live QuickBooks (the money-module ruling); CourtListener in-app (Q-6 resolved with FLP); PACER PCL (a federal matter that needs it); rules-based calendaring vendors (closed for good in favor of FC-9's engine); Microsoft 365 Backup as the third copy (closed for good against #137's independence requirement); address validation (closed for good, as CCS-1 ruled).
- STATUS: **RULED BY SELECTION — CLOSED UNTIL TRIGGER (eight) / CLOSED FOR GOOD absent a new ruling (three).** Effect on the Q-API set: Q-API-6, -7, -8, -10 close on this list; Q-API-9 (eCFR), -11 (NPPES), -12 (contacts export), -14 (deep link/Intune), -15 (CloudLex — now authorized as an act), -16 (Power Automate — answered by decision 2), -17 (2 a.m. mail — answered by decision 2's "(i) now"), -18 (MS BAA — item 1, conditional), -19 (ServeManager — a fact for him) are dispositioned separately or remain small.

### Item 13 — DECISION 6: travel, Q-API-13 (put ~01:20 CDT)
- Michael's pick, verbatim option label: **"Yes — package it now (Recommended)"**. Option text as put: "I write the capture and handoff, the ruling sheet, and the packet, and save the zip to `inbox/` on mdb-pllc over the bridge for batch 94, then report the re-listed result."
- STATUS: **RULED BY SELECTION — PACKAGE NOW.** EVIDENCE: v1, v2, the challenge prompt, Grok's response as pasted, the reconciliation, this ledger → `docs/record/api-integrations-landscape-2026-09-08/`. RULING: the ruling sheet → `docs/specs/`. `#153` entry. Register rows. AS-Q4 amendment as an ANNOTATION on the REQ-CAPTURE §14.1, never an edit of the ruled text. Nothing authorizes a build. **The Microsoft BAA text goes nowhere.**

## Sitting closed for rulings at item 13. Thirteen items put; twelve ruled (items 2–13; item 1 a fact partly answered — an earlier draft of this line said "eleven," an arithmetic slip corrected here before the ledger shipped); nothing built; no ID minted beyond the packet-local Q-API series; no src edited; no database touched.

### POST-SITTING NOTE (packaging, ~01:30 CDT) — the ordinal and the batch number moved after the picks above were recorded
- A LIVE `git ls-remote origin refs/heads/master` at packaging read `d4c5e7d`, not the `44409ef` the sitting opened on: **batch 94 committed at 00:44:13 CDT — two minutes before item 1 was put — filing the TRANSIT-EXECUTION session as `#153`.** This sitting's entry is therefore **`#154`** and its packet runs as **batch 95**; the "`#153` entry" in item 13 and the "batch 94" in the option text are the forecasts as they were put, left as written.
- `Q-WF-4` HAS its own register row (written `- ⬜ **Q-WF-4.**`, no backticks — a backticked grep returned a false zero earlier in the sitting); the packet flips it ⬜ → 🟡, ruled in shape, execution pending. **And `Q-STAT-5` HAS its own row too** (`- ⬜ **Q-STAT-5 — How far should the Insurance line go …**`, the same backtick-less form): the packaging pass first recorded it as absent on the same false zero, after diagnosing that zero for `Q-WF-4` — corrected before the zip closed; both rows are annotated add-only.
- The repo moved a SECOND time during packaging: `b2dc222` (01:27:20 CDT, a Code session Michael directed — the `RC-4` row and the `RF-2` alias onto `TC-OPEN-4`); every check was re-run there and held.
- Instructions v31 were found IN FORCE by a live read of the instructions field at packaging; the knowledge meter read 1,610,695 / 2,000,000 = 80.5%.
- The Microsoft BAA text: still nowhere, checked by grep over every packet file before the zip closed.
