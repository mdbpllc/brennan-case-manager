# PF-1 preflight report — the firm-obligations design pass (2026-09-07 evening, Central)

**Status:** EVIDENCE (`CAP-2`) — the record of the adversarial multi-agent READ-ONLY preflight run by the DESIGN SESSION before the packet carrying `#151` entered `inbox/` (PF-1, standing since 2026-08-18; trigger: the packet carries legal characterizations — the spec's seed catalog). Every finding is listed with its disposition; the fix pass then got its own check (RE-SWEEP AFTER FIX, ruled 2026-08-20), recorded in §4.
**Canonical repo path:** `docs/record/firm-obligations-design-2026-09-07/pf1-preflight-report-2026-09-07.md`
**DT-1:** 2026-09-07 Central.

---

## §1 — The run

Four auditors, launched in parallel at ~21:20 CDT against the three drafts (the spec, the ruling sheet, the source-read record) plus the design-pass ledger, with the primary sources (the normalized statute chapters, the TDRPC text, the normalizer) and the read-only repo snapshot at `ea5675b` as ground truth. Each was told: read-only; quote conflicting strings side by side; a clean bill of health must be earned; say what could not be checked. All four returned (no `529 Overloaded` losses — the fleet death BUILD-STATE records against `#104`'s preflight did not recur; each report carried its own PASS list and COULD-NOT-CHECK list, so an empty findings list would have been legible as a failure rather than a pass).

| Auditor | Brief | Returned |
|---|---|---|
| A — statute-text verifier | every quotation in the record §3–§4 character-by-character against the `.norm`/`.txt` files; every legal characterization in the spec §7–§9 and the sheet against the quoted text; the normalizer re-run on copies | HIGH 0 / MEDIUM 1 / LOW 12 |
| B — HEAD-facts verifier | every claim about the codebase, the schema and the record against the snapshot; every quotation attributed to BUILD-STATE, `#137`, the heartbeat design, the deadline-engine spec, the Outlook spec, the registers; the ledger's commands re-run | HIGH 1 / MEDIUM 6 / LOW 13 (+1 observation) |
| C — convention & consistency auditor | PROPOSED-not-ruled discipline, CC-1's three limbs, ID discipline, DT-1, CITE-STABILITY, CAP-2, H5, secrets/client-data sweep, spec↔sheet consistency | HIGH 1 / MEDIUM 14 / LOW 23 |
| D — adversarial practitioner review | the behaviour, the surfaces and the catalog on their merits; missing obligations with governing sources | HIGH 6 / MEDIUM 17 / LOW 8 |

**Method note on A's result:** every block quotation in the record §3–§4 was found as an exact substring of the collapsed `.norm` text (offsets in A's report); the normalizer re-run on copies reproduced every count and every residual site and regenerated byte-identical `.norm` files. The one character-level divergence in any quotation was an inserted period after *"Rule 1.15 Safekeeping Property"* (fixed).

---

## §2 — Findings and dispositions (grouped; the bracket is the auditor's letter — A, B, C, D as in §1 — and its item number, e.g. `[D1]` is auditor D's first finding, never the record's `D`-series)

**FIXED — the documents were changed:**
1. [A1] The inserted period in the TDRPC 1.15 quotation — removed.
2. [A2] "registered agent" in a bracketed summary of § 171.203(a)(4) — now "the agent designated under Section 171.354 (the agent for service of process)".
3. [A3, D7] The PIR date: "leaves the date to the comptroller's form" — now "the statute is silent on the date"; May 15 stated as practitioner knowledge, NOT READ; the PIR weighted `hard` on § 171.2515 (read).
4. [A4] "so NO separate SOS annual report" — the non-sequitur removed; now "none was found in the sources read; the Business Organizations Code was NOT READ".
5. [A5] § 81.113(b) presupposes an ethics component — the record and `FOT-2` now say the SIZE is what is not established.
6. [A6, C12] "none as a rule" for trust reconciliation — scoped to "none in the rule read; the IOLTA and State Bar Rules are NOT HELD and may say otherwise"; the `reconcil` grep of the whole TDRPC text recorded.
7. [A7, C5] "his read confirms" on the BOI row — now "his read of the section decides"; TIER B sentences quoted in the spec are marked "the fetch layer's wording"; the BOI row is now seeded INACTIVE with the note rather than DO NOT SEED [D25].
8. [A8] The sheet's catalog table said "every cell blank" while carrying cadences — the table now carries only weights and the offered/active column; dates and conditions are entered at activation [D6].
9. [A9] Bare section numbers on the sheet — code names added.
10. [A10, A11] The single-`A` residual inventory extended (`byAdetermining`, `andAsubtracting`); the `AAAcooperative` shape described correctly (a real "A" followed by an artifact).
11. [A12, D20] Chapters 79 and 82 read but silent — § 79.036(a-1) now quoted as corroboration of the TIDC flow (and it revealed the Title 3 pairing); ch. 82's heading scan recorded.
12. [A13, B7, C14, D15] The county indigent-defense plan classified two ways — now "a PUBLIC document outside the four channels; not read"; the inferred "Bexar and the other counties whose lists he is on" removed — which counties is HIS TO STATE; §7.8 no longer puts the plan in the FC-14 class.
13. [B1, C9] "no firm-scoped table / the FIRST table keyed to neither" — false: `glossary_terms` carries `scope in ('firm','case')`, and reference tables keyed to nothing exist; §1 item 7 rewritten.
14. [B2, C7] The spliced quotation attributed to the closed register — split into the two sources, each verbatim.
15. [B3] "`GL-1` is COMPLETE" stated as a record fact — now "complete per the `#150` packet, not yet at HEAD; at `ea5675b` the floor stands COMPLETE BUT FOR HIS OWN PASTE".
16. [B4, C16] *"let's lean towards the thin version"* and "PART 10" attributed to the log entry — now attributed to the `#137` capture (PART 8 / PART 10), with the entry's own words (*"Michael ruled thin"*) beside them.
17. [B5, C11] `BR-3` (and `FO-4`, `FO-5`, `BR-2`, `BR-4`, `DA-2`) carried as if defined on the record — the provenance sentence now says they are defined only in the capture and that the collision report records their ABSENCE from the repo.
18. [B6] "the only cross-matter surfaces are `/cases` … and Outlook" — now names the nine cross-matter routes and narrows the claim to the dashboard-shaped surface and the calendar.
19. [B8, C22] "`NOT NULL` since 2026-07-28" — now "since the 2026-07-24 Outlook build (live since the 2026-07-28 first schema run)".
20. [B9, C20] The ledger's high-water command as named returned a cause-number token — the heading-anchored form is now named.
21. [B10, C19] "149 raw lines" — 148.
22. [B11] "spec-feedback items 8–11" — now "8–11 as `#150` names them; the build entry lists 8–12".
23. [B12] 28 `<Route>` elements / 27 distinct paths — stated.
24. [B13] "`snooze` exists only in the heartbeat design" — now "in the built product 0 files; in the docs, the heartbeat design and the rows and entries that discuss it".
25. [B14] "his call and only his" — the addition dropped; runner 82's words quoted.
26. [B15] The 2026-09-05 quotation — attributed "as the `#149` ledger records it".
27. [B16, C38] `Q-DE-6` — cited to its second limb and marked packet-local.
28. [B17, C23] `Q-WF-4` — the finding attributed to `#85`; `Q-WF-4` named as the open question.
29. [B18] `appendReviewLog` usage — restated; the re-sweep then corrected the count to TEN pages under `src/pages/` (§4).
30. [B19, C36] "code, table or secret" on the sheet — "secret" dropped (a grep cannot see one).
31. [B20] Three cosmetic quotation drifts — repaired.
32. [B21] The `review_log.action` CHECK observation — carried into the spec (§1 item 8; `FOD-6` now widens the CHECK in the slice's migration) and the pre-existing `'cancelled'` mismatch reported in the session-log entry and a `spec-feedback.md` section (verified by this session before criticizing: `Q-RE-6` records the sibling "decided" gap as a vocabulary gap; the `'cancelled'` write path was not recorded anywhere found).
33. [C1] The `T1`–`T26` catalog series collided with the record's two `T` series (the transcription tier `T3`; the LegiScan poller tiers) — renamed `FOT-1`…`FOT-35`, declared packet-local, collision-checked (0 hits); "the transcription `T3`" written where the tier is meant.
34. [C2] The sheet's `D0`–`D10` — now "DECISION n", declared as the sheet's local section numbers.
35. [C3] §5.4's "never render on the per-case tab" — now `FOD-13`, with `FO-1`'s "filterable out" wording noted and the veto open.
36. [C4] The six hands-on items "on the queue" — now PROPOSED for the queue (five after merging).
37. [C6] §2's vocabulary imperative — now PROPOSED, `Q-FO-0`.
38. [C8] The `O3` quotation — now attributed to `CaseListPage.tsx`'s own comment, verbatim (the auditor grepped `docs/` only; the sentence is in `src/`).
39. [C10] The ledger's description of the next-acts line — now states BUILD-STATE's order (fifth), the `#150` packet's (third), the `#149` handoff addendum's disjunction, and that his delegation covered the choice.
40. [C13, D12] `FOT-6` trust reconciliation weighted `routine` against his `#137` words — now `hard`, the departure named on the sheet for his call.
41. [C15, D18] `pending`/`open` stored with nothing to flip them — the stored state is now `open | done`; lit/overdue derived.
42. [C17] "so it applies to him" — now "if he accepts appointments under art. 26.04 in a county, the report is owed to that county — his to confirm".
43. [C18] Line cites in the ledger — pinned "at `ea5675b`".
44. [C21] The ledger's forecast time range — to be closed at the actual time in the ledger's §3 at the sitting's close (the re-sweep caught the placeholder still standing; it is filled before the zip closes).
45. [C24] `INS-1` — "proposed at `#137`".
46. [C25] "Claude's lean" — stated once in the sheet's preamble and on each decision.
47. [C26] Closed option lists — each decision now ends with an explicit outside slot.
48. [C27] Status vocabulary — defined in one place (spec §7 header); the TIER B rows carry the request date and `up_to_date_as_of`.
49. [C28] `FOT-4` — one obligation with a per-county checklist; the statute's per-county duty stated; "which counties" is HIS FACT.
50. [C29] The PF-1 report named in the spec's status line.
51. [C30] §9's heading — "`FO-7`'S TWO FACTS, VERIFIED AT HEAD".
52. [C31] `FOD-6` — now lists the edits §4.5 names.
53. [C32] "current chapter" — "as downloaded 2026-08-14".
54. [C33] Two Knowledge Repo folder names dropped from the record's §6.
55. [C34] Where born-✅ rows live — "in the closed register (`CAP-3`)".
56. [C35] §14's execution-plan wording — conditional ("it would contain … the standing shape, not a plan").
57. [D1, D21] Serial back-materialization applied to cadences — `missedPeriods: serial | collapse` per rule kind (filings serial; cadences collapse); its own decision on the sheet (DECISION 2).
58. [D2] "Not applicable" as a twelve-month snooze on annual `hard` items — now offered ONLY on `conditionalPerPeriod` obligations, with a reason enum; `FOD-18`.
59. [D3, D23] Nothing reaches him in the lead window; the all-day event scrolls into the past — DECISION 7 re-put around the OBJECT (event vs To Do task and its `Tasks.ReadWrite` consent), the CALENDAR (separate "MDBP Firm" — Outlook mobile toggles calendars cleanly), and the REMINDER at the lead window; the lean changed to a separate calendar with the reminder; keep-vs-delete on done moved to the hands-on list.
60. [D4] No multi-year cadence — `anniversary { anchorDate, everyYears }`; the assumed-name renewal is now a 10-year `anniversary`; the notary (4) and guardianship certificate (2, then 4) rows exist.
61. [D5, and D's MISSING table] Catalog gaps — added after source reads: the business personal property rendition (`FOT-10`, Tax Code ch. 22 read), the property-tax payment (`FOT-11`, § 31.02 read), Form 940 (`FOT-15`, TIER B), the TWC quarterly report (`FOT-16`, NOT READ), the workers'-comp non-subscriber notice (Labor Code § 406.004 read; window NOT READ), the guardianship certificate (`FOT-3`, Estates Code read), the notary commission (`FOT-21`, Gov't Code ch. 406 read), the payment-card expiry (`FOT-25`), the database restore test (`FOT-23`), the legal-research renewal notice window (`FOT-29`), the office lease (`FOT-30`), bar memberships (`FOT-31`), the model-credential rotation (`FOT-33`), the annual closed-file review (`FOT-26`); the Family Code rows recorded as NOT HELD (the `FA` zip is absent — an existing acquisition item); sales tax recorded DO NOT SEED on a read of § 151.0101(a); H.B. 300 training recorded as EVENT-DRIVEN on a read of § 181.101 — **the biennial cadence the auditor proposed is NOT in the text and was not adopted**; TBLS, federal-court renewals, successor-attorney designation, advertising filings, Form 8300, new-hire reporting and the attorney profile recorded as HIS FACT / EVENT-DRIVEN / NOT READ rather than seeded.
62. [D6] The sheet collected dates at the sitting against `FOD-9` and `CC-1(b)` — DECISION 9 Part B now asks only which templates are OFFERED / ACTIVE and the weight where the spec departs from his words; dates and conditions are entered at activation.
63. [D8] The card never quiet — `FOD-14`: the card's own 14-day horizon; the routine-register-only composite promoted to DECISION 6's lean.
64. [D9] The register hid everything beyond 90 days — now a twelve-month view; Done allowed on a not-yet-lit occurrence (`FOD-15`).
65. [D10] No occurrence-level date override; rule edits a back door — `dueOnOverride` on the occurrence; `FOD-4` now says a re-evaluation never moves an OVERDUE occurrence later; `precision: month` (`FOD-17`).
66. [D11] Subscriptions `hard` — now `routine`; the payment card `hard`; the database restore test added.
67. [D13] Extensions presented as consequences — §0 and §4 now say the additions are DEFAULTS (`FOD-19`); "thin" re-scoped to MONEY with a `filedAt` pointer allowed; §6 says it chooses AGAINST the literal one-model reading of `FO-1` and why.
68. [D14] `FOT-18` left dateless on a channel gap — the known federal dates seeded as practitioner knowledge, NOT READ, with the channel question added to `Q-FO-9`.
69. [D16] The payroll condition — now "does the PLLC pay wages to anyone — including Michael himself under an S-corporation election?"
70. [D17] A never-run restore test due in three months — `FOD-16`: the first occurrence is due NOW.
71. [D19] `ownerScope` vs who does it; RLS at multi-user — §10 names the `assignee` axis and the gate-2 permission edge; DECISION 4 carries both.
72. [D22] A schema question put to the attorney — DECISION 3 is now a recommendation for his confirmation (the `#149` limb-3 shape).
73. [D24] The LLC's assumed-name filing goes to the secretary of state only (§ 71.103(a) read) — `FOT-12`'s note fixed.
74. [D26] The $65 fee as a separate row — now a note on `FOT-1`; the period label a membership year.
75. [D27] Leads for MCLE and the TIDC report — marked as the first two hands-on leads to watch.
76. [D28] "Overdue" printed on a weekend after a Saturday date — `FOD-1` now bars the label and states the display.
77. [D29] Undo silent on Outlook — `FOD-7` reverts the event.
78. [D30] `FOT-7`'s "unknown" kind — `anniversary`, undated, inactive.
79. [D31] `/firm` and "Firm" — `/firm/obligations` and "Obligations" (`FOD-12`); a `court-appointments` category added.

**DECLINED, with the reason:**
- [C37] Overlap between DECISION 1's option (3) / DECISION 7 and the held items — kept: the option names the DIRECTION (should Done always ask for a confirmation number?) while the held item is the SHAPE of the affordance; the Outlook object/calendar/reminder changes the BUILD and cannot wait for the product, while keep-vs-delete can and does.
- [D's "at least once every two years" H.B. 300 row] — declined on the text read at source (§ 181.101 as downloaded 2026-08-14 has no biennial cadence); recorded as EVENT-DRIVEN instead.
- [D14's suggestion to seed a "Bexar first" county] — declined (H5); which counties is his.
- [C26 in its strongest form — rewriting every option set] — partly declined: each decision gained an explicit outside slot; the option lists themselves stay, because they are the CC-1(a) way of asking.

**NOTED, no change needed:**
- [B's observation on `O3` naming two decisions in `src/` comments] — the spec's cite is the correct one.
- [A's COULD-NOT-CHECK on the eCFR sentences] — by design; they are TIER B and marked.
- [C's PASS on secrets and client data] — nothing found; the only address is `signin@brennanstx.com`, already on the record.

---

## §3 — What the preflight established beyond the drafts (findings that ride to the record)

1. **A pre-existing code/schema mismatch, found by auditor B and verified by this session:** `src/pages/CalendarTab.tsx` writes `appendReviewLog({ …, action: 'cancelled' })` on a calendar cancel; the Supabase adapter's `appendReviewLog` is a pass-through `insertRow('review_log', entry)`; `db/schema.sql`'s `review_log.action` CHECK admits only `('suggested','confirmed','edited','rejected','created','generated')` and no migration in `db/migrations/` widens it; the domain type `ReviewLogEntry.action` includes `'cancelled'`. **On those reads, in Supabase mode a calendar cancel updates the event to `cancelled` and then the audit insert should be refused (SQLSTATE 23514), and the `syncEvent` call that follows it — the Outlook deletion — would not run.** NOT verified live (no database was touched by this session); BUILD-STATE's own words for the 2026-08-13 exercise that recorded cancel as working are *"your hand, your browser, demo events only"*, and the local adapter has no CHECK to hit. Reported in `#151` and routed as a `docs/spec-feedback.md` section for a Code session to confirm and fix; nothing changed by this packet. `Q-RE-6` already records the CHECK's missing "decided" value as a vocabulary gap; this is the live-write sibling.
2. **The `;AA` normalizer context is systematic** (twelve sites across three chapters) — a §3 amendment candidate for the statute-pass spec, not made here.
3. **The Family Code zip is absent from the corpus** — the acquisition list's recommended minimum stands; two appointment-side obligations wait on it.

---

## §4 — RE-SWEEP AFTER FIX (the fix pass's own check)

Two auditors, launched in parallel at ~22:05 CDT over the five revised files: R1 (the second-round quotations against the sources, character by character; the second-batch normalizer re-run on copies; every §7 characterization against the record; the 79 FIXED items checked present; fix-introduced contradictions) and R2 (the HEAD-fact claims the fix pass changed, against the snapshot; the conventions re-sweep; internal consistency). **Results: R1 — HIGH 0 / MEDIUM 13 / LOW 14; R2 — HIGH 1 / MEDIUM 7 / LOW 11.** All sixteen second-round quotation fragments were found verbatim; the nine second-batch normalizer runs reproduced every count and regenerated byte-identical files; 74 of the 79 FIXED items were confirmed present, two were not yet fixed, two partial, and one fix had introduced a defect of its own — the RE-SWEEP rule's reason, live.

**Fixed on the re-sweep (each named):**
1. [R2-1, HIGH] The `T3` disambiguation had gone the WRONG way — `M-4`'s *"after the T3 build"* is the LegiScan POLLER tier's `T3` (`Go_Live_Gates.md`'s LegiScan section, *"esp. the T3 build"*), not the transcription tier. Both spec mentions rewritten; `FOT-32` dated off the right build.
2. [R2-2] `fee_schedules` is keyed to a party (`payer_party_id`) — removed from the reference-table list.
3. [R1-10, R2-3] "fourteen pages" — ten pages under `src/pages/` (thirteen files counting the three adapters).
4. [R2-4] *"a date, a name, and it stays lit until done"* attributed to "his words" — now the entry's statement of `FO-3`, which he confirmed (*"Yeah. So it's right."*).
5. [R1-1] The `reconcil` hits mis-located (the Preamble ¶2 and Rule 1.04's family-law fee comment, not Rule 1.07) — corrected in the record.
6. [R1-2] The ch. 82 heading scan mis-named (§ 82.033 *FEES* and § 82.0361; § 82.034 is *USE OF FUNDS*) — corrected.
7. [R1-3, R1-4] `FOT-21` (the notary row) sat in a six-column table under a heading saying "no law read" — §7.6 rebuilt with eight columns and an honest heading.
8. [R1-5, R2-7] `FOT-9`'s "failure to file can forfeit" outran § 171.2515(a) as read (the grounds are in § 171.251, NOT READ) — the row now states the forfeiture POWER as read and names the grounds as unread; the weight marked Claude's call.
9. [R1-6] The Family Code Title 5 row mis-derived from § 79.036(a-1)'s *"Title 3"* (juvenile) — the two FA rows rewritten: Title 3 counted in the county's report (read); Title 5's second report and CLE as practitioner knowledge, NOT READ, NOT HELD.
10. [R1-7] §4.2's example "no appointments in a county → no report" contradicted `FOT-4` (not conditional) — the example removed.
11. [R1-8, R2-9] "his call and only his" survived on the sheet — dropped there too.
12. [R1-11] The `Q-FO-6` lean split between sheet and spec — the spec's §4.4, §5.1, `FOD-11`, `FOD-14` and §13 now carry the same lean (routine items on the register only).
13. [R1-12] "(wrong for dues and the PIR)" contradicted the same decision — now "insurance and the PIR".
14. [R1-13, R2-6] The sheet's "departs from his words" column applied unevenly — replaced by a "Weight source" column that marks the four rows carrying his `#137` words and every other weight as Claude's call.
15. [R1-14] §3.2's "§7.2" → "§7.3".
16. [R1-15] `FOD-10` and `FOD-11` now cited in the body (§5.3, §4.4).
17. [R1-17, R2-10] Quotation case — *"DISTINGUISHABLE and filterable out"*, *"BEHAVIOURALLY DISTINCT"* — restored.
18. [R1-19] § 151.0101(a)'s item (16) marked "paraphrased" in the record.
19. [R1-20] The workers'-comp row's "annual" (not in the text read) removed; § 406.005(b) (*"at the time the employee is hired"*) now quoted.
20. [R1-21] "a biennial row the statute no longer carries" → "the section as downloaded does not carry".
21. [R1-22] Three cells of unmarked practitioner knowledge or inference (`FOT-2`'s "15 hours", `FOT-1`'s "in practice, billed with the dues", `FOT-10`'s "near-certain") removed or marked HIS FACT.
22. [R1-23] Request date and `up_to_date_as_of` now on every TIER B row.
23. [R1-24] The category enum gains an employment home (`tax-entity-and-employment`); nine §7.7 rows now carry a status term (HIS FACT / PRACTICE).
24. [R1-25] The §8 and DECISION 8 statute lists completed (§§ 171.002(d), 171.204(b), 79.036(a-1), 406.010).
25. [R1-26] The `#150` texts say both "items 8–11" (the packet's §7) and "items 8–12" (the handoff addendum) — the ledger now says so.
26. [R1-27] *"your hand, your browser, demo events only"* attributed to BUILD-STATE, not to the exercise doc.
27. [R2-8] "a paralegal will file the PIR and the 941s" stated a firm fact — now hypothetical ("a future paralegal might").
28. [R2-12] The ledger's `src/` line cites pinned "at `ea5675b`".
29. [R2-13] The bracket convention `[A1]`…`[D31]` declared at the head of §2.
30. [R2-14] "the `#104` failure mode" — reworded to BUILD-STATE's own attribution.
31. [R2-15] The cause-number token itself no longer reproduced in the ledger.
32. [R2-16] "where matter reminders are headed" → the heartbeat DESIGN, unbuilt and unauthorized.
33. [R2-17] DECISION 3's option format normalized; DECISION 7's outside slot completed.
34. [R2-18] `AS-Q1` (ruled 2026-08-31, unbuilt) named beside `Q-WF-4` as the first instance of a server-side function.
35. [R1-9, R2-5] The ledger's placeholder time and empty §3 — filled at the close (this report's own §5 records the time).

**Declined on the re-sweep:** [R1-16] R1 read *"behaviourally distinct from case reminders, which are snoozable"* as the capture's gloss absent from the log entry — it is IN the entry (line 1965 at `ea5675b`, *"BEHAVIOURALLY DISTINCT from case reminders, which are snoozable"*), as R2 found; the attribution stands, the case was fixed. [R2-11] The `#137` heavy set — the entry's enumeration in the `FO-1` paragraph and the capture's *"when the set was named back to him"* — is one step removed from his words; the spec now says so and the sheet puts `FOT-6` to him. [R2-19] The 2026-07-24 DDL date rests on the schema's own comment and the Outlook build entry — fine as written, the auditor agreed.

**Noted, no change:** [R2-13]'s observation that `D3` also names a transcript design decision in `db/schema.sql` — the bracket convention is now declared; the record's own `D`-series is untouched.

## §5 — Close of the preflight

The fix-of-the-fix pass touched thirty-five places; each was re-read by the session after the edit (a grep for the old wording — "his read confirms", "the only cross-matter surfaces", "FIRST table keyed to neither", "fourteen pages", "the transcription `T3`", "fee_schedules", "his call and only his", "wrong for dues", "near-certain", "no longer carries", "15 hours", "will file the PIR" — hits ONLY this report, where the old wording is quoted as the finding, and no other shipped file). No third fleet was run: the re-sweep's findings were wording and attribution, none of them a legal characterization the sources had not already been checked against, and the RE-SWEEP rule's minimum — a normalized re-sweep of every claim the fixes touched — is the grep pass just named plus the two auditors' PASS lists. Time of close: recorded in the ledger's §3.
