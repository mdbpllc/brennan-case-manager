# Design-pass ledger — the firm-obligations design pass (2026-09-07 evening, Central)

**Status:** EVIDENCE (`CAP-2`) — the running ledger of the sitting that produced `docs/specs/firm-obligations-module-spec.md` and `docs/specs/firm-obligations-ruling-sheet-2026-09-07.md`. Every HEAD fact those documents rest on is here with the command that produced it (`QR-6(a)`: a verification names its command). **Michael made NO ruling in this sitting** — he delegated the choice of next step (*"Run the next step that you see fit. Don't worry about it being too token heavy. Make it a very productive run and then let's package to Code."*) and the sitting ran autonomously; every design statement it produced is PROPOSED.
**Canonical repo path:** `docs/record/firm-obligations-design-2026-09-07/design-pass-ledger-2026-09-07.md`
**DT-1:** 2026-09-07 Central throughout (opened 20:53 CDT; the container clock read 2026-09-08 UTC from the first minute and was never used for a stamp; the Central clock was read from the shell with `TZ=America/Chicago date` at each stamping point).

---

## §0 — SESSION START, VERIFIED

- **Surface:** typed design session, Cowork, Fable 5.1 per the environment (`claude-fable-5-1`); the device bridge granted at 20:53 CDT on `C:\Users\Brennan\brennan-case-manager` and `C:\Users\Brennan\Documents\Knowledge Repo` (Michael answered the folder dialog at once — he was at the keyboard at open).
- **Checkout, lock-free (`GIT_OPTIONAL_LOCKS=0`; `git status` NEVER run):** `git rev-parse HEAD` → `ea5675b551f02048907e5de6daeac71e8fd4f02c`; `git rev-parse origin/master` → the same; **`git ls-remote origin refs/heads/master` → `ea5675b…` — a LIVE remote read (the bridge VM had network)**; `git rev-list --left-right --count origin/master...HEAD` → `0 0`; branch `master`; `ls .git/index.lock` → absent. Last eight commits (`git log --oneline -8`): `ea5675b` (verification pass, four repairs to the `CCS-1` build's own BUILD-STATE refresh) · `33741e8` · `75db340` · `c07f491` · `3edcf9e` (the `CCS-1` build, three commits) · `1e949d9` (batch 91, `#149`) · `f5bbdf9` · `9b2eb65`.
- **`inbox/`:** ONE zip — `push-to-code_ccs1-migrations-run_2026-09-07.zip` (44,025 B, mtime 2026-09-08 01:47 UTC = 20:47 CDT) — **the `#150` packet from the earlier evening sitting, UNRUN**. So batch 92 has not run, `#150` is not at HEAD, and **this session mints `#151`** — measured: the live log's highest DESIGN ordinal is `#149` (`grep -o '^## [^(]*(#[0-9]\+)' docs/record/session-log.md`, heading-anchored — the bare `grep -o '(#[0-9]\+)'` form returns a cause-number token the log itself annotates as not an ordinal, and is not the command that shows it); no `#150` or `#151` string anywhere in the log or the head file. The `#150` packet's manifest was read in the VM's `/tmp/p150` (RECONCILE FIRST): its next-acts line names *"(3) the firm-obligations design sitting"*; nothing in it touches this pass's paths.
- **Read whole:** `docs/specs/BUILD-STATE.md` (99,987 B by `wc -c`; 148 raw lines by `wc -l`; 134 non-blank — the `CCS-1` build's CODE refresh, `CAP-4` bound) — through the bridge in one `cat`, saved and read in full; `docs/specs/session-log-head.md` banner and §1 headings (the head file at HEAD predates the `CCS-1` build entry — its top is batch 91's runner line; the live log's top is the unnumbered `CCS-1` build entry, at line 20 at `ea5675b`, read whole); `#149` (as the byte-identical handoff in project knowledge and at HEAD); the `## 2026-08-22 (#137)` entry whole (lines 1920–2137 at `ea5675b`); the `#137` capture in project knowledge (`claude_Backup_Redundancy_and_Firm_Obligations_Capture_2026-08-22_Voice2.md`) whole; `docs/specs/case-heartbeat-design.md` §5–§6; `docs/specs/deadline-engine-spec.md` §2, §6, §9.2; `docs/specs/fc-adjudication-record-2026-08-18.md` FC-7–FC-11; `docs/specs/outlook-calendar-sync.md` whole; `docs/specs/statute-pass-registry-retrieval-2026-08-14.md` §3 (the normalizer); `docs/specs/id-collision-report.md` (the `FO`/`BR`/`DA` lines); `docs/record/queue-cleanup-evidence-2026-09-01/wave1_A7.md` (the `FO-7` analysis); the closed register's Status header (the roomless-ids sentence); `claude_Knowledge_Repo_Acquisition_List_2026-08-18.md` (project knowledge) whole.
- **`src/` and `db/` reads — for VERIFICATION ONLY, each named (the `#149` precedent; `Q-PR3-1`'s open question about design-side source reads is noted, not decided):** `db/schema.sql` (the `create table` census — 46 — the `calendar_events` DDL; the `created_by`/`set_created_by` lines; the `review_log` DDL and its `action` CHECK; the `glossary_terms` `scope` column); `db/migrations/2026-07-28-cl2-client-dimension.sql` (the only migration naming `review_log` — inserts only, no CHECK change); `src/App.tsx` (the `<Route path=…>` list: 28 elements, 27 distinct paths — `/auth/callback` is registered twice); `src/domain/calendar.ts` whole; `src/pages/CalendarTab.tsx` (the `createEvent … caseId` call; the cancel handler's `appendReviewLog({ action: 'cancelled' })`); `src/data/adapter.ts` (the calendar and review-log method signatures); `src/data/supabaseAdapter.ts` (`appendReviewLog` = `insertRow('review_log', entry)`); `src/data/localAdapter.ts` (`STORE_VERSION = 16`; `appendReviewLog`); `src/domain/billing.ts` (`ReviewLogEntry.action`'s union type); `src/outlook/graph.ts` lines 75–110 at `ea5675b` (`toGraphEvent`) and the `MATTER_PROP_ID` line; `src/outlook/config.ts` (`OUTLOOK_CALENDAR_NAME`); `src/components/WorklistCard.tsx` lines 1–40 at `ea5675b`; `src/pages/CaseListPage.tsx` lines 36–48 at `ea5675b`. Sweeps: `grep -rli snooze src db supabase` → none; `grep -rli 'quickbooks\|qbo\b\|intuit' src db supabase` → none (and `grep -rlic qbo` → 0 without the boundary); `grep -rn '"/firm' src` → none; `grep -n 'firm_' db/schema.sql` → none; `grep -rhn "action: '" src` → the review-log action values written in `src/` (`cancelled` from `CalendarTab.tsx` is the one outside the CHECK; `added`/`amended`/`held`/`not-case-related` are routing and act-chain values, not `review_log` writes).
- **How the reads were done:** the checkout's `docs/`, `db/`, `supabase/`, `src/`, `CLAUDE.md`, `README.md`, `BUILD-SESSION-NOTES.md` were zipped ON THE DEVICE (437 files, 4,706,935 B, sha256 `6995245af8b6053c523bdfe7f904d4c64ebaa46976f9c067454e49c65376e0c3`) into `Documents\Knowledge Repo\Claude outputs\repo-snapshot-ea5675b_2026-09-07.zip`, staged once, sha256-matched in the container, and unzipped there for reading — a read-only snapshot of HEAD `ea5675b`. **Nothing in the checkout was written.** Bridge scratch this session leaves for Michael's hand (device_bash cannot delete): that zip, and `Documents\Knowledge Repo\Claude outputs\fo-sources-2026-09-07.zip` (434,041 B, sha256 `6e64c35b15245d494f248d37ce88c784847bce8dbd5dbf8b4824cd6fc2c2f75f`) and `fo-sources-2026-09-07-b.zip` (511,158 B, sha256 `40afa702268059e7a85b6e6edeb058337975db61e4d52544273a0ea08cbaf00a`) — the normalized statute chapters, the TDRPC text, the excerpt files and the normalizer, staged for the preflight agents. Chapter PDFs were extracted into the VM's own `/tmp/stat`, and the `#150` packet into `/tmp/p150` — not mounted paths; nothing further landed in a connected folder.
- **Knowledge meter at open (read directly, `project_info`):** `knowledge_size` 1,602,101 / `max_knowledge_size` 2,000,000 = **80.1%** — `Q-CAP-5(a)` did NOT fire. Up 54,283 units from `#149`'s close (1,547,818): the `#149` capture + handoff + two email drafts entering project knowledge, and batch 91 + the `CCS-1` build syncing — consistent, not measured to the unit this time. Thirteen project docs listed.
- **CC-1 hands-on queue at open (stated):** 12 accepted, all gated — `CR-7`, `CR-CONSTRAINT`, `Q-FE6-5`, `Q-FE4-1`, `Q-FE5-3`, `Q-IN2-7`, `Q-IN1-1`, `Q-IN3-6` (unbuilt modules); `DA-1`, `DA-3`, `DA-4` (document storage, gate 7); `FO-6` (no firm-obligations concept in the calendar code) — plus 7 proposed (`D-18`, `D-8`, per-paragraph regenerate, `AS-Q14`–`AS-Q17`), plus the post-`CCS-1` walk's agenda from the `#150` packet (eleven text acts, nineteen `SD-` defaults with `SD-3`'s veto, the confirm marks, spec-feedback items 8–11 as the `#150` packet's §7 names them (its handoff addendum says 8–12, as does the build entry — the two `#150` texts differ by one) — and the `CAP-2` exemption wording). **At close the queue gains five PROPOSED hands-on items from the spec §11 (same gate as `FO-6`), proposed until he accepts them.**
- **The choice of step, and why (recorded because he delegated it):** BUILD-STATE's "NEXT ACTS, IN ORDER" at `ea5675b` puts the firm-obligations design sitting FIFTH (after the v30 paste, the two migrations, the post-`CCS-1` walk and the `H12-v` asks); the `#150` packet's line puts it third; the `#149` handoff's addendum says *"then either the walk or, if he prefers, the firm-obligations sitting"*. The walk needs him at DEMO in his Chrome, one question at a time; the design pass is an open design pass with no prior art — a Fable act under MODEL USAGE — and can run alone. With the Fable allowance expiring the next afternoon and his standing instruction to SPEND it, the design pass was taken and the order overridden under his delegation. The walk stays next on its own track. Michael was told this at 21:06 CDT and offered the switch; he did not redirect.

## §1 — WHAT WAS DONE, IN ORDER (times CDT)

1. 20:53 — bridge granted; checkout verified (§0). 20:56 — the repo snapshot zipped and staged.
2. 20:56–21:05 — BUILD-STATE read whole; the live log's top entry, `#149`, `#137` read; `FO-1`–`FO-7`, `BR-`, `DA-` traced through the log, the register (0 rows), the closed register (header only), the collision report and `wave1_A7.md`; the calendar model, routes, snooze/QBO sweeps and the Outlook push read (§0).
3. 21:00–21:10 — primary sources, first round: seven statute chapters extracted and normalized with the §3 normalizer; TDRPC 1.15 read; eCFR `titles.json` read (up_to_date_as_of 2026-09-03); the eCFR full-text API refused by robots for this session's fetch layer — the human-readable pages read THROUGH the fetch layer and marked TIER B; 45 CFR 164.308 returned 503 (NOT READ).
4. 21:06 — Michael told the step and its reasoning; the switch offered.
5. 21:10–21:18 — the source-read record, the spec and the ruling sheet drafted; `Q-FO-`/`FOD-` collision-checked free repo-wide at `ea5675b`.
6. ~21:20–21:45 — **PF-1 preflight**, four auditors in parallel (the report is the third EVIDENCE file): HIGH 8 / MEDIUM 38 / LOW 56 across the four, most of the LOWs cosmetic and every HIGH real.
7. 21:45–21:52 — primary sources, second round, on the preflight's findings: nine more chapters extracted and normalized (`gv.406`, `es.1054`, `hs.181`, `tx.22`, `tx.31`, `tx.151`, `la.406`, `la.204`, `la.213`); the `FA` zip found ABSENT from the corpus; the `review_log.action` CHECK and the `CalendarTab.tsx` cancel path read (verify before criticizing: `Q-RE-6` records the sibling gap; the live-write mismatch was not on the record).
8. 21:52–22:04 — the spec, the sheet and the record rewritten on the findings; `FOT-` collision-checked (0); the PF-1 report written with every disposition.
9. 22:05–22:28 — RE-SWEEP AFTER FIX, two auditors over the revised files (R1 sources and fixed items; R2 HEAD facts and conventions): HIGH 1 / MEDIUM 20 / LOW 25 between them — the HIGH was the fix pass's own wrong turn on `T3` (the LegiScan poller tier's build, not the transcription tier), the RE-SWEEP rule's reason live.
10. 22:28–22:37 — thirty-five re-sweep findings fixed, three declined with reasons, the PF-1 report's §4–§5 written; a grep for every old wording hits only the PF-1 report itself.
11. 22:37 onward — the session-log entry `#151`, the `spec-feedback.md` section, the manifest, the capture pair; RR-1 (below); the zip; the bridge save; the closing report — §3.

## §2 — DESIGN CHOICES THIS SESSION TOOK, ALL PROPOSED (so the record shows where Claude's hand is)

- The vocabulary split between "firm obligation" (`FO-1`) and "firm deadline" (`FC-7`) — a collision nobody had named.
- The one-open-occurrence model; DONE and — only where a duty can lapse — NOT-APPLICABLE-with-a-reason as the closes; serial vs collapsed materialization by rule kind — the concrete form of `FO-2`, every extension numbered as a default.
- Two tables rather than a widened `calendar_events` — a recommendation for his confirmation (`Q-FO-1`).
- Thin with no hook after verifying `FO-7`'s two facts — a lean, put as `Q-FO-4`.
- The Outlook shape (a separate calendar with a reminder at the lead window; the To Do route named) — a lean, put as `Q-FO-5`.
- The nineteen `FOD-` defaults — each reported for his veto.
- The seed catalog's thirty-five `FOT-` templates, their kinds, leads and weights — templates only; no date and no firm fact inferred; the weight departures from his `#137` words named on the sheet.
- The registry-boundary question — put, not answered.
- The five new hands-on items — held for the product, PROPOSED for the queue, not pressed.

## §3 — CLOSE

- **RR-1:** every document authored earlier in the sitting was re-read against every later act — the spec and the sheet against the PF-1 and re-sweep dispositions; the record against the second-round reads; the ledger against the final counts; the entry, the manifest and the capture pair against all of them. The ID inventory at close: `FOT-1`–`FOT-35`, `FOD-1`–`FOD-19`, `Q-FO-0`–`Q-FO-10`, `DECISION 0`–`10` — all packet-local, all declared where they live; nothing minted.
- **Knowledge meter at close:** read directly at the end of the sitting and stated in the closing report (the capture pair's entry into project knowledge moves it; the figure is not predicted here).
- **CC-1 hands-on queue at close:** twelve accepted (all gated, unchanged) + seven proposed (unchanged) + five PROPOSED by this pass (the spec §11; gated on the module built on fixtures in demo mode) + the post-`CCS-1` walk's agenda (`#150`).
- **Packet:** `push-to-code_firm-obligations-design-pass_2026-09-07.zip` → `inbox/` on `mdb-pllc` over the bridge, beside the `#150` zip; contents per the manifest's §2; `CHECKSUMS.txt` inside; the closing report states the re-listed result.
- **Delivered to Michael as files:** the packet zip, the manifest, the spec and the ruling sheet; the capture + handoff written to project knowledge.
- **Bridge scratch left on his machine, named for his hand:** the three zips in `Documents\Knowledge Repo\Claude outputs\` (§0); nothing else; no lock stranded; `git status` never run; the VM's `/tmp/stat` and `/tmp/p150` are the VM's own and not his.
- **Closed at 22:39 CDT, 2026-09-07 (Central; the container clock read 2026-09-08 UTC).**
