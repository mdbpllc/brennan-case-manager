# mock-source-2026-09-08 — the source of `firm-obligations-mock-2026-09-08.html` (EVIDENCE)
`domain.js` — the spec's §3–§4 as pure functions (the FOM- switches select PROPOSED alternatives); `fixture.js` — the fictional fixture (two registers; every date invented; the 36 catalog rows' strings copied from spec §7); `ui.js` — the three surfaces and the decision panel; `styles.css` — the product's tokens (`src/index.css` values) plus the workbench; `build.py` — assembles the single file and injects the ruling sheet's DECISION texts verbatim (it carries the container paths it ran with; evidence of the build, not a repo script); `test/domain.test.js` — the harness (`node test/domain.test.js`); `test-run-2026-09-08.txt` — its run at the final build, 124/124.
Nothing here is product code and nothing here is built into the app.
