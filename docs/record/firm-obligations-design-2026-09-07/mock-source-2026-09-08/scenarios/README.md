# scenarios — the headless click scripts that checked the mock (EVIDENCE)
Playwright scripts run against `dist/firm-obligations-mock-2026-09-08.html` in headless Chromium (1480 × 1000), in this order:
`look-1-first-look.mjs` — the one rendered look (three screenshots) and one Done end to end, before the audit; `look-2-after-audit-fixes.mjs` — after the audit's fix pass: two Dones then Undo, the retire path (its retire click missed the expander and was redone in look-3), a one-time Done, the Sunday state, the multi-user preview, the To Do projection; `look-3-retire-and-weekend.mjs` — Retire via the expander, Done on the retired row, the Sunday badge; `look-4-resweep-scenarios.mjs` and `look-5-resweep-scenarios-b.mjs` — the RE-SWEEP's scenarios re-driven after its fix pass: the Sunday item on the card and in the current month, Retire → Done → Re-activate → Undo refused, option 3's full fan-out, no owner badge at solo, the card-position toggle, a custom word in the Outlook subject. Every run reported zero page/console errors. The scripts carry the container paths they ran with (`/home/claude/…`, the global Playwright at `~/.npm-global`); they are evidence of the checks, not scripts the repo runs. The screenshots they wrote are not shipped.
