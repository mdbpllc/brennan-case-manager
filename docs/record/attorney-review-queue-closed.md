# ATTORNEY REVIEW QUEUE — CLOSED REGISTER (repo-only, bridge-reachable, append-only)

> **Created 2026-09-02 by the one-time split ruled at `CAP-3` (`docs/specs/capacity-pass-2026-09-02.md` §3.3).**
> Every ✅ row of `docs/specs/attorney-review-queue.md` lives HERE, text intact, under the section heading it sat
> under in the register; the register carries ⬜ and 🟡 rows only, plus any top-level ✅ parent held there with
> an indented open child (eleven at the split — CAP-OPEN-2). The queue runner APPENDS a row here when it
> flips it to ✅ (runner v13, Step 4 item 2, third act) and never edits or removes one. `docs/record/` is excluded
> from the design-side sync by the standing `TC-4` exclusion — **absence from design-side retrieval is BY DESIGN and
> is never evidence of absence.** Cite by heading or quoted sentence (CITE-STABILITY).

## RECONCILE HISTORY — the register's superseded per-batch sentences, oldest first, moved verbatim

> The register's Status paragraph carries only the CURRENT "Reconciled again…" sentence (`CAP-3`); when the runner
> writes a new one it appends the superseded sentence at the END of this block.

**Moved header text, verbatim, joined as it stood in the register:** **Reconciled to session-log #28 on 2026-07-28 (BS-1a item 10)** — §7 was added in that pass, and `BUILD-STATE.md` now points here instead of carrying its own roster. **Reconciled again to session-log #34 on 2026-08-07** (rulings batch: FE-1, FE-2, PR-3, QR-1, RR-1, D-CL2-3, CL2-AC-1; the FE series was absent from this file entirely and was added). **Reconciled again to session-log #38 on 2026-08-08** (contact-directory reframe: CD series added and CD-1 issued, FE-1 annotated superseded, FE-2 build home parked, CL2-AC-1 to direction-confirmed, CORPUS-HOME closed). **Reconciled again to session-log #39 on 2026-08-08** (QR-2 CLOSED; CL2-AC-1's two locations consolidated into the ID-bearing entry; two sweep items entered). **Reconciled again to session-log #44 on 2026-08-09** (queue-runner batch, eighth invocation: the T3/Phase 0 advisory H series added with full question text per QR-1, its H1–H6 collision with the heartbeat register flagged rather than renumbered; the P15/P1 runner-copy item narrowed to P15 only). **Reconciled again to session-log #46 on 2026-08-10** (queue-runner batch, ninth invocation: QR-4 entered with full text and CLOSED the same day it was proposed; the Q-3 `docs/skills/` sync-picker re-check entered with full text, having never been registered here; FE-1 annotated with the provider-data evidence channel, which now feeds CD-1). **Reconciled again to session-log #48 on 2026-08-11** (first REQ-1 REQ-CAPTURE: CD-2 framing ruled, FE-4–FE-7 and new IN series entered, §4 registry batch VERIFIED, templates home ruled, CD-1 path ruled as bounded-evidence-pass-then-session). **Reconciled again to session-log #58 on 2026-08-12** (queue-runner batch, twentieth invocation: REQ-CAPTUREs 4 and 5 — the **DE series added**, ruled into existence by Michael; CL-3, CD-3, FE-13–FE-17, IN-6, IN-7 entered with full question text per QR-1; FE-10, FE-11, FE-12 amended in place; CD-2 annotated; the FE-13–FE-17 gating question and the registry-verification backlog entered). **Reconciled again to session-log #59 on 2026-08-12** (the Uvalde docket-worksheet capture filed UNRECONCILED at §5 — sixth REQ-1 capture, first from CRIM DEFENSE, no durable IDs assigned and no registry entries opened, because it arrived by Michael's hand rather than through a packet). **Reconciled again to session-log #60 on 2026-08-12** (queue-runner batch, twenty-first invocation: the registry-placement flag CLOSED as confirmed, the REQ-10 placeholder-discipline carry given the queue row it never had, and the registry count reconciled at 27). **Reconciled again to session-log #62–#63 on 2026-08-12** (queue-runner batch, twenty-second invocation, two packets: the Uvalde capture RECONCILED and its row replaced by the new **CR series** CR-1–CR-11 plus the binding B&W constraint; registry backlog 27 → **33**; `party_type` retention CONFIRMED; **FE-gating CLOSED** and FE-8–FE-17 each annotated with its FE-D1 disposition; distillation candidates CLOSED into §13.4; **WF-2–WF-8** entered with gates; three form-engine §11 items registered that had no home; the GPU-telemetry item registered and ruled). **Reconciled again to session-log #65 on 2026-08-13** (queue-runner batch, twenty-fourth invocation: the cite-check memo pointer added add-only to the registry-backlog row; V-1, V-2, V-3 entered with full question text per QR-1). **Reconciled again to session-log #66 on 2026-08-13** (queue-runner batch, twenty-fifth invocation, the RULING RUN: ~20 rows closed or annotated in one pass — V-1, V-3, O1, O3, O5, Q-5, SAT-1, H22, D-CL2-3a, DL-INPUT, the demographics/OAA question, and the Go_Live_Gates paste-ask all CLOSED; O4, CL2-CHECK-1, the IME/204.1 row, and four heartbeat questions (H40, H50, H60, H77) annotated with their rulings while their rows stay open; **the T3/Phase 0 advisory series RE-LETTERED H → HK** by ruling, ending its collision with the heartbeat H1–H83 register; registry backlog **33 → 34** on the entry-4 split). **Reconciled again to session-log #68 on 2026-08-13** (both sweeps ruled and run: duplication CLOSED no-duplicate with three pointer glyphs stripped; status-drift CLOSED as a pass at findings #2–#9 with all fixes annotated; OBS-1 entered). **Reconciled again to session-log #72 on 2026-08-13** (queue-runner batch, twenty-eighth invocation: the registry workbook + citator pass filed as PROPOSED design input; **V-2 annotated add-only and NOT closed**; **V-4–V-8 entered with full question text per QR-1**; the packet's un-numbered `HK-*` device-bridge ask given the ID **HK-7** after collision-check). **Reconciled again to session-log #73 on 2026-08-13** (ruling run, same session as the batch that entered them: **V-4 through V-8 all RULED, one at a time** — keep-as-two, split-all-three, keep-and-reword, narrow-23, record-all-three — **every one of them EXECUTION-PENDING DESIGN-SIDE, no registry file and no design doc touched**; **V-9 opened** by the V-8 ruling, carrying the binding-rule gap the *Irwin* class exposed). **Reconciled again to session-log #74 on 2026-08-13** (queue-runner batch, twenty-ninth invocation: the CD-2 role mining pass filed as PROPOSED design input; the packet's packet-local **Q1–Q10 assigned durable IDs CD-4 – CD-13** after a repo-wide collision check found the CD series at CD-3, with full question text per QR-1; **HK-7 annotated SATISFIED-FOR-ONE-SESSION and deliberately NOT closed**, carrying the two new device-bridge findings that bear on QR-3 — CRLF-false-DIRTY and no-network-`git fetch`). **Reconciled again to session-log #75–#76 on 2026-08-14** (queue-runner batch, thirtieth invocation, two packets: the deadline-engine research memo and the statute pass both filed as PROPOSED design input; **`Q-STAT-1` – `Q-STAT-6` entered with full question text per QR-1**, collision-checked repo-wide with no prior `Q-STAT` ID anywhere; **the deadline memo's five questions entered with full text but WITHOUT durable IDs** — the packet forbade minting `DL-1` by name, `V-10…V-14` would collide with the unhyphenated V10–V17 residuals in §§1–2, and `DE` is the deficiency series, so they are filed under packet-local IDs and the ID assignment is itself a row below (**ID-DL-1**, and #59's Uvalde capture is the precedent for filing without durable IDs); **HK-7 annotated SATISFIED-FOR-ONE-SESSION a second time and again NOT closed**; two count corrections reported and applied to BUILD-STATE only — `RETRIEVAL: NOT RUN` is 21 rows not 20, and the registry is four files not three, with the **backlog unchanged at 34**). **Reconciled again to session-log #77 on 2026-08-14** (queue-runner batch, thirty-first invocation, one packet, no staged file: **Q-STAT-1 RULED and CLOSED — adopt as drafted**, its original question preserved verbatim beneath the ruling per QR-1, and **Q-STAT-2 through Q-STAT-6 left open by the packet's own instruction**; **two new rows entered — paste v18** (his hand; it supersedes the revised v17, **which was in fact pasted**, correcting a BUILD-STATE line carried since 08-13) **and the route-(c)-as-standing-law question**, which had no row here and is held for the Fable review pass). **Reconciled again to session-log #78 on 2026-08-15** (queue-runner batch, thirty-third invocation, one packet, no staged file, a correction to the record: the #76 Insurance-Code absence claim was **over-broad** — the fourth registry file was never searched and carries ENTRY 8 on Ins. Code ch. 1467 — so the two passages in `statute-pass-registry-retrieval-2026-08-14.md` were corrected **in place** and **Q-STAT-2 and Q-STAT-5 were ANNOTATED, not answered**; chs. 541/542 remain absent from all four files on a re-verified search, so **no candidate was withdrawn, no count moved, and the backlog stays 34**). **Reconciled again to session-log #79 on 2026-08-15** (queue-runner batch, thirty-fourth invocation, one packet: the chartered **record-integrity audit** filed as PROPOSED design input — 46 claims sound, 6 stale, **1 confirmed-false** (the carrier-duties "TWENTY VERIFIED" is 22 on disk), 1 contradicted, 11 checkable only by Michael; **OPEN-1 – OPEN-5 entered with full verbatim question text per QR-1** after a repo-wide collision check found no `OPEN-<n>` ID anywhere; **the paste-v18 row ANNOTATED with AUD-1 and deliberately NOT closed** — v18 appears already in force, and that is a design-side-only fact Code cannot confirm; **the route-(c) and Q-STAT-3 rows annotated add-only with the audit's two recommendations, neither closed and neither ruled. NOTHING from the audit was applied** — the stale figures stay in BUILD-STATE, each with a pointer, until OPEN-5 is ruled). **Reconciled again to session-log #80 on 2026-08-15** (queue-runner batch, thirty-fifth invocation, one packet: **CHAT-DISPATCH Task 7** — the Bexar local rules + eFileTexas memo filed as PROPOSED design input with its TIER A/TIER B provenance markers intact; **its six questions entered with full verbatim text per QR-1 but NO durable IDs** — `LR-`/`EF-`/`BX-` were checked free and deliberately not minted, so **ID-DL-1 now governs two packets** and was annotated accordingly; **H43 given a row here for the first time**, cross-linked to the heartbeat register rather than renamed; **HK-7 annotated satisfied-for-one-session a third time and again not closed**; the two `LR-LOOK` looks recorded as Michael's. **Nothing built, nothing verified, no registry file touched, backlog still 34, and none of the six repo docs the memo reconciles against was edited.**). **Reconciled again to session-log #81 on 2026-08-15** (queue-runner batch, thirty-sixth invocation, one packet: **CHAT-DISPATCH Task 8** — the three form-engine specs FE-4, FE-5 and FE-6 filed as PROPOSED design input with their TIER A / FLP provenance markers intact; **twenty-two questions and three named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-FE4-`, `Q-FE5-`, `Q-FE6-` and `FE5-LOOK-` re-checked repo-wide, all free, all deliberately not minted, so **ID-DL-1 now governs THREE packets**; **the FE-4, FE-5 and FE-6 rows annotated with their specs and left ⬜** — a spec is not a closure and the build is still unauthorized; **the headline is Q-FE5-9, a divergence inside a VERIFIED entry** — the TRCP 190.3(b)(3) entry's *"each party"* against the rule's *"any other party,"* different budgets whose gap scales with the number of opposing parties — **flagged, not reworded, and no registry file touched**; **HK-7 annotated satisfied-for-one-session a fourth time and again not closed**; Q-STAT-5, OPEN-4 and the route-(c) row annotated add-only, none closed; **one runner-found defect entered** — the FE-5 spec as staged is missing its title, status and §1 head, landed verbatim anyway and reconstructed by nobody. **Nothing built, nothing verified, backlog still 34, and `form-engine.md`, `fe-d1-build-slice.md` and every `legal-rule-registry-*` file were left untouched.**). **Reconciled again to session-log #82 on 2026-08-15** (queue-runner batch, thirty-seventh invocation, one packet, no staged file — the Fable disposition batch: **the paste-v18 row RULED and CLOSED ⬜→✅ (A-1, from AUD-1), original text preserved verbatim beneath it**, on the ruling and a design-side inference rather than on anything Code can verify. **No other row moved, and nothing else in this file was touched** — OPEN-1 through OPEN-5 stay open where the audit put them, the registry rows and `Go_Live_Gates.md` were fenced off by the packet's §3, and the backlog stays 34). **Reconciled again to session-log #83 on 2026-08-15** (queue-runner batch, thirty-eighth invocation, one packet: **CHAT-DISPATCH Task 9** — the two intake specs IN-1 and IN-3 filed as PROPOSED design input; **eighteen questions entered with full verbatim text per QR-1 but NO durable IDs** — `Q-IN1-` and `Q-IN3-` re-checked repo-wide, both free, both deliberately not minted, so **ID-DL-1 now governs FOUR packets** and was annotated with the edge the earlier three lacked: **Q-IN1-8 asks whether the `IN` series stands at all**, so a rename would land on the namespace ID-DL-1 is deciding how to join; **the IN-1 and IN-3 rows annotated and both left ⬜** — and unlike Task 8's, **neither item has a ruling to elaborate**, so both specs map the design space and hand the question back rather than elaborating anything; **HK-7 annotated satisfied-for-one-session a FIFTH time and again not closed**, carrying a THIRD bridge finding — the bridge is the probable source of the stale `.git/index.lock`, corroborated natively this batch as a second occurrence, **while the CRLF false-DIRTY did NOT reproduce natively**; **OPEN-4 annotated a seventh time**, resume point Task 10, ten tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **Reconciled again to session-log #84 and #85 on 2026-08-15** (queue-runner batches, thirty-ninth and fortieth invocations — **recorded together because the #84 sentence was omitted from this header when that batch ran**; its rows landed normally and `Q-IN2-1` – `Q-IN2-9` are in §7 above, so what was missing was the header line, not the reconcile. **#84** = CHAT-DISPATCH Task 10, the IN-2 crash-report extraction spec, nine questions with full text, no durable IDs. **#85** = **CHAT-DISPATCH Task 11** — the WF-2–WF-8 email-workflow spec filed as PROPOSED design input; **ten questions and two named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-WF-` and `WF-LOOK-` re-checked repo-wide, both free, both deliberately not minted, so **ID-DL-1 now governs SIX packets** and was annotated with this packet's own edge: **Q-WF-2 raises a SECOND naming question of the same class**, the `T3` collision, so two open naming rulings now sit side by side; **all seven WF rows annotated and ALL LEFT ⬜** — the spec **MAPS rather than elaborates**, because #63 adopted a document, not a design; **seven of seven gates re-verified UNSATISFIED**; **the BAA/compliance review was NOT added as an eighth gate and the `T` series was NOT re-lettered** — both are rulings; **Q-WF-10's pointer annotations deliberately NOT added**, since doing them would answer the question; **HK-7 annotated satisfied-for-one-session a SEVENTH time and again not closed**, carrying the first *negative* bridge datum — the predicted stale `.git/index.lock` was **absent**, making the pattern three-for-four; **OPEN-4 annotated a NINTH time**, resume point Task 12, eight tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **Reconciled again to session-log #86 on 2026-08-15** (queue-runner batch, forty-first invocation, one packet: **CHAT-DISPATCH Task 12** — the PR-3 re-parenting migration proposal filed as PROPOSED design input; **seven questions and one named look entered with full verbatim text per QR-1 but NO durable IDs** — `Q-PR3-` and `PR3-LOOK-` re-checked repo-wide at HEAD, both free, both deliberately not minted, so **ID-DL-1 now governs SEVEN packets**; **the PR-3 row annotated add-only and left ⬜ with its execution hold unchanged** — a proposal is not a closure, and the proposal's own target-state section is deliberately a question because PL-1..PL-4 are unruled; **PL-1..PL-4 and D-CL1-3 annotated, none narrowed, none answered**; **the headline is `Q-PR3-2`, a defect in a ruled constraint** — `sideSetFor()` (`roster.ts:195–202`) falls through to plaintiff-defendant for every non-Criminal practice area under a header claiming REQ-14 is satisfied *by construction*, **re-confirmed at HEAD by the runner and changed nowhere**; **`Q-PR3-3` is live** — the unrun CD-1 item-7 migration's two-value IN list makes the PR-3/CD-1 ordering consequential, and the quieter failure is the order currently scheduled; **the three superseded passages deliberately NOT annotated**, since annotating them answers `Q-PR3-5`; **HK-7 annotated satisfied-for-one-session an EIGHTH time and again not closed**, with the lock hypothesis now three-for-five; **OPEN-4 annotated a TENTH time**, resume point Task 13, seven tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, no schema file touched, nothing re-parented, backlog still 34.**). **Reconciled again to session-log #87 on 2026-08-15** (queue-runner batch, forty-second invocation, one packet: **CHAT-DISPATCH Task 13** — the QBO integration research memo filed as PROPOSED design input with its `[V-1]`/`[V-3]` provenance tiers intact; **eight questions and four named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-QBO-` and `QBO-LOOK-` checked repo-wide at HEAD, both returned **ZERO**, both deliberately not minted, so **ID-DL-1 now governs EIGHT packets** and was annotated with this packet's edge: **there is no nearest durable series to fold into**, because no QBO or money series exists and the module is unruled by BUILD-STATE's standing *"NO MONEY MACHINERY"* line; **the headline is `Q-QBO-2` — there is NO read-only OAuth scope**, so "read-only first" can be a property of the code but never of the grant; **`Q-WF-4` annotated with a SECOND consumer** (`Q-QBO-3`: QBO needs a confidential client and a *rotating* refresh-token home, and gate 4's static-key comparator does not transfer); **`Q-WF-6` and `Q-STAT-5` annotated with a THIRD registry absence of the same shape** — `iolta`/`trust`/`safekeeping`/`commingl`/`client funds`/`escrow` all **zero** across all four registry files on a natively re-verified sweep, under a constraint whose stated stake is a bar complaint; **`future-modules-capture-2026-07-28.md`, `Go_Live_Gates.md`, every `legal-rule-registry-*` file, `db/schema.sql` and every migration were NOT touched** — editing any of them would answer `Q-QBO-1`, `Q-QBO-4` or `Q-QBO-6`; **HK-7 annotated satisfied-for-one-session a NINTH time and again not closed**, carrying the first *cost* of a repo-only grant — `QBO-LOOK-4` was NOT RUN for want of the Knowledge Repo folder; **OPEN-4 annotated an ELEVENTH time**, resume point Task 14, six tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **Reconciled again to session-log #88 on 2026-08-16** (queue-runner batch, forty-third invocation, one packet: **CHAT-DISPATCH Task 14** — the RE-1 referral-engine inputs memo filed as PROPOSED design input with its `[A]`/`[B]`/`R` provenance marks intact; **nine questions and three named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-RE-`, `P-RE-` and `RE-LOOK-` checked repo-wide at HEAD, all three returned **ZERO**, all deliberately not minted, so **ID-DL-1 now governs a NINTH packet** and was annotated with this packet's edge: **`RE-1` is a live durable ID in this very file**, so `Q-RE-1` reads as if it belonged to it and does not — the closest collision-by-appearance yet; **the RE-1 row annotated add-only and left ⬜** — it has had no design-doc home since it was raised, and the memo maps rather than designs; **`Q-STAT-5`, `Q-WF-6` and `Q-QBO-6` annotated with a FOURTH registry absence of the same shape** — fourteen fee-sharing, referral and disciplinary terms **zero** across all four registry files on a natively re-verified sweep, **and at four it is a scope rather than a run of gaps: the registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE, a boundary nobody chose**; **`QBO-LOOK-4` annotated ANSWERED AS TO THE CORPUS HALF: NO** — none of SOURCING's three layers reaches the TDRPC, which sit in a Government Code appendix the bulk corpus does not publish, while three statutes the corpus *does* carry incorporate them by name; **HK-7 annotated satisfied-for-one-session a TENTH time and again not closed**, carrying a new bridge limit — a bridge read shows a LOCAL head and nothing about origin; **OPEN-4 annotated a TWELFTH time**, resume point Task 15, five tasks still on a clipboard; and **`QR-5` entered and CLOSED the same day it was ruled** — the session-log entry may assert no post-commit action, the deletion is verified, and the runner goes to **v8**. **Nothing built (§5 NONE), nothing verified, no registry file touched, no schema file touched, backlog still 34.**). **Reconciled again to session-log #89 on 2026-08-16** (queue-runner batch, forty-fourth invocation, one packet: **CHAT-DISPATCH Task 15** — the communications-log ingest research memo filed as PROPOSED design input with its `[A]`/`[B]`/`[C]`/`R` provenance marks intact and **`[C]` NOT promoted to `[B]`**; **twelve questions, four named looks and five propositions entered with full verbatim text per QR-1 but NO durable IDs** — `Q-COM-`, `COM-LOOK-` and `P-COM-` checked repo-wide at HEAD, all three returned **ZERO**, all deliberately not minted, so **ID-DL-1 now governs a TENTH packet**; **the merge is TWENTY-ONE items, not the sixteen the packet's own §0 and §3 state** — row 17 of its §7 collapses `P-COM-1..5` into one label — **and it was sourced from the memo's §11, §12 and §9.1 rather than from §7, because §7's table does NOT carry full question text and says so itself**, so a verbatim §7 merge would have destroyed twenty-one questions, the precise failure that retired K-6/K-7; **the headline is that the dispatch called two constraints "ruled" and the record says in terms that no ruling was made** (`Q-COM-1`, the same gate as `Q-QBO-1` firing a second time), and the two resolve differently — the scope exclusion is honored and costs nothing, the architectural commitment is tested and **priced for the first time**; **`Q-WF-4` annotated with a THIRD and most-demanding consumer** (`Q-COM-4`: a log wants everything continuously, and even the metadata-only version needs a scheduled runner to beat a 30-day window) — **`Q-WF-4` itself was NOT amended**; **a FIFTH instance of the `Q-RE-8` absence pattern and the first already load-bearing in shipped code** — `192.5` 0, `503` 0, `work product` 1 across all four registry files, every one of the seven `privilege` hits about assertion mechanics, **while `db/schema.sql` carries two live `not null` `privilege_tier` columns that have been writing rows**; **`transcript-workflows.md`, `future-modules-capture-2026-07-28.md`, `db/schema.sql` and every migration were NOT touched** — four live CHECK constraints and two `default 'work-product'` values are reported as defects and **none was fixed**, because reconciling the §1.3 prose against the built default is a ruling, not a repair; **HK-7 annotated satisfied-for-one-session an ELEVENTH time and again not closed**; **OPEN-4 annotated a THIRTEENTH time**, resume point Task 16, four tasks still on a clipboard; and **one runner-found item entered — `QR-6(a)`–`(f)`**, six proposed runner conventions from a five-auditor adversarial re-check of the batch's own Step 1 report, filed at `docs/specs/queue-runner-step1-audit-2026-08-16.md`. **Nothing built (§5 NONE), nothing verified, no registry file touched, no schema file touched, backlog still 34.**). **Reconciled again to session-log #90 on 2026-08-16** (queue-runner batch, forty-fifth invocation, one packet: **CHAT-DISPATCH Task 16** — the T3 pilot-recording protocol filed as PROPOSED — PREPARATION ONLY, with its `R`/`K`/`I` provenance marks intact and **`I` not promoted**; **seven questions entered with full verbatim text per QR-1 but NO durable IDs** — `Q-T3P-`, `t3-pilot`, `pilot-recording-protocol` and `pilot-manifest` checked repo-wide at HEAD, **all four returned ZERO**, all deliberately not minted, so **ID-DL-1 now governs an ELEVENTH packet**; **unlike #89's, this packet's §7 carries full question text and was merged from directly**; **the headline is `Q-T3P-1`/`Q-T3P-2` — HK-4 describes an act that may already have happened**, since the 2026-07-25 pilot-fixtures entry records the thirteen recordings as already archived **with a provenance README** at the exact target path, making HK-4 probably a P15 → P1 **transfer** rather than a staging act, and a transfer can silently overwrite a README no design session has ever read; **HK-4 annotated add-only and NOT re-worded or closed** — `Q-T3P-2` asks whether it should be re-worded, so editing the row would be the ruling; **`Q-T3P-7` carries an INTERIM PLACEMENT direction from Michael in-session — file at `docs/` for now — and the row stays OPEN**, the ruling still owed; **OPEN-3 annotated with the `get_device_info` candidate datum and expressly NOT closed**; **KICK-1, HK-6 and the telemetry lockdown all annotated as gates this document does not clear**; **HK-7 annotated and again not closed**, carrying the **first recorded instance of a single-folder grant being sufficient** — no Knowledge Repo look arose, against #87's `QBO-LOOK-4`, which was NOT RUN for want of the second folder — **and a RUNNER-FOUND record defect: this is the ELEVENTH annotation actually present on the row, not the twelfth the packet's §7 and #89's own header sentence both assume. #89's claimed eleventh annotation was never written into the row.** It is the **inverse of the #84 defect recorded earlier in this same header** — there the rows landed and the header sentence went missing; here the header sentence landed and the row annotation went missing. **Reported, not back-written:** writing an #89 annotation now would fabricate a record of an act that did not happen; **OPEN-4 annotated a FOURTEENTH time**, resume point Task 17, three tasks still on a clipboard. **Nothing built (§5 NONE), nothing staged, nothing verified, no queue row re-worded, no registry file touched, no schema file touched, `src/` not read, backlog still 34.**). **Reconciled again to session-log #91 on 2026-08-16** (queue-runner batch, forty-sixth invocation, one packet: **CHAT-DISPATCH Task 17** — the session-log table of contents filed as a **FINDING AID — PROPOSED** at `docs/specs/session-log-toc.md`, a new file with no predecessor of any name; **five questions entered with full verbatim text per QR-1 but NO durable IDs** — `TOC-<n>` collision-checked repo-wide at HEAD, **ZERO**, deliberately not minted, so **ID-DL-1 now governs a TWELFTH packet**; **the document corrects nothing and every log entry stands as written** — its two record findings are filed here as questions rather than applied there, `TOC-1` (no entry anywhere carries `#1`, and the file cannot distinguish a lost label from a series that began at `#2`) and `TOC-2` (the `runner 23`/`#65` ordering, the one of five out-of-order dates the log explains nowhere); **nothing was tidied** — no entry renumbered, no `#1` backfilled, `#30`'s divergent heading shape not normalized, the five orderings not resequenced, and the staged file not hand-edited, since it regenerates and a manual fix would be destroyed at the next pass; **`TOC-5` carries a consequence that arrives without a ruling** — at ~53 KB in the synced `docs/specs/`, the index enters project knowledge and spends context budget against the working-set policy, **flagged to Michael in-session and NOT acted on, no sync-picker change made**; **the packet's §9 item 1 was CLOSED rather than carried** — it asked whether the forty-fifth batch had landed and named the check it could not run, and the native `git fetch` answered YES on both limbs, the push (`HEAD == origin/master == a5a95a9`, the forty-fifth's own commit) and the deletion (`inbox/` held only the Task 17 zip); **every figure the index states about its source survived independent re-derivation** — 190 headings against 190 rows, 842,825 bytes, 9,188 raw / 7,996 non-blank — **including the one that looked like a discrepancy and was a corroboration**, a mechanical `(#nn)` sweep returning 88 rather than 89 because of the `#30` heading shape the index itself records. **Nothing built (§5 NONE), nothing verified, no registry file touched, no schema file touched, no log entry altered, backlog still 34.**). **Reconciled again to session-log #94 on 2026-08-16** (queue-runner batch, forty-ninth invocation, one packet — the FABLE-RUN adjudication rulings, and the first batch in a long while that closed rows rather than entering them: **`QR-6(a)` through `(f)` ALL RULED IN and CLOSED**, each on its own reason, with **runner v8 → v9 executed in the same batch** (the v6→v7 mid-batch precedent) — **`(d)` landed in DYNAMIC form, not as proposed**, format coming from the runner text's own Step 4 rules rather than from a named static exemplar that had already gone stale; **`OPEN-5(a)` RULED IN and rode the same amendment** into Step 4 item 3, **while `(b)` and the `AUD-5..9` one-word "apply" were deliberately not put and the row stays open on those limbs**; **`Q-COM-11` RULED (A) — no default, classify at creation, NULL meaning unclassified-must-classify — and CLOSED**, with the **schema half EXECUTED** (`db/schema.sql` amended on both `privilege_tier` columns, migration **authored and NOT run**) and **`Q-COM-10` expressly untouched, so neither CHECK vocabulary was settled by implementation**; **the registry gained its FIRST WORK-PRODUCT PROPOSITIONS anywhere** — TRCP 192.5(d), 192.5(c)(1) and 192.3(h)'s definition, **all UNVERIFIED**, taking the backlog **34 → 37** and `legal-rule-registry-discovery-enforcement-and-pleading.md` **27 → 30**, its count header raised **at Michael's ruling because leaving it would have made that file's header understate its own contents** — the exact collision the file was opened to avoid; **the three entries were placed in SUBJECT ORDER rather than appended** as the packet's §4.2 said, because that file's own Structure paragraph orders entries by subject with rules before the cases that support them, **and the deviation is recorded rather than silent**; **Entry C carries a dedupe note** because a `192.3(h)` entry already existed on a *different proposition*, resolved under that file's own TRCP 196.2(b) precedent at Michael's ruling; **`Q-WF-4` annotated with all THREE consumers at his word "Amend the row," its substance untouched and the identity question still his**; **`Q-COM-12` annotated and NOT closed** — the ruling creates a creation-time classification point but not the transcribe/don't-transcribe gate, and the UI was expressly not built; **three new rows entered — `v20-PASTE`, `MIGRATION` and `REGISTRY-V`**, all his hand; **`transcript-workflows.md` §1 item 3 annotated add-only with its prose unaltered**. **THREE RUNNER-FOUND ITEMS, none back-written.** *(1)* **`QR-6(b)`'s own defect has happened THREE times, not the once its ruling text records** — this header's reconcile high-water was **#91**, so **#92 and #93 are both missing their sentence while their rows landed normally**, the #84 shape twice more; reported, not back-written, per the #90 precedent already in this header. *(2)* **`QR-6(c)` landed HALF** — the pinning half is in at Step 1 item 4, but **no insertion puts "delete by explicit filename" into Step 4 item 5**, where the deletion actually happens; not added, because inventing a ninth insertion is precisely the packet-added act `QR-6(e)` reserves to Michael. *(3)* **the packet's `v19` paste close had NO TARGET** — no `v19` row has ever existed here, the chain being v14 ✅ / **v17 ⬜** / v18 ✅, so the v17 row was annotated **overtaken by events and deliberately NOT closed**. **`QR-6(e)` fired on its own first batch:** the packet reported a stranded 0-byte `.git/index.lock` and routed its deletion nowhere, it would have blocked the close-out commit, and **Michael authorized the removal in-session at the Step 1 STOP.** **This batch was NOT docs-only** — §5 routed `db/schema.sql` — **so the health check ran** per `QR-6(f)`'s other limb. **`EXTRACT` and `OPEN-4`/Task 19 verified present and open, and deliberately left as written.**). **Reconciled again to session-log #95 on 2026-08-16** (queue-runner batch, fiftieth invocation — the wording-adjudication packet: **four rows CLOSED** — the `v17` paste row on Michael's one-word ruling the forty-ninth reserved to him, the `v20` paste row on observation, the route-(c) standing-law row RULED IN as standing law firing trigger #3 into v21, and the `QR-6(c)` half-landing completed as runner **v10**; **three rows ENTERED with full text per QR-1** — `v21-PASTE`, `V-EXEC`, and `READ-A`; **two rows ANNOTATED add-only** — `Q-STAT-6`, whose four material entries were adopted and executed while its fourteen non-material divergences keep it open, and the `V-2` block, where three of the #93 annotation's four items were ruled. **RUNNER-FOUND, fiftieth invocation: three of the packet's four directed closures had no standing row** — Task 19 sequencing and the entries-1/3 wording lived only inside the `V-2` annotation, and Reading A's scope lived nowhere at all, so the first two were ruled where the questions actually live and the third was entered as `READ-A`. The `v19`-shaped no-target problem the forty-ninth found is therefore not a one-off; **the merge rule's own answer applies — the register is the only place a question survives its packet.** This header's reconcile high-water was **#94**, so **#92 and #93 remain missing their sentences** — still reported, not back-written, per the #90 precedent.). **This file is the single register of what awaits Michael's ruling; keep it current or the pointer lies.** **Reconciled again to session-log #96 on 2026-08-17** (queue-runner batch, fifty-first invocation — opened 2026-08-16 and closed after midnight Central, so this sentence carries **08-17** where the packet's routed text said 08-16: the V-5 split executed and `V-EXEC` closed; `Q-STAT-6` annotated add-only; the v21 paste row closed on the design side's observation; **`V5-IDS`, `V5-ATTRIB` and `TASK-19` entered**, the last because it had no row at all). **Reconciled again to session-log #97 on 2026-08-17** (queue-runner batch, fifty-second invocation: the Task 19 sign-off worklist staged as a new file and **nothing verified** — `TASK-19` stays open and no Status line moved; **`Q-T19-1` and `Q-T19-2` entered with full question text** from the packet's §7, and **`Q-T19-3` entered as a runner-found defect on the fe-5 precedent** — two arithmetic errors inside the staged file, left standing because §4.1 ordered it written verbatim and a correction to packet content is yours under `QR-6(e)`. **`V5-IDS` and `Q-STAT-6` were ANNOTATED ADD-ONLY** with cross-pointers to `Q-T19-1` and `Q-T19-2` respectively, on the GLR-1 precedent that a second row riding on an existing question must be visible from that question. **The `Q-T19-` IDs were collision-checked repo-wide at HEAD and returned ZERO**; `Q-T19-1` and `Q-T19-2` come from the packet's own §7 and `Q-T19-3` is runner-coined — all three stay **packet-local, deliberately NOT minted**, minting being yours, so **`ID-DL-1` now governs a FOURTEENTH packet**). **Reconciled again to session-log #98 on 2026-08-17** (queue-runner batch, fifty-third invocation — the Task 19 sign-off walk: **five rows CLOSED** — `Q-T19-2` (ROUTE-C fired five times, all five wordings adopted), `V-4`/**Q1** (re-put at the walk, ruled KEEP SEPARATE again), `OPEN-4` (dispatch home ruled `docs/prompts/`, both files landed), `REGISTRY-V` (WP-1/2/3 verified), and `TASK-19` (walked; remaining scope restated to sixteen entries). **Seven rows ANNOTATED ADD-ONLY:** `Q-STAT-6`, `V5-ATTRIB`, `V5-IDS`, `Q-T19-1`, `V-8`, `V-9`, `V-EXEC`. **`D-2` EXECUTED:** the four stale “34” backlog figures — in `V-5`, `READ-A`, `Q-FE5-7` and `Q-STAT-2` — were re-derived from the two in-backlog registry files at execution and conformed to **40 entries, 16 still UNVERIFIED**; the four quoted copies inside `V-EXEC`'s runner-found annotation were left alone, being a record of a finding rather than a live figure. **Two rows ENTERED with full question text per QR-1:** `WESTLAW-5` and `Q-T19-4`.) **Reconciled again to session-log #99 on 2026-08-17** (queue-runner batch, fifty-fourth invocation — the CHAT-DISPATCH v3 T-20–T-25 docs-only batch: **thirteen rows ENTERED with full question text per QR-1** — `RL-1`..`RL-6`, `WS-1`..`WS-4`, `WB2-1`, `WB2-2` and `DE1-SPEC-1` — **all thirteen collision-checked repo-wide at HEAD, backticked and on word boundaries, returning ZERO, and all thirteen stay packet-local and deliberately NOT minted.** **Ten rows ANNOTATED ADD-ONLY and NOT ONE CLOSED:** `V5-ATTRIB`, `TASK-19`, `WESTLAW-5`, `V-8`, `V-9`, `Q-COM-10`, `Q-COM-12`, `OPEN-1`, `HK-7`, and `ID-DL-1` at a **corrected** ordinal. **NO REGISTRY STATUS LINE MOVED — locating is not verifying**, and 20b, 24 and 17 all stay UNVERIFIED despite being located, identified and designated respectively. **THE PACKET'S OWN ORDINAL INSTRUCTION CAUGHT THE PACKET:** its §6 item 15 said `ID-DL-1` *"now governs a FIFTEENTH packet"* and told the runner to verify that against this file rather than take it from the line — where the `Q-T19-4` row, written by the fifty-third invocation, **already read FIFTEENTH**, and BUILD-STATE independently read *"now FIFTEEN packets"* — **so this batch is the SIXTEENTH.** **Runner-found and NOT acted on: this file's `HK-7` annotation series ends at *"A FOURTEENTH TIME … (#93)"* while BUILD-STATE asserts #98 was the nineteenth — five consecutive annotations recorded only there, on top of the `#89` and `#92` gaps the row already carries against itself; nothing was back-written.** **Also runner-found: `TOC-6` has no row in this file at all** — it was raised in the C-2 Code entry, which was not a runner batch, so no QR-1 merge ever carried it, and it lives in `session-log-toc.md` alone.) **Reconciled again to session-log #100 on 2026-08-18** (queue-runner batch, FIFTY-FIFTH invocation, executing the 2026-08-18 Fable adjudication in which **24 items were put and 24 ruled, zero deferrals**: **EIGHTEEN rows CLOSED** — `RL-1`–`RL-6`, `WS-1`–`WS-4`, `WB2-1`, `WB2-2`, `DE1-SPEC-1`, `V-8`, `V-9`, `OPEN-1`, `Q-COM-12`, `TOC-4` — with `GLR-1` and `AUD-2` closing inside `OPEN-1`'s row where they were annotated rather than given rows of their own. **FIVE rows ANNOTATED ADD-ONLY AND NOT CLOSED:** `Q-COM-10` (direction ruled toward one shared vocabulary by `WS-4`; closes only when Michael adopts the drafted unified list at T-30), `V5-ATTRIB` (`RL-1`'s adopt-for-both carries the propositions whole and resolves no attribution), `WESTLAW-5` (`RL-6` stages the nine reads; the cite supply for 30/31 stays his), `V-7` (its statement of entry 24's blocker is now out of date and is corrected by annotation, not rewritten), and `HK-7` (the ruled forward gap-note for #94–#98, nothing back-written). **ONE registry Status line moved, and it moved the OTHER WAY:** entry 19b returned to UNVERIFIED when `RL-1`'s rewording detached its 2026-08-17 verification. **NO new packet-local IDs were minted this batch** — every ID it touches was already entered by the fifty-fourth invocation — and **the `ID-DL-1` ordinal was CHECKED rather than copied: the highest recorded in this file is SIXTEENTH and BUILD-STATE independently reads "SIXTEEN packets," so the packet's §6 claim of a SEVENTEENTH is correct.** **`TOC-6` was ruled without ever having a row here** — the fifty-fourth invocation flagged its absence and declined to mint one, and the ruling reached it anyway through the index file. **Runner-found, not acted on: `T-26` (the Group A verification looks, entry 13 first) is on the packet's §7 hand list and has no row in this file** — `TASK-19` is closed and no successor row exists; it survives in `docs/prompts/CHAT-DISPATCH-v4.md`, and minting a row is Michael's act, not this batch's.) **Reconciled again to session-log #102 on 2026-08-17** (queue-runner batch, FIFTY-SIXTH invocation — the CHAT-DISPATCH v4 chain, T-27 through T-32, docs-only and **§5 NONE**: **TWENTY-SIX rows ENTERED with full question text per QR-1** — `Q-WS2-1`–`Q-WS2-3`, `Q-WS1-1`, `Q-WS3-1`–`Q-WS3-5`, `Q-RL6-1`–`Q-RL6-6`, `Q-COM-10-A`–`Q-COM-10-F`, `Q-DES-1`–`Q-DES-5` — and **THREE rows ANNOTATED ADD-ONLY, none closed**: `Q-COM-10` (T-30 drafted the list; it closes only at Michael's adoption), `WESTLAW-5` (T-27 spent the nine-opinion read; the five Westlaw pulls are untouched) and `Q-PR3-1` (T-30 is the second task to run into it, and deliberately did not answer it by reading source). **NO row was CLOSED, no registry entry was created, and no Status line moved anywhere in the repo — the backlog stands at 40 entries, 23 VERIFIED / 17 UNVERIFIED, re-derived at HEAD rather than carried.** `Q-WS2-3` and `Q-WS3-1` are ONE question with two entry points and were deliberately given ONE row, per QR-1's no-second-row discipline. The `ID-DL-1` ordinal was DERIVED rather than copied — **this packet stated none** — from the SEVENTEENTH recorded here and BUILD-STATE's independent "SEVENTEEN packets": it now governs an **EIGHTEENTH**. **Runner-found and still not acted on, second consecutive batch: `T-26` has no row in this file** — it rides the packet's §7 hand list and survives in `docs/prompts/CHAT-DISPATCH-v4.md` at HEAD; minting a row is Michael's act, not a runner's.) **Reconciled again to session-log #103 on 2026-08-18** (queue-runner batch, FIFTY-SEVENTH invocation — the adversarial-audit corrections packet, docs-only and **§5 NONE**: **FOUR rows ENTERED** with full question text per QR-1 — `Q-WS3-6`, `Q-DES-6`, `Q-DES-7`, `Q-AUDIT-1` — **NINE rows AMENDED IN PLACE** — `Q-WS2-1` (fifth limb `(e)`), `Q-WS3-1` (NARROWED — the work-product gap does not exist and this project already holds the answer as a VERIFIED entry), `Q-WS3-5`, `Q-RL6-1`, `Q-RL6-3` (remedy corrected from pinpoint to parenthetical), `Q-RL6-5` (count corrected to four, constraint corrected to a TRAP 77.3 **bar**), `Q-COM-10-A` (rewritten to put the prior, never-ruled question), `Q-DES-1` (premise corrected), `Q-DES-4` — **and FOUR annotated ADD-ONLY**: `Q-WS2-2`, `Q-WS1-1`, `Q-COM-10-B`, `Q-DES-5`. **NO duplicate row was minted for any amended item and NOT ONE ROW WAS CLOSED.** **RUNNER-FOUND, AND THE PACKET WAS WRONG IN TWO DIRECTIONS: its §7 header reads "Twenty-eight items" and `#103` reads "26 → 28" while the shipped table holds THIRTY — 26 + 4 new IDs, counted mechanically off the ID column — and its §4.7 amend list named EIGHT rows where its own table marks NINE, the ninth being `Q-DES-4`. The merge followed the table; neither figure was edited, `#103` having been ordered prepended verbatim (`Q-T19-3` precedent).** **The CHAT-DISPATCH v4 block therefore stands at THIRTY rows, re-derived here after the merge.** **No registry entry was created and no Status line moved anywhere in the repo — the backlog stands at 40 entries, 23 VERIFIED / 17 UNVERIFIED, re-derived at HEAD rather than carried.** **Runner-found and still not acted on, THIRD consecutive batch: `T-26` has no row in this file** — minting one is Michael's act, not a runner's.) **Reconciled again to session-log #104 on 2026-08-18** (queue-runner batch, FIFTY-EIGHTH invocation — the authority-read packet, docs-only and **§5 NONE**: **FOUR rows ENTERED** with full question text per QR-1 — `Q-AUTH-1` (*Ex parte Pharr* unread and load-bearing), `Q-AUTH-2` (*Whaley*’s publication notation not found), `Q-AUTH-3` (*Anastassov* as its own entry?), `Q-AUTH-4` (pair folder reads with a search pass?) — and **THREE rows AMENDED IN PLACE**, each because the authority behind it was actually read: `Q-RL6-1` (the entry-31 bridge is ***Hurlburt***, routing through ***Pharr*** rather than *LaPorte*, and the criminal-episode limb drops out for costs), `Q-WS3-5` (*XL Specialty* CONFIRMS the *Fontenot* test rather than undercutting it) and `Q-WS3-6` (*Arden* is the *Fontenot* pattern, not a third-party rule). **No duplicate row was minted for any amended item and NOT ONE ROW WAS CLOSED.** **The CHAT-DISPATCH v4 block therefore goes THIRTY → THIRTY-FOUR, re-derived here after the merge.** **No registry entry was created and no Status line moved anywhere in the repo — the backlog stands at 40 entries, 23 VERIFIED / 17 UNVERIFIED, re-derived at HEAD rather than carried.** **Runner-found and still not acted on, FOURTH consecutive batch: `T-26` has no row in this file** — minting one is Michael’s act, not a runner’s.) **Reconciled again to session-log #105 on 2026-08-18** (queue-runner batch, FIFTY-NINTH invocation — the Fable spend-down adjudication packet, docs-only and **§5 NONE**: **TWO rows CLOSED**, the first closures anywhere in the CHAT-DISPATCH v4 block — `Q-AUDIT-1` (**PF-1: the adversarial preflight is STANDING law**, trigger (a), run by the design session before shipping) and `Q-COM-10-A` (**`privilege_tier` is purpose-scoped, NOT exhaustive; Option 2 — three values plus a `witness_statement` boolean**) — each amended IN PLACE with its original question preserved above the ruling per QR-1, and **no duplicate row minted for either**. **ONE row ENTERED:** the **v23 instructions paste**, on the v20/v21 pattern, which had no row anywhere and would otherwise have survived only in a deleted packet. **`Q-COM-10-B`–`F` annotated add-only as UNBLOCKED; none closed.** **The CHAT-DISPATCH v4 block therefore goes THIRTY-FOUR OPEN → THIRTY-TWO OPEN + TWO CLOSED, re-derived here after the merge.** **Items 3–5 of the spend-down list were NOT REACHED and nothing rides on the silence** — `Q-WS2-1`, `Q-RL6-1`/`Q-AUTH-1` (**the *Pharr* read stays unauthorized**), `Q-WS3-5`/`Q-WS3-6` all stand as written. **No registry entry was created and no Status line moved anywhere in the repo — the backlog stands at 40 entries, 23 VERIFIED / 17 UNVERIFIED, re-derived at HEAD rather than carried.** **Runner-found and still not acted on, FIFTH consecutive batch: `T-26` has no row in this file** — minting one is Michael's act, not a runner's.) *(**Same-day correction, 2026-08-18, from Michael:** the v23 row entered by that batch is **NOT a ready paste — no v23 document has been assembled.** The row is amended in place to say so; the batch's own runner line in the session log is committed and pushed and is therefore annotated there rather than rewritten.)* **Reconciled again to session-log #106 on 2026-08-18** (queue-runner batch, SIXTIETH invocation — the FC-block adjudication: **three rows CLOSED** — `Q-RE-1` ruled "Both, separate triggers," `Q-COM-2` ruled "Yes — adopt the list" as ruled-DIRECTION with **the CHECK migration expressly NOT authorized**, and `Q-RE-9` RESOLVED as a side effect of FC-14 rather than by a ruling on its own text; **two rows ANNOTATED AND DELIBERATELY LEFT OPEN** — `Q-AUTH-1` ("Yes — both limbs": the *Pharr*/*Hurlburt* pulls are Michael's hand, the read is authorized to an Opus session) and `Q-IN2-1` ("Yes — both": blank CR-3 form + versioned instruction manual, his hand) — each stays ⬜ until the documents actually land; **the v23 row AMENDED, not reversed** — the same-day correction above stands as written and a Code session still cannot assemble v23, but the DESIGN side assembled it at #106, so the item returns to ONE act (paste) and the row stays open until Michael confirms it; **and a NEW `FC` series entered** with `FC-12` (corpus import posture) and `FC-13` (corpus propositions → registry entries), both DEFERRED with full question text per QR-1, FC-12 carrying the conservative default that **no corpus content imports** while it is open. **Nothing was verified, no registry entry was created, and no Status line moved** — the backlog re-derived at HEAD is unchanged. **`T-26` is flagged by a SIXTH consecutive batch and still has no row in this file** — minting one is Michael's act, not a runner's.) **Reconciled again to session-log #107 on 2026-08-18** (queue-runner batch, sixty-first invocation: a **new `WS-P` series entered** — `WS-P1`–`WS-P4` from the 2026-08-18 project-knowledge prune, all OPEN, all Michael's, **with full question text per QR-1**. Two are clicks in the claude.ai sync picker that no Code session can make or verify: **`WS-P1` re-applies the RULED `docs/reference/` exclusion, which the live config read 2026-08-18 does NOT contain**, and **`WS-P2` — excluding `docs/authority/pdf/`, ~194 scanned pages and the probable home of the sync's ~1.38M-token weight — has never been ruled at all.** **Nothing was closed, nothing was verified, no registry entry was created and no Status line moved.** **`T-26` is flagged by a SEVENTH consecutive batch and still has no row in this file** — minting one is Michael's act, not a runner's.) **Reconciled again to session-log #108 on 2026-08-18** (queue-runner batch, sixty-second invocation: `Q-WS2-1` (five limbs), `Q-WS2-2`, `Q-WS3-5`, `Q-WS3-6`, `FC-12` and `FC-13` CLOSED on Michael's rulings; the four `WS-P` rows annotated and all four left OPEN — `WS-P2` ruled "Exclude" but pending his click; the v23 row annotated now that #106 assembled the document; **`T-26` MINTED with full task text per QR-1 and CLOSED the same day it was minted**, ending seven consecutive batches that flagged its absence; and one ID-less row entered for the post-execution verification acts the batch created.) **Reconciled again to session-log #109 on 2026-08-18** (queue-runner batch, sixty-third invocation: `Q-WS1-1`, `Q-WS3-1` and `Q-WS3-2` CLOSED on Michael's rulings — 215.1(e) split to its own entry, the WS-3 cross-reference adopted and the comment-9 derivation kept; four registry Status lines moved to VERIFIED (215.4(b), 194.2(b)(9), 194.5, and criminal entry 4b); the WS-3 entry INSERTED, entering UNVERIFIED; and the sixty-second invocation's ID-less post-execution row updated add-only and LEFT OPEN, carrying its successor — the one-look verifications of the five UNVERIFIED ruled-wording entries.) **Reconciled again to session-log #110 on 2026-08-18** (queue-runner batch, sixty-fourth invocation: the sixty-third batch's ID-less successor row CLOSED — all five ruled-wording entries VERIFIED on Michael's word, emptying the adopted-but-unverified tail; five registry Status lines moved to VERIFIED, all in the enforcement file; and the two authorized-but-unrun Opus-lane acts — FC-13's five entry drafts and the `Q-WS3-5` five-lead read — routed to `docs/prompts/OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md` as a copy-paste prompt awaiting his paste. No other row was touched.) **Reconciled again to session-log #111 on 2026-08-19** (queue-runner batch, sixty-fifth invocation: the Grok external review's O series added with FULL question text per QR-1 — O-1 through O-7 as the packet carried them, plus O-8 and O-9 created by the executing batch when ruling 4's Postgres 15+ gate FAILED at 14.5 and the item was stopped rather than worked around. The O-1..O-10 vs. housekeeping `O5` ID collision is FLAGGED, not renumbered, per the packet's §6. **Amended later the same night: `O-8` CLOSED and `O-9` MOOT when Michael upgraded Postgres, ruling 4 then landing under its existing authorization; `O-10` opened on a blocker the upgrade retired before anyone had recorded it.**) **Reconciled again to session-log #112 on 2026-08-19** (queue-runner batch, sixty-sixth invocation: the FC-13 draft series and the `Q-WS3-5` second-pass series added with FULL question text per QR-1 — `FC13-Q-1..6` and `Q-WS3P-1..6`, twelve rows. Neither ID stem had any prior occurrence in this file, so nothing collided and nothing was renumbered. **The batch closes no row.**) **Reconciled again to session-log #113 on 2026-08-19** (queue-runner batch, sixty-seventh invocation: NO row was added and NO row was closed. The `Q-PR3-3` row was ANNOTATED ADD-ONLY — the CD-1 migration ran 2026-08-19 with PR-3 unexecuted, which is the order that row warned about, and backfill step (c) matched ZERO rows, so the backfill limb is spent while the row itself stays OPEN. The `#113` design session created no new queue item: the `ORDERING` header defect it found in `db/migrations/2026-08-18-grok-review-fixes.sql` was found, ruled and executed inside one day, so it is recorded in `#113` and needs no row — stated here so the absence reads as a decision rather than an oversight.) **Reconciled again to session-log #114 on 2026-08-19** (queue-runner batch, sixty-eighth invocation: NO row was added and NO row was closed. One SCOPE paragraph was inserted after the Convention line per the ruling at `#114` — *fix the claim, not the register*: the GO-LIVE GATES (Gates 1–10, `GL-1`'s floor items, and the trigger-1 instructions revision `T#1`) deliberately do NOT live in this register, because they await Michael's ACTION rather than his RULING; minting rows for them would make this an action tracker as well as a ruling queue and would create a THIRD place gate status is asserted. This file is the complete inventory of open RULINGS, not of open items.) **Reconciled again to session-log #115 on 2026-08-19** (queue-runner batch, sixty-ninth invocation: the **G10 series added** — `G10-2` and `G10-4` entered with full question text per QR-1, extracted from the slice doc rather than retyped. NO row was closed. The four go-live gates named in the packet's §7 — gate 10, gate 9, gate 3 and `T#1` — deliberately took NO row, the scope ruling at `#114` applying to them exactly as written; `G10-1` was ruled the same day and `G10-3` was assigned to the build session, so neither is a row either. Open-ROW count re-derived at 342, not the 354 stated last batch — that figure counts every line containing the glyph, 12 of which are prose.) **Reconciled again to the unnumbered gate 9 SMTP runbook entry on 2026-08-19** (queue-runner batch, seventieth invocation: the **D-SMTP series added** — `D-SMTP-1` and `D-SMTP-2` entered with full question text per QR-1, EXTRACTED from the packet's §7 by program rather than retyped. NO row was closed. **Gate 9 itself again took NO row**, the `#114` scope ruling applying to it as written — but the two DECISIONS inside it do take rows, because they await Michael's RULING and not merely his ACTION, which is the first time that boundary has actually been exercised rather than stated.) **Reconciled again to session-log `#116` on 2026-08-19** (queue-runner batch, SEVENTY-FIRST invocation, gate-10 front-end + `anon`/C-2 corrections packet: the O series gained **`O-11`** (residual-privilege remedy) and **`O-12`** (enforce-vs-inherit, deferred by ruling), **`G10-5`** opened the front-end authorization, and **`GL1-1`** entered under housekeeping — all with full question text per QR-1, EXTRACTED from the packet's §7 by program rather than retyped. **The packet's fifth row collided:** it minted `O-10` for the C-2 restatement while an `O-10` already existed here, and unlike the flagged `O-5`/`O5` and `GL1-1`/`GL-1` near-misses the two strings are identical. **Michael ruled it renumbered to `O-13` and flagged**; the collision note sits in the O series and no existing row was renumbered, edited or closed. `O-13` is RULED-and-recorded, so it takes no open glyph. Row-anchored open rows 346 → 350, exactly the four new open items.) **Reconciled again to session-log `#117` on 2026-08-19** (queue-runner batch, SEVENTY-SECOND invocation, CHAT-DISPATCH v5 T1–T7 docs-only packet: **`G10-6`** and **`LE-1`** entered with full question text per QR-1, EXTRACTED from the packet's §7 by program rather than retyped. **The packet's §8.2 said it asked for no new rows while its own §7 carried two items that had no row in this file** — the contradiction was put to Michael at execution and he ruled BOTH entered. `G10-6` is the second false `RlsProbePanel` sentence, which the packet itself calls load-bearing on the front-end kickoff prompt's Step 5 branch; `LE-1` is the bridge-`git status` / `.gitattributes` question, marked “OPEN, new” in that §7. **`Q-7` was deliberately NOT used for the second row — it is already taken and RESOLVED in the log, and a `Q-07` also exists here** — so a fresh `LE` prefix was minted rather than manufacture the very ID collision this file keeps flagging. **The packet's four packet-local question groups — `Q-G3-1`–`Q-G3-5`, `Q-521-1`–`Q-521-5`, `Q-DE-1`–`Q-DE-6`, `Q-PH-1`–`Q-PH-5`, 21 questions — took NO durable IDs and NO rows, on his ruling**: QR-1's rationale is that the packet is deleted, and their full text lives in the seven documents this batch filed, which are permanent. **No row was closed.** Row-anchored open rows 350 → 352, exactly the two new open items.) **Updated 2026-08-19 by a DIRECT RULING, not a queue-runner batch — stated as such so the per-batch chain above is not read as covering it:** Michael ruled **`G10-6` IN** the same day it was entered, so it **CLOSES** as ruled-and-recorded and the `G10-5` row was conformed to name both `RlsProbePanel` sentences rather than one. **Row-anchored open rows 352 → 351** — one row closed, none added, none opened. **Updated again 2026-08-19 by a second DIRECT RULING, also not a queue-runner batch:** Michael ruled **`G10-5` IN** — *“fire the front-end build”* — so it **CLOSES** as ruled-and-recorded. **Row-anchored open rows 351 → 350.** **Reconciled again to session-log #120 on 2026-08-20** (queue-runner batch, SEVENTY-THIRD invocation, three docs-only packets — `#118`, `#119`, `#120`: **TWO rows ADDED, `PF-2` and `SW-1`**, both minted by this runner on Michael’s in-session ruling, their full question text extracted from the packets’ §7 by program per QR-1. **`Q-CAP-1`–`Q-CAP-5` deliberately took NO durable IDs and NO rows, on the same ruling** — QR-1’s rationale is that the packet is deleted, and their full text lives in `docs/specs/knowledge-capacity-measurement-2026-08-20.md` §5, which this batch filed and which is permanent. **No row was closed.** Row-anchored open rows 350 → 352, exactly the two new open items.) **Updated 2026-08-20 by a DIRECT RULING, not a queue-runner batch — stated as such so the per-batch chain above is not read as covering it:** Michael ruled **`Q-CAP-2` IN** (`/docs/reference/` leaves the sync picker). **NO ROW WAS ADDED OR CLOSED and row-anchored open rows are UNCHANGED at 352** — `Q-CAP-2` has no row by his own seventy-third-batch ruling, and **`WS-P1`, which already carried this exclusion as RULED on 2026-08-12, is ANNOTATED AND LEFT OPEN because what it awaits is the CLICK, an ACTION rather than a ruling.** **Reconciled again to session-log #121 on 2026-08-20** (queue-runner batch, SEVENTY-FOURTH invocation, one docs-only packet — `#121`: **TWO rows CLOSED, `D-SMTP-1` and `D-SMTP-2`**, each on Michael’s ruling of 2026-08-20 quoted verbatim from the packet’s §4 and appended to the row, **the full question text preserved in place per QR-1** rather than replaced by the answer. **No row was added.** Row-anchored open rows 352 → 350, exactly the two closures. **`Q-G3-1` and `Q-G3-2` were also ruled and deliberately took NO rows** — Michael’s batch-72 ruling — their ruling record being `#121` plus dated annotations appended to their own bullets in `docs/specs/gate3-write-path-test-protocol.md`.) **Reconciled again to session-log #124 on 2026-08-20** (queue-runner batch, SEVENTY-SEVENTH invocation, one docs-only packet — `#124`: **NO row was added and NO row was closed.** The sole queue act was an **EVIDENCE APPEND to the tail of the existing `LE-1` row**, byte-copied from the packet’s edit-order fence rather than retyped — a second bridge-side design session’s decisive CRLF check, preserved here because its only other home was a chat transcript. **`LE-1` remains ⬜ OPEN and its status is unchanged; the append is EVIDENCE, not a ruling.** Row-anchored open rows **UNCHANGED at 350**. **`Q-CAP-3`–`Q-CAP-5` and `Q-G3-3`–`Q-G3-5` again took NO rows**, on Michael’s seventy-third- and seventy-second-batch rulings respectively — their full text lives permanently in `docs/specs/knowledge-capacity-measurement-2026-08-20.md` §5 and in `docs/specs/gate3-write-path-test-protocol.md` plus `docs/specs/gate3-write-path-test-run-2026-08-20.md` §7/§11, each verified present at HEAD during this batch. Gate 9’s §4 remainder and `Q-G3-4` are gates-side and stay out of this file under the 2026-08-19 scope ruling.) **Reconciled again to session-log #126 on 2026-08-20** (queue-runner batch, SEVENTY-NINTH invocation, one docs-only packet — `#126`, the gates re-check's ruling night: **THREE ROWS CLOSED, NO ROW ADDED.** **`GL1-1`** closed on "Adopt as drafted" — the correcting append landed beneath gate 1 in `docs/specs/Go_Live_Gates.md` this batch, program-extracted from the draft at HEAD. **`LE-1`** closed on "Note yes, .gitattributes no" — the operational note rides v24, no `.gitattributes`. **`WS-P2`** closed AS MOOT under `Q-CAP-4`, its premise measured false at `#120` and re-verified at HEAD this batch (`docs/authority/pdf/` = one tracked `README.md`); **what was mooted is the pending click of its own 2026-08-18 "Exclude" ruling, and `WS-P1` is untouched and stays open.** Every question text preserved per QR-1; answers APPEND. **Row-anchored open rows 350 → 347, exactly the three closures.** **`Q-G3-4`, `Q-G3-5`, `Q-CAP-4` and `Q-CAP-5` again took NO rows** — row-less by Michael's batch-72 and batch-73 rulings; their ruling record is `#126`. **`PF-2` was NOT closed and is REPORTED: the `#118` proposal it carries was ruled "Adopt as standing" the same sitting, but no routing row in this packet reaches it, and an unauthorized closure is exactly what QR-6(e) bars — it stays ⬜ OPEN for Michael.**) **Reconciled again to session-log #132 on 2026-08-21** (queue-runner batch, EIGHTIETH invocation, one docs-only packet — the thin-constitution restructure: **NINE ROWS ADDED, NO ROW CLOSED.** A new **TC series** carries the eight items the 2026-08-21 sitting left open, each with full question text per QR-1 — `TC-OPEN-5` first because it is the only one with an ORDER attached (exclude `docs/record/` in the sync picker AFTER this batch's push and BEFORE syncing, then re-read the meter). **`RECON-1` was entered for the first time**: it had been carried packet-to-packet with no row in this file at all, which is exactly the QR-1 failure class — packets are deleted and the row is the only survivor. **`TC-OPEN-8` is an ID minted by this batch** for the federated-projects question, which the packet carried in an ID-less table row. **`TC-1`–`TC-12` themselves took NO rows** — they are RULED and closed, and their record is `docs/specs/thin-constitution-restructure-2026-08-21.md` §2, verified present at HEAD this batch. **Row-anchored open rows 347 → 356, exactly the nine additions.**) **Reconciled again to session-log #134 on 2026-08-22** (THE BACKFILL — five previously-unfiled design sessions filed at `#127`–`#131`, the numbers batch 80 reserved and Michael ruled executed 2026-08-22, plus the six-part correction entry at `#133`. **One row added: `H12-v`**, the disclosures model-call vendor route, which had no row anywhere in this file. **No rows were minted for the rulings those five entries carry** — ruled 2026-08-22: a filed log entry is a ruling's home and this file registers what is OPEN. **The disclosures `H` series is renamed `HD-1`–`HD-22`, forward-going only** — see the note in the H section. **Nothing was closed by this batch.**) **Reconciled again to session-log #137 on 2026-08-23** (queue-runner batch, EIGHTY-SECOND invocation: the three previously-unfiled 2026-08-22 design sessions filed at `#135`, `#136` and `#137`. **NOT ONE ROW WAS ADDED, CLOSED OR EDITED.** The executing packet's §2 and §6 both reserve every one of its §7 items to Michael, and `QR-1` would require the full question text he has not ruled — so the merge's row half is a deliberate no-op and only this sentence was written. **Open rows 357 → 357 and closed 124 → 124 on this file's own matcher, both unmoved.** *The two available precedents pointed opposite ways and the choice is named rather than smoothed: batch 78 declined this sentence outright on a batch that touched the queue not at all, while this header's own words are “keep it current or the pointer lies.” It was written because silence is the more misleading of the two — three new entries full of open items with no trace in this file cannot be told apart from a merge that was forgotten.* **WHAT IS GENUINELY OPEN AND HAS NO ROW ANYWHERE, named so the gap is on the record rather than in a deleted zip: `DA-3` — the PHI boundary on phone-in-public document access, asked twice in `#137` and answered never** — with `FO-6` (calendar distinguishability and the failure-mode edge), `FO-7` (whether a research memo and a future-module staging satisfy `FO-3`'s QuickBooks condition), `DA-1` and `DA-4` beside it, and `FO-1`/`FO-2`/`FO-3`, `BR-1`–`BR-5` and `RC-1`–`RC-3` living in the log entries alone. **Minting any of them is Michael's act, not a runner's.**) **Reconciled again to session-log #138 on 2026-08-31** (queue-runner batch, EIGHTY-THIRD invocation: the RECON-1 disclosures fold-in. **ONE ROW CLOSED — `SK-v2` — AND NO ROW ADDED.** `SK-v2`'s remaining limb closes on evidence Michael gave on 2026-08-21, was staged design-side at `#129`, and is ROUTED by the packet rather than decided by it. The executing packet's §4.5 reserves the roughly forty open items in the newly filed REQ-CAPTURE's §18 — the `RC` series, the ELEVEN unminted `§18.B` seams, the `HD` residue and `R11` — to Michael, and **he was asked in this session and confirmed the ADD-NONE default, naming no rows**, so the merge's row half is again a deliberate no-op. **What is different this time is that `docs/specs/REQ-CAPTURE_disclosures-expert-designation_2026-08-20.md` IS NOW AT HEAD**, so §18 holds that question text durably for the first time instead of only inside a zip that gets deleted — which is the exact failure `QR-1` exists to prevent. **Counts MEASURED, not asserted, with a matcher that sees all three markers (`^\s*- (⬜|✅|🟡)`): 357 → 356 `⬜`, 124 → 125 `✅`, 5 → 5 `🟡`, 486 marked rows unmoved.** The open figure is **356 `⬜`, or 361 open-in-substance** — and the second is the honest one, because **this file's own Convention line above documents only TWO markers where the file uses THREE**, so the two obvious regexes silently drop `HK-6`, the GPU-telemetry posture item, `V-5`, `V-6` and `V-7`. **The Convention line was NOT edited:** no routing row in this packet reaches it and `QR-6(e)` reserves an unrouted act to Michael — it is named here so the gap is on the record rather than silently carried a third time. ⛔ **And one finding of this batch belongs in this header because it is a queue-integrity fact: `docs/specs/attorney-review-queue-audit-2026-08-24.md` and `docs/specs/id-collision-report.md` ARE NOT AT HEAD.** They are the audit whose 108 disposition candidates this batch's hardened companion re-checks, and the collision report carrying the heartbeat register's true `H1`–`H83` range where this project's record had assumed `H1`–`H23`. Both sit untracked in `docs/specs/`, both carry *“Canonical repo path WHEN FILED”* on their faces, **neither was ever filed, and both are one `git clean` from destruction.** Michael was asked in this session and ruled **DO NOTHING**, so they stay untracked by his choice and not by oversight. **Of the 2026-08-24 audit's 58 closure-class candidates, `attorney-review-queue-audit-HARDENED-2026-08-25.md` — filed at HEAD by this batch — finds 19 verify clean, 39 needing his eye, and ELEVEN that would DESTROY an open question in this file if acted on as written.** It adjudicates nothing and neither does this sentence.) **Reconciled again to session-log #139 on 2026-08-31** (queue-runner batch, EIGHTY-FOURTH invocation: the RC-1 sitting fold-in. **ONE ROW CLOSED — `RECON-1` — AND NO ROW ADDED.** `RECON-1` was DISCHARGED at `#138` and the file it demanded reached HEAD at batch 83, whose routing touched only `SK-v2` — so the row outlived the act it demanded by one batch and is closed here on the record's own evidence, ROUTED by the packet rather than decided by the runner, with Michael put to it at the packet's STOP and ruling the packet ready as presented ("zip it"). **Counts MEASURED, not asserted, with a matcher that sees all three markers (`^\s*- (⬜|✅|🟡)`): 356 → 355 `⬜`, 125 → 126 `✅`, 5 → 5 `🟡`, 486 marked rows unmoved.** The open figure is **355 `⬜`, or 360 open-in-substance**, and the second is again the honest one: **the Convention line above still documents TWO markers where the file uses THREE**, and it was NOT repaired — no routing row in this packet reaches it and `QR-6(e)` reserves an unrouted act to Michael, so the gap is named here a third time rather than carried silently. **NO ROW WAS MINTED FOR ANY 2026-08-31 RULING.** Twenty-one ruling exchanges (some forty-nine discrete rulings) were made that night; every one lives in the `#139` entry and in `REQ-CAPTURE_disclosures-expert-designation_2026-08-20.md` §18, because a filed log entry is a ruling's home (ruled 2026-08-22) — and the sixteen durable IDs that file's §3 proposes were put to Michael with the packet and NOT answered, so minting them stays his act. ⛔ **One statement of this header is overtaken, and says so here rather than being edited out:** `RC-1`–`RC-3`, which the sentence above records as "living in the log entries alone," are **CLOSED** at the 2026-08-31 sitting — the no-minting rule over those IDs stands; their openness does not. ⛔ **And a correction the packet itself needs, recorded because acting on it as written would have put a false sentence in this header:** the executing packet's §4.7 directed this sentence to overtake *"the header's 'EMPTY until he accepts the four proposed items'"* about the `CC-1` hands-on queue — **that wording is not in this header and never was.** `CC-1` occurs ZERO times in this file; the sentence lives in the runner-82 line of `docs/record/session-log.md` and in its derived echo in `docs/specs/session-log-head.md`, neither of which a runner rewrites. The fact itself is recorded here because it is queue-adjacent and nowhere else in this register: **the CC-1 hands-on queue is TWENTY-ONE ACCEPTED** as of 2026-08-31 — `#137`'s four (`DA-1`, `DA-3`, `DA-4`, `FO-6`), the 2026-08-24 audit's twelve, and the sitting's own five (the hard stop's shape, ND-7(c), Q5, Q10, the rider's supervisor beneath a collective paragraph) — **none of them a row in this register, all of them Michael's**, and the sitting they need is a separate one with the running product in front of him.) **Reconciled again to session-log #140 on 2026-09-01** (queue-runner batch, EIGHTY-FIFTH invocation: the FE-D1 amendment-slice packet. **ONE ROW ADDED — `FE-D1A-1` — AND NO ROW CLOSED.** The row carries its FULL question text per `QR-1` — one text, identical in the slice's §15 and the kickoff prompt's header box — because the packet carrying it is deleted at close-out and this register is where the question survives. **Counts MEASURED, not asserted, with a matcher that sees all three markers (`^\s*- (⬜|✅|🟡)`): 355 → 356 `⬜`, 126 → 126 `✅`, 5 → 5 `🟡`, 486 → 487 marked rows.** The open figure is **356 `⬜`, or 361 open-in-substance**, and the second is again the honest one: **the Convention line above still documents TWO markers where the file uses THREE**, and it was NOT repaired a fourth time — no routing row in this packet reaches it and `QR-6(e)` reserves an unrouted act to Michael, so the gap is named here rather than carried silently. **`FE-D1A-1` IS THE ONLY ROW THIS BATCH MINTS, and it is the one act the packet asks of Michael.** EIGHTEEN further rulings were made at the 2026-08-31 late sitting and every one lives in the `#140` entry and at `docs/specs/fe-d1-amendment-slice.md` §2, because a filed log entry is a ruling's home (ruled 2026-08-22); **the six IDs a YES would mint — FE-18, FE-19, FE-20, FE-21, FE-22 and `CD-14` — are NOT minted here**, and the slice's four new questions (`AS-Q14`–`AS-Q17`) plus ten default-shaped items are PROPOSALS for the `CC-1` hands-on sitting, which stays at **TWENTY-ONE ACCEPTED** and is not moved by this batch: a YES to `FE-D1A-1` neither accepts nor declines them. ⛔ **One consequence belongs in this header because a YES sets it running:** the slice's §5 states that once the build lands, the live Medical tab FAILS on the renamed column until BOTH `MIG-1` and the amendment migration have run — the designed path is both in the same sitting as the pull, `MIG-1` first and unchanged. The two untracked 2026-08-24 files are unchanged by this batch and still one `git clean` from destruction, by Michael's own DO NOTHING and not by oversight.) **Reconciled again to session-log #143 on 2026-09-01** (queue-runner batch 86, three packets: **EIGHT rows added and NOT ONE existing row closed, flipped, annotated, merged, renumbered or reworded** — `TFI-1`–`TFI-3` from the REQ-CAPTURE's third fold-in and `HVR-1`–`HVR-5` from the `H12-v` vendor-route research, each carrying its FULL question text per `QR-1`; a ninth §7 row, `TFI-4`, was NOT minted on its own "NO ACTION — recorded only" status and its FULL text is reproduced verbatim in the `runner 86` line instead. **The third packet minted nothing by its own §6 — the queue-cleanup ADJUDICATION SHEET now at `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01.md` is RULING-READY and holds its own questions, and Michael rules from it row by row; no row of THIS file was touched by it.** Markers MEASURED at this HEAD, never carried: ⬜ 356 → 364, ✅ 126, 🟡 5, marked rows 487 → 495. **The Convention line is still NOT repaired** — unrouted, and `QR-6(e)` reserves it to Michael — so the honest open figure is 364 ⬜ or 369 open-in-substance, named for the fifth time rather than carried silently.)

**Reconciled again to session-log #145 on 2026-09-02** (queue-runner batch 87, two packets — the queue-cleanup WAVE-2 SUPPLEMENT and THE CAPACITY PASS).

**This is the first batch under `CAP-3`, and the register is now TWO files:** the one-time split moved **115 ✅ blocks (162,425 B) and 66 superseded reconcile sentences (73,026 B)** to `docs/record/attorney-review-queue-closed.md` — repo-only, bridge-reachable, append-only — leaving this file with ⬜ and 🟡 rows plus **eleven ✅ parents held here because each has an indented open child (`CAP-OPEN-2`)**; bytes and row counts conserved, verified by the script before it wrote. **FIVE rows added**, each carrying its FULL question text per `QR-1` — `CAP-OPEN-1` (the retirement sweep), `CAP-OPEN-2` (the eleven held parents), `CAP-OPEN-3` (the emptied section headings), `CAP-CARRY-1` and `CAP-CARRY-2`. **ONE row was closed and moved:** `TC-OPEN-1`, the two deferred growers, answered in both limbs by `CAP-3` and `CAP-4`. **ONE row was annotated and left open:** `TC-OPEN-2`, whose stated destination (“move to ARCHIVE”) `CAP-1` supersedes — its own question, WHICH files retire, is untouched and is now also carried as `CAP-OPEN-1`. **No other existing row was closed, flipped, annotated, merged, renumbered or reworded.** Counts by the register's own leading-marker method, measured and never carried: this file ⬜ 368, ✅ 11, 🟡 5; the closed register ✅ 116; total ✅ 127. **The Convention line was REPAIRED this batch by `CAP-3a` (WO-10)** — it now documents 🟡 and the split, after five batches of being named and not fixed.

**Reconciled again to session-log #146 on 2026-09-03** (queue-runner batch 88, one packet — the FE-D1A-1 AUTHORIZATION and TRANSIT).

## CLOSED ROWS — under their register headings (level-2 and level-3 headings reproduced; the register's H1 is not)

## 1 — COURT OPINIONS

- ✅ **In re Chefs' Produce of Houston, Inc., 667 S.W.3d 297 (Tex. 2023)** — read 2026-07-26. Both holdings confirmed; record as limited-by-*Ortiz*.
- ✅ **In re Sherwin-Williams Co., 668 S.W.3d 368 (Tex. 2023)** — read 2026-07-26. Entry 10(a)'s three-element test confirmed verbatim.
- ✅ **In re Auburn Creek Ltd. P'ship, 655 S.W.3d 837 (Tex. 2022)** — read 2026-07-26. Nexus element has independent content; state it separately.
- ✅ **Tony Gullo Motors I, L.P. v. Chapa, 212 S.W.3d 299 (Tex. 2006)** — read 2026-07-26. All three propositions confirmed; join table survives.

## 2 — STATUTES AND RULES IN CURRENT TEXT

- ✅ **H.B. 4145 enrolled bill** — read 2026-07-26. Applicability keyed to **date of service**; per-bill exposure flag survives.
- ✅ **Tex. Bus. & Com. Code §17.50(d)** — read 2026-07-26. "Shall be awarded," unqualified; `basis: mandatory` holds. Three date-anchored fee cuts surfaced.

## 2 — STATUTES AND RULES IN CURRENT TEXT
### Items ruled 2026-07-26 (recorded so the queue's status lines stay truthful)

- ✅ **S-1 — probate a real practice line?** **CLOSED: yes** — a mapped line, full build-out, notwithstanding low volume.
- ✅ **PR-1 — independent vs. dependent administration?** **CLOSED: independent administration is in scope**; the deadline machinery matters on this line, and §352.003(b) (county-court jurisdiction over independent executors' alternate-compensation applications) is live.
- ✅ **PR-2 — contested vs. uncontested?** **CLOSED: will contests are a rare branch**; §352.052 builds as secondary, not core.
- ✅ **D3 / H8 — shared touch substrate?** **CLOSED: the case-event core (CE), shape (c)** — shared spine plus per-consumer facets, with an operational/evidentiary boundary. **Unblocks CE1; does NOT authorize it.**
- ✅ **FAM-1 — family furniture in `src`/`db`?** **CLOSED by Code verification 2026-07-26: none exists** — no family practice area or case type, no family-specific roles, no family tables, columns, or enum values. Nothing to remove; the removal was doc-only in fact as well as in authorization.
- ✅ **FLP / CourtListener account + connector.** **CLOSED 2026-07-26** — signed up, connector live. The 30-day window doubled access for members and non-members alike (a usage promotion, not a signup window), so the 8/6 date carried on nine consecutive entries was never a cliff. **The V10 citator pass is now RUNNABLE**, subject to the majority-opinion rule.

## 5 — PROJECT DOCUMENTS AWAITING REVIEW (none opened today)

- ✅ ~~**`case-heartbeat-design.md` §10 — D3: one touch substrate or two?**~~ — **RULED 2026-07-26: the case-event core (CE), shape (c)** — a shared spine plus per-consumer facets, owned design-side, with an operational/evidentiary boundary (only evidentiary facets feed a sworn fee affidavit). Ruled against four consumers, not two. **The former "blocks T1" phrasing is retired with it** — the substrate slice is now **CE1** (N-1 rename; the transcript T-series is unchanged), and **CE1 is unblocked but NOT authorized.**
- ✅ ~~**`REQ-CAPTURE_uvalde-docket-worksheet_2026-08-12.md` — FILED UNRECONCILED**~~ — **RESOLVED 2026-08-12 by Michael's group ruling** on the presented disposition table (the roster-capture precedent). The sixth REQ-1 capture, and the only one that reached the repo *before* reconciliation — that difference is preserved at #59, not erased. Reconciled against BUILD-STATE's fifty-fourth refresh and the log through #61, i.e. **after the CD-1 build**, so its directory dispositions cite built structure rather than planned structure. Full disposition table in the capture's appended addendum. Its open questions now live in the CR rows below; **the capture is no longer an open item.**

## 5 — PROJECT DOCUMENTS AWAITING REVIEW (none opened today)
### Criminal-side requirements (CR series) — ADDED 2026-08-12

- ✅ **`Go_Live_Gates.md`** — **can you paste gates 1–5 verbatim so the repo copy stops being a stub?** Gate 3 also needs its amendment appended, pointing at gate 6. **CLOSED 2026-08-13 (#66) AS ALREADY-DONE — the ask was STALE.** The file has been complete since 2026-07-26: gates 1–5 verbatim, gates 6–8, gate 9, GL-1, and both amendments (the gate 2 multi-user clarification and gate 3's pointer to gate 6). Verified on screen design-side 2026-08-13 and **re-verified in the repo by the runner** the same day. This is **status-drift finding #1** of the cross-document sweep, which stays open. *(One correction to the finding as written: the design side put the file at 106 lines; it is **84**. The completeness claim is right, the line count is not.)*

## 7 — PROJECT AND BUILD OPEN ITEMS
### Closed by ruling, recorded so the status lines stay truthful

- ✅ **AUTH-1 — sign-in method for the single-user auth slice?** **CLOSED 2026-07-28: magic link** ("Let's go with Magic Link"). Built and exercised the same day; see log #28.
- ✅ **BS-1a — BUILD-STATE was at cap with the ledger split delivering one line: designate more content for the ledger, split live state differently, or accept the cap pressure?** **CLOSED 2026-07-28 by eleven item-by-item dispositions** — deletion, compression, and pointers rather than ledger moves. Post-cut length 139 of 150. See log #29.
- ✅ **D-CL2-8** — parallel, not promotion. **Adopted 2026-07-28 as Michael's own ruling**; the "Claude's call" asterisk retired.
- ✅ **D-CL2-2a** — earliest limitations derives from **unresolved clients only**, resolved meaning disbursement (D-CL2-4a). Closed 2026-07-26.

## 7 — PROJECT AND BUILD OPEN ITEMS
### Client model — the design doc's §10 is authoritative for all of these

- ✅ **D-CL2-3** — **does the time tracker's "one rate per case, uniform" rule survive per-client fee arrangements, or become one rate per client?** **CLOSED 2026-08-07: one rate per CLIENT.** Michael supplied the fact pattern that withdrew Claude's keep-it-case-level recommendation — some cases have two clients, one at a discounted rate and one at standard. The rate moves onto the client record beside `fee_arrangement`; single-client cases render exactly as today (D-CL2-7 principle). **Mid-case changes are PROSPECTIVE** via an effective-dated rate history: hours are valued at the rate in force when incurred, nothing silently revalues, and a retroactive courtesy is a visible invoice write-down rather than a recompute. Full ruling and quotes: design doc §10 and `rulings-capture-2026-08-07.md` Part 5. **Authorizes nothing** — the time tracker stays parked behind CE1.
- ✅ **D-CL2-3a — RULED 2026-08-13 (#66): the affidavit carries the rate of the CLIENT WHOSE CLAIM CARRIES THE FEE DEMAND; multi-client demands get PER-CLIENT EXHIBITS.** *(Original question, preserved: fee-affidavit export under per-client rates — the export assumed one uniform case rate, lodestar-shaped. Which rate does the affidavit carry when clients differ — the rate of the client whose claim carries the fee demand, or something else?)* **Authorizes nothing** — the time tracker stays parked behind CE1.

## 7 — PROJECT AND BUILD OPEN ITEMS
### Form engine (FE series) — ADDED 2026-08-07

- ✅ **FE-1 — chronology sources carry provider identities but NOT addresses, phones, or billed charges; the engine cannot ship a servable draft from a chronology alone. Where does provider data come from — §4 interview cards, a persistent provider-directory table, or another source?** **CLOSED 2026-08-07: the persistent provider-directory table**, with the §4 interview card surviving as the fallback for a provider not yet in the directory, writing back into it (enter-once). Three sub-rulings, all confirmed: **(a) identity in the directory, dollars on the case** — billed charges are per-case facts, and storing them per-provider would recreate the wrong-level defect class CL-2 was built to kill; **(b) Option A pointer model** — case-level provider records point at the directory entry and read live, with the served .docx files as the historical record of what went out; **(c) silent trust** — directory fills carry no confirm flags, since the data is attorney-entered and the proofread every filing gets already catches staleness. Michael's own load-bearing requirement, recorded: editing a provider **from within a case** must propagate to every linked case, which only the pointer model delivers. Build-facing consequences for whenever the engine is authorized: the directory entry must be editable in case context, and that edit surface must state firm-wide scope with a linked-case count (labeling, not a confirm click). **Spec ruling only — authorizes no table, migration, or UI.** **SUPERSEDED 2026-08-08 by CD-1** (contact-directory reframe): the ruling stands for the provider question as framed; the live question is CD-1. Surviving mechanics carry into CD-1 as PROPOSED. See `rulings-capture-2026-08-08.md` Parts 1–2. **Evidence channel opened 2026-08-10:** the `drafting-disclosures` skill (`docs/skills/drafting-disclosures/SKILL.md`) emits a mandatory provider-data block every run — designed as the FE-1 scout, to accumulate a real provider corpus so the directory-vs-interview-cards question could be ruled on evidence. Since FE-1 closed and was superseded, **that evidence now feeds CD-1**, not FE-1. Blocks are saved by Michael's hand outside the repo; the skill never writes them here.
- ✅ **FE-2 — one billing entity existed ONLY in the chronology's billing-record document names, with no chronology row of its own; a human caught it and §4 intake would have walked past it. Should intake sweep document-name columns for entities not otherwise listed?** **CLOSED 2026-08-07: ruled yes.** Finds surface as flags for Michael — a catch-net, never an auto-add, since nothing enters a disclosure without his call. Because document names are messy (abbreviations, records vendors, misspellings), dismissal is one click and **dismissals are remembered per case** so the engine doesn't re-ask every draft. **Spec ruling only.** **Build home PARKED 2026-08-08**, unattached: ruled, awaiting a build home; likely lands with the CD-1 build or the intake pipeline, whichever comes first. (Slice A, its loose prior attachment, was withdrawn.) **RE-PARKED 2026-08-11: FE-2 lands with the INTAKE PIPELINE**, superseding "whichever comes first" — reason: the sweep operates on document-name columns and nothing in the CD-1 slice (or the app today) ingests documents, so the parked ruling's premise does not hold here. **The 2026-08-07 ruling itself is untouched** — this moves the home only, it does not reopen the ruling. See `docs/specs/cd1-build-slice.md`.
- ✅ **FE-3 — `form-engine.md` §8 carries real-case-flavored shell content predating the packet rules; needs a design-session read of §8 on screen before ruling what example content ships in the spec.** Michael's. *(Design-side searches on 2026-08-07 could not surface an FE-3 row in this file; verification confirmed it was genuinely absent, not a search miss. Added here with full text per QR-1.)* **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** **CLOSED 2026-08-12 (RULED): the §8 on-screen read is complete (full text; design session with Michael supplying the section from the P1 checkout). The five formatting bullets and the contamination bullet's ruling content ship as written; the bullet's two real prior-case example strings are GENERALIZED to generic descriptions by in-place amend with a dated note (never silent rewrite). The strings are not reproduced in the record. See #55.**
- ✅ **FE-gating — CLOSED 2026-08-12 (#63): FE-13–FE-17 TAKE THE #54 TREATMENT**, ruled at the form engine's slice-scoping session exactly as FE-8–FE-12 were. The question was: do the five new FE items inherit the #54 gating, given that #54 was ruled the day before they existed? Answer: yes. All ten open FE items were dispositioned in that same session; the full table is in `docs/specs/fe-d1-build-slice.md`.
- ✅ **Distillation candidates — CLOSED 2026-08-12 (#63): all four ENTERED `form-engine.md` §13.4** — `template_petition_udja-uim-carrier.md`, `template_deficiency-letter_written-discovery.md`, `template_motion-to-compel_written-discovery.md`, `template_deficiency-grid_columns.md`. **Entering is not adopting:** each is `queued`, none distilled, none format-authoritative, and FE-7's guardrails hold — adoption is a separate explicit ruling of yours, and adoption is what flips a template's FE-12 provenance to format-authoritative.
- ✅ **`FE-D1A-1` — Does Michael authorize the FE-D1 AMENDMENT SLICE at `docs/specs/fe-d1-amendment-slice.md`, scope IN (§3) and OUT (§4) as written, defaults (§10) as named, to be built by a fresh Opus Code session fired from `docs/prompts/PROMPT-fe-d1-amendment-slice-build-session.md`, the queue runner BARRED, fixture-only, migrations written and not run?** (one text, identical in the slice's §15 and the kickoff prompt's header). Full text: *Slice at `docs/specs/fe-d1-amendment-slice.md`, PROPOSED — NOT BUILD AUTHORIZED, authored 2026-08-31 (a second typed sitting that day, Cowork, Fable 5, bridge granted) against HEAD `ed1ddf6`; kickoff prompt filed UNFIRED at `docs/prompts/PROMPT-fe-d1-amendment-slice-build-session.md`. The slice builds the disclosures generator as ruled on 2026-08-31 (`RC-1` and the late sitting's eighteen, quoted at the slice's §2): the case-scoped provider record `R17` on the Medical tab with promote-to-directory by hand; the versioned TEXT chronology (bytes not retained); the extraction call (names, credentials, dates, visit rows, summaries — never type, role or facility membership); app assembly of a bold lead plus four slots over fixed basis and causation sentences keyed by provider type — the twelve types plus mental health (no fixed sentences, the §5.1 hard pause, no generated paragraph), other physician (MD/DO) and other licensed non-physician provider; the three enforcement tiers with the three must-fix stops; oldest-first in every rendering; the rename (`facility_party_id` ×3, `facility_billing_profiles`, "Facility"); `renders-care-at` with an effective period; the typed designee slot; a per-designation paragraph record; additive supplementation (new providers only) over the app's last generation, said on screen; the small retained track; the `ParagraphWriter` interface with a FIXTURE writer in both modes and a Supabase Edge Function stub that refuses without a secret — NO vendor, key, endpoint or model name until `H12-v` and the BAA; ONE migration file hard-gated on `MIG-1`, written and NOT run; fixture-only; EXCLUDED from the GL-1 floor; the queue runner BARRED. Sixty-five named build defaults (slice §10), each PROVISIONAL and reported when taken; four further questions (`AS-Q14`–`AS-Q17`) and ten default-shaped items are PROPOSED for the hands-on queue at the slice's §15 — carried as PROPOSALS to the hands-on sitting; this ruling does not put them, and a YES neither accepts nor declines them. The slice's §5 names a consequence to hear before authorizing: until BOTH migrations run, the live Medical tab fails on the renamed column. A YES MINTS FE-18, FE-19, FE-20, FE-21, FE-22 and CD-14 in the same act (his ruling, `AS-Q13b`); the other ten §3 IDs stay proposed. The kickoff prompt's Step 0 tests only his affirmative words in a session-log entry at HEAD naming the slice document; a queue glyph can only disconfirm.* — **✅ AUTHORIZED 2026-09-02 (log entry `#146`; Michael's words, verbatim: "YES — as written (Recommended)"); `FE-18`, `FE-19`, `FE-20`, `FE-21`, `FE-22` and `CD-14` MINTED by the same act; not yet built. Ruled 23:44 CDT after the slice's §5 consequence was quoted to him verbatim; alternatives named and not taken: YES with a time-boxed dual-read EDIT; NO / hold. The build fires in its own fresh Opus Code session from the kickoff prompt, AFTER the batch carrying `#146` has landed; the queue runner stays BARRED. `TFI-1` and `TFI-3` are NOT answered by the mint.**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Form corpus (FC series) — ADDED 2026-08-18 (#106)

- ✅ **`FC-12` — Import posture: is any of the four-library form corpus (MDB PLLC, Team Brennan, Old Office Files, Goldman; ~33,800 files) ever imported into the form engine as templates at all?** The options as put: **(a)** hand-authored clean templates only, with the corpus kept as reference; **(b)** import with a mandatory scrub gate; **(c)** import structure only — skeletons, section orders, clause lists — and never body text. Evidence weighed at the put: **nothing in any library is safely blank** (mining doc §8), and **six of the fifteen tokenized letters hard-code prior content.** **DEFERRED 2026-08-18 (#106), bare — his word: "Defer." No revisit trigger was named.** **CONSERVATIVE DEFAULT GOVERNS WHILE THIS IS OPEN: NO CORPUS CONTENT IMPORTS.** Deferral is not import authorization. FE-D1 is unaffected — its §11.3 skeleton extraction works from Michael’s own shell per `form-engine.md` §8, not from the mined corpus. — **OPEN, deferred; yours** **RULED 2026-08-18 (#108) — "(c) Structure only," after Michael asked for and received a plain-terms explanation and picked on the re-put. The four-library corpus may contribute **skeletons, section orders and clause lists — NEVER body text**; body prose is always written clean. This replaces the interim no-imports default. **No import tooling is authorized to build by this ruling** — that is a later, separately-authorized act. CLOSED.**
- ✅ **`FC-13` — Do any of the corpus’s legal propositions become registry entries, and which?** The candidates as put: **Prop. Code ch. 55** (hospital / EMS liens); **CPRC ch. 146** timely billing, including the **2025 (c-1) amendment**; **Rule 47 tier bands**; **§ 18.001 / § 41.0105** affidavit mechanics; **TDRPC 1.04(f)** fee-splitting. **DEFERRED 2026-08-18 (#106) — his word, in full: "We should defer all of these for now and see if we can resolve any of them by running through the ‘Knowledge Repo’ folder to see which of them can be confirmed through the exact official texts of rules that are contained in there."** **The directed pass RAN the same session** (record §4): TDRPC 1.08(g) and 1.04(f)(2) match their citations as read; TRCP 239a is a notice rule (the SCRA question stays federal and outside all channels); TRCP 47(c)’s $250k bands and discovery bar confirmed; TRCP 194a confirmed for Family Code suits. **ALL OF THAT IS RETRIEVAL AND ALL OF IT IS UNVERIFIED — no registry entry was created, no Status line moved, and only Michael verifies.** The row holds the open half: **whether any proposition opens as an entry at all, one at a time, your call.** — **OPEN, deferred; yours** **RULED 2026-08-18 (#108), BOTH LIMBS. First limb (multi-select, all four picked): registry entries OPEN for **Prop. Code ch. 55** (hospital/EMS liens), **CPRC ch. 146 including the 2025 (c-1) amendment**, **TRCP 47(c) bands**, and **§ 18.001 / § 41.0105**. Second limb: **"Open 1.04(f) entry"** — TDRPC 1.04(f) opens as the registry's FIRST practice-of-law proposition, drafted from the State Bar clean-authority PDF. All five are **drafting acts for a later Opus session** — SOURCING-compliant, source named per item, under the standing verbatim-drafting direction — and every one enters **UNVERIFIED**; opening is not verification. **Michael chose the plain open over the offered open-plus-boundary-ruling variant, so NO standing ruling on the cases-vs-practice registry boundary was made** — that question (`Q-STAT-5` stack) stays open and undecided. CLOSED; the five drafting acts are pending.**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Contact directory (CD series) — ADDED 2026-08-08

- ✅ **`party_type` RETENTION — CONFIRMED by Michael 2026-08-12 (#62).** The CD-1 build **retained** `party_type` where the slice text said "retiring" it, and disclosed the deviation in its #61 handback. **Confirmed, not merely accepted:** dropping the column meant rebuilding registry field rendering, which is outside the slice's scope; role tags supersede it as the thing the app filters on; the tag vocabulary is **derived** from the party-type registry so the two cannot drift; and `role_tags[0]` mirrors it. `docs/specs/cd1-build-slice.md` carries a dated qualification note — **the original slice text stands; nothing was silently rewritten.**

## 7 — PROJECT AND BUILD OPEN ITEMS
### T3 / Phase 0 advisory (HK series — ADDED 2026-08-09; RE-LETTERED H → HK 2026-08-13, #66)

- ✅ **HK-1 (formerly H1) — RULED 2026-08-13 (#66): FILE IT, as drafted. EXECUTED this batch** into
  `docs/spec-feedback.md`. The filed text: "5 advisories (1 moderate, 4 high) on fresh install, P1,
  2026-08-08; advisories sit in dev-dependency chains of a lockfile green at 232 tests on 07-28;
  triage as a routed task on a machine that can test the result, **not** auto-fixed via
  `npm audit fix`, which can silently desync the machine from the tested lockfile."
- ✅ **HK-2 (formerly H2) — CLOSED 2026-08-13 (#66) via the v17 economics refresh.** Fable 5 became
  standard on Max (banner 2026-08-09), leaving the MODEL USAGE section's economic assumptions
  describing the old terms. v17 refreshes them to the observed 2026-08-13 Max terms (separate Fable
  bar + all-models bar, reset Tue 3:59 PM Central). Closes with **Q-5**, which rode the same clause.
- ✅ **HK-5 (formerly H5) — RULED 2026-08-13 (#66): ADOPTED, WITH A CAVEAT.** The rule: **preflight
  rows and questions about Michael-supplied material are answered by Michael FIRST — never by
  sweeping his machine unprompted, because filenames alone can carry client information.** The
  caveat, which is part of the ruling: **when Michael cannot recall, he MAY DIRECT a search, per
  instance — the search is his call, never a session's default.** Homes: `CLAUDE.md` (landed this
  batch) + instructions v17. *(Origin: a proposed recursive `$env:USERPROFILE` sweep for audio files
  was refused design-side — the listing is itself an exposure.)*

## 7 — PROJECT AND BUILD OPEN ITEMS
### Working set and project knowledge (WS-P series) — ADDED 2026-08-18 (#107)

- ✅ **`WS-P2` — Sync picker: should `docs/authority/pdf/` be excluded too?** The directory holds **16 scanned opinion PDFs, ~194 pages**, and carries no exclusion in the live config read 2026-08-18. It is the **probable home of the sync's ~1.38M-token weight** — the single largest lever on project capacity now identified. **This has never been ruled and would need its own ruling** — it is not covered by the 2026-08-12 `docs/reference/` ruling. Note the trade the design side cannot make for you: excluding it removes the scanned opinions from what design sessions can retrieve. — **OPEN, unruled; yours** **RULED 2026-08-18 (#108): "Exclude."** `docs/authority/pdf/` (16 scanned opinion PDFs, ~194 pages, the probable ~1.38M-token sync weight) is excluded from the GitHub sync — the largest single capacity lever identified; bridge reads at HEAD and the Knowledge Repo remain the verification paths, and the locator index stays synced. **Row STAYS OPEN pending his click** (ruled-pending-execution, the `Q-AUTH-1` shape); verification is from the live config, design-side.** *(**RULED 2026-08-20 (`Q-CAP-4`) — "Close as moot."** Its premise — 16 PDFs / ~1.38M tokens in `docs/authority/pdf/` — was measured false at `#120`: the directory holds one tracked `README.md`, which names itself a *destination* for PDFs that live in project knowledge, not in the repo. The one real PDF cost was `docs/reference/`'s scanned CR3 sheet, already excluded under `Q-CAP-2`. Recorded at `#126`.)* **[Executing batch's note, 79th invocation: what mootness disposes of here is the PENDING CLICK, not an unruled question — this row was already RULED 2026-08-18 (#108) "Exclude" and stayed open on the `Q-AUTH-1` ruled-pending-execution shape. The click is moot because the target directory has nothing to exclude; verified at HEAD this batch by `git ls-files docs/authority/pdf/` → one file, `README.md`, 388 B. `WS-P1` is NOT affected and stays open — its click has a real target.]** — **CLOSED**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Grok external review (O series) — ADDED 2026-08-19 (#111)

- ✅ **`O-13` — C-2 RESTATED — RULED 2026-08-19, recorded so the status lines stay truthful.** Full text: *The 2026-08-18 C-2 ruling ("keep current posture; `ALTER DEFAULT PRIVILEGES` stays unset") rested on a premise falsified 2026-08-19 by Michael's own `pg_default_acl` read: the default IS set — by Supabase's bootstrap, granting TRUNCATE/REFERENCES/TRIGGER/MAINTAIN to `anon`/`authenticated`/`service_role` on every `postgres`-created table in `public`. RESTATED by Michael 2026-08-19 ("Restate on the true warrant"): explicit per-migration grants stand; an ungranted table's unreachability rests on the VENDOR default withholding the four DML privileges, not on the absence of a default, and would change silently if that default changed. The grok record carries the dated §W annotation; nothing in it was rewritten.* — **RULED — recorded**
  *(Minted `O-10` design-side 2026-08-19; RENUMBERED to `O-13` at execution on an ID collision — an `O-10` already existed in this series, the CD-1/PG15 hand-run-migration question above. Michael ruled the renumbering 2026-08-19. **The §W annotation appended to `docs/specs/grok-external-review-2026-08-18.md` and the `#116` session-log entry both name this row `O-10`; that reference resolves HERE.** No existing row was renumbered or edited.)*

## 7 — PROJECT AND BUILD OPEN ITEMS
### Gate 10 PII promotion (G10 series) — ADDED 2026-08-19 (#115)

- ✅ **`G10-5` — Does Michael authorize gate 10's FRONT-END half?** Full text: *Slice at `docs/specs/gate10-pii-frontend-slice.md`, PROPOSED, drafted 2026-08-19. Until it fires, the app keeps writing SSN and licence numbers into `parties.fields` and `select('*')` carries them on every party read — the exclusion limb is delivered in the schema and not in effect in the app. Authorization is Michael's own act, its own Code session, the CD-1 / FE-D1 / gate-10 pattern; the queue runner is BARRED. The SSN visibility model inside it is already RULED (masked + explicit reveal, no reveal log — 2026-08-19); reveal logging rides `O-1`. Includes the `RlsProbePanel` false-sentence repair — **BOTH sentences in the privilege-wall paragraph, not one: `G10-6` was ruled IN 2026-08-19 and the slice's §7 item 6 was amended to cover the second, *“The role has no SQL privilege on these tables”*, which is out of pattern for every `anon`-anchored sweep.*** — **RULED IN — AUTHORIZED AND CLOSED 2026-08-19.** Michael's words, verbatim: *“rule G10-5 in — fire the front-end build.”* **This is the authorization the slice, the kickoff prompt and BUILD-STATE all waited on.** It authorizes the slice's §7 items 1–8 as written — including item 6 as AMENDED by `G10-6` the same day, i.e. BOTH `RlsProbePanel` sentences. **It authorizes nothing beyond that list:** the §6 rejected designs stay rejected, no reveal log is built (`O-1`), no column-level `REVOKE`, no backfill out of `fields`, no migration and no database connection. **Gate 10 is NOT thereby closed — that is a separate ruling.**
- ✅ **`G10-6` — RULED IN 2026-08-19** — **Does the front-end slice's **§7 item 6** grow to cover the second false `RlsProbePanel` sentence, *"The role has no SQL privilege on these tables"*? **Now load-bearing** — it decides the kickoff prompt's Step 5 branch.** Full text: *The sentence renders two lines above the one the slice's §7 item 6 names, inside the same `<p className="notice bad">` block in `src/components/RlsProbePanel.tsx`, and is false in the same way and for the same reason: `anon` holds `TRUNCATE`, `REFERENCES`, `TRIGGER` and `MAINTAIN` on all 37 tables, just none of the four DML privileges. It is OUT OF PATTERN for every `anon`-anchored sweep, which is why task C1 found it by reading rather than by grep (2026-08-19). As the slice's §7 stands, the gate-10 front-end build repairs one false sentence and leaves the other on the screen. The kickoff prompt at `docs/prompts/PROMPT-gate10-frontend-build-session.md` branches on this at its Step 5: ruled in → repair both sentences; otherwise → repair only the sentence §7 item 6 names and report that the paragraph still renders a false one. Naming, because it has been got wrong twice: it is the SLICE's §7 item 6 — `G10-5` is a single yes/no queue row and has no items. Cited by its text rather than by line number (CITE-STABILITY).* — **RULED IN — CLOSED 2026-08-19.** Michael's words: *“rule G10-6 in — repair both sentences in the front-end slice.”* **`docs/specs/gate10-pii-frontend-slice.md` §7 item 6 was AMENDED the same day** and now names both sentences, cited BY TEXT rather than by line number (CITE-STABILITY), with the second carrying a PROPOSED replacement wording that is **not separately adopted and stays Michael's to override**. The kickoff prompt's Step 5 branch is annotated to match. **NOTHING UNDER `src/` WAS TOUCHED: the repair is a build act inside the slice, and the slice still fires only on `G10-5`, which remains OPEN — amending the work list is not authorizing the work.**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Gate 9 production SMTP — the two decisions inside it (D-SMTP series) — ADDED 2026-08-19

- ✅ **`D-SMTP-1`** — Which SMTP provider for magic-link auth email? Compared in docs/smtp-setup.md §1 on price, deliverability posture, and setup friction, all figures read 2026-08-19: **Postmark** (free 100/mo tier that never expires, then $15/mo for 10K; transactional-only shared IPs and the strongest deliverability reputation; friction: new-account human review) vs **Resend** (free 3,000/mo / 100/day; lowest friction — DNS plus an API key; newer provider riding SES infrastructure) vs **Amazon SES** (à la carte $0.10/1,000 — pennies at this volume; highest friction: AWS account, IAM SMTP credentials, sandbox-exit production-access request; no comparable activity log). Volume estimate, stated as an estimate: solo magic-link use ≈ tens of emails/month, so all three cost $0–$15/mo. What would decide it is in the doc's §1 closing paragraph. *(**RULED 2026-08-20 — "Postmark."** Free 100/mo against realistic tens/mo; deliverability-first posture and per-message activity log; the new-account human-review friction accepted with the choice. Recorded at `#121`.)*
- ✅ **`D-SMTP-2`** — Sender identity for auth email: (a) the From address (e.g. signin@brennanstx.com vs no-reply@brennanstx.com — no mailbox needed, replies are not part of the flow); (b) root domain vs a send subdomain (e.g. mail.brennanstx.com). Providers recommend a subdomain to isolate sending reputation; for auth-only volume from the firm's own domain, root-domain sending is also reasonable and none of the three providers' standard setup touches the M365 mail records either way. Judgment call; doc §0 states both sides. *(**RULED 2026-08-20 — "signin@ + send subdomain"** — `signin@brennanstx.com` via a send subdomain (`mail.brennanstx.com`-shaped); provider-recommended reputation isolation; no M365 mail record touched either way (runbook §0/§2). Recorded at `#121`.)*

## 7 — PROJECT AND BUILD OPEN ITEMS
### Thin constitution and knowledge capacity (TC series) — ADDED 2026-08-21 (#132)

- ✅ **`RECON-1` — THE DISCLOSURES REQ-CAPTURE FIRST APPLICATION — FIRST APPLICATION, NOT EDIT.** **Expressly not the 2026-08-21 session's work and still owed by the next disclosures design session.** The `_Voice2` handoff's reconciliation table describes the addendum as needing *editing* to carry the H12 text and the silent-phone posture; **a grep against the copy in project knowledge shows it carries neither.** Exact wording lives in the fold-in capture and in the walk capture at ruling 7. *Entered here 2026-08-21 because it had NO row in this file — it had been carried packet-to-packet, and packets are deleted.* — **OPEN; carried, unchanged** **CLOSED 2026-08-31 — ROUTED on the record's own evidence; put to Michael at the 2026-08-31 packet's STOP as the one item needing his confirmation, and he ruled the packet ready as presented ("zip it").** RECON-1 was DISCHARGED at `#138` (2026-08-25): the REQ-CAPTURE was filed at its canonical path for the first time, REWRITTEN NOT ANNOTATED, and batch 83 landed it at `7a7f797` on 2026-08-31 `cmp`-equal, 153,103 B. Batch 83's routing closed only `SK-v2`, so this row outlived the act it demanded by one batch. Its successor act — `RC-1`, the resume point — was RULED 2026-08-31 in the typed Fable sitting and the file was rewritten a second time by the same packet that closes this row.
- ✅ **`TC-OPEN-1` — THE TWO DEFERRED GROWERS.** `attorney-review-queue.md` grows ~61 KB/day (~0.87% of budget/day) and `BUILD-STATE.md` ~14 KB/day (~0.20%/day), **both by ruled design** — QR-1 puts full question text in this file, and BUILD-STATE is rewritten every batch. **`TC-1` deferred both to their own sitting. What, if anything, changes about either?** Carry with it the finding that **`BS-1a`'s 150-line cap is a LINE cap that density has defeated — BUILD-STATE is at exactly 150 non-blank lines and 130,746 bytes, 871 bytes per line, having grown 6.35× since 08-12 without ever breaching its cap.** Not a violation (the runner says the cap exists "for READABILITY, not token cost"), but the file is ~1.9% of the budget and rising under a cap that structurally cannot stop it. — **CLOSED 2026-09-02 (`#145`): both limbs answered by the capacity pass. The register limb by `CAP-3` — every ✅ row and the superseded reconcile sentences move to `docs/record/attorney-review-queue-closed.md`, this file keeping ⬜ and 🟡 (plus eleven ✅ parents held by an indented open child, `CAP-OPEN-2`); at the split this file went 662,065 → 426,614 bytes. The BUILD-STATE limb by `CAP-4` — a 100,000-byte ceiling beside the 150-line cap, the shortfall named in the banner and the displaced text carried verbatim in the batch's runner line. **The row's embedded finding is answered in its own terms:** the line cap that density defeated now has a byte cap beside it. The figures above are superseded — BUILD-STATE reached **142,777 bytes at exactly 150 non-blank lines (952 B/line)** by 2026-09-02, up from the 130,746 this row recorded. Spec: `docs/specs/capacity-pass-2026-09-02.md` §3.3–§3.4.**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Capacity pass (CAP series) — ADDED 2026-09-02 (`#145`)

- ✅ **`CAP-CARRY-1`** — The wave-2 sweep evidence doc in project knowledge (147,253 units = 7.4 points) is SPENT when batch 87 lands the supplement; **the next design session with the bridge verifies the supplement's content at HEAD and deletes it.** A design session's act, not Michael's; carried here so it is not a drawer. — **✅ DISCHARGED (`#146`, 2026-09-02/03 Central): verified at HEAD `cae00ba` — the doc's content is at `docs/record/queue-cleanup-evidence-2026-09-01/wave2_opus_sweep_evidence.md` byte-identical (435,976 B, sha256 `e8c94cbc…d16a82`) — relocated to Michael's machine inside `brennan-case-manager_transit-sweep-doc-and-0902-pair_2026-09-02.zip` (sha256 `0a8fba85…16ade`, verified in his Downloads folder over the bridge), and deleted from project knowledge on his word. Manifests at `docs/record/fe-d1a-1-authorization-2026-09-02/`.**
- ✅ **`CAP-CARRY-2`** — **v29 of the project instructions — drafted 2026-09-02, delivered as a .md; Michael's paste.** Trigger 3 (four conventions) and trigger 4 (the working set changed). — **✅ REPORTED DONE (`#146`): the 2026-09-02 late design session read the project-instructions field live and found v29 IN FORCE — closed on the design side's attributed read; a Code session cannot see that field and nothing in the repo verifies it (the v28 / `#140` precedent).**

## 7 — PROJECT AND BUILD OPEN ITEMS
### Process, tooling, and housekeeping

- ✅ **O5 — RULED 2026-08-13 (#66): BOTH ATTRIBUTES CONFIRMED ON SUBSTANCE.** `direction` stands as **its own axis**; `conditionalDowngrade` is **a conditions predicate over structured facts — never a boolean.** *(The question had been half-answered: both were absorbed into the ruled fee-profile shape without the substance ever being ruled, and a 2026-08-07 ruling of scope deferred it as un-rulable from memory.)* **Still open and NOT closed by this: the wording sign-off, the V10 citator pass, and V13.**
- ✅ **Q-5 — RESOLVED 2026-08-13 (#66).** The model-usage clarifying clause drafted at `model-routing-plan.md` §7.2 is **ADOPTED into instructions v17**, alongside the 2026-07-27 economics ruling; the 07-27 closure-by-replacement stands. Row closes. *(Adopting it fired instructions trigger #3 — v17 was drafted and delivered the same session. `model-routing-plan.md` itself remains UNRULED as a memo; adopt nothing else from it.)*
- ✅ **`GL1-1` — Does `Go_Live_Gates.md` gate 1 take a correcting append?** Full text: *Gate 1's stated reason — "the free tier has no automatic backups" — is half wrong: Michael's dashboard shows ELEVEN physical backups spanning 12–19 August, a week before the Pro purchase, each with a Restore button; `#114`'s "manual-dump-or-nothing footing" reasoning should not be cited forward as written. One limb is permanently unanswerable: whether restore was available FROM A FREE ACCOUNT on 18 August — he reads the button from a Pro account and the upgrade destroyed the only state that could answer it. Gate 1 stays closed and Pro's other purchases stand; it is the REASON that was wrong. The gates doc is append-only and design-space canonical, so the append is Michael's ruling; full finding at `docs/spec-feedback.md`, the 2026-08-19 gate-1 entry.* — **OPEN — Michael's** *(**RULED 2026-08-20 — "Adopt as drafted."** The candidate correcting append at `docs/specs/gl1-1-gate1-append-draft-2026-08-19.md` was placed beneath gate 1 in `docs/specs/Go_Live_Gates.md` **this batch**, extracted from that file at HEAD by program and never retyped, the `> ` blockquote markers stripped to the indented-italic shape gate 2's clarifications use; the draft file is untouched and remains the record of what was put. Gate 1's own text stands as written. Recorded at `#126`.)* — **CLOSED**
- ✅ **`LE-1`** — **Does the **bridge-`git status`** finding earn a project-instructions operational note, and is a `.gitattributes` wanted?** Full text: *A `git status` taken through the device bridge reported 196 files modified on a tree that is clean on Michael's own machine. Established decisively rather than consistently: for all 196 the committed blob and the worktree file are identical after stripping CR — 196 CR-only, 0 genuine content differences. Cause: `core.autocrlf=true` lives in Michael's Windows GLOBAL git config (the repo's own `.git/config` has it unset), so Windows materialises the LF blobs as CRLF while the Linux bridge VM's git has `autocrlf` unset and compares CRLF against LF. `--ignore-cr-at-eol` and `-w` do NOT suppress it — the cheap-looking check is the misleading one, and only the hash comparison disconfirms. The count is not stable: it is the number of files Windows has materialised as CRLF, and it changes with every `git checkout --`. Taken at face value it reads as a QR-3 dirty-stop and would block the queue runner. So: (a) does this earn a line in the project instructions' operational notes, so a design-side session does not report “dirty — the runner is blocked” from a reading that is an artifact; and (b) is a `.gitattributes` wanted — which would change committed bytes and is squarely a ruling, not a tidy-up. Full finding: `docs/specs/post-sync-verification-2026-08-19.md` §1.1.* — **OPEN — Michael's** **[Evidence appended 2026-08-20, `#124`, no status change: a second bridge-side design session (Opus 5, 2026-08-20) independently rediscovered the false-dirty on the same checkout and supplied the cheapest decisive check yet recorded — run through the bridge, `git -c core.autocrlf=true status` reports the tree CLEAN, 0 modified, pinning the entire delta to CRLF-vs-LF normalization. Direct evidence on both halves of this question: the operational note, and whether a `.gitattributes` is wanted.]** *(**RULED 2026-08-20 — "Note yes, .gitattributes no."** The operational note **IS** wanted and rides the **v24** instructions delivered the same sitting — pasting them is Michael's hand, and no Code session routes instruction text. A **`.gitattributes` is NOT wanted for now**: normalization churn across the tree outweighs a design-side-only nuisance already mitigated by lock-free reads and by the decisive checks preserved on this row. Revisit only if the false-dirty ever bites a Code session. Recorded at `#126`.)* — **CLOSED**
- ✅ **V-EXEC — NEW 2026-08-16 (#95). Michael's hand: fire the next dispatch's Task A — draft the V-5 splits (entries 19/20/21 → six entries), the V-6 rewords (entries 33/34), and the V-7 narrowing (entry 23) per #73's terms.** An **Opus design session with the bridge**; it ships as its own packet. **Drafting is design's** — #73's own line, *"the executions are design's"* — so Code neither drafts nor lands these; the packet does. **Task 19 sign-off resumes ONLY after V-EXEC lands**, which is the direct consequence of the execute-then-verify sequencing ruled this session: those four entries carry wording already ruled to change, and verifying them first would attach Michael's verification to wording that will not survive. **When V-5 executes the backlog goes 34 → 37.** *(The dispatch worklist itself was delivered to Michael separately and is a design-side artifact — not in the packet, not in the repo.)* **CLOSED 2026-08-17 (#96): all three landed.** V-5 executed — entries 19/20/21 split into **19a/19b/20a/20b/21a/21b**, propositions, cites and reliance lines carried verbatim, nothing re-researched. V-6 entries **33 and 34** adopted, with `V6-33-HEAD`, `V6-34-HEAD` and `V6-34-CITE` also adopted. V-7 entry **23** adopted with `V7-23-HEAD`, and `V7-24-XREF` appended to the *Irwin* entry. **`V7-23-CITE` was PUT AND REJECTED** — entry 23's cite stays `§ 37.001 et seq.`, so no open row was entered for it: it is ruled, not awaiting a ruling. **Everything stays UNVERIFIED.** — **`V5-COUNT` ANNOTATION, the row's own words left standing above: "When V-5 executes the backlog goes 34 → 37" is STALE.** That is #73's figure, spent by the forty-ninth invocation's three work-product additions; **the correct figure is 37 → 40.** **RUNNER-FOUND AND NOT ACTED ON** (touching them is a packet-added act under QR-6(e), so it needs Michael's word): a preflight sweep found **at least four further present-tense stale-34 sites in this file** — the **`V-5` row**'s *"BACKLOG 34 → 37 WHEN EXECUTED — it is still 34 in the files today"* (both halves already false at HEAD), **`READ-A`**'s *"the whole 34-entry backlog"*, **`Q-FE5-7`**'s *"the backlog is 34 today"*, and **`Q-STAT-2`**'s *"sits outside the backlog of 34"*. The packet routed only the one site above, so the record still carries two figures. **ANNOTATED 2026-08-17 (#98), ADD-ONLY: THE FOUR RUNNER-FOUND STALE-34 SITES ARE NOW CONFORMED.** Michael ruled them **APPLY** at the walk (`D-2`), so the `V-5`, `READ-A`, `Q-FE5-7` and `Q-STAT-2` sites were **re-derived from the two in-backlog registry files at execution and rewritten** — **40 entries, of which 16 remain UNVERIFIED after the walk.** **The four quoted phrases above are left exactly as they were**: they are this row's record of what the preflight found, and rewriting a finding is not conforming a figure.
- ✅ **TASK-19 — NEW 2026-08-17 (#96), entered as its own row on your ruling at the Step 1 STOP. The CHAT-DISPATCH Task 19 registry sign-off — is the gate open, and where does it resume?** **THE GATE IS NOW OPEN.** Sequencing was ruled **EXECUTE-THEN-VERIFY** at #95, so sign-off waited on V-5, V-6 and V-7 landing; **all three landed this batch** — the V-5 split executed, entries 33 and 34 carry adopted V-6 wording, and entry 23 carries adopted V-7 wording. **Entered as its own row because it had none:** a `grep` for `TASK-19` in this file returned **nothing**, and the gate sentence lived only inside the `V-EXEC` row — which closed this batch and would have carried the sentence closed with it. Same disposition as `READ-A` one batch earlier. **Nothing is verified: verification attaches to the NEW wording and is yours alone.** **CLOSED 2026-08-17 (#98) — THE WALK RAN.** Tiers 1–5 of the worklist §8 order plus the ROUTE-C adoption step, item by item, Michael live throughout: **24 of the 40 entries VERIFIED**, five ROUTE-C wordings adopted, five directed edits, six housekeeping rulings — all recorded per item in `task-19-signoff-record-2026-08-17.md`. **REMAINING SCOPE IS SIXTEEN ENTRIES**, restated here so the row's successor questions stay visible: **five pending post-execution verification** (2, 12, 13, 29, 32 — one look each, CHAT-DISPATCH v3 T-26); **seven records/cite looks** (15, 16, 17, 18, 19a, 20a, 21a — T-20/T-21 stage them); **four blocked** (20b, 24, 30, 31 — Westlaw pulls and your cite supply; see `WESTLAW-5`). **GATE EFFECTS, RECORDED NOT ACTED ON: FE-14's registry gate lifts now that entry 27's verification has executed** (FE-14's build position is unchanged — it is outside FE-D1's slice), and **CR-10 stays HARD-GATED** (29 and 32 pending execution-plus-verification; 30 and 31 cite-less). **Sign-off is permission to build later, not an unblocking act.** **ANNOTATED 2026-08-17 (#99), ADD-ONLY — the row stays CLOSED as the walk left it; this records what arrived behind it.** Workbook v2 (`docs/specs/registry-verification-workbook-v2-2026-08.md`, T-23) is filed, and **the sixteen entries still open now carry a per-entry blocker statement built on the EXECUTED text rather than on the record's expected text.** **It does NOT supersede the v1 workbook**, whose rows remain the numbering authority `V5-IDS` and `Q-T19-1` rest on, and WP numbering stays DEFERRED (`D-6`). **Nothing in it is verified and its counts were re-derived at HEAD.** Whether two live descriptions of one backlog is the `#13`/`R-3` copy-forward shape is its own question at `WB2-1`.
- ✅ **Q-T19-2 — NEW 2026-08-17 (#97). Do the four "same-shape" divergences fire ROUTE-C?** Entries **12, 13, 29 and 32** carry flagged-not-adopted `Q-STAT-6` divergences that the #97 session reads as **changing what a module built on the entry would do** — **entry 12** carrying the identical "may"-for-"shall … unless" defect you already ruled meaning-changing on **entry 11**, on a rule the statute pass did not classify as material; **entry 13** requiring the 191.2 certificate on a *response* where the rule does not (an overstatement against your own side) and omitting "and the effort failed"; **entry 29** saying Penalty Group 1 where the section now reads **1 or 1-B** and omitting "by aggregate weight, including adulterants or dilutants"; **entry 32** applying art. 102.073's highest-category rule to every cost where it reaches only category-dependent ones. **THE QUESTION: on your close read, does any of the four change what its entry MEANS, such that ROUTE-C fires and conforming wording is drafted and queued for each independently?** **Raised as four separate acts, never as a package. Nothing was drafted — drafting before the ruling inverts route (c).** Context: of the eighteen flagged entries, four were ruled material and fixed at #95; of the remaining fourteen, four have had wording changed since for other reasons (1 at #95; 23, 33, 34 at #96), leaving **ten live: 2, 4, 7, 8, 9, 12, 13, 26, 29, 32.** **CLOSED 2026-08-17 (#98) at the sign-off walk — ROUTE-C FIRED FIVE TIMES, EACH RULED INDEPENDENTLY.** All four candidates — **12, 13, 29, 32** — were put to Michael separately and **all four were ruled MATERIAL**; **entry 2 was additionally ruled material at the walk**, when he chose ROUTE-C over verify-as-is on being shown the omission. Conforming wording was then drafted and put to him **per entry, never as a package**, and **all five were ADOPTED VERBATIM** (`task-19-signoff-record-2026-08-17.md` §2), executed into the two registry files this batch. **No cite line changed.** Two heading acts rode with them and were ruled separately: **E-1** (entry 27 → `TRCP 47(b)–(d)`) and **E-4** (entry 29 → `POCS PG1 or 1-B`). **THE FIVE ENTRIES REMAIN UNVERIFIED BY DESIGN** — adoption is not verification; verification attaches to the new wording at a later look (execute-then-verify, #95), which is CHAT-DISPATCH v3 **T-26**.
- ✅ **V-1 — RULED 2026-08-13 (#66): BUMP TO v17 NOW, and it was drafted and delivered the same
  session** (trigger #3, same-day). v17 carries: the Descrybe-out tooling convention; **SAT-1**; the
  **§7.2 model-routing clause** folded into MODEL USAGE with economics refreshed to the observed
  2026-08-13 Max terms (separate Fable bar + all-models bar, reset Tue 3:59 PM Central) — which
  closes both the **Q-5** remnant and **HK-2**; the **HK-5** rule; the "BS-1 provenance" phrase
  corrected to the ledger-pointer line; and the pre-Cowork read-only wording updated (tracked
  content stays read-only design-side, while project knowledge, checkout reads, and inbox saves are
  within design-side reach when granted). **The paste is your hand — the file was delivered to you,
  NOT routed to the repo.** The next fresh design session verifies v17 in force (#54 precedent).
  *(Original question, preserved verbatim per QR-1: **record the Descrybe exclusion as
  session-level tool routing only, or bump the instructions to v17 now — folding in the two
  already-queued touch-ups** — the "BS-1 provenance" phrase; the pre-Cowork "Claude cannot write
  project docs" line? The occasion: Michael ruled 2026-08-13, in his own words, that **Descrybe is
  not used in this project at all until further notice** — revisit at go-live if still useful;
  FLP/CourtListener plus named public sources is the case-retrieval path.)*
- ✅ **V-3 — CONFIRMED BOTH, 2026-08-13 (#66). CLOSED.** PROBATE was created with v1 instructions
  and the carry snippets placed; CRIM DEFENSE carries v1 plus the Uvalde worksheet instructions.
  **Both hand-items close.** *(Original question, preserved: are both now done — the PROBATE
  practice project created (v1 + the carry snippet into each of the two grandfathered matter
  workspaces), and the Uvalde instructions plus v1 confirmed pasted into CRIM DEFENSE? Neither was
  visible to Code, so the record could not close itself.)*
- ✅ **V-4 — RULED 2026-08-13 (#73): KEEP AS TWO, CROSS-REFERENCED.** One proposition per entry, so
  each carries its own status; **cross-references added between the halves of each pair so they
  cannot drift apart.** Governs all three pairs, as the question required. **The reason the ruling
  turned on:** consolidating would merge a VERIFIED entry with an UNVERIFIED one, and verification
  attaches to wording — the merged text would be new wording, so all three verified halves would
  have dropped back to UNVERIFIED. **EXECUTION IS DESIGN-SIDE AND PENDING** (registry files are
  read-only in Code sessions); no registry file was touched. *(Original question, preserved
  verbatim per QR-1, below.)* **RFP response-form consolidation.** The sibling registry file
  carries a VERIFIED **TRCP 196.2** entry for the 30-day response period; the enforcement file
  carries a separate UNVERIFIED **TRCP 196.2(b)** entry for the four permitted response forms.
  **The question: do you want 196.2 and 196.2(b) consolidated into one entry, or kept as two
  propositions on one rule number?** The same question rides on **197.2(a) beside 197.2(c)** and
  **198.2 beside 198.1**. One answer should govern all three pairs so they cannot drift apart.
  Source: `registry-verification-workbook-2026-08-13.md` §5 Q1.
  **CLOSED 2026-08-17 (#98) — RE-PUT AT THE WALK AND RULED THE SAME WAY (`D-4`, `task-19-signoff-record-2026-08-17.md` §4): KEEP SEPARATE.** All three pairs — 196.2 / 196.2(b), 197.2(a) / 197.2(c), 198.2 / 198.1 — stay distinct entries. **Michael's reason, recorded because it is not #73's reason:** different propositions on one rule number are distinct under the one-proposition-one-home rule, **and consolidation would splice unverified text into a file headed ALL-VERIFIED.** **RUNNER-FOUND AND NOT ACTED ON (`QR-6(e)`):** #73's ruling also called for **cross-references between the halves of each pair**, and at HEAD those run **one direction only** — the enforcement file's three entries each carry a dedupe note naming the sibling entry, while `legal-rule-registry-discovery-and-carrier-duties.md` carries **no reciprocal note on any of the three** (checked by grep at execution). Editing a file headed ALL-VERIFIED is unrouted by this packet, so nothing was written there. **Whether the reciprocal half is owed is yours.**
- ✅ **V-8 — RULED 2026-08-13 (#73): RECORD ALL THREE HAZARDS in
  `registry-courtlistener-integration-design.md` §0.1** — the duplicate-cluster hazard, the
  *Irwin* no-cite/no-typing class, and a standing rule that **a citing count is never displayed as
  a precise figure and never read as a currency signal.** They join the existing
  cluster-vs-sub-opinion hazard. **AND the *Irwin* class opens as its own item, V-9** — it is a gap
  in a BINDING rule, and amending a binding rule is Michael's act, not a side effect of a
  design-doc note. **Nothing was edited into CLAUDE.md, and §0.1 is UNEDITED — the design-doc edit
  is design-side and PENDING.** **Q-6 still bars wiring the API into the app**, so nothing is built
  either way. *(Original question, preserved verbatim per QR-1, below.)* **The FLP duplicate-record
  class, beyond one entry.**
  **ANNOTATED 2026-08-17 (#98), ADD-ONLY — THE §0.1 EDIT IS STILL PENDING AND CANDIDATE TEXT IS NOW COMMISSIONED.** CHAT-DISPATCH v3 **T-25** drafts CANDIDATE §0.1 text for the duplicate-cluster hazard (*Alford*: two clusters, one cite; *Irwin*: two clusters, no cite) as **input to your ruling** — the dispatch expressly does not edit the design doc. **A FRAMING MISMATCH IS FLAGGED RATHER THAN RESOLVED:** this row reads **RULED at #73, execution pending**, while the #98 packet's §7 carries the same item as **“Q5 — OPEN”**. Both cannot be right about its status. Nothing was changed on either side; **which framing governs is yours.**
  **ANNOTATED 2026-08-17 (#99), ADD-ONLY — THE CANDIDATE TEXT IS DELIVERED AND THE FRAMING MISMATCH IS STILL UNRESOLVED.** `docs/specs/q5-v8-section-0-1-candidate-text-2026-08-17.md` (T-25) drafts the §0.1 text and **is split deliberately: its §B executes the THREE hazards this row ruled, and its §C proposes FOUR ADDITIONS that are not ruled** and must each be ruled separately, so nothing new rides into §0.1 on a 2026-08-13 ruling that did not contemplate it. **`registry-courtlistener-integration-design.md` WAS NOT EDITED.** **A correction the drafting caught and recorded rather than made silently: CHAT-DISPATCH v3's summary understated this ruling as two hazards plus the existing one — the ruled text is THREE**, and the T-20 memo had followed the dispatch before being corrected against the record. **The mismatch this row already carries stands: whether §B is execution or proposal turns on it, and it is yours.**
  **RULED FRESH 2026-08-18 (#100) AND EXECUTED THIS BATCH — the framing mismatch is RESOLVED BY SUPERSESSION, neither prior description needing to win.** Michael was shown the mismatch in plain terms at his request and ruled the substance today rather than adjudicating the framing. **The three hazards entered `registry-courtlistener-integration-design.md` §0.1 as 0.1.1 / 0.1.2 / 0.1.3**, taken verbatim from the `q5-v8-section-0-1-candidate-text-2026-08-17.md` §B text, with **0.1.2's V-9 passage conformed to today's V-9 ruling** (the one substitution ordered). **§C's four additions were each put INDEPENDENTLY and ALL FOUR ADOPTED**, entering as **0.1.4 – 0.1.7**; 0.1.6 carries Michael's rider in his words — *"possibly we should seek another way of determining the official texas designations"* — commissioned as CHAT-DISPATCH v4 **T-31**. `fable-adjudication-record-2026-08-18.md` §1 (Tier 5). **CLOSED.**
- ✅ **V-9 — NEW 2026-08-13 (#73), opened by the V-8 ruling. What does the majority-opinion rule
  require when the data cannot support positive identification?** CLAUDE.md's majority-opinion rule
  is BINDING and prescribes a method: enumerate `sub_opinions`, then identify the majority
  **positively** by opinion-type marker and authoring language, never by position or assumption.
  ***Allstate Ins. Co. v. Irwin* is a case where that method cannot run at all** — two clusters
  (4885466, 4885465), each a single untyped `010combined` with no author, no per-curiam marker, and
  an empty citations array; a probe of the 26,366-character stored text for "delivered the opinion
  of the Court" returned **zero matches**, so even the authoring-language fallback does not fire.
  **The question: what does the rule require in that state — refuse to characterize the opinion at
  all, fall back to the reporter, or something else — and should that fallback be written into
  CLAUDE.md's rule 5?** Raised because the rule currently has no stated answer, and silence in a
  binding rule gets filled by whoever hits it next. **Michael's act; nothing edited.** Source:
  `registry-citator-pass-2026-08-13.md` §4. *Alford
  Chevrolet-Geo* has two FLP clusters on one reporter cite (2419858, filed 1999-08-26, and 5269700
  "In re Chevroletgeo", filed 1999-06-10, same docket 97-1171); *Irwin* has two clusters with no
  reporter cite at all and no opinion typing, so the majority-opinion rule's method has nothing to
  work from; and citing counts disagree across three FLP surfaces (Alford: 187 search index / 207
  clusters API / 168 `cites:` query, same case, same session). All are retrieval hazards of the
  same family as the majority-opinion rule, and all were found in a sample of ten cases. **The
  question: should `registry-courtlistener-integration-design.md` §0.1 record a duplicate-cluster
  hazard and a "citing counts are not precise" rule alongside the existing cluster-vs-sub-opinion
  hazard?** Design input only; **Q-6 still bars wiring the API into the app**, so nothing is built
  either way. Source: workbook §5 Q5 and `registry-citator-pass-2026-08-13.md` §1, §2.3, §4.
  **ANNOTATED 2026-08-17 (#98), ADD-ONLY — STAYS OPEN, and the walk sharpened its cost.** Entry 23 (ch. 37) was VERIFIED today, so **the UDJA availability holding now waits entirely on *Irwin* (entry 24)** — the one entry in the class where the binding rule cannot run. **Michael raised the adjacent question himself before verifying 23** — whether anything says the UDJA is no longer the vehicle for UM/UIM coverage suits — and the answer from the record is that **nothing adverse exists**; the V-7 narrowing was structural, not substantive. He verified on that frame. **A Westlaw pull of *Irwin* is on your hand list (`WESTLAW-5`) and may moot this row for this case without answering it as a rule** — amending CLAUDE.md's binding rule 5 stays yours.
  **ANNOTATED 2026-08-17 (#99), ADD-ONLY — ONE CASE IS ANSWERED AND THE QUESTION IS NOT.** The *Irwin* majority is now identifiable **from a private Westlaw copy**, whose face reads *"JUSTICE DEVINE delivered the opinion of the Court, in which JUSTICE LEHRMANN, JUSTICE BOYD, JUSTICE BLACKLOCK, and JUSTICE BUSBY joined. CHIEF JUSTICE HECHT filed a dissenting opinion, in which JUSTICE GUZMAN, JUSTICE BLAND, and JUSTICE HUDDLE joined."* — **5–4.** **This does not answer V-9**, which asks what a BINDING rule requires when FLP's data cannot support identification: **a Westlaw pull is a workaround, not a rule**, and the next entry in the class will hit the same wall. **AND A PREREQUISITE NOBODY HAD NAMED IS NOW VISIBLE: entry 24's proposition has never been located inside the Devine opinion**, so the next act on that entry is not verification — that is `WB2-2`.
  **RULED 2026-08-18 (#100) — ADOPTED AS DRAFTED, his words: *"Adopt as drafted, but also keep my ideas to possibly use when we implement it."* EXECUTED THIS BATCH.** CLAUDE.md rule 5 now carries the named-source fallback chain — **the court's own document; a paginated vendor copy stating on its face who delivered the opinion; or Michael's own identification, put to him and recorded against the specific record and text he identified** — with **cannot-identify-STOP as the floor**: no characterization, no inference from position or combined text, and **the entry is flagged, never staged for verification**, until identification is possible. The same amendment is conformed into §0.1.2. **Fires instructions trigger #3.** **His prompt-me mechanism is recorded SEPARATELY as PROPOSED design input — `V-9-UI`, `fable-adjudication-record-2026-08-18.md` §4 item 1 — and is NOT build authorization; Q-6 still bars wiring the API into the app.** `fable-adjudication-record-2026-08-18.md` §1 (Tier 0) and §2. **CLOSED.**
- ✅ **The post-execution verification acts this batch created — NEW 2026-08-18 (#108), NO DURABLE ID (minting is yours).** Two looks now wait on you, both created by acts executed in the sixty-second queue-runner invocation. **(1) The two CONFORMED entries — TRCP 215.4(b) (enforcement file) and CCP art. 102.073, entry 4b (criminal file):** their Rule lines were replaced with the VERBATIM official texts you directed and ADOPTED, they stay **UNVERIFIED**, and verification attaches to the new wording — **a one-look character comparison against the official copies** (execute-then-verify, #95). **(2) The five INSERTED entries — A (TRCP 192.3(h) own-statement right), B (193.4(a)), D (194.2(b)(9)), E (194.5) and F (199.6) — plus the amended WS-3 draft:** all entered or stay **UNVERIFIED**; the WS-3 draft is still uninserted and gated on `Q-WS3-1`/`Q-WS3-2`. **Only you verify; nothing here may be hard-coded or drive a computed legal outcome until you do.** — **OPEN; yours (looks)** **UPDATED 2026-08-18 (#109), ADD-ONLY — the two looks above are now PART-SATISFIED and this row carries their successor.** Of limb (1), **both conformed entries were VERIFIED on Michael's word** at #109 — TRCP 215.4(b) on an exact character comparison against the July 2026 TRCP PDF extraction, and CCP art. 102.073 (entry 4b) on an exact match against the official corpus with the characterized artifacts normalized. Of limb (2), **entries D (194.2(b)(9)) and E (194.5) were likewise VERIFIED**, E asserting nothing on `Q-WS2-3`, which stays open. **Entries A, B and F went to CONFORMATION instead of verification** — their full divergence lists were put, Michael split A on `Q-WS1-1` ("Own entry"), and all four resulting texts were ADOPTED; **the WS-3 draft is now INSERTED** on `Q-WS3-1`/`Q-WS3-2`. **SUCCESSOR ACT, AND IT IS YOURS: a one-look verification of the FIVE UNVERIFIED ruled-wording entries — A (TRCP 192.3(h) own-statement right), B (193.4(a)), F (199.6), the NEW 215.1(e) entry, and the inserted WS-3 entry.** All five are UNVERIFIED, adoption is not verification, and nothing in them may be hard-coded or drive a computed legal outcome until you sign off. Record: `post-execution-signoff-record-2026-08-18.md` §1–§3. — **OPEN; yours (looks), next sitting** **CLOSED 2026-08-18 (#110) — COMPLETED, ALL FIVE VERIFIED, AND THE RULED-WORDING TAIL IS NOW EMPTY.** Entries **A** (TRCP 192.3(h) own-statement right), the new **215.1(e)**, **B** (193.4(a)) and **F** (199.6) were verified on **programmatic character-exact comparisons** against the July 2026 TRCP PDF extraction — **zero flags across all four**, which is the standing verbatim direction ruled at #108 paying for itself. The **WS-3** privilege-limit entry was verified on its layered basis put plainly rather than smoothed: the 192.3(h) clause and the full comment-9 quotation mechanically verified, the *Fontenot* / *ExxonMobil* characterizations resting on the #102/#104 reads as recorded, and the **Pinpoint caveat SURVIVING as verified text** — star-pagination pinpoints must still be checked against the reporter before any quotation enters a filing. **It is the first characterization-bearing entry to reach verification since the V-9 amendment; your judgment, not a text match.** The 215.1(e) preamble-tension flag likewise survives as verified text **asserting no answer**. **No entry with adopted-but-unverified wording remains anywhere in the registry.** **The FC-13 five-entry drafting and the `Q-WS3-5` five-lead read — both AUTHORIZED at #108 and still unrun — now travel as a routed copy-paste prompt at `docs/prompts/OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md`; pasting it into an Opus 5 design chat is YOUR act, and a Code session never executes it.** Record: `five-look-signoff-record-2026-08-18.md` §1. CLOSED.**
- ✅ **`Q-WS2-1`** — **The four-part Rule 194 collision, and it holds two drafted entries.** `WS-2` ruled TRCP 194.1(a), 194.2(9) and 194.5 into existence as three new entries in `legal-rule-registry-discovery-enforcement-and-pleading.md`. But `legal-rule-registry-discovery-and-carrier-duties.md` already carries a **VERIFIED** entry headed **"TRCP 194 — initial disclosures (post-2021), EXPANDED wording"** (Michael, 2026-08-12) whose own wording states the disclosure duty, and that file also carries an **unresolved span flag** — is that entry's scope the whole of Rule 194, or the enumerated span 194.1–194.4? **(a)** Does the span question resolve to the whole rule or to 194.1–194.4? **(b)** On your answer: should **194.1(a)** enter as its own entry here, or instead as a **second observation** on the existing verified TRCP 194 entry (the TRCP 193.7 / one-proposition-one-home precedent)? **(c)** Same question for **194.5**, which is outside the enumerated span but inside the whole rule. **(d)** 194.2(b)(9) appears to be new on either reading — confirm. **Nothing executes until you answer; entries C and E are drafted and HELD.** Source: doc 3 §3. **AMENDED 2026-08-18 (#103) — NOW FIVE PARTS, and the new limb is a defect in the check itself.** §3 said it checked the collision *"against both files at HEAD."* It did not: it checked the **sibling** file for Rule 194 and **missed a VERIFIED `TRCP 192.5(c)(1)` entry sitting in the DESTINATION file, eight headings above its own insertion point.** **(e) Given that VERIFIED entry, does Rule 194.5 earn its own entry at all, or should it be a second observation there** — the same one-proposition-one-home question as (b) and (c), now with a second existing home in play? Source: doc 2 §3, §6. — **OPEN, blocks two entries (C and E, still HELD)** **RULED 2026-08-18 (#108) — ALL FIVE LIMBS. (a) "Whole Rule 194" — the span flag RESOLVES. (b) "Second observation" — drafted entry C is NOT inserted; 194.1(a) enters as a second observation on the VERIFIED TRCP 194 entry (one-proposition-one-home, TRCP 193.7 precedent). (c)+(e) "Own entry + cross-ref" — 194.5 is its own UNVERIFIED entry WITH an express cross-reference to the VERIFIED 192.5(c)(1) entry. (d) "Confirmed — execute" — 194.2(b)(9) is new on either reading. A reciprocal add-only cross-reference was also ruled onto the VERIFIED TRCP 194 entry (#73 keep-as-two pattern). EXECUTED in the sixty-second queue-runner invocation: entries A, B, D, E, F inserted UNVERIFIED; C not inserted. Record: `t26-signoff-and-adjudication-record-2026-08-18.md` §2. CLOSED.**
- ✅ **`Q-WS2-2`** — **Cite conformation, flagged as its own act and never done silently (ROUTE-C).** The ruling records the second entry as **"194.2(9)."** The operative rule has **no such subdivision**: item (9) sits under subdivision **(b) Content**, and the rule cites its own subdivisions through the (b) level (`194.2(b)(3) and (4)` appears twice in the rule text; a bare `194.2(9)` appears **0 times**). The draft is written as **TRCP 194.2(b)(9)**. **Confirm the cite conformation, or direct the ruling's shorthand to be carried as written.** Source: doc 3 §3. **ANNOTATED 2026-08-18 (#103), ADD-ONLY — the count was corrected and the conclusion STRENGTHENED, not weakened:** the string appears **once, in two forms**, not zero times as the arithmetic was stated. The cite-conformation question is unchanged. — **OPEN** **RULED 2026-08-18 (#108) — "Confirm 194.2(b)(9)." The conformation from the ruling's shorthand "194.2(9)" is adopted and **stays flagged on entry D's cite note as its own act** (ROUTE-C — never silent). EXECUTED with entry D. CLOSED.**
- ✅ **`Q-WS1-1`** — **215.1(e) inside the WS-1 entry, or its own entry?** `WS-1`'s entry cites 215.1(e) *within* the entry as its enforcement limb rather than minting a separate 215.1(e) entry, per the conservative default. **Confirm that placement, or direct 215.1(e) to take its own entry** — the file already carries 215.1(c) and 215.1(d) as separate entries, which cuts the other way. Source: doc 3 §6. **ANNOTATED 2026-08-18 (#103), ADD-ONLY:** entry A now also flags a tension the original pass did not name — **the 215.1 preamble's "A party" wording**, which bears on who may move under (e). — **OPEN, entry A drafted on the conservative default** **RULED 2026-08-18 (#109) — "Own entry." TRCP 215.1(e) takes its OWN entry in `legal-rule-registry-discovery-enforcement-and-pleading.md`, matching the file's existing 215.1(c)/(d) pattern, its Rule line the rule's exact two sentences. Entry A keeps the 192.3(h) right, its Rule line reduced to the exact rule sentence, and its prior Dedupe note REPLACED by an enforcement cross-reference. **Both texts ADOPTED ("Adopt both") — adoption is not verification; both are UNVERIFIED and their one-look verification is your successor act.** The 215.1 preamble tension ("A party" vs "the person who made the request") was separately RULED **"Keep flagged"** and moved with the enforcement limb to the new entry, where it asserts nothing. EXECUTED in the sixty-third queue-runner invocation. Record: `post-execution-signoff-record-2026-08-18.md` §2 items 1–2, §3 items 2–3. CLOSED.**
- ✅ **`Q-WS3-1`** — **The work-product limb, which no located authority reaches.** The drafted `WS-3` entry states the **attorney-client** limit only. **Do you want a second entry — or an expansion of this one — addressing the work-product limb**, i.e. whether TRCP 194.5's bar on "any objection or assertion of work product" against a Rule 194 disclosure displaces the pre-2021 work-product witness-statement cases (*In re Jimenez*, 4 S.W.3d 894; *In re Team Transport*, 996 S.W.2d 256)? **No located authority decides it**, so any entry would be a reasoned reading rather than a supported proposition — which is why it is not drafted. Source: doc 4 §6. **Cross-ref `Q-WS2-3`; one question, one row.** **AMENDED 2026-08-18 (#103) — NARROWED, because the premise was false and this row would have spent a ruling on a settled question.** **The work-product gap does not exist.** `TRCP 192.5(c)(1)` says on its face that *"the following is **not** work product protected from discovery: (1) information discoverable under Rule 192.3 concerning experts, trial witnesses, **witness statements**, and contentions"* — and *In re Jimenez* and *In re Team Transport* **applied that provision and ORDERED PRODUCTION**; they rejected a work-product assertion rather than resting on one. **This project already carries the proposition as `## TRCP 192.5(c)(1)`, Status VERIFIED — Michael, 2026-08-17, in the very file the new entry was to be inserted into.** Rule 194.5's work-product bar is separately a **1999** provision, not a 2021 one. **ALL THAT SURVIVES OF THIS ROW: does the `WS-3` entry need an express cross-reference to that VERIFIED entry, or does it stand alone on the attorney-client limit?** Source: doc 3 §6. — **OPEN — narrowed to a cross-reference question** **RULED 2026-08-18 (#109) — "Cross-reference." The WS-3 entry carries an express cross-reference line to the VERIFIED `## TRCP 192.5(c)(1)` entry in the same file — the two-propositions-two-homes pattern ruled at #108 for 194.5 — so the work-product side is visibly settled there and this entry states the attorney-client limit only. No second entry was minted. EXECUTED in the sixty-third queue-runner invocation: the WS-3 entry is INSERTED, entering **UNVERIFIED**, with the stale "located but did not read" clause conformed as separately ruled. Record: `post-execution-signoff-record-2026-08-18.md` §2 items 5, 7–8, §3 item 6. CLOSED.**
- ✅ **`Q-WS3-2`** — **Adopt the comment-9 derivation, or let the 192.3(a) phrasing stand?** The ruled shorthand is *"192.3(a)'s non-privileged limit governs (h)."* Both located cases reach the result **through comment 9**, and **no located case uses the 192.3(a) formulation**. **Adopt the entry's comment-9 derivation as drafted, or direct the 192.3(a) phrasing to stand?** (ROUTE-C: the wording is put to you; adoption is not verification.) Source: doc 4 §6. — **OPEN** **RULED 2026-08-18 (#109) — "Comment-9." The entry's comment-9 derivation stands as drafted, including its Derivation note stating the shorthand honestly — that both opinions reach the result through comment 9 and no located case uses the 192.3(a) formulation. The 192.3(a) phrasing does NOT stand as the entry's rationale. EXECUTED with the insertion; the entry is UNVERIFIED and adoption is not verification. Record: `post-execution-signoff-record-2026-08-18.md` §2 item 6. CLOSED.**
- ✅ **`Q-WS3-5`** — **Five confirmed leads not read.** *City of Dickinson*, *CSX*, *Baytown Nissan*, *Pope* and *Jimenez* were **not read**, the drafted entry not resting on them. **Do you want a second pass reading them** — chiefly to test whether the Texas Supreme Court has touched the comment-9 limit — **or does the intermediate-court authority suffice for an entry that only ever enters UNVERIFIED?** Source: doc 4 §6. **AMENDED 2026-08-18 (#103) — A SIXTH LEAD, AND IT IS THE STRONGEST ITEM IN THIS WHOLE BLOCK.** ***In re XL Specialty Ins. Co.*, 373 S.W.3d 46 (Tex. 2012)** — **the Texas Supreme Court on TRE 503(a)(2) "representative of the client," which is the very element *Fontenot*'s holding rests on** — was never located in the original pass. **Do you want it read before the `WS-3` entry is ever verified?** **LOCATOR-ONLY: not read, not V-9-identified, designation unconfirmed** — named so you can choose, staged for nothing. Source: doc 3 §6. — **OPEN — now the strongest item here** **AMENDED 2026-08-18 (#104) — READ, AND IT DOES NOT UNDERCUT *FONTENOT*; IT RUNS THE OTHER DIRECTION AND CONFIRMS THE TEST.** ***In re XL Specialty Ins. Co.*, 373 S.W.3d 46 (Tex. 2012)** was pulled and V-9-identified from the face of the document (**Chief Justice Jefferson, “delivered the opinion of the Court,” joined by seven — Hecht, Wainwright, Medina, Green, Johnson, Guzman, Lehrmann**). The party claiming privilege was the **INSURER**, and the **INSURED** was held not to be a representative of the insurer’s client-side: *“Cintas could not have been a ‘representative of the client,’ as it did not have the authority to obtain legal services for its insurer, XL.”* ***Fontenot* is the reverse posture** — the carrier as representative of the insured client, under a policy conferring the duty to obtain his defense. **Same TRE 503(a)(2) test, opposite direction, and a real boundary added: in a bad-faith action the parties are adverse and the privilege does not bridge them.** Failure of proof again: *“XL has neither pleaded nor proved that this is the case.”* **So the question is no longer whether to read it — it is: do you want candidate `C-XL-1` adopted into the `WS-3` entry, or does that entry stand as drafted?** Source: `costs-and-privilege-authority-read-2026-08-18.md` §3.1, §5. — **OPEN — now decidable** **RULED 2026-08-18 (#108), BOTH LIMBS. (i) "Adopt C-XL-1" — *In re XL Specialty* enters the WS-3 DRAFT as an express boundary; ROUTE-C, **adoption is not verification**, the draft stays UNVERIFIED and uninserted. EXECUTED in the sixty-second invocation (draft amended; not inserted into any registry file — insertion stays gated on `Q-WS3-1`/`Q-WS3-2`). (ii) "Run the second pass" — a read of the five unread confirmed leads (*City of Dickinson*, *CSX*, *Baytown Nissan*, *Pope*, *Jimenez*) is **AUTHORIZED, Opus lane, retrieval only, NOT YET RUN** — it verifies nothing and V-9 discipline binds. CLOSED as a question; the authorized read is a pending act.**
- ✅ **`Q-WS3-6`** — **NEW 2026-08-18 (#103). An adjuster case that cuts the opposite way from what the original pass asserted, and it is why a whole paragraph was withdrawn.** The original `ws3-…` §3.1 stated that a witness statement taken from **a third-party witness or an adjuster** is *"not protected by the attorney-client privilege at all."* **That sentence is WITHDRAWN** — wrong three ways: *W&G Trucking*'s declarant was the **defendant driver, a party**, and the case turned on a **failure of proof** (*"the record does not establish that Jamison was a 'client'"*) rather than on any categorical rule; *In re ExxonMobil* protected documents whose witness-statement content **was a recounting of a third party's words**; and ***In re Arden*** (El Paso 2004) **appears to hold an insured's recorded statement to his own carrier's ADJUSTER privileged, applying *Fontenot*.** **Do you want *In re Arden* pulled and V-9-identified?** It bears directly on what the memo called the quietest exposure this firm has — the adjuster call. **LOCATOR-ONLY: not read, not V-9-identified, designation unconfirmed; named so you can choose, staged for nothing.** Source: doc 3 §3.1, §6. — **OPEN, your hand** **AMENDED 2026-08-18 (#104) — READ, AND IT IS THE *FONTENOT* PATTERN RATHER THAN A THIRD-PARTY RULE, so the withdrawn sentence was wrong for a SECOND reason nobody had named.** ***In re Arden*** was pulled and V-9-identified (**Richard Barajas, Chief Justice; Panel No. 2 — Barajas, C.J., McClure, Chew, JJ.**; “MEMORANDUM OPINION ON PETITION FOR WRIT OF MANDAMUS,” **civil**, post-2003; no reporter cite, 2004 WL 576064). The facts run the other way from what the locator snippet suggested: **Arden was the DEFENDANT** — he rear-ended Bryan — **Farmers insured Arden**, and the adjuster took **Arden’s own** recorded statement *“while acting as his representative, for the purpose of obtaining and facilitating his legal representation because he believed that the carrier would owe him a legal defense.”* **It therefore does NOT stand for “adjuster recordings are privileged.”** Two things survive and are worth keeping: **the proof was made** (*“The adjuster’s affidavit was the only evidence presented”*), which is the difference from *W&G Trucking*; and **Arden expressly conceded that witness statements are not work product**, a party conceding exactly what TRCP 192.5(c)(1) says and **independently corroborating `#103`’s correction to the `WS-3` memo’s §4**. Disposition: *“we conditionally grant the Relator’s petition for writ of mandamus.”* **So the question is now: do you want candidate `C-ARD-1` adopted, or is *Arden* recorded as read-and-not-used?** Source: `costs-and-privilege-authority-read-2026-08-18.md` §3.3, §5. — **OPEN — now decidable** **RULED 2026-08-18 (#108) — "Adopt C-ARD-1." *In re Arden* enters the WS-3 DRAFT as the record-establishes-the-role condition, the WL cite carried **with its designation stated** (mem. op., no reporter cite — V-9). Same ROUTE-C terms: the draft stays UNVERIFIED and uninserted. EXECUTED in the sixty-second invocation. CLOSED.**
- ✅ **`Q-COM-10-A`** — **The exclusivity problem, and it blocks the migration.** `privilege_tier` holds ONE value, but *Fontenot*/*ExxonMobil* hold that a witness statement inside a confidential attorney-client communication **is** privileged — so a record can be **both**, and a fourth enum value forces a false choice with opposite consequences. **Do you want Option 1 (four values, one column, accept the flattening) or Option 2 (three values plus a `witness_statement boolean`, which can express both)?** `WS-4`'s direction is satisfied either way. Source: doc 6 §6. **AMENDED 2026-08-18 (#103) — A PRIOR QUESTION HAS SURFACED THAT HAS NEVER BEEN RULED, AND EVERY OPTION ABOVE PRESUPPOSES AN ANSWER TO IT.** The overlap is **not special to the fourth value — it is a GENERAL cardinality property of the column**: `'attorney-client'` and `'work-product'` **already** overlap today, since one document can be both, and the argument that defeats a four-value enum defeats the three-value one already in the schema. **So the prior question is: should `privilege_tier` be EXHAUSTIVE at all — one value naming the whole privilege posture of a record — or is it a partial label with the rest expressed elsewhere? That has never been ruled.** Answer it first; **then** Option 1 (four values, one column, accept the flattening) or Option 2 (three values plus a `witness_statement boolean`). Source: doc 5 §0, §2, §6. — **OPEN, blocks the migration** **RULED 2026-08-18 (#105), BOTH LIMBS.** **(a) `privilege_tier` is NOT exhaustive** — it is a purpose-scoped classification under a stated precedence rule; orthogonal attributes live in their own fields, matching the schema's existing separate-boolean pattern (`discoverable_flag`, `phi_flag`). Pick verbatim: 'Purpose-scoped + precedence.' **(b) Option 2 — three values plus a `witness_statement` boolean**, which expresses 'work-product AND owed as an initial disclosure' without precedence gymnastics; `WS-4`'s direction — the fourth state in both vocabularies — is satisfied on this shape. Pick verbatim: 'Option 2: 3 values + boolean.' **CONSEQUENCE: the authored, UNRUN migration was drafted to the FOUR-VALUE shape and must be REDRAFTED to Option 2 before anything executes; it remains NOT AUTHORIZED and the run is Michael's hand.** *(Runner-found 2026-08-18, fifty-ninth invocation, and recorded here because the names collide: that migration is PROPOSAL TEXT at `privilege-tier-unified-vocabulary-proposal-2026-08-17.md` §4, **not** the file `db/migrations/2026-08-16-privilege-tier-no-default.sql` on your hand list — the latter's only DDL is four `drop default` / `drop not null` statements and it touches no CHECK and no vocabulary, so this ruling does not reach it. The proposal document already prescribed the remedy in its own words: 'if Michael adopts Option 2 the file is re-drafted, not edited.')* **`Q-COM-10-B` through `-F` are UNBLOCKED** (Opus lane). — **CLOSED**
- ✅ **`Q-AUDIT-1`** — **NEW 2026-08-18 (#103), AND IT IS THE ONE ITEM HERE THAT OUTLIVES ITS OWN BATCH — a question about a CONVENTION rather than about any document.** The adversarial audit that produced `#103` confirmed **twenty-two defects — three HIGH, eight MEDIUM, eleven LOW** — across five research documents. **`RR-1` RAN ON THOSE DOCUMENTS AND CAUGHT NONE OF THEM.** That is not an RR-1 failure: RR-1 re-reads documents authored earlier in a session **against rulings made later in the same session**, and it caught two genuine cross-document interactions doing exactly that. **It is not designed to check a document against its PRIMARY SOURCES, and NO house convention in this project is.** The multi-agent read-only adversarial preflight is the only mechanism that has ever caught this class, **and it has now paid three times out of three — `#96` (a citation flag reworded inside an act ruled verbatim), `#97` (two arithmetic errors inside a staged document's own prose), and this audit.** It still runs **only when you ask**. **DO YOU WANT IT MADE A STANDING CONVENTION for any packet carrying legal characterizations — and if so, at what TRIGGER?** Candidate triggers, none recommended: every packet whose §4 routes a document making statements about statutes, rules or opinions; every packet touching the registry; every packet above some size; or your call per packet as now. **Note the cost is real and is yours to weigh — it is N parallel agent runs per packet, and the failure mode of NOT having it is a defect that reaches tracked content and reads as current.** Source: `#103` FAILURE CLASS field; this batch's runner line. — **OPEN — the standing-convention question** **RULED 2026-08-18 (#105) — THE PREFLIGHT IS STANDING LAW (PF-1).** Standing convention. **Trigger (a): any packet carrying a legal characterization or a proposed registry entry.** **Run by the DESIGN SESSION before shipping, pre-`inbox/`** — the sending-side sibling of RR-1: RR-1 re-reads against same-session rulings; PF-1 checks against PRIMARY SOURCES, which no other house convention does. Picks verbatim: '(a) Legal-content packets' / 'Design session, before shipping.' Reason recorded with the ruling: all three paid runs (#96, #97, the 2026-08-18 authority-read preflight) were on exactly that content class, and pre-ship keeps the defect out of the record entirely. Discretionary preflights on other content remain available at Michael's direction, per instance. Full record: `fable-adjudication-record-2026-08-18.md`, spend-down section. **Instructions trigger #3 fired → v23.** — **CLOSED**
- ✅ **`RL-1`** — **ROUTE-C candidate on entry 19a, and the coupling that makes it not a simple adopt.** Entry 19a reads *"It is an abuse of discretion to deny discovery going to the heart of a party's claim."* The *Castillo* lead opinion reads *"A trial court abuses its discretion when it denies discovery going to the heart of a party's **case** or when that denial severely compromises a party's ability to present a viable **defense**."* Two differences: claim/case, and **an entire second disjunctive limb the entry omits** — which narrows the entry against its own authority. Proposed conforming wording: *"It is an abuse of discretion to deny discovery going to the heart of a party's case, or where the denial severely compromises a party's ability to present a viable defense."* **THE COUPLING IS THE QUESTION: 19a and 19b carry this text VERBATIM by design (the V-5 split preserved it), and 19b is VERIFIED as of 2026-08-17.** Adopting for 19a alone makes the pair say different things and ends the split's verbatim premise; adopting for both **rewords a VERIFIED entry and detaches its verification**, the limit route (c) does not reach; rejecting leaves a narrower entry than its authority, knowingly. **All three live; none recommended.** **RULED 2026-08-18 (#100) — ADOPT FOR BOTH 19a AND 19b, with the consequence chosen eyes-open; EXECUTED THIS BATCH.** Both entries take the conforming wording and stay verbatim-identical by design; **no cite line changed** (ROUTE-C); **19b's 2026-08-17 verification DETACHED** — verification attaches to wording (#95) — and re-verification is Michael's at a later look. The file header was re-derived at execution, not carried: **20 VERIFIED / 13 UNVERIFIED of 33**. Ruling and adopted wording: `fable-adjudication-record-2026-08-18.md` §1 (Tier 1) and §2. **CLOSED.**
- ✅ **`RL-2`** — **Does the *Alford* supersession inference come out of the record?** Entry 20a and `registry-citator-pass-2026-08-13.md` §2.3 carry: *"The June 10 record is most likely the original opinion superseded on rehearing, but that characterization is inference and is not asserted — no opinion text was read."* **Text has now been read.** The opinion's own caption reads *"Decided June 10, 1999. Rehearing Overruled August 26, 1999."* — **rehearing was OVERRULED; the June 10 opinion stands; nothing was superseded.** Should the sentence be retired from the entry and replaced with the caption facts, or should the entry keep the records-hazard flag and simply drop the inference? **Note the E-3 precedent cuts both ways: the citator pass is a dated record of what a pass found on a date, and conforming a finding falsifies it — so the entry may be the right place to correct and the pass record the wrong one.** **RULED 2026-08-18 (#100) — REPLACE WITH THE CAPTION FACTS, ENTRY ONLY; EXECUTED THIS BATCH.** Entry 20a's inference sentence is retired for the caption *"Decided June 10, 1999. Rehearing Overruled August 26, 1999."* — rehearing OVERRULED, nothing superseded, two records of ONE decision, operative record for citation 2419858. The duplicate-record hazard flag stays. **`registry-citator-pass-2026-08-13.md` was NOT touched** — the E-3 both-ways note was ruled the way it cuts: the dated pass record stands as written. `fable-adjudication-record-2026-08-18.md` §1 (Tier 1). **CLOSED.**
- ✅ **`RL-3`** — **Which subsequent-history parenthetical, on each of entries 15, 21a and 20b — three separate one-word calls, each put independently per ROUTE-C.** Candidates are staged in full at `registry-records-look-staging-2026-08-17.md` §1.2 (Volt Power: leave as written / add "Apr. 5, 2023" / add date + cause no. 12-23-00047-CV / `mem. op.` NOT AVAILABLE), §1.3 (Redman: same four, cause no. 12-23-00212-CV, `mem. op.` NOT AVAILABLE), and §2.3 (Collins: `No. 02-14-00294-CV, 2017 WL 218286 (Tex. App.—Fort Worth Jan. 19, 2017, pet. denied) (mem. op.)`, every element confirmed from the copy and **each element separable**). **No candidate is recommended.** **RULED 2026-08-18 (#100) — THREE SEPARATE CITE ACTS, each put and ruled independently per ROUTE-C; ALL THREE EXECUTED THIS BATCH.** 20b took the full parenthetical (cause no. + Jan. 19, 2017 + pet. denied + (mem. op.)); 15 and 21a each took date + cause number, **no pinpoint and no designation notation — not available from any source read**. Heading and Cite line moved together on all three. **All three entries remain UNVERIFIED: a cite act is not a verification**, and every reporter-cite flag survives. `fable-adjudication-record-2026-08-18.md` §1 (Tier 2), §3 E-A1/E-A2/E-A3. **CLOSED.**
- ✅ **`RL-4`** — **Should TRCP 193.4(a) and 199.6 become registry entries?** *Collins v. Kappa Sigma* states the evidence-burden rule and cites **TRCP 193.4(a) and 199.6** for it: *"as a general rule, a party objecting to discovery must present some evidence necessary to support its objections. See Tex. R. Civ. P. 193.4(a), 199.6."* The registry currently carries the evidence-burden argument on **case** authority only (entries 20a/20b, both UNVERIFIED). **Does the DE-1 taxonomy's evidence-burden category want the rule-side authority as its own entry, or is the case authority sufficient?** Raised because the DE-1 spec has to decide what it cites and **should not create a registry entry by implication.** **RULED 2026-08-18 (#100) — YES, BOTH: TRCP 193.4(a) AND 199.6 become registry entries**, rule-side authority for the evidence-burden category, entering UNVERIFIED. **The proposition texts were NOT drafted this batch** — entry drafting is design's (the V-EXEC precedent) and is commissioned at CHAT-DISPATCH v4 **T-29** from the clean-authority TRCP PDF; they arrive by a later packet. Backlog effect on that execution: +2. `fable-adjudication-record-2026-08-18.md` §1 (Tier 3). **CLOSED as a ruling.**
- ✅ **`RL-5`** — **Should entries 17 and 18 be cross-referenced?** *In re Sting Soccer* (17) cites *In re Ochoa* (18) at \*2 for precisely the proposition entry 18 carries — *"see also In re Ochoa, No. 12-04-00163-CV, 2004 WL 1192444, at \*2 (Tex. App.—Tyler May 28, 2004, orig. proceeding) (citing TEX. R. CIV. P. 192.5(c)(1))"* — a 2017 Dallas court relying on the 2004 Tyler opinion, and **the closest thing to a treatment signal either entry has.** Neither entry records the relationship. **This is the same shape as the unexecuted second half of #73's `V-4` ruling — "keep as two, CROSS-REFERENCED" — where at HEAD the cross-references run one direction only. One ruling could sensibly govern both.** **RULED 2026-08-18 (#100) — CROSS-REFERENCE BOTH SETS; EXECUTED THIS BATCH.** (i) Entries 17 ⇄ 18 took reciprocal notes recording the *Sting Soccer* citation of *Ochoa* at \*2. (ii) **#73's `V-4` second half finally EXECUTED** — three reciprocal dedupe notes added to `legal-rule-registry-discovery-and-carrier-duties.md` (197.2(a), 196.2, 198.2). **Notes only: no proposition and no Status line in that verified file was touched, and its 22 VERIFIED status lines were counted before and after and are unchanged.** `fable-adjudication-record-2026-08-18.md` §1 (Tier 3). **CLOSED.**
- ✅ **`RL-6`** — **The nine unnamed opinions in `Documents\Knowledge Repo\Opinions\` — locator pass, full read, or nothing?** Beyond the WESTLAW-5 and *De Anda*, the folder holds: `Bonilla v. State_452 S.W.3d 811`, `Middleton v. State_634 S.W.3d 46`, `Williams v. State_253 S.W.3d 673`, `State v. Schmitt_2012 Tex. Crim. App. Unpub. LEXIS 887`, `Ex parte Bailey_2011 Tex. Crim. App. Unpub. LEXIS 388`, `Ex parte Carter_521 S.W.3d 344`, `Ex parte Green_457 S.W.3d 90`, `Ex parte Simmons_2014 Tex. Crim. App. Unpub. LEXIS 501`, `Ex parte Simmons_2015 Tex. Crim. App. Unpub. LEXIS 776`. **None was opened.** Entries **30 and 31** are cite-less and their cites are expressly Michael's act; entry 31 specifically needs a case bridging *LaPorte*'s **Penal Code chapter 3** definition of "a single criminal action" to **art. 102.073**'s use of the same phrase in the court-costs context, and the reported criminal opinions above are the obvious place such a bridge would be. **Reading them would be a session deciding which authority supports a cite-less entry — the act reserved to Michael.** **RULED 2026-08-18 (#100) — FULL READ of all nine**, routed to an Opus session (CHAT-DISPATCH v4 **T-27**), staging identity / holding / what-it-could-support as **CANDIDATES** per opinion with the majority-opinion rule as amended by V-9 run on each, and specifically testing the entry-31 *LaPorte* → art. 102.073 bridge. **The cite supply for entries 30 and 31 remains Michael's act on the staged findings — the read stages, never selects.** **Nothing was read this batch** (§6 barred it). `fable-adjudication-record-2026-08-18.md` §1 (Tier 1). **CLOSED as a ruling.**
- ✅ **`WS-1`** — **Should 192.3(h)'s third proposition become a third entry?** The rule's final sentence — *"Any person may obtain, upon written request, his or her own statement concerning the lawsuit, which is in the possession, custody or control of any party"* — is carried by neither entry 2 nor entry 3, **and TRCP 215.1(e) gives it teeth**: failure to comply supports a motion to compel plus reasonable expenses including attorney fees. **Note the ruling that already governs the shape:** #95 ruled the two existing 192.3(h) entries **STAY SEPARATE**, on the reasoning that a different proposition on the same rule number is a new entry. **That reasoning points toward a third entry, but it was ruled about two, and extending a ruling is not the same act as applying it.** **RULED 2026-08-18 (#100) — YES, create the third entry** (192.3(h)'s own-statement right), UNVERIFIED, **with TRCP 215.1(e) cited WITHIN the entry as its enforcement teeth** — Michael expressed no preference for 215.1(e) as a separate entry, so the conservative default governs and is flagged for the drafting pass. Text drafted design-side at **T-29**, not this batch. Backlog effect on execution: +1. `fable-adjudication-record-2026-08-18.md` §1 (Tier 3). **CLOSED.**
- ✅ **`WS-2`** — **Does the registry want the Rule 194 disclosure machinery — TRCP 194.1(a), 194.2(9) and 194.5 — and if so as one entry or three?** The registry carries 192.3(h) and 192.5(c)(1) — **the CHARACTER of the artifact — and nothing about the DUTY that attaches to it.** Under the current rule a witness statement is a **Rule 194.2(9) initial disclosure owed without awaiting a request** (194.1(a)), and **Rule 194.5 permits no objection or assertion of work product to a Rule 194 disclosure at all.** *(Family Code suits differ: Rule 194a.2(h) makes it available on request.)* **The duty is where the practical exposure sits**, and the DE-1 template and any transcript-pipeline design would both otherwise cite rules that are not in the registry. **RULED 2026-08-18 (#100) — YES, AS THREE ENTRIES**: TRCP 194.1(a), 194.2(9) and 194.5, each its own proposition and Status line, all UNVERIFIED. Texts drafted design-side at **T-29** from the clean-authority PDF, not this batch. Backlog effect on execution: +3. `fable-adjudication-record-2026-08-18.md` §1 (Tier 3). **CLOSED.**
- ✅ **`WS-3`** — **Does TRCP 192.3(a)'s non-privileged limit govern 192.3(h)?** 192.5(c)(1) removes **work-product** protection from witness statements and says nothing about **attorney-client privilege**. 192.3(a) opens the scope rule with *"any matter that is not privileged and is relevant,"* and (h) is a subsection of 192.3 — so the candidate reading is that **a recording of a genuinely privileged attorney-client communication is not made discoverable merely by being a recording of a person with knowledge of relevant facts.** *(192.5(c)(5), carving out work product created within a TRE 503(d) exception, shows the drafters had the two privileges in view separately.)* **NO TEXAS AUTHORITY DECIDING THIS WAS LOCATED**, and **the entire client-recording and intake-recording posture rests on it.** Is this a Michael-reads-it item, a Westlaw pull, or a research pass? **RULED 2026-08-18 (#100) — READ THE LEADS AND DRAFT THE ENTRY** (CHAT-DISPATCH v4 **T-28**). **Michael answered by his own hand in-session**, running the question through Lexis Protégé and supplying its answer — **treated as a LOCATOR, never authority: a vendor AI asserting law is a model asserting law**, and the primary opinions must be read before any proposition is drafted. Leads captured in the record (*In re ExxonMobil*, 97 S.W.3d 353, the load-bearing exhibit; Fontenot, Scherer, Kona Coast, Young, Jimenez, Franklin Ctr., City of Dickinson, CSX, Baytown Nissan, Pope, TRE 503, Dorsaneo §90.02, two AG letter rulings). The drafted entry enters **UNVERIFIED**; verification is Michael's after it exists. `fable-adjudication-record-2026-08-18.md` §1 (Tier 4). **CLOSED.**
- ✅ **`WS-4`** — **Should either `privilege_tier` vocabulary be able to express "witness statement — owed as an initial disclosure"?** `generated_documents` admits `('attorney-client','work-product','non-privileged')`; `transcripts` admits `('privileged','work-product','non-privileged')`. **Neither can express a fourth state with different consequences from all three: not privileged, not protected as work product, and owed as an initial disclosure without a request.** Classifying it `'non-privileged'` records the conclusion but **loses the duty — and the duty is the part that gets a firm sanctioned.** Rides `Q-COM-10`, which has four homes (two CHECKs, two TypeScript unions). **No schema act proposed.** **RULED 2026-08-18 (#100) — YES: the fourth state ("witness statement — owed as an initial disclosure") enters BOTH vocabularies**, and `Q-COM-10`'s direction is thereby ruled toward **ONE SHARED vocabulary**. Execution shape follows the privilege-tier precedent (#94): an **authored-UNRUN** migration plus the two TypeScript unions, **Michael's to run**, verification checks in words. **The exact unified value list is drafted and put to him BEFORE execution (T-30) — `Q-COM-10` does NOT close until he adopts it.** No existing row is re-characterized; re-characterization is a legal act and his. **No CHECK, union or migration was touched this batch (§5 NONE).** `fable-adjudication-record-2026-08-18.md` §1 (Tier 3). **CLOSED.**
- ✅ **`WB2-1`** — **Does workbook v2 replace the v1 workbook's rows for the sixteen open entries, or sit beside them?** `registry-verification-workbook-2026-08-13.md` is banner'd stale in seven identified ways and its rows are deliberately untouched **because they are the numbering authority `V5-IDS` and `Q-T19-1` rest on.** So v2 now describes the same sixteen entries in a second place. **Two live descriptions of one backlog is the `#13`/`R-3` copy-forward shape** — the failure where an item propagated across a dozen entries on both sides before anyone checked. **Does v2 become the working layer with v1 frozen as the numbering record, or does v2 stay a session artifact and the open backlog live in `attorney-review-queue.md` alone?** **RULED 2026-08-18 (#100) — v2 BECOMES THE WORKING LAYER; v1 IS FROZEN as the numbering record only; EXECUTED THIS BATCH.** One layering line added to each file's status block / banner. **No v1 row was touched**, so the numbering authority `V5-IDS` and `Q-T19-1` rest on survives intact. `fable-adjudication-record-2026-08-18.md` §1 (Tier 1). **CLOSED.**
- ✅ **`WB2-2`** — **Entry 24's next act is not verification — should the queue row say so?** The queue and BUILD-STATE both carry entry 24 as blocked on identifying the majority. **That blocker is gone** (Devine, J., 5–4, from the paginated copy) **and a different one is now visible: entry 24's proposition has never been located inside the Devine opinion.** Does the row get re-worded, or stay as written with workbook v2 as the annotation? **Raised because a row naming a cleared blocker reads as more blocked than it is, and a row that silently changes what it means is worse.** **RULED 2026-08-18 (#100) — ANNOTATE ADD-ONLY; EXECUTED THIS BATCH.** The row keeps its text and gains a dated annotation: **the majority IS identified (Devine, J., 5–4, from the paginated copy) and the operative blocker is now proposition-location inside the Devine opinion.** The annotation was placed on the `V-7` row, which is the open row still describing the cleared blocker. `fable-adjudication-record-2026-08-18.md` §1 (Tier 6). **CLOSED.**
- ✅ **`DE1-SPEC-1`** — **Should the DE-1 template's authority slots be DATA — each carrying a registry entry number and its verification status, with the renderer refusing to emit an UNVERIFIED authority into a filing draft without a visible flag — or should the template hold authority as text, with verification enforced by practice rather than by the tool?** The first is assumed throughout the spec draft and **has a cost: it couples the drafting tool to the registry's Status lines, so a template's output changes when a Status line changes.** That is the point and it is also a commitment. **The second is simpler and puts the whole burden on the drafter at the moment of drafting — which is the moment the record shows things get missed.** **RULED 2026-08-18 (#100) — DATA.** Authority slots carry the registry entry number **and** its verification status; **the renderer refuses to emit an UNVERIFIED authority into a filing draft without a visible flag**; the coupling to the registry's Status lines is accepted as a commitment, not a side effect. **DE-1 remains NOT authorized to build** — this rules the spec's shape only, and the DE-1 spec draft was NOT conformed to this ruling (it is a dated record; §6). `fable-adjudication-record-2026-08-18.md` §1 (Tier 5). **CLOSED.**
- ✅ **`TOC-4` — When and by whom does the index regenerate?** It is stale the moment the next entry is appended, and it is derived, mechanical, and cheap — a Sonnet-eligible job per the dispatch's own labelling of Task 17. Candidates: on demand only; as a step in the QUEUE-RUNNER close-out alongside the BUILD-STATE rewrite; or periodically. **Nothing is proposed as ruled here** — adding a step to the runner is a change to a binding convention and would fire instructions-trigger #3. **What cadence does Michael want, if any?** **OPEN — for Michael (2026-08-16).** **RULED 2026-08-18 (#100) — EVERY RUNNER BATCH.** Regeneration of `docs/specs/session-log-toc.md` became a QUEUE-RUNNER **Step 4 item 1** obligation — **runner v10 → v11**, firing instructions trigger #3 — so the index rides the same commit as the entries it indexes. **The new obligation was executed this batch**, its first firing. `fable-adjudication-record-2026-08-18.md` §1 (Tier 6). **CLOSED.**
- ✅ **`Q-COM-2` — Two live CHECK constraints say a communication can only be recorder, phone, or manual. Which values should they carry?** `transcripts.source` and `staging_items.source` both admit exactly `('recorder','phone','manual')`, and all eleven `transcripts.context_type` values are audio or meeting contexts. A written-channel communication has nowhere to be today. **Does a communications log EXTEND `transcripts` — widening `source` and `context_type`, making the existing table the communications log — or does it get its own table, accepting that the system then has two per-case communication records with two privilege vocabularies and two source enums?** *(Nothing was widened; naming the values is a design act and widening a CHECK is a schema act.)* **RULED 2026-08-18 (#106), his word: "Yes — adopt the list."** Direction for the CHECK widening, verbatim from the option set put to him: **`e-service`, `email`, `first-class mail`, `certified-mail-RRR` (its tracking number as its own field), `fax`, `portal`** alongside the existing `recorder` / `phone` / `manual`. **THE MIGRATION IS A SEPARATE LATER ACT AND IS NOT AUTHORIZED** — naming the values is the design act ruled here; widening the two live CHECKs on `transcripts.source` and `staging_items.source` belongs to a future authorized slice and nothing was widened this batch. The row closes as ruled-direction, not as executed schema. Full record: `fc-adjudication-record-2026-08-18.md`. — **CLOSED**
- ✅ **`Q-COM-11` — Both columns default to `'work-product'`, which by TRCP 192.5(d) is an assertion of privilege, and for witness interviews and depositions it is contrary to the rule. Does the default change?** **RULED (A) — NO DEFAULT; CLASSIFY AT CREATION. Michael, 2026-08-16 (#94). CLOSED.** Both `privilege_tier` columns lose `DEFAULT` and `NOT NULL`; **NULL means unclassified-must-classify.** The reason he ruled on: no row ever asserts a stance nobody chose — and the fix is cheap while every row is fiction, becoming a re-characterization (a legal act) after go-live. **The CHECK vocabularies were NOT touched: `Q-COM-10` stays unruled and is expressly not settled by implementation.** *(Runner note, forty-ninth invocation: the schema half EXECUTED this batch — `db/schema.sql` amended on both columns and `db/migrations/2026-08-16-privilege-tier-no-default.sql` **authored and deliberately NOT run**, Michael's hand per the CD-1 item-7 pattern; see the MIGRATION row in §7. The creation-time classification UI is the recorded follow-on act and was NOT built.)* `P-COM-3` and `P-COM-4` put a recorded, verbatim-transcribed witness interview squarely inside the 192.5(c)(1) exception; `transcript-workflows.md` §1.3 already says such transcripts are *"presumptively discoverable at creation"*; the built column defaults them to `'work-product'` anyway. At fixture volume this is a defect. At ingest volume it is a production posture. **Does the default become `NULL`-with-a-flag (unclassified, must be decided), does it stay `'work-product'`, or does it become context-dependent — and is that a ruling now or a design pass later?** *(Nothing was changed. Runner note: `transcript-workflows.md` was NOT edited either — reconciling its §1.3 prose against the built default is a ruling, not a repair, per the packet's §6 item 7.)*
- ✅ **`Q-COM-12` — Transcribing a recorded conversation may convert attorney's notes into a discoverable witness statement. Should the pipeline have a decision point there?** TRCP 192.3(h) makes *"any substantially verbatim transcription"* of a recording of a witness's oral statement a witness statement, while *"Notes taken during a conversation or interview with a witness are not a witness statement"* — and a substantially verbatim transcription is exactly what the stack produces. A communications log that transcribes by default performs that conversion at scale, with no decision point. **Does the design need an explicit transcribe/don't-transcribe gate keyed to context type, and does that gate belong to the attorney at capture time rather than to the pipeline?** **ANNOTATED add-only 2026-08-16 (#94), NOT CLOSED — PARTIALLY ANSWERED FROM THE SIDE.** `Q-COM-11`'s ruling (A) creates a **creation-time classification decision point** where none existed: with no default and no `NOT NULL`, a row arrives unclassified and something must classify it. That is the same moment this question asks about, reached from the privilege-tier direction rather than the pipeline direction. **But it is not the gate:** the ruling settles that a *classification* is chosen at creation, not that a *transcribe/don't-transcribe* choice is offered, and **the creation-time UI is a recorded follow-on act that was expressly NOT built.** The rule text this question turns on now sits in the registry as **TRCP 192.3(h)** (definition, including notes-are-not-a-statement) and **192.5(c)(1)**, both UNVERIFIED. **Timing is his: the gate can ride the creation-time UI when that is authorized, or be ruled separately first.** **ANNOTATED 2026-08-17 (#99), ADD-ONLY, NOT CLOSED — the commissioned research note is filed, and its headline is that this question's premise is off by one step in the expensive direction.** `docs/specs/witness-statement-conversion-research-2026-08.md` (`D-5`, dispatch T-22) reads TRCP 192.3(h)(2) as crossing the line **at RECORDING, not at transcription** — the recording is already a witness statement and the verbatim transcript is a second artifact of the same character — **so a decision point placed only at transcription sits downstream of the event it exists to control.** **And the memo goes further than the question does: a witness statement is a Rule 194.2(9) INITIAL disclosure owed without a request (194.1(a)), against which Rule 194.5 permits no work-product assertion at all.** *Trahan v. Lone Star Title Co. of El Paso*, 247 S.W.3d 269, is the same failure sanctioned; **the quietest exposure in the pipeline is the adjuster call, because nobody classifies it as a witness interview.** **A candidate restatement is proposed at the memo's §6 — the original question stands unless you replace it.** **Four registry gaps flagged (`WS-1`..`WS-4`) and NONE created; every proposition in the memo is UNVERIFIED.** **RULED 2026-08-18 (#100) — RESTATEMENT ADOPTED AND ANSWERED: BOTH.** The row takes the restated question (**recording, not transcription, crosses the 192.3(h)(2) line**), and the design direction is ruled: **decision points at BOTH capture and transcription**, with the discoverability suggestion derived from linked **PARTICIPANTS** rather than the human-chosen `context_type`. **Design direction only — nothing is built and this is not build authorization**; the transcript-pipeline specs pick it up at their next pass. `fable-adjudication-record-2026-08-18.md` §1 (Tier 5). **CLOSED.**
- ✅ **`QR-6(a)` — Should a runner step that states a verification be required to name the command that produced it?** **RULED IN — Michael, 2026-08-16 (#94). CLOSED.** Reason he gave: it did the decisive work at #93. Landed as runner **v9**'s Step 0 evidence rule, governing every step of the file, and carrying the audit's §4 complement — where a fact is genuinely unverifiable from the repo, say so and ask rather than assert. Two Step 1 claims were re-grounded this batch because their evidence could not have failed: `git rev-parse origin/master` reads the **local tracking ref** and returns a plausible SHA whether or not it fetched, and `git log --grep` searches **commit messages, not paths** and returns empty whether or not a file was ever committed. **Both share one shape — a check that cannot produce a disconfirmation.** The proposed rule: a command that cannot distinguish *absent* from *not looked for* does not count as a verification.
- ✅ **`QR-6(b)` — Should the runner text name the queue-header reconcile sentence as the second half of the merge?** **RULED IN — Michael, 2026-08-16 (#94). CLOSED.** Reason he gave: the #84 omission is recorded in the header itself. Landed at runner **v9** Step 4 item 2 — the merge is TWO acts, rows AND the header sentence. *(Runner note, forty-ninth invocation: on execution the omission proved to have happened **three** times, not once — the header's reconcile high-water was **#91**, so #92 and #93 are both missing their sentence while their rows landed. Reported, not back-written, per the #90 precedent recorded earlier in this same header.)* This file's Status header carries a per-batch *"Reconciled again to session-log #NN"* sentence and closes *"keep it current or the pointer lies."* **Runner Step 4 item 2 and every packet §8 to date mention only the row merge.** The omission has already happened once and is recorded **inside this very header** — #84's sentence went missing while its rows landed normally.
- ✅ **`QR-6(c)` — Should the Step 4 deletion be by explicit filename, with the packet's identity pinned at Step 1?** **RULED IN — Michael, 2026-08-16 (#94). CLOSED.** Reason he gave: the mid-STOP swap is on the record and the matcher covers explicit names at zero cost, proven at the forty-third and forty-fifth. Landed as runner **v9** Step 1 item 4 (the pinning half). *(**RUNNER-FOUND DEFECT, forty-ninth invocation — the ruling landed HALF.** The packet's §4.1 specified eight insertions and the pinning half is one of them; **no insertion adds "delete by explicit filename" to Step 4 item 5**, whose text still describes the deletion generically. The pinned identity is named as "what Step 4 item 5 deletes against," which carries the intent but does not state the by-name rule where the deletion happens. **Not added — inventing a ninth insertion is exactly the packet-added act `QR-6(e)` reserves to Michael.** One line of runner text would close it.)* The allowlisted matcher is a **glob**, `Bash(rm -f inbox/*)`, and Step 1's STOP is an open-ended window in which a new packet can land — **one was in fact swapped mid-STOP an invocation ago.** This batch deleted by name and pinned identity first (37,195 bytes, mtime `2026-08-16 14:38:58`, sha256 `eb0e2981…`); the allowlist still matches either form. **THE HALF-LANDING IS CLOSED — 2026-08-16 (#95). Michael authorized the one line, and the fiftieth queue-runner invocation landed it: runner v9 → v10.** Step 4 item 5 now opens *"Delete the processed zips from `inbox/` **BY EXPLICIT FILENAME — never the bare glob — each name checked against the identity pinned at Step 1**"*, and the version comment records the completion. **QR-6(e) was satisfied in the correct order** — the act was put to Michael and authorized *before* any runner edit was staged, which is the whole point of the rule that produced the finding. Nothing was renumbered; the four internal cross-references were re-checked after the edit and still hold.
- ✅ **`QR-6(d)` — Should the runner name its own format exemplar?** **RULED IN — IN DYNAMIC FORM, NOT AS PROPOSED. Michael, 2026-08-16 (#94). CLOSED.** The ruled wording: *format comes from the runner text's own Step 4 rules, and is never copied from an entry authored under an earlier runner version.* Reason he gave: the static exemplar the audit proposed had already gone stale — six compliant lines now exist — and a named static pointer is itself the QR-5 shape. Landed at runner **v9** Step 4 item 1. The forty-first and forty-second entries both close with *"Packet deleted after execution per Step 4.5,"* which v8 Step 4 item 1 now bans in terms. **Only the forty-third entry is a v8-compliant model**, so a runner copying a recent entry as a template has two chances in three of reproducing the banned sentence.
- ✅ **`QR-6(e)` — Should a packet-added act that appears in no routing-table row and no Step 4 item require in-session authorization?** **RULED IN — Michael, 2026-08-16 (#94). CLOSED.** Reason he gave: the §8.1 lock deletion was inert, and the next such act may not be. Landed as runner **v9** Step 3 item 4. *(Runner note, forty-ninth invocation: the rule fired on its first batch. The #94 packet's §1 reported a stranded 0-byte `.git/index.lock` and pointed at "the runner's existing stale-lock practice" without routing the deletion anywhere; it would have blocked the close-out commit. **Michael authorized the removal in-session at the Step 1 STOP** and it was recorded rather than done silently.)* This packet's §8.1 asked for a deletion **inside `.git/`**. It was inert (no literal `.git/index.lock` existed; `.git/index` had a later mtime, so index-modifying commands were already running), it was disclosed as cosmetic rather than as unblocking, and **Michael authorized it in-session before it ran.** Should that be the rule, or may a runner perform packet housekeeping silently?
- ✅ **`QR-6(f)` — On a docs-only batch, should the runner record the health-check skip explicitly?** **RULED IN — Michael, 2026-08-16 (#94). CLOSED.** Reason he gave: already exercised in terms at the forty-seventh and forty-eighth. Landed as runner **v9**'s unnumbered preamble at the top of Step 4 — deliberately unnumbered so no existing item renumbers and none of the four internal cross-references goes stale. *(Runner note, forty-ninth invocation: this batch was **not** docs-only — §5 routed `db/schema.sql` — so the health check ran and its result is recorded in the runner line, which is the other limb of the same rule.)* CLAUDE.md's standing instruction is `npm test` + `npm run build` + `npm run lint` before ending a session. §5 was NONE and no `src/` or build-tooling path was routed, so the gate would prove nothing. **It was stated rather than silently skipped** — silent omission of a standing instruction is indistinguishable from having forgotten it.
- ✅ **`Q-RE-1` — Which reading of the "out-of-area" trigger is the operative one: geographic (a matter in a county or state where the firm does not practice) or subject-matter (a matter outside PI, civil litigation, criminal defense and probate)?** The record carries both, from the same session and the same day — `session-log.md:7170` and the queue row say "out-of-area matters," while that session's own capture at `statutes-pass-est352-cprc71-2026-07-26.md:551–552` glosses the same sentence as "matters outside the practice areas." The two need different data: subject matter is answerable from `cases.practice_area` / `cases.case_type`, while geography is answerable from nothing the schema holds — `cases` has `court_name` and `cause_number` as free text and **no county, venue, district or jurisdiction column at all**. If the answer is "both," they are two triggers with two data requirements, not one. **The memo's sharpest question; the runner did not pick a reading.** **RULED 2026-08-18 (#106), his word: "Both, separate triggers."** The out-of-area referral trigger tests **geography AND subject-matter independently** — two triggers, two data requirements, exactly as the row framed the "both" branch. **FC-8, ruled the same session, separately puts structured county / court / district fields on `cases` when a schema act is next authorized**, which is what makes the geographic test answerable at all; **no migration is authorized by that ruling.** Full record: `fc-adjudication-record-2026-08-18.md`. — **CLOSED**
- ✅ **`Q-RE-9` — Does SOURCING acquire a fourth layer for the Texas Disciplinary Rules, and what is it?** SOURCING names three — the official bulk statutes corpus, the clean-authority TRCP/TRE/TRAP PDFs, and the eCFR API — and this session established that **none reaches the TDRPC**: the bulk `GV.pdf.zip` holds 494 chapter files and no appendix (the rules sit in a Government Code appendix, not a chapter), and the Knowledge Repo holds TRCP, TRE, TRAP and the criminal e-filing rules and no disciplinary-rules PDF. **The gap is not incidental: Gov't Code §82.065(b), §82.0651 and Penal Code §38.12(c) each incorporate the disciplinary rules BY NAME**, so three statutes the corpus does carry are unreadable in operative effect without a layer it does not. **Amending a binding convention is yours.** `RE-LOOK-3` is the cheapest concrete form of the fix; a named-official-web-source layer with a per-item currency requirement is another; and a third option is to rule that this layer stays out of scope, which would be a real answer and would also settle `Q-RE-8`. *(Runner note: SOURCING lives in the project instructions, not the repo, and was NOT amended.)* **RESOLVED 2026-08-18 (#106) — not by a direct ruling on this row but as a side effect of FC-14: SOURCING GAINS A FOURTH NAMED CHANNEL.** State Bar / court-published Texas conduct-and-administration rules, held as clean-authority PDFs in `Documents\Knowledge Repo\`, cited by each PDF’s own effective date. Michael acquired six documents by hand mid-session (TDRPC eff. 3/7/2025; Rules of Disciplinary Procedure amends. eff. 10/1/2024; Rules of Judicial Administration amends. eff. 7/1/2026; Code of Judicial Conduct; judicial-candidate disciplinary rules 04/01/2026; rules of judicial education). **The unverifiable-citation wall this row described falls with it** — TDRPC 1.04(f)(2) and 1.08(g) were checked against the source’s own face in the same session (record §4b). **That check is RETRIEVAL and everything from it stays UNVERIFIED; only Michael verifies.** SOURCING itself lives in the project instructions, not the repo — the amendment rides the v23 paste, which is Michael’s hand. Full record: `fc-adjudication-record-2026-08-18.md`. — **CLOSED**
- ✅ **OPEN-1 (from AUD-2):** GL-1 item 5 reads "instructions v15" in the canonical gates doc, "v17" in BUILD-STATE, and v18 is in force. **Do you append a one-line clarification to GL-1 reading item 5 as "the instructions current at the re-check, per trigger 1," or rule that the gates doc's frozen version number is fine because trigger 1 governs anyway?** *(On the launch path. `Go_Live_Gates.md` is canonical and append-only and was NOT edited.)*
  - *(**RE-RAISED 2026-08-16 as `GLR-1` by the CHAT-DISPATCH Task 18 runbook packet — NO SECOND ROW WAS CREATED. This is ONE question with THREE recordings, and one ruling closes all of them.** Forty-seventh queue-runner invocation. Prior recordings: `docs/specs/docs-lint-sweep-2026-08-13.md` §1 item 1 — which already framed the identical reading question, *"does GL-1 require literally v15, or 'the version current at the re-check'? Only Michael can say which"* — and then `AUD-2`, this row. **The Task 18 wording is carried here in full per QR-1, because the packet is deleted and this row is the only place it survives:** *"GL-1 floor item (5) names 'instructions v15,' and the instructions are now v19. GL-1 was ruled 2026-08-11, when v15 was current, and floor item (5) reads 'the gates re-check session and instructions v15 per trigger 1.' Trigger 1 is the go-live instructions revision, so the item almost certainly means 'do the trigger-1 revision against whatever version is then current,' not 'v15 specifically.' But read literally, a future session could treat item (5) as already satisfied because v15 exists — and v15 was in fact produced by trigger 3 (CRIM DEFENSE + K-5), not trigger 1 at all. Does Michael confirm floor item (5) means the trigger-1 revision against the then-current version, whatever its number? GL-1 stands as written either way; this asks how it is read, not that it be rewritten."* **TWO THINGS IN THAT WORDING ARE NEW, AND ARE WHY IT IS CARRIED RATHER THAN DROPPED AS A DUPLICATE:** (a) the **literal-reading hazard** — item (5) can be read as ALREADY SATISFIED, because v15 does exist, which is a sharper failure than a merely stale number; and (b) **v15 was produced by trigger 3 (CRIM DEFENSE + K-5), not trigger 1**, so the version number and the trigger it is bolted to do not even refer to the same revision. **ONE THING IN IT IS CONTRADICTED AT HEAD AND WAS NOT ADOPTED:** the clause *"the instructions are now v19."* `BUILD-STATE.md` records **v18 IS IN FORCE**, with **v19 a DRAFT** whose paste is Michael's and **expressly unasserted**, and `session-log.md` still carries "project-instructions v19 paste" as an open carried item. **The drift ACROSS the three recordings is itself evidence for this row: v17 → v18 → v19, each recording silently modernizing the number, which is precisely the failure the question asks you to close.** Nothing was edited in `Go_Live_Gates.md`, and this annotation resolves nothing.)*
  **ANNOTATED 2026-08-17 (#99), ADD-ONLY — UNTOUCHED AND GETTING OLDER.** **v21 is in force and the canonical gates doc still reads "instructions v15" at GL-1 item 5.** Nothing was edited here or there. **The question is now recorded THREE times** — `docs-lint-sweep-2026-08-13.md` §1 item 1, this row via `AUD-2`, and `GLR-1` — **and no fourth row was created.** **The drift is itself the argument for ruling it**, and #99's own meter note names the gates re-check (GL-1 item 5) as one of the two strongest remaining claims on your time.
  **RULED 2026-08-18 (#100) AT ITS FOURTH RECORDING — APPEND THE CLARIFICATION; EXECUTED THIS BATCH.** One line appended to GL-1 in `Go_Live_Gates.md` reads item (5) as **"the instructions current at the re-check, per trigger 1."** The frozen "v15" stands as written — the doc is append-only and was honored as such — and **the number stops carrying meaning, so it can never drift again.** **This closes `AUD-2` / `OPEN-1` / `GLR-1` together**: one question, four recordings, one ruling — which is exactly what QR-1's no-second-row discipline was protecting. `fable-adjudication-record-2026-08-18.md` §1 (Tier 6). **CLOSED.**
- ✅ **OPEN-4 (§12 of the charter, raised not decided):** This audit's charter and CHAT-DISPATCH are design-chat prompts with no durable home; Q-2 makes `docs/prompts/` canonical only for prompts executed by a CODE session. **Does Q-2 extend to cross-interface prompts executed by DESIGN chats (`docs/prompts/` with an "executed by: design chat" line), or does a parallel home belong somewhere else?** CHAT-DISPATCH's loss would make Tasks 7–19 unrecoverable except by re-derivation; it has now survived five sessions on the clipboard. **ANNOTATED 2026-08-15 (#81), NOT CLOSED — a SIXTH session, and Tasks 9–19 are still unwritten.** Task 8 completed from the same clipboard copy; the resume point is Task 9 (IN-1 and IN-3 spec drafts). **Eleven tasks now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#83), STILL NOT CLOSED — a SEVENTH session.** Task 9 completed from the same clipboard copy; the resume point is **Task 10** (IN-2 crash-report field extraction). **Ten tasks (10–19) now ride on a clipboard**, and every one of the seven sessions so far has re-pasted it by hand. **ANNOTATED AGAIN 2026-08-15 (#84), STILL NOT CLOSED — an EIGHTH session.** Task 10 completed from the same clipboard copy, which Michael re-pasted mid-session and which **matched the preserved knowledge copy verbatim**; the resume point is **Task 11**. **Nine tasks (11–19) now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#85), STILL NOT CLOSED — a NINTH session.** Task 11 completed from the same clipboard copy; the resume point is **Task 12** (the PR-3 re-parenting migration proposal). **Eight tasks (12–19) now ride on a clipboard**, and nine consecutive sessions have re-pasted it by hand. **ANNOTATED AGAIN 2026-08-15 (#86), STILL NOT CLOSED — a TENTH session.** Task 12 completed from the same clipboard copy, and the design session again reported that the **preserved knowledge copy at `claude/claude_CHAT-DISPATCH_v1_PRESERVED_2026-08-15.md` matched the pasted text** — a second consecutive corroboration, which narrows the loss risk without removing it, since the preserved copy is design-side and the prompt still has no repo home. The resume point is **Task 13** (the QBO integration research memo). **Seven tasks (13–19) now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#87), STILL NOT CLOSED — an ELEVENTH session.** Task 13 completed from the same clipboard copy, and the design session again reported that the **preserved knowledge copy matched the pasted text** — a **third consecutive** corroboration. The resume point is **Task 14** (the RE-1 inputs memo). **Six tasks (14–19) now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-16 (#88), STILL NOT CLOSED — a TWELFTH session.** Task 14 completed from the same clipboard copy. The resume point is **Task 15** (the communications-log ingest research memo). **Five tasks (15–19) now ride on a clipboard**, and twelve consecutive sessions have re-pasted it by hand. **ANNOTATED AGAIN 2026-08-16 (#90), STILL NOT CLOSED — a FOURTEENTH session.** Task 16 completed from the same clipboard copy, and the design session again reported that the **pasted text matched the preserved knowledge copy verbatim** — now **six times running**. The resume point is **Task 17** (the session-log table of contents, Sonnet-eligible). **Three tasks (17, 18, 19) now ride on a clipboard**, and **19 is INTERACTIVE — it runs only with Michael live**, so the clipboard has to survive at least until a session he attends. *(The risk is unchanged in kind and smaller in size: three tasks rather than eleven. What has not changed at all is the cause — the prompt still has no durable home in the repo, and this row is still the only place that fact is registered.)* **ANNOTATED A FIFTEENTH TIME — 2026-08-16 (#91).** Task 17 completed from the same clipboard copy, and the design session again reported the **pasted text matching the preserved knowledge copy verbatim** — now **seven times running**. The resume point is **Task 18** (the go-live runbook, pointer-style, a full read of `Go_Live_Gates.md` first). **Two tasks (18, 19) now ride on a clipboard**, and **19 is still INTERACTIVE — it runs only with Michael live.** *(A note this row can now make that it could not before: the fourteen prior annotations describe a risk that has never once materialized, and the seven verbatim matches are evidence about the copy's fidelity, not about its durability — a clipboard that has held perfectly for seven sessions is exactly as easy to lose on the eighth. **The count of successful pastes is not an argument against giving the prompt a home; `docs/prompts/` already exists for exactly this and OPEN-4 is the ruling that would put it there.**)* **ANNOTATED A SIXTEENTH TIME — 2026-08-16 (#93) — AND THE PACKET ASKED FOR A FIFTEENTH.** *(Corrected at merge rather than carried: #91 above already took the fifteenth, so this is the **sixteenth**. The off-by-one has a cause worth recording — **#92's session used the same clipboard and left no annotation on this row**, exactly as it left none on `HK-7`, so the row under-counts the very sessions it exists to measure. Recorded, not smoothed.)* **Task 19 was ATTEMPTED and is NOT COMPLETE, so for the first time in sixteen annotations the resume point did NOT advance — it is still Task 19.** **ONE task now rides on a clipboard**, and it is the **INTERACTIVE** one, so it runs only with Michael live. The prompt still has **no durable home in the repo** — the cause this row exists to register, and the thing OPEN-4 would rule. **CLOSED 2026-08-17 (#98) — `D-1` (`task-19-signoff-record-2026-08-17.md` §4): the durable home is `docs/prompts/`.** Michael's reason: **Q-2 already makes `docs/prompts/` canonical for cross-interface prompts, and the clipboard era ends at 21 sessions.** Executed this batch — **`docs/prompts/CHAT-DISPATCH-v3.md`** and **`docs/prompts/CODE-DISPATCH-v3.md`** both landed, copied verbatim from the packet.
- ✅ **Q-STAT-1 — RULED 2026-08-14 (#77): ADOPT AS DRAFTED. CLOSED.** The sourcing convention for statutes, rules and regulations is **binding**, sitting under Binding conventions beneath TOOLING, which it parallels for non-case law. Six sub-clauses as ruled: Texas statutes from the official bulk corpus at `Documents\Knowledge Repo\Statutes <download-date>\`, cited by code and chapter with the corpus download date (`/Docs/<CODE>/htm/<CODE>.<CHAPTER>.htm` the named fallback, `?link=<CODE>` never used); Texas rules from the clean-authority PDFs, cited with the PDF's own effective date; federal regulations from the eCFR API with request date and the title's `up_to_date_as_of` per item; **currency never inferred from a document**; **extraction artifacts characterized, not guessed** — transform only characterized contexts, REPORT the rest, spot-check every quotation against raw extraction; and **retrieval is not verification**. **Each source named per item. Trigger #3 fired and was resolved the same day: the full text lives in v18**, drafted and delivered, **Michael's hand to paste** — it is not repo content and was not written into any repo file. *(Original question preserved verbatim per QR-1 below.)*
- ✅ **Meter reading — STATED 2026-08-15 at 00:02 Central. The request, open since #68, CLOSES.** All-models **51% used**, Fable **78% used**, both resetting **Tue 2026-08-18 4:00 PM Central**; session 9%. **A timestamped snapshot, not a standing figure** — it moves continuously, and a stale percentage carried in the record would be worse than none. **Allocation read, design input and not a ruling: Fable is SCARCE, not idle** (~22% across 88 hours, realistically one substantial session), so the **ration-it** branch of the ruled economics clause applies this week rather than spend-it-before-it-expires; all-models at 51% is comfortable, so **Opus and Sonnet work is unconstrained**. Strongest claims on the Fable remainder per §7.2: the **gates re-check** (GL-1 item 5), an **adversarial audit of the record**, and **V-9**. **Chain execution, FE-D1, the CD-1 migration and packet assembly stay OFF Fable. No allocation is ordered.** *(One observation, flagged and NOT corrected: the meter displays the reset as **4:00 PM** where the 2026-08-13 observation recorded **3:59 PM Central**. Not grounds for a version bump; v18 keeps the older figure as delivered.)*
- ✅ **Paste v18 into the Claude project's instructions — RULED 2026-08-15 (#82): v18 IS IN FORCE. CLOSED (A-1, from the audit's AUD-1).** Michael ruled it item by item in the Fable disposition session. Evidence: **two independent design sessions read their own governing instructions and found them headed `v18 — 2026-08-14` with all v18 markers** — the 08-15 auditing session and the disposition session itself. **Paste date not stated; the row stays undated.** **BUILD-STATE's two v18 lines were rewritten at the seventy-first refresh and "paste v18" came OFF the your-hand list.** *(Recorded plainly because it bears on how much this closure is worth: **a Code session structurally cannot see a paste.** This row closes on Michael's ruling and on a design-side inference, not on anything the runner verified — which is precisely why #79's packet forbade Code closing it and why it took a ruling to close.)* *(Original question preserved verbatim per QR-1 below.)*
- ✅ **NEW 2026-08-14 (#77) — Should route (c) become STANDING LAW, or stay the one-off it was ruled as?** Route (c) is the divergence-handling route ruled 08-14: **a session drafts PROPOSED replacement wording for materially divergent registry propositions**, the non-material ones staying as flags. It was **deliberately NOT elevated into v18**, at Michael's direction to choose the safest option and review later with Fable 5, **because a standing rule licensing sessions to draft registry wording pushes against the discipline that wording acts are Michael's alone.** **The question: does route (c) become a standing convention, stay a per-pass ruling you give each time, or get narrowed further?** **Recorded as a choice, not an omission — it is the first item for the Fable review pass.** Nothing is elevated anywhere in the meantime: not CLAUDE.md, not BUILD-STATE, not the spec. **ANNOTATED add-only 2026-08-15 (#79), ROW NOT CLOSED — the Fable audit recommends KEEPING IT A ONE-OFF (audit doc §5).** Its reasoning: the audit's own AUD-3 shows registry wording drifting at the *transcription* layer where no drafting license exists at all, so widening the licensed surface is the wrong direction; and no flag-rot evidence has appeared to argue the other way. **A recommendation, not a ruling — the ruling is yours.** **ANNOTATED add-only 2026-08-15 (#81), ROW STILL NOT CLOSED — the strongest test case yet arrived and the restraint held.** Task 8 found a divergence **inside a VERIFIED entry** (Q-FE5-9) — the situation where drafting replacement wording is most tempting and most dangerous, because verification attaches to wording and a redraft would silently detach it. **The session drafted no replacement wording and edited no registry file**, citing route (c) as a ruled one-off. **Recorded as evidence for the ruling, not as an argument either way:** the one-off posture produced the right behavior without a standing licence. **ANNOTATED 2026-08-16 (#93), ADD-ONLY — ROW STAYS OPEN AND REMAINS THE FABLE REVIEW PASS'S FIRST ITEM.** In the Task 19 session Michael stated his test verbatim — *"I honestly think that the proposition should match the actual rule text"* — and clarified it the same session: *"To be clear, my statement earlier is consistent with Reading A."* **READING A = a proposition must not MISSTATE the operative text; deliberate narrowing to the clause a module relies on stays.** (Reading B — that a proposition must *reproduce* the text — was put and **not** taken.) **SCOPE WAS PUT AND NOT ANSWERED**, so the session took the conservative option per the standing instruction for delegated scope decisions: **Reading A governs entries 1, 2 and 3 only, it was applied to NONE of them, and route (c) stays the one-off it was ruled.** **Whether Reading A is a general principle, and whether route (c) becomes standing law, are both still yours — unchanged by this.** **CLOSED 2026-08-16 (#95) — RULED: ROUTE (c) IS STANDING LAW, and it fires instructions trigger #3 → v21.** The adopted convention, in the ruling's own terms: **whenever a retrieval or verification pass finds a divergence that CHANGES WHAT AN ENTRY MEANS, proposed conforming wording is drafted and queued for Michael's adopt/reject/edit — never adopted silently.** Michael's reason: **it matches how the mechanism actually worked** across #76 (the four §9 drafts), #93 (drafts for entries 1 and 3, deliberately not staged so they would not look ruled), and #95 (all six put to him one at a time and adopted). **Note what the ruling does and does not license:** drafting and queueing, never adopting — the adopt/reject/edit act stays Michael's alone, which is the discipline this row was opened to protect. **The Fable audit's contrary recommendation (#79, keep it a one-off) is recorded as considered and not followed**; the two years of evidence since ran the other way, and the restraint the audit worried about is preserved by the never-silently clause rather than by withholding the licence. The convention lives in **instructions v21**, not in CLAUDE.md, BUILD-STATE, or any spec — see the `v21 INSTRUCTIONS PASTE` row; until that paste, v20 is in force.
- ✅ **K-5** — Uvalde docket worksheet instructions: home undecided. **CLOSED 2026-08-11 (RULED): home is the CRIM DEFENSE practice project's knowledge, uploaded by Michael's hand — never the repo. Ruled in the same session that created the CRIM DEFENSE practice project (all criminal-side practice work; REQ-1 extended; instructions v15). See #54.**
- ✅ **GH-1 — The two real prior-case identifying strings generalized out of `form-engine.md` §8 by the FE-3 ruling (#55) survive in git history: prior commits of the file still contain them. Rewrite history to purge them, or accept?** **RULED 2026-08-12: ACCEPTED — history stands.** Reason: a rewrite changes every commit sha (invalidating the shas the session log cites as record), forces re-clones on both machines and a design-side re-sync, with MM-1 coordination throughout; the repo is private and the strings are of modest sensitivity, absent from the working tree since `cfa1692`. **TRIPWIRE, standing: REVISIT before repo visibility widens** — multi-user phase (gate 2 / instructions trigger 2), any outside collaborator, or the repo going non-private. Pointer at gate 2 per same-day ruling. See #56; dialogue trail in the project-knowledge capture. *(New ID 2026-08-12; collision-checked at reconciliation per the K-6/K-7 rule — no GH series existed anywhere in this file or the session log.)*
- ✅ **QR-1** — QUEUE-RUNNER merges packet open items down to ID + label and then deletes the packet, **losing the question itself** (this destroyed Q-5's original wording). **CLOSED 2026-08-07: ruled yes.** The runner carries FULL question text into this file when merging packet open items; ID+label-only merges are barred. `QUEUE-RUNNER.md` and `.claude/commands/queue-runner.md` went to **v3** in the same batch. Fires instructions trigger #3 → v6 drafted.
- ✅ **QR-2 — PROPOSED, unruled (2026-08-07). Should the machine-local queue-runner skill/command copies stop carrying the runner's full text and become POINTERS — "read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it" — so that a stale local copy is structurally impossible?** Evidence: the machine-local copies have now loaded **stale twice in two consecutive runs** (#35 loaded v1 against repo v2; #36 loaded v1 against repo v3), and **both times only the runner's own vigilance caught it.** The repo copy is already canonical; this makes the invocation defer to it. **Cost:** one extra file read per invocation. **Rejected alternative, and the reason it is rejected:** keeping full text locally and re-syncing by hand IS the observed failure mode. **Scope note (verified from Code 2026-08-07):** `.claude/commands/queue-runner.md` is **tracked and current at v3** — it updates with the repo and is not the problem; the lag lives in the **untracked machine-local skill copies**, of which there are now at least two (P15, P1). **Michael's.** **CLOSED 2026-08-08: ruled YES** ("QR-2 - yes"). Copies become pointers — *"read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it"* — and that file is now the ONLY full copy. Decisive evidence: #38 was the **third consecutive stale-skill run** (local v1 against repo v3) and cost nothing only because the packet and the design reply independently supplied both deltas — luck standing in for process. `.claude/commands/queue-runner.md` was rewritten to pointer form the same day; **the P15/P1 machine-local copies are Michael's hand.** Fires instructions trigger #3 → v7 drafted. Full reasoning: `rulings-capture-2026-08-08b.md` Part 1.
- ✅ **QR-3 — the runner's pointer reads `docs/prompts/QUEUE-RUNNER.md` at HEAD, but HEAD is whatever the local checkout happens to be; a stale tree serves stale runner text with no signal (demonstrated 2026-08-08: v1 read as current, 14 commits behind). Should Step 0 gain a gate — fetch origin and confirm the checkout is at origin HEAD before reading the runner; if behind, sync or stop?** CLOSED 2026-08-08: ruled yes, sync-and-proceed shape — behind-but-clean-on-master fast-forwards and continues; dirty/diverged/off-master stops. Runner to v4. See `rulings-capture-2026-08-08c.md`. **AMENDED TO v7, 2026-08-13 (#67): the gate ALSO STOPS WHEN HEAD IS AHEAD of `origin/master`** (unpushed local commits) — tell Michael, proceed only on his word, never build on an unpushed tree silently. **Why it was needed:** the twenty-fifth invocation found that a gate stopping only on behind / dirty / diverged / off-master lets *ahead* sail through — so the one state that means "the design side has not seen these commits" was the one state with no guard, and the packet running at the time had explicitly relied on the gate to enforce push-ordering. Runner text amended the same batch; the clause was also folded into the unpasted v17. *(No standalone row ever existed for the gap — it was flagged in BUILD-STATE and the session log by the twenty-fifth invocation and is annotated here rather than given a row that would exist only to be closed: the #57/#60 disposition.)*
- ✅ **QR-4 — PROPOSED, unruled (2026-08-10). Step 1 of QUEUE-RUNNER.md orders packets by file modification time, but inbox mtimes are SAVE times, not session times, and can invert authoring order — demonstrated 2026-08-09/10, when the later-authored t3-kickoff packet (filename 2026-08-10) carried an EARLIER mtime than the earlier-authored slack-claude-tag packet (filename 2026-08-09), because it was saved into `inbox/` first. Order is semantic, not cosmetic: the conflict rule ("later packet wins") and the session-log append order both turn on it. Should Step 1 be amended to: sort by filename date, oldest first, with mtime as tiebreak and as fallback for undated filenames (flagged explicitly); ALWAYS compute both orders and print both whenever they disagree — never silently pick one; and, after unzipping, cross-check the confirmed order against each manifest's §3 entry date, stopping on contradiction? The Michael-confirm gate stays exactly as is — this changes the inferred order and makes disagreement visible; it does not remove the stop. Cost: a few lines of runner text. Rejected alternative — keep mtime primary: rejected because the 2026-08-10 inversion was caught only by the running session noticing the disagreement unprompted; the gate showed one inferred order and nothing forced the conflict into view. If ruled yes: runner v5 → v6; fires instructions trigger #3 → v10 draft. Michael's.** **CLOSED 2026-08-10: RULED ADOPTED** as a standing amendment to Step 1 — filename date primary (oldest first), mtime as tiebreak and fallback for undated filenames, both orders computed and printed whenever they disagree, and the confirmed order cross-checked against each manifest's §3 date. Ruled during the NINTH queue invocation, where the same mtime/filename-date inversion recurred — the second consecutive batch requiring a hand ordering call. `docs/prompts/QUEUE-RUNNER.md` went to **v6** the same day; per QR-2 the tracked command file is a pointer and carries no version text, so it was not edited (see the runner entry's flag). Fires instructions trigger #3 → v10 drafted design-side 2026-08-10. Reasoning: `rulings-capture-2026-08-10.md` §1.
- ✅ **MM-1 — with both machines (P15 + P1 Gen 8) in concurrent two-workspace use, what does concurrency itself require beyond the standing cross-machine rules?** CLOSED 2026-08-08, ruled yes to all four parts: one-runner-at-a-time with the non-FF-stop backstop; inbox-bound destinations name the machine; user-level runner copies deleted (not converted — shadowing risk); bootstrap addendum (git identity + deletion step). Runner to v5. See `rulings-capture-2026-08-08d.md`.
- ✅ **QR-5 — a session-log entry was asserting actions that had not happened yet, and the one Step 4 action nobody ever allowlisted.** **CLOSED 2026-08-16 (RULED BY MICHAEL IN TWO PARTS, and executed the same day by the forty-third invocation).** **Part one, ruled on the leftover-zip finding: *"Verify + narrow rm allowlist."*** **Part two, ruled after a Code session's own QR-3 v7 ahead-stop exposed the deeper defect: *"Strike the claim + carry forward."*** The defect: the sentence *"Packet deleted after execution per Step 4.5"* was **template boilerplate written into the entry at Step 4 item 1 and committed at item 3 — before the push at item 4 and the deletion at item 5.** It could never be a report; it was a prediction in the voice of one. It happened to come true for the forty-first invocation and **went FALSE for the forty-second, whose close-out was interrupted at the push** — so the false sentence is permanently in the record at `398c78c`, corrected in #88 and never edited in place. **The ruling, runner v8:** (a) **the entry must assert NO post-commit action** — results go to Michael in-session and any failure is carried into the NEXT batch's runner line, where it can be said truthfully; (b) the deletion is **VERIFIED** by re-listing `inbox/`, Step 1 marks a packet whose deliverable already exists as **POSSIBLY ALREADY EXECUTED** while distinguishing *committed-and-pushed* from *committed-but-unpushed*, and Step 0 gains the precondition `Bash(rm -f inbox/*)` in the untracked machine-local allowlist — **that narrow and no wider, never committed.** Alternatives Michael did **not** take: minimal repair keeping the template (leaves the sentence that produced the false claim), and amending the entry after the delete (truthful, but a second commit and push per batch, against the one-push shape). `CLAUDE.md`'s mirrored `inbox/` sentence updated add-only; **QR-2 keeps the full text in the runner alone.** *(The permission gap is real and is fixed — the deletion was the only Step 4 action never allowlisted, so it prompted on every run and was unanswerable from a phone — **but it was NOT this failure's proven cause**: a close-out interrupted at item 4 never reaches item 5 at all, so nothing establishes the deletion was ever attempted. The fix is recorded without letting it imply a diagnosis.)* **Fires project-instructions trigger #3; a v19 draft was produced design-side and the paste is Michael's.**
- ✅ **SK-v2 — the `drafting-disclosures` skill's v2 revision.** Per the ruled upgrade protocol the spec fold-in comes FIRST (landed 2026-08-11 at `form-engine.md` §§12.7–12.13); **a design session then revises `docs/skills/drafting-disclosures/SKILL.md` to v2 citing the spec at HEAD, with a version bump.** Sequenced, not blocked — needs a design session. *(Code must not edit the skill; the 08-10 packet's §6 bars it.)* **v2 AUTHORED AND LANDED 2026-08-11** — the revision is a design-session deliverable per the ruled upgrade protocol (spec fold-in first, at `form-engine.md` §§12.7–12.13; then the design-side revision citing the spec at HEAD `13f736a`), so the queue runner landing it is routing, not editing, and the Code-must-not-edit bar is intact. v2 folds attribution verification + the imaging-entity question (§12.7), operator-local service dates (§12.8), style-by-role assertion with the step-1 style harvest (§12.9), caption label style (§12.10), block keep-together in the ship gate (§12.11), served-final-as-next-shell (§12.12); the provider-data block's purpose line now names the contact directory as its consumer (FE-1 scout → CD-1 seed evidence). **REMAINING, and why this entry stays open — Michael's hand, now actionable:** **upload `docs/skills/drafting-disclosures/SKILL.md` (v2) as the claude.ai skill copy now that the revision is at HEAD.** *(This absorbs the standalone "Hand-upload of `drafting-disclosures` as a claude.ai skill" item carried from #45 — still open, a `.skill` packaging was offered in-session and not acted on — consolidated here per QR-1's one-home rule; text moved, not deleted.)* **CLOSED 2026-08-25.** The remaining limb — "upload `docs/skills/drafting-disclosures/SKILL.md` (v2) as the claude.ai skill copy now that the revision is at HEAD" — is DISCHARGED: v2 was authored and landed 2026-08-11 (the file's own status line reads `STATUS: v2, 2026-08-11`, citing the spec at HEAD `13f736a`), and **Michael read the version line in his uploaded claude.ai copy on 2026-08-21 and confirmed it reads v2.** The earlier in-session finding that the upload was still owed was wrong and was corrected on the record the same day (actor Opus 5, 2026-08-21 voice session; failure class: asserting from a consistent signal — the file's timestamp — when the decisive one, the version line inside the file, was one read away). **This closure also discharges the standalone hand-upload item absorbed here from `#45`.** Staged design-side at `#129`; packaged 2026-08-25.
- ✅ **v14 INSTRUCTIONS PASTE — Michael's hand, same day per instructions trigger #3: paste project instructions v14 into the project's instructions field.** v14 carries **DT-1**, the date-stamp convention ruled 2026-08-11 Central and registered in log #50: *every date a design-side session stamps — session dates, ruling and verification dates, "Entered" lines, filename dates on packets and captures — uses Michael's wall-clock Central date, never the container's UTC date. The container rolls to the next date at 19:00 Central (daylight) / 18:00 (standard); sessions running past that hour check before stamping.* The draft was handed to Michael the evening of 2026-08-11. **Until the paste happens the convention is recorded but not in force for future design sessions** — the log entry binds nothing on its own. *(Entered 2026-08-11 with full text per QR-1. Code verifies nothing about the paste; instructions are Michael's hand entirely.)* Context: DT-1 removes the cause that QR-4 handles as a symptom — the same container-UTC failure class produced QR-4's filename/mtime inversions and #47's certificate-of-service date defect. **CLOSED: paste VERIFIED design-side 2026-08-11** — the CD-1 schema session read the live instructions field at v14 at session start and recorded it in its entry (log #51); **DT-1 is in force.** First in-force application caught its own target failure class live in that session: chat confirmations stamped 08-12 from container UTC until the pre-authoring clock check (2026-08-11 21:38 CDT); no artifact was mis-stamped. *(Code verifies nothing about the paste itself — this closure rests on the design-side session's own verification, cited per this entry.)*
- ✅ **v17 INSTRUCTIONS PASTE — NEW 2026-08-13 (#66). Michael's hand, same day per instructions trigger #3: paste project instructions v17 into the project's instructions field.** The file was **delivered to Michael, deliberately NOT routed to the repo** (project instructions are his, and Code must not edit them). v17 carries: the **Descrybe-out** tooling convention (Descrybe is not used in this project at all until further notice; revisit at go-live); **SAT-1** (satellite-project pattern, both parts); the **§7.2 model-routing clause** folded into MODEL USAGE with economics refreshed to the observed 2026-08-13 Max terms — which is what closes **Q-5** and **HK-2**; the **HK-5** rule (preflight questions about Michael-supplied material are answered by Michael first, never by sweeping his machine unprompted; he may direct a search per instance); the "BS-1 provenance" phrase corrected to the ledger-pointer line; and the pre-Cowork read-only wording updated. **Until the paste happens, these conventions are recorded but not in force for future design sessions** — a log entry binds nothing on its own (the v14 precedent directly below). **The next fresh design session verifies v17 in force** (#54 precedent). *(Code verifies nothing about the paste; instructions are Michael's hand entirely.)* **REVISED SAME DAY, 2026-08-13 (#67) — same version number, new file: the QR-3 v7 ahead-stop clause and the line-count convention were folded in. PASTE THE REVISED FILE delivered 2026-08-13 evening; the earlier same-day download is SUPERSEDED.** **ANNOTATED add-only 2026-08-16 (#94) — OVERTAKEN BY EVENTS, AND DELIBERATELY NOT CLOSED.** **v19 is in force by observation:** the #94 design session read its own live project instructions and they were v19, dated 2026-08-16 with the QR-5 header present — the #82/A-1 mechanism, and the same evidence class that closed the v18 row above. Michael's own recall was *"No / not sure"*; the observation, not the recall, is the evidence, and that is recorded rather than smoothed. **Consequence for this row: an instruction to paste v17 asks for an act two versions stale.** *(**RUNNER-FOUND RECORD DEFECT, forty-ninth invocation.** The #94 packet's §4.3 item 5 directed that *"the project-instructions v19 paste item CLOSES."* **No `v19` row has ever existed in this file** — the paste chain here is v14 ✅, **v17 ⬜ (this row)**, v18 ✅, and the "v19 paste" the packet closes was a carried item in the session log's own hand-list, not a register row. **So the close has no target here and none was performed.** Closing THIS row on the v19 observation is a one-word ruling and is Michael's; it is not done, because the ruling names v19 and this row names v17.)* **CLOSED 2026-08-16 (#95) — RULED BY MICHAEL: overtaken by events, three versions stale.** The one-word ruling the forty-ninth invocation reserved to him was given in the #95 design session: v19 then v20 were each confirmed in force by observation (the #82/A-1 mechanism), so an instruction to paste v17 asks for an act no longer available. **The row closes as spent, not as wrong** — nothing in the v17 text is retracted, and every convention it carried reached force through the later versions.
- ✅ **v20 INSTRUCTIONS PASTE — NEW 2026-08-16 (#94). Michael's hand, same day per instructions trigger #3: paste project instructions v20 into the project's instructions field.** The file was **drafted in the #94 design session and delivered to Michael SEPARATELY — deliberately never inside the packet and never routed to the repo** (project instructions are his, and Code must not edit them). **Trigger #3 fired TWICE in that one session,** and v20 carries both new conventions: **(1) the cite-stability convention** — line-number cites are permitted only into *append-only* files; **prepending** files (the session log) and **wholesale-rewritten** files (BUILD-STATE) are cited by heading or by quoted sentence instead. Derivation: #91 (prepend → no line cites), #92 (append → fine), #93 plus the forty-eighth runner line (rewrite → never); BUILD-STATE's own header note was the existing half, and this makes it project-wide law. **(2) the classifier operational note** — the auto-mode classifier may refuse **even allowlisted bare commands** (the forty-seventh invocation's bare `git push origin master` was refused on *both* shells, which retired the earlier "prefer bare forms" guidance); a remotely-driven close-out can therefore strand at the push with permissions fully in place; QR-5(a) plus the QR-3 ahead-stop is the designed catch; and **never conclude a batch landed without `git ls-remote`.** **Until the paste happens these conventions are recorded but not in force for future design sessions** — a log entry binds nothing on its own (the v14 and v17 precedents above). **The next fresh design session verifies v20 in force**, the #54/#82 mechanism. *(Code verifies nothing about the paste; instructions are Michael's hand entirely. This row REPLACES the carried "v19 paste" hand-item, which #94 closed on the observation above.)* **CLOSED 2026-08-16 (#95) — CONFIRMED IN FORCE BY OBSERVATION, the #82/A-1 mechanism used for the third consecutive time.** The #95 design session read its own live project instructions and they are **v20** — dated 2026-08-16, carrying the QR-6(a)–(f) and CITE-STABILITY headers. **The observation, not recall, is the evidence**, and it is the same evidence class that closed the v18 row and settled v19. The paste happened; the row closes.
- ✅ **v21 INSTRUCTIONS PASTE — NEW 2026-08-16 (#95). Michael's hand, same day per instructions trigger #3: paste project instructions v21 (delivered separately 2026-08-16) into the project's instructions field.** v21 is **v20 plus the route-(c) standing convention under registry discipline, the trigger-#3 history line, and the header bump.** The convention it carries, ruled this session: **whenever a retrieval or verification pass finds a divergence that CHANGES WHAT AN ENTRY MEANS, proposed conforming wording is drafted and queued for Michael's adopt/reject/edit — never adopted silently.** The reason he gave: it matches how the mechanism actually worked across #76, #93 and #95. **Until pasted, v20 remains in force** — a log entry binds nothing on its own (the v14, v17 and v20 precedents above). **A future design session closes this by observation, the #82/A-1 mechanism.** The file was **drafted in the #95 design session and delivered to Michael SEPARATELY — deliberately never inside the packet and never routed to the repo** (project instructions are his, and Code must not edit them; the packet's §6 bars carrying the v21 text in any repo file). *(Code verifies nothing about the paste; instructions are Michael's hand entirely.)* **CLOSED 2026-08-17 (#96) on the design side's own observation, attributed as such:** the #96 design session reported reading its **own live instructions field** and finding **v21, 2026-08-16**, carrying ROUTE-C as a standing convention — the #82/A-1 mechanism, fourth consecutive use. **A Code session cannot verify this** (the project-instructions field is not readable from the repo, and #95 records that v21's text is in no repo file), so this closes on that report and **not** on any check this session ran.
- ✅ **REGISTRY-V — NEW 2026-08-16 (#94). Michael's verification, nobody else's: verify the three new TRCP work-product entries.** `legal-rule-registry-discovery-enforcement-and-pleading.md` gained **TRCP 192.5(d)** (an assertion of work product is an assertion of privilege), **TRCP 192.5(c)(1)** (witness statements excepted from work-product protection), and **TRCP 192.3(h)** (what a witness statement is — including that notes taken during an interview are *not* one). **All three are UNVERIFIED and drive nothing computed.** They are the registry's **first work-product propositions anywhere**, answering the narrowest part of the fifth absence pattern (#89–#88's finding that the registry carries the law of the firm's cases and nothing about the law of its practice). **Source caveat carried, not smoothed:** the clean-authority PDF's opening page has no separate effective-date line, so *"July 2026"* is the filename's designation rather than a stated effective date. **Two placement questions ride with it, both one-word answers:** whether the two `192.3(h)` entries should be consolidated into one entry carrying two propositions, and whether the header count convention should be re-checked after any consolidation. **CLOSED 2026-08-17 (#98) — ALL THREE VERIFIED BY MICHAEL AT THE SIGN-OFF WALK** (`task-19-signoff-record-2026-08-17.md` §1, rows WP-1/WP-2/WP-3): **TRCP 192.5(d)**, **TRCP 192.5(c)(1)** and **TRCP 192.3(h)**'s definition of a witness statement. Each Status line now reads `VERIFIED — Michael, 2026-08-17`. **The effective-date caveat was ACCEPTED, not smoothed away** — he accepted the source PDF's filename designation as its currency statement, and that acceptance covers all three. **WP-3 additionally COMMISSIONED a research note in his own words** (`D-5`): “make a research note for later to look into the implications of transcribing certain conversations and when the line of work product to witness statement is crossed” — routed to CHAT-DISPATCH v3 **T-22**, adjacent to `Q-COM-12`. **The labels WP-1/2/3 remain packet-local: `Q-T19-1` is still open and numbering is still yours.**
- ✅ **NEXT BUILD SLICE — which slice does Michael name as the next build target?** The build queue has been empty since CL-2, and the question has ridden in session-log §7 tables since #31 ("Next build slice — OPEN, Michael names it") **without ever being registered in this file** — entered here 2026-08-11 with full text per QR-1 when the CD-1 packet's routing looked for it and found nothing. **OPEN BY RULING 2026-08-11: naming is DEFERRED** — Michael ruled that the next slice is named in a later design session **citing `docs/specs/contact-directory.md` at HEAD**, not in the schema session itself. What changed with that spec landing (statuses, not rulings): **the form engine is again nameable** — its identity source is resolved, FE-4–FE-7 unblock for spec completion, and FE-2's parked build home can be raced honestly. Code must not name a slice; nothing enters the build queue without an explicit authorization packet in Michael's words. **Michael's, next design session.** **CLOSED — RULED 2026-08-11: the CD-1 directory build, scoped and authorized; text at `docs/specs/cd1-build-slice.md`.** The deferral's own condition (name the slice in a later session citing `contact-directory.md` at HEAD) was satisfied the same evening, after the thirteenth queue invocation landed the spec at `13f736a` and the design side verified the sync. Reason recorded: substrate first — the form engine consumes the directory and its own spec is incomplete (FE-4–FE-7 are spec-completion work). Anti-resurrection check run deliberately and on the record: **NOT Slice A returning** — Slice A was the provider-level framing, withdrawn 2026-08-08 for baking the wrong level; this builds the ruled level. Michael fires the build session; see log #52.
- ✅ **Queue-wide duplication sweep — does any OTHER item live twice?** Raised 2026-08-08 by the CL2-AC-1 finding (that item lived in two places in this file until the same-day dedup ruling consolidated it). *(Wording carried verbatim from the 08-08b packet's §7 per QR-1 — it had never been entered in this register, only in the packet table.)* **RULED AND RUN 2026-08-13 (#68), by widget, full-text against the checkout at `9ac12c8`: CLOSED — NO DUPLICATE.** No question lives as two full rows anywhere in the register. Three glyph-bearing pointer rows found (PR-3, RE-1, registry 1–10 sign-off) and ruled the same session: glyphs stripped (this batch). Cross-register observation, not queue duplication: the "Itemized-bill ingest" capture row overlaps billing Phase 1b scope — OBS-1, Michael's.
- ✅ **Cross-document status-drift sweep — do any design-doc status rows or tables state a ruling status that lags the queue/log record?** Raised 2026-08-08 by the §10 CL2-AC-1 row found stale (still "PROPOSED, unruled" after the DIRECTION-CONFIRMED ruling). Distinct from the queue-duplication sweep item. **FINDING #1 LOGGED 2026-08-13 (#66): the Go_Live_Gates paste-ask was stale** (see that row, now closed as already-done). **RUN 2026-08-13 (#68), same session and method: CLOSED AS A PASS — findings #2–#9** (seven stale locations across four design docs + the model-routing status header, plus three CLAUDE.md paragraphs; six lag the #66 ruling run, #9 lagged BUILD-STATE since 2026-07-28). All fixed this batch by add-only annotation per ruling. A pass, not a standing guarantee — re-running after future ruling batches is cheap and is Michael's to order.
- ✅ **RR-1** — re-read documents authored earlier in a session against rulings made later in it, before the packet ships. **CLOSED 2026-08-07: ruled yes; now a binding convention** (sending-side mirror of RECONCILE FIRST). Live exhibit: FE-3's shell content, which the rule would have caught. Binds Claude, not Michael. Fires instructions trigger #3 → v6 drafted.
- ✅ **SAT-1 — RULED 2026-08-13 (#66): ADOPTED, BOTH PARTS. Rides instructions v17.** The
  satellite-inventory instructions update fires at the first NON-Domser satellite. The adopted text,
  preserved: Two parts. **(a) The criteria:** a piece gets its own satellite project when it needs a large specialized corpus, confidentiality-segregated material, or a sustained observation/research function; a satellite HOLDS knowledge and produces index/synthesis/captures that travel **by Michael's hand**. **(b) The ONE-RULING-SPACE principle:** satellites never rule; nothing enters the build queue from a satellite; the log, the queue, and packet issuance stay singular in the design project; ordinary module design stays in the design project. Each satellite gets Domser-template instructions at birth.
- ✅ **CORPUS-HOME — PROPOSED, unruled (2026-08-07). Final home of `probate_knowledge_corpus.md`: the DOMSER PROJECT?** This **supersedes the #34 ARCHIVE recommendation**. Rationale offered: Domser is the one place Part III privilege is load-bearing, the corpus is core knowledge there rather than dilution, and licensed material stays inside one privileged workspace. **Until ruled, the ARCHIVE recommendation stands and `Probate Corpus.zip` stays UNTOUCHED in `inbox/` per the #31 ruling** — not deleted, not extracted, not moved, not diffed. **Michael's.** **CLOSED 2026-08-08: the ARCHIVE project.** (Michael: "Yes." — the DOMSER proposal above was not adopted; the #34 ARCHIVE recommendation became the ruling.) Execution Michael's hand — an upload into that project's knowledge, not a repo action; the corpus never enters the repo regardless (licensed James Publishing / LexisNexis; Part III privileged). `Probate Corpus.zip` stays untouched in the P15's gitignored `inbox/` until that upload; deleting it afterward is Michael's step. See `rulings-capture-2026-08-08.md` Part 6.
- ✅ **O1 (transcript sort & route) — RULED 2026-08-13 (#66): D1 CONFIRMED — confirm-only STANDS for v1; auto-file stays deferred.** *(Original question: confirm or veto D1, confirm-only in v1 with auto-file deferred until the decision log shows real precision. It gated nothing — the build is confirm-only either way — and had been waiting on the Phase 0 scorecard as its evidence base. Ruled without waiting for it.)*
- ✅ **O3 (transcript sort & route) — RULED 2026-08-13 (#66): non-case recordings go to a PERSONAL STORE. Nothing is destroyed.** *(Original question: where do NOT-case-related recordings go — a personal store, or discard-with-log? The discard half is rejected.)*
- ✅ **O2 (transcript sort & route) — P1 OS at purchase: native Ubuntu vs. Windows+WSL2? CLOSED 2026-08-07 (Michael: "windows only")** — Windows host, WSL2 runtime. Native Ubuntu and dual-boot rejected; NeMo has no native Windows support, so WSL2 with CUDA passthrough is the runtime *inside* the Windows-only ruling. Escape hatch on record: a dedicated headless native-Ubuntu box if volume outgrows the laptop. Full text at `transcript-sort-and-route-design.md` §10 and §12.
