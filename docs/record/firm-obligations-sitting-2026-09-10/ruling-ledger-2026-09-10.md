# RUNNING LEDGER — the firm-obligations RULING SITTING (typed, Cowork, Fable 5.1) — 2026-09-10 (Central, DT-1)

Kept in the container within each exchange, in Michael's words (the 2026-08-31 practice). This file is the source the capture copies from; it is filed as EVIDENCE.

## Session facts at open (bridge reads via staged copies; the device shell could not mount the folder — "no Plan9 drive shares mounted")
- 21:34 CDT open (container UTC 2026-09-11 02:34; Central date 2026-09-10 — DT-1).
- HEAD = `357ca8e` = local `origin/master` tracking ref (`.git/refs/heads/master` = `.git/refs/remotes/origin/master` = `357ca8e77cfae34f256229814e7e588e0105d0b7`; FETCH_HEAD same, mtime 2026-09-09 12:23:39 CDT). A LOCAL read, not a QR-3 pass; no live ls-remote possible tonight.
- Reflog (old→new, Central): batch 94 → `d4c5e7d` 00:44:13 09-09; RC-4/RF-2 Code commit → `b2dc222` 01:27:20; batch 95 → `4be996c` 12:20:17; spec-feedback follow-on (Opus) → `357ca8e` 12:23:28.
- `inbox/` EMPTY (device_list_dir). No `.git/index.lock` in the `.git` listing.
- BUILD-STATE at HEAD: 99,915 B, 136 non-blank (150 raw) — 85 B under CAP-4. Head file 191,090 B (§1 nine entries, back to #151; §2 184 entries, design 90 / runner 73 / code 21; high-water #154). Live log 1,885,553 B. Register synced 450,196 B; closed 274,378 B.
- Knowledge meter (project_info): 1,628,404 / 2,000,000 = 81.4%; THIRTEEN docs (the #154 capture/handoff pair entered 2026-09-09 06:38 UTC). Q-CAP-5(a) does not fire.
- Instructions: v31 in force (live read of the instructions field via project_info).
- Michael at open: "Let's keep working. It's Thursday evening and we are only at 42% usage on Fable. Do what ever you suggest we do next and don't worry about tokens." (Fable usage meter 42%, his reading.)

## Picks at open (AskUserQuestion, ~21:50 CDT)
- Tonight: "FO ruling sitting now (Recommended)".
- Q-API-20: "After (Recommended)" — the split-store slice is drafted after the FO sitting and the post-CCS-1 walk, once the Azure facts (Q-API-18) are in hand.

## Rulings (one per exchange, his words verbatim)

## "Before the first decision" checks (sheet, line "Before the first decision")
- session-log-head.md read WHOLE (§1 nine entries, batch 95 runner line → #151); BUILD-STATE.md read WHOLE (150 lines) — both from staged copies at HEAD `357ca8e`.
- The SPEC is at its canonical path, 73,044 B by device_list_dir = the size batch 92 landed (its runner line: "firm-obligations-module-spec.md 73,044 B") — UNCHANGED. The sheet is 20,678 B = batch 92's 20,121 B + batch 93's one pointer line (present, read).
- CC-1 hands-on queue stated at the top of the session (SendUserMessage ~21:50 CDT), with the CC-1(c) unprompted call on the walk.
- SPEC §1 facts RE-READ AT HEAD `357ca8e` (staged `db/schema.sql` 91,426 B, `src/App.tsx` 5,979 B, `src/outlook/graph.ts` 5,558 B; named verification reads, nothing edited):
  1. `calendar_events.case_id uuid not null references cases (id) on delete cascade` — HOLDS.
  2. `src/App.tsx` routes: `/cases/:id/calendar` and no `/calendar`, no `/firm/*` — HOLDS (28 Route lines incl. the duplicated `/auth/callback`).
  3. `snooze` — not in the three staged files; the wider src/db/supabase negative CARRIED from `ea5675b` on the record's own statements that no src/db/supabase file was written by batches 92–95, the `b2dc222` commit or the `357ca8e` commit.
  4. No deadline engine — carried (BUILD-STATE at HEAD).
  5. `quickbooks|qbo|intuit` — not in the three staged files; the wider negative CARRIED as in 3. The research memo `qbo-integration-research-memo-2026-08-15.md` EXISTS (device_list_dir, 51,809 B).
  6. `toGraphEvent(ev: CalendarEvent, caseRec: CaseRecord)` at graph.ts:81; category `'MDBP Case'`; `MATTER_PROP_ID` carries `${caseRec.fileNumber}|${caseRec.id}|${ev.id}`; `OUTLOOK_CALENDAR_NAME` from `./config` — HOLDS.
  7. `glossary_terms.scope text not null default 'firm' check (scope in ('firm','case'))` at schema.sql:968 — HOLDS; `firm_obligation` occurs 0 times in schema.sql; 46 `create table`.
  8. `review_log.action` CHECK = ('suggested','confirmed','edited','rejected','created','generated') — HOLDS, no migration widens it (11 files in db/migrations/, the same eleven BUILD-STATE names).
  9. STORE_VERSION = 16 — carried from BUILD-STATE at HEAD (no src read for it).
- Register rows `Q-FO-0`–`Q-FO-11` and `Q-STAT-7` present in the synced register (batch 92/93 runner lines; confirmed by grep below when the register is read).
- The mock: claude.ai artifact "Firm Obligations Mock" (https://claude.ai/code/artifact/90fd6290-c9fb-4b2c-9cf5-ed90499ce450) — Michael opens it beside the chat.
- (Michael, after the DECISION 0 prose went out without a widget: "Are you running or did we get stuck here? Just checking. Let me know if you need anything. No rush at all." — not stuck; the question was re-put as a widget.)
- **DECISION 0 — `Q-FO-0` — THE WORD. RULED (~22:05 CDT): "Firm obligations (Recommended)".** → nav "Obligations" at `/firm/obligations`; card "Firm obligations"; tables `firm_obligations` / `firm_obligation_occurrences`; "firm deadline" stays `FC-7`'s.
- **DECISION 1 — `Q-FO-2`(a),(c) — THE CLOSES. RULED: "As drafted (Recommended)".** → two closes only (Done; Not-applicable only on `conditionalPerPeriod` rows, reason required); undo; no third "delegated" state; no snooze/later/dismiss/bulk. `FOD-18`, `FOD-19` stand; no `assignee` axis.
- **`FOM-11` — undo's test. ADOPTED: "Adopt (Recommended)".** → §4.2 drops "within the same period"; `FOD-7`'s "while the next occurrence is untouched" is the only test.
- **`FOM-12` — weekend/holiday due dates. COMPOSITE (his words, verbatim): "I am ruling with option 1, but with a caveat: If the deadline falls on the weekend and hence is due on the following business day, I want it to be made clear that the due date is the following business day, but also want strong encouragement that we satisfy and meet the deadline by the business day prior to the weekend/holiday."** → Option 1 adopted (never "overdue"/day-count on a weekend-dated occurrence as such); PLUS the caveat changes `FOD-1`: the roll IS shown as the due date where it applies, and the business day BEFORE the weekend/holiday is the TARGET, strongly encouraged. Re-asked narrower (how the roll is known per obligation; holidays) — see next lines.
  - **`FOM-12`(a) — how the roll is known. RULED: "Per-obligation setting, confirmed at activation (Recommended)".** → new field `weekendRule` on `FirmObligation` (rolls-forward | no-roll | unknown); each template carries a PROPOSED default marked UNVERIFIED (registry-backed under DECISION 8); confirmed/changed at activation; unknown = the `FOD-1` note + the target line. Every row shows rule date · DUE (rolled where it applies) · TARGET (last business day before the weekend/holiday, the strong line); lead window, card and Outlook reminder key off the TARGET; "target passed" (warning colour) after the target; "overdue" only after the due date.
  - **`FOM-12`(b) — holidays. RULED: "Weekends now; holidays flagged, not computed (Recommended)".** → the first slice computes weekends only; a rule date on a holiday shows the `FOD-1`-style note ("a holiday rule may apply; not computed"); a holiday calendar comes once, with `FC-9`'s, for both engines.
- **DECISION 2 — `Q-FO-2`(b) — A MISSED PERIOD. RULED: "As drafted (Recommended)".** → filings serial, cadences collapse, per-obligation override; `FOD-5` stands.
- **`FOM-1` — the collapse rule. ADOPTED: "Adopt both limbs (Recommended)".** → (1) a cadence completion closes the period it falls in; next = first rule date in a LATER period; (2) a dated kind under collapse never re-materializes its own date — next = first rule date after the later of its due date and the completion. `FOD-5`'s collapse wording and `materializeNext` change; interval-from-completion unaffected.
- **`FOM-4` — a period missed before activation. ADOPTED: "Adopt (Recommended)".** → first occurrence = first rule date on or after today, stated for the fixed kinds too; a serial obligation takes an OPTIONAL "last period completed" date at activation, and if given the first occurrence is the period after it, possibly already overdue. `FOD-9` unchanged.
- **`FOM-10` — backlog cleared one Done at a time. ADOPTED: "Adopt (Recommended)".** → one plain sentence added to §4.1.
- **DECISION 3 — `Q-FO-1` — STORAGE. CONFIRMED: "Two tables (Recommended)".** → `firm_obligations` + `firm_obligation_occurrences`, RLS/policy/GRANT/probe in the same commit, store v17 demo-side; `calendar_events` untouched. `FO-1`'s "new SCOPE, not a new type" read as the behaviour, not the table. No migration authorized by this answer.
- **DECISION 4 — `Q-FO-3` — `FO-5` OWNER SCOPE. RULED: "Owner-scope field now (Recommended)".** → `ownerScope` firm|attorney from the first migration (§7.1–§7.2 templates `attorney`, the rest `firm`); nothing on screen at solo; fan-out one occurrence per licensed user at multi-user; the assignee axis at multi-user (gate 2). `FO-5` CLOSES on this.
- **`FOM-15` — the owner column at solo. ADOPTED: "Adopt (Recommended)".** → §5.2's parenthetical becomes "only once more than one licensed user exists"; no owner badge at solo.
- **DECISION 5 — `Q-FO-4` — `FO-7`. RULED: "Thin, no hook; FO-7 closes (Recommended)".** → the research memo is not "spec'd" in `FO-3`'s sense; the limb does NOT fire; no `externalRef`; `doneNote` + `filedAt` are the only money-adjacent fields; a QuickBooks slice, if ever authorized, adds a nullable `ledgerRef` then. **`FO-7` CLOSES.**
- **DECISION 6 — `Q-FO-6` — WEIGHT. RULED: "Two classes; routine on the register only (Recommended)".** → `weight` hard|routine; the `/cases` card shows `hard` only; routine on the register; order and emphasis only, never behaviour. `FOD-11` stands.
- **`FOM-9` — order inside the Overdue pin. RULED: "Hard first, then most-overdue (Recommended)".** → §4.3 states it: hard above routine within the pin; inside each class, most days overdue first.
- **`FOM-13` — the card's horizon. ADOPTED: "Adopt the conjunctive reading (Recommended)".** → `FOD-14` reads: the card shows a `hard` item only once it is lit (or overdue) AND its TARGET is within 14 days; the card never shows what the register has not lit. [CORRECTED AT THE AUDIT: the built rule is hard AND (OVERDUE, or LIT with target within 14 days) — an overdue item is never dropped; the phrase in this line reproduces the option text he picked and is superseded by the rulings record §2 and the slice §2.2, which flag the wording for his eye.]
- **`FOM-14` — the register's horizon. ADOPTED: "Adopt (Recommended)".** → §4.3 reworded: the register shows every open occurrence; lit and overdue are the emphasis.
- **DECISION 7 — `Q-FO-5` — OUTLOOK. RULED: "All-day event, separate "MDBP Firm" calendar, reminder at the lead (Recommended)".** → the option text he picked: subject "Firm obligation: <name> (<period>)", the event on the TARGET date (per `FOM-12`), `isReminderOn` with the reminder at the lead window; no new consent (`Calendars.ReadWrite` suffices); `FOD-10` stands with the target-date change; the no-case `toGraphEvent` variant is a build item; keep-vs-delete on Done HELD for his Outlook (hands-on).
- **`FOM-8` — the long reminder. ADOPTED: "Adopt (Recommended)".** → a live-Graph check of `reminderMinutesBeforeStart` at the lead window (30–180 days, all-day event) is a NAMED build item; fallback = a second all-day "lights today" event on the lead day (= the `lightsOn` day) if the long reminder is not honoured. No To Do consent.
- **DECISION 8 — `Q-FO-7` — THE REGISTRY BOUNDARY. RULED: "Registry entries, new file (Recommended)".** → a FIFTH registry file `docs/specs/legal-rule-registry-firm-obligations.md`; drafts by a LATER Opus act (the `FC-13` drafting precedent; PF-1 fires on that packet); every entry UNVERIFIED until he verifies; the module's `sourceNote` points at the entry; `Q-STAT-5`'s practice-side pass folds in; the `FOM-12` roll defaults are entries in the same file. **The cases-vs-practice boundary is DECIDED BY ACT: the registry now covers the firm's PRACTICE too.** (Consequence for `Q-STAT-5`: the "fifth registry file" placement question has an answer — annotate add-only.)
- **DECISION 9 Part A — `Q-FO-9` — ACQUISITIONS AND CHANNELS. RULED: "All five; (4) and (5) as named channels (Recommended)".** → he acquires (1) the State Bar Rules + MCLE Regulations, (2) the TAJF IOLTA rules, (3) the FA zip by hand (permanently his hand); (4) TIDC-published county indigent-defense plans and (5) the U.S. Code via OLRC / govinfo.gov become the FIFTH and SIXTH SOURCING channels — **TRIGGER 3 FIRES: instructions v32 is owed tonight.** Which counties' lists he is on stays HIS FACT (H5).
- **DECISION 9 Part B — `Q-FO-8` — THE CATALOG. RULED: "Offer all 35; activate the core at go-live (Recommended)".** → all 35 `FOT-` templates OFFERED; his intended go-live activations: `FOT-1`, `FOT-2`, `FOT-6`, `FOT-19` (the #137 four), `FOT-4`, `FOT-8`, `FOT-9`, `FOT-22`–`FOT-24`, `FOT-25`, `FOT-27` — TWELVE rows (the ledger first wrote "thirteen"; corrected on a recount before the slice was written); everything else activated when its fact holds; every activation still takes his date in the product (`FOD-9`). Weights as drafted (no change named).
- **`FOM-2` — conditional columns. ADOPTED: "Adopt (Recommended)".** → the catalog splits "Applies if" (activation fact) from "Can lapse per period" (the flag); `conditionalPerPeriod` on eight rows: `FOT-8`, `FOT-10`, `FOT-11`, `FOT-13`, `FOT-14`, `FOT-15`, `FOT-16`, `FOT-17`.
- **`FOM-3` — beyond twelve months. ADOPTED: "Adopt (Recommended)".** → a "Later" group at the foot of the register; Done allowed there; the date shown in full.
- **`FOM-5` — where inactive rows live. ADOPTED: "Adopt (Recommended)".** → an Inactive section at the foot of the register, collapsed by default, Activate… per row (his date); never-added templates live only in the catalog.
- **`FOM-6` — month precision vs the lead. ADOPTED: "Adopt (Recommended)".** → a `precision: month` occurrence lights at the EARLIER of the 1st of its month and target − lead; `FOD-17` amended.
- **`FOM-7` — `periodLabel` for two kinds. ADOPTED: "Adopt (Recommended)".** → "by <due date>" for interval kinds; the due year for one-time.
- (All fifteen `FOM-` findings now put: 14 adopted as proposed, `FOM-12` adopted as a composite with two narrowings; none rejected.)
- **DECISION 10 — `Q-FO-10` — ROWS. RULED: "Mint all, and carry the three definitions (Recommended)".** → the packet mints register rows: `FO-1`, `FO-2`, `FO-3` born ✅ (closed register, their #137 ruling text); `FO-4` ✅ (the design pass — spec + sheet exist and DECISIONS 0–9 are ruled); `FO-5` ✅ on DECISION 4; `FO-7` ✅ on DECISION 5; `FO-6` ⬜ (firm-wide calendar page; hands-on queue); `BR-3` ⬜ pointing at `FOT-22`; and the definitions of `BR-2`, `BR-4`, `DA-2` (and `FO-4`, `FO-5`, `BR-3`) copied VERBATIM from the #137 Voice2 capture into an EVIDENCE record so the Voice2 pair becomes transit-eligible. The `FO-`/`BR-`/`DA-` ids are thereby MINTED by his act.
- **Decisions 0–10 and all fifteen findings CLOSED at ~22:40 CDT.** Next: the slice + kickoff prompt drafted, then put as its own authorization (the `CCS-1` pattern); instructions v32 (trigger 3: SOURCING channels 5 and 6); package.
- **`FOS-1` — THE SLICE AUTHORIZATION. RULED YES — Michael, verbatim, 2026-09-10 ~23:03 CDT: "Yes".** Put WHOLE, with the slice (49,838 B) and the kickoff prompt (15,629 B) delivered as files first; the option set, as the widget put it: "Yes" / "Yes, FOD-20 OUT" / "Break it down into limbs" / "Not tonight" (this line first wrote the second label as "Yes with FOD-20 OUT"; corrected from the widget text at the audit). The YES carries `FOD-20` IN and the §2.3 three-state synthesis of his `FOM-12` composite (named in the question text). One text, identical in the slice's §11, the queue row and the prompt.
- **THE FIVE §11 HANDS-ON ITEMS. RULED: "Accept all five (Recommended)".** → they join the CC-1 hands-on queue as ACCEPTED (no longer PROPOSED), gated on the FO build in demo mode, named THE FIRM-OBLIGATIONS HANDS-ON SITTING; Claude calls it when the build lands (CC-1(c)).
- **Rulings closed ~23:05 CDT. Everything after this line is autonomous work: the rulings record, the spec §16, the sheet's RULED line, the register acts, the #137 definitions carry, instructions v32, the entry, the manifest, the audit, the capture pair, the packet to `inbox/`.**

## Additional staged reads (after the pre-decision checks)
- For the slice: `src/outlook/config.ts` (852 B) staged and read whole — `OUTLOOK_CALENDAR_NAME` = `VITE_OUTLOOK_CALENDAR_NAME` || `'MDBP Cases'`; `outlookConfigured = Boolean(MSAL_CLIENT_ID)`. Also `db/migrations/2026-09-07-address-model-schema.sql` (6,487 B) and `2026-09-03-fe-d1-amendment-fix.sql` (12,001 B) for the migration shape and the catalog-lookup CHECK drop; `docs/record/transit-2026-09-08/project-instructions-v31-as-delivered-2026-09-08.md` (84,958 B, sha256 `070088d5…`) as the byte-exact base for v32; the CCS-1 slice, its prompt, the forward-sitting rulings record and the QUEUE-RUNNER text as shape precedents; both registers whole for the row acts.

## The audit (after the drafting, before the zip)
- Two read-only adversarial lanes (Opus subagents, told to REFUTE): **lane A — fidelity to this ledger: 20 findings, 2 HIGH / 6 MEDIUM / 12 LOW; lane B — HEAD facts and conventions: 15 findings, 2 HIGH / 6 MEDIUM / 7 LOW.** The four HIGHs, all real: (1) the closed register's ✅ baseline was written as 154 (the batch-95 TOTAL) when the closed file holds 143 — predictions corrected to closed 143 → 163, total 154 → 174; (2) the card horizon was stated two ways — §3 item 6 "overdue OR (lit AND target within 14 days)" against §2.2/§16/the rulings record "lit (or overdue) AND target within 14 days", which would drop a long-overdue item; conformed to the disjunctive reading everywhere, a test case added (hard overdue 40 days IS on the card), and the option text's wording noted for his eye; (3) v32's header said "additions have never counted as a trigger-4 firing" — false (the 2026-08-07 probate-index-set firing was an addition); reworded to the standing practice for TRANSIT files; (4) the §7 item 5 fixture used "Sat Jan 31 2027", which is a Sunday — fixture moved to Sat Jan 30 2027 and §2.3's example aligned. Every MEDIUM and LOW disposed too (the ID-anchored matcher named; config.ts added to the read lists; the manifest's misattributed quote; the DT-1 straddle claim removed — the sitting had not crossed midnight when the entry was drafted; the routes list completed to 28; build/lint figures sourced to the CCS-1 build entry; the reflog "last three" completed; the Q-STAT-5 heading located; the reconcile sentence's nested parentheticals; the BR-3 row's "spec'd at #151"; the v32 file's exact path; FOD-31's FOD-17 clause; `past-date-unknown` named as the fifth state; the FOM-8 fallback day standardized; the FO-2 quote taken from the carry; the ninth row attributed to the authorization act; the §6 date clause reworded; INS-1 separated from his DECISION 10 words; the audit record and CHECKSUMS.txt added to the packet). The audit record is filed as EVIDENCE.
- RE-SWEEP AFTER FIX: a third read-only auditor over every claim the fixes touched — its result is recorded in the audit record.

## Packaging (after the re-sweep; the Central clock read at this line's write)
- Knowledge meter at packaging: 1,628,404 / 2,000,000 = 81.4% — unchanged from open (`project_info`); instructions field still v31 by its header (his paste of v32 is ahead). The #155 pair's units are added after this ledger closes and are read at the next sitting.
- Deliverables to Michael: the slice and the kickoff prompt (sent as files before `FOS-1`); instructions v32 (sent as a file, and committed to the checkout's `Claude outputs\` as `project-instructions-v32-2026-09-10.md`); the packet zip into `inbox/` on `mdb-pllc` by `device_commit_files` (the VM shell could not mount the folder — no on-device sha256; the runner pins the identity at Step 1; the zip's size and sha256 are reported in the closing report).
- The #155 capture and handoff written to project knowledge with TRANSIT lines (`claude/claude_Firm_Obligations_Ruling_Sitting_Capture_2026-09-10.md`, `claude/claude_Handoff_Session_Log_2026-09-10.md`).
- Final sizes at packaging: the slice 52,382 B (sha256 `7e5ac4de…`); v32 89,785 B (sha256 `5660f091…`); every file's full hash in `CHECKSUMS.txt`.
