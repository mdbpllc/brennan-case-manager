CHAT-DISPATCH v5 — 2026-09-01 (Central) — THE OPUS CHAIN THAT RUNS BESIDE THE FABLE QUEUE-AUDIT SITTING

Canonical repo path WHEN FILED: `docs/prompts/CHAT-DISPATCH-v5.md` (the v3/v4 precedent — `docs/prompts/` is the dispatch home; Task 1's packet files it; until then this is a delivered prompt, not a repo document). This dispatch authorizes nothing on its own; the record at HEAD is the authority.

PASTE THIS WHOLE FILE as the first message of a FRESH Cowork chat in the "Project Management Software" (build) project, OPUS 5 selected, the chat linked to `mdb-pllc`. Generated 2026-09-01 (Central) by the typed Fable 5 design session running concurrently, using the `opus-dispatch` skill (v2) in DISPATCH-PACKET MODE. It is a chain of EXECUTION tasks — fold-ins, research, drafting, data prep, a relocation — every one of which the project's MODEL USAGE clause (§7.2) routes to Opus. It adjudicates nothing. No CODE-DISPATCH is issued today: the only Code acts on the table are the queue run, which Michael starts when he chooses, and the `FE-D1A-1` build session if he authorizes it (its prompt is already filed UNFIRED). No queue run is pending (`inbox/` holds no zip), so nothing precedes Task 1. No practice-project (CIVIL LIT / CRIM DEFENSE / PROBATE) or ARCHIVE-project task is in this chain — different projects; none is issued today. The build project's instructions (v28) are AUTHORITATIVE for every convention referenced here by ID; where this packet and the instructions disagree, the instructions win and you say so.

Your first output, before any task, is the standard typed-session opening: (1) the CC-1 HANDS-ON QUEUE stated at the top — at generation it is TWENTY-ONE accepted (`#139`) plus FOURTEEN proposed by the amendment slice (`AS-Q14`–`AS-Q17` among them; a YES to `FE-D1A-1` neither accepts nor declines them), a separate Fable sitting worth about an hour that this chain does NOT run; (2) the §7.2 self-identification: "This session runs on Opus 5 and EXECUTES. It adjudicates nothing. Everything rulable it produces is PROPOSED until Michael rules. It closes, annotates, merges or rewords NO queue row."; (3) the knowledge meter read directly (`knowledge_size` / `max_knowledge_size`; Q-CAP-5(a) is self-executing at ≥ 90%); (4) the assumptions block below re-verified against HEAD over the bridge.

0. ASSUMPTIONS AT GENERATION (re-verify; a failed assumption STOPS the affected task, not the chain)

* HEAD believed `7f02131f215605b3062a8351c2b7ec96f46c004c` = `origin/master`, verified 2026-09-01 ~10:55 CDT by `git ls-remote origin refs/heads/master` over the bridge (queue-runner batch 85; the FE-D1 amendment slice and its UNFIRED kickoff prompt at HEAD). Instructions v28 in force (read live in the field). `inbox/` on `mdb-pllc` holds NO zips — only the loose staging folder `inbox/push-to-code_fe-d1-amendment-slice_2026-08-31/` (Michael's hand; ignore it; never delete it). No `.git/index.lock`. Two untracked files in `docs/specs/` — `attorney-review-queue-audit-2026-08-24.md` and `id-collision-report.md` — DO NOTHING by Michael's ruling (batch 83); never stage, file, move or delete them. Knowledge meter at the generating session's open: 1,658,739 / 2,000,000 = 82.9%.
* `FE-D1A-1` is OPEN (nothing in this chain authorizes a build); `MIG-1` unrun (his hand); `H12-v` unruled (his; the BAA a hard gate); R11 gated (no TRCP 195.2 deadline is computed, displayed or proposed by anything in this chain); `RF-2` his hand-in (H5 bars a machine search).
* If HEAD has moved when you read it (a batch landed), every task's own GATE below tells you what to re-check. A task whose premise is gone is SKIPPED and REPORTED, never forced.
* Bridge: request `C:\Users\Brennan\brennan-case-manager` at session start (HEAD reads; committing zips into `inbox/`). Request `Documents\Knowledge Repo` only when Task 3 begins. The folder dialog times out silently if Michael is away — keep working on what does not need it and re-ask in conversation, never retry blind. Read the checkout lock-free (`GIT_OPTIONAL_LOCKS=0`; plain file reads); a bare bridge-side `git status` strands a 0-byte `.git/index.lock` and reports a false-dirty CRLF wall (LE-1) — if you need status, `git -c core.autocrlf=true status`. A bridge read establishes the WORKING TREE, never origin: origin is `git ls-remote`. Never read `src/` (working-set policy; `Q-PR3-1` unruled).

1. THE FENCE — what the concurrent Fable session is doing, and what this chain therefore never touches

A typed Fable 5 design session is running TODAY, in parallel, on the same repo and the same `inbox/`: the adversarial verify pass over the review-queue audits that the record says never ran (the 2026-08-24 audit's own §9, read at HEAD in the working tree: "The adversarial verify pass that would catch this did not run — the Fable allowance was exhausted at the fleet's return."; `#138` ran only the evidentiary half). It reads `docs/specs/attorney-review-queue.md` whole, the two untracked 2026-08-24 files in the working tree, `attorney-review-queue-audit-HARDENED-2026-08-25.md`, the live log and the closed archive, and produces `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01.md` (PROPOSED — a ruling-ready disposition per candidate row) shipped as `push-to-code_queue-audit-adversarial-pass_2026-09-01.zip`, and — only after Michael rules, row by row — a later packet of queue-row closure work orders. It also owns the queue file's Convention-line repair (three status markers, two documented) as a PUT item.

Therefore this chain, in every packet it ships:

1. Makes NO work order against any EXISTING row of `docs/specs/attorney-review-queue.md` — never closes, annotates, merges, renumbers or rewords one. The only queue changes a chain packet may cause are (a) the NEW rows the RUNNER mints from the packet's own open-item list, each with its FULL question text (QR-1), and (b) the runner's Status-header reconcile sentence (QR-6(b)) — both the runner's acts, not yours.
2. Adjudicates no queue row and no audit candidate. If a task's reading suggests a row is stale or closable, that observation goes in the packet's §7 open items as a NOTE addressed to the Fable session's sheet — nothing more.
3. Proposes no edit to `BUILD-STATE.md` text beyond the manifest's standard "what BUILD-STATE says after this batch" paragraph; the runner rewrites that file.
4. Never touches the Fable session's outputs if they appear in `inbox/` or at HEAD mid-chain — do not open the zip, do not reconcile it away, do not run the queue (Cowork cannot; Code does, when Michael says).
5. Ships into the same `inbox/` under distinct filenames — every zip of this chain is `push-to-code_<slug>_2026-09-01.zip` with a slug that names the task (below). Two design sessions writing different zips to one inbox is not an MM-1 problem (MM-1 governs RUNNER sessions); the runner will order the batch by filename date then mtime (QR-4). Each packet's §3 entry says "expected next design ordinal at HEAD — MEASURED by the runner, never assumed; a concurrent session's packet may land in the same batch."
6. Names its captures distinctly: every `claude_` file this chain writes to project knowledge carries `Opus_Chain` in the filename.
7. Relocates or deletes NOTHING from project knowledge except in Task 8, and there only after Michael confirms step 2 (see the task).

2. COMMON RULES (stated once; they apply to every task)

* Session-start reads, in full, over the bridge at HEAD (preferred) or from synced knowledge: `docs/specs/session-log-head.md` (derived, capped; cite by heading or quoted sentence, never by line — CITE-STABILITY), `docs/specs/BUILD-STATE.md` (150 non-blank lines, wholesale-rewritten — same cite rule). Anything the head file lacks is a bridge read of `docs/record/session-log.md` or `docs/archive/session-log-archive-2026-07-21_2026-08-12.md` away; RAG absence is never evidence of absence.
* RECONCILE FIRST against BUILD-STATE and the log before staging anything. RR-1 before every zip: re-read everything authored earlier in the session against everything Michael said later in it. RE-SWEEP AFTER FIX: every fix pass gets its own normalized re-sweep before the zip closes. PF-1: where a packet carries a legal characterization or a proposed registry entry, run the adversarial multi-agent READ-ONLY preflight against PRIMARY SOURCES before the zip enters `inbox/` (Tasks 2 and 3 fire it; Task 4 fires it on the intervals it carries; every other packet RECORDS that PF-1 did not fire and why — the QR-6(f) shape; the skip is never silent). A fleet that returns zero findings and zero tokens is a dead fleet, not a clean bill (`#104`): confirm every agent actually returned content. PF-1 blocks SHIPPING, not drafting — a packet that cannot be preflighted yet is HELD in the container and shipped when it can be.
* PROPOSED until ruled. Options are a way of asking, never a menu (CC-1(a)); put questions one at a time, in prose, with a recommendation stated and the alternatives named; never record a nod as a closure. Hands-on questions are HELD to the named queue, not pressed (CC-1(b)). Nothing enters the build queue without Michael's explicit ruling.
* DT-1: every date you stamp — entry dates, "Entered" lines, filename dates on packets and captures — is Michael's Central wall-clock date. The container runs UTC and rolls to 2026-09-02 at 19:00 CDT; past that hour, check the Central date before stamping anything.
* No real client data, credentials, or keys in any artifact, packet, prompt or chat text. H5: never sweep Michael's machine; read only paths this packet or the instructions name; a directory listing is itself an exposure. The one sanctioned listing is the top level of `Documents\Knowledge Repo\` (the SOURCING authority corpus) to locate a rules PDF by its own name.
* SOURCING and TOOLING: primary law from named official sources, each source named per item — Texas rules from the clean-authority PDFs in `Documents\Knowledge Repo\` cited by the PDF's own effective date; federal regulations from the eCFR API (`ecfr.gov`, no key) named "eCFR via official API" with the request date and the title's `up_to_date_as_of`; case law via FLP/CourtListener and named public sources only — Descrybe is OUT by ruling. Registry-style drafting for every legal proposition: proposition · cite · UNVERIFIED · source named; retrieval is not verification; ONLY Michael verifies. The `A`-for-space normalizer at `statute-pass-registry-retrieval-2026-08-14.md` §3 is for the Legislature's statute PDFs ONLY — never run it on the TRCP/TRE/TRAP PDFs (different publisher; raw `AA` count 0). ROUTE-C is standing: a divergence that changes what an entry means gets proposed conforming wording queued for adopt/reject/edit, each entry put independently, never as a package; a cite change is its own flagged act; registry Rule lines quote operative text VERBATIM where practicable (the `#108` drafting direction).
* Web: WebFetch/WebSearch only. A page that cannot be fetched is recorded as NOT FETCHED with its URL — never approximated from search snippets as if read, never retrieved by shell or script.
* Packets, via the `push-to-code` skill: one zip per completed repo-bound task (a single-file packet still ships as a zip — the runner collects zips; Q-1 wants each packet in `inbox/` as it is produced, and a context reset must not strand landed-ready work). The manifest opens with RECONCILE FIRST; every staged document's status line names its canonical repo path; exact-string work orders assert each OLD string unique BY PROGRAM at HEAD and predict their `numstat`; REPLACE-WHOLE rows name the byte-level invariants the runner must STOP on; the §3 session-log entry is in house format (a correction, if any, carries the ruled fields: what was asserted · what is true · which entry · actor "Opus 5" · failure class · what changed); the DO-NOT list is explicit; open questions carry FULL text with packet-local IDs that receive durable IDs only at reconciliation. Build the zip in the container and commit ONLY the zip into `inbox/` by `device_commit_files` — never loose files (the `#140` residue is the exhibit). Record each zip's byte size and sha256 in your closing report. The chain never runs the queue.
* Captures: at every packaging point and at the end, run `refresh-chat` — the granular raw capture and the combined handoff go to project knowledge as `claude_…Opus_Chain…` files, each carrying the TRANSIT line beneath its status line, reference only, never routed to the repo.
* Closing report shape (every session of this chain): tasks done / skipped and why · every zip with bytes + sha256 · anything HELD unshipped and what unblocks it · any `.git/index.lock` you stranded or scratch you left in a connected folder, by exact path · flags for Michael's ruling · knowledge meter before/after if Task 8 ran · and, if unfinished, the line "RESUME AT TASK n — paste CHAT-DISPATCH v5 in a fresh chat with that line."

3. HOW THE CHAIN RUNS

Order is fixed and is by leverage: T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8. T8 is INTERACTIVE and runs last, only with Michael present and saying go. Chaining works only within a session — a finished session cannot start the next, Michael's paste is the only trigger, and a fresh chat cannot see prior output until it has LANDED at HEAD and synced (or until it reads the zip's existence in `inbox/` over the bridge). Say this plainly if you have to break.

Per task: finish it, RR-1 it, ship its zip, write a ONE-LINE checkpoint, and CONTINUE without waiting — except STOP and ask on a failed assumption, a record ambiguity at a gate, anything client-data- or credential-shaped, or before T8.

CONTEXT BUDGET rule: before each task, judge whether the task PLUS a packaging pass fits your remaining context. If not: RR-1 over everything staged, ship what is complete, refresh-chat, and end with the RESUME line. On RESUME: fresh session-start reads; determine for T1–T7 which outputs have (a) LANDED at HEAD, (b) SHIPPED as a zip still in `inbox/` — counts as done, do not redo — or (c) neither; resume at the first (c). No two tasks here form an ATOMIC pair (each packet is self-contained); T2's memo and its entry drafts ride ONE zip, as do T3's retrievals and its wording proposals.

TASK 1 — THE REQ-CAPTURE's THIRD FOLD-IN (the record's own words: "OWED and an Opus design session's act")

Slug: `disclosures-third-fold-in`. GATE: at HEAD, `docs/specs/REQ-CAPTURE_disclosures-expert-designation_2026-08-20.md` §18.G still carries the blockquote beginning "POINTER, ADDED 2026-08-31 (late sitting) — NOT A FOLD-IN" and its face reads "Second edition, 2026-08-31." If either is gone, someone folded it — SKIP and report. Sources at HEAD: `docs/specs/fe-d1-amendment-slice.md` §2 (the EIGHTEEN rulings, `AS-Q1`–`AS-Q13b`, Michael's selections quoted with what each answered — THE governing record) and §§3–4, 6–9, 15 where §2 points; the `#140` entry in the head file §1 (its NAMED DIVERGENCES paragraph is your work list for `form-engine.md`); the REQ-CAPTURE second edition; `docs/specs/form-engine.md` as edited at batches 84–85; `docs/specs/contact-directory.md` §5. Reference only, never the record: the project-knowledge capture `claude_Disclosures_Amendment_Slice_Rulings_Capture_2026-08-31_Late.md` — where it and the filed record disagree, the record governs and you flag the disagreement. Do: (a) REQ-CAPTURE THIRD EDITION — fold the eighteen rulings into §§11–§18 REWRITTEN, NOT ANNOTATED (the `#135`/`#138` pattern): no superseded text reproduced beside its replacement; no parallel disclosures spec created; the supersession ledger on the file's face extended with every rewritten section and what rewrote it; the §18.G POINTER blockquote replaced by the fold (it is spent) and §18.G's "still Michael's" list refreshed (`FE-D1A-1` OPEN joins it; §3's sixteen IDs, `RF-2`, `MIG-1`, `H12-v`, `HD-3`, B6–B8, `RC-4`, `CD-14`(i), `RF-5`, the TRCP 195.2 verification, Q6 stay); `AS-Q14`–`AS-Q17` and the ten default-shaped items enter §18.F as PROPOSED for the hands-on queue, NOT accepted — the accepted count stays twenty-one; the six IDs (FE-18–FE-22, `CD-14`) stay UNMINTED — they mint only with the authorization. Orientation map, the slice §2 governs where it differs: AS-Q1 → §16.1/§16.3 (server-side function + one vendor-neutral secret; fixture writer; nothing about a vendor); AS-Q2 → §17.1a ("and nothing else" is a named wording defect — the contract is names, credentials, dates, visit rows, per-individual summary; never type, role or facility membership); AS-Q3 → §17.1/§17.7 (`R17` CASE-SCOPED; promote by hand; TYPE per case pre-filled from last time; the model never creates a contact or a link); AS-Q4 → the chronology section (extracted text per version; bytes not retained; file store at gate 7); AS-Q5 → the type vocabulary (FIFTEEN; mental health = §5.1 pause and NO generated paragraph, a deliberate gap kept as a TYPE; other physician; other licensed non-physician, writer-only, panel-flagged); AS-Q6 → §16.5; AS-Q7a/b/c and AS-Q8a/b/c → §11.6–§11.8 and §17.6 (§9.11 placed WHOLE for custodian-only; four limbs for the pharmacy too; Part 3's "And/or Custodian(s) of Records" count rule — "Custodian of Records" alone when nobody is named, "Pharmacist(s) and/or Custodian of Records" for a pharmacy; §9.1's short form for all six; the bold-name LEAD before slot 1; the rider's fixed sentence approved as shown); AS-Q9 → the interview cards retired for treating facilities, the never-assert-board-certification writer instruction; AS-Q10 → one instrument per plaintiff, provider list and chronology per client; AS-Q11 → §17.3 and the migration paragraph (`facility_party_id` ×3, `facility_billing_profiles`, "Facility"; `effective_from`/`effective_to` on the edge; ONE amendment migration hard-gated on `MIG-1`); AS-Q12 → §2 step 7 stated UNVERIFIED with no date computed, the FE-1 ledger pointer, the FE-D1 prompt SPENT, writer instructions as versioned template data, the §12.7 imaging question as a panel line; AS-Q13a/b → §18's IN/OUT and the minting rule. Preserve the DO-NOT-ATTRIBUTE discipline absolutely: the option texts are Claude's; only the selection words are Michael's; every place a Claude option sits beside a ruling carries a `MARKED` guard — the file has ELEVEN at HEAD and the third edition has at least eleven, never fewer. INVARIANTS the runner STOPs on (state them in the routing row): the `## PARTS 1–8 — MICHAEL'S CAPTURE, VERBATIM` region (inclusive) to `# RECONCILIATION ADDENDUM` (exclusive) is 21,838 B / sha256 `eaeec76bc0593c86568677d0c6f7bbe69a8d502d072886ea68224d893ec51f4f` before AND after (measure both sides by program, exactly as batch 85 did); the `Canonical repo path` header unchanged; the filename's `2026-08-20` capture stamp unchanged; the face reads "Third edition, 2026-09-01"; `MARKED` count ≥ 11. (b) `form-engine.md` conforming edits, exact-string, for the NAMED DIVERGENCES `#140` lists: §10's "The model populates NAMES and nothing else" and its twelve-type list (AS-Q2, AS-Q5); and the DESCRIPTION of the block's custodian line wherever a spec still states the pre-AS-Q7c form (`#140` says "§3" without naming the file — locate it by READING the REQ-CAPTURE and `form-engine.md`, not by grep; at HEAD `form-engine.md` §3 carries no such literal). Two things are never edited here: the block TOKEN itself — Part 3's "CONTACT BLOCK" is Michael's verbatim and `#139` records the token untouched; AS-Q7c is a RENDERING rule (the count rule; "Custodian of Records" alone when nobody is named; "Pharmacist(s) and/or Custodian of Records" for a pharmacy) and the fold conforms the specs' description of that rule, not his text — and `form-engine.md` §9, the APPROVED VARIANT LIBRARY, marked "verbatim — do not rewrite" (the pharmacy variant's "pharmacist(s) and/or Custodian of Records" at §9.10 is his approved wording and already agrees with AS-Q7c; the one §9 edit ever made, §9.4, was his own 2026-08-31 ruling). For the block-reads-the-selection divergence (`#140`: "§3" and FE-18's RATIFIED text speak of "the facility at the far end of a `renders-care-at` affiliation edge"; AS-Q3 puts the block on the SELECTED facility; the slice reconciles it as D-8 — the block always reads the selection, a disagreeing edge is a panel line, never a substitution): conform the DESCRIPTION wherever it sits (same locate-by-reading rule), but do NOT edit FE-18's ratified sentence — ratified wording is Michael's; give it a dated conformance note quoting D-8 and PUT the question (does he re-ratify FE-18 to the selection reading?) as an open item with full text. Read each target file's line-ending convention from the bytes first (`git ls-files --eol`: `form-engine.md` is `w/mixed`) and preserve it per replaced line. (c) `contact-directory.md` §5 — read it; conform to AS-Q11's frozen `renders-care-at` period (`effective_from`/`effective_to`) only if it lacks it. (d) Routing row for THIS DISPATCH: file this packet verbatim at `docs/prompts/CHAT-DISPATCH-v5.md` (the v3/v4 precedent) so the chain's existence is in the record. (e) Verification of your own output: RR-1; a second reader running a "build session's read" over the folded file (the `#140` practice — SILENT-DECISION / INTERNAL-COLLISION / UNNAMED-DISAGREEMENT / UNBUILDABLE), fixes, then RE-SWEEP. PF-1 does not fire for this task unless you write a legal characterization not already in the record verbatim — record the skip and why. Ship: the REQ-CAPTURE (REPLACE WHOLE, invariants as STOPs) · the `form-engine.md` / `contact-directory.md` work orders · `docs/prompts/CHAT-DISPATCH-v5.md` (NEW) · the §3 entry · open items with full text (the FE-18 re-ratification question; any seam the fold surfaces, the `#138` §18.B pattern — surfaced, never minted).

TASK 2 — `H12-v` VENDOR-ROUTE RESEARCH MEMO — the row's own words: "All vendor facts above are SEARCH-DERIVED and were NOT verified against the vendors' own pages — no page was fetched"

Slug: `h12-v-vendor-route-research`. GATE: the `H12-v` row in the queue is still ⬜ OPEN at HEAD and no file at HEAD named `h12-v-*` exists. If Michael has ruled the vendor, SKIP. Do: read the `H12-v` row in full (it begins "THE DISCLOSURES MODEL-CALL VENDOR ROUTE, AND THE BAA THAT GATES IT") and REQ-CAPTURE §16.1–§16.3. Then, from each vendor's OWN pages, fetched today and quoted: AWS Bedrock (BAA via AWS Artifact — self-service or not; Anthropic Claude models served; per-token pricing model; any eligibility statement bearing on a solo firm); OpenAI API (BAA request path; the zero-data-retention-eligible-endpoints condition the row records as a build constraint — confirm or correct from the page); Anthropic direct (current BAA terms — Michael REJECTED the Enterprise route on cost, "that defeats the whole purpose"; record current terms only, do not re-argue); Google Cloud Vertex AI (Claude on Vertex; BAA path); Microsoft Foundry / Azure (whether Anthropic models are offered and under what BAA); the local-model option on the P1 (no BAA question, no per-token cost; capability for this task UNTESTED — say so; note the telemetry-lockdown posture item is NOT SET per BUILD-STATE). Per vendor: URL · fetch date (Central) · the operative sentence quoted · what it establishes and does NOT · NOT FETCHED where that is the truth. Then the two things only Michael can answer, prepared not answered: a question list for the malpractice carrier (AI-assisted drafting over client medical records — coverage position, notice, any exclusion language to ask about) and the AWS-for-a-solo-firm question stated with whatever the page does or does not say. Do NOT estimate token cost per matter — offered and DECLINED at `#130`; it stays unestimated. Do NOT re-propose the stripped-chronology approach — REJECTED. The HIPAA propositions the `#130` sitting stated as background — the eighteen-identifier set, dates of service among them, safe harbor and expert determination as the two de-identification routes — are UNVERIFIED legal propositions never routed to the registry: retrieve 45 CFR § 164.514(a)–(b) via the eCFR API (targeted section requests; record the request date and Title 45's `up_to_date_as_of`), and draft registry-candidate entries — proposition · cite · UNVERIFIED · "eCFR via official API" — in a SEPARATE drafts file, DRAFTED AND NOT INSERTED, no registry file touched. PF-1 FIRES: preflight every legal claim in both documents against the eCFR text and every vendor claim against the fetched page before the zip closes. Ship: `docs/specs/h12-v-vendor-route-research-memo-2026-09-01.md` (PROPOSED — RESEARCH ONLY; decides nothing; no credential, no account, no sign-up) · `docs/specs/hipaa-deidentification-entry-drafts-2026-09-01.md` (DRAFTED, NOT INSERTED) · the §3 entry · open items with full text (where the drafts would insert, if at all — the `G10-4`/`Q-WF-6` siblings' placement rule points at a NEW registry file, not a fold-in; that is Michael's).

TASK 3 — TRCP RETRIEVAL + ROUTE-C WORDING PROPOSALS for the divergences the record has already named (needs the `Documents\Knowledge Repo` grant)

Slug: `trcp-divergence-wording-proposals`. GATE: `Q-FE5-9` still ⬜ at HEAD; `docs/specs/deadline-engine-spec.md` still carries `P-2` reading "added days attach to the method used on the individual party, and only to mail" with its own caveat beneath it that the July 2026 text still carries the commercial-delivery limb, and its closing line that `P-2` "remains UNVERIFIED and unentered in any registry file." If the Knowledge Repo folder is not granted, HOLD this task (do not substitute a web copy — SOURCING names the PDF) and move on; return to it if the grant arrives. Do: list only the top level of `Documents\Knowledge Repo\` to find the TRCP PDF by its own name; extract with `pdftotext` in the device VM (no staging; no scratch in a connected folder — work in the VM's own `/tmp`); cite the PDF's own effective date. Retrieve VERBATIM, spot-checked against raw extraction: TRCP 190.3(b)(3) (the `Q-FE5-9` divergence — the VERIFIED entry says "each party," the rule says "any other party"; 25 versus 25 × N); 190.4(b)(2) (the phase-scoped Level 3 period the `#75` memo names); 21a in full, with 21a(c)'s method list (the deadline-engine spec's `P-2` doubt — "by mail or by commercial delivery service"); the discovery-response extension text the `#75` memo reads as FAMILY-CODE-ONLY against `trcp-deadline-skeleton-2026-03-01.md` §5 — present both readings side by side, PROPOSE NOTHING for the skeleton (a spec, not a registry entry; "resolving it is your verification act"). EXCLUDE TRCP 195.2 entirely — R11 is gated, its two register rows exist, and nothing about a 195.2 deadline is retrieved for, computed, displayed or proposed here. Then, ROUTE-C, one entry at a time, never as a package: for `Q-FE5-9` draft the conforming wording for the TRCP 190.3(b)(3) entry quoting the operative text verbatim, state that adoption keeps the entry UNVERIFIED until Michael re-verifies the new text (verification attaches to wording, `#95`), and state FE-5's consequence (an engine built to the entry's wording refuses discovery the rule permits); for `P-2` draft a conforming note for the deadline-engine spec; for the "no TRCP 21a entry exists anywhere" gap draft a NEW-ENTRY candidate (proposition · cite · UNVERIFIED · source named) DRAFTED, NOT INSERTED. No registry file is touched (`legal-rule-registry-discovery-and-carrier-duties.md` is barred in terms). PF-1 FIRES — preflight every quotation and every characterization against the PDF text. Ship: `docs/specs/trcp-divergence-wording-proposals-2026-09-01.md` (PROPOSED; each proposal put independently) · the §3 entry · open items with full text (each adopt/reject/edit as its own item; the 190.3(b)(3) cite is unchanged so no cite act rides).

TASK 4 — THE HEARTBEAT FOLD: captures e and f into `case-heartbeat-design.md` §8 (FOLD PENDING since 2026-07-25; BUILD-STATE carries it every batch)

Slug: `heartbeat-fold-captures-e-f`. GATE: `docs/specs/case-heartbeat-design.md` §8.12 still reads that captures e and f are "NOT yet folded — §8.12 lags two captures", and `docs/specs/case-heartbeat-walkthrough-capture-2026-07-25e.md` and `-f.md` exist at HEAD (they do at `7f02131`: 37,422 B and 38,075 B). If §8.12 no longer says so, SKIP. Do: read the design doc whole (75,878 B) and both captures whole; read `apil-2025-course-book-mining-pass1/2/3.md` only where §8.12 says H59–H83 were minted there. Fold the narrative the fold queue itself lists — capture e: the default-judgment thread, the post-judgment appellate clocks, the no-answer fork (H32), the discovery phase, the deposition timing model; capture f: the deposition no-dates ladder end to end, mediation in full (arming, cadence, roster, payment chain, post-mediation fan-out), the DCO ingester, the software-wide audit log, the expert-disclosure cadence — as new §8 subsections in the doc's own voice and provenance marks ([C] for anything captured from Michael's walkthrough, with the capture and Part cited; [D] only where the doc's own conventions make a Claude design step [D]; [H] for anything the captures leave unruled — decide NOTHING). Rewrite §8.12 (folded 2026-09-01; still unwalked: trial prep · trial · settled incl. disbursement/liens · closed; the APIL pass-4 resume point preserved verbatim) and the status line's "lags two captures" clause; touch the §11 register ONLY where a capture RULED a row and the row does not yet say so, quoting the capture — mint no H-number; H1–H83 are never renamed. Every legally consequential interval the fold carries (the Rule 99(b) answer date, appellate clocks, default-judgment timing, anything with a rule cite) is marked [C] with its capture cite AND "UNVERIFIED — registry entry required, none exists" (§7.2's own rule: intervals are registry entries, not constants); no registry entry is drafted here. PF-1 FIRES on those intervals: preflight them against the TRCP/TRAP PDFs in `Documents\Knowledge Repo\` — which is why this task runs after Task 3's grant; if the grant is absent, HOLD the packet unshipped and continue. Invariants (REPLACE WHOLE; the runner STOPs on them): §§1–7 and §§9–12 byte-identical to HEAD except the named §11 register rows; the header's `Status:` clause and `Date:` line change only as named here — extended, not replaced; the §11 register's row COUNT unchanged. Ship: `docs/specs/case-heartbeat-design.md` (REPLACE WHOLE) · the §3 entry · open items (any [H] the fold surfaces, full text; the registry-entry-required list).

TASK 5 — `SKILL.md` v3 DRAFT for `docs/skills/drafting-disclosures/` (on Michael's open list since `#139`; the LIVE drafting path)

Slug: `drafting-disclosures-skill-v3`. GATE: the file at HEAD still reads `STATUS: v2, 2026-08-11`. If it reads v3, SKIP. Do: read the skill (11,058 B) and its own UPGRADE PROTOCOL — learnings fold into the spec FIRST, then the skill revises citing the spec at HEAD, the version line bumps, and the claude.ai uploaded copy is re-uploaded by Michael's hand (so v2 stays the live path until he uploads v3 — say so on the file's face). Then conform the skill to what is AT HEAD `7f02131` — cite only documents at HEAD: `form-engine.md` §9 as edited at batch 84 (the fixed BASIS and CAUSATION sentences keyed by PROVIDER TYPE; §9.4 chiropractic → medical probability; §9.11 the custodian's four limbs; §9.12's rider sentence) and §10 (`R17`, the typed designee slot, the fixed-sentence table); REQ-CAPTURE §11.6–§11.8 (the paragraph schema: LEAD → writer opening → fixed BASIS → writer middle → fixed CAUSATION last; the custodian sentence the writer's, all four limbs required; plural forms for a same-type group; the radiologist the only split; DC+PT combined — explain each, then pair them); the ND rules (ND-1 individuals are designated, never facilities; ND-2 the facility only as affiliation; ND-3 no two providers share one stock paragraph; ND-4–ND-9 as they stand in the REQ-CAPTURE and as ruled at `#139`, ND-7(c) HELD); the event noun (collision only for motor-vehicle collisions; "incident" for everything else); the mid-level rider (Example C: writer opening → the fixed scope sentence); AS-Q9's never-assert-board-certification instruction and AS-Q10's one-instrument-per-plaintiff rule as recorded in the slice §2. The skill operationalizes; it does not restate rationale, and it makes no legal characterization of its own — every rule sentence is a pointer to the spec section that holds it. Michael's held items stay held in the skill too (the stop's shape, ND-7(c), Q5, Q10, the rider's supervisor beneath a collective paragraph — the skill says "hold; ask Michael" where the spec holds). PF-1 does not fire (no new legal characterization; record the skip and why). Two readers: RR-1, and a "drafter's read" that tries to draft one fictional provider paragraph from the skill alone and reports every place it had to guess. Ship: `docs/skills/drafting-disclosures/SKILL.md` v3 (REPLACE WHOLE; v2 preserved in git history; the face names the re-upload as Michael's hand item) · the §3 entry · open items (every guess the drafter's read surfaced, full text).

TASK 6 — `TC-OPEN-2`'s FIRST QUESTION: a proposal for the ruled home of a RETIRED spec (the `#138` survey: "there is NO RULED HOME for a retired spec")

Slug: `retired-spec-home-proposal`. GATE: `TC-OPEN-2` still ⬜ at HEAD and no ruling on a retired-spec home appears in the log above `#138`. Do NOT touch `TC-OPEN-1` / `TC-1` (the two deferred growers) — deferred by ruling; reopening is a ruling, not a task. Do: read `docs/specs/superseded-specs-candidates-2026-08-25.md` (22,344 B) and the thin-constitution rulings (`docs/specs/thin-constitution-restructure-2026-08-21.md`; TC-4/TC-6/TC-8 in the instructions). Draft the OPTIONS as a way of asking, one recommendation stated: e.g., a `docs/retired/` directory that is repo-only and sync-excluded on `docs/record/`'s rule (live-but-unsynced), versus retirement-in-place by banner, versus the ARCHIVE project — each with what it costs the meter, what it does to `git log` continuity, how a retired file is cited afterwards (CITE-STABILITY), and how the survey's five clean retire-candidates and six superseded-in-place would move under each; the nine repo-resident RAW CAPTURES' category question (does TC-8 reach captures already in the repo?) put as its own question. Move NO file. Note the operational corollary from the TC-4 execution: if a new directory must be EXCLUDED from the sync, Code pushes first and the picker exclusion happens second. Ship: `docs/specs/tc-open-2-retired-spec-home-proposal-2026-09-01.md` (PROPOSED) · the §3 entry · open items (the home question; the captures-in-repo question; full text, packet-local IDs). PF-1 does not fire (no legal content; record the skip).

TASK 7 — THE HANDS-ON SITTING SCRIPT (data prep for the Fable sitting; KNOWLEDGE-BOUND, never the repo, no packet)

GATE: the sitting has not run — no log entry above `#140` records the hands-on queue being taken. Do: for each of the twenty-one accepted items — `#137`'s four (`DA-1`, `DA-3`, `DA-4`, `FO-6`); the 2026-08-24 audit's twelve (`CL2-AC-1`, `CL2-CHECK-1`, `FE-§11.4`, the bill-label pre-fill, `CR-7`, `CR-CONSTRAINT`, `Q-FE6-5`, `Q-FE4-1`, `Q-FE5-3`, `Q-IN2-7`, `Q-IN1-1`, `Q-IN3-6`); the RC-1 sitting's five (the hard stop's SHAPE, `ND-7(c)`, Q5, Q10, the PA rider's supervisor beneath a collective paragraph) — and the fourteen PROPOSED at the slice's §15 (`AS-Q14`–`AS-Q17` and the ten default-shaped items): one block carrying the FULL question text as it stands in its home (REQ-CAPTURE §18.F, the slice §15, `#137`, the queue rows — read, never paraphrased), the screen or route where it is best answered (BUILD-STATE's "Screens live" and the case-detail tab table), the build default in force meanwhile, the ruling options as a way of asking, and — wherever document structure is involved — a RENDERED FICTIONAL EXAMPLE (Michael's stated preference: examples before ruling on structure). Order the blocks by SCREEN so the sitting walks the product once. Read the queue rows and audit items READ-ONLY; classify nothing. Deliver: `claude_Hands_On_Sitting_Script_Opus_Chain_2026-09-01.md` into project knowledge — a LIVE WORKING DOCUMENT (not a capture; TC-8 says it stays until spent), spent when the sitting runs; also delivered as a file in chat. No repo path; no packet.

TASK 8 — TRANSIT STEP 1 for the eligible `claude_` captures — INTERACTIVE TAIL: only if Michael is present and says go; the chain is complete without it

Read first: `claude/claude_TRANSIT_Verification_Worknote_2026-09-01.md` in project knowledge — the two-condition table read at `7f02131`, sixteen files ELIGIBLE (three subject to his call on the `RF-2` row), the Voice2 pair NOT eligible (BR/DA/FO have no design-doc home), the acquisition list STAYS, the form-corpus file "verify its routing table before moving," the worknote itself deleted in the same step-3 pass. Do, in order, stopping where it says stop: (1) RE-RUN the two-condition check at YOUR HEAD over the bridge — condition 1 by the entry heading in `docs/record/session-log.md`, condition 2 by the existence of every routed document at its canonical path — for every file the worknote lists, and verify the form-corpus file's routing table; the worknote is a read at `7f02131`, not a standing fact. (2) ASK MICHAEL, one question at a time: the destination for this relocation (his machine or the LEGAL AUTHORITY ARCHIVE project — his choice per relocation; for the fifteen of 2026-08-21 he chose his machine); his call on the three `RF-2`-caveated files; whether the acquisition list is spent. If he is not present, STOP HERE — report the re-verified table and end. (3) STEP 1: extract each eligible file via the Projects tool, record byte size and sha256, zip losslessly with a manifest (`#107`'s method), byte-verify every entry against the pre-removal inventory, and deliver the zip in chat. Never into the repo, never into `inbox/`. (4) STEP 2 is Michael's — he saves the set where he named. WAIT for his confirmation in words. (5) STEP 3, only after (4): delete the relocated files from project knowledge one at a time via the Projects tool, re-listing after each; delete the worknote last; read the knowledge meter before and after and report both. Anything unlanded is FLAGGED by name, never relocated. Nothing is deleted on a nod, a silence, or an assumption.

DELIBERATELY OMITTED (and why — do not "find" these as work)

Any work order against an existing `attorney-review-queue.md` row, the queue's Convention line, and any adjudication of the 2026-08-24 / 2026-08-25 audit candidates — the concurrent Fable session's (§1). `FE-D1A-1` — Michael's ruling; the build is its own fresh Code session, never this chain, never the runner. `MIG-1` and the amendment migration — his hand. The two untracked 2026-08-24 files — DO NOTHING ruled. `TC-OPEN-1` / `TC-1` (queue and BUILD-STATE growth; `pi-case-playbooks.md`) — deferred by ruling. The probate ladder, PR-3 execution, `PL-1`–`PL-4` — deferred pending the Domser matter; the two grandfathered probate workspaces — never addressed. `FC-13`'s six insertion questions, `FC-12`'s open half, §3's sixteen durable IDs, `RF-2`, `RC-4`'s § 18.001 candidate, `CD-14`(i), `HD-3`'s prior-art claim, B6–B8, Q6's retained-expert capture, R11 / TRCP 195.2 — Michael's. The hands-on sitting itself — Fable, Michael present (Task 7 only prepares it). `fe-5`'s missing head — reconstruct NOTHING (K-6/K-7) unless he directs it. The citator pass — untouched by design. Any build, any `src/` read, any Descrybe call, any CourtListener wiring (Q-6), any vendor account or credential. The two practice items from `#106` — his files. Token-cost estimation for `H12-v` — declined.

HONEST PROBLEMS WITH A CHAIN LIKE THIS (stated so nobody is surprised)

Context exhaustion is the hard ceiling — hence per-task shipping and the budget rule. Error propagation is bounded because every output is PROPOSED or DRAFTED-NOT-INSERTED and no task consumes another's unlanded output. Late-session drift is mitigated by RR-1 at every packaging point and a second reader on the two heaviest rewrites (T1, T5). Builds are excluded because fresh context per build is the standing practice. Two sessions writing packets to one inbox on one day is new: the runner's Step 1 orders and pins them; the ordinal is measured; and no two packets carry work orders against the same lines — the fence in §1 keeps every EXISTING queue row to the Fable side while new rows are the runner's appends, and every other file this chain touches is one the Fable session does not. The one thing this packet cannot make true is the record itself — every premise above is a read at `7f02131`, and your first act is to re-read it.

End of CHAT-DISPATCH v5. Generated 2026-09-01 (Central) by the Fable 5 design session; opus-dispatch skill v2. Nothing here is a ruling, a build authorization, or a verification.
