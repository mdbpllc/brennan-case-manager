CHAT-DISPATCH v6 — 2026-09-18 (Central) — THE PHONE-NIGHT OPUS CHAIN

Canonical repo path WHEN FILED: `docs/prompts/CHAT-DISPATCH-v6.md` (the v3/v4/v5 precedent — `docs/prompts/` is the dispatch home; the 2026-09-18 Fable conversation's own packet files it, expected `#158`; until then this is a delivered prompt, not a repo document). This dispatch authorizes nothing on its own; the record at HEAD is the authority, and the project instructions (v33) govern every convention named here by ID.

HOW THIS IS STARTED. A FRESH Cowork chat in the "Project Management Software" (build) project, OPUS 5 selected. First message: either this whole file pasted, or the one line — `Run CHAT-DISPATCH v6: project_read claude/claude_CHAT_DISPATCH_v6_2026-09-18.md and follow it.` Generated 2026-09-18 ~17:30 CDT by the typed Fable 5.1 design conversation of that day, using the `opus-dispatch` skill (v2) in DISPATCH-PACKET MODE, on Michael's instruction, verbatim: "Prepare an opus prompt to run in a clean cowork session that will start working on whatever needs to be done to move any pending items forward. I will be out of the house tonight and will be working on this from my phone." It is a chain of EXECUTION tasks — drafting, research, data prep — every one of which the MODEL USAGE clause routes to Opus. It adjudicates nothing. NO CODE-DISPATCH is issued tonight: the Code acts on the table (the queue run these packets will need; the Wave 0(a) deploy) need Michael at a terminal, and a remotely driven Code session cannot answer a permission prompt.

YOUR FIRST TOOL CALL, before anything else: `device_request_folder_access` for BOTH `C:\Users\Brennan\brennan-case-manager` and `C:\Users\Brennan\Documents\Knowledge Repo`, one request, reason stated in a sentence. Michael approves it AT THE LAPTOP before he leaves; the dialog times out silently if he has already gone — that is not a refusal. Granted → MODE A. Timed out, or no `mcp__remote-devices__*` tools in the session → MODE B. Re-try the request ONCE only if he says he is at the laptop. Never wait on the bridge.

YOUR FIRST OUTPUT (short — he is reading on a phone), via SendUserMessage: (1) the CC-1 HANDS-ON QUEUE: at generation ELEVEN accepted items wait on unbuilt modules (`CR-7`, `CR-CONSTRAINT`, `Q-FE6-5`, `Q-FE4-1`, `Q-FE5-3`, `Q-IN2-7`, `Q-IN1-1`, `Q-IN3-6`, `DA-1`, `DA-3`, `DA-4`), `FO-6` is ruled in shape (🟡, a later slice), THREE are HELD (`Q-API-14`; `Q-API-3`'s filing half; the Dorsaneo Ch. 1 phase-ladder comparison), and a RIPE set exists that the Fable conversation CALLED as an hour-sized hands-on sitting, best after the firm-obligations migration runs: the six PROVISIONAL `CCS-1` text acts (`R1` ×2, `R10`, `R15`, `SD-11`, `SD-12`) and `SD-14`, the `FOS-2` build's PROVISIONAL wording, `FXD-1`–`FXD-11`, and `docs/spec-feedback.md`'s 2026-09-17 section — this chain does NOT run that sitting (Task 7 prepares its kit); (2) the self-identification: "This session runs on Opus 5 and EXECUTES. It adjudicates nothing. Everything rulable it produces is PROPOSED until Michael rules. It closes, annotates, merges or rewords NO register row."; (3) the knowledge meter read directly (`knowledge_size` / `max_knowledge_size`) — at generation 1,779,044 / 2,000,000 = 89.0%, seventeen docs, BEFORE this dispatch's own project-knowledge copy; at or above 90% at open → say so in one line: Q-CAP-5(a) fires, Task 8's table is the capacity pass's staging, and the chain continues; (4) which MODE you are in, and therefore which tasks will run; (5) the FACTS list of §4, marked "answer any time — I am not waiting." Then start Task 1 in the same turn.

0. ASSUMPTIONS AT GENERATION (re-verify; a failed assumption STOPS the affected task, not the chain)

* HEAD believed `4940ed3b885750d6b076e00e5d4f44313e8914cc` on `master`, equal to the LOCAL `origin/master` tracking ref — a plain-file read over the bridge 2026-09-18 ~17:20 CDT, NOT an `ls-remote` and never a QR-3 pass (QR-3 cannot run through the bridge). Its last commit is the `FOS-2` fix build's close-out ("chore: refresh BUILD-STATE — the FOS-2 fix build's close-out"); BUILD-STATE is the 145th refresh, generated 2026-09-17, written at base commit `5430bc0`. Batch 98 (2026-09-16) is the last queue run. `inbox/` on `mdb-pllc` was EMPTY at generation. No `.git/index.lock`. Untracked and Michael's — DO NOTHING to them, ever: `Claude outputs/`, `docs/specs/attorney-review-queue-audit-2026-08-24.md`, `docs/specs/id-collision-report.md`.
* THE LIVE LOG'S TOP ENTRY IS NOT IN THE HEAD FILE. `docs/specs/session-log-head.md` was regenerated at batch 98; the 2026-09-17 `FOS-2` CODE entry sits only in `docs/record/session-log.md` (bridge-only) until batch 99. In MODE B what you know of `FOS-2` is what BUILD-STATE says and nothing more.
* Instructions v33 in force (read live in the field 2026-09-18). High-water design entry at HEAD `#157`. The firm-obligations migration `db/migrations/2026-09-10-firm-obligations.sql` is written, amended in place by `FOS-2`, and NOT run as of the 09-17 entry — his hand, never yours, and never something to prepare a "run card" for on a phone night.
* The device VM's shell MOUNTED the checkout on 2026-09-18 after failing 2026-09-10 through 2026-09-16. If `device_bash` reports no shares mounted, re-try once, then work by STAGED read-only copies (`device_list_dir` / `device_stage_files` / `device_commit_files`), as the instructions' 2026-09-10 note describes.
* MICHAEL'S MACHINE IS A LAPTOP THAT DOCKS, SLEEPS AND TRAVELS — his words, 2026-09-18: "I do not have an office PC per se, rather I use the same laptop whether in the office or on the road. I plug the laptop into a docking station. When I am going to be trying to use this app, in all likelihood my PC will be asleep or turned off." The bridge can therefore drop at any moment tonight: on a bridge error re-try that call ONCE, then finish the current task from what you already hold if you can, drop to MODE B for the rest, and say so in one line. Never retry blind; never wait.

1. THE FENCE — what else is moving tonight, and what this chain never touches

* THE FABLE CONVERSATION THAT GENERATED THIS FILE SHIPS ITS OWN PACKET TONIGHT: `push-to-code_status-phone-forms-conversation_2026-09-18.zip` into `inbox/` on `mdb-pllc`, expected ordinal `#158`. It files this dispatch at `docs/prompts/CHAT-DISPATCH-v6.md` and asks the runner to mint FIVE new ⬜ rows, LABELS ONLY (durable IDs are Michael's to mint — the `#157` precedent): `[FORMS-CREATE]` (self-service form creation), `[HOSTING]` (is the front end hosted, where, behind what sign-in, before or after go-live), `[DEVICE-GATE]` (session expiry, disk encryption, auto-lock, remote wipe — a gate or not), `[PHONE-WIDTH]` (a build convention: every new or touched screen works at phone width), `[PHONE-SIGNIN]` (an emailed six-digit code beside the magic link). Do NOT open that zip, do NOT reconcile it away, and do NOT mint rows that duplicate those five: your sheets POINT AT them by label and add only sub-questions that are new — each one tested against the register's ROWS as well as against paths (batch 98's FINDING 1 is the exhibit: four of five new rows overlapped rows already open).
* ORDINALS. Every packet's §3 entry says: "expected next design ordinal at HEAD — MEASURED by the runner, never assumed; `#158` is expected to be the 2026-09-18 Fable conversation's packet; a concurrent session's packet may land in the same batch." You mint no `#nn` yourself.
* EXISTING REGISTER ROWS: no work order against any existing row of `docs/specs/attorney-review-queue.md` — never close, annotate, merge, renumber or reword one. A row that looks stale is a NOTE in Task 9's table, nothing more.
* HIS HAND, NEVER YOURS: the migration; go-live; the two `H12-v` asks (but see the tail, §4); the DECISION 9A acquisitions; the Tier 1 Lexis download; the TRCP PDF's currency; sync-picker clicks; the TRANSIT export (it needs his Chrome and his click). No queue run (Cowork cannot; Code does, when he says).
* NEVER: read `src/` (working-set policy; `Q-PR3-1`) — `supabase/` and `db/` are inside the working set and may be read; a Descrybe call; CourtListener wiring (`Q-6`); a vendor account, credential, key or secret in any artifact; any real client data; a bare bridge-side `git status` (it strands a 0-byte `.git/index.lock` and shows a false-dirty CRLF wall — read lock-free: plain file reads, `GIT_OPTIONAL_LOCKS=0`); any write into a connected folder other than a finished zip into `inbox/` and a finished deliverable into `Claude outputs\`.

2. COMMON RULES (stated once; the instructions are authoritative on every ID)

* Session-start reads IN FULL — MODE A: over the bridge at HEAD, `docs/specs/session-log-head.md`, `docs/specs/BUILD-STATE.md` (long lines: fold before reading so nothing is cut at 2,000 characters), then the top of `docs/record/session-log.md` down through the 2026-09-17 CODE entry's "Next:" list. MODE B: the same two synced files by project retrieval, stated as RETRIEVAL, not a full-text read — RAG absence is never evidence of absence, and every MODE B deliverable says on its face "drafted from synced retrieval, not a bridge read at HEAD."
* RECONCILE FIRST against BUILD-STATE and the log before staging anything. RR-1 before every zip. RE-SWEEP AFTER FIX on every fix pass. CITE-STABILITY: cite the head file, BUILD-STATE and the log by heading or quoted sentence, never by line; cite `docs/spec-feedback.md` by section date and heading. DT-1: every stamp is Michael's Central wall-clock date — the container rolls to 2026-09-19 UTC at 19:00 CDT TONIGHT; run `TZ='America/Chicago' date` before every stamp.
* PF-1: a packet carrying a legal characterization or a proposed registry entry gets the adversarial multi-agent READ-ONLY preflight against PRIMARY SOURCES before its zip enters `inbox/` (Tasks 2 and 4 fire it). Every other packet RECORDS that PF-1 did not fire and why. A fleet that returns zero findings and zero tokens is a DEAD fleet, not a clean bill (`#104`): confirm every agent returned content. PF-1 blocks SHIPPING, not drafting — a packet that cannot be preflighted is HELD and reported.
* PROPOSED until ruled. You put NO ruling question to Michael tonight. If he VOLUNTEERS a ruling from his phone, record it verbatim with the Central time in your ledger, re-ask narrower only if it is ambiguous (CC-1(a)), and carry it in the packet as HIS ruling in HIS words. Hands-on questions are HELD to the named queue, never pressed (CC-1(b)).
* SOURCING and TOOLING, each source named per item: Texas statutes from the official corpus in `Documents\Knowledge Repo\Statutes <download-date>\` (unzip the one chapter into the VM's own `/tmp`, never a mounted path; the `A`-for-space normalizer at `statute-pass-registry-retrieval-2026-08-14.md` §3 is for the Legislature's statute PDFs ONLY — never the TRCP PDFs); Texas rules and the conduct-and-administration rules from the clean-authority PDFs in `Documents\Knowledge Repo\`, cited by each PDF's own effective date; federal regulations from the eCFR API named "eCFR via official API" with request date and `up_to_date_as_of`; the U.S. Code from uscode.house.gov / govinfo.gov per section with the edition stated on the page and the request date; case law via FLP/CourtListener and named public sources only. A source that is on his machine and unreachable tonight is UNREACHABLE — it goes in a gap table; you never substitute another source for it. Currency is never inferred from a document. Every quotation is spot-checked against raw extraction. Retrieval is not verification; ONLY Michael verifies.
* H5: never sweep his machine; read only paths this file or the instructions name. The sanctioned listings are the top level of `Documents\Knowledge Repo\` and the SOURCING-named folders under it, to locate a rules PDF or a code zip by its own name. Never open `Civil\Dorsaneo\` (licensed LexisNexis material — nothing tonight needs it) or `Session Captures\`.
* Web: WebFetch / WebSearch only. A page that cannot be fetched is recorded NOT FETCHED with its URL — never approximated from snippets, never fetched by shell or script. Vendor facts are quoted from the vendor's OWN page with the fetch date.
* Packets, via the `push-to-code` skill: ONE ZIP PER COMPLETED REPO-BOUND TASK, zip only, never loose files; manifest opens with RECONCILE FIRST; every staged document's status line names its canonical repo path; EVERY ROUTING ROW CARRIES ITS CAP-2 CLASS (`RULING` born under `docs/specs/` or `docs/prompts/`; `EVIDENCE` born under `docs/record/<slug>-2026-09-18/`; an edit-in-place row carries `—`, CAP-2a); the DO-NOT list is explicit; open questions carry FULL text (QR-1) with packet-local labels. MODE A: commit ONLY the zip into `inbox/` by `device_commit_files`, from a path or a FRESH `SendUserFile` uuid — a uuid is a SNAPSHOT of the file at send time, so after any edit re-send and commit the new one, then stage the committed file back and compare byte counts (learned 2026-09-16). MODE B: deliver the zip in chat and list it as HELD — NOT in `inbox/`. Record each zip's bytes and sha256. Filenames: `push-to-code_<slug>_<Central date>.zip`.
* CAPACITY DISCIPLINE, FOR THIS CHAIN ONLY (the meter opens within a point of Q-CAP-5(a)'s line; Michael may overrule in a word): deliverables he reads go to him as chat files and, in MODE A, into `C:\Users\Brennan\brennan-case-manager\Claude outputs\` — NOT into project knowledge. At the end (and at a RESUME break) run `refresh-chat` ONCE: write ONLY the combined handoff into project knowledge, lean, as `claude_Opus_Chain_Handoff_2026-09-18.md` with its TRANSIT line; the granular capture goes to chat and `Claude outputs\`. Read the meter after writing and report it.
* Keep a RUNNING LEDGER in your scratchpad from the first minute: every word of Michael's that matters, verbatim, with the Central time; every skip and why. The handoff is a copy of it, not a reconstruction.

3. HOW THE CHAIN RUNS — PHONE-NIGHT EDITION

* Order, by leverage: T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 → T9, then the tail. MODE B runs T1 → T3 → T5 → T6 only and SKIPS the rest with the reason "bridge-only inputs."
* Per task: finish it, RR-1 it, ship its zip (or hold it), send ONE checkpoint by SendUserMessage — three lines at most: what is done, the one file to open first, what is next — and CONTINUE WITHOUT WAITING. STOP and ask only on: a failed assumption that reaches beyond one task, a record ambiguity at a gate, anything client-data- or credential-shaped.
* He is on a phone. Short messages; no monologues; every file he should read is SENT (SendUserFile), never merely mentioned. Approval prompts (a zip into `inbox/`, a web page) reach his phone — batch them at task ends, and list anything that stalled on an approval in the night report.
* CONTEXT BUDGET: before each task, judge whether the task PLUS a packaging pass fits what remains. If not: RR-1 over everything staged, ship what is complete, `refresh-chat` per the capacity rule, and end with "RESUME AT TASK n — start a fresh chat with the one-line kickoff plus that line." On RESUME: fresh reads; for each task decide whether its output has (a) LANDED at HEAD, (b) SHIPPED as a zip still in `inbox/` — counts as done — or (c) neither; resume at the first (c). No two tasks form an ATOMIC pair; no task consumes another's unlanded output. Chaining works only within a session — a finished session cannot start the next; Michael's message is the only trigger.
* THE NIGHT REPORT (your last message, phone-sized, then the tail's widget): done / skipped and why · each zip with bytes + sha256 and whether it is in `inbox/` or HELD · the ONE thing to read first · what needs him AT THE LAPTOP next — the queue run (batch 99) on `mdb-pllc` then Sync; the TRANSIT execution with his Chrome (Task 8's table); the migration · any lock stranded or scratch left, by exact path · the meter before and after · the RESUME line if unfinished.

TASK 1 — THE WAVE 0(a) DEPLOY-SESSION KICKOFF PROMPT [MODE A or B] — BUILD-STATE's own words: a separate Code session "whose kickoff prompt is a design act not yet drafted"

Slug: `wave0a-deploy-session-prompt`. GATE: BUILD-STATE at your HEAD still says of the two edge functions "DEPLOYMENT AUTHORIZED 2026-09-09 (`#154`, Wave 0 (a)) AND NOT YET RUN", and no file under `docs/prompts/` names Wave 0(a) or an edge-function deploy. Otherwise SKIP and report. AUTHORIZATION: locate and QUOTE it at your HEAD — `docs/specs/api-integrations-ruling-sheet-2026-09-09.md` (the Wave 0 item; his pick named all four acts) and the `#154` entry ("a SEPARATE Code session with Michael's Supabase sign-in, NOT a runner act"); if you cannot find the words, STOP the task. Read: `docs/statute-cache-setup.md` (whole — it is short); `supabase/functions/statute-fetch/index.ts` and `supabase/functions/legiscan-poller/index.ts`; `statute-text-and-bill-tracking-design.md` §5 (cadence); `Go_Live_Gates.md` gate 4 and its LegiScan facts; BUILD-STATE's "Known stubs & fakes" bullet on the two functions (`F-14` latent; `service_role` shows no DML — INDICATED, not measured; Diagnostic 6 in `docs/spec-feedback.md` staged and UNRUN; `O-11`, `O-12`); and, for house shape, `docs/prompts/PROMPT-firm-obligations-fix-slice-build-session.md`. Draft `docs/prompts/PROMPT-wave0a-edge-function-deploy-session.md`: Michael PRESENT at the terminal throughout (his Supabase sign-in; nothing here runs remotely); the queue runner barred from doubling (`MM-1`); STEP 0 with a live `git ls-remote`; both gates quoted from the record; the live precondition CHECKED by a named read-only query he pastes (do the statute tables `db/schema.sql` defines exist live?), never assumed; deploy `statute-fetch` — put the `--no-verify-jwt` choice to him in session with the setup doc's own words ("Leaving JWT verification ON is fine and slightly tighter"); deploy `legiscan-poller`; confirm the `LEGISCAN_API_KEY` secret EXISTS BY NAME ONLY — the key is never echoed, printed, logged or pasted (gate 4); invoke the poller ONCE by hand and read its JSON log BEFORE any cron; say in advance what each outcome means — on BUILD-STATE's evidence the poller may fail on PRIVILEGE, which is exactly the evidence `F-14` and `O-11` want — and that granting any privilege is NOT authorized by Wave 0(a): it goes back to Michael as `O-11`'s ruling; run Diagnostic 6; cron only on his word after a clean manual run (the setup doc's interim cadence is monthly, `0 6 1 * *`); the close-out trio; no `#nn` (`TOC-6`). Ship: the prompt (RULING) · §3 entry · open items in full text (JWT on or off; what he wants done if the poller fails on privilege; the cadence). PF-1 does not fire — record why.

TASK 2 — THE `#157` RULING SHEET, PREPARED — NOT RULED [MODE A ONLY] — BUILD-STATE: "`#157`'s FOUR PROPOSED CORRECTIONS AWAIT HIS RULING … ALL UNVERIFIED, NOTHING EDITED"

Slug: `dorsaneo-corrections-ruling-sheet`. GATE: the five ⬜ rows under the register's DORSANEO ASSESSMENT PASS heading are still ⬜, and no entry above `#157` records a ruling on them. Read WHOLE: the five files at `docs/record/dorsaneo-pass-2026-09-13/` (the assessment's §3 carries the four corrections and the ROUTE-C wording it drafted and did not stage); the five rows and their four cross-linked twins (`[DL-memo Q1]`, `[DL-memo Q3]`, `Q-FE5-9`, §3's `TRCP 194.2(a)` row); `docs/specs/trcp-deadline-skeleton-2026-03-01.md` §5 and its footnote; the VERIFIED TRCP 190.3(b)(3) and TRCP 194 entries in `legal-rule-registry-discovery-and-carrier-duties.md`; `P-2` in the deadline memo and `deadline-engine-spec.md`. Then re-read every rule AT THE OPERATIVE TEXT — `Documents\Knowledge Repo\Civil\texas-rules-of-civil-procedure July 2026.pdf`, `pdftotext -enc UTF-8 -layout` into the VM's `/tmp`: rules 4, 21a, 190.2, 190.3, 194, 194a, 195.2, 196.2, 196.7, 197.2, 198.2. Build the sheet he will rule from, ONE BLOCK PER ROW, EACH INDEPENDENT (ROUTE-C — never a package): the row's question in full · the operative text quoted by rule and subsection · the wording as it stands today, quoted · the proposed conforming wording · a RENDERED BEFORE/AFTER (his stated preference: examples before ruling on structure) · what adoption does to verification (adopting new wording DETACHES a VERIFIED entry's verification — `#95`, execute-then-verify) · every cite change flagged as ITS OWN act · the options as a way of asking, one recommendation stated. The fifth row (Dorsaneo as a named SOURCING channel): both readings put; `#157`'s read was that it should NOT — a treatise is a locator (`WS-3`) — and adopting it would fire trigger 3. ON THE SHEET'S FACE: that PDF states NO currency date — its currency is NOT ESTABLISHED and is his act; nothing on the sheet is verified. Edit NOTHING — not the skeleton, not a registry file, not a row. PF-1 FIRES: read-only agents for (i) every quotation against raw extraction, (ii) every characterization against the operative text, hunting overstatement, (iii) every repo fact against the file at HEAD, (iv) client data and treatise body text — none of either belongs on the sheet. Ship: `docs/specs/dorsaneo-corrections-ruling-sheet-2026-09-18.md` (RULING) · the PF-1 report (EVIDENCE) · §3 entry · NO new rows — the five exist.

TASK 3 — HOSTING, THE PHONE, AND THE TRAVELING LAPTOP: a research memo and the sheet he will rule from [MODE A or B]

Slug: `hosting-phone-device-sheet`. WHERE THIS CAME FROM — Michael, 2026-09-18, verbatim: "Cloudlex also has an app that I use on my iPhone. Are we going to be able to have an app as well for this software? If so, when should we start working on this?" — and the laptop statement quoted in §0. ON THE RECORD ALREADY: the master spec §2 ("An iPhone app is wanted later") and §3 (React chosen for it); `#137`'s `DA-1` — his words "I wanna be able to access every single document that's for that file that lives in OneDrive" (requirement CONFIRMED, nothing designed), `DA-3` (the PHI boundary on the phone — asked twice, never answered; HELD, do not press it), `DA-4`; `Q-API-14` (held). WHAT THE APP IS, from BUILD-STATE: a browser SPA whose UI talks only to a DataAdapter — localStorage in demo mode, Supabase directly in central mode; NO server of its own; three Edge Functions, two undeployed and one a stub; a single-tenant public-client Entra registration, MSAL/PKCE, `Calendars.ReadWrite` only, redirect `localhost:5173`, no secret; delegated `Files.ReadWrite` and `Mail.Send` ruled IN at `#154`, neither consented; "Allow new users to sign up" OFF; sign-in by magic link (the `HS-5` note: a magic link always opens the OS default browser). IT IS HOSTED NOWHERE — it runs from `npm run dev` on the laptop. The Fable conversation found NO statement about hosting, phone layouts, session expiry, disk encryption, auto-lock or remote wipe in the gates doc, the runbook, BUILD-STATE, CLAUDE.md or the master spec (grep, 2026-09-18) — re-check in MODE A. PROPOSED there, unruled: a home-screen web app first, built so that a native wrapper is later a wrap and not a rewrite; hosting BEFORE go-live, tested on the one TEST record; the host behind his Microsoft sign-in, because the built bundle LIKELY carries template text and the disclosures master — an ASSUMPTION (`src/` is not read): write it as a one-look, read-only verification for a Code session, never as a fact; a short device-and-session gate before go-live. FETCHED 2026-09-18 — RE-FETCH before you quote any of it in a shipped document: Supabase sessions "By default, it lasts indefinitely", and time-boxed sessions, inactivity timeout and single-session are "only available on Pro Plans and up" (supabase.com/docs/guides/auth/sessions); Supabase email OTP — the `{{ .Token }}` template, six digits, `verifyOtp`, one-hour default expiry (supabase.com/docs/guides/auth/auth-email-passwordless); on iOS "Safari and an installed PWA don't share storage, cookies, or cache", so a magic link signs in Safari only (a Progressier help article — find a WebKit or Apple source if you can); iOS home-screen web apps have web push and badging from iOS 16.4 once added to the Home Screen, and no Background Sync (a MagicBell guide — a vendor blog; its "50MB" and "7-day" storage claims are DISPUTED: do not repeat them without a WebKit or Apple source); Capacitor 8 — "To build iOS apps, you will need macOS", Xcode 26.0 minimum (capacitorjs.com); Apple — $99 a year, TestFlight builds expire after 90 days, unlisted distribution needs App Review and installs by link (developer.apple.com/support/unlisted-app-distribution); Azure Static Web Apps — "Custom authentication is only available in the Azure Static Web Apps Standard plan" (learn.microsoft.com). DO: from each vendor's OWN pages, fetched tonight and quoted — (1) two or three hosting candidates that can refuse to serve ANYTHING before a sign-in restricted to his one Entra tenant (Azure Static Web Apps; Cloudflare Pages behind Cloudflare Access; at most one more), each with plan, price, how tenant restriction is actually done, custom domain and HTTPS, and how a Vite SPA's routes and the two auth redirects (Supabase; MSAL) behave behind it; (2) Supabase Auth settings that matter — session timebox and inactivity timeout, the redirect allowlist, the OTP template; (3) Entra — adding a hosted origin as a SPA redirect URI, and MSAL redirect-versus-popup inside an iOS home-screen app; (4) Microsoft 365 plans — which include Intune and what app-protection and device policies reach a Windows laptop and an iPhone (the tenant's SKU is `Q-API-18`'s first fact and is HIS — never guess it); (5) Windows device encryption and BitLocker by Windows edition, from Microsoft's page. CHARACTERIZE NO LAW: whether any of this engages HIPAA or a disciplinary rule is routed as an UNVERIFIED candidate question, never answered. Build the sheet: one question at a time, each pointing at the Fable packet's row by label — `[HOSTING]`, `[DEVICE-GATE]`, `[PHONE-WIDTH]`, `[PHONE-SIGNIN]` — options as a way of asking, one recommendation each, rendered examples where a screen or a flow is at issue (the phone sign-in, step by step; what a phone-width case screen would and would not show). Ship: `docs/specs/hosting-phone-device-ruling-sheet-2026-09-18.md` (RULING) · `docs/record/hosting-phone-device-2026-09-18/` research memo and fetch record (EVIDENCE) · §3 entry · only sub-questions that are NEW, tested against the register's rows. PF-1 does not fire if you kept to the rule above — record why.

TASK 4 — DECISION 8's FIRST TRANCHE: registry ENTRY DRAFTS for the firm's practice [MODE A ONLY] — the ruling's words: "drafts by a LATER Opus act on the `FC-13` precedent, PF-1 running on that packet; every entry UNVERIFIED until he verifies"

Slug: `fo-registry-entry-drafts-tranche-1`. GATE: `docs/specs/legal-rule-registry-firm-obligations.md` does not exist at HEAD and no FO entry-drafts file exists. Read WHOLE: DECISION 8, 9A and 9B in `docs/specs/firm-obligations-rulings-2026-09-10.md`; `docs/specs/firm-obligations-module-spec.md` (the thirty-five `FOT-` templates, each with its named source and status, and §16); `docs/specs/fc13-entry-drafts-2026-08-18.md` for the house shape of an entry draft; one existing registry file for entry format. SCOPE TONIGHT: the TWELVE intended go-live activations — `FOT-1`, `FOT-2`, `FOT-4`, `FOT-6`, `FOT-8`, `FOT-9`, `FOT-19`, `FOT-22`, `FOT-23`, `FOT-24`, `FOT-25`, `FOT-27` — and ONLY where the template's named source is REACHABLE tonight under SOURCING. A template whose source is one of his unmade acquisitions (the State Bar Rules and MCLE Regulations, the TAJF rules, the county plans — his hand, open at the 2026-09-17 refresh) gets a row in a GAP TABLE saying exactly which document is missing; you never substitute a website for it. A template with NO legal proposition behind it (a backup heartbeat, a domain renewal) gets a one-line "no registry entry — a business practice, not a rule of law" with your reason, as a QUESTION for him, not a conclusion. Each draft: proposition in registry style · cite · the operative text quoted and spot-checked against raw extraction · source named per item with its currency statement taken from the source itself · UNVERIFIED · the three-date discipline `#157` recommended (enactment, effective, applicability) where the source gives them · the `FOM-12` weekend-roll default for that row as its own proposition where the source speaks to it, and "source silent" where it does not. CARRY `#157`'s caution, UNVERIFIED: an October 2024 package changed seven disciplinary rules — for every TDRPC-based draft state the PDF's own effective date. DRAFTED, NOT INSERTED: you do not create the fifth registry file; whether tranche 1 is inserted now or waits for the whole set is an open item for him, full text. PF-1 FIRES (the fleet of Task 2, against these sources). Ship: `docs/record/fo-registry-entry-drafts-2026-09-18/` — the drafts, the gap table, the PF-1 report (all EVIDENCE) · §3 entry · open items in full text.

TASK 5 — WAVE 0(d): THE BACKUP VENDOR ONE-PAGER [MODE A or B] — authorized with the other Wave 0 acts at `#154` ("a design act, put back to him as a choice"); BUILD-STATE calls it "a design act on request", and his 2026-09-18 instruction to move pending items forward is read as that request — if he says otherwise, SKIP

Slug: `backup-vendor-one-pager`. GATE: no backup one-pager at HEAD. REQUIREMENTS, from `#137` — read PART 8 and PART 12 of `claude_Backup_Redundancy_and_Firm_Obligations_Capture_2026-08-22_Voice2.md` in project knowledge (MODE A: also `docs/record/firm-obligations-sitting-2026-09-10/voice2-definitions-carried-2026-09-10.md`, and the third-copy row of the `#154` memo v2 at `docs/record/api-integrations-landscape-2026-09-08/`): a BAA; MICROSOFT-INDEPENDENCE — the restore path cannot route through Microsoft, so not Azure, and Microsoft 365 Backup is closed for good (`#154`); MONITORING as a hard requirement, both a heartbeat and a restore test; the monthly ceiling he confirmed — quote the figure where you find it, and if you cannot find it at source, say so rather than carrying "$50" from this file; CloudLex is the ORIGIN and OneDrive the mirror; a self-hosted server is his eventual destination and a managed vendor the interim — his reason "attention, not cost." NEW FACT: the laptop sleeps (§0) — so WHO RUNS THE COPY is part of the choice: the `#154` memo allowed a scheduled job on the laptop ("the one place where the laptop's sleeping is tolerable, because a missed night is caught by the heartbeat") or a cloud runner; put both. DO: each vendor's OWN pages, fetched tonight — Backblaze B2 and AWS S3 / S3 Glacier / AWS Backup, plus at most two others that pass the BAA and not-Microsoft filters on their own pages: BAA availability and how it is obtained, versioning and object lock, egress and restore cost, price per 100 GB and per 1 TB a month. The size of his document tree is HIS fact — never measure it. ONE PAGE, put back as a choice, one recommendation stated, rendered as a comparison he can read on a phone. Characterize no law. Ship: `docs/specs/backup-vendor-one-pager-2026-09-18.md` (RULING) · the fetch record (EVIDENCE) · §3 entry.

TASK 6 — THE FORM BUILDER BEYOND DISCLOSURES: a design-INPUT memo that maps and hands the questions back [MODE A or B] — it is NOT a design, and a build session must never read it as one (the `IN-1` / `IN-3` precedent)

Slug: `form-builder-design-inputs`. WHERE THIS CAME FROM — Michael, 2026-09-18, verbatim: "Another thing that I want to talk about is the form builder. Up until now, we have spent a considerable amount of time dealing with solely the disclosures form builder. I am going to want the software to be able to generate other forms as well and am going to want to be able to create forms. Is that something that we are thinking about as well? If not, let's put this on the list of items to address as well." TWO LIMBS. (a) OTHER FORMS — on the record: `form-engine.md`'s face ("The same engine later drives all firm forms"), its §11 items 2 and 4, §13 (`FE-4`–`FE-7`, RULED 2026-08-11 and unauthorized) with the seven-candidate distillation queue at §13.4, and §3's token rulings (`FC-1`–`FC-4`); the register's FE series (`FE-4` through `FE-17`, `FE-§11.1`, `FE-§11.2`, `FE-SEED-1` — nothing seeds the template bank in live mode); `de-1-deficiency-letter-template-spec-2026-08.md`; the CR series; `docs/templates/discovery/`; the Bexar monitoring-court forms doc in project knowledge. (b) CREATING FORMS HIMSELF — NOT designed: the spec's §1 settles the opposite division of labor ("Claude builds the engine, new complex templates, and new merge-field wiring; Michael owns routine wording changes"); the built editor is minimal by ruling (plain text with tokens; "Save as a new version"); `FE-7` distillation is a design-session act; `FC-12` ruled "(c) Structure only" and NO import tooling is authorized. Read also `claude/claude_Form_Corpus_Mining_and_Defects_2026-08-18.md` in project knowledge: 104 of 204 tokens in his own letters are split across Word runs; "Nothing in any library is safely blank"; the design requirement it proposed — starting from a clean template must be cheaper than copying the last document. H5: build the inventory FROM THE RECORD ONLY — never list or open his form libraries or any folder of his to find forms. DO: (1) an inventory table of every form the record already names as wanted, each with its source row or document, whether it is PURE MERGE (no model call, so no `H12-v` / BAA dependency — the Fable conversation's PROPOSED point: a pure-merge form can reach real matters long before the disclosures paragraphs do) or needs a writer, and which fields it needs that the token registry, as BUILD-STATE and `db/schema.sql` describe it, does not have; (2) research, fetched and cited, on how document-automation tools let a non-programmer author a template — marking blanks in a Word document, token repair across runs, conditions and repeating blocks, field mapping, "ask me" questions that write back — no more than six products or libraries, and no recommendation to adopt any of them; (3) the design questions, in full text, as SUB-QUESTIONS of `[FORMS-CREATE]` (what "create a form" means — upload and mark, type from scratch, or hand over a finished document and receive a proposed template; the field list as the real limit and "ask me" blanks as the escape; conditions and repeats; the scrub step, and whether `FC-12` reaches his own upload of his own document; provenance and adoption, `FE-12`; who may author when a paralegal joins; `FE-SEED-1` as the prerequisite to ANY live form; output to PDF and OneDrive, unbuilt); (4) RENDERED FICTIONAL EXAMPLES of each shape of "create a form" — a short fictional letter as he would hand it over, the marked-up template, the questions the wizard would ask, the filled result. All invented; no real name, address, carrier or cause number. Ship: `docs/record/form-builder-design-inputs-2026-09-18/` memo and inventory (EVIDENCE) · `docs/specs/form-builder-rendered-examples-2026-09-18.md` (RULING — he reads it to decide) · §3 entry · new sub-questions only, tested against the FE rows. "Forms" here means documents; the client intake web form is a separate banked item (master spec §14) — say so once.

TASK 7 — THE HANDS-ON SITTING KIT (data prep for the Fable sitting the 2026-09-18 conversation called) [MODE A ONLY] — no packet, not project knowledge

GATE: no entry above the 2026-09-17 CODE entry records the sitting. Read: that CODE entry WHOLE (its text-act list, `FXD-1`–`FXD-11`, its walk rows F1–F9 / M0–M4 / B6-1–B6-3, "Awaiting/Returned from Code, unreviewed"); `docs/spec-feedback.md`, the 2026-09-17 section, parts A, B and C; `docs/specs/firm-obligations-fix-slice.md`, and `docs/specs/firm-obligations-build-slice.md` §12 (BUILD-STATE: "some act strings still PROVISIONAL (slice §12)"); `docs/specs/cc1-hands-on-sitting-rulings-2026-09-05.md` and `docs/specs/firm-obligations-hands-on-rulings-2026-09-12.md` for the six provisional `CCS-1` text acts and `SD-14`; and, for shape, `Claude outputs\KICKOFF-firm-obligations-hands-on-sitting-and-post-ccs1-walk_2026-09-12.md`. Build one block per item: the FULL text as it stands in its home (read, never paraphrased) · the exact click path in DEMO mode (`npm run dev:demo`, port 5175 — confirm against `package.json` and BUILD-STATE) · the build default in force meanwhile · the question as a way of asking · a rendered fictional example wherever wording or document structure is at issue. Order by SCREEN so the sitting walks the product once. Mark the items that can only be seen in CENTRAL mode after the migration (the thirteen SQL refusal messages) — those are read as text, not walked. Classify nothing. Deliver: `KICKOFF-hands-on-sitting-fos2-and-provisional-text-acts_2026-09-18.md` as a chat file and into `Claude outputs\`.

TASK 8 — TRANSIT READINESS TABLE [MODE A ONLY] — verification only; the export needs his Chrome and his click, so nothing is exported and NOTHING IS DELETED tonight

For each of: the `#137` Voice2 pair, the `#154` pair (2026-09-09), the `#155` pair (2026-09-10), the `#156` pair (2026-09-12), and `claude/claude_Form_Corpus_Mining_and_Defects_2026-08-18.md` — `project_read` it, find its routing table, and check BOTH TRANSIT conditions at your HEAD by full-text read: (1) its session-log entry is filed (the heading in `docs/record/session-log.md`), (2) every document its routing table names exists at its canonical path. Anything unlanded is named SPECIFICALLY. The two draft emails, the acquisition list, this dispatch and tonight's handoff are LIVE WORKING DOCUMENTS and are not candidates. Also record, as an observation for the runner's refresh: BUILD-STATE says the `#157` pair entered project knowledge, and the doc list on 2026-09-18 showed no 2026-09-13 file (the capture itself IS filed, at `docs/record/dorsaneo-pass-2026-09-13/`). Deliver the table as a chat file and into `Claude outputs\`; it rides Task 9's packet as EVIDENCE if Task 9 runs, otherwise the last packet you ship.

TASK 9 — THE REGISTER: OVERTAKEN-ROW CANDIDATES [MODE A ONLY; OPTIONAL; LAST] — an audit-shaped need DOWNGRADED to data prep: this task adjudicates nothing and proposes no edit

Slug: `register-overtaken-candidates`. Method: `docs/prompts/OPUS-SWEEP_queue-live-rows_2026-09-01.md`, scoped to what has happened SINCE the two 2026-09-01 cleanup adjudications (`attorney-review-queue-cleanup-adjudication-2026-09-01.md` and `-wave2.md` — read both first: a row they deliberately LEFT is not a candidate; verify before criticizing). Use read-only sub-agents by register section; a row is a CANDIDATE only when a dated entry or a BUILD-STATE sentence, QUOTED, shows the act it asks for has happened. Two seeds seen 2026-09-18, to be verified by you, not trusted: the `MIGRATION — NEW 2026-08-16 (#94)` row still reads ⬜ while BUILD-STATE says "THE MIGRATION RAN 2026-08-19 (#113)"; the `v23 INSTRUCTIONS` row still reads ⬜ while the instructions record `PF-1` and `FC-14` reaching the live text in v28. Ship: `docs/record/register-overtaken-candidates-2026-09-18/` — one table: row lead · the quoted evidence and where it is · your confidence · what a Fable sitting would need to decide (EVIDENCE) · §3 entry. No work order against any row.

4. THE TAIL — FACTS BY PHONE, AND ONE ACT ONLY ON HIS WORD

FACTS (not rulings; H5 — asked, never looked up). List them in your first message as "answer any time"; put whatever is still unanswered through the answer widget at the very END, four to a call, room left for his own words: (F1) has the firm-obligations migration been run since 2026-09-17 — if yes, his word is recorded as his word; the ten checks in words are still owed to the record; (F2) the Microsoft 365 plan the tenant is on — `Q-API-18`'s first fact, and it decides `Q-API-14`'s Intune question; (F3) the record's `OPEN-3`: is `mdb-pllc` the ThinkPad P1 Gen 8, and is the P15 still in use; (F4) beyond documents, what he actually does in the CloudLex app on his phone; (F5) the three or four forms he reaches for most; (F6) whether either `H12-v` ask has gone out; (F7) which DECISION 9A acquisitions are done; (F8) `Q-API-19`: does the process server use ServeManager. An unanswered fact blocks nothing.

ONLY IF HE SAYS SO IN WORDS TONIGHT ("drafts yes"): create the two `H12-v` asks as OUTLOOK DRAFTS through the Microsoft 365 connector from `claude/claude_Draft_Email_AWS_BAA_Solo_Firm_2026-09-07.md` and `claude/claude_Draft_Email_Malpractice_Carrier_AI_Drafting_2026-09-07.md` — text as drafted, the To line EMPTY (the addressees are placeholders and are his to fill), his standard signature block and confidentiality notice at the foot, DRAFT ONLY — nothing is ever sent by you. Report the two draft subjects.

WHEN THE CHAIN COMPLETES (every task done or skipped — NOT at a RESUME break): in MODE A, confirm `push-to-code_status-phone-forms-conversation_2026-09-18.zip` is in `inbox/` or that `docs/prompts/CHAT-DISPATCH-v6.md` is at HEAD, then `project_delete` the project-knowledge copy of this dispatch (it is a phone-launch convenience; the canonical copy rides that packet and a byte-identical copy is in `Claude outputs\`) and report the meter. In MODE B leave it and say so.

DELIBERATELY OMITTED (and why — do not "find" these as work)

The SPLIT-STORE SLICE — `Q-API-20` was ruled "After", and BUILD-STATE's words are "drafted after the walk, once `Q-API-18`'s facts are in"; the second fact is a determination Michael left OPEN, so the slice is not drafted tonight. `FO-6`'s calendar page and per-paragraph regenerate — ruled "later", each its own authorization. The firm-obligations migration, his real activations, go-live — his hand, his day. The hands-on sitting itself and every ruling sitting — Fable, Michael present (Tasks 2, 3, 6 and 7 only prepare). Insertion of any registry entry; any edit to the deadline skeleton, a registry file or an existing register row. `FE-SEED-1`, `FC-12`'s open half, `FC-13`'s six insertion questions, `Q-STAT-2`–`Q-STAT-7`, `Q-COM-10`, the `CD-` and `Q-PR3-` series, `V5-IDS`, `V5-ATTRIB`, the cite supply for entries 30 and 31 — his rulings and his acts. The probate ladder, PR-3 execution, `PL-1`–`PL-4` — deferred pending the Domser matter; the two grandfathered probate workspaces — never addressed. T3 / Phase 0 — unauthorized until `KICK-1`'s kickoff doc is located or re-issued. CE1 and the time tracker — parked. The `#154` closed-for-now list. Wave 0(b) and (c) — his hand; Wave 0(e) — deferred to the mail-intake slice. The TRANSIT export and any deletion of a capture — needs his Chrome. Instructions v34 — no trigger has fired; carry, in your handoff, the notes that should ride the next revision: the `fileUuid`-is-a-snapshot note (2026-09-16), the VM shell mounting again on 2026-09-18, and "his machine is a laptop that sleeps" (the master spec's older "office PC" wording notwithstanding). Any build, any `src/` read, any credential, any real client data.

HONEST PROBLEMS WITH A CHAIN LIKE THIS (stated so nobody is surprised)

One session will not finish nine tasks — context is the hard ceiling; expect two to four, and the RESUME line for the rest. The bridge may drop the moment the laptop sleeps; MODE B still produces four deliverables, but their zips stay HELD until a bridged session or Michael's hand puts them in `inbox/`. Tasks 2 and 4 are only as good as their PF-1 fleets, and a dead fleet looks exactly like a clean one. MODE B deliverables rest on retrieval, not full-text reads, and say so on their faces. Error propagation is bounded because every output is PROPOSED, DRAFTED-NOT-INSERTED, or a table of candidates, and no task consumes another's unlanded output. Two design-side sessions are writing packets to one `inbox/` tonight: the runner's Step 1 orders and pins them, the ordinals are measured, and the fence in §1 keeps their rows apart. The one thing this file cannot make true is the record itself — every premise above is a read at `4940ed3` on 2026-09-18, and your first act is to re-read it.

End of CHAT-DISPATCH v6. Generated 2026-09-18 (Central) by the Fable 5.1 design conversation; opus-dispatch skill v2. Nothing here is a ruling, a build authorization, or a verification.
