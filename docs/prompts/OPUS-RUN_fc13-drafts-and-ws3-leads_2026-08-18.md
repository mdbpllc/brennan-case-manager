# OPUS RUN — FC-13 Entry Drafts + Q-WS3-5 Five-Lead Read (authorized 2026-08-18, #108)

**Paste this whole file into an Opus 5 design chat in the brennan-case-manager project. It is a
CHAT-DISPATCH-class prompt: design-chat execution only — a Code session never executes it.**
**Canonical repo path:** `docs/prompts/OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md`.

## §0 What this is

Two acts Michael AUTHORIZED on 2026-08-18 (#108; terms restated below — this prompt authorizes
nothing new). Work them top-down with a checkpoint after each task. **Resume rule: a dead
session's unpacked work is lost — on a fresh paste, check HEAD for each task's staging doc
(named in §1/§2) and redo any task whose doc is absent; never look for an in-progress packet.**
**RECONCILE FIRST (before any drafting):** read the top 2–3 session-log entries and all of
BUILD-STATE at HEAD; act only on genuine deltas and say so.

## §1 Task 1 — draft the five FC-13 registry entries (do NOT insert them)

Ruled at #108: all four statutory/rule candidates selected in the multi-select, and the fifth
opened by the pick "Open 1.04(f) entry" — five entries OPEN. Draft each as entry TEXT in a staging
doc (`docs/specs/fc13-entry-drafts-<DT-1 Central date>.md`), destination file stated per entry,
**not inserted** — insertion is a later directed act. **Re-paste/done check: if a
`docs/specs/fc13-entry-drafts-*.md` staging doc already exists at HEAD, Task 1 is done** — the
registry files can never show these entries pre-insertion, so never check there.

Binding terms:
- **The standing VERBATIM direction (ruled #108) governs every Rule line:** quote operative text
  verbatim where practicable; condensation only where the provision is too long or the entry
  deliberately states one limb, flagged as such in the entry.
- **SOURCING, named per item:** Prop. Code ch. 55 (hospital/EMS liens), CPRC ch. 146 **including
  the 2025 (c-1) amendment**, and CPRC §18.001 / §41.0105 from the official bulk corpus
  `Documents\Knowledge Repo\Statutes 26-08-14\` (PR.pdf.zip and CP.pdf.zip; unzip and extract in
  the device VM's own filesystem, never a mounted path; the doubled-`A` artifact transforms only
  in characterized contexts — report anything else; currency per the source's own statement, 89th
  Leg. 2nd C.S. 2025). TRCP 47(c) from
  `Documents\Knowledge Repo\Civil\texas-rules-of-civil-procedure July 2026.pdf`. TDRPC 1.04(f)
  from `Documents\Knowledge Repo\texas-disciplinary-rules-of-professional-conduct-pdf.pdf`
  (FC-14's fourth SOURCING channel; TDRPC eff. 3/7/2025) — **the registry's first practice-of-law
  proposition; the cases-vs-practice boundary question (Q-STAT-5 stack) stays open and this entry
  decides nothing about it.**
- Every quotation spot-checked against raw extraction. Every entry enters **UNVERIFIED**; only
  Michael verifies. IDs packet-local; minting durable IDs is Michael's. Scope notes assert
  nothing not in the quoted text. **The 1.04(f) entry states 1.04(f) only — nothing about
  1.04(g) was ruled; do not add it.**

## §2 Task 2 — the Q-WS3-5 five-lead read (retrieval only)

Ruled at #108 (pick verbatim: "Run the second pass"): read *City of Dickinson*, *CSX*, *Baytown
Nissan*, *Pope*, and *Jimenez* — chiefly to test whether the Texas Supreme Court has touched the
comment-9 limit on TRCP 192.3(h).

Binding terms:
- FLP/CourtListener plus named public sources, **each source named per item** (TOOLING; Descrybe
  is OUT). A vendor AI research assistant is a locator, never authority.
- **V-9 discipline in full:** majority identified only from the court's own document, a paginated
  vendor copy stating authorship on its face, or Michael's own identification put to him and
  recorded; otherwise "cannot identify — STOP," flagged, never staged.
- Characterize an opinion only after reading the relevant passages in full. Output: a read-record
  doc (`docs/specs/ws3-second-pass-read-<DT-1 Central date>.md`), candidates stated as UNADOPTED —
  **adoption and any change to the now-VERIFIED WS-3 entry are Michael's ruling; rewording a
  VERIFIED entry detaches verification.** Retrieval is not verification; the read verifies
  nothing and closes nothing.

## §3 Close-out (both tasks)

RR-1 re-read against any rulings Michael makes mid-session. **PF-1 is MANDATORY before the packet
ships** — this is a legal-content packet; two independent read-only adversarial agents design-side,
pre-`inbox/`, result recorded in the packet. DT-1: stamp Michael's Central wall-clock date (the
container clock is UTC; the ~19:00-Central-to-midnight window reads a day ahead). Package via
push-to-code: one zip to the repo checkout's `inbox\` on the machine that will run the queue,
verified by re-listing; destination paragraph NAMES the machine (MM-1 — inboxes never sync).
Session-log entry design-minted from the ceiling at HEAD. Nothing builds; §5 NONE.
