# FIRM-OBLIGATIONS FIX SLICE — `FOS-2` — the hands-on sitting's rulings, in two groups

**Status:** RULING-class (`CAP-2`). Canonical repo path `docs/specs/firm-obligations-fix-slice.md`. Drafted 2026-09-12 (Central, DT-1) by THE FIRM-OBLIGATIONS HANDS-ON SITTING (`#156`, Fable 5.1, typed) from its running ledger, AFTER every ruling of the sitting; the rulings record is `docs/specs/firm-obligations-hands-on-rulings-2026-09-12.md` (RULING, filed by the same packet), cited by its own §. **Authorization: `FOS-2` — see §11.** Nothing in this document is built by the sitting that wrote it; a build happens only in a fresh Code session fired from `docs/prompts/PROMPT-firm-obligations-fix-slice-build-session.md`, and only on `FOS-2` ruled affirmatively at HEAD. Two adversarial read-only audits ran on the first draft (twenty-nine findings, four HIGH — the column count, the reminder cap, a missing deliverable, an unsupported R15 claim); every HIGH and MEDIUM is fixed in this text and the record is `docs/record/firm-obligations-hands-on-2026-09-12/adversarial-audit-2026-09-12.md`.

## 0. HOW TO READ THIS DOCUMENT — four provenance classes, and the rule for each

- **RULED** — Michael's pick, quoted from the rulings record (`#156`) or from `#155`/the build's four stops. Binding as written.
- **FACT AT HEAD** — read from the repo at `5781420` (staged read-only copies; the bridge VM shell could not mount the folder, so no `git` command ran and no `src/` file was read this sitting — `src/` facts below come from the 2026-09-12 CODE entry and the 2026-09-07 `CCS-1` CODE entry, named as such). The build session re-verifies every one at its own HEAD before writing a line (§9 step 2).
- **BUILD DEFAULT** — a decision the record did not make; each carries an `FXD-n` id, is PROVISIONAL, is taken as named and reported in the build's session entry by number, none "improved". The precedent is the `FOD-`/`SD-` series.
- **TEXT ACT** — an on-screen string; PROVISIONAL with its cite until Michael confirms it at a hands-on sitting. Every string this slice adds is marked in code exactly as the `FOS-1` build marked its strings (the AST-walking test extends to them).

The same rule as `FOS-1`: **a default is reported, never approved by silence**; a reading the text does not make is a STOP if it changes what a ruling means, and a default otherwise.

## 1. THE STATE THIS SLICE IS SPECIFIED AGAINST (HEAD `5781420`, read 2026-09-12)

- The firm-obligations slice (`FOS-1`) is BUILT — commits `7c68eb9` (vitest excludes `docs/**`), `0cd201c` (the build) and `e046906` (the review's fixes and the close-out), per the 2026-09-12 CODE entry. **`5781420` is the local HEAD and the local `origin/master` tracking ref as read this sitting — origin itself was NOT read (no `ls-remote` was possible); the build entry's own live `ls-remote` was of `cab3b1c` at its Step 0.** Fixture-only in demo mode; `STORE_VERSION = 17`; `SCHEMA_TABLES` 48; tests 1,081 / 56 files; `npm run build`, `npm run lint` (4 warnings, all in the filed mock), `npx tsc -b` all exit 0 (the build entry's HEALTH, AFTER).
- **`db/migrations/2026-09-10-firm-obligations.sql` (23,843 B) is WRITTEN AND NOT RUN — Michael's word this sitting, 2026-09-12: *"Not run"*.** Because it has not run, this slice AMENDS THAT FILE in place (§5) rather than writing a second migration: the ruled order is sitting → migration → his real activations, and the standing bar on amending a migration that has run does not reach an unrun one. The file's gate (`firm_obligations` must not exist) is unchanged, so a second run still stops before any statement.
- `docs/spec-feedback.md`'s `## 2026-09-12` section (parts A–C) is the return this sitting ruled on. Its part C items map to `#156` as follows: item 1 → A8; items 2, 9, 10 → accepted as built (the card, `#156` §1 item 2); item 4 → confirmed as built (§1 item 4a); item 5 → ruled as built (§1 item 4b); item 6 → A5; item 7 → A6; item 8 → A7; item 11 → closed, double activation stays allowed (§1 item 4b); item 13 → closed, the override keeps "due date" (§1 item 4a); item 14 → A7; item 15 → closed by consequence of A5 (§1 item 9); item 16 → the `#156` packet's exact-match correction of the kickoff prompt (no build act). **Items 3 (the condition-not-label guard) and 12 (the same-name calendar guard) were NOT put at `#156`: both are guards the build reported, and this slice leaves both as built.**
- The `CCS-1` slice is BUILT (2026-09-07); its two migrations RAN on LIVE by Michael's hand 2026-09-07 (`#150`). The amendment slice (FE-D1A) is BUILT (2026-09-03). The disclosures paragraph writer is the FIXTURE writer in both modes; no vendor; `H12-v` untouched — and this slice touches none of that.
- **The `R15` scalars `{instrument_title}` and `{footer_title}` exist and "resolve nothing today"** (`spec-feedback.md`, THIRD TRANCHE item 10) — the master `.docx` carries the static heading `PLAINTIFF'S 194.2(b) & 195.5 DISCLOSURES` **twice** and no token for it (same item). The ruled `R15` title shape is the `CCS-1` slice §2.2's (the responding plaintiff named ONLY on a multi-client case; a one-client case keeps the static form). What the one-client scalar value must be, once the master carries the tokens, is stated at §3 item 16.
- The instructions field is v32 (live read at this sitting's open); v33 is delivered by `#156` (`CAP-2a`, trigger 3) and does not gate this slice.
- Demo mode runs by `npm run dev:demo` on port **5175** (`package.json` at HEAD: `vite --mode demo --port 5175`); the Entra redirect is `localhost:5173` (BUILD-STATE's auth-surface line), so any live Outlook check from the demo app needs `npx vite --mode demo --port 5173` (a command, not a tracked-file edit — the `FOS-1` build's temporary `launch.json` edit is not repeated).

## 2. THE RULINGS THIS SLICE BUILDS — pointer table (the record is `docs/specs/firm-obligations-hands-on-rulings-2026-09-12.md`)

### 2.1 Group A — firm obligations (`#156` §1)

| # | Ruling (his pick, verbatim) | Record | What it changes |
|---|---|---|---|
| A1 | *"That looks correct."* on the reminder composite; earlier, verbatim: *"I do not want all of the events to carry a reminder."* | `#156` §1 item 6(b) | DECISION 7's reminder limb AMENDED: hard items only; 30 days before the target by default (or the lead if the lead is shorter than 30); a per-obligation Outlook-reminder-days field, raised by hand on the long-think rows. `FOD-29` re-measured from the reminder day. |
| A2 | *"Keep it, retitled, but kill the reminder"* | `#156` §1 item 6(a) | `FOD-22` AMENDED: Done PATCHes the subject to "Done — …" AND sets `isReminderOn: false`; Undo restores both. |
| A3 | *"Add the column (Recommended)"* | `#156` §1 item 9, spec-feedback part B item 1 | `firm_obligation_occurrences.materialized_from uuid` + `touched boolean not null default false`; Undo's test reads columns, not the log blob. |
| A4 | *"Tighten it (Recommended)"* | `#156` §1 item 9, part B item 2 | The row CHECK becomes `((outcome is not distinct from 'not-applicable') = (outcome_reason is not null))`. |
| A5 | *"Build the RPC functions now"* | `#156` §1 item 9, part C item 6 | One Postgres function per act (§5.3); the Supabase adapter calls them; compensation code retired for the acts they cover; part C item 15 closes by consequence. |
| A6 | *"Retry on next sync (Recommended)"* | `#156` §1 item 9, part C item 7 | The orphaned Outlook event id from an Undo is kept and the sync drain deletes it when Outlook is next connected. |
| A7 | *"One act, one line (Recommended)"* | `#156` §1 item 9, part C items 8 and 14 | One adapter act `activateFromInactive` (edit + re-activate), one `review_log` line, one RPC function. |
| A8 | *"(A) Yes — settings are editable any time"* | `#156` §1 item 1, part C item 1 | NO code change. Slice §8 and §7 item 21 of `firm-obligations-build-slice.md` are amended by the `#156` packet's exact-match orders; this slice's own §8 carries the amended words. |
| A9 | *"Register and card are enough"* | `#156` §1 item 6(c), part B item 14 | NO code change; the `FOM-8` staleness limitation is closed. |

### 2.2 Group B — disclosures / form engine (`#156` §2)

| # | Ruling (his pick, verbatim) | Record | What it changes |
|---|---|---|---|
| B1 | *"Don't add the sentence"* | `#156` §2 (D-8) | The provider block never adds "Currently practicing at …"; the mismatch panel line (amendment slice §8.2 line 17) stays. D-8's sentence path removed. |
| B2 | *"No episode sentence at all"* | `#156` §2 (D-18) | The custodian-only shape places §9.11's two sentences whole with `{facility_name}` and nothing else; the writer returns NO part for that shape (`care_episode_clause` retired from the contract). |
| B3 | *"Pause still fires, then designate"* (after *"Designate under the treating paragraph"*) | `#156` §2 (AS-Q17) | A `mental health`-MARKED individual at a facility of another type: the §5.1 hard pause fires on the marker as built; once cleared, the individual IS designated in the treating paragraph under its causation sentence, and in its LEAD — the amendment slice §8.3 / §15 `AS-Q17`-default EXCLUSION is removed. (A facility TYPED mental health is `AS-Q5`'s ruled pattern, ruled at `#146`, not put at `#156`, unchanged.) |
| B4 | *"Fix — print it once"* | `#156` §2 (THIRD TRANCHE item 8) | At N = 0 the 195.5 block emits the custodian line ONCE — his words: the custodian line is suppressed when the top line already is it; the mechanism is `FXD-6`. |
| B5 | *"Have Code tokenize the master"* | `#156` §2 (THIRD TRANCHE item 10) | The Code session edits Michael's master `.docx` at EXACTLY the two static heading spots (FACT AT HEAD: `PLAINTIFF'S 194.2(b) & 195.5 DISCLOSURES`, "present twice, carrying no token" — THIRD TRANCHE item 10), replacing them with `{instrument_title}` and `{footer_title}` and nothing else; D-61/R15 then reaches the served heading. |
| B6 | *"The Providers section's list (case_providers)"* | `#156` §2 (THIRD TRANCHE item 11) | The new-bill form's provider picker reads the case's `case_providers` facilities (the Medical tab's list), not linked `providerBusiness` parties. |

### 2.3 What `#156` ruled that this slice does NOT build (each with its home)

- `FO-6` — a firm-wide calendar page is WANTED, *"Firm-wide calendar page, later"* — a separate later slice; the row is 🟡. **Not here.**
- Per-paragraph regenerate — *"Yes, later slice"* — a separate later slice; the row is 🟡. **Not here.**
- `AS-Q14` (b), `AS-Q15`, `AS-Q16` — ruled as their build defaults; nothing to build.
- THIRD TRANCHE item 9 (the 14-token registry stub) — not widened; stays recorded.
- `CAP-2a` — a convention, not code.
- The leads — *"Stand as shipped"*; nothing to build.
- Every text act CONFIRMED at `#156` — the marker in code stays; only the record's status changes (the `#156` packet's exact-match orders on the build slice's text-act inventory).

## 3. SCOPE — IN (numbered)

**Group A — firm obligations**

1. **`firm_obligations.outlook_reminder_days integer not null default 30 check (outlook_reminder_days >= 0)`** (A1) — the per-obligation Outlook reminder lead, in the UNRUN migration, `db/schema.sql`, the domain type (`outlookReminderDays`), both adapters, the activation form (catalog and custom), the edit form, and the local store's v18 step (item 9). Templates carry NO per-template value (`FXD-1`); the form PRE-FILLS `min(30, leadDays)` (`FXD-9`).
2. **The reminder rule (A1), stated once so it can be tested:** for an occurrence of obligation O with target T (§2.3 of the build slice), `reminderOn = T − O.outlookReminderDays` days — the field is what fires, so a value he raises by hand takes effect exactly (his composite's limb 4); the composite's "or at the lead if the lead is shorter than 30" is honoured by the PRE-FILL (`FXD-9`), not by a cap. The Outlook event carries `isReminderOn = (O.weight === 'hard')` and, when true, `reminderMinutesBeforeStart` = the real minutes between 00:00 local on `reminderOn` and 00:00 local on T (the `FOD-29` stop-3 rule, re-based from `lightsOn` to `reminderOn`; month-precision rows use their T exactly as day-precision rows do — `FXD-11`). A routine obligation's event carries `isReminderOn: false` and `reminderMinutesBeforeStart: 0` (`FXD-8`). `lightsOn` and every register/card state are UNCHANGED — the lead remains the register/card window. A value above the lead is ALLOWED (it rings before the row lights) and the form shows a hint (`FXD-9`).
3. **Done and Undo on the Outlook event (A2):** `markOccurrenceDone` PATCHes the event to subject "Done — <subject>" AND `isReminderOn: false`; `markOccurrenceNotApplicable` does the same (`FXD-10`); `undoOccurrence` restores the original subject AND `isReminderOn` per item 2 (true only if hard). The existing PATCH path is extended; no new event is created.
4. **`firm_obligation_occurrences.materialized_from uuid references firm_obligation_occurrences (id) on delete set null`** and **`touched boolean not null default false`** (A3), in the UNRUN migration and `db/schema.sql`. `materialized_from` is set on the occurrence a close materializes (the "next"); `touched` flips true on the first edit, done, override or re-activation that reaches that next. Undo's test (`FOD-7`, `FOM-11`) reads: the closed occurrence's next exists with `materialized_from = closed.id` and `touched = false`. The JSON record the `FOS-1` build wrote into the close line's `review_log.new_value` (its key name as built — the build entry says only "the undo link lives in the close line's `review_log.new_value` trail") is KEPT as a record but is no longer read by Undo. The local adapter mirrors both fields on its occurrence shape.
5. **The row CHECK (A4):** `check ((outcome is not distinct from 'not-applicable') = (outcome_reason is not null))` replaces `check ((outcome = 'not-applicable') = (outcome_reason is not null))` (the file's line as read) in the UNRUN migration and `db/schema.sql` — an open row (outcome NULL) can no longer carry a stray reason. The file's inline comment beside it ("STATED, NOT FIXED: the third is three-valued …") is removed with the fix.
6. **One Postgres function per act (A5), §5.3** — nine: `firm_activate` (catalog or custom activation), `firm_activate_from_inactive` (A7 — the Inactive "Activate…": patch + re-activate in one act), `firm_update` (an edit on an active row), `firm_retire`, `firm_reactivate` (a retired row's open occurrence gaining Re-activate — the act the `FOS-1` build reports as "Re-activate replaces Retire on a retired row's open occurrence"; NOT the Inactive path), `firm_mark_done`, `firm_mark_not_applicable`, `firm_undo`, `firm_set_due_override` — each `security invoker`, `language plpgsql`, taking a jsonb argument the adapter builds from the domain's PLANNED act (the domain stays the decider; the function applies the plan atomically and writes the ONE `review_log` line — `FOD-6`, §7 item 16). The Supabase adapter calls each by `rpc()`; the compensation code (UPDATE-restore, "could not restore", "saved without its log line") is RETIRED for these acts; the three message classes collapse to ONE — "not saved" — with the function's error text. `GRANT EXECUTE … TO authenticated` and `REVOKE EXECUTE … FROM public, anon` on each (the `F-1` precedent: `revoke execute on function next_file_number() from public`). The local adapter keeps its atomic in-memory acts and gains the same `activateFromInactive` act (item 7).
7. **`activateFromInactive(id, input)` on BOTH adapters (A7):** one act that applies an edit patch and re-activates in one plan, writing ONE `review_log` line (`action: 'edited'`, `entityType: 'firm_obligation'`, `new_value` carrying both the patch and the re-activation) — the Inactive "Activate…" form calls it; `reactivationProblem`'s dry run stays as the page's pre-check. The two-line path (`updateFirmObligation` then `reactivateFirmObligation`) is removed from that form.
8. **Undo's Outlook delete retried (A6):** when Undo's Graph delete fails or Outlook is not connected, the removed next occurrence's `outlook_event_id` is written to a new `firm_obligations.pending_outlook_deletes jsonb not null default '[]'::jsonb` (an array of `{ eventId, occurrenceId, recordedAt, attempts }`; UNRUN migration + `db/schema.sql` + local store), and the sync drain's firm half deletes each and removes it from the array on 2xx or 404 (`FXD-2`); the register's "not deleted in Outlook" warning becomes "queued to delete on next sync" (TEXT ACT). Retrying never stops (`FXD-2`).
9. **Store v18:** the two new obligation fields (`outlookReminderDays` 30, `pendingOutlookDeletes` `[]`) and the two new occurrence fields (`materializedFrom` back-filled from each done occurrence's close-line JSON where present, else null; `touched` false — `FXD-7`), a literal `18`, a `-backup-v17`, every older version chained through it; the `FOD-21` fixture seeded with `outlookReminderDays: 30` on every row.
10. **Text acts, all PROVISIONAL, all marked with their cite:** the activation/edit forms' "Outlook reminder" field label and its above-the-lead hint (A1, `FXD-9`), the routine row's "no Outlook reminder (routine)" line in Details (A1), the "queued to delete on next sync" warning (A6), the single "not saved" message class (A5). Every string carries `#156` and its item as the cite.
11. **Tests** — every invariant of §7; the trio green; `npx tsc -b` clean.

**Group B — disclosures / form engine**

12. **D-8's sentence path removed (B1):** `src/forms/` no longer emits "Currently practicing at …" beneath the block's phone; the mismatch panel line (§8.2 line 17) is unchanged; the test that pinned the sentence is inverted to pin its ABSENCE.
13. **D-18 retired (B2):** the custodian-only shape's part contract drops `care_episode_clause`; the writer is asked for no part on that shape; §9.11's two sentences are placed whole with `{facility_name}`; the fixture writer's custodian-only fixture returns nothing for it; the structural test pins "nothing between §9.11's sentences".
14. **AS-Q17 (B3):** the §5.1 hard pause on an individual's effective `mental health` marker at a facility of another type is UNCHANGED; once the pause is cleared, that individual is INCLUDED in the treating paragraph's designation, under its medical-causation sentence, and in its LEAD like any other individual — the exclusion the amendment slice states at §8.3 and as `AS-Q17`'s §15 default is removed; D-65's block membership is unchanged (the individual was already on the block); AS-Q5's facility-TYPE rule is unchanged. Panel line 11's marker-limb wording changes from "paragraph drafted by hand in Word" to `FXD-5`'s PROVISIONAL string (TEXT ACT).
15. **The duplicate custodian line (B4):** at N = 0, when `blockItem()`'s `topLine` IS the custodian line, `custodian_line` is emitted EMPTY (`FXD-6` — the mechanism for his "suppressed"); the walk's exhibit (a designated facility with no individuals) renders "Custodian of Records" once.
16. **The master `.docx` (B5):** the Code session edits Michael's master skeleton at EXACTLY two spots — the two occurrences of the static heading `PLAINTIFF'S 194.2(b) & 195.5 DISCLOSURES` — replacing the one at the instrument's head with `{instrument_title}` and the one at its foot with `{footer_title}`; the edit is by the repo's own DOCX XML tooling (the `document.xml` / `merge_runs` precedent in the form engine's ZIP/docx layer); a byte-diff of the document before and after is filed as EVIDENCE showing those two runs and nothing else changed. **AND the two scalars are made to resolve:** on a one-client case each resolves to the static heading's exact text (`PLAINTIFF'S 194.2(b) & 195.5 DISCLOSURES`), so a one-client instrument renders byte-identically to today; on a multi-client case each resolves to the ruled `R15` title (the `CCS-1` slice §2.2's shape). **STOP if the master holds the heading anywhere other than exactly twice, if the two are not one at the head and one at the foot, or if either sits inside a run the tooling cannot replace cleanly.** The master's path is read from `docs/skills/drafting-disclosures/SKILL.md` / the form engine's template record, never guessed.
17. **The new-bill form's picker (B6):** reads the case's `case_providers` rows (facility party id + label), the Medical tab's list; `R7`'s pre-fill then works from the same row; the linked-parties source is removed from that form only.
18. **Tests** for items 12–17; the trio green.

## 4. SCOPE — OUT, each with its home

- A firm-wide calendar page (`FO-6`, 🟡) → a later slice, not authorized.
- Per-paragraph regenerate (🟡) → a later slice, not authorized.
- Any per-template Outlook-reminder value, any weekend-rule default, any registry entry → DECISION 8's Opus act (PF-1 there).
- The token registry widening (THIRD TRANCHE item 9) → his call, unruled.
- The Outlook first-edit duplication defect → inherited, unchanged.
- RPC for the CASE calendar path or any other module → nothing; this slice's functions cover the nine firm acts only.
- Running the migration, connecting to any database, seeding anything in Supabase mode → his hand, after this slice lands.
- `AS-Q4`'s split-store slice (`Q-API-20`, "After") → its own slice.
- Any change to `toGraphEvent`'s case path, `calendar_events`, `cases`, any matter table → DECISION 3; untouched.
- The `FOS-1` build's test named for the §8-against-`FOD-4` conflict → left as is (A8 is no code change); renaming it is a build-session nicety, not an act of this slice.

## 5. THE DATA MODEL — the UNRUN migration `db/migrations/2026-09-10-firm-obligations.sql`, AMENDED IN PLACE

### 5.1 `firm_obligations` — two columns added
`outlook_reminder_days integer not null default 30 check (outlook_reminder_days >= 0)` (item 1) · `pending_outlook_deletes jsonb not null default '[]'::jsonb` (item 8). The rest of §5.1 of the build slice is unchanged.

### 5.2 `firm_obligation_occurrences` — two columns added, one CHECK replaced
`materialized_from uuid references firm_obligation_occurrences (id) on delete set null` · `touched boolean not null default false` (item 4). The third row CHECK becomes `check ((outcome is not distinct from 'not-applicable') = (outcome_reason is not null))` (item 5). The partial unique index, RLS, policy, GRANT, triggers are unchanged. **Four columns in all across the two tables.**

### 5.3 The nine functions (item 6)
Each `create or replace function public.firm_<act>(p jsonb) returns jsonb language plpgsql security invoker`, applying the domain's PLANNED act — the rows to insert/update/delete and the one `review_log` row — inside the function's own transaction; raising with a plain message on any refusal the plan did not already exclude (the one-open index, the CHECKs); returning the affected ids. `grant execute on function public.firm_<act>(jsonb) to authenticated; revoke execute on function public.firm_<act>(jsonb) from public, anon;` for each. **RLS still applies inside them (`security invoker`).** The functions are listed by name in the file's header and each gets a foot check.

### 5.4 The file's shape, amended
The gate is unchanged. STEP 0 unchanged. **The header:** its `Authorization:` line gains `FOS-2` beside `FOS-1`; a WHAT THIS DOES list is ADDED above the existing `WHAT THIS DOES NOT DO, deliberately` list, naming the four columns, the replaced CHECK and the nine functions; the `STATED, NOT FIXED` comment beside the outcome-reason CHECK is removed (item 5). **Foot checks become TEN:** the seven as written, plus (8) the four new columns present with their defaults (`information_schema.columns`); (9) the nine functions exist and `has_function_privilege('authenticated', 'public.firm_<act>(jsonb)', 'EXECUTE')` true ×9 while `anon` false ×9; (10) `pg_get_constraintdef` on the occurrences table's outcome-reason CHECK reads `is not distinct from`. `db/schema.sql` carries every object in the same commit. RUN BY MICHAEL'S HAND, after a backup, pasted ALONE, ten checks answered in words.

## 6. NAMED BUILD DEFAULTS — every one PROVISIONAL (`FXD-` series; collision check this sitting: `grep -r 'FXD-'` over the fifteen staged repo files and a project-knowledge search — 0 hits; NOT repo-wide, because no shell reached the checkout; the build re-checks repo-wide at Step 2)

| ID | Default | Why it is a default |
|---|---|---|
| **FXD-1** | Every catalog template pre-fills `outlookReminderDays` from `FXD-9`'s rule; no template carries its own value. | A1 named 30 as THE default; per-template values would be a design act (the leads precedent, "Stand as shipped"). |
| **FXD-2** | `pending_outlook_deletes` is a jsonb array of `{ eventId, occurrenceId, recordedAt, attempts }`; the drain removes an entry on 2xx or 404 and otherwise increments `attempts` and keeps it; retrying NEVER stops; an entry with `attempts ≥ 3` is ALSO named in "Needs attention" so he can delete by hand if he prefers. | A6 ruled the retry, not its shape. |
| **FXD-3** | An occurrence whose `reminderOn` is already past at push time is sent as computed (a past fire time) — no adjustment — per A9. | A9's "register and card are enough". |
| **FXD-4** | The RPC functions take and return jsonb; the Supabase adapter maps the domain's plan to one call per act; the local adapter keeps its in-memory acts and adds only `activateFromInactive`. | A5 ruled the functions, not the calling convention. |
| **FXD-5** | Panel line 11's marker-limb wording → *"<Name> is marked mental health at <Facility> — designated in the treating paragraph once this pause is cleared."* | TEXT ACT — his eye. |
| **FXD-6** | The custodian line is suppressed at N = 0 by emitting `custodian_line` EMPTY; the master's two token spots stay; an empty token renders nothing. | B4 ruled the suppression; the mechanism is the build's. |
| **FXD-7** | Store v18 back-fills `materializedFrom` from the close-line JSON where a done occurrence's `new_value` carries it; occurrences without it get null and Undo is therefore refused on them (the conservative reading of `FOD-7`). | Fixture data only in demo mode; nothing real exists. |
| **FXD-8** | A routine obligation's Outlook event carries `isReminderOn: false` and `reminderMinutesBeforeStart: 0`; Graph's default reminder is not relied on. | A1's "hard items only". |
| **FXD-9** | The "Outlook reminder" field PRE-FILLS `min(30, leadDays)` at activation (catalog and custom) and is editable to any value ≥ 0; the reminder fires at exactly the stored value; a stored value greater than the lead is allowed and the form shows a hint *"rings before this row lights on the register"*. | The composite's limbs 3 and 4 read together: the lead-cap is the DEFAULT's, and a hand-raised value must take effect or the field is pointless. **Put to Michael in the `FOS-2` question as a reading.** |
| **FXD-10** | Not-applicable treats the Outlook event as Done does: subject "Done — …", `isReminderOn: false`; Undo restores. | A2 spoke of Done; Not-applicable closes the period the same way (`FOD-18`) and leaving its reminder ringing would be the defect A2 fixes. |
| **FXD-11** | Month-precision rows compute `reminderOn` from their T exactly as day-precision rows do; `FOM-6`'s earlier-of rule stays with `lightsOn` only. | `FOD-29`'s month limb was about lighting; the reminder is now its own day. |

## 7. TESTS AND INVARIANTS — each traceable

1. **Reminder rule (A1):** hard row, lead 180, reminder days 30 → `reminderMinutesBeforeStart` = the real minutes between 00:00 on T−30 and 00:00 on T (43,200 outside a clock change; 43,260 / 43,140 across one); hard row, lead 5 → pre-fill 5 → 5 days; hard row raised by hand to 60 with lead 30 → 60 days (rings before lit); routine row → `isReminderOn: false`; the register/card states unchanged for every fixture row (a snapshot of `stateOf(today)` before and after the slice is byte-equal).
2. **Done/Undo on the event (A2, `FXD-10`):** Done and Not-applicable → subject "Done — …" and `isReminderOn: false`; Undo → original subject and `isReminderOn` per weight.
3. **Undo's test on columns (A3):** `materialized_from` set on the next; `touched` flips on edit/done/override/re-activation; Undo allowed iff next exists untouched; refused otherwise; a done occurrence whose next was created before v18 without the JSON link → Undo refused (`FXD-7`).
4. **The CHECK (A4):** a text assertion that the migration and `db/schema.sql` both read `is not distinct from`; the domain refuses an open row with a reason (already true).
5. **RPC (A5):** the Supabase adapter's fake proves one `rpc()` call per act and NO compensation branch reached; a failing `rpc()` yields exactly "not saved" with the function's message; the migration text names all nine functions and the GRANT/REVOKE pair for each; the compensation-branch tests in the firm adapter's test file are REPLACED by the RPC-shaped tests — every pin unrelated to compensation (review-log action and entity type per act, the drain surviving a failing read, etc.) is KEPT.
6. **`activateFromInactive` (A7):** one act, one `review_log` line on both adapters; the page's dry-run pre-check still fires; no two-line path remains (a grep-shaped test for the retired call sequence).
7. **Pending deletes (A6, `FXD-2`):** Undo with Outlook disconnected → one entry queued; next drain with a mocked 204 → entry removed and the event delete called once; 404 → removed; a failure → `attempts` incremented, entry kept; at 3 → also named in "Needs attention"; a later success still removes it.
8. **Store v18:** a v17 store comes through with the four new fields present; the `FOD-21` fixture carries 30 on every row; the step is pinned to a literal `18`; the v9→v18 chain runs forward.
9. **D-8 (B1):** the block never contains "Currently practicing at"; the mismatch panel line still fires on the two-edge fixture (invariant 17 of the amendment slice).
10. **D-18 (B2):** the custodian-only paragraph is exactly §9.11's two sentences with the facility name; the writer receives no request for that shape; the structural test's "nothing between" pin.
11. **AS-Q17 (B3):** the marked individual at an EM facility: pause fires; after clear, the individual is in the treating paragraph's LEAD and designation; a facility TYPED mental health still writes no generated paragraph (AS-Q5, unchanged by this slice).
12. **Custodian once (B4):** the walk's exhibit renders "Custodian of Records" exactly once at N = 0; at N ≥ 1, on a fixture with NO D-8 mismatch edge, the block is byte-identical to today.
13. **The master (B5):** a text assertion that the master's `document.xml` contains `{instrument_title}` once and `{footer_title}` once and the static heading zero times; a one-client generation on a fixture containing NO custodian-only shape and NO D-8 edge is byte-identical to a pre-slice generation (the static text resolved from the scalar); a multi-client generation's heading carries the ruled `R15` title.
14. **The bill picker (B6):** on the Garcia matter the picker lists the nine `case_providers` facilities; `R7`'s pre-fill fills from the picked row (asserted in code; the `Combobox` automation limit of THIRD TRANCHE item 12 is noted, so a source-asserted result is acceptable and said so).
15. **The migration file** names every object of §5 (a text assertion); the SQL is run only by Michael's hand — stated, not tested.
16. **`SCHEMA_TABLES`** unchanged at 48; the probe's policy list unchanged; the sequence test green.
17. **Text-act marker test** extended to every string this slice adds.

## 8. DO NOT — the record's list, gathered with its cites, plus this slice's own

- **Do not build a snooze, a "later", a dismiss, a bulk action, or any control that unlights an occurrence other than Done, Not-applicable (conditional rows only), Undo — AND `FOD-4`'s edits on a NOT-past-due occurrence (a lead, rule or override edit may unlight or re-date such an occurrence — a weekend-rule edit routes through the same re-evaluation, as built; nothing may move a past-due occurrence's D later)** — `FO-2`, `FOD-18`, `FOD-19`, DECISION 1, **`#156` item 1 (A8)**.
- Do not compute a holiday, and do not ship a holiday list — `FOM-12`(b). Do not set any template's `weekendRule` to anything but `unknown`; do not infer a roll from a cite or a source note — §2.3, DECISION 8.
- Do not print "overdue" or a day count on a weekend-dated occurrence whose `weekendRule` is `unknown` — `FOM-12` option 1; `#156` item 2 accepted the build's reading.
- Do not draft or insert a registry entry; do not create `legal-rule-registry-firm-obligations.md`; do not reword any SPEC §7 cite string — DECISION 8.
- Do not add a money field, `externalRef`, `ledgerRef`, or any QuickBooks reference — DECISION 5. Do not add an assignee, a delegated marker, a per-user fan-out, or an owner badge — DECISIONS 1 and 4, `FOM-15`.
- Do not request `Tasks.ReadWrite` or any new consent; do not build a To Do task — DECISION 7. **Do not change which calendar the event lands on, its all-day shape, or its target date** — DECISION 7; only the reminder limb is amended (A1, A2).
- Do not put a firm occurrence on any case's Calendar tab; **do not build a firm-wide calendar page — `FO-6` is 🟡 for a LATER slice, not this one.**
- Do not touch `calendar_events`, `cases`, `case_parties`, `party_pii`, any matter table, or `toGraphEvent`'s case path — DECISION 3.
- Do not fix the Outlook first-edit duplication defect here.
- **Do not run the migration; do not connect to any database; do not write a SECOND migration for these columns — amend the UNRUN file in place (§5), and STOP if the live log at HEAD shows it has run since this slice was written.**
- Do not seed anything active in Supabase mode; do not enter, guess or infer a real date or fact about the firm.
- Do not delete a done occurrence or an obligation; retire, never delete — `FOD-8`, `FOD-23`. (Undo's delete of the untouched next is REQUIRED — §7 item 8 of the build slice, `FOD-7`; the kickoff prompt's earlier "no delete" restatement is corrected by the `#156` packet's exact-match order, spec-feedback part C item 16.)
- Do not wire a vendor, call a model, or pass any record through a model path — `H12-v`. **Group B changes the fixture writer's part contract only; `resolveParagraphWriter()` still returns the fixture writer in both modes.**
- **Do not edit any run of the master `.docx` other than the two static heading occurrences (B5); STOP if the count is not exactly two or the placement is not head-and-foot.** Do not reword any §9 approved sentence; do not touch `form-engine.md`.
- Do not edit `docs/specs/firm-obligations-module-spec.md`, the ruling sheet, the rulings records, the companion, `Go_Live_Gates.md`, or any spec in `docs/specs/` (`BUILD-STATE.md` excepted); spec problems go to `docs/spec-feedback.md` as a new dated section.
- Do not regenerate `docs/record/session-log-toc.md`; do not write to `docs/specs/session-log-head.md`; do not mint an `#nn`, a queue row, or an ID beyond the `FXD-` defaults named here; do not double as the queue runner.
- Do not read "walked" as "live."

## 9. BUILD CHECKLIST — for the authorized session ONLY (`FOS-2` RULED YES), in this order

1. **STEP 0 (`QR-3`-shaped):** `git fetch origin`; `git ls-remote origin refs/heads/master` equals HEAD; on `master`; clean; `inbox/` listed (if non-empty, this session is NOT the runner — leave it). Confirm `FOS-2` ruled YES at HEAD by reading the `#156` entry's `FOS-2` line — the words, not a glyph. **Confirm the migration is UNRUN at HEAD: no entry in `docs/record/session-log.md` above `#156` records it run; if one does, STOP.**
2. **Re-verify §1** — every fact — at this HEAD, including the master's heading count and the current one-client value of the two `R15` scalars; `grep -r 'FXD-'` and `FOS-2` repo-wide; report deltas in the entry.
3. Baseline health: `npm test`, `npm run build`, `npm run lint`, `npx tsc -b`; exit codes read off the commands.
4. Group A, items 1–11, in that order; commit.
5. Group B, items 12–18; the master edit LAST, with its byte-diff filed as EVIDENCE under `docs/record/firm-obligations-fix-build-<date>/`; commit.
6. The whole-build adversarial review (the `FOS-1` shape: finder lanes + refuters), fixes, RE-SWEEP AFTER FIX; commit.
7. Click-through in demo mode on the fixture (both groups), reported per row; a fresh store AND a migrated v17 store.
8. Health, after; the session-log CODE entry (unnumbered, `TOC-6`) with every `FXD-` reported by number, every text act PROVISIONAL, every STOP recorded with his pick; BUILD-STATE rewritten in full under `CAP-4`; `spec-feedback.md` a new dated section if anything; push; `git ls-remote` after.

## 10. ON LANDING — what BUILD-STATE says

- **At the build's commit:** the fix slice BUILT, fixture-only; the ONE migration still WRITTEN AND NOT RUN, now carrying four new columns, one replaced CHECK and nine functions, ten checks; store v18; the master `.docx` tokenized at two spots (EVIDENCE path); `SCHEMA_TABLES` 48; tests/counts re-derived.
- **After Michael runs the migration:** his word, ten checks; then his real activations.
- **`FO-6` and per-paragraph regenerate:** 🟡, later slices, unbuilt.

## 11. THE AUTHORIZATION — `FOS-2`

**`FOS-2` — Does Michael authorize THE FIRM-OBLIGATIONS FIX SLICE at `docs/specs/firm-obligations-fix-slice.md`, both groups, scope IN (§3) and OUT (§4) as written, the UNRUN migration amended in place (§5), defaults (§6, `FXD-1`–`FXD-11`) as named — `FXD-9`'s reading of the reminder composite included — to be built by a fresh Opus Code session fired from `docs/prompts/PROMPT-firm-obligations-fix-slice-build-session.md`, the queue runner BARRED, fixture-only, the migration still not run by that session, the master `.docx` edited at exactly two spots?** His answer, when given, is recorded in `#156` verbatim; the prompt tests only his affirmative words in a log entry at HEAD naming this document. **Until that entry exists at HEAD, this document authorizes nothing.**
