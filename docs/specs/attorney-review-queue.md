# ATTORNEY REVIEW QUEUE — everything awaiting Michael's personal read or ruling

**Status:** WORKING CHECKLIST. Compiled 2026-07-26 in the design space, then updated the same day as items closed. **Reconciled to session-log #28 on 2026-07-28 (BS-1a item 10)** — §7 was added in that pass, and `BUILD-STATE.md` now points here instead of carrying its own roster. **Reconciled again to session-log #34 on 2026-08-07** (rulings batch: FE-1, FE-2, PR-3, QR-1, RR-1, D-CL2-3, CL2-AC-1; the FE series was absent from this file entirely and was added). **Reconciled again to session-log #38 on 2026-08-08** (contact-directory reframe: CD series added and CD-1 issued, FE-1 annotated superseded, FE-2 build home parked, CL2-AC-1 to direction-confirmed, CORPUS-HOME closed). **Reconciled again to session-log #39 on 2026-08-08** (QR-2 CLOSED; CL2-AC-1's two locations consolidated into the ID-bearing entry; two sweep items entered). **Reconciled again to session-log #44 on 2026-08-09** (queue-runner batch, eighth invocation: the T3/Phase 0 advisory H series added with full question text per QR-1, its H1–H6 collision with the heartbeat register flagged rather than renumbered; the P15/P1 runner-copy item narrowed to P15 only). **Reconciled again to session-log #46 on 2026-08-10** (queue-runner batch, ninth invocation: QR-4 entered with full text and CLOSED the same day it was proposed; the Q-3 `docs/skills/` sync-picker re-check entered with full text, having never been registered here; FE-1 annotated with the provider-data evidence channel, which now feeds CD-1). **Reconciled again to session-log #48 on 2026-08-11** (first REQ-1 REQ-CAPTURE: CD-2 framing ruled, FE-4–FE-7 and new IN series entered, §4 registry batch VERIFIED, templates home ruled, CD-1 path ruled as bounded-evidence-pass-then-session). **Reconciled again to session-log #58 on 2026-08-12** (queue-runner batch, twentieth invocation: REQ-CAPTUREs 4 and 5 — the **DE series added**, ruled into existence by Michael; CL-3, CD-3, FE-13–FE-17, IN-6, IN-7 entered with full question text per QR-1; FE-10, FE-11, FE-12 amended in place; CD-2 annotated; the FE-13–FE-17 gating question and the registry-verification backlog entered). **Reconciled again to session-log #59 on 2026-08-12** (the Uvalde docket-worksheet capture filed UNRECONCILED at §5 — sixth REQ-1 capture, first from CRIM DEFENSE, no durable IDs assigned and no registry entries opened, because it arrived by Michael's hand rather than through a packet). **Reconciled again to session-log #60 on 2026-08-12** (queue-runner batch, twenty-first invocation: the registry-placement flag CLOSED as confirmed, the REQ-10 placeholder-discipline carry given the queue row it never had, and the registry count reconciled at 27). **Reconciled again to session-log #62–#63 on 2026-08-12** (queue-runner batch, twenty-second invocation, two packets: the Uvalde capture RECONCILED and its row replaced by the new **CR series** CR-1–CR-11 plus the binding B&W constraint; registry backlog 27 → **33**; `party_type` retention CONFIRMED; **FE-gating CLOSED** and FE-8–FE-17 each annotated with its FE-D1 disposition; distillation candidates CLOSED into §13.4; **WF-2–WF-8** entered with gates; three form-engine §11 items registered that had no home; the GPU-telemetry item registered and ruled). **Reconciled again to session-log #65 on 2026-08-13** (queue-runner batch, twenty-fourth invocation: the cite-check memo pointer added add-only to the registry-backlog row; V-1, V-2, V-3 entered with full question text per QR-1). **Reconciled again to session-log #66 on 2026-08-13** (queue-runner batch, twenty-fifth invocation, the RULING RUN: ~20 rows closed or annotated in one pass — V-1, V-3, O1, O3, O5, Q-5, SAT-1, H22, D-CL2-3a, DL-INPUT, the demographics/OAA question, and the Go_Live_Gates paste-ask all CLOSED; O4, CL2-CHECK-1, the IME/204.1 row, and four heartbeat questions (H40, H50, H60, H77) annotated with their rulings while their rows stay open; **the T3/Phase 0 advisory series RE-LETTERED H → HK** by ruling, ending its collision with the heartbeat H1–H83 register; registry backlog **33 → 34** on the entry-4 split). **Reconciled again to session-log #68 on 2026-08-13** (both sweeps ruled and run: duplication CLOSED no-duplicate with three pointer glyphs stripped; status-drift CLOSED as a pass at findings #2–#9 with all fixes annotated; OBS-1 entered). **Reconciled again to session-log #72 on 2026-08-13** (queue-runner batch, twenty-eighth invocation: the registry workbook + citator pass filed as PROPOSED design input; **V-2 annotated add-only and NOT closed**; **V-4–V-8 entered with full question text per QR-1**; the packet's un-numbered `HK-*` device-bridge ask given the ID **HK-7** after collision-check). **Reconciled again to session-log #73 on 2026-08-13** (ruling run, same session as the batch that entered them: **V-4 through V-8 all RULED, one at a time** — keep-as-two, split-all-three, keep-and-reword, narrow-23, record-all-three — **every one of them EXECUTION-PENDING DESIGN-SIDE, no registry file and no design doc touched**; **V-9 opened** by the V-8 ruling, carrying the binding-rule gap the *Irwin* class exposed). **Reconciled again to session-log #74 on 2026-08-13** (queue-runner batch, twenty-ninth invocation: the CD-2 role mining pass filed as PROPOSED design input; the packet's packet-local **Q1–Q10 assigned durable IDs CD-4 – CD-13** after a repo-wide collision check found the CD series at CD-3, with full question text per QR-1; **HK-7 annotated SATISFIED-FOR-ONE-SESSION and deliberately NOT closed**, carrying the two new device-bridge findings that bear on QR-3 — CRLF-false-DIRTY and no-network-`git fetch`). **Reconciled again to session-log #75–#76 on 2026-08-14** (queue-runner batch, thirtieth invocation, two packets: the deadline-engine research memo and the statute pass both filed as PROPOSED design input; **`Q-STAT-1` – `Q-STAT-6` entered with full question text per QR-1**, collision-checked repo-wide with no prior `Q-STAT` ID anywhere; **the deadline memo's five questions entered with full text but WITHOUT durable IDs** — the packet forbade minting `DL-1` by name, `V-10…V-14` would collide with the unhyphenated V10–V17 residuals in §§1–2, and `DE` is the deficiency series, so they are filed under packet-local IDs and the ID assignment is itself a row below (**ID-DL-1**, and #59's Uvalde capture is the precedent for filing without durable IDs); **HK-7 annotated SATISFIED-FOR-ONE-SESSION a second time and again NOT closed**; two count corrections reported and applied to BUILD-STATE only — `RETRIEVAL: NOT RUN` is 21 rows not 20, and the registry is four files not three, with the **backlog unchanged at 34**). **Reconciled again to session-log #77 on 2026-08-14** (queue-runner batch, thirty-first invocation, one packet, no staged file: **Q-STAT-1 RULED and CLOSED — adopt as drafted**, its original question preserved verbatim beneath the ruling per QR-1, and **Q-STAT-2 through Q-STAT-6 left open by the packet's own instruction**; **two new rows entered — paste v18** (his hand; it supersedes the revised v17, **which was in fact pasted**, correcting a BUILD-STATE line carried since 08-13) **and the route-(c)-as-standing-law question**, which had no row here and is held for the Fable review pass). **Reconciled again to session-log #78 on 2026-08-15** (queue-runner batch, thirty-third invocation, one packet, no staged file, a correction to the record: the #76 Insurance-Code absence claim was **over-broad** — the fourth registry file was never searched and carries ENTRY 8 on Ins. Code ch. 1467 — so the two passages in `statute-pass-registry-retrieval-2026-08-14.md` were corrected **in place** and **Q-STAT-2 and Q-STAT-5 were ANNOTATED, not answered**; chs. 541/542 remain absent from all four files on a re-verified search, so **no candidate was withdrawn, no count moved, and the backlog stays 34**). **Reconciled again to session-log #79 on 2026-08-15** (queue-runner batch, thirty-fourth invocation, one packet: the chartered **record-integrity audit** filed as PROPOSED design input — 46 claims sound, 6 stale, **1 confirmed-false** (the carrier-duties "TWENTY VERIFIED" is 22 on disk), 1 contradicted, 11 checkable only by Michael; **OPEN-1 – OPEN-5 entered with full verbatim question text per QR-1** after a repo-wide collision check found no `OPEN-<n>` ID anywhere; **the paste-v18 row ANNOTATED with AUD-1 and deliberately NOT closed** — v18 appears already in force, and that is a design-side-only fact Code cannot confirm; **the route-(c) and Q-STAT-3 rows annotated add-only with the audit's two recommendations, neither closed and neither ruled. NOTHING from the audit was applied** — the stale figures stay in BUILD-STATE, each with a pointer, until OPEN-5 is ruled). **Reconciled again to session-log #80 on 2026-08-15** (queue-runner batch, thirty-fifth invocation, one packet: **CHAT-DISPATCH Task 7** — the Bexar local rules + eFileTexas memo filed as PROPOSED design input with its TIER A/TIER B provenance markers intact; **its six questions entered with full verbatim text per QR-1 but NO durable IDs** — `LR-`/`EF-`/`BX-` were checked free and deliberately not minted, so **ID-DL-1 now governs two packets** and was annotated accordingly; **H43 given a row here for the first time**, cross-linked to the heartbeat register rather than renamed; **HK-7 annotated satisfied-for-one-session a third time and again not closed**; the two `LR-LOOK` looks recorded as Michael's. **Nothing built, nothing verified, no registry file touched, backlog still 34, and none of the six repo docs the memo reconciles against was edited.**). **Reconciled again to session-log #81 on 2026-08-15** (queue-runner batch, thirty-sixth invocation, one packet: **CHAT-DISPATCH Task 8** — the three form-engine specs FE-4, FE-5 and FE-6 filed as PROPOSED design input with their TIER A / FLP provenance markers intact; **twenty-two questions and three named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-FE4-`, `Q-FE5-`, `Q-FE6-` and `FE5-LOOK-` re-checked repo-wide, all free, all deliberately not minted, so **ID-DL-1 now governs THREE packets**; **the FE-4, FE-5 and FE-6 rows annotated with their specs and left ⬜** — a spec is not a closure and the build is still unauthorized; **the headline is Q-FE5-9, a divergence inside a VERIFIED entry** — the TRCP 190.3(b)(3) entry's *"each party"* against the rule's *"any other party,"* different budgets whose gap scales with the number of opposing parties — **flagged, not reworded, and no registry file touched**; **HK-7 annotated satisfied-for-one-session a fourth time and again not closed**; Q-STAT-5, OPEN-4 and the route-(c) row annotated add-only, none closed; **one runner-found defect entered** — the FE-5 spec as staged is missing its title, status and §1 head, landed verbatim anyway and reconstructed by nobody. **Nothing built, nothing verified, backlog still 34, and `form-engine.md`, `fe-d1-build-slice.md` and every `legal-rule-registry-*` file were left untouched.**). **Reconciled again to session-log #82 on 2026-08-15** (queue-runner batch, thirty-seventh invocation, one packet, no staged file — the Fable disposition batch: **the paste-v18 row RULED and CLOSED ⬜→✅ (A-1, from AUD-1), original text preserved verbatim beneath it**, on the ruling and a design-side inference rather than on anything Code can verify. **No other row moved, and nothing else in this file was touched** — OPEN-1 through OPEN-5 stay open where the audit put them, the registry rows and `Go_Live_Gates.md` were fenced off by the packet's §3, and the backlog stays 34). **Reconciled again to session-log #83 on 2026-08-15** (queue-runner batch, thirty-eighth invocation, one packet: **CHAT-DISPATCH Task 9** — the two intake specs IN-1 and IN-3 filed as PROPOSED design input; **eighteen questions entered with full verbatim text per QR-1 but NO durable IDs** — `Q-IN1-` and `Q-IN3-` re-checked repo-wide, both free, both deliberately not minted, so **ID-DL-1 now governs FOUR packets** and was annotated with the edge the earlier three lacked: **Q-IN1-8 asks whether the `IN` series stands at all**, so a rename would land on the namespace ID-DL-1 is deciding how to join; **the IN-1 and IN-3 rows annotated and both left ⬜** — and unlike Task 8's, **neither item has a ruling to elaborate**, so both specs map the design space and hand the question back rather than elaborating anything; **HK-7 annotated satisfied-for-one-session a FIFTH time and again not closed**, carrying a THIRD bridge finding — the bridge is the probable source of the stale `.git/index.lock`, corroborated natively this batch as a second occurrence, **while the CRLF false-DIRTY did NOT reproduce natively**; **OPEN-4 annotated a seventh time**, resume point Task 10, ten tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **Reconciled again to session-log #84 and #85 on 2026-08-15** (queue-runner batches, thirty-ninth and fortieth invocations — **recorded together because the #84 sentence was omitted from this header when that batch ran**; its rows landed normally and `Q-IN2-1` – `Q-IN2-9` are in §7 above, so what was missing was the header line, not the reconcile. **#84** = CHAT-DISPATCH Task 10, the IN-2 crash-report extraction spec, nine questions with full text, no durable IDs. **#85** = **CHAT-DISPATCH Task 11** — the WF-2–WF-8 email-workflow spec filed as PROPOSED design input; **ten questions and two named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-WF-` and `WF-LOOK-` re-checked repo-wide, both free, both deliberately not minted, so **ID-DL-1 now governs SIX packets** and was annotated with this packet's own edge: **Q-WF-2 raises a SECOND naming question of the same class**, the `T3` collision, so two open naming rulings now sit side by side; **all seven WF rows annotated and ALL LEFT ⬜** — the spec **MAPS rather than elaborates**, because #63 adopted a document, not a design; **seven of seven gates re-verified UNSATISFIED**; **the BAA/compliance review was NOT added as an eighth gate and the `T` series was NOT re-lettered** — both are rulings; **Q-WF-10's pointer annotations deliberately NOT added**, since doing them would answer the question; **HK-7 annotated satisfied-for-one-session a SEVENTH time and again not closed**, carrying the first *negative* bridge datum — the predicted stale `.git/index.lock` was **absent**, making the pattern three-for-four; **OPEN-4 annotated a NINTH time**, resume point Task 12, eight tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **Reconciled again to session-log #86 on 2026-08-15** (queue-runner batch, forty-first invocation, one packet: **CHAT-DISPATCH Task 12** — the PR-3 re-parenting migration proposal filed as PROPOSED design input; **seven questions and one named look entered with full verbatim text per QR-1 but NO durable IDs** — `Q-PR3-` and `PR3-LOOK-` re-checked repo-wide at HEAD, both free, both deliberately not minted, so **ID-DL-1 now governs SEVEN packets**; **the PR-3 row annotated add-only and left ⬜ with its execution hold unchanged** — a proposal is not a closure, and the proposal's own target-state section is deliberately a question because PL-1..PL-4 are unruled; **PL-1..PL-4 and D-CL1-3 annotated, none narrowed, none answered**; **the headline is `Q-PR3-2`, a defect in a ruled constraint** — `sideSetFor()` (`roster.ts:195–202`) falls through to plaintiff-defendant for every non-Criminal practice area under a header claiming REQ-14 is satisfied *by construction*, **re-confirmed at HEAD by the runner and changed nowhere**; **`Q-PR3-3` is live** — the unrun CD-1 item-7 migration's two-value IN list makes the PR-3/CD-1 ordering consequential, and the quieter failure is the order currently scheduled; **the three superseded passages deliberately NOT annotated**, since annotating them answers `Q-PR3-5`; **HK-7 annotated satisfied-for-one-session an EIGHTH time and again not closed**, with the lock hypothesis now three-for-five; **OPEN-4 annotated a TENTH time**, resume point Task 13, seven tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, no schema file touched, nothing re-parented, backlog still 34.**). **Reconciled again to session-log #87 on 2026-08-15** (queue-runner batch, forty-second invocation, one packet: **CHAT-DISPATCH Task 13** — the QBO integration research memo filed as PROPOSED design input with its `[V-1]`/`[V-3]` provenance tiers intact; **eight questions and four named looks entered with full verbatim text per QR-1 but NO durable IDs** — `Q-QBO-` and `QBO-LOOK-` checked repo-wide at HEAD, both returned **ZERO**, both deliberately not minted, so **ID-DL-1 now governs EIGHT packets** and was annotated with this packet's edge: **there is no nearest durable series to fold into**, because no QBO or money series exists and the module is unruled by BUILD-STATE's standing *"NO MONEY MACHINERY"* line; **the headline is `Q-QBO-2` — there is NO read-only OAuth scope**, so "read-only first" can be a property of the code but never of the grant; **`Q-WF-4` annotated with a SECOND consumer** (`Q-QBO-3`: QBO needs a confidential client and a *rotating* refresh-token home, and gate 4's static-key comparator does not transfer); **`Q-WF-6` and `Q-STAT-5` annotated with a THIRD registry absence of the same shape** — `iolta`/`trust`/`safekeeping`/`commingl`/`client funds`/`escrow` all **zero** across all four registry files on a natively re-verified sweep, under a constraint whose stated stake is a bar complaint; **`future-modules-capture-2026-07-28.md`, `Go_Live_Gates.md`, every `legal-rule-registry-*` file, `db/schema.sql` and every migration were NOT touched** — editing any of them would answer `Q-QBO-1`, `Q-QBO-4` or `Q-QBO-6`; **HK-7 annotated satisfied-for-one-session a NINTH time and again not closed**, carrying the first *cost* of a repo-only grant — `QBO-LOOK-4` was NOT RUN for want of the Knowledge Repo folder; **OPEN-4 annotated an ELEVENTH time**, resume point Task 14, six tasks still on a clipboard. **Nothing built (§5 NONE), nothing verified, no registry file touched, backlog still 34.**). **This file is the single register of what awaits Michael's ruling; keep it current or the pointer lies.**

**Canonical repo path:** `docs/specs/attorney-review-queue.md` — filed 2026-07-26. Duplicate-routing check run at filing: no equivalent review-queue document existed; the only prior partial is `trcp-deadline-skeleton-2026-03-01.md` §9 (TRCP entries only), which now points here and remains the TRCP-priority detail.

**Why this exists.** The mining has outrun the ruling. Research has produced propositions faster than they can be verified, and the queue — not the research — is now the project's bottleneck (H22). This document is the whole queue in one place, so the bottleneck is at least visible.

**Convention:** ✅ = closed (2026-07-26 unless a later date is stated) · ⬜ = open. Each open item carries **the actual question in bold**.

---

## 1 — COURT OPINIONS

- ✅ **In re Allstate Indem. Co., 622 S.W.3d 870 (Tex. 2021)** — read 2026-07-26. Docket No. 20-0071 confirmed; two-*Allstate* question closed. §18.001(f) qualification holding; reasonable-notice content standard; reasonable notice ≠ Rule 702/*Robinson* reliability.
  - ⬜ **Residual (V1): are the p.876 and p.882 statements load-bearing or dicta?** Targeted re-read of those two pages. Interacts with Entry 2(j) and H77.
- ✅ **In re Chefs' Produce of Houston, Inc., 667 S.W.3d 297 (Tex. 2023)** — read 2026-07-26. Both holdings confirmed; record as limited-by-*Ortiz*.
- ✅ **In re Sherwin-Williams Co., 668 S.W.3d 368 (Tex. 2023)** — read 2026-07-26. Entry 10(a)'s three-element test confirmed verbatim.
- ✅ **In re Auburn Creek Ltd. P'ship, 655 S.W.3d 837 (Tex. 2022)** — read 2026-07-26. Nexus element has independent content; state it separately.
- ✅ **In re H.E.B. Grocery Co., 492 S.W.3d 300 (Tex. 2016)** — read 2026-07-26. Less-intrusive-means analysis captured.
  - ⬜ **Residual (V3): does Entry 10(c)'s "trend" framing survive?** *H.E.B.* (2016) already compelled an exam on unremarkable facts using the same test, which suggests the line is settled rather than shifting.
- ✅ **Tex. State Univ. v. Tanner, 689 S.W.3d 292 (Tex. 2024)** — read 2026-07-26. No freestanding diligence requirement; relation-back; jurisdictional against governmental defendants.
  - ⬜ **Residual (V6): do you adopt *Tanner* as the H21 cite, and does the draft entry live in the registry file or the TRCP skeleton doc?** Asked and not answered.
- ✅ **Rohrmoos Venture v. UTSW DVA Healthcare (Tex. 2019)** — read 2026-07-26. Four proof elements (not five); contemporaneity ruled a badge.
  - ⬜ **Residual (V7): is *Arthur Andersen & Co. v. Perry Equip. Corp.*, 945 S.W.2d 812, a 1997 case?** Stated as 1977 in session.
- ✅ **Tony Gullo Motors I, L.P. v. Chapa, 212 S.W.3d 299 (Tex. 2006)** — read 2026-07-26. All three propositions confirmed; join table survives.

---

## 2 — STATUTES AND RULES IN CURRENT TEXT

- ✅ **CPRC §18.001(d)–(g)** — official text pulled 2026-07-26. All four deadline formulations recorded, including the (e-1) inversion.
  - ⬜ **Residual (V2): what governs "the date the defendant files an answer" in a multi-defendant case with staggered answers?** Same gap as TRCP 194.2(a).
- ✅ **Tex. Prop. Code Ch. 55** — full chapter pulled 2026-07-26. **EMS threshold = counties of 800,000 or less** (a ceiling). Fatal/amount divide sorted.
  - ⬜ **Residual (V4): are notice defects fatal to the lien?** §55.005(g) saves only non-receipt of a properly mailed notice; it says nothing about failure to send or a defective §55.005(e) EMS form notice. **Statute cannot close this — case law must. This still gates the disbursement checklist.**
  - ⬜ **Residual (V5): does a lien attach where the tortfeasor is a county or ISD?** §55.001(4) excludes them from "person"; §55.002(a) requires negligence of "another person."
- ✅ **H.B. 4145 enrolled bill** — read 2026-07-26. Applicability keyed to **date of service**; per-bill exposure flag survives.
- ✅ **CPRC §38.001 + H.B. 1578** — codified text and enrolled bill read 2026-07-26. 87th Leg. 2021, Ch. 665; applies to actions **commenced** on or after 9/1/2021. Four carve-outs and §38.006 newly surfaced.
  - ⬜ **Residual (V8): is the commenced-date capture still worth building?** Any pre-9/1/2021 action is now ~5 years old, and the date is derivable from the filing date.
- ✅ **Tex. Bus. & Com. Code §17.50(d)** — read 2026-07-26. "Shall be awarded," unqualified; `basis: mandatory` holds. Three date-anchored fee cuts surfaced.
- ✅ **Tex. Prop. Code §53.156** — full chapter read 2026-07-26 (session 2); enrolled S.B. 539 retrieved. **Date corrected: 2011, not 2021.** May→shall was the only change; applies to proceedings commenced on/after 9/1/2011. Bidirectional. Residential carve-out is a one-directional downgrade as to the owner-as-payor.
  - ⬜ **Residual (V10): does "equitable and just" permit a ZERO award despite "shall"?** Michael's working position 2026-07-26: yes, given extenuating circumstances. **Not verified — no case law read.** Needs the citator pass.
  - ⬜ **Residual (V13): is the bidirectional exposure live?** Has a Servpro lien ever been attacked with fees sought against the client?
- ✅ **Tex. Prop. Code ch. 28** — full chapter read 2026-07-26 (session 2). **O1 closed: fees are DISCRETIONARY** (§28.005(b)). Three corrections to the drafted entry; §28.0091 was missing entirely.
  - ⬜ **Residual (V11): 1½%/month — simple or compound?** Statute silent. Affects any annualized figure.
  - ⬜ **Residual (V12): public-entity prompt payment** is outside ch. 28 and outside this project. Gap, not error.

- ✅ **Tex. Est. Code ch. 352** — **full chapter read 2026-07-26.** Both queue questions answered: "on proof satisfactory to the court" **IS** the operative standard (the 2026-07-25 voice note was right), and the two-lane separation survives **more strongly than framed** — the lanes are separate **subchapters** (A compensation/commission; B expenses and attorney's fees), an architectural feature, not a practice caution. **But the profile classification FAILED:** `discretionary-equitable` is wrong on both halves — §352.051 says the representative **"is entitled to"** (entitlement, not court permission), and **"equitable and just" appears nowhere in ch. 352**. Second consecutive session in which a voice-session classification failed against primary text (cf. O1). **§352.052 was missing from our docs entirely** — will-contest allowance, with the defend-vs-contest asymmetry. Corrected classification and full treatment: `statutes-pass-est352-cprc71-2026-07-26.md`.
  - ⬜ **Residual (V14a): what procedural vehicle carries a §352.051(2) fee request?** V14 ruled that fees sit outside §352.053, and §352.053 is the only procedure ch. 352 states — so the chapter is silent on the vehicle, and the vehicle is what the export must be shaped against.
  - ⬜ **Residual (V16, narrowed): does "is entitled to" bind the court**, or is it looser than "shall award"? The schema half dissolved into the O6 decomposition; this legal half stands.
  - ⬜ **Residual (V17): are a §71.004(c)-compelled representative's fees reimbursable under §352.051(2)** where the WD recovery is not the estate's? **Decides whether the probate fee profile touches PI at all.**
- ✅ **Tex. Civ. Prac. & Rem. Code ch. 71** — **full chapter read 2026-07-26.** **WD proceeds are not estate funds, on three independent hooks:** §71.004(a) exclusive benefit of surviving spouse, children, parents; §71.010(b) apportionment in the jury's verdict among those alive at that time; §71.011 not subject to the decedent's debts. §352.002's commission base therefore has nothing to attach to on a WD-only file — **including** the §71.004(c) case where the representative is compelled to prosecute. **WD half of V15 closed against the commission, pending sign-off.**
  - ⬜ **Residual (V15, survival half): OPEN and load-bearing.** **CLAUDE CORRECTION, recorded:** Claude stated in-session that a survival recovery is estate property. **§71.021(b) does not say that** — the action survives "to and in favor of the **heirs, legal representatives, and estate**," three unranked takers. Whether funds pass through the representative in administration cannot be closed from ch. 71; needs case law, same shape as V4. Even where they flow, §352.002(b)(2)(C) excludes cash paid to an heir or legatee as such, so the commission would compute on the receipt side only.

### Still to pull — the session stopped here

- ⬜ ~~**Tex. Fam. Code §§106.002, 6.708, 6.502(a)(4), 156.005**~~ — **REMOVED FROM THE QUEUE 2026-07-26. Family law is out of scope by ruling**; no family case type will ever exist, so there is no family profile to encode a mandatory branch into. The §156.005 stress-test lesson is already baked into the ruled O6 decomposition; **the probate chapters replace it as the stress test.**
- ⬜ **TDRPC 1.04** — **← RESUME POINT (moved 2026-07-26; was §352.051).** **RETAINED notwithstanding the family removal** — this is the fee-*reasonableness* rule and is core to PI contingency work; only its divorce-contingency subpart is mooted. Narrowed question: the reasonableness factors and the unconscionability ceiling as they bear on PI contingency work.
- ⬜ **Tex. R. Civ. P. 204.1** (the rule text; the case line is now done) — **should the IME task hook fire on serving a damages expert report, or on an earlier signal?** **RULED 2026-08-13 (#66): DEFERRED WITH SHAPE** — the trigger and the task content get ruled TOGETHER at the heartbeat/deadline pass rather than separately, and the **H64 designation-acts framing** (*your own designation creates the 204.1 exposure*) rides this row as the leading candidate. Stays open by design.
- ⬜ **The Estates Code territory the probate line needs** (new 2026-07-26, per S-1/PR-1/PR-2 — the line's spine is **independent, uncontested administration**): will admission and letters; heirship; notices to beneficiaries and creditors; inventory, appraisement, and list of claims; claims presentation and allowance; independent administration. Also referenced but not pulled: Est. Code §404.003 and Subch. B, ch. 361 (removal, via §352.004); ch. 503 / §501.006 (foreign representatives, via §71.012/§71.022).
- ⬜ **Probate research targets — FROM MEMORY, NOT READ, NOT ASSERTED.** A starting list for a future pull, deliberately carrying **no cites**. Nothing here opens a registry entry, and nothing here is authority until the text is in front of us.

### Items ruled 2026-07-26 (recorded so the queue's status lines stay truthful)

- ✅ **S-1 — probate a real practice line?** **CLOSED: yes** — a mapped line, full build-out, notwithstanding low volume.
- ✅ **PR-1 — independent vs. dependent administration?** **CLOSED: independent administration is in scope**; the deadline machinery matters on this line, and §352.003(b) (county-court jurisdiction over independent executors' alternate-compensation applications) is live.
- ✅ **PR-2 — contested vs. uncontested?** **CLOSED: will contests are a rare branch**; §352.052 builds as secondary, not core.
- ✅ **D3 / H8 — shared touch substrate?** **CLOSED: the case-event core (CE), shape (c)** — shared spine plus per-consumer facets, with an operational/evidentiary boundary. **Unblocks CE1; does NOT authorize it.**
- ⬜ **PR-3 — the `Probate companion` case type is MIS-PARENTED. NEW, OPEN.** Source verification 2026-07-26 found it at `src/domain/caseTypes.ts` **under Personal Injury**, inheriting `_piDefault` — a ladder the code's own comment concedes does not fit its arc. **S-1 rules probate a practice line in its own right, which that structure contradicts.** Re-parenting touches the case-type tree and the ladder assignment, so it is your call; it converges with the pending probate-ladder design pass (`spec-feedback` item 1) rather than pre-empting it. **Nothing re-parented, no ladder changed, nothing built.**
  - **Direction CONFIRMED 2026-08-07 per V17** (own practice area, own ladder(s), companion concept gone); **EXECUTION HELD** until the probate-ladder design pass produces the destination ladder(s) — a placeholder ladder is how the current wrong one happened. That pass is now **schedulable**: the Texas probate corpus arrived design-side. **New sub-question the pass must answer: proceeding-driven case types/ladders** (independent administration vs. muniment vs. heirship vs. small estate have different arcs — one "Probate" ladder may recreate the `_piDefault` mistake one level up). D-CL1-3 stays gated on PR-3. **Still open; still nothing built.**
  - **Annotation updated 2026-08-07 (evening session).** The execution hold is **unchanged**, but the gating design pass is now **DEFERRED pending the Domser matter** — Michael has not run a probate matter end to end, so a ladder drafted tonight would be treatise-derived rather than practice-validated. **Concrete unblock condition** (this is what the hold now waits on, replacing "schedulable"): the **letters-issued arc capture** from the Domser matter, plus a rescheduled pass. Checkpoints are at LETTERS and CLOSING. The PL-1..PL-4 series below carries the questions the pass must answer. See `rulings-capture-2026-08-07b.md`.
  - **Annotation added 2026-08-15 (#86), ADD-ONLY — the row stays ⬜ and the execution hold is UNCHANGED.** A **migration proposal now exists and is PROPOSED**: `docs/specs/pr-3-reparenting-migration-proposal-2026-08-15.md` (CHAT-DISPATCH Task 12, design session, Opus 5; **Michael did not participate and made no rulings**). Its own top line reads **PROPOSED — NOT AUTHORIZED TO BUILD**, and its target-state section is **deliberately left as a question** rather than inventing a destination ladder — which is the exact error PR-3 exists to correct, and which this row already names (*"a placeholder ladder is how the current wrong one happened"*). **A proposal is not a closure.** Three findings from it that bear on the eventual act: **(1) it is not a schema migration** — `cases.practice_area` and `cases.case_type` are free text with no CHECK, no enum, no FK, no index and no view (`db/schema.sql:38–39`), and a word-bounded sweep of `db/` + `supabase/` returns **exactly five** occurrences, so the act is a **source change plus a two-column value backfill**; **(2) `Q-PR3-3`** — the unrun CD-1 item-7 migration derives caption alignment from a literal two-value IN list (`db/migrations/2026-08-12-cd1-contact-directory.sql:100`), so **PR-3-first flags re-keyed probate rows** while **CD-1-first (the currently scheduled order) silently gives them plaintiff-defendant and nothing revisits it**; **(3) `Q-PR3-2`** — `sideSetFor()` falls through, below. **`Q-PR3-1..7` and `PR3-LOOK-1` are all yours; nothing was re-parented, no ladder changed, no schema file touched, nothing built.**
- ⬜ **RE-1 — referral engine.** **NEW, OPEN.** Ruled yes-eventually; triggers, logging, referral letter, and conflicts interaction all open. Not family-specific — conflicts, out-of-area matters, and overflow are all inputs.
- ✅ **FAM-1 — family furniture in `src`/`db`?** **CLOSED by Code verification 2026-07-26: none exists** — no family practice area or case type, no family-specific roles, no family tables, columns, or enum values. Nothing to remove; the removal was doc-only in fact as well as in authorization.
- ✅ **FLP / CourtListener account + connector.** **CLOSED 2026-07-26** — signed up, connector live. The 30-day window doubled access for members and non-members alike (a usage promotion, not a signup window), so the 8/6 date carried on nine consecutive entries was never a cliff. **The V10 citator pass is now RUNNABLE**, subject to the majority-opinion rule.

---

## 3 — TRCP DEADLINE SKELETON (none touched today)

- ⬜ **TRCP 194.2(a)** — the anchor; everything downstream derives from it. **Is the trigger 30 days after the first answer or appearance, exactly as the skeleton states?**
- ⬜ **TRCP 190.3(b)(1)** — the cascade rule. **Is the discovery-period end the earlier of 30 days before trial or nine months after the anchor?**
- ⬜ **TRCP 195.2(a) and (b)** — the most expensive thing on this list to miss. **Are the 90-day and 60-day offsets from the discovery-period end correct?**
- ⬜ **TRCP 166a(d-1), (e-1), (g-1)** — a **current-practice risk today**, independent of the build. **What changed in the 2026 edition, and does it alter anything you're doing right now?** Confirm you are reading the 2026 edition — a 2020 PDF was uploaded by mistake once.
- ⬜ **TRCP 99(b)** — the answer date is 10:00 a.m. on the Monday next after twenty days expire. **Is that formulation exact enough to write a tested function against?**

---

## 4 — STATUS CHECKS (none done today)

- ⬜ **Sheppard v. Martinez subsequent history** — **has it drawn any subsequent history that changes Entry 1 or Entry 5?** One of seven cases in Entry 1's citator pass.
- ⬜ **In re Greystar · In re Brenham Nursing · In re Pinnergy** — all pending as of the course book's June 2025 window. **Have any been decided?** *Ortiz* is the cautionary example: it sat on this list as "pending" and had in fact been decided in May.
- ⬜ **Kostura's 30-day CPN rule vs the 2024 CMP rules** — the paper itself flags possible supersession. **Which text governs**, before any conditional-payment thread template is drafted?
- ⬜ **AMA CPT license terms** — **can the module store descriptor text at all, or is it numeric codes only?** Entry 7's only real blocker.
- ⬜ **NEW: *Ortiz* citator pass** — the opinion is ~12 weeks old; rehearing window and early treatment unknown.

---

## 5 — PROJECT DOCUMENTS AWAITING REVIEW (none opened today)

- ⬜ **Entry 2 redraft v2** (now the Entry 2 section of `legal-rule-registry-draft-entries-medical-billing.md`, replaced in place 2026-07-26) — **does (e) state the claimant-offers-the-counteraffidavit move correctly, is (f)'s uncontroverted floor worth building as a computed figure, and how should (j) record the contested *Allstate* dicta?**
- ⬜ **`legal-rule-registry-draft-entries-medical-billing.md`** — Entries 1, 3, 6, 7, 8, 9 (Entries 2, 4, 5, 10 advanced today). **Entry 1(c-3) first: does a reasonableness-conditioned LOP cap the "incurred" amount?** The billing module cannot encode either reading until you rule.
- ⬜ **`time-tracker-fee-basis-profiles-design.md`** — **do you adopt it as canonical, and do you accept the §3 call putting claims in their own table?** Still marked DRAFT; written in a Code session, never given a design-side read.
- ✅ ~~**`case-heartbeat-design.md` §10 — D3: one touch substrate or two?**~~ — **RULED 2026-07-26: the case-event core (CE), shape (c)** — a shared spine plus per-consumer facets, owned design-side, with an operational/evidentiary boundary (only evidentiary facets feed a sworn fee affidavit). Ruled against four consumers, not two. **The former "blocks T1" phrasing is retired with it** — the substrate slice is now **CE1** (N-1 rename; the transcript T-series is unchanged), and **CE1 is unblocked but NOT authorized.**
- ⬜ **`case-heartbeat-walkthrough-capture-2026-07-25e.md`** — H35–H41. **H40, asked twice without an answer: is a deposition thread dead on notice served, on the depo being calendared, or on the transcript being in the file?** — **H40 RULED 2026-08-13 (#66): deposition threads are STAGED** — notice → calendared → taken → transcript; the thread **dies at transcript-in-file**. Row stays open for its remaining H35–H41 questions.
- ⬜ **`case-heartbeat-walkthrough-capture-2026-07-25f.md`** — H42–H58. **H50: is the supplementation opt-out a hard kill, or dormant with wake events?** The only opt-out whose penalty is automatic exclusion. — **H50 RULED 2026-08-13 (#66): DORMANT WITH WAKE EVENTS, never a hard kill** (the automatic-exclusion penalty is why). Row stays open for its remaining H42–H58 questions.
- ⬜ **`apil-2025-course-book-mining-pass2.md` §8** — H59–H67. **H60: do payer threads arm at intake off the coverage inventory, rather than at settlement?** — **H60 RULED 2026-08-13 (#66): YES, ARM AT INTAKE** off the coverage inventory, with traditional / Advantage / supplemental captured as arming data. Row stays open for its remaining H59–H67 questions.
- ⬜ **`apil-2025-course-book-mining-pass3.md` §10** — H68–H83. **H77: does the registry get a `precedential_status` field separate from verification status?** *Gregory v. Chohan* is a plurality treated as binding by nearly every court citing it. **Now has a second live exhibit** — the contested *Allstate* dicta in Entry 2(j). — **H77 RULED 2026-08-13 (#66): YES — the registry GETS a `precedential_status` field, separate from verification status.** Plurality / dicta / memorandum recorded **as data, flag-only**; both live exhibits stand. **Ruled ≠ authorized: no schema or code change follows until a slice authorizes it.** Row stays open for its remaining H68–H83 questions.
- ⬜ **`trcp-deadline-skeleton-2026-03-01.md` §9** — **H22: do the TRCP entries queue behind entries 1–10, or interleave by build urgency?** The queue arithmetic is the project's bottleneck. — **RULED 2026-08-13 (#66): INTERLEAVE BY BUILD URGENCY** — whatever a ruled slice needs is verified first, regardless of series.
- ⬜ **`registry-courtlistener-integration-design.md`** — **webhook or email given current hosting posture, and does cite-validation pull forward?** *(FLP account + Tier 1 + connector: CLOSED 2026-07-26 — see §2. The "promo ends 8/6" clause is struck; it was never a cliff.)*
- ⬜ **`criminal-appointment-intake-and-docket-enhancements.md`** — **which appointee names count as yours, and does that vary by county?** The Hill Country Regional Public Defender Office would currently hard-stop.
- ✅ ~~**`REQ-CAPTURE_uvalde-docket-worksheet_2026-08-12.md` — FILED UNRECONCILED**~~ — **RESOLVED 2026-08-12 by Michael's group ruling** on the presented disposition table (the roster-capture precedent). The sixth REQ-1 capture, and the only one that reached the repo *before* reconciliation — that difference is preserved at #59, not erased. Reconciled against BUILD-STATE's fifty-fourth refresh and the log through #61, i.e. **after the CD-1 build**, so its directory dispositions cite built structure rather than planned structure. Full disposition table in the capture's appended addendum. Its open questions now live in the CR rows below; **the capture is no longer an open item.**

### Criminal-side requirements (CR series) — ADDED 2026-08-12

*(**Series CREATED by Michael's ruling 2026-08-12**, the WF-1/DE precedent: criminal practice items fit neither FE nor IN nor DE. **The #48 elicitation caveat applies to CR as it does to IN and DE — renaming is a one-word veto until these IDs get load-bearing.** **COLLISION FLAGGED, DELIBERATELY NOT RENAMED (the H-series disposition):** `CR-3` already means the **Texas crash-report form** in this file at IN-2 and in `contact-directory.md` §7. That is a form designation, not an ID series, so the packet's collision test did not fire — but the string now carries two meanings in this file. Michael was asked before these rows landed and chose to proceed as CR with the clash flagged; renaming an attorney's ID series is not Code's call. **When citing one, say "CR-3 (criminal requirement)" or "the CR-3 crash report."**)*

- ⬜ **CR-1 — Hearing-date extraction with attorney confirmation at matter creation.** From the appointment paperwork: when a matter is created from an OAA, prompt as to whether a hearing is already set — it may be in the paperwork — and if so pull it from the document. Priority: now. **Dedupe instruction carried:** check against `criminal-appointment-intake-and-docket-enhancements.md` at HEAD; if the intake spec already covers it, this records as a second observation on the covering section rather than a standalone item.
- ⬜ **CR-2 — Per-cause prosecutor assignment, by-prosecutor grouping of the day's docket, and a county prosecutor roster with collision-safe short codes.** Prosecutors ask off-hand "what cases do we have together today?" and the answer must be readable from page 1. **Question riding here (§5 Q1): should the prosecutor assignment live at the CAUSE level or at the DOCKET-ENTRY level**, given that one defendant's multiple causes were split between two prosecutors on the same docket, and that the assignment observed at court differed from the printed docket on roughly three of eight defendants? Priority: now. The roster half is **CD-1 directory substrate** (contacts with roles) — annotated onto the CD entries too.
- ⬜ **CR-3 — At-court correction capture** (prosecutor, attorney-of-record, setting type) that persists to the matter and reconciles against the next published docket, with a **court-confirmed flag distinguishing observed fact from imported data**. Treat printed docket data as unreliable: live evidence includes a blank attorney field resolving on the next docket and a clerk attorney-code error flagged in the docket's own error log. Cross-links CR-2. Priority: soon. *(This is CR-3 the criminal requirement — not the CR-3 crash-report form cited at IN-2.)*
- ⬜ **CR-4 — Front-page expected-disposition marker** (plea / contest / status) **plus per-setting appearance outcome** (appeared, no-show, in custody elsewhere) feeding the follow-up list and the next worksheet's banners. Live evidence: plea-expected cases marked by hand with a "P"; no-shows recorded in the margins; one no-show client on bond; a matter needing a reset after a bench-out did not happen. Priority: soon.
- ⬜ **CR-5 — Custody status per client** — facility, county/state, and the **source and date of the information** — **including out-of-state facilities**, plus **bench-warrant/writ status**, both surfaced in the client-page banner. Live evidence: a revocation client confined in another state's correctional facility, with handwritten notes tracking that he did not get benched out here and needed a reset. Priority: soon.
- ⬜ **CR-6 — Designated scratch and quick-intake blocks on the worksheet, plus a post-court reconciliation step** that routes captured notes to the correct matters and creates new matters from intake blocks. Live evidence of what happens without them: one client's negotiation numbers written on a different client's page; a new case for an existing client written upside-down on a blank back; a full new-client intake handwritten on the front page. **Question riding here (§5 Q7): when handwritten court notes are transcribed and routed to matters after court, what marks the resulting record as ATTORNEY-REVIEWED rather than raw OCR, so downstream worksheets don't print unverified transcription as fact?** Priority: soon.
- ⬜ **CR-7 — Three-way number mapping: defense cause number ↔ docket entry number ↔ DA file/incident number.** Stored per cause, printed on the worksheet, optionally imported from the DA's charge-table document. Live evidence: the DA's own charge-table printout hand-annotated — indicted rows circled, numbered, and mapped to docket entries — because the worksheet carries only cause and docket numbers while the DA's office talks in its own file numbers. **Question riding here (§5 Q5): manual entry at court, OCR-assisted import of the DA charge table, or both — and can the mapping print as a front-page column without crowding the handwriting space?** Priority: now.
- ⬜ **CR-8 — Offer lifecycle tracking: offer versions as first-class records** (date, source, full terms, option sets), with the worksheet rendering history, a changed/unchanged flag, and option sets. Live evidence: one offer tracked through three versions with dates and sources (email vs. offer sheet vs. revised sheet); another flagged "UNCHANGED from the sheet you had before the last setting"; a two-option offer laid out as a client choice; a probation option disappearing and returning. **The email-workflow doc's criminal plea-offer observation is its unruled relative — cross-referenced, never merged.** Priority: now.
- ⬜ **CR-9 — Notes attach to the MATTER, not the worksheet, and print forward automatically** — including transcribed handwritten notes from CR-6's reconciliation step. Live evidence: the later worksheet reproduced the earlier one's typed notes in CARRY-OVER blocks so nothing was lost, but handwritten content did not carry and had to be re-copied by hand; a phone number was handwritten twice and never entered any system. Absorbs the print-forward half of the contacts-at-capture requirement. Priority: now.
- ⬜ **CR-10 — Plea-paperwork audit rendered as a signing checklist:** degree of offense vs. charged statute, jail-credit presence for in-custody/bench-warrant defendants, completeness of required certifications, and non-concurrent cost totals. **The capture's own highest-value item.** Live evidence, all caught on paper before the judge signed: zero days' jail credit printed for a defendant who had been sitting on a bench warrant; the degree of offense misprinted on two causes; a blank, unsigned certification of right to appeal on every cause; and court costs that do not run concurrent. **HARD GATE, restated from CLAUDE.md rule 1: no computed check runs while the underlying propositions are UNVERIFIED.** Those six propositions now live in `docs/specs/legal-rule-registry-criminal-plea-and-costs.md` (all UNVERIFIED), and **CR-10 is gated on their verification.** **Question riding here (§5 Q6): can these checks be encoded as rules run against drafted judgments, and which verified-authority registry do they check against? — ANSWERED IN PART by the ruling: that registry file, once verified.** Priority: soon, but highest-value.
- ⬜ **CR-11 — Rule-generated banners from matter state** (prior FTA with a transportation concern, bench-warrant history, clerk data error, new appointment, changed offer) **plus a "FOLLOW-UPS — not on tomorrow's docket" closing page** fed by open tasks, **plus quick task capture from court notes** (a records subpoena to issue, mitigation to gather, items to run down with the court coordinator). Priority: soon.
- ⬜ **CR-CONSTRAINT (no ID — BINDING DESIGN CONSTRAINT, not a feature): all worksheet semantics must survive BLACK-AND-WHITE printing.** Borders, shading density, weight, and icons — **never color-coded meaning.** Michael prints in black and white only; the highlighting observed on one worksheet was a one-time conversation aid and was expressly **not** a feature request. **This governs ALL worksheet rendering, including every CR item above.** **Question riding here (§5 Q4): which black-and-white-safe typographic conventions should carry banner and flag semantics?** FE-10's format profiles are the civil-side sibling — **cross-referenced, never merged.**
- ⬜ **`spec-feedback.md`, 2026-07-25 OAA entry** — **do you want a cause-number-pending reminder, and should gender and race be captured at all?** **WIDENED 2026-08-12 (#64), NOT resolved: the CR-3 intake path raises the identical question.** The crash report carries ethnicity and sex fields, so if extraction is ever built it faces the same choice the OAA path already put to you. **One answer should govern both intake paths** — recorded here so they cannot drift into two different defaults. **RULED 2026-08-13 (#66), BOTH HALVES: CAPTURE BOTH sex and race/ethnicity, on both intake paths (OAA + CR-3), one answer governing both — #64's cross-reference is satisfied. Cause-number-pending reminder: YES — a visible flag on matters created without a cause number, cleared on entry.** Annotated add-only on the spec-feedback entry the same batch; the question CLOSES. *(Ruling is DESIGN status — nothing is authorized to be built from it; no field exists on the party registry today.)*
- ⬜ **`BUILD-SESSION-NOTES.md`** — 2026-07-21 audit, unreviewed across ~10 log entries. **Should case classification stay frozen at creation, or become editable?**
- ✅ **`Go_Live_Gates.md`** — **can you paste gates 1–5 verbatim so the repo copy stops being a stub?** Gate 3 also needs its amendment appended, pointing at gate 6. **CLOSED 2026-08-13 (#66) AS ALREADY-DONE — the ask was STALE.** The file has been complete since 2026-07-26: gates 1–5 verbatim, gates 6–8, gate 9, GL-1, and both amendments (the gate 2 multi-user clarification and gate 3's pointer to gate 6). Verified on screen design-side 2026-08-13 and **re-verified in the repo by the runner** the same day. This is **status-drift finding #1** of the cross-document sweep, which stays open. *(One correction to the finding as written: the design side put the file at 106 lines; it is **84**. The completeness claim is right, the line count is not.)*
- ⬜ **Texas Ethics Opinions 532 and 533** — **where is the Op. 532 line on case-level detail leaving the system to a third party — does it reach benchmark pooling and multi-user deployment, and should a go-live gate say so?**

---

## 6 — THE TWO HIGHEST-LEVERAGE LEVERS

Unchanged by today's work, and worth stating plainly at the top of any session:

1. **D3** — the shared touch substrate. Blocks every schema build in the project.
2. **Entry 1(c-3) and Entry 4** — qualified LOP and the fatal-defect conflict. Together they block the disbursement checklist and the billing module's LOP math. *Entry 4 advanced today but did not close: the surviving question (V4) is a case-law question the statute cannot answer.*

*(The former "hardest date on the board: FLP by 8/6" line is struck — closed 2026-07-26. There is no dated deadline on this board.)*

---

## 7 — PROJECT AND BUILD OPEN ITEMS

**Added 2026-07-28 (BS-1a item 10).** These were carried in `BUILD-STATE.md`'s own roster until that ruling replaced it with a pointer to this file. They are Michael's, same as everything above; they are separated only because they are build/process items rather than legal-research items.

### Closed by ruling, recorded so the status lines stay truthful

- ✅ **AUTH-1 — sign-in method for the single-user auth slice?** **CLOSED 2026-07-28: magic link** ("Let's go with Magic Link"). Built and exercised the same day; see log #28.
- ✅ **BS-1a — BUILD-STATE was at cap with the ledger split delivering one line: designate more content for the ledger, split live state differently, or accept the cap pressure?** **CLOSED 2026-07-28 by eleven item-by-item dispositions** — deletion, compression, and pointers rather than ledger moves. Post-cut length 139 of 150. See log #29.
- ✅ **D-CL2-8** — parallel, not promotion. **Adopted 2026-07-28 as Michael's own ruling**; the "Claude's call" asterisk retired.
- ✅ **D-CL2-2a** — earliest limitations derives from **unresolved clients only**, resolved meaning disbursement (D-CL2-4a). Closed 2026-07-26.

### Client model — the design doc's §10 is authoritative for all of these

`docs/specs/claimant-dimension-and-case-links-design.md` §10 carries the full table. Listed here by ID so the queue is complete.

**Amended 2026-08-07 by QR-1:** the former instruction here — *"do not maintain a second copy of the question text"* — is **superseded for the question text itself**. QR-1 rules that every open item carries its FULL question text in this file, because this file is the register that survives when packets are deleted. The design doc §10 remains authoritative for the *ruling detail and reasoning*; what must not be duplicated is analysis, not the question. Where the two disagree on wording, §10 governs the substance and this file governs completeness.

- ✅ **D-CL2-3** — **does the time tracker's "one rate per case, uniform" rule survive per-client fee arrangements, or become one rate per client?** **CLOSED 2026-08-07: one rate per CLIENT.** Michael supplied the fact pattern that withdrew Claude's keep-it-case-level recommendation — some cases have two clients, one at a discounted rate and one at standard. The rate moves onto the client record beside `fee_arrangement`; single-client cases render exactly as today (D-CL2-7 principle). **Mid-case changes are PROSPECTIVE** via an effective-dated rate history: hours are valued at the rate in force when incurred, nothing silently revalues, and a retroactive courtesy is a visible invoice write-down rather than a recompute. Full ruling and quotes: design doc §10 and `rulings-capture-2026-08-07.md` Part 5. **Authorizes nothing** — the time tracker stays parked behind CE1.
- ✅ **D-CL2-3a — RULED 2026-08-13 (#66): the affidavit carries the rate of the CLIENT WHOSE CLAIM CARRIES THE FEE DEMAND; multi-client demands get PER-CLIENT EXHIBITS.** *(Original question, preserved: fee-affidavit export under per-client rates — the export assumed one uniform case rate, lodestar-shaped. Which rate does the affidavit carry when clients differ — the rate of the client whose claim carries the fee demand, or something else?)* **Authorizes nothing** — the time tracker stays parked behind CE1.
- ⬜ **CL2-AC-1 — Auto-create the client record when a party is linked with the Client role on a PI case — a PI client with no damages-scope record is an impossible real-world state.** *(ID issued 2026-08-07 for the 2026-07-28 walkthrough capture, so the question cannot die the K-6/K-7 death.)* **Michael's own framing, from the CL-2 live walkthrough (moved here 2026-08-08, not shrunk):** linking a party with the Client role on a PI case and having no damages-scope record is an **impossible real-world state** — a PI client without damages isn't a client. Michael: don't make me build it by hand; assigning the Client role on PI case types should auto-create the damages-scope record, posture defaulting from case type, ready to populate. Data model stays parallel (D-CL2-8); **UI workflow only**. Brushes directly against deferred **CL2-CHECK-1** — "auto-create on PI" and "flag Client-role-without-record" are two answers to the same gap, and Michael fell into that gap himself on 2026-07-28. **DIRECTION-CONFIRMED 2026-08-08** (was PROPOSED, unruled): auto-create on PI client-role link is the policy. **UNRULED, doc-on-screen required:** link-removal behavior (persist/orphan/prompt), mixed-posture cases, backfill vs. new-links-only. **Not closed until the edges are ruled.** *(The morning's "yes" was withdrawn to direction-confirmed by Michael's own choice of option (b) after Claude flagged its own bad elicitation — `rulings-capture-2026-08-08.md` Part 5. **Consolidated here 2026-08-08** from the "Captured 2026-07-28" block per the CL2-AC-1 dedup ruling — one register, one home per question; see `rulings-capture-2026-08-08b.md` Part 2.)*
- ⬜ **CL2-CHECK-1** — advisory client-role ↔ client-record consistency check (flag/review-log, never auto-fix). **DEFERRED 2026-07-28 — do not build.** Adopt later or drop? **RE-AFFIRMED 2026-08-13 (#66): KEEP PARKED** — the deferred-do-not-build posture stands; revisit after CD-1's flags prove out.
- ⬜ **D-CL1-1 · D-CL1-2 · D-CL1-3** — CL-1 / `case_links` shape. **D-CL1-3 is gated on PR-3 alone.** *(Annotated 2026-08-15, #86, add-only: the PR-3 migration proposal exists and is PROPOSED — it makes D-CL1-3 **answerable if PR-3 ever executes**, and it does **not** make it answered. Still ⬜, still gated, still nothing built.)*
- ⬜ **CL-3 — Follow-on matter creation: create a linked follow-on matter that inherits selected entities and facts from a source matter, with explicit role transforms, instead of starting blank (live evidence: a settled third-party motor-vehicle case spawned a first-party UIM/UDJA suit sharing plaintiff, occurrence facts, and counsel, with the former defendants recast — the defendant driver became the non-party "underinsured motorist," the co-defendant vehicle owner a non-party settling party, and the client's own insurer entered the defendant slot). Sub-question (UIM capture §5 Q1): should a follow-on matter inherit the source matter's evidence and discovery products (crash report, records indices, deposition transcripts) as linked references, or as copies — and does privilege/work-product tracking differ between the two?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now; ID RULED by Michael the same day as part of the group ruling on the capture's disposition table). CL-series continuation: the follow-on edge carries role-transform mappings ({{party}} was {{role_a}} in source, is {{role_b}} in target) as its payload. **CL-1's non-cascading rule stands** — its spec says anything that should propagate is a separate, explicit ruling, and **CL-3 is that ruling's designated home, scoped to the follow-on link type only.** Cross-reference `docs/specs/claimant-dimension-and-case-links-design.md` §7. Companion finding, no separate ID: the capture's cross-matter party-role-transform requirement is already satisfied by the ruled directory — one global row, roster entry = contact × capacity × role per case, and `contact-directory.md` §4.2's worked example is literally the UIM at-fault driver. **NOT authorized for build.**
- ⬜ **UM-1 · UM-2 · PR-GATE-1 · MIN-1** — the four unruled proposals; none blocks any authorized slice.
- ⬜ **CIV-1** — **civil-litigation damages are entirely UNSPECIFIED.** Needs its own design session; breach of contract is the workhorse of that line.
- ⬜ **PROB-1** — probate client profile unwritten. ⬜ **PA-1** — practice-area edit vs. derived profiles.

### Form engine (FE series) — ADDED 2026-08-07

**This series was absent from this file entirely** until the 2026-08-07 rulings batch; it had lived only in `form-engine.md` §12.6. Added here so the register is complete. `form-engine.md` §12.6 carries the finding text and the ruling detail.

- ✅ **FE-1 — chronology sources carry provider identities but NOT addresses, phones, or billed charges; the engine cannot ship a servable draft from a chronology alone. Where does provider data come from — §4 interview cards, a persistent provider-directory table, or another source?** **CLOSED 2026-08-07: the persistent provider-directory table**, with the §4 interview card surviving as the fallback for a provider not yet in the directory, writing back into it (enter-once). Three sub-rulings, all confirmed: **(a) identity in the directory, dollars on the case** — billed charges are per-case facts, and storing them per-provider would recreate the wrong-level defect class CL-2 was built to kill; **(b) Option A pointer model** — case-level provider records point at the directory entry and read live, with the served .docx files as the historical record of what went out; **(c) silent trust** — directory fills carry no confirm flags, since the data is attorney-entered and the proofread every filing gets already catches staleness. Michael's own load-bearing requirement, recorded: editing a provider **from within a case** must propagate to every linked case, which only the pointer model delivers. Build-facing consequences for whenever the engine is authorized: the directory entry must be editable in case context, and that edit surface must state firm-wide scope with a linked-case count (labeling, not a confirm click). **Spec ruling only — authorizes no table, migration, or UI.** **SUPERSEDED 2026-08-08 by CD-1** (contact-directory reframe): the ruling stands for the provider question as framed; the live question is CD-1. Surviving mechanics carry into CD-1 as PROPOSED. See `rulings-capture-2026-08-08.md` Parts 1–2. **Evidence channel opened 2026-08-10:** the `drafting-disclosures` skill (`docs/skills/drafting-disclosures/SKILL.md`) emits a mandatory provider-data block every run — designed as the FE-1 scout, to accumulate a real provider corpus so the directory-vs-interview-cards question could be ruled on evidence. Since FE-1 closed and was superseded, **that evidence now feeds CD-1**, not FE-1. Blocks are saved by Michael's hand outside the repo; the skill never writes them here.
- ✅ **FE-2 — one billing entity existed ONLY in the chronology's billing-record document names, with no chronology row of its own; a human caught it and §4 intake would have walked past it. Should intake sweep document-name columns for entities not otherwise listed?** **CLOSED 2026-08-07: ruled yes.** Finds surface as flags for Michael — a catch-net, never an auto-add, since nothing enters a disclosure without his call. Because document names are messy (abbreviations, records vendors, misspellings), dismissal is one click and **dismissals are remembered per case** so the engine doesn't re-ask every draft. **Spec ruling only.** **Build home PARKED 2026-08-08**, unattached: ruled, awaiting a build home; likely lands with the CD-1 build or the intake pipeline, whichever comes first. (Slice A, its loose prior attachment, was withdrawn.) **RE-PARKED 2026-08-11: FE-2 lands with the INTAKE PIPELINE**, superseding "whichever comes first" — reason: the sweep operates on document-name columns and nothing in the CD-1 slice (or the app today) ingests documents, so the parked ruling's premise does not hold here. **The 2026-08-07 ruling itself is untouched** — this moves the home only, it does not reopen the ruling. See `docs/specs/cd1-build-slice.md`.
- ✅ **FE-3 — `form-engine.md` §8 carries real-case-flavored shell content predating the packet rules; needs a design-session read of §8 on screen before ruling what example content ships in the spec.** Michael's. *(Design-side searches on 2026-08-07 could not surface an FE-3 row in this file; verification confirmed it was genuinely absent, not a search miss. Added here with full text per QR-1.)* **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** **CLOSED 2026-08-12 (RULED): the §8 on-screen read is complete (full text; design session with Michael supplying the section from the P1 checkout). The five formatting bullets and the contamination bullet's ruling content ship as written; the bullet's two real prior-case example strings are GENERALIZED to generic descriptions by in-place amend with a dated note (never silent rewrite). The strings are not reproduced in the record. See #55.**
- ⬜ **FE-4 — Definitions as case-level objects: one definitions list (incident, vehicles by VIN, party set, ESI terms) written once per case with its own identity, referenced by every instrument rather than baked into each document, so an amendment propagates; the "You/Your" addressee definition substitutes per target. How do definition objects attach to the case and version across a discovery wave?** Entered 2026-08-11 (REQ-CAPTURE). Exemplar: `docs/templates/discovery/template_definitions-instructions_requests.md` (¶2 = per-target substitution; ¶12+ = case-specific slot; optional-ESI brackets = conditional blocks). Gated behind CD-1 with the rest of the engine. **RULED 2026-08-11 — design at `form-engine.md` §13.1** (case-level versioned definitions objects: immutable versions, every rendered instrument stamps the version used, prospective propagation; the You/Your addressee definition is render-time per-target substitution, NOT versioned content). Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.** **SPEC LANDED 2026-08-15 (#81): `docs/specs/fe-4-definitions-sets-spec-2026-08-15.md`** — elaborates §13.1, adjudicates nothing, states that where it differs from §13, §13 governs. **The row stays ⬜: a spec is not a closure, and the build is still unauthorized.** Six new open questions ride below (Q-FE4-1..6), the sharpest being **Q-FE4-4's snapshot-vs-live-reference tension** — if a version stamp resolves against a live roster, the stamp does not identify what went out.
- ⬜ **FE-5 — Interrogatory count budget: live count per set against a configurable cap by discovery level; document-identification-only interrogatories excluded from the count; soft warning where an interrogatory has facially severable subparts. How aggressive should subpart detection be — attempt facial detection (multiple sentences, enumerated clauses) and show a worst-case count beside the numbered count, accepting false positives on compound-but-unified interrogatories, or warn only on request?** Entered 2026-08-11 (REQ-CAPTURE §5.2). Legal substrate — TRCP 190.3(b)(3), 190.4 — VERIFIED by Michael 2026-08-11; counts may display as facts. Design risk on record: warning fatigue. **RULED 2026-08-11 — design at `form-engine.md` §13.2:** the middle path between this entry's two poles — subpart detection is always-on and purely facial, the worst-case count displays **passively** beside the numbered count, and it escalates to a warning **only when the worst-case crosses the cap while the numbered count does not**, which honors the recorded warning-fatigue risk. Severability judgment stays the attorney's. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.** **SPEC LANDED 2026-08-15 (#81): `docs/specs/fe-5-interrogatory-budget-spec-2026-08-15.md`** — and **this row's own legal substrate is now under flag.** The line above records TRCP 190.3(b)(3) as "VERIFIED by Michael 2026-08-11; counts may display as facts" — **the spec finds that the verified entry's wording states a different budget than the rule text (Q-FE5-9), so "counts may display as facts" is only as sound as the wording behind it.** Three further scope findings ride below: the cap is **pairwise and cumulative** (per propounding/responding pair, across waves), **Level 1 also caps RFPs and RFAs at 15 each** while FE-5 as ruled counts interrogatories only (Q-FE5-1), and **Level 3 has no cap to look up** — it is an order value, so `cap = f(level)` is structurally wrong there. **The row stays ⬜.**
- ⬜ **FE-6 — Instrument packaging modes: render the same request content as one combined served document or three separate documents without re-entry, from one item model (item = id, text, scope common|role:<tag>|entity-specific, instrument type; set = ordered filtered list). Sub-question never asked in the capture: does each standalone instrument repeat the definitions block or incorporate it by reference?** Entered 2026-08-11 (REQ-CAPTURE). Combined-mode layout fully evidenced (caption → title → TO → preamble → definitions → instructions → interrogatories → RFPs → RFAs → signature → COS, plus the template's page-break structure); separate mode needs its own layout ruling. **RULED 2026-08-11 — design at `form-engine.md` §13.3:** packaging is a render-time choice from one item model, and the never-asked sub-question is answered — **each standalone instrument REPEATS the definitions block in full, no incorporation by reference** (served documents self-prove; FE-4's versioning removes repetition's drift cost). Separate-mode layout derives from the evidenced combined layout, one COS per document, verification page rides the interrogatories instrument only. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.** **SPEC LANDED 2026-08-15 (#81): `docs/specs/fe-6-instrument-packaging-spec-2026-08-15.md`** — elaborates §13.3 and restates **FE-17's from-birth annotation as a pointer, not an amendment**: FE-17 *"rides whichever slice creates the ITEM table (the discovery slice), in the same commit as that table,"* so whoever builds FE-6 is bound under a different item's number. Seven new open questions ride below, the sharpest being **Q-FE6-4** — an RFA whose wording flips with the target's relationship to a named party is unmodelled by the three ruled scope values, which say *which targets get an item*, not *which wording a target gets*. **The row stays ⬜.**
- ⬜ **FE-7 — Template distillation queue: mark a produced document "distill to template" so a later session generates the client-clean template with merge fields. Guardrails (in from birth): distillation output is client-clean BY CONSTRUCTION — the no-real-client-data rule is why distillation is a deliberate separate step, never "save as template" — and distilled templates are PROPOSED artifacts until Michael adopts them as house standard. What does the queue look like pre-engine, and what does adoption look like?** Entered 2026-08-11 (REQ-CAPTURE). Pre-engine the queue is a list in `form-engine.md`; first three candidates: entity trucking set, driver set, nonsubscriber battery (from the 2026-08-11 drafting session). **RULED 2026-08-11 — design at `form-engine.md` §13.4**, where the pre-engine queue is now a LIVE table seeded with those three candidates (all `queued`, none distilled, none format-authoritative). Route respects REQ-1: marking happens in practice space, the candidate travels by Michael's hand, distillation is a design-session act producing a client-clean template into `docs/templates/<category>/`. **Adoption is Michael's explicit ruling and is what flips the template's FE-12 provenance to format-authoritative**; until then it is distilled-PROPOSED and nothing drafts from it as authority. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.**
- ⬜ **FE-8 — Attorney-edit round-trip diffing: retain the as-generated version of every rendered instrument so an attorney-edited return can be diffed precisely — what the attorney changed is both the finalization worklist and a signal about house conventions (live evidence: a five-set family came back edited with no stored as-delivered copy to diff against; edits had to be inferred). Sub-question (capture §5.6): should the as-generated retained copy be immutable and content-addressed so "what did we deliver" is provable later, and how long are superseded generations retained?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Extends form-engine §10's generated-document record and FE-1(b)'s served-docx history. Lineage note: the capture cites a prior packet's "REQ-12 (artifact provenance)" — the cite resolves to nothing on record (design side searched 2026-08-12; Michael searched CIVIL LIT the same day); recorded as a probable citation defect, nothing reconstructed per the K-6/K-7 rule; this entry's text is complete on its own. Gated behind CD-1 with the rest of the engine. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status.
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, retention half only.** As-generated retention IS the §10 generated-document record (.docx + metadata) — that record is the retention design, so FE-D1 builds it. The DIFF half is DEFERRED to the slice with the first real consumer (the transform / discovery work), where FE-13 needs it anyway.**
- ⬜ **FE-9 — Document-family consistency: model a request-set family as shared core + per-recipient overlays so a checker can distinguish intentional per-recipient tailoring from drift and report ONLY the drift (live evidence: attorney edits introduced an "ADMISSION"/"ADMISSIONS" section-header divergence in one set of five, and small-caps character formatting lost in one retyped paragraph). Sub-questions: should character-level formatting (small caps, underline) inside body text be enforced or only surfaced as advisory, given attorneys legitimately retype text in Word (capture §5.2)? Does the drift report belong in the finalization UI (blocking, pre-service) or a review log (non-blocking) — is drift ever acceptable to serve (capture §5.5)?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Cross-link: FE-6's item model (scope common|role:<tag>|entity-specific) is the substrate — FE-6 is packaging, FE-9 is drift detection; not merged. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status.
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1, home named: the DISCOVERY slice.** Family-drift detection needs FE-6's packaging families, which FE-D1 does not build.**
- ⬜ **FE-10 — Render-time format lint: format profiles carrying paragraph-level properties (indent, hanging indent, spacing) per element class — item paragraph, prompt label (ANSWER: / RESPONSE: / ADMIT OR DENY:), section header, certificate block, verification block — enforced at render time, plus a lint pass flagging formatting outliers among elements that should be identical (a 20-of-21 paragraph-property histogram makes the 1 obvious; live evidence: the final response-prompt label carried different indentation than the other 20/31/19 identical labels in every set — a generation-side glitch invisible until rendered). Sub-question (capture §5.1): when an attorney's edit conflicts with the stored profile, is it a one-off deviation preserved in that document only, or a profile update propagating to future renders — and who confirms which?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE, priority soon). Extends form-engine §§12.7–12.13 (style-by-role, caption label style) from documented method to enforcement. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; UIM-UDJA REQ-CAPTURE REQ-05, ruled — no new ID, one home per question): the lint gains a NUMBERING pass. Generated instruments use continuous logical numbering and never hard-coded restart values, and a render-time lint verifies the visible numbering sequence is gapless and duplicate-free (live evidence: an exemplar's second numbered list was defined with a start value chained to the first list's final count, so inserting one fact paragraph silently produced a duplicate visible number until the start value was manually bumped). Additional sub-question riding here (UIM capture §5 Q6): should the numbering lint run automatically on every rendered instrument, or also exist as a standalone document-check tool that can be pointed at exemplars and incoming documents?** Format profiles also own the footer, per FE-15's cross-link.
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, FROM BIRTH — including the numbering pass.** The render-time format lint ships with the engine's first slice rather than being retrofitted.**
- ⬜ **FE-11 — Caption-body integrity: captions, titles, and pronouns render from roster data. Fork (roster capture §5.4): hard requirement — they cannot be hand-edited out of sync — or a lint/validation layer over free-form editing? The observed error rate argues the former; drafting flexibility argues the latter. Live evidence (roster REQ-16): caption-body drift is endemic across both banks — caption ≠ body plaintiff, filename ≠ caption party, singular/plural and pronoun drift, venue mismatches, store-number mismatches, parties in body missing from caption. The capture calls this error class the single strongest practice argument in the bank for a roster-driven engine: if the party list is data, every one of these defects becomes unrepresentable.** Entered 2026-08-12 (roster-mining REQ-CAPTURE). Distinct from FE-9: FE-9 compares family documents to each other; FE-11 compares documents to the roster. Interacts with FE-8's diff model — attorney edits are expected input, not defects. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; UIM-UDJA REQ-CAPTURE REQ-04, ruled — no new ID, one home per question per CL2-AC-1): the roster-driven consistency sweep covers EVERY STORY of the document — headers, footers, and footnotes — not caption-vs-body alone (live evidence: one exemplar carried THREE different insurer identities — one in the caption, a second in the body, and a third in the running page footer; caption-vs-body checking alone would have missed the footer). Two further sub-questions ride here — (UIM capture §5 Q2) when the consistency checker sweeps headers and footers for party identifiers, how should it distinguish a deliberate short-form (a footer that intentionally uses a shortened case style) from genuine drift against the roster? (§5 Q3) should "non-party tortfeasor" be a formal roster role in first-party coverage suits — the person appears throughout the declarations and facts but is not a party — and if so, does the checker verify their name consistency with the same rigor as parties?** Footer merge fields ({{case_short_style}}, {{instrument_name}}, page X of Y) route to the form engine as format-profile evidence, not an afterthought — REQ-04's practice moment is the proof.
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1, home named: the DISCOVERY slice.** Stated explicitly because the two look alike: **FE-D1's lint does NOT absorb FE-11.** FE-10 compares a document to its format profile; FE-11 compares a document to the ROSTER.**
- ⬜ **FE-12 — Template/exemplar provenance attribute: format-authoritative / content-only / known-bad-kept-for-reference. ATTORNEY INSTRUCTION OF RECORD (2026-08-11, standing, binding on drafting regardless of how this item resolves): the current-practice bank is format authority; the prior-firm bank is content-only, its formats expressly non-authoritative. Sub-question (roster capture §5.6): should source-trust level become an explicit attribute in the template library design, and does "known-bad, kept for reference" deserve first-class status, given a defective exemplar ("BAD DO NOT USE") was found filed alongside good ones?** Entered 2026-08-12 (roster-mining REQ-CAPTURE). Cross-links: FE-7 (distilled templates arrive born format-authoritative); the docs/templates/ home ruled in #48. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; deficiency-handling REQ-CAPTURE REQ-05, ruled — no new ID): the provenance attribute gains RENDER-TIME ENFORCEMENT. Per-source authority levels (format-authoritative / content-only / known-bad-kept-for-reference) are enforced when an instrument renders, generalizing the two-bank ATTORNEY INSTRUCTION OF RECORD to a per-instrument rule (live evidence from one letter: the house letterhead asset was the formatting authority, a third-party CLE treatise form was the content model, and language was mined from a prior-firm exemplar whose header was expressly forbidden — three sources, three different authority levels, one document).**
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, FROM BIRTH.** Provenance tiers are enforced at render time from the engine's first slice.**
- ⬜ **FE-§11.1 — Document production / Bates-stamping module** (auto-stamping pipeline, numbering scheme, prefix conventions, output formats). **BANKED — needs its own design session.** *(Registered 2026-08-12 (#63) by a commissioned check of `form-engine.md` §11 against this file: it had no row here. The only "Bates" text in this file was DE-1's `evidence_refs[]`, which is a different thing — item-level pins to produced documents, not a stamping pipeline.)*
- ⬜ **FE-§11.2 — Remaining template conversions beyond disclosures** — designation of lead counsel, motion to withdraw, discovery requests and responses, criminal forms. **Later slices; explicitly OUT of FE-D1.** *(Registered 2026-08-12 (#63), same commissioned check — no row existed.)*
- ⬜ **FE-§11.4 — In-app template editor UX pass.** FE-D1 builds a **MINIMAL** editor (edit → new `template_version`, no styling UI) because §1's Michael-owns-the-wording principle requires *some* editor; **the UX pass itself stays open as a later item.** *(Registered 2026-08-12 (#63), same commissioned check — no row existed.)*
- ⬜ **FE-13 — Instrument transform as a first-class named operation: "draft instrument for matter B using exemplar E for structure and matter A for facts," with the field mapping (who maps to whom) surfaced for attorney confirmation (live evidence: the drafting instruction was literally "redraft document X using document Y as a guide" — same plaintiff and occurrence, new defendant, new instrument posture, the former defendants recast as non-parties).** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). Cross-links: FE-7 (distillation queue), FE-12 (exemplar provenance — E must be format-authoritative under the two-bank ATTORNEY INSTRUCTION OF RECORD), FE-8 (attorney-edit diffing on the transform output). Template candidate flagged by the capture for FE-7's §13.4 queue: `template_petition_udja-uim-carrier.md`. *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1.** Instrument transform needs FE-8's diff half plus the exemplar bank; later slice.**
- ⬜ **FE-14 — Relief bracket as a per-matter picklist among the fixed statutory options, rendered into the pleading template (live evidence: the rule-mandated relief statement had to be swapped between fixed statutory brackets, and the exemplar's bracket did not match the new case).** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). **Gated on the TRCP 47(b)–(c) registry entry, which is UNVERIFIED — the option list's wording is not encoded until Michael verifies.** *(That is a REGISTRY gate under CLAUDE.md's rule 1, not the #54 slice-scoping treatment; see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1** — pleading-side, and separately **gated on the UNVERIFIED TRCP 47(b)–(c) registry entry**, so it could not ship regardless.**
- ⬜ **FE-15 — Instrument posture (original / amended / supplemental) as a render parameter driving title, certificate-of-service inclusion, and footer instrument name TOGETHER (live evidence: converting a "first amended petition" exemplar into an "original petition" changed the title, required deleting the certificate of service — an original petition is served with citation, not by certificate — and changed the footer's instrument name). Sub-question (UIM capture §5 Q4): should instrument posture automatically include or exclude the certificate of service at render time, or only warn the attorney that the combination looks wrong?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). Cross-links: **IN-4** — its certificate-of-service logic must be posture-aware, because an original petition carries no certificate at all — and **FE-10** (format profiles own the footer). *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, SCOPED TO DISCLOSURES POSTURE ONLY.** The full original/amended/supplemental parameter across instrument families is not in this slice.**
- ⬜ **FE-16 — Citation bank per instrument class: reusable citation blocks tagged UNVERIFIED until the attorney verifies, with verified status persisting across drafts, backed by the legal rule registry (live evidence: every rule and case citation in the deficiency letter and motion came from a third-party CLE treatise and is unverified; drafts carry a delete-before-service banner saying so — that banner is the interim practice).** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority soon). Registry discipline governs: automation flags, only Michael verifies. **EXTENDED the same day by the UIM capture's REQ-09** — the bank stores the canonical current cite with verification state and flags slip cites (docket number + Westlaw) that likely have reporter cites by now; live evidence on both sides (an exemplar carried a slip-opinion cite for a case long since published in the reporter; the deficiency capture's own §4 carries five WL/slip cites, six by Code's count — see the registry note). That currency flag also routes onto `docs/specs/registry-courtlistener-integration-design.md` as evidence (Layer B cite validation; banked feature #13 citation-currency). **Q-6 posture UNCHANGED — nothing here authorizes CourtListener integration.** *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1.** Citation bank is registry/CourtListener-adjacent; **Q-6 posture untouched**; later slice.**
- ⬜ **FE-17 — Internal/outbound channel with render-time exclusion: a hard flag on every item, enforced by the renderer at the schema level and never by convention, so attorney-only rows physically cannot emit into an outbound instrument (live evidence: the deficiency grid contains impeachment material, a defense-favorable fact, and a verifying-officer credibility overlap that must NEVER render into an outbound letter or motion). Sub-question (deficiency capture §5 Q4): should the internal/outbound flag be enforced at the schema level — a renderer physically cannot emit internal items into an outbound instrument — and should the same mechanism serve the privilege firewall's genericization gate elsewhere? Second question riding here (§5 Q5): when a defined term in a propounded set draws a definitional dodge (the responder denies the term describes anything at its facility), should the software support a "stipulated definition" object that, once offered in a conferral letter, propagates into amended requests, the motion, and the proposed order automatically?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). §5 Q4 makes this **bigger than deficiency work** — the genericization gate is a privilege-firewall question. §5 Q5 lands here as the nearest render-layer home, **flagged for re-homing if the DE series grows a definitions object.** *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1 — but with a BINDING FROM-BIRTH ANNOTATION: it rides whichever slice creates the item table.** Recorded so the internal/outbound flag can never be retrofitted onto a table that already exists without it.**
- ✅ **FE-gating — CLOSED 2026-08-12 (#63): FE-13–FE-17 TAKE THE #54 TREATMENT**, ruled at the form engine's slice-scoping session exactly as FE-8–FE-12 were. The question was: do the five new FE items inherit the #54 gating, given that #54 was ruled the day before they existed? Answer: yes. All ten open FE items were dispositioned in that same session; the full table is in `docs/specs/fe-d1-build-slice.md`.
- ✅ **Distillation candidates — CLOSED 2026-08-12 (#63): all four ENTERED `form-engine.md` §13.4** — `template_petition_udja-uim-carrier.md`, `template_deficiency-letter_written-discovery.md`, `template_motion-to-compel_written-discovery.md`, `template_deficiency-grid_columns.md`. **Entering is not adopting:** each is `queued`, none distilled, none format-authoritative, and FE-7's guardrails hold — adoption is a separate explicit ruling of yours, and adoption is what flips a template's FE-12 provenance to format-authoritative.
- ⬜ **Placeholder discipline — a CARRY, not an open question (entered 2026-08-12, #60). The deficiency capture's REQ-10 dispositioned to a content route: placeholder inventory per document plus a fill checklist at delivery, into the form-engine spec as documented method** (live evidence: service dates of the propounded sets, conferral details, and the bar number were unknown to the drafting session, so drafts carried `{{snake_case}} `placeholders for the attorney to fill and file — already house practice in both practice-project sessions, now written down). **CONFIRMED AS CARRY by Michael 2026-08-12:** it rides whichever future session next amends `form-engine.md` — likely the engine's slice-scoping session. **Nothing is owed now, and the edit is design's, never Code's.** *(Registered here by the twenty-first invocation. The twentieth recorded the carry in the session log and BUILD-STATE but never gave it a queue row, so this is the row — reconciled, not duplicated.)*

### Contact directory (CD series) — ADDED 2026-08-08

**New heading, Code's call per the packet's §4.2** — the file already sections §7 by series (FE, PL), so a CD heading fits the structure rather than fighting it. Placed directly beneath the FE series because CD-1 supersedes FE-1's framing.

- ⬜ **CD-1 — One global contact directory as the identity source for the form engine and all case-linked people/entities; provider is a role on it. How does it relate to `case_parties` and `case_clients` — views over one directory, or separate tables with links?** Living spec — revisited as modules surface new field needs. Needs its own design session with schema on screen. NOT authorized for build. Issued 2026-08-08, superseding FE-1's provider framing. **Path RULED 2026-08-11:** the session fires after a BOUNDED evidence pass — opportunistic REQ-CAPTUREs continue, plus ONE deliberate roster-mining pass over the document bank (run in PI DISCOVERY, Michael's hand) — then the typed session with schema on screen. Prep scaffold: `docs/specs/cd-1-session-prep.md`. **CONSTRAINT (roster capture REQ-14, ruled 2026-08-12): sides are a property of the case type, not a constant — any roster model with exactly two sides fails on documents the practice demonstrably files (interpleader, certificates of interested parties, scire facias, ex parte petitions, ad litem joinder by court action). GATE MET 2026-08-12: the bounded evidence pass of #48 ruling 4 is complete — the roster-mining capture is reconciled and ruled; the schema session fires at Michael's choosing. SESSION QUESTIONS added from the roster capture, full text in `docs/specs/REQ-CAPTURE_roster-mining-pass_2026-08-11.md` §5.1 (probate roster modeling: defer entirely, or reserve now for the estate-representative/heirs pattern the PI documents already prove), §5.2 (non-party actors with roles in scope from day one, or parties-only first — the mined evidence says parties-only misses the people drafting revolves around; the gate finding, REQ-15), §5.3 (attorneys-of-record in the same directory with a role, or a separate structure), §5.5 (identity resolution across roles/cases/aliases at CD-1, or per-case identity for the first consumer).** **RULED 2026-08-11 — the schema session ran; every CD-1 question was ruled item by item. Spec: `docs/specs/contact-directory.md` (LIVING SPEC — revisited as modules surface new field needs; additions to its §5/§6 controlled vocabularies are spec-level acts). The core fork resolves as SEPARATE TABLES WITH LINKS, the shape already built: `parties` IS the directory, `case_parties` stays the roster link, `case_clients` stays parallel, D-CL2-8 untouched. Session questions are answered in the spec: §2 scope (non-parties in from day one; attorneys in the same directory, attorney is a role; probate reserves the PI-proven estate-adjacent pattern only), §3.3 identity resolution (none at CD-1 — cross-case identity is inherent in the one-global-row shape; merge-two-contacts is a named future need). BUILD STILL NOT AUTHORIZED — no table, no migration, no UI, no `party_type` migration. This entry stays open as the living-spec pointer.** *(The prep scaffold `docs/specs/cd-1-session-prep.md` was absorbed by the spec and deleted the same day, per its own delete-or-absorb line.)* **BUILD SLICE AUTHORIZED 2026-08-11 within the scope of `docs/specs/cd1-build-slice.md`** — seven IN items (parties evolution; `case_parties` evolution with flags-never-guesses backfill per the CL-2 pattern; roster definitions as data, seeded only for bank-evidenced case types; the contact-edges table; directory UI including the scope-label edit surface; RLS + probe extension from birth; live migration by Michael's hand). Explicit OUTs: form engine, IN-2 fact table, merge tooling, service-story fields, probate beyond the reserved pattern, the `/rules` seed. **The spec remains the design authority; living-spec posture unchanged; anything outside that slice's scope remains unauthorized.** The build is a separate Code session, not the queue runner.
- ⬜ **CD-2 — Case-type party rosters + typed contact relationships (the "family" layer). Each case type carries an expected roster of party roles presented as slots at intake — never auto-created records (trucking PI: carrier / driver / lessor / trainer / parent; premises: owner / occupier / management; probate: decedent / personal representative / heirs / creditors; criminal: defendant / complainant / prosecution) — and directory contacts carry typed edges (parent/subsidiary, employer/employee, spouse, heir-of). Role tags drive downstream content selection: the same RFA flips phrasing depending on whether the target IS the tractor owner or a sibling of it, so tags are template selectors, not labels. How do rosters attach to the case-type tree, and how do contact edges relate to the directory schema?** FRAMING RULED 2026-08-11 (general across practice areas — trucking corporate edges and probate family edges are one structure; roster mechanism belongs to the case-type model, not a trucking feature later abstracted). Design UNRULED — input to the CD-1 session. NOT authorized for build. Distinct from CL-1: `case_links` links cases; this links contacts — a CD-1 session names both so nobody merges them later. Evidence: REQ-CAPTURE 2026-08-11 (trucking first sets); probate is the second real consumer. **EVIDENCE LANDED 2026-08-12: the roster-mining capture (`docs/specs/REQ-CAPTURE_roster-mining-pass_2026-08-11.md`) — per-type constellations REQ-01–10, typed-relationship inventory REQ-11, capacity multiplicity REQ-12 (party = person × capacity), expected/optional/rare slot frequencies REQ-13, service-path-as-party-attribute REQ-19 (cross-link IN-4). TTCA roster default (attorney practice note, 2026-08-12): governmental body alone in nearly all cases; employee joinder deliberate and exceptional.** **RULED 2026-08-11 into the spec — BOTH layers. This entry becomes a pointer; the evidence annotations above stay.** Roster layer → `docs/specs/contact-directory.md` §4: slot definitions are data on the case-type tree with inheritance and expectancy tiers (expected / optional / rare-with-procedure), never auto-creating records; the roster entry decomposes into role / caption alignment (drawn from the case type's defined side set — REQ-14 satisfied by construction, "Plaintiff" is an alignment, not a role) / party status / firm perspective (the existing `side` column's true meaning); entries are history, not snapshot (joined-by + active state, because FE-8 and IN-4 both need roster-as-of). Relationship layer → §5: one directional typed edge structure with optional case scope (null = world fact), a controlled extensible vocabulary seeded from REQ-11, the one-home rule (capacity references stay on the roster entry and never auto-create edges), and the **CL-1 firewall named** — contact edges and `case_links` never merge, never share a structure, and neither ever holds the other's kind of link (the D-CL1 items stay untouched and unruled). **NOT authorized for build.** **EVIDENCE ADDED 2026-08-12 (#58), CD-1/CD-2 both, full text per QR-1 — two annotations, no new IDs:** (a) *registered agent as entity-level data* (UIM capture REQ-07, same home as roster REQ-19's service-path-as-party-attribute) — the service paragraph's agent name and address were inherited from an exemplar and had to be independently checked, so registered-agent name and address are stored per entity with a **verify-before-service state and a staleness date, never copied as prose**; (b) *verification-officer tracking* (deficiency capture REQ-11, priority someday) — **track who verified which response set (contact × role) and cross-reference against fact-witness roles**, live evidence being that the same officer verified interrogatory answers and personally conducted the investigation those answers denied, a deposition/credibility lever. (b) is carried at full length deliberately: it is the K-6/K-7 cheap insurance. **EVIDENCE ADDED 2026-08-12 (#62), from the Uvalde capture, no new IDs:** (c) *the county prosecutor roster* — contacts with roles, with collision-safe short codes, is directory substrate; the requirement itself is **CR-2**; (d) *contacts-at-capture is SATISFIED BY CONSTRUCTION* — "contacts with roles attach to the matter at capture" is the built directory plus roster link (log #61, items 1–2), so it took no ID at all; its print-forward half rides **CR-9**. **(e) ADDED 2026-08-12 (#64): carrier-ID and financial-responsibility TYPE codes from the CR-3 code sheet are directory-vocabulary CANDIDATES** — PROPOSED only, pointer at `docs/specs/cr3-field-code-map.md`. **Adding directory vocabulary is a spec-level act on the living spec (the CD-3 precedent), so nothing was added.**
- ✅ **`party_type` RETENTION — CONFIRMED by Michael 2026-08-12 (#62).** The CD-1 build **retained** `party_type` where the slice text said "retiring" it, and disclosed the deviation in its #61 handback. **Confirmed, not merely accepted:** dropping the column meant rebuilding registry field rendering, which is outside the slice's scope; role tags supersede it as the thing the app filters on; the tag vocabulary is **derived** from the party-type registry so the two cannot drift; and `role_tags[0]` mirrors it. `docs/specs/cd1-build-slice.md` carries a dated qualification note — **the original slice text stands; nothing was silently rewritten.**
- ⬜ **CD-3 — Insurer entity subtype (county mutual / foreign insurer / domestic company) as directory-level data, with a flag raised whenever the defendant slot changes and citizenship/diversity implications follow (live evidence: the exemplar's defendant paragraph pleaded Texas citizenship — accurate for its county-mutual defendant, inaccurate and removal-relevant for a foreign insurer in the new defendant slot). Sub-question (UIM capture §5 Q5): is entity-type-driven removal-exposure flagging — a foreign insurer in the defendant slot plus a relief bracket above the federal jurisdictional threshold — in scope for the software's deadline/strategy engine, or is that attorney-only analysis the software should merely annotate?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority soon). **Flag-only per registry discipline — the software annotates, the attorney analyzes.** Additions to `contact-directory.md`'s §5/§6 controlled vocabularies are spec-level acts under its living-spec posture, so the subtype vocabulary is not written by a Code session. **NOT authorized for build.**

*(**CD-4 – CD-13 ADDED 2026-08-13, twenty-ninth queue-runner invocation**, from the CD-2 role mining pass — `docs/specs/cd2-role-mining-pass-2026-08-13.md`, PROPOSED design input, data prep only. The packet carried them as packet-local **Q1–Q10** and explicitly declined to mint a series, leaving ID assignment to the runner. **Collision check run repo-wide before assignment: only CD-1, CD-2, CD-3 existed anywhere in `docs/`, so CD-4+ is a continuation of the existing series, not a new one** — which is what the packet's §7 named as the natural home. Mapping for anyone tracing the packet: Q1→CD-4, Q2→CD-5, Q3→CD-6, Q4→CD-7, Q5→CD-8, Q6→CD-9, Q7→CD-10, Q8→CD-11, Q9→CD-12, Q10→CD-13. Full question text carried per QR-1 — the packet is deleted after processing, so these rows are the only place the questions survive. **None is authorized for build; the mining pass rules nothing and the CD-2 row above stays a pointer.**)*

- ⬜ **CD-4 — Should the two source files that hold the authoritative role vocabulary — `src/domain/partyRegistry.ts` and `src/domain/roster.ts` — be pasted into a design chat so the CD-2 mining pass can mark BUILT vs. CANDIDATE with certainty instead of `TAG-CHECK: NOT RUN`? The project instructions bar design sessions from reading `src/` and name pasting as the escape hatch; roughly 60 rows in the pass's §3 turn on it. Alternatively: should a Code session run the comparison instead, since Code reads `src/` freely?** Entered 2026-08-13 (CD-2 mining pass §5 Q1). **Blocks precision on ~60 rows.** The design session had bridge access to both files this session and **deliberately did not open them**, treating bridge access as no repeal of the design-side `src/` bar — recorded because the restraint is the reason the gap is honest rather than papered over. **The packet's §6 forbids a Code session running the comparison unprompted:** converting `TAG-CHECK: NOT RUN` into resolved rows changes a design-side artifact's evidentiary basis, and this row offers it as an option Michael has not chosen. Two words close it either way.
- ⬜ **CD-5 — Is a role that is defined by the ABSENCE of another role — probate's "interested person" (§352.052(c) excludes creditors and claimants) and property law's "derivative claimant" ("a claimant other than an original contractor", §53.056) — expressible in the ruled roster model at all, or does it need a new construct?** Entered 2026-08-13 (CD-2 mining pass §5 Q2). Two independent practice areas produced the same shape from unrelated statutes, which argues it is **structural rather than incidental**; nothing in the built vocabulary expresses it. Practical consequence named by the pass: a generic "interested party" contact label **silently admits the excluded classes** — the exclusion IS the entry.
- ⬜ **CD-6 — Should `case_parties.role` remain FREE TEXT? It is the axis the whole taxonomy is about, and it is the only one of the six ruled axes with no CHECK constraint, no enum, and no lookup table — `party_status`, `capacity_kind`, `joined_by`, `active_state`, and the 19 edge types are all DB-enforced; `role` is not.** Entered 2026-08-13 (CD-2 mining pass §5 Q3). **The packet's §6 forbids a Code session adding a CHECK constraint to that column** — this is Michael's ruling, not a cleanup. Related fact from the same pass: `role_tags` likewise has no enum, no CHECK, and no lookup table; its vocabulary is enforced **entirely in TypeScript** (a derivation guarantee, not a database one).
- ⬜ **CD-7 — Should the asymmetric contact-edge types get their inverse directions — `lessee-of` to match `lessor-of`, `bailor-of` to match `bailee-of`? Adding an edge type is a spec-level act, so this is a ruling, not a fix.** Entered 2026-08-13 (CD-2 mining pass §5 Q4). **The packet's §6 forbids "fixing" the one-directional edges** — the absences are deliberate pending this ruling. Only one direction of each asymmetric edge exists in the built 19.
- ⬜ **CD-8 — Should `parent-of` be split? It currently serves both corporate parent/subsidiary (the trucking holding-company family) and human parent/child (the next-friend case). One edge type, two ontologies.** Entered 2026-08-13 (CD-2 mining pass §5 Q5). **The packet's §6 forbids resolving the overload** by splitting, aliasing, or adding a discriminator.
- ⬜ **CD-9 — Four separate party types (Business, Insurance company, Government entity, Provider business) each carry their own `registered agent (name & address)` field, while `registered-agent-of` also exists as an edge type. That is duplicated identity across types — the condition the enter-once principle exists to prevent. Consolidate onto the edge, or keep the fields?** Entered 2026-08-13 (CD-2 mining pass §5 Q6). Cross-links the CD-2 evidence annotation (a): registered-agent name and address are stored **per entity with a verify-before-service state and a staleness date, never copied as prose** — whichever way this rules, that constraint rides along.
- ⬜ **CD-10 — Do internal firm actors (responsible attorney, assigned paralegal, timekeeper, the ReviewLog "who") live in the SAME person namespace as parties, or in a separate auth/user namespace?** Entered 2026-08-13 (CD-2 mining pass §5 Q7). The case-event core's `actor` field and the time facet's `timekeeper` both point at this unresolved namespace, and **the system is single-user today with staff later** — which is exactly the condition under which the wrong answer is cheap now and expensive later.
- ⬜ **CD-11 — On the criminal side, are "the State," "the DA's office," and the individual prosecutor one entity at three resolutions, or three records?** Entered 2026-08-13 (CD-2 mining pass §5 Q8). The record uses all three **interchangeably and never reconciles them**, while CR-7's three-way number mapping implies the DA's office holds its own identity namespace per cause. Cross-links CR-2 §5 Q1 (prosecutor as a cause-level role needing a provenance/confidence dimension no other contact role needs).
- ⬜ **CD-12 — Does BONDSMAN attach at cause level or matter level? The Uvalde capture lists it among fields "needed per cause/matter" without resolving which — bonds are written per cause, but a defendant may use one bondsman across causes. (Same question, third instance: does "client's mother" attach per matter or per client? A client with three causes has one mother.)** Entered 2026-08-13 (CD-2 mining pass §5 Q9). This is the **same ambiguity CR-2 §5 Q1 flags for prosecutor, and it was not flagged in the capture** — the pass caught it. Mirror-image datum from the same pass: jail/correctional facility is **client-level, not cause-level**.
- ⬜ **CD-13 — Is there a path for an ORGANIZATIONAL CLIENT? The Client party type is built entirely around an individual (DOB, SSN, driver's license, emergency contact, health insurer), while the Servpro restoration operator is a business and business plaintiffs exist on the civil line.** Entered 2026-08-13 (CD-2 mining pass §5 Q10). Related and already on the record as an open spec-feedback item: **the party-type promotion path (PNC → Client) does not exist**, so re-entering a person as a new Client party would split their cross-case history.

### Intake pipeline (IN series) — ADDED 2026-08-11

*(New heading by the same structural logic as the CD heading, #39. FE-2's parked build home already names "the intake pipeline" as a candidate landing zone; IN-1 and IN-2 arriving makes it look like a real module — drift named, not decided. **Elicitation caveat, kept on the record: the IN-series naming rode inside ruling 1's "Yes on 1"; if Michael meant FE numbers instead, renaming is a one-word veto before these IDs get load-bearing.** Placement note — the fold-in said "after the FE series," but the file documents at the CD heading that CD sits directly beneath FE deliberately, because CD-1 supersedes FE-1's framing; the IN heading therefore lands after the CD series rather than splitting that documented adjacency.)*

- ⬜ **IN-1 — Answer-mining for discovery hooks: when a defendant's filed answer is ingested, flag affirmative pleadings and capacity denials that support contention discovery and suggest request stubs (live evidence: an alternative-benefits-policy assertion and a not-proper-party denial each generated a contention interrogatory + RFP + RFAs). Is answer ingestion a distinct pipeline step, and where in the UI do suggestions surface?** Entered 2026-08-11 (REQ-CAPTURE §5.3). FE-2's rulings carry as PROPOSED defaults: flags for Michael, never auto-add; one-click dismissal; dismissals remembered per case. Honest capability note: recognizing "this paragraph pleads an affirmative defense" is document understanding, not string matching — the Phase-1b local-LLM capability class, and the spec should say so. **SPEC LANDED 2026-08-15 (#83): `docs/specs/in-1-answer-mining-spec-2026-08-15.md`** — and it lands in the OPPOSITE posture from the Task 8 form-engine specs. FE-4/FE-5/FE-6 were open because the **build** was gated while the design was RULED at `form-engine.md` §13; **IN-1 has no ruling to elaborate** — this row asks the design question in terms and nothing on the record answers it. **The spec therefore MAPS the design space and hands the question back; it says so on its face, and a build session must not read it as a design to implement.** **The row stays ⬜: a spec is not a closure.** Three findings ride below as Q-IN1-1..9. **IN-1 has no design-doc home at all** — word-bounded at HEAD it appears 16 times across 6 files (BUILD-STATE, this queue, the log, three REQ-CAPTUREs) and **in no design document**. **The four PROPOSED defaults on the line above are RULED for FE-2 only** — `form-engine.md` line 236 is their sole canonical statement and it rules them for FE-2's document-name sweep in a disclosures draft, not for mining a filed pleading; two restatements on the record are lossy (`cd1-build-slice.md` line 62 drops *"one click"*). **And the IN-6-as-substrate claim cannot be reconciled** (Q-IN1-3) — IN-6 parses a served *response set*, IN-1's input is a filed *answer*: different documents, different procedural moments, so IN-1 cannot silently inherit IN-6's parser or entity shape.
- ⬜ **IN-2 — Crash-report field extraction + discrepancy flags: structured extraction of CR-3 fields (VINs, carrier identity, DOT number, owner/lessee, driver-license class codes) into the case record, plus automatic flags when petition allegations and crash-report fields diverge (live evidence: two real conflicts — incident time; pleaded vehicle ownership). Implies per-field SOURCE ATTRIBUTION on the case record ("this VIN came from the crash report; the petition says otherwise") — a data-model decision touching CD-1 (vehicle/instrumentality identity) and the intake spec, not a feature bolt-on. Where does source attribution live?** Entered 2026-08-11 (REQ-CAPTURE). Extraction half is Phase-1b-class ingestion (easiest possible target — fixed state form — but still PDF ingestion, GPU-arm gated). **EXTENDED 2026-08-12 (attorney-edit REQ-CAPTURE, ruled — no new ID, one home per question per the CL2-AC-1 dedup principle): generalized to source-fact pinning. Case facts used in any instrument are first-class data — {fact_id, value, source_document, source_field, extraction_method, verified_by_attorney} — referenced by merge fields, so every instrument can be re-verified mechanically and a corrected fact propagates to every instrument that uses it. Image-only sources (scanned forms with no text layer) take OCR or vision extraction with a confidence flag. Sub-question (capture §5.3): what confidence threshold or human-confirmation step gates a "fact verified" state, and should an unverifiable fact block the finalized state (interacts with IN-4's lifecycle)?** **HOME QUESTION RULED 2026-08-11 — `docs/specs/contact-directory.md` §7: source attribution lives on the CASE-SCOPED FACT TABLE ({fact_id, value, source_document, source_field, extraction_method, verified_by_attorney}), not on directory entries and not in a sidecar (FE-1c's silent-trust posture is preserved; a sidecar would erect a second provenance regime over data ruled to need none). Identity-class facts (a carrier's DOT number, a facility's legal name) stage on the fact table and promote into the directory entry ONLY by attorney verification — one direction of flow, no copies. Confidence scores order the review queue and drive display flags; NO threshold ever auto-verifies (registry discipline transposed to facts: automation flags, only Michael verifies), which answers the confidence half of the §5.3 sub-question. Discrepancy flags fall out of the shape — two sources asserting different values for one fact row IS the flag.** **STILL OPEN: the EXTRACTION half** — structured CR-3 field extraction remains Phase-1b-class ingestion, GPU-arm gated, and the question text above is retained in full because none of it was ruled. **The block-finalized sub-question ("should an unverifiable fact block the finalized instrument state") now RIDES IN-4** — deliberately not ruled in the CD-1 session. **EVIDENCE ADDED 2026-08-12 (#64): the authoritative CR-3 field/code inventory is filed at `docs/specs/cr3-field-code-map.md`** (synthesis, PROPOSED design input) **with the raw code sheet at `docs/reference/CR3-code-sheet-2023.pdf`** (Form CR-3CS 4/1/2023, governs wrecks 2023-forward). **The load-bearing requirement it carries: crash-report code tables are VERSIONED EXTERNAL VOCABULARIES.** The 2023+ sheet supersedes earlier sheets, so extraction must store **(code, decoded label, code-sheet version)** and the tables must carry effective dates — otherwise a 2022 wreck's codes decode against 2023 meanings and nothing signals it. The extraction design consumes this; **IN-2 stays queued and unauthorized.** **SPEC LANDED 2026-08-15 (#84): `docs/specs/in-2-crash-report-extraction-spec-2026-08-15.md`** — and its gate resolves **BOTH ways**, unlike either predecessor in this chain: the **HOME** half is RULED at `contact-directory.md` §7 and the spec **elaborates** it; the **EXTRACTION** half is unruled and the spec **only maps** it. The spec's §1.1 names which of its sections do which, so a build session cannot read a mapped paragraph as a ruled one. **The row stays ⬜: a spec is not a closure.** Nine findings ride below as Q-IN2-1..9. **The structural finding: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — coded fields (license class, carrier ID type) take versioned vocabularies; **VINs and the DOT number have no code list at all** and take format validation; carrier and owner identity take §7.2's promotion gate. **Three of the five are not class A**, so a build that implements #64 alone ships with no VIN validation and no promotion gate, silently. **Two lines of `cr3-field-code-map.md` are corrected in the spec and NEITHER file was edited:** list 38 has **70** enumerated values, not the *"(79 codes)"* stated — 79 is the highest code number, the space is sparse (5–13 and 72 absent), and **range validation is therefore wrong**; and the demographic question that file calls open **was RULED at #66**, which names *"CR-3 extraction"* as a governed intake path, making fields 17/18 the only IN-2 fields whose scope is ruled. **Also newly on the record: `oaa_intakes.fields_json` already carries "value/confidence/provenance per field"** — precedent for the shape, **not a substrate** (a `text` blob on an intake event, no `verified_by_attorney`, and two sources cannot disagree inside it); **`cases.date_of_incident` is a `date`, so the incident-time conflict the row itself cites has no column to conflict with**; and **vehicle identity has no home anywhere** — `parties.kind` admits only individual/organization, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO across the schema and all three migrations.
- ⬜ **IN-3 — Held sets with service triggers: a document/set status of HOLD with a defined trigger event (answer filed / appearance) that surfaces a finalize-and-serve prompt (live evidence: a full driver set drafted pre-appearance with placeholder COS). Trigger source PRE-ANSWERED for v1: MANUAL attorney input only, per the standing Q-6 posture — docket-watch is CourtListener integration, which is UNAUTHORIZED; the upgrade path routes through Q-6 whenever that is revisited, so this never becomes a second unguarded door into the same integration. What does the HOLD lifecycle look like in the document model?** Entered 2026-08-11 (REQ-CAPTURE §5.4). Cross-link 2026-08-12: IN-4 now holds the full instrument lifecycle; IN-3's HOLD/trigger stands separate as the pre-service fragment. **SPEC LANDED 2026-08-15 (#83): `docs/specs/in-3-held-sets-service-triggers-spec-2026-08-15.md`** — same posture as IN-1's: **no ruling to elaborate**, so the spec maps the design space and hands the question back. **The row stays ⬜.** Nine findings ride below as Q-IN3-1..9. **HOLD cannot be expressed on a document today:** `generated_documents` has **no status column of any kind**, its `doc_type` CHECK admits one value, and it has **no set/parent grouping column** — so the "set" in "held set" has no structural existence either; the token `held` exists twice in the schema (`staging_items.status`, `routing_decisions.action`), both on the transcript inbox, neither on a document. **And nothing can fire the trigger** — no case-event table, no docket table, no `appearance` column, and neither `case_parties.joined_by` nor `calendar_events.event_type` carries an `'answer'`/`'appearance'` value. **The two trigger facts were already observed at party level** — the origin capture's entity-level merge fields carry `{{answer_filed_date}}` and `{{appeared}}` (bool), evidence that trigger state belongs on the roster entry next to the service story `contact-directory.md` §6.2 already puts there. **Evidence, not a decision (Q-IN3-5).** **Sharpest scheduling consequence: Q-IN3-3** — `cd1-build-slice.md` and the CD-1 migration both deferred the service-story columns to *"the first instrument consumer"* and **that consumer has never been named**; IN-3 and IN-4 are both candidates, and CD-1's living-spec revisit opens on whichever is named.
- ⬜ **IN-4 — Instrument lifecycle states: generated → delivered → attorney-edited → finalized → served, each with timestamp and file provenance; the certificate-of-service date is bound to the SERVICE EVENT — stamped at service time, never typed into the text — and drives downstream deadline computation (live evidence: a hand-filled certificate date across five documents goes silently stale if service slips a day). Sub-question (attorney-edit capture §5.4): how does the system handle mixed service across a document family (some recipients served one day, others later) — one certificate per recipient with per-recipient dates?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Cross-links: IN-3 (HOLD + service trigger — the pre-service lifecycle fragment; stands separate); the queued deadline-engine input (per-defendant service dates within one wave, each response deadline computed independently); roster REQ-19 (service path as a party attribute).
- ⬜ **IN-5 — Disclosure-mining: when the other side's disclosures are ingested, extract the four categories the attorney reads them for — identities of responsible third parties alleged by the other side; identities of witnesses or persons with knowledge of the claims and defenses; insuring agreements; identities and/or production of documents that side intends to use in the prosecution/defense of their case — feeding the contact roster and the case record. Where do the extracted hooks surface, and how do they interact with IN-1's answer-mining suggestions?** Entered 2026-08-12, sourced to Michael's direct statement in the design session (not a REQ-CAPTURE). Sibling of IN-1 (same flags-only, never-auto-add defaults carry as PROPOSED). Cross-links: CD-1's non-party scope question (three of the four categories are non-party actors); the "who is whose insurer" relationship (roster REQ-06). Legal substrate: the expanded TRCP 194 registry entry (VERIFIED 2026-08-12). Document-understanding capability class, Phase-1b-adjacent, like IN-1.
- ⬜ **IN-6 — Response-set ingestion: parse a served response set into structured items (instrument type, number, request text, objections[], answer text) — the mirror image of the incoming-request ingestion requirement already observed on the responding side (live evidence: the opponent's response documents restate each request followed by objections and an answer, and building the deficiency grid required manually decomposing about 60 items).** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). This is **the substrate IN-1's answer-mining runs on** — without structured response items there is nothing for answer-mining to mine. Cross-links IN-1, IN-5. Entity shape observed by the capture: `response_set` {set_number, instrument_mix, served_date, verifier} and `response_item` {instrument_type: rog|rfp|rfa, number, request_text, objections[], answer_text, categories[], evidence_refs[], cure_demand, outbound: bool, cluster}.
- ⬜ **IN-7 — Cross-document contradiction detection: link answer assertions to contrary evidence in the produced record; even manual flagging with structured links would capture most of the value. Live evidence — the highest-value deficiencies were contradictions between the opponent's answers and its OWN production and disclosures: an answer denying any investigation while produced statement forms recite an investigation on their face; an admission denial contradicted by a same-day, same-declarant interrogatory answer; a persons-with-knowledge disclosure omitting people named in the opponent's own documents.** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority soon). **Manual structured links first**, per the capture's own instinct. Document-understanding capability class, Phase-1b-adjacent, like IN-1/IN-5; **flags only, never conclusions.**

### Deficiency enforcement (DE series) — ADDED 2026-08-12

*(**Series RULED into existence by Michael, 2026-08-12**, in the same group ruling that assigned the deficiency capture's durable IDs — the anchor item fits neither FE (not rendering) nor IN (not intake), which is the **WF-1 structural precedent**; a WF home was offered and declined. Placement is Code's call and is stated rather than buried: the file sections §7 by series, and DE lands directly after the IN series because **its own substrate is an IN item** — DE-1's item store is what IN-6's response-set ingestion feeds — so the reading order FE → CD → IN → DE follows the data, with WF/DL/PL/H after. **The #48 elicitation caveat applies to DE exactly as it does to IN: renaming is a one-word veto until these IDs get load-bearing.**)*

- ⬜ **DE-1 — Deficiency taxonomy as first-class data: a classification vocabulary applied per item — boilerplate objection (BP); answer given "subject to" objections (STO); objection-only with no substantive response (OO); evasive or incomplete (EV); contradicted by the responder's own documents (CONTRA); privilege asserted without the required withholding procedure (PRIV); and RFP responses stating none of the rule's permitted response forms — driving downstream rendering. It is the enforcement-side sibling of the objection library already observed on the responding side. ABSORBS the module's shape (capture REQ-04): one item store with renderers per instrument — internal working grid (all items), deficiency letter (outbound items, per-item cure paragraphs, doctrinal preamble, conferral designation and fuse), motion chart and proposed order (outbound items grouped by cluster, sustain/overrule column left blank for the court) — with clusters grouped by subject matter rather than seriatim, per the treatise's judge-facing advice. ALSO ABSORBS evidence pinning (capture REQ-08): item-level `evidence_refs[]` resolving to produced-document identifiers and rendered inline in letters and motions, because deficiency assertions gain force by citing the opponent's own production by Bates range. Three questions ride here — (§5 Q1) should the deficiency taxonomy (enforcement side) and the objection library (responding side) be modeled as ONE shared vocabulary viewed from two postures, since an objection pattern we assert when responding is the same object we attack when enforcing, or as two separate libraries that merely cross-reference each other? (§5 Q3) how should motion-chart subject-matter clusters be assigned — manually at enforcement time, or derived from topic tags attached to each request when the set was originally propounded, which would argue for tagging requests at drafting time in the propounding module? (§5 Q6) the treatise recommends multiple small motions to compel, one subject area each, over one omnibus motion — should the motion renderer support splitting one deficiency data set into several instrument instances, and how should the conferral certificate and exhibit lists be shared across them?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). **Series anchor.** Cross-links: **FE-6** (same one-item-model, many-renderings principle on the propounding side — **cross-linked, deliberately NOT merged**: different instrument classes); **IN-2** (source attribution, same family as `evidence_refs[]`). **Design substrate, reconciled as CONFIRMATION rather than collision: the parked discovery-deficiency engine (case-heartbeat walkthrough capture, Part 6 — the Bright treatise closed list, the TRCP 193.2(e) waiver linchpin, the thread-shaped workflow). The CLE treatise this capture's instruments used IS that same paper, and REQ-02's taxonomy is that closed list made data. DE-1/DE-2 are the parked buildout's durable HOME, not its green light — nothing here is authorized for build.**
- ⬜ **DE-2 — Conferral/escalation timeline engine: letter → cure deadline → motion → hearing → compliance deadline, computed as PROPOSALS for attorney verification and integrated with docket-control-order deadlines (live evidence: the letter carries a cure fuse of N days from the letter date; the motion's conference certificate must recite the effort; the agreed DCO imposes a discovery cutoff, a serve-by date of cutoff minus 30 days, expert deadlines, and mediation — and the enforcement track must land compelled production before expert workup needs it). Sub-question (§5 Q2): should the software track the opponent's response-service dates and automatically propose a conferral fuse and escalation timeline, as proposals for attorney confirmation, or should enforcement timelines be created only manually?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). Shape observed: `escalation_timeline` {letter_date, cure_days, cure_deadline, motion_filed, hearing, compliance_deadline}, **all as proposals**. Cross-links: **DL-INPUT** (per-defendant service dates) and **IN-3** (HOLD / service trigger). **Q-6 posture untouched — triggers are MANUAL for v1; no docket-watch.** NOT authorized for build.

### Workflow channels (WF series) — ADDED 2026-08-12

*(New heading, Michael's ruling 2026-08-12: the item fits neither FE — not rendering — nor IN — not intake. Same structural logic as the IN heading in #48.)*

- ⬜ **WF-1 — Review-folder as a workflow channel: a per-matter inbox/outbox convention the software understands — where generated drafts land, where attorney-edited files return — with state transitions inferred from file arrival (live evidence: the attorney's working convention is a dedicated "drafts for review" folder, separate from the generated-output location).** Entered 2026-08-12 (attorney-edit REQ-CAPTURE, priority someday). Kin to the design-side email-workflow requirements doc (PROPOSED, un-packetized). Feeds IN-4's delivered → attorney-edited transition.

*(**WF-2–WF-8 ADDED 2026-08-12 (#63)** by Michael's group ruling adopting `docs/specs/email-workflow-requirements.md` into the repo. Adoption makes the catalog part of the record; **it builds, schedules, and authorizes nothing.** Each row carries its GATE — the thing that must exist or be ruled before the pipeline is buildable — so none can silently die, the K-6/K-7 insurance. The standing carry "email-workflow doc, PROPOSED and un-packetized" **CLOSES** with these.)*

*(**ALL SEVEN ROWS ANNOTATED add-only 2026-08-15 (#85), FORTIETH queue-runner invocation — NONE CLOSED, ALL STAY ⬜.** A specification pass landed at `docs/specs/wf-2-wf-8-email-workflow-spec-2026-08-15.md` (CHAT-DISPATCH Task 11, PROPOSED throughout; Michael did not participate and made no rulings). **The spec MAPS rather than elaborates** — its §1.1 draws that line section by section, because **an ADOPTED document is not an adopted design**: #63's own words say three times over that it *"builds, schedules, and authorizes nothing,"* and three rows below say **"Module design UNRULED"** in terms. **A spec is not a closure.** Findings recorded here, none of them a ruling: **(a) SEVEN OF SEVEN GATED, ZERO SATISFIED**, checked row by row against BUILD-STATE at HEAD — **no WF item is buildable today and none becomes buildable by a design pass**, since every gate is either Michael's ruling or another module's construction. **(b) `T3` is an unflagged namespace collision** — two independent `T1`–`T4` tiers exist in `docs/specs/`, the transcription pipeline service (`transcript-sort-and-route-design.md:148`, **unauthorized** under KICK-1) and the LegiScan poller tier (`statute-text-and-bill-tracking-design.md:107`, **substantially built**) — and the two readings gate **oppositely**. **FLAGGED, NOT RENAMED (Q-WF-2)**; the H → HK precedent at #66 makes re-lettering Michael's act. **(c) The hardest gate is on no row at all (Q-WF-4)** — every WF pipeline is a background watcher measured in days and weeks, and the single auth surface is a single-tenant public-client browser SPA with one delegated scope and **no server-side identity**. **(d) `docs/specs/outlook-email-intake.md`'s HIPAA-by-design constraint is cited by neither `email-workflow-requirements.md` nor these rows nor #63 (Q-WF-5) — so the BAA/compliance review is not among the seven named gates. It was NOT added as one: adding a gate to a ruled gate table is a ruling.** **(e) Q-WF-10 asks whether these rows should carry pointer annotations to five substantive treatments elsewhere in the repo that the rows do not cite; the spec's §5 records which. NOT ADDED — that is Michael's.** **No existing repo file was edited by that session or this runner:** `email-workflow-requirements.md` was adopted **VERBATIM** and stays untouched, as does `outlook-email-intake.md`.)*

- ⬜ **WF-2 — E-filing envelope lifecycle.** GATE: inbox-facing detection is gated on **T3** (KICK-1 governs, so T3 work is itself unauthorized until Michael locates or re-issues the kickoff doc). The envelope-number dedupe key is recorded as a **T3 design constraint**. Module design UNRULED. **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-2 subsection), not designed.** Gate re-verified UNSATISFIED: T3 is unauthorized under KICK-1, **and which `T3` the row means is itself in question (Q-WF-2)**. No table, column, or migration exists for it — the word-bounded probe for `envelope` across `db/schema.sql` and all three migrations returns **0**.
- ⬜ **WF-3 — E-signature lifecycle.** GATE: **T3** for inbox detection; final-PDF capture needs the **document-storage model**, which does not exist — form-engine §10's OneDrive-plus-metadata pattern is the nearest precedent, not a decision. Module design UNRULED. **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-3 subsection), not designed.** Both gates re-verified UNSATISFIED. The spec adds a **negative** finding worth having: `generated_documents.privilege_tier` is a real precedent for classifying stored content by privilege, **but that table cannot serve WF-3** — its `doc_type` CHECK admits exactly one value and it has no status and no set/parent column. **A precedent for a column, not a home.**
- ⬜ **WF-4 — LOP medical pipeline.** GATE: medical-tab **treatment records, not yet built** (form-engine §10's write-back names them as future); the approval-request queue **gates client care** and needs its own design pass. Cross-link: the medical module. **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-4 subsection), not designed.** Both gates re-verified UNSATISFIED: treatment records are still unbuilt, and **the approval-queue design pass has never been run**. This is the row where the HIPAA question bites hardest — see Q-WF-5 and Q-WF-6, and **`WF-LOOK-1`**, which asks whether the record named the wrong statute.
- ⬜ **WF-5 — Settlement pipeline. DOUBLY GATED.** GATE: **money machinery is deliberately unbuilt and UNRULED** — no settlement ledger, no trust/IOLTA, no liens, a standing BUILD-STATE line. WF-5 records the observed shape only; **nothing proceeds until Michael rules the money-module question first.** **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-5 subsection), not designed.** Both gates re-verified UNSATISFIED. **The money gate is the one gate on this series that no amount of design work moves** — it is a ruling Michael has not given, and the spec does not nibble at it.
- ⬜ **WF-6 — Criminal appointment pipeline. LARGELY COVERED ALREADY** — OAA intake Tier 1 is BUILT, and the **CR series** carries the rest: custody is **CR-5**, offers are **CR-8**, docket prep is the worksheet items. The intake spec's docket sections cover more. **WF-6's actual residue is the inbox-facing auto-intake half, gated on T3.** Cross-links recorded deliberately **so WF-6 never duplicates CR work.** **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-6 subsection), not designed.** The spec confirms the row's own scoping: **the residue really is the inbox-facing auto-intake half only**, and that half is T3-gated like the rest. Nothing here re-opens the built OAA Tier 1 intake, and **no CR row was touched.**
- ⬜ **WF-7 — Records retrieval.** Module design UNRULED; the request-aging / stall-alert shape is recorded. Clerk-fee cost tracking **cross-links the future money question — flagged, not assumed.** **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-7 subsection), not designed.** The spec raises a **scoping** question rather than answering the design one: request aging, stall alerts and follow-up generation need no money model at all, so WF-7 could be severed from cost tracking and freed of the gate that doubly blocks WF-5 — **but severing may create the half-item problem Q-IN2-9 raises about IN-2. That is Q-WF-8, and it is Michael's; nothing was severed.**
- ⬜ **WF-8 — Service of process.** Cross-links the **CD-1-deferred service-story fields** ("land with the first instrument consumer") and IN-3/IN-4's service events. **WF-8's board is EVIDENCE FOR that deferred revisit, not a separate structure.** GATE: T3 for inbox detection of returns. **ANNOTATED 2026-08-15 (#85), STAYS ⬜ — MAPPED at the spec's §5 (WF-8 subsection), not designed.** Both gates re-verified UNSATISFIED, and the second one has a live edge: CD-1 deferred its service-story fields to *"the first instrument consumer,"* and **#83 found that consumer has never been named** (Q-IN3-3). **WF-8 was NOT nominated as that consumer** — naming one reopens CD-1's living spec, so the spec put it as **Q-WF-9**, which also asks whether WF-8's inbound evidentiary service story and IN-3/IN-4's outbound generative one are **one column set or two**. Related naming hazard already on this queue: **Q-IN3-9** — `service_date`, `service_start` and `service_end` already mean **medical dates of service** in this schema.
- ⬜ **Cross-cutting constraints on all future email/intake work (RULED 2026-08-12 as recorded constraints, not buildable items):** deadline extraction is the **highest-value function and leads any T3 scoping**; dedupe keys (envelope number, claim number, document name) are **T3 design constraints**; a **hard ignore layer precedes any triage feature**; and — recorded with rule force — **client emails get human routing, NEVER templated automation.**

### Deadline engine (DL series) — ADDED 2026-08-11

*(Placement is Code's call and non-obvious, so it is stated: §7 carried no deadline-engine design-input home — §3 above holds TRCP **verification** items, which is a different act — and the file sections §7 by series. A DL heading here matches that structure rather than burying a data-model fork among housekeeping items.)*

- ⬜ **DL-INPUT (2026-08-11) — Per-defendant service dates within one discovery wave: six documents in one wave may carry six certificate-of-service dates, and each defendant's response deadline computes independently from its own service date. Is the deadline model per-(case, party), not per-case? A data-model fork the deadline-engine design pass must take deliberately; nothing in the heartbeat/deadline docs rules it either way.** From REQ-CAPTURE §5.5. Response-period substrate (TRCP 197.2(a), 196.2, 198.2, deemed admissions) VERIFIED by Michael 2026-08-11. **RULED 2026-08-13 (#66): the deadline model is PER-(CASE, PARTY)** — each party's response clock computes from its own service date. The deadline-engine design pass builds on this. **Ruled ≠ authorized: no deadline engine is in the build queue.**

### Probate ladder (PL series) — ADDED 2026-08-07 (evening)

**All four ISSUED UNRULED and DEFERRED** with the probate-ladder design pass itself (see the PR-3 annotation at §2). The deferral is deliberate and Michael's: the Domser matter is the validation instrument, and these get answered against a lived arc rather than the treatise. First evidence arrives at the LETTERS capture. Full reasoning: `rulings-capture-2026-08-07b.md`.

*(**Annotated 2026-08-15 (#86), add-only — all four stay ⬜, none narrowed, none answered.** The PR-3 migration proposal (`pr-3-reparenting-migration-proposal-2026-08-15.md`) **points at this series and deliberately does not pre-empt it**: its §3 target-state section is written as a question precisely because PL-1..PL-4 are unruled, and its §4 step table marks which steps are **destination-dependent** — i.e. unsizable until this series is answered. One structural fact from that proposal is worth carrying here because it bears on **PL-1**: the proposal finds the base act **DDL-free** (free-text columns, no CHECK/enum/FK), but that property is **shape-dependent** — **PL-1's proceeding-ATTRIBUTE branch would need a `cases` column that does not exist**, so choosing that shape converts a source-plus-backfill act into a schema migration. Also: **reversibility decays at first probate use** — status values written under a probate ladder do not un-write — which is `Q-PR3-4`'s scheduling question, not a PL question.)*

- ⬜ **PL-1 — Model shape for probate: does PROCEEDING = CASE TYPE under a Probate practice area (each proceeding its own type + ladder, shared intake front-segment, mid-matter changes via the existing review-logged case-type change), or is it ONE "Decedent's Estate" case type with a proceeding ATTRIBUTE keying the ladder?** Sub-questions: is heirship a conditionally-present phase of intestate independent administration (with standalone Heirship Determination reserved for no-letters title clearing)? Is temporary administration an overlay object rather than a case type? **Claude's recommendation for proceeding-as-case-type is on record as PROPOSED ONLY** — it is not a ruling and must not be built from. **OPEN — deferred pending Domser arc evidence.**
- ⬜ **PL-2 — Which proceedings get DRAFTED ladders when the pass runs, versus stubs?** Independent administration is the ruled spine (PR-1); do muniment, standalone heirship, and small-estate ladders draft in the same pass, or defer until one of them is live? **OPEN — deferred with the pass.**
- ⬜ **PL-3 — Independent-administration ladder content: which of the Ch. 7 arc's stations are LADDER STATUSES versus checklist/tracked-object material?** To be answered against the Domser lived arc, not the treatise alone. **OPEN — deferred; first evidence at the letters capture.**
- ⬜ **PL-4 — Ladder / deadline-engine boundary: the ladder stays phase-shaped while every probate deadline enters the registry UNVERIFIED as its own batch** (planned in the design, per `spec-feedback` item 1) — **confirm that boundary and the registry-batch scope when the pass runs.** **OPEN — deferred with the pass.**

### T3 / Phase 0 advisory (HK series — ADDED 2026-08-09; RE-LETTERED H → HK 2026-08-13, #66)

**THE ID COLLISION IS ENDED BY RULING.** These arrived from the 2026-08-09 design-side advisory
packet lettered H1–H6, which **collided with the case-heartbeat register's H1–H83** (H8, H22,
H35–H83 are cited throughout this file). Renumbering an attorney's items was not Code's call, so
the collision was stated and carried — until **Michael ruled the re-lettering on 2026-08-13 (#66):
H → HK, numerals preserved** (H1→HK-1, H2→HK-2, H4→HK-4, H5→HK-5, H6→HK-6). **The heartbeat
H1–H83 IDs are untouched and must never be renamed.** The append-only session log keeps the old
IDs in old entries; each row below notes its former ID once. **HK-3 (formerly H3 — paste-state of
the audio-row exception ruling) is CLOSED** per the entry's 2026-08-10 amendment and appears in no
table.

- ✅ **HK-1 (formerly H1) — RULED 2026-08-13 (#66): FILE IT, as drafted. EXECUTED this batch** into
  `docs/spec-feedback.md`. The filed text: "5 advisories (1 moderate, 4 high) on fresh install, P1,
  2026-08-08; advisories sit in dev-dependency chains of a lockfile green at 232 tests on 07-28;
  triage as a routed task on a machine that can test the result, **not** auto-fixed via
  `npm audit fix`, which can silently desync the machine from the tested lockfile."
- ✅ **HK-2 (formerly H2) — CLOSED 2026-08-13 (#66) via the v17 economics refresh.** Fable 5 became
  standard on Max (banner 2026-08-09), leaving the MODEL USAGE section's economic assumptions
  describing the old terms. v17 refreshes them to the observed 2026-08-13 Max terms (separate Fable
  bar + all-models bar, reset Tue 3:59 PM Central). Closes with **Q-5**, which rode the same clause.
- ⬜ **HK-4 (formerly H4) — stage the 13 pilot recordings into `..\data`** (create the directory at
  staging time, **outside the repo tree**); a session then re-runs the audio preflight row and
  **Stage 1 proceeds under the ORIGINAL 2026-08-07 authorization — no new ruling needed.** OPEN —
  Michael's hand; text unchanged by the #66 run. *(This is the single thing standing between the P1
  environment and Stage 1 scoring; the audio row stays RED and the preflight stays NOT-passed until
  it is done.)*
- ✅ **HK-5 (formerly H5) — RULED 2026-08-13 (#66): ADOPTED, WITH A CAVEAT.** The rule: **preflight
  rows and questions about Michael-supplied material are answered by Michael FIRST — never by
  sweeping his machine unprompted, because filenames alone can carry client information.** The
  caveat, which is part of the ruling: **when Michael cannot recall, he MAY DIRECT a search, per
  instance — the search is his call, never a session's default.** Homes: `CLAUDE.md` (landed this
  batch) + instructions v17. *(Origin: a proposed recursive `$env:USERPROFILE` sweep for audio files
  was refused design-side — the listing is itself an exposure.)*
- 🟡 **HK-6 (formerly H6) — RULED 2026-08-13 (#66): ADOPT the raw capture into the repo. EXECUTION
  GATED, and the gate is Michael's.** Adoption target: `docs/specs/t3-kickoff-day-capture-2026-08-09.md`,
  status line *"RAW CAPTURE — design-side advisory session, 2026-08-08→09; not canonical, not a
  design doc, not in the build queue"*. **The file is absent from current project knowledge
  (searched 2026-08-13) and is not in the repo** — Michael locates it (ARCHIVE project or Downloads)
  and supplies it; it ships in a later packet. Ruled, not executable.

### Process, tooling, and housekeeping

- ✅ **O5 — RULED 2026-08-13 (#66): BOTH ATTRIBUTES CONFIRMED ON SUBSTANCE.** `direction` stands as **its own axis**; `conditionalDowngrade` is **a conditions predicate over structured facts — never a boolean.** *(The question had been half-answered: both were absorbed into the ruled fee-profile shape without the substance ever being ruled, and a 2026-08-07 ruling of scope deferred it as un-rulable from memory.)* **Still open and NOT closed by this: the wording sign-off, the V10 citator pass, and V13.**
- ✅ **Q-5 — RESOLVED 2026-08-13 (#66).** The model-usage clarifying clause drafted at `model-routing-plan.md` §7.2 is **ADOPTED into instructions v17**, alongside the 2026-07-27 economics ruling; the 07-27 closure-by-replacement stands. Row closes. *(Adopting it fired instructions trigger #3 — v17 was drafted and delivered the same session. `model-routing-plan.md` itself remains UNRULED as a memo; adopt nothing else from it.)*
- ⬜ **Q-6** — **FLP internal-tooling terms.** Research use is within bounds; wiring the CourtListener API into the app is **UNAUTHORIZED** until Michael resolves it with FLP directly.
- *(**PR-3** — the mis-parented `Probate companion` case type: full row and status at §2 above, where the 2026-08-07 direction-confirmed / execution-held annotation lives. Glyph stripped 2026-08-13 (#68) — pointer rows carry no status glyph.)*
- *(**RE-1** — referral engine: full row and status at §2 above. Glyph stripped 2026-08-13 (#68).)*
- ⬜ **M-3** — Medchron fictional-content check. ⬜ **M-4** — **LegiScan key rotation after the T3 build (firm).**
- 🟡 **GPU telemetry posture — RULED 2026-08-12 (#63), open only for your confirmation. The question was:** NeMo pulled `wandb`, `sentry-sdk`, and OneLogger onto the machine that will process **privileged audio** (smoke test run 08-09, tone only) — what is the posture? **RULED: OFFLINE LOCKDOWN.** The packages stay installed but are **forced offline machine-wide BEFORE any privileged audio is processed.** Recipe and verification steps: `docs/gpu-telemetry-offline.md`. **The variables get set on the GPU machine by your hand — never from a Code session** — and this **closes fully only when you report verification 3 passed.** *(Registered here 2026-08-12 (#63). **FINDING: this item had NO ROW in this file** — it lived only in BUILD-STATE's design-side section, so the packet's "annotate the telemetry row" instruction had no row to annotate. Same class as the REQ-10 finding at #60: an item tracked in BUILD-STATE but never registered in the register. Recorded with full question text per QR-1 so it cannot die the K-6/K-7 death.)*
  - **SELF-CHECK RUN 2026-08-13 (#66): the machine-scope variables came back EMPTY** (screenshot,
    Michael's own run) — **the lockdown is NOT in place.** The recipe was re-supplied with the WSL2
    half emphasized. The item moves from status-unknown to **known not-yet-done** and stays open.
    **The hard rule stands: no privileged audio until you report verification 3 passed.**
- ⬜ **Registry verification backlog — NEW 2026-08-12 (#58), WIDENED 27 → 33 on 2026-08-12 (#62),
  NOW 34 on 2026-08-13 (#66).** **The six added entries are criminal**, in a third registry file, `docs/specs/legal-rule-registry-criminal-plea-and-costs.md` (ALL UNVERIFIED), opened because no existing file's subject honestly covers criminal offence classification, plea procedure, and costs — the #50/#58 placement reasoning. **Two of those six carry NO CITE AT ALL**: the capture stated them bare (degree-of-offence/punishment correspondence, and per-cause non-concurrent court costs), and **supplying a cite is a verification act, so it is Michael's, not Code's** — they were copied exactly as stated and nothing was researched or filled in. **These six hard-gate CR-10**, the plea-paperwork signing checklist: no computed check runs until they are verified. Original twenty-seven below. **Twenty-seven entries went into the registry UNVERIFIED in the #58 batch and await Michael's verification, one by one or batched at his direction.** Home: `docs/specs/legal-rule-registry-discovery-enforcement-and-pleading.md` (all entries UNVERIFIED, by construction; the file states why it is a new file rather than a fold-in). **PLACEMENT FLAG CLOSED — CONFIRMED by Michael 2026-08-12** (post-run review of the twentieth invocation; see #60): the new-file deviation stands, for the honest-status-header reason, split later if volume warrants. **That closes the placement question only — the twenty-seven entries themselves remain UNVERIFIED and are still your homework.** *(Note on where this closed: no standalone placement row ever existed in this file — the flag lived in the registry file's header, in BUILD-STATE, and in the twentieth invocation's handback. It is annotated here rather than given a row of its own, since the row would exist only to be closed; the #57 disposition.)* Composition: **twenty-two from the deficiency-handling capture** — the largest single batch since trucking — covering discovery scope, response completeness, objection specificity and waiver, permitted RFP response forms, contention discovery, sanctions and expenses, the conference requirement, and nine supporting case propositions; and **five from the UIM-UDJA capture** — CPRC § 37.001 et seq., § 37.009, § 15.002(a)(1), TRCP 47(b)–(c), and *Allstate Ins. Co. v. Irwin*, 627 S.W.3d 263 (Tex. 2021). *(Count note: the packet estimated "~26 propositions" for the deficiency batch; the capture's §4 carries 24 bullet lines, of which 3 deduped onto existing VERIFIED entries and one line split into two distinct cites — 22 entries. The estimate and the count are both right about different things.)* **Two reads specifically flagged for Michael:** (a) the existing VERIFIED TRCP 194 entry's expanded four-category wording versus the UIM capture's narrower "194.1–194.4" span — same entry, two wordings, and verification attaches to wording; (b) **six** WL/slip cites in the deficiency batch flagged for reporter-cite check at verification time (the packet named five — Volt Power, Sting Soccer, Ochoa, Redman, Collins — and *De Anda v. Jason C. Webster, P.C.*, 2018 WL 3580579, is a sixth carrying the same defect; flagged, not corrected). **Correcting a slip cite to a reporter cite is Michael's verification act, not Code's** — nothing was changed. Blocks **FE-14**, whose picklist wording cannot be encoded until TRCP 47(b)–(c) is verified.
  - **Cite-check memo filed 2026-08-13 (#65)** at `docs/specs/registry-cite-check-2026-08-13.md` —
    the six slip-cite checks and the two cite-less criminal entries each reduced to one look; see
    the memo's §3 priority list. Research support only; every verification act remains Michael's.
  - **COUNT 33 → 34, 2026-08-13 (#66):** former criminal entry 4 was **SPLIT by your ruling** into
    **4a** (separate criminal actions — costs per cause, concurrent sentences do not merge them;
    still CITE-LESS) and **4b** (single criminal action — each cost once at the highest category,
    art. 102.073(a)–(b), fine-only actions excluded by (c)). One proposition became two. **BOTH
    UNVERIFIED, and nothing was verified by the split** — supplying 4a's cite and verifying 4b's
    statutory text against the official source remain your acts. **CR-10's cost check must now ask
    same-action-or-separate BEFORE totaling.**
- ⬜ **V-2 — NEW 2026-08-13 (#65). The cite-check memo's one-look list — six looks, in priority
  order.** The question: **for each item below, does the flagged check close, and with what text?**
  (1) **Ochoa** — pull 2004 WL 1192444 on Westlaw; the header shows the S.W.3d parallel cite if one
  exists, and the opinion's **"(PUBLISH)" notation says it plausibly does** — the single
  highest-value look of the six. (2) **Collins v. Kappa Sigma Fraternity, 2017 WL 218286** —
  retrieve it (not in FLP, not on the public web); **beware the 2010 sibling appeal**,
  No. 02-09-00305-CV, same litigation. (3) **Sting Soccer** — confirm the Nov. 30, 2017 opinion's
  designation (memorandum vs. published). (4) **Volt Power / Redman / De Anda** — confirm each WL
  number maps to the substantive opinion named in the memo; then **the "no reporter cite exists or
  will exist" closure is yours to record**; for Volt Power and Redman also decide whether the
  dismissed-as-moot subsequent history merits a parenthetical, and for De Anda confirm "pet.
  denied." (5) **Criminal entry 3** — verify Penal Code §§ 12.34/12.35 plus *Mizell v. State*,
  119 S.W.3d 804, 806 (Tex. Crim. App. 2003), as the cite set. (6) **Criminal entry 4** — **rule on
  the wording split under Tex. Code Crim. Proc. art. 102.073 BEFORE any cite is supplied**: the
  statute's verbatim text cuts against the carried "per cause, non-concurrent" proposition for
  causes disposed in a **single criminal action**, where each cost is assessed only once at the
  highest offense category; read *LaPorte* / *Hurlburt* if wanted. Evidence staged at
  `docs/specs/registry-cite-check-2026-08-13.md`. **Nothing in that memo is verified and no
  registry entry was touched** — supplying or upgrading a cite is your verification act alone.
  - **ANNOTATED 2026-08-13 (#72), ADD-ONLY — V-2 IS NOT CLOSED AND NOT ALTERED; all six looks
    remain yours.** Two new research-support files reduce each backlog entry to one look:
    `docs/specs/registry-verification-workbook-2026-08-13.md` (one row per backlog entry —
    proposition as written, cite as written, retrieval, resolves Y/N, reporter-cite candidate,
    the ONE concrete look) and `docs/specs/registry-citator-pass-2026-08-13.md` (majority
    resolution, citing-cases picture, treatment flags, why-look/no-look per case). **Both are
    PROPOSED design input; nothing in either is verified and no registry file was touched.**
    **Item 4 becomes a confirmation rather than a search:** the citator supplies FLP opinion
    **9385744** (*Volt Power*, the April 5 2023 substantive opinion) and **9890720** (*Redman*,
    the October 11 2023 substantive opinion), found via the *Park Cities Bank* citation graph —
    each WL number now has a specific opinion id to map to. Net of both files: **five entries
    need a look** (Ochoa, Collins, Irwin, Alford, Castillo), **five need none for cite or
    majority** (Able Supply, Peeples, Dillard, Park Cities Bank, Mizell — *each still needs a
    commercial-citator currency check before any filing; FLP is not a citator*), **three are
    confirmation-only** per item 4, **one needs its designation first** (Sting Soccer).
- ✅ **V-1 — RULED 2026-08-13 (#66): BUMP TO v17 NOW, and it was drafted and delivered the same
  session** (trigger #3, same-day). v17 carries: the Descrybe-out tooling convention; **SAT-1**; the
  **§7.2 model-routing clause** folded into MODEL USAGE with economics refreshed to the observed
  2026-08-13 Max terms (separate Fable bar + all-models bar, reset Tue 3:59 PM Central) — which
  closes both the **Q-5** remnant and **HK-2**; the **HK-5** rule; the "BS-1 provenance" phrase
  corrected to the ledger-pointer line; and the pre-Cowork read-only wording updated (tracked
  content stays read-only design-side, while project knowledge, checkout reads, and inbox saves are
  within design-side reach when granted). **The paste is your hand — the file was delivered to you,
  NOT routed to the repo.** The next fresh design session verifies v17 in force (#54 precedent).
  *(Original question, preserved verbatim per QR-1: **record the Descrybe exclusion as
  session-level tool routing only, or bump the instructions to v17 now — folding in the two
  already-queued touch-ups** — the "BS-1 provenance" phrase; the pre-Cowork "Claude cannot write
  project docs" line? The occasion: Michael ruled 2026-08-13, in his own words, that **Descrybe is
  not used in this project at all until further notice** — revisit at go-live if still useful;
  FLP/CourtListener plus named public sources is the case-retrieval path.)*
- ✅ **V-3 — CONFIRMED BOTH, 2026-08-13 (#66). CLOSED.** PROBATE was created with v1 instructions
  and the carry snippets placed; CRIM DEFENSE carries v1 plus the Uvalde worksheet instructions.
  **Both hand-items close.** *(Original question, preserved: are both now done — the PROBATE
  practice project created (v1 + the carry snippet into each of the two grandfathered matter
  workspaces), and the Uvalde instructions plus v1 confirmed pasted into CRIM DEFENSE? Neither was
  visible to Code, so the record could not close itself.)*
- 🟡 **V-4 — RULED 2026-08-13 (#73): KEEP AS TWO, CROSS-REFERENCED.** One proposition per entry, so
  each carries its own status; **cross-references added between the halves of each pair so they
  cannot drift apart.** Governs all three pairs, as the question required. **The reason the ruling
  turned on:** consolidating would merge a VERIFIED entry with an UNVERIFIED one, and verification
  attaches to wording — the merged text would be new wording, so all three verified halves would
  have dropped back to UNVERIFIED. **EXECUTION IS DESIGN-SIDE AND PENDING** (registry files are
  read-only in Code sessions); no registry file was touched. *(Original question, preserved
  verbatim per QR-1, below.)* **RFP response-form consolidation.** The sibling registry file
  carries a VERIFIED **TRCP 196.2** entry for the 30-day response period; the enforcement file
  carries a separate UNVERIFIED **TRCP 196.2(b)** entry for the four permitted response forms.
  **The question: do you want 196.2 and 196.2(b) consolidated into one entry, or kept as two
  propositions on one rule number?** The same question rides on **197.2(a) beside 197.2(c)** and
  **198.2 beside 198.1**. One answer should govern all three pairs so they cannot drift apart.
  Source: `registry-verification-workbook-2026-08-13.md` §5 Q1.
- 🟡 **V-5 — RULED 2026-08-13 (#73): SPLIT ALL THREE, one entry per case.** Six entries where there
  were three. **BACKLOG 34 → 37 WHEN EXECUTED — it is still 34 in the files today.** The attribution
  defect disappears (each entry names one case for one proposition), and the split frees **Able
  Supply, Alford, and Peeples** to be verified now instead of waiting on *Castillo*'s attribution
  question and *Collins*'s retrieval. **The consequence that decided it, surfaced by the citator
  pass:** a joint entry verifies at the speed of its slowest case, so *Alford* — a 1999 Texas
  Supreme Court authority — was hostage to a 2017 memorandum opinion that is not in FLP, not on the
  public web, and may never be retrieved. **EXECUTION IS DESIGN-SIDE AND PENDING**; no registry file
  was touched, and nothing was verified by the ruling. *(Original question, preserved verbatim per
  QR-1, below.)* **Two-case entries.** Three entries cite two cases each for one
  proposition (Castillo/Able Supply; Alford/Collins; Redman/Peeples). The registry file keeps them
  joint because the originating capture relied on them jointly, and offers to split on your word.
  **The question: split each into one entry per case, or keep them joint?** Practical consequence
  discovered this session: in the Castillo/Able Supply entry the joint framing makes it impossible
  to tell from the registry alone which case supplies which half — and *Castillo* has a
  concurrence (opinion 9513076, Wainwright, J.), so a verifier cannot confirm the attribution
  without reading both opinions. Source: workbook §5 Q2.
- 🟡 **V-6 — RULED 2026-08-13 (#73): BOTH STAY IN THE REGISTRY, REWORDED to state their operative
  tests** rather than to observe that a form family exists. Entry 34 nearly does already (*with the
  written consent of the parties* is a test); entry 33 needs the more work. **Criminal file stays at
  7 entries; the instrument list stays under registry discipline** — cite, status, and a
  re-verification trigger — which is the point, on a checklist whose job is catching errors before a
  judge signs. **Two things recorded so the ruling is not misread:** removing them would NOT have
  unblocked CR-10 (entries 30 and 31 still carry no cite and 32 is partial, so the gate narrows to
  five and does not open), and **rewording is a wording act — verification attaches to wording, so
  both entries verify against the NEW text, not the old.** **EXECUTION IS DESIGN-SIDE AND PENDING.**
  *(Original question, preserved verbatim per QR-1, below.)* **Do criminal entries for art. 42.0197
  and art. 27.18 state legal rules at all?** Both are recorded as *observations that a plea-paperwork instrument family
  exists* (affirmative criminal-street-gang finding; plea by videoconference with written consent)
  rather than as propositions with an operative legal test. **The question: should these remain
  registry entries, or should CR-10's completeness check reference an instrument list directly and
  these two come out of the registry?** Raised because verification of a non-proposition is an odd
  act, and because both currently sit inside CR-10's hard gate — meaning CR-10 stays blocked on
  verifying something that may not need verifying. Source: workbook §5 Q3.
- 🟡 **V-7 — RULED 2026-08-13 (#73): NARROW ENTRY 23 to what § 37.001 et seq. actually provides;
  the availability holding lives on the *Irwin* entry (24), with a cross-reference between them.**
  Consistent with V-4 and V-5, ruled the same session: separate entries, cross-referenced, never a
  joint-authority entry. **The cost, stated and accepted:** the availability proposition now waits
  on entry 24, **the least verifiable entry in the backlog** — the citator pass found *Irwin* under
  two clusters, each with no reporter cite and no opinion typing, a citation search for
  627 S.W.3d 263 returning zero hits, and **no basis in FLP's data for the positive majority
  identification the binding rule requires** (see V-9). Resolving it means the reporter or a
  by-hand read, not FLP. **EXECUTION IS DESIGN-SIDE AND PENDING.** *(Original question, preserved
  verbatim per QR-1, below.)* **Entry 23's shape.** CPRC § 37.001 et seq. is cited for the
  proposition that "the UDJA is available as the vehicle for establishing an insured's UIM-coverage
  prerequisites against the insured's own carrier." That availability holding is *Allstate v.
  Irwin*'s (entry 24), not the statute's text. **The question: narrow entry 23 to what § 37.001 et
  seq. actually provides and let the Irwin entry carry the availability holding, or restate entry
  23 as a joint statute-plus-case proposition?** This is a wording act, and verification attaches
  to wording. Source: workbook §5 Q4.
- 🟡 **V-8 — RULED 2026-08-13 (#73): RECORD ALL THREE HAZARDS in
  `registry-courtlistener-integration-design.md` §0.1** — the duplicate-cluster hazard, the
  *Irwin* no-cite/no-typing class, and a standing rule that **a citing count is never displayed as
  a precise figure and never read as a currency signal.** They join the existing
  cluster-vs-sub-opinion hazard. **AND the *Irwin* class opens as its own item, V-9** — it is a gap
  in a BINDING rule, and amending a binding rule is Michael's act, not a side effect of a
  design-doc note. **Nothing was edited into CLAUDE.md, and §0.1 is UNEDITED — the design-doc edit
  is design-side and PENDING.** **Q-6 still bars wiring the API into the app**, so nothing is built
  either way. *(Original question, preserved verbatim per QR-1, below.)* **The FLP duplicate-record
  class, beyond one entry.**
- ⬜ **V-9 — NEW 2026-08-13 (#73), opened by the V-8 ruling. What does the majority-opinion rule
  require when the data cannot support positive identification?** CLAUDE.md's majority-opinion rule
  is BINDING and prescribes a method: enumerate `sub_opinions`, then identify the majority
  **positively** by opinion-type marker and authoring language, never by position or assumption.
  ***Allstate Ins. Co. v. Irwin* is a case where that method cannot run at all** — two clusters
  (4885466, 4885465), each a single untyped `010combined` with no author, no per-curiam marker, and
  an empty citations array; a probe of the 26,366-character stored text for "delivered the opinion
  of the Court" returned **zero matches**, so even the authoring-language fallback does not fire.
  **The question: what does the rule require in that state — refuse to characterize the opinion at
  all, fall back to the reporter, or something else — and should that fallback be written into
  CLAUDE.md's rule 5?** Raised because the rule currently has no stated answer, and silence in a
  binding rule gets filled by whoever hits it next. **Michael's act; nothing edited.** Source:
  `registry-citator-pass-2026-08-13.md` §4. *Alford
  Chevrolet-Geo* has two FLP clusters on one reporter cite (2419858, filed 1999-08-26, and 5269700
  "In re Chevroletgeo", filed 1999-06-10, same docket 97-1171); *Irwin* has two clusters with no
  reporter cite at all and no opinion typing, so the majority-opinion rule's method has nothing to
  work from; and citing counts disagree across three FLP surfaces (Alford: 187 search index / 207
  clusters API / 168 `cites:` query, same case, same session). All are retrieval hazards of the
  same family as the majority-opinion rule, and all were found in a sample of ten cases. **The
  question: should `registry-courtlistener-integration-design.md` §0.1 record a duplicate-cluster
  hazard and a "citing counts are not precise" rule alongside the existing cluster-vs-sub-opinion
  hazard?** Design input only; **Q-6 still bars wiring the API into the app**, so nothing is built
  either way. Source: workbook §5 Q5 and `registry-citator-pass-2026-08-13.md` §1, §2.3, §4.
*(**THE TASK 13 MEMO'S EIGHT QUESTIONS AND FOUR NAMED LOOKS — ADDED 2026-08-15, FORTY-SECOND queue-runner invocation, with NO DURABLE IDs**, from `docs/specs/qbo-integration-research-memo-2026-08-15.md` §11 and §12 (CHAT-DISPATCH Task 13). `Q-QBO-` and `QBO-LOOK-` were collision-checked repo-wide at HEAD and returned **ZERO**; they stay **packet-local and were deliberately NOT minted** — minting is Michael's act, per the `Q-PR3-` / `PR3-LOOK-` precedent from #86, and **ID-DL-1 now governs an EIGHTH packet**. Full text per QR-1 — the packet is deleted, so these rows are the only place these questions live. **The memo is PROPOSED — RESEARCH ONLY. Nothing was ruled, nothing built (§5 NONE), and the four staged constraints it tests come from a capture BUILD-STATE still lists as "UNRULED, adopt nothing" — they are NOT constraints in force.**)*

- ⬜ **`Q-QBO-1` — Do you adopt the four staged constraints as ruled design constraints, or do they stay a PROPOSED capture?** `future-modules-capture-2026-07-28.md` has been on the record since 2026-07-28 and `BUILD-STATE.md` still lists it as "UNRULED, adopt nothing." The rulings-capture files record "QBO ruling" on the not-covered list twice (08-07 and 08-08). This memo was instructed to treat the four constraints as governing, and did — but a research pass following a proposal is not the same as the proposal being adopted, and the longer it is treated as governing the more it will read as settled. **Do link-don't-rebuild, read-only-first, sandbox-only-until-gates, and trust/operating separation become ruled constraints on any future QBO work, or does the whole capture stay unruled until the money-module conversation happens?** *(Runner note: `future-modules-capture-2026-07-28.md` was NOT edited or annotated — §6.4 bars it, because annotating it answers this question.)*
- ⬜ **`Q-QBO-2` — Given that no read-only OAuth scope exists, does "read-only first" change shape?** Three independent scope lists show one accounting scope granting full read/write, with no read-only variant. "Read-only first" can therefore be a property of the code but never of the grant: the token the firm authorizes against its live books is read/write regardless of what the integration does with it. **Does that (a) leave the constraint as written, with read-only understood as a code discipline; (b) raise the bar for when a live connection is made at all, since the credential is undifferentiated; or (c) mean a read-only stage should run only against sandbox companies indefinitely, with the live connection deferred to the write stage where the grant would be honest about itself?**
- ⬜ **`Q-QBO-3` — Does the QBO question wait on `Q-WF-4`, or does it get asked separately?** QuickBooks requires a client secret and a durable, writable home for a rotating refresh token; the application is a public-client browser SPA with no secret and no server-side identity (`BUILD-STATE.md:69`). `Q-WF-4` already asks whether this application acquires a server-side identity and of what shape. QBO's read path is narrower than the WF series' — it can be user-initiated, so no daemon and no webhook endpoint are strictly required — but the confidential-client requirement is irreducible. **Is QBO folded into `Q-WF-4` as a second consumer of the same answer, or does it get its own question because its shape is narrower and might be answerable sooner?**
- ⬜ **`Q-QBO-4` — Is a live QBO connection a gate-2 trigger?** The staged bullet says live OAuth waits for the go-live gates "and arguably after the professional security review." Gate 2's 2026-08-11 clarification says the security review gates the multi-user phase and solo live use proceeds without it — so on the current reading, a QBO live connection would not be gated by gate 2. **Do you want gate 2 extended to name credential-tier third-party connections regardless of user count, do you want a new gate appended for them, or does the existing gate-2 reading stand and the "arguably" resolve against waiting?** *(Runner note: `Go_Live_Gates.md` was NOT amended — §6.6 bars it, because appending a gate answers this question.)*
- ⬜ **`Q-QBO-5` — When a case view shows a trust figure it read from QuickBooks, what is the display contract?** QuickBooks records trust/operating separation as a naming convention and enforces nothing: it "won't prevent you from creating negative client balances or commingling funds — it simply records what you tell it." A read integration can therefore display a per-matter trust balance that is wrong — mis-parented sub-account, funds under another client, drifted naming — with the authority of a computed number. The project's answer to this class elsewhere is flag-don't-decide (IN-2's flag-never-auto-correct; the roster multi-match flag; `case_client_flags`). **Does that pattern govern here — the integration flags what it cannot verify and never asserts a trust balance as fact — or is a read-only trust display simply out of scope until the money module exists and can be reconciled against?**
- ⬜ **`Q-QBO-6` — Should Texas client-property and trust-accounting authority enter the registry, and where?** A word-bounded dedupe at HEAD across all four registry files returns zero hits for `iolta`, `trust`, `safekeeping`, `commingl`, `client funds`, and `escrow`. A day-one design constraint whose stated stake is a bar complaint rests on an authority layer the registry does not carry — the third instance of that shape, after `Q-WF-6`'s privacy gap and #78's insurance narrowing. Nothing was drafted here because the source was not retrieved and SOURCING does not name the disciplinary rules among its sources. **Do you want a retrieval pass run against a named authority for that layer; if so does it land in an existing registry file, in the fifth file `Q-STAT-5` already asks about, or nowhere until a money module exists to rely on it?** *(Runner corroboration, forty-second invocation: the sweep was re-run natively at HEAD across all four `legal-rule-registry-*` files and **held exactly** — `iolta` 0, `trust` 0, `safekeeping` 0, `commingl` 0, `client funds` 0, `escrow` 0, `disciplinary` **1** and that one is the unrelated Ins. Code §146.004 no-disciplinary-action clause at `legal-rule-registry-draft-entries-medical-billing.md:213`. **No registry file was opened for writing; backlog stays 34.**)*
- ⬜ **`Q-QBO-7` — Do you want a standing constraint recorded in the Q-6 family for QBO?** Q-6 bars wiring the CourtListener API into the app until you resolve terms with FLP; the Task 7 memo's Q6 asks the same about an OCA-certified EFSP relationship. QBO differs on the axis that defines Q-6 — Intuit's terms are published and commercial — but matches on credential-tier exposure and adds a structural prerequisite the other two do not. **Do you want it recorded that no QBO integration is designed until you rule the terms and credential-custody question, so a future money-module session is scoped from the start to a linkage that assumes nothing about live connection; or is QBO deliberately outside the Q-6 family because its terms are not in dispute?**
- ⬜ **`Q-QBO-8` — Does the CorePlus metering change how a QBO read stage would be scoped?** Intuit's App Partner Program meters data-out calls — "reading accounts, querying company information, and fetching reports" — while data-in calls are unmetered and free; the free Builder tier carries 500,000 CorePlus credits a month and blocks rather than bills above the cap. For a solo practice the cap is not a real ceiling, so the practical answer may be "no." It is asked because it inverts the read-is-cheap assumption the 2026-07-28 capture was written under, and because "blocked at the cap" is an availability posture worth knowing before anything depends on a read. **Does this change anything, or is it filed as a fact?**
- ⬜ **`QBO-LOOK-1` — one browser session against `developer.intuit.com`.** The canonical API reference was unreadable from the design session: four fetches, four navigation shells. Every scope, entity, limit and security requirement in the memo is therefore first-party-blog, first-party-library, or third-party. In order: **(a)** that no read-only accounting scope exists; **(b)** the current rate-limit and batch figures, where two third-party sources already conflict; **(c)** the production security requirements for a private app; **(d)** the current default minor version. **Nothing should be built on the memo's §5 or §6 until this look runs.**
- ⬜ **`QBO-LOOK-2` — which QuickBooks Online subscription does the firm have?** A one-line fact question with a real branch behind it: class tracking, one of the two per-client tagging strategies for trust sub-ledgers, requires **Plus or Advanced**. On Simple Start or Essentials that strategy is unavailable and the sub-account-per-client structure is the only route. **The cheapest item on the whole task**, and the same shape as #85's "has the firm signed a BAA with anyone?"
- ⬜ **`QBO-LOOK-3` — when did / does the Reports API migration actually take effect?** Intuit's own post says **2026-08-31**; a third-party source says **2026-06-30**. Today is 2026-08-15, so the difference is whether the cutover is sixteen days out or six weeks past. Intuit's 2025-12-01 roundup of upcoming changes does not mention it at all. Folds naturally into `QBO-LOOK-1`. *(§6.9 bars "resolving" it by preferring the first-party figure — the first-party post carried no publication date.)*
- ⬜ **`QBO-LOOK-4` — NOT RUN, and named as not-run.** No retrieval was attempted against Texas client-property or trust-accounting authority. Two reasons: only the repo checkout was granted to the session, so the `Documents\Knowledge Repo` sources SOURCING names were unreachable; and the Texas Disciplinary Rules of Professional Conduct sit in a Government Code **appendix**, so whether the bulk statutes corpus carries them at all is itself unverified. **Deciding whether that retrieval happens, and against what named source, is upstream of `Q-QBO-6`.**

*(**THE TASK 12 PROPOSAL'S SEVEN QUESTIONS AND ONE NAMED LOOK — ADDED 2026-08-15, FORTY-FIRST queue-runner invocation, with NO DURABLE IDs**, from `docs/specs/pr-3-reparenting-migration-proposal-2026-08-15.md` §9 (CHAT-DISPATCH Task 12). `Q-PR3-` and `PR3-LOOK-` were collision-checked repo-wide at HEAD and are FREE; **`PR` is an existing durable series (PR-1, PR-2, PR-3) and minting into it is Michael's act**, so they stay packet-local. Full text per QR-1 — the packet is deleted, so these rows are the only place these questions live. **The proposal is PROPOSED — NOT AUTHORIZED TO BUILD; PR-3 stays ⬜ with its execution hold intact, and Michael made no rulings in the session that produced it.**)*

- ⬜ **Q-PR3-1 — the session's own method, raised against itself.** The working-set policy says design sessions do not read source (`BUILD-STATE.md:142`); that sentence predates the device bridge, and CHAT-DISPATCH's session-start line asks for the checkout precisely so HEAD can be read full-text. The Task 12 session read `src/domain/{caseTypes,types,roster}.ts` and the pages, and **every Class-A/B/C finding in the proposal is undrivable without them** — while #74 recorded *not* reading `roster.ts` as *"the honest gap"* that left ~60 CD-2 rows `TAG-CHECK: NOT RUN` (`BUILD-STATE.md:147`). Nothing from `src/` entered project knowledge and no source file was edited. **The question: is a design-side `src/` read through the device bridge sanctioned — standing, per-instance, or not at all? If it is not, the proposal's §2.3, §2.4 and §8 should be treated as irregularly obtained.**
- ⬜ **Q-PR3-2 — a DEFECT IN A RULED CONSTRAINT, confirmed at HEAD by the runner and changed nowhere.** `src/domain/roster.ts:195–202` returns `SIDE_SETS['plaintiff-defendant']` for **every practice area that is not `'Criminal'`**, while that file's own header at `:7` states it satisfies **roster capture REQ-14, ruled 2026-08-12 — *"SIDES ARE A PROPERTY OF THE CASE TYPE, NOT A CONSTANT"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when it was written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant caption alignments. **The defect exists today and is unreachable only because no fourth practice area exists yet.** The codebase therefore carries **two opposite defaults for the same question**: `statusesFor()` **throws** on an undeclared pair by deliberate design (`caseTypes.ts:6–9`, 2026-07-21 audit item 4), while `sideSetFor()` silently supplies a wrong-but-plausible answer — the precise behaviour that audit item was written to kill. **The question: repair inside PR-3, repair sooner as a standalone REQ-14 conformance fix, or leave it flagged? And which side set does a probate caption actually take — a legal question, not a coding one.** *(Runner note, forty-first invocation: re-read at HEAD and **confirmed verbatim**; §6 barred repair and nothing was changed.)*
- ⬜ **Q-PR3-3 — a LIVE sequencing conflict with the UNRUN CD-1 migration.** `db/migrations/2026-08-12-cd1-contact-directory.sql:100` derives caption alignment only `where c.practice_area in ('Personal Injury','General Civil Litigation')` — a literal two-value IN list, in a migration that is **still unrun and still Michael's hand**. **PR-3 first** → re-keyed probate rows fall outside the list and get **flagged** by the `case_roster_flags` insert at `:135–146`, which is the migration working as designed. **CD-1 first** — the order the current launch path schedules — → probate rows are still Personal Injury at that moment, take the plaintiff-defendant derivation, and **nothing later revisits it**. **The quieter ordering is the one currently scheduled, and nothing on the record had noted that the order has a consequence. Coupled to `Q-PR3-2`:** even with the right order, the `sideSetFor()` fall-through re-supplies Plaintiff/Defendant at the UI layer for every probate matter created afterward — **the migration order and the code default have to be answered together or the fix leaks.** **The question: in what order do PR-3 and the CD-1 item-7 migration run?**
- ⬜ **Q-PR3-4 — before or after go-live?** Rollback is cheap in the base case (no DDL, free-text columns, slots are DATA, the demo store backs up at each step), so a revert plus an inverted update undoes PR-3 — **with one exception that does not un-write: status values written under a probate ladder.** Once a probate matter is actually worked in the app, reverting `caseTypes.ts` strands that string in `cases.status`. **Reversibility therefore decays at first probate use.** **The question: does PR-3 execute before or after go-live — is probate in or out of the go-live population?** Scheduling only; **does not need PL-1 answered first.**
- ⬜ **Q-PR3-5 — three superseded passages still written as open, and none was edited.** `docs/spec-feedback.md:171` still poses extends-vs-supersedes as a live design decision; `docs/specs/pi-case-playbooks.md:951` still asks whether to build a companion case type; `docs/specs/case-management-project-instructions.md:94` still carries the companion type as **SETTLED**. **V17 closed all three on 2026-08-07.** Annotating them is a ruling, so nothing was annotated — by the session or by the runner. **The question: annotate in place, fix in one pass at execution, or leave as written?** Same class as `Q-IN2-2`.
- ⬜ **Q-PR3-6 — the estate's client, and a three-site ternary.** **What posture does an estate's client carry?** And: the three-way `'claimant'` ternary appears at `src/data/localAdapter.ts:168`, `ClientsCard.tsx:69`, and in the **already-run** SQL at `db/migrations/2026-07-28-cl2-client-dimension.sql:143`. **Should those three sites be consolidated to one enforcement point the way `showsMedicalTab()` and `showsClientLayer()` were?** *(§6 barred consolidating them; nothing was touched.)*
- ⬜ **Q-PR3-7 — roster slots for a probate matter.** **Does a probate matter seed practice-area roster slots, case-type slots once PL-1 names the types, or none for now?** Small by design; raised because **omission here is invisible to the compiler** — nothing fails, the slots simply never exist.
- ⬜ **`PR3-LOOK-1` — YOURS, and the cheapest move on the whole task. One query against the live database: how many `cases` rows carry `case_type = 'Probate companion'`?** **If the answer is zero, PR-3's entire data half drops out and the act becomes source-only** — a materially smaller authorization. Design-side could not answer it: no fixture uses the type (`src/data/seed.ts` defines three cases at `:157`, `:168`, `:176`, and a case-insensitive `probate` sweep of `src/data/` returns nothing), but whether any **fictional live row** carries it is unknown. Two related facts already established, so the look is the only unknown: the un-backfilled state is **benign and self-announcing** — `CaseDetailPage.tsx:107–110` catches the throw and shows *"Unknown case type."* at `:231`, no crash — and a re-parented matter **loses the Medical tab only if it has no bills**, since `:55` reads `showsMedicalTab(rec.practiceArea) || hasBills`. *(§6 barred the runner from running it: it is against the live database and it is Michael's look.)*

*(**THE TASK 11 SPEC'S TEN QUESTIONS AND TWO NAMED LOOKS — ADDED 2026-08-15, FORTIETH queue-runner invocation, with NO DURABLE IDs**, from `docs/specs/wf-2-wf-8-email-workflow-spec-2026-08-15.md` §10 and §11 (CHAT-DISPATCH Task 11). `Q-WF-` and `WF-LOOK-` were collision-checked repo-wide at HEAD and are FREE; **`WF` is an existing durable series and minting into it is Michael's act**, so they stay packet-local. Full text per QR-1 — the packet is deleted, so these rows are the only place these questions live. **The spec is PROPOSED and MAPS rather than elaborates; all seven WF rows stay ⬜, and Michael made no rulings in the session that produced it.**)*

- ⬜ **Q-WF-1.** The #63 adoption ruled that `email-workflow-requirements.md` enters the repo verbatim as a design input and that its contents *"remain PROPOSED at the module level"* — but that limitation is stated only in the adoption addendum and in the queue, while the seven pipeline paragraphs in the document's body are written in a survey's confident register (*"Module implications: envelope-state tracking, returned-filing deadline alerts, service-failure detection"*) with no marking of their own. A build session that opens the file and reads it top-down encounters seven module descriptions before it encounters any statement that none of them is ruled. **Should the seven body paragraphs carry an in-place PROPOSED marker, or does the addendum-plus-queue placement suffice — and if a marker is wanted, is adding one an edit to a document that was adopted VERBATIM, and therefore yours rather than a session's?**
- ⬜ **Q-WF-2.** `T3` names two different build tiers in two repo design docs: the transcription pipeline service (`transcript-sort-and-route-design.md:148`, gated on P1 hardware and KICK-1, **unauthorized**) and the LegiScan poller/matcher/watch-flags tier (`statute-text-and-bill-tracking-design.md:107`, gated on the O1 key, and **substantially built** per `session-log.md:7718` and BUILD-STATE's live `/statutes` and `/bills` screens). Five of the seven WF gates say "gated on T3" without naming which series, and the two readings gate oppositely — one blocks the series, the other reads it as unblocked. The record has ruled on this exact class once, re-lettering H → HK at #66 rather than tolerating a collision. **Does the `T` series get the same treatment — and if so, which one moves — or is a pointer note on the WF rows naming the intended doc sufficient? Renaming is your act; this session flagged and changed nothing.**
- ⬜ **Q-WF-3.** BUILD-STATE's `T3` (the transcription tier) is a **speech** pipeline, and nothing in the WF rows explains why an **email** pipeline should be gated on it. The proposed explanation is that the real gate is `CLAUDE.md:272–275` — *"PHI-touching AI processing … runs locally on Michael's GPU machine by design … Do not introduce cloud AI processing of case documents without an explicit decision from Michael"* — so "gated on T3" means "gated on the local processing arm," and mail bodies are case documents. **Q-IN1-7 asks the identical question of IN-1 and reaches the same dependency.** **Is that the correct reading of the WF gates — and if it is, should the gate be restated in those terms, since "the local AI arm exists" and "the speech pipeline is authorized" are different conditions that could be satisfied at different times?**
- ⬜ **Q-WF-4.** The single auth surface is a single-tenant public-client browser SPA using MSAL with PKCE, holding one delegated scope (`Calendars.ReadWrite`), with **per-browser tokens**, a `localhost:5173` redirect, and no server-side identity. Every WF pipeline is a background watcher measured in days and weeks (*"stuck for weeks," "weeks-long stalls," "10+ days"*), Graph webhooks require a publicly reachable HTTPS endpoint answering in 3–10 seconds, and even delta polling needs a process running when no browser is open — which means app-only credentials, i.e. a client secret or certificate this registration deliberately does not have. **Does this application acquire a server-side identity, and of what shape (deployed Supabase Edge Function, a process on the GPU machine, something else)? This question is upstream of all seven WF rows, it is not in the queue under any ID, and no WF gate names it.** **ANNOTATED add-only 2026-08-15 (#87), NOT CLOSED — A SECOND CONSUMER NOW STACKS BEHIND IT.** The Task 13 QBO memo reaches this same question from a different direction and, on one axis, harder: the QuickBooks token exchange **requires a client secret** (a confidential client) and the refresh token **rotates**, so custody needs a **durable, writable home for a value that changes** — where gate 4's LegiScan comparator is a *static* key rotated by hand, and does not transfer. **One thing is easier than the WF case and is recorded so the question is not over-costed:** QBO's read path can be user-initiated, so **no daemon, no webhook endpoint and no app-only credential are strictly required** — what is irreducible is the confidential-client exchange and the rotating-token home. **Whether QBO folds in here or gets its own question is `Q-QBO-3`, and it is yours.**
- ⬜ **Q-WF-5.** `docs/specs/outlook-email-intake.md` (captured 2026-07-23, EXPLORATORY) states as a *"first-class design constraint"* that *"the entire pipeline — transport, storage, indexing, any AI processing of email content — must be HIPAA compliant by design, not retrofitted"* and that *"any third-party processing of email content (including AI services) needs BAA/compliance review before it touches real mail."* That document is **not cited by `email-workflow-requirements.md`, not cited by the queue's WF rows, and not mentioned in #63** — so the BAA/compliance review is not among the seven named gates, even though it was on the record eighteen days before the WF series existed and `criminal-appointment-intake-and-docket-enhancements.md:84` already points at it for what is now WF-6's residue. **Is `outlook-email-intake.md` superseded by the WF series, merged into it, or a live eighth gate on every inbox-facing WF item — and does the BAA/compliance review get its own queue row?**
- ⬜ **Q-WF-6.** There is **no privacy, HIPAA, PHI, or medical-records-confidentiality proposition in any of the four registry files** — verified at HEAD; the only Health & Safety Code hit is § 481.115(b), a controlled-substances offense. Registry discipline says *"every legal proposition any module relies on is a registry entry with a cite and a verification status,"* and the WF series relies on a privacy proposition in its architecture. **Do the privacy propositions belong in the registry at all — and if so, in which file, or does this need a fifth registry file? (Q-STAT-5 already holds an unresolved fifth-file question from the insurance sweep; this would be the second candidate, and deciding them together may be cheaper than deciding them apart.)** **ANNOTATED add-only 2026-08-15 (#87), NOT CLOSED — A THIRD ABSENCE OF THE SAME SHAPE.** The Task 13 QBO memo ran a word-bounded sweep of all four registry files for `iolta`, `trust`, `safekeeping`, `commingl`, `client funds` and `escrow` and got **zero on every term** (re-run natively by the runner at HEAD and confirmed) — so **Texas client-property and trust-accounting authority is absent** the same way the privacy layer is, and the constraint resting on it names a **bar complaint** as its stake. **Nothing was drafted and no registry file was opened** (`Q-QBO-6`). Three instances now: this row's privacy gap, #78's insurance narrowing, and the trust gap.
- ⬜ **Q-WF-7.** This session could not obtain verbatim primary-law text for the privacy question: the eCFR API path is robots-blocked to it, the eCFR reader URL rate-limited and then returned paraphrase rather than quotation, and the sanctioned targeted fetch for Tex. Health & Safety Code ch. 181 (`statutes.capitol.texas.gov/Docs/HS/htm/HS.181.htm`) returned the site's navigation shell twice with no statutory text. The Knowledge Repo corpus was **not** reached, per H5. **When a design session needs Texas statutory text and the targeted official fetch fails, is a per-instance directed grant of the `Documents\Knowledge Repo` corpus folder the intended path (the #75 deadline-memo precedent), or should the session stop and hand you the look? This is the third chain task to hit a retrieval ceiling and the first where the fallback failed too.**
- ⬜ **Q-WF-8.** WF-7's row records that clerk-fee cost tracking *"cross-links the future money question — flagged, not assumed."* WF-7 could be designed without any cost tracking at all (request aging, stall alerts, and follow-up generation need no money model), which would free it from the money gate that doubly blocks WF-5. **Is WF-7 severable from cost tracking — designed as a records-chase board with fees explicitly out of scope — or does severing it create the half-item problem Q-IN2-9 raises about IN-2, where the deferred half never gets finished?**
- ⬜ **Q-WF-9.** WF-8's row says its board is *"evidence FOR that revisit, not a separate structure,"* referring to CD-1's deferred service-story fields, which CD-1 deferred to *"the first instrument consumer"* — and #83 found that consumer **has never been named** (Q-IN3-3). WF-8's service evidence is inbound and evidentiary (a return of service, with a date that drives deadlines); IN-3/IN-4's service events are outbound and generative (a certificate of service on an instrument the firm serves). **Is WF-8 a candidate to be that first consumer — and are those two service stories one column set or two? Naming a consumer reopens CD-1's living spec, so this session nominated nothing.**
- ⬜ **Q-WF-10.** Five of the seven WF rows have a substantive treatment elsewhere in the repo that the row itself does not cite: WF-2's filing-moment contest and EFSP exposure live in `bexar-local-rules-and-efiling-2026-08-15.md` (#80, with `LR-LOOK-1` already open); WF-6's residue is described at `criminal-appointment-intake-and-docket-enhancements.md:84`; WF-3's storage precedent is form-engine §10; WF-4's client dimension is CL-2's; WF-8's is CD-1's deferral. **Should the WF rows carry pointer annotations to those treatments, and is adding them a queue-runner act at the next reconcile or a ruling act of yours? The gate table exists to stop items dying silently; an item whose substantive work sits in a doc it does not cite is a softer version of the same death.** *(Runner note: NOT added — the row annotations above carry a pointer to the new spec only, which is what the packet's §4.2 authorized.)*
- ⬜ **`WF-LOOK-1` — the privacy authority, and it is the only one that changes the architecture.** Three propositions in §6.5, none verified, one (P-3, Tex. Health & Safety Code ch. 181) not even retrieved. The question in one line: **does ch. 181's broader "covered entity" definition reach this firm, and is HIPAA — the constraint the record actually names — the wrong statute to have named?** §6.4's handling requirements hold either way, so **no build is blocked on this**; what turns on it is whether those requirements are a statutory compliance program or prudential design hygiene. The practical first move is the cheaper fact question inside P-2: **whether the firm has signed a BAA with any provider, clinic, or vendor** — that is a look at the firm's own files, not a research task, and it may resolve the business-associate half without any statutory reading at all.
- ⬜ **`WF-LOOK-2` — the Graph figures.** Every number in §4.2 is TIER B (Microsoft Learn through a summarizing fetch layer). They are stable, vendor-published, and low-risk, but the subscription lifetimes and the 3-second/10-second delivery windows are load-bearing if a webhook design is ever chosen. A build session confirms them against the live documentation before relying on them. **Not yours — a build session's, recorded here so it is not skipped.**

*(**THE TASK 10 SPEC'S NINE QUESTIONS — ADDED 2026-08-15, THIRTY-NINTH queue-runner invocation, with NO DURABLE IDs**, from `docs/specs/in-2-crash-report-extraction-spec-2026-08-15.md` §10 (CHAT-DISPATCH Task 10). `Q-IN2-` and `IN2-LOOK-` were collision-checked repo-wide at HEAD and are FREE; **`IN-` is an existing durable namespace and minting into it is Michael's act**, so they stay packet-local. Full text per QR-1 — the packet is deleted, so these rows are the only place these questions live. **The spec is PROPOSED; IN-2's extraction half is unruled and its build is unauthorized, and the IN-2 row stays ⬜.**)*

- ⬜ **Q-IN2-1.** The repo holds the CR-3 **code sheet** — the controlled vocabularies — and does not hold the CR-3 **report form**, so there is no authoritative inventory of which fields exist on the report, where they sit, or what they are labelled. Extraction needs the field inventory more than it needs the vocabularies. **Should a blank CR-3 report form be added to `docs/reference/` alongside the code sheet (public TxDOT form documentation, same client-clean class), and if so is that your hand under H5 — or should IN-2's field inventory instead be derived at build time from a real report the extractor is pointed at, accepting that the inventory is then discovered rather than specified?**
- ⬜ **Q-IN2-2.** `cr3-field-code-map.md` line 34 states list 38 has "(79 codes)"; raw extraction shows **70** enumerated values with real gaps at 5–13 and 72 (spec §2.2). The file is a PROPOSED design input, not a registry file and not an append-only record, so correcting it in place is a smaller act than route (c) — but it is still not a design session's to edit. **Do you want the line corrected in place by a Code session with a note naming this spec, annotated add-only in the house pattern, or left exactly as written with the correction living only here?**
- ⬜ **Q-IN2-3.** `cases.date_of_incident` is a `date` and cannot hold the CR-3's time of crash, so the first live discrepancy the queue names — incident time — has no column to conflict with (spec §6.3). Adding `cases.incident_time` recreates the writable-mirror defect the schema's own comment warns against four lines below (*"a writable column meant to mirror derived data stops mirroring it silently"*). **Should the incident time live only as fact rows with the case header displaying the attorney-verified value — and if so, what should the header display before any value is verified: the CR-3's value marked unverified, the petition's, both, or nothing?**
- ⬜ **Q-IN2-4.** A VIN is an identity-class fact about a thing that has no row anywhere: CD-1 ruled identity lives in `parties`, `parties.kind` admits only `'individual'` and `'organization'`, and §7.2's promotion examples are both organizations, so **for IN-2's most-named target the promotion half of the ruling has no destination** (spec §6.4). Three shapes: **(a)** vehicles become directory rows (a third `kind` or a `party_type` registry entry) — cheap, and it puts trailers in the contact directory permanently; **(b)** vehicles stay case-scoped fact clusters with no identity of their own — honest to §7.1, but one fleet tractor across three matters is three unlinked clusters; **(c)** a separate instrumentality entity — which is a second identity table, the defect class the schema comment above `parties` says CL-2 was built to kill. **Which, and does the answer reopen `contact-directory.md` as a living-spec revisit?**
- ⬜ **Q-IN2-5.** §7 rules what happens when **two sources** disagree but is silent on **one source, re-read** — the same scanned CR-3 re-extracted after an OCR-model upgrade or against a corrected code-sheet version. **Should extraction results be append-only with supersession pointers, so a background re-run can never silently change a value you already verified (spec §7.3) — or is update-in-place with a review-log entry sufficient for a solo practice, accepting that a verified fact can move under you between the draft and the filing?**
- ⬜ **Q-IN2-6.** `oaa_intakes.fields_json` already carries *"value/confidence/provenance per field"* for the criminal OAA intake path — the same three properties IN-2 needs, against the same class of fixed government form — but as a serialized `text` blob on an audit row, with no `verified_by_attorney` and no way for two sources to disagree inside it (spec §6.2). **Should the IN-2 fact table be designed as the shared home for both intake paths, with `oaa_intakes` becoming a producer into it — the same "one answer governs both so defaults cannot drift" reasoning #66 used for demographics — or does OAA keep its own audit shape, accepting two provenance regimes?**
- ⬜ **Q-IN2-7.** §7.1 orders the review queue low-confidence-first, which is ruled. But class-A coded values fail loudly (membership test) while class-B identifiers fail silently — a VIN with one transposed character passes every check the system has and flows into definitions, requests, and subpoenas looking correct (spec §7.4). **Should the review surface additionally mark high-confidence identifiers that no independent check has corroborated, so "confident" and "corroborated" are not displayed as the same thing — without disturbing the ruled queue order?**
- ⬜ **Q-IN2-8.** The CR-3's Person Type (list 14) enumerates driver, passenger, pedestrian, pedalcyclist, motorcycle occupant; the directory's role vocabulary is derived from the party registry *"so it cannot drift,"* and additions to CD-1's controlled vocabularies are spec-level acts (spec §8). **Is mapping list 14 onto directory role tags a spec-level act requiring your ruling on each mapped value — or may an extraction propose a role tag as a flagged suggestion the way it proposes any other unverified fact?**
- ⬜ **Q-IN2-9.** IN-2's extraction half is GPU-arm gated behind Phase 1b, T3 is UNAUTHORIZED (KICK-1), and the live crash report in the origin matter was image-only with no text layer — so the "easiest possible target" still needs the gated arm (spec §3.3). Meanwhile FE-4 renders instrumentality definitions from IN-2 fact rows that do not exist, and Q-FE4-3 asks what to do in the interim. **Is there a deliberate interim IN-2 — the fact table and the discrepancy shape built and filled by ATTORNEY HAND ENTRY, with no extractor at all — so the definitions block, the promotion gate, and the flag mechanics can be exercised before the GPU arm opens? Or does splitting IN-2 that way create a half-item that never gets finished?**

*(**THE TASK 9 SPECS' EIGHTEEN QUESTIONS — ADDED 2026-08-15, THIRTY-EIGHTH queue-runner invocation, with NO DURABLE IDs**, from the two intake specs `docs/specs/in-1-answer-mining-spec-2026-08-15.md` §7 and `docs/specs/in-3-held-sets-service-triggers-spec-2026-08-15.md` §8 (CHAT-DISPATCH Task 9). `Q-IN1-` and `Q-IN3-` were collision-checked repo-wide at HEAD and are FREE; **`IN-` is an existing durable namespace and minting into it is Michael's act**, so they stay packet-local. Full text per QR-1 — the packet is deleted, so these rows are the only place these questions live. **Both specs are PROPOSED and neither item's design question is answered; nothing below is a design.**)*

- ⬜ **Q-IN1-1.** The queue's own question, unanswered: *"Is answer ingestion a distinct pipeline step, and where in the UI do suggestions surface?"* Three surfaces exist to build on and they are not equivalent. (a) The **/inbox pattern** — staging items with ranked suggestions and confirm-to-file, which already has the suggest/dismiss/tuning-log machinery but is transcript-shaped and case-agnostic at entry. (b) A **case-detail tab or card**, alongside the roster-flag card's Mark-handled affordance, which is case-scoped by construction but adds a surface. (c) **Inside the drafting flow**, where hooks appear only when a set is being built, which guarantees they are seen in context but means an answer filed today surfaces nothing until someone drafts. Which, and is answer ingestion its own step or a mode of a general document-intake step that does not exist yet?
- ⬜ **Q-IN1-2.** The scope of "denials" differs between the two statements of this requirement. The origin capability sentence (trucking capture, REQ-05) reads *"flag affirmative pleadings and denials that support contention discovery"* — denials unqualified. The §5.3 question and the queue row both read *"affirmative defenses and **capacity** denials."* The practice moment cited is a *"wrong-capacity/not-proper-party denial."* **Is IN-1 scoped to the capacity/not-proper-party class, or to any denial that supports contention discovery?** The second is a much larger surface — a general denial of a pleaded allegation is a hook by that reading, and most answers deny most allegations.
- ⬜ **Q-IN1-3.** `REQ-CAPTURE_deficiency-handling_2026-08-11.md` calls IN-6 *"the substrate IN-1's answer-mining runs on,"* but IN-6 parses *"a served response set into structured items (instrument type, number, request text, objections[], answer text)"* — a discovery response — while IN-1's input is *"a defendant's filed answer"* — a pleading. **Which is meant?** If IN-6 really is the substrate, then IN-1's subject is the `answer_text` of response items and its name is misleading. If the pleading is the subject, IN-1 needs an ingestion path IN-6 does not provide, and the dependency stated on the queue is not a dependency at all. Both readings change what gets built first.
- ⬜ **Q-IN1-4.** Do IN-1's hooks land on the **IN-2 case-scoped fact table** whose home `contact-directory.md` §7 already ruled — `{fact_id, value, source_document, source_field, extraction_method, verified_by_attorney}` — or in a separate annotation store? A capacity denial has §7's exact shape: an assertion about the case, sourced to a document and field, extracted by a method, awaiting attorney verification. A **suggested request stub** does not: it is a proposal, not a fact. **Does IN-1 split — denials to the fact table, stubs somewhere else — or does the fact table stay IN-2's alone?**
- ⬜ **Q-IN1-5.** What is the **identity of a dismissed hook**, given that dismissals are remembered per case? If identity is the source passage's text, an amended answer that restates the same defense in different words re-offers a hook the attorney already rejected. If identity is (party, hook class), a second genuinely different affirmative defense of the same class is silently suppressed — **which is the dangerous direction, because a suppressed hook is invisible.** Is there a third identity, and does a dismissal ever expire?
- ⬜ **Q-IN1-6.** **Amended and supplemental answers.** An amended answer supersedes the original as the live pleading. Do hooks from the superseded pleading survive, and do dismissals carry forward to the restated version? A defense dropped on amendment is a fact worth surfacing in its own right; so is a defense added.
- ⬜ **Q-IN1-7.** CLAUDE.md's data-hygiene rule reads: *"PHI-touching AI processing (transcription, bill/EOB ingestion) runs **locally** on Michael's GPU machine by design — this is a privilege/PHI posture, not a hosting shortcut. Do not introduce cloud AI processing of case documents without an explicit decision from Michael."* **Is a publicly-filed answer inside the "case documents" that bar covers?** The document itself is a public court filing; the *fact that this firm is reading it for this client's case* is work product. If the bar applies, IN-1 is gated on the GPU arm — which BUILD-STATE reports as **T3 WORK IS UNAUTHORIZED** until KICK-1's missing kickoff doc is located or re-issued. **This question decides whether IN-1 is a near-term item or a Phase-1b one**, and it is yours.
- ⬜ **Q-IN1-8.** The IN-series elicitation caveat says renaming is *"a one-word veto before these IDs get load-bearing."* This spec and its IN-3 sibling are the first artifacts to build structure on `IN-1` and `IN-3` as names. **Does the IN series stand?** Answering now costs one word; answering after a build costs a rename across a schema. **SHARPER 2026-08-15 (#84): the IN-2 spec is the THIRD artifact building structure on an `IN` name**, and it is the one the rest reach into — FE-4 renders from "IN-2 fact rows," IN-1's Q-IN1-4 asks whether its hooks land there, and IN-3's questions cross-reference it. The veto is still one word; its blast radius grew again.
- ⬜ **Q-IN1-9.** Multi-defendant cases produce **one answer per defendant** — the trucking posture ran five answering entity defendants plus an unserved driver. Is the unit of work **per answer** (five independent hook sets, five dismissal ledgers) or **per case** (one merged list)? The FE-5 spec found the interrogatory budget to be pairwise per (propounding, responding) party; hooks look pairwise too, and a merged list would collapse five defendants' distinct pleadings into one undifferentiated queue.
- ⬜ **Q-IN3-1.** The queue's own question, unanswered: *"What does the HOLD lifecycle look like in the document model?"* Concretely, it decomposes into four: **(a)** what are the states — is HOLD a value in one lifecycle vocabulary alongside IN-4's `generated → delivered → attorney-edited → finalized → served`, or a separate flag that can coexist with any of them? **(b)** What transitions are legal — can a held instrument be edited, re-rendered, or withdrawn without being served? **(c)** Who or what can release — only the attorney, or can a recorded trigger occurrence release automatically once the condition is met? **(d)** Is a hold ever satisfied by something other than its stated trigger (a defendant who answers by filing a special appearance; a defendant who is dismissed)?
- ⬜ **Q-IN3-2.** **What object carries the HOLD — the document, the "set," or the (instrument, target) pair?** The queue row says *"a document/set status,"* which is two answers. No set concept exists in the schema, and the live evidence is one wave of six documents with one held — which reads as a per-pair state, not a per-wave one. If sets become real objects (FE-6 packaging modes make that likely), does a set-level HOLD mean every member is held, or is set-level HOLD a summary of its members' states?
- ⬜ **Q-IN3-3.** `cd1-build-slice.md` deferred the service-story columns to *"the first instrument consumer, as the living spec's first planned revisit,"* and the CD-1 migration repeats it. **Is IN-3 that first consumer, or is IN-4?** IN-3 needs the target's service story to know what a release even means; IN-4 executes the service and stamps the certificate. **Whichever is named, the CD-1 living spec's first planned revisit opens on that ruling** — so this is a scheduling decision about CD-1 as much as a scoping one about IN-3.
- ⬜ **Q-IN3-4.** **Who owns the certificate-of-service object — IN-3 or IN-4?** IN-3's origin practice moment is *"a full set drafted for a defendant who has not yet appeared, **with a placeholder certificate of service**"* — the placeholder is inside IN-3's evidence. IN-4's origin makes the certificate date *"bound to the service event, not typed into the text — stamped at service time."* That is one artifact seen from two directions: IN-3 owns the placeholder that must not be served, IN-4 owns the real date that replaces it. **Does the certificate live in one item's design with the other pointing at it, and which?**
- ⬜ **Q-IN3-5.** **Where does a trigger occurrence live?** Three candidates, none of which exists. **(a)** A **roster-entry attribute** — the origin capture's own inventory carries `{{answer_filed_date}}` and `{{appeared}}` (bool) at entity level, and `contact-directory.md` §6.2 already puts the service story on the same row. **(b)** A **case-event row** in a table the schema does not have and that several other items (DE-2's escalation timeline, IN-4's lifecycle timestamps, the deadline engine's recompute events) would also want — which argues for building it once, deliberately, rather than four times narrowly. **(c)** A **`calendar_events` row**, whose `event_type` CHECK has no value for it and whose `start_local`/`end_local` are naive local datetimes stored as `text`. Which, and is the choice IN-3's to make or does it belong to whatever names the case-event table?
- ⬜ **Q-IN3-6.** **What happens to a set held for a defendant who never appears?** Nothing on the record answers this and the failure is silent by construction: a finished instrument sits in a state nobody is prompted about, aimed at a party who may have been non-suited, defaulted, or served by publication months ago. **Should a hold carry a review date, a staleness prompt, or an expiry?** The record already has the adjacent failure class on file — a hand-filled certificate date going *"silently stale"* across five documents (IN-4's origin) — and this is the same class one level up.
- ⬜ **Q-IN3-7.** **Does a HOLD freeze the instrument or leave it editable?** A held set can be made wrong by events outside it: an amended petition, a new defendant, a corrected VIN, a re-versioned definitions set. FE-9 (family drift detection) and FE-11 (roster-driven consistency sweep) own drift on the drafting side and are both **OUT of FE-D1** with the discovery slice named as their home. **Does release run a consistency check before the prompt fires, and if it finds drift, is that blocking or advisory?** The record has a live precedent for the shape of that question — the attorney-edit capture asks whether a family-consistency drift report belongs *"in the finalization UI (blocking, pre-service) or in a review log (non-blocking)."*
- ⬜ **Q-IN3-8.** **Does release require a fresh render?** The FE-4 spec filed yesterday (#81) records that definitions-set propagation is **prospective only** — *"New renders use the latest version; served documents are history via their `.docx`."* A set held across a version bump is neither: it is rendered but not served. **Is a held instrument "rendered" (frozen at its version, released as-is) or "unserved" (re-rendered at release against the current version)?** Q-FE4-4 — whether a definitions set snapshots roster-derived text or holds a live reference — decides part of this from the other end, and the two should be answered together or the answers will not agree.
- ⬜ **Q-IN3-9.** **Naming.** `service_date`, `service_start` and `service_end` already mean *medical dates of service* on `medical_bills` and `bill_line_items`. Do you want service-of-process fields named distinctly from birth — `served_on`, `service_of_process_method` — so that a reader of the schema cannot confuse the two? This is a one-line decision now and a migration later.

*(**THE TASK 8 SPECS' TWENTY-TWO QUESTIONS AND THREE LOOKS — ADDED 2026-08-15, THIRTY-SIXTH queue-runner invocation, with NO DURABLE IDs**, from the three form-engine specs `docs/specs/fe-4-definitions-sets-spec-2026-08-15.md`, `docs/specs/fe-5-interrogatory-budget-spec-2026-08-15.md` and `docs/specs/fe-6-instrument-packaging-spec-2026-08-15.md` (CHAT-DISPATCH Task 8), all three **PROPOSED, nothing built, nothing verified, no registry file touched, and no existing repo file edited**. **The `Q-FE4-`, `Q-FE5-`, `Q-FE6-` and `FE5-LOOK-` namespaces were re-checked repo-wide by this runner and returned ZERO — and were deliberately NOT minted**: the packet's §6 bars it and routes the series decision to **`ID-DL-1`**, which now governs **THREE** packets' packet-local questions. Text below is **verbatim from each spec's open-questions section** (FE-4 §6, FE-5 §7, FE-6 §6) per QR-1, reflowed to one line per row and otherwise unaltered. **The three specs adjudicate nothing** — FE-4, FE-5 and FE-6 were each RULED 2026-08-11 at `form-engine.md` §13.1/§13.2/§13.3, and each spec states that where it differs from §13, §13 governs. **Their FE- rows above stay ⬜; a spec is not a closure.** **Provenance convention carried intact: TIER A = clean-authority PDFs read locally; FLP = CourtListener. No summarizing fetch layer was used anywhere in this packet, and there is no TIER B in it.**)*

- ⬜ **Q-FE4-1.** §13.1 says "a material edit creates a new version." **What makes an edit *material*?** A typographical correction to a standard definition, a substantive narrowing of the ESI definition, and the addition of a newly-identified vehicle are three different kinds of change, and only the last two obviously deserve a version. **Does the engine classify materiality itself, present the choice to the attorney at save time, or treat every edit as material and accept version inflation as the cost of never guessing wrong?**
- ⬜ **Q-FE4-2.** FE-4 has no FE-D1 disposition on the record. `fe-d1-build-slice.md` dispositions FE-8 through FE-17 in or out with named homes, and FE-2 is separately parked, but FE-4 (and FE-5, FE-6, FE-7) appear in neither list. **Is FE-4 IN FE-D1 — disclosures under TRCP 194 do not conventionally carry a definitions block, which argues out — or is its home the discovery slice alongside the §13 item model? Should the four already-ruled items get explicit dispositions so the FE series reads uniformly?** *(Runner note: the same question is asked from the FE-5 and FE-6 sides as Q-FE5-2 and Q-FE6-1; one ruling closes all three. `fe-d1-build-slice.md` was NOT edited — the packet's §6 bars it.)*
- ⬜ **Q-FE4-3.** §13.1 sources instrumentality definitions (vehicles by VIN) from IN-2 fact rows. IN-2 has no spec and no table, and BUILD-STATE lists the IN-2 fact table as explicitly OUT of the CD-1 slice. **Until IN-2 exists, is a VIN-keyed instrumentality definition (a) an ordinary case-specific slot the attorney fills by hand, (b) blocked entirely so the engine never renders a half-populated instrumentality definition, or (c) a reason the definitions set should not ship before IN-2 does?**
- ⬜ **Q-FE4-4.** The party-set definition "renders from the roster," and definitions-set versions are immutable with every instrument stamping the version used. If the roster changes after version *n* is minted, does re-rendering version *n* produce the new roster's text or the roster as it stood when *n* was minted? **Put concretely: does the definitions set snapshot roster-derived text at version-mint time, or does it hold a live reference that resolves at each render?** A live reference means the version stamp does not identify what went out; a snapshot means the roster feeds composition rather than rendering, and a roster correction requires a new version.
- ⬜ **Q-FE4-5.** **Where does the definitions-set version stamp live?** The natural home is the generated-document record alongside `template_version` (§10), but `generated_documents` at HEAD constrains `doc_type` to a single value and stores content inline. **Does the instrument render extend that table, or does the form engine get its own generated-instrument table — and if the latter, what happens to the §10 language that treats them as one record?**
- ⬜ **Q-FE4-6.** §13.1 lists three item kinds (standard / case-specific slot / conditional block). **Are those three exhaustive and closed — a CHECK constraint — or an initial vocabulary expected to grow?** The registry-vocabulary pattern CD-1 used (derive the vocabulary from one source so it cannot drift) would argue for a single definition point either way.
- ⬜ **Q-FE5-1.** FE-5 as ruled budgets **interrogatories only**. On the July 2026 text, Level 1 also caps **requests for production at 15** (190.2(b)(4)) and **requests for admissions at 15** (190.2(b)(5)), each with the identical discrete-subpart sentence — while Level 2 caps neither. **Does FE-5's budget extend to RFPs and RFAs when the case is in Level 1, or is that a separate item?** The practice consequence is direct: the trucking sets combined interrogatories, RFPs and RFAs in one served document, so in a Level 1 case a single document would be running three budgets at once, two of which the engine as ruled does not track.
- ⬜ **Q-FE5-2.** FE-5 has no FE-D1 disposition on the record. `fe-d1-build-slice.md` dispositions FE-8 through FE-17 in or out with named homes, but FE-4, FE-5, FE-6 and FE-7 appear in neither list. **Is FE-5's home the discovery slice — which is where the item table it counts is going — and should the four already-ruled items get explicit dispositions so the FE series reads uniformly?**
- ⬜ **Q-FE5-3.** *In re Swepi L.P.* is the only empirical datum on record about facial-vs-legal subpart counts, and it comes from the Fourth Court of Appeals: **19 numbered interrogatories, a facial subpart count of 98 asserted by the objecting party, and a holding that none of the nineteen had multiple discrete subparts.** Under §13.2's escalation rule (worst-case 98 > cap 25 while numbered 19 ≤ 25) **the engine would have warned**, and on the holding the warning would have been wrong. **Does that change the escalation threshold, argue for a ratio-based or absolute-margin condition instead of bare crossing, or does it confirm the ruled design** — on the reasoning that a warning which prompts one attorney look is cheap next to a recharacterization fight, and that the passive display was correct the whole time? The recorded warning-fatigue risk is the thing being traded. *(The spec records this as evidence about firing frequency, expressly NOT as an argument that the ruling is wrong.)*
- ⬜ **Q-FE5-4.** *Braden v. Downey* construes the **thirty-answer limit of former Rule 168**, not Rule 190's discrete-subpart rule; the current rule's Comment 3 cites it for the narrower proposition that the boundary is *"not susceptible of precise definition."* **Does *Braden* enter the registry as an antecedent proposition with that limitation stated on its face, does it ride only inside a Rule 190 comment entry, or does it stay out of the registry as pre-rule authority?**
- ⬜ **Q-FE5-5.** The pleaded discovery level and the operative one can differ: TRCP 190.1 requires the plaintiff to plead the intended level, but Comment 1 says the pleading *"does not bind the court or other parties"* and that a case not in Level 1 is in Level 2 until a Level 3 plan is ordered. **Does the case record store one level field that the attorney maintains, or two — pleaded and operative — with the budget reading the operative one?** A single field silently answers a question the rule leaves open; two fields make the divergence visible and cost an intake keystroke. *(Runner corroboration: `cases` has no discovery-level column and a repo-wide grep across `db/` for `discovery_level` / `discovery_control` / `190.3` returns nothing — FE-5's hard prerequisite is a field on the case, not a form-engine table.)*
- ⬜ **Q-FE5-6.** **Should the budget model the ways the cap moves?** TRCP 190.6 puts Rule 202 and Rule 621a discovery outside these limits; 190.5 requires the court to allow additional discovery in stated circumstances; 191.1 allows modification by agreement or by order for good cause. A cap rendered as a fixed number is a simpler UI and a less accurate one. If the answer is yes, an enlargement is an event with a source (order / Rule 11 agreement) and there is no case-event table to hold it — **CE1 is not authorized (D-CL2-9).**
- ⬜ **Q-FE5-7.** **Which of the twelve propositions in §2 become registry entries?** The dedupe at §2.1 sorts them three ways. **P-4 and P-5 duplicate VERIFIED entries** and add content those entries do not carry. **P-12 is the unstated half of an existing UNVERIFIED 197.1 entry** — a different proposition on the same rule number, which this project's own precedent treats as a new entry rather than a second observation. **Everything else is genuinely absent**: all of Rule 190.2 (the three Level 1 caps), cmt. 3's *definition* of a discrete subpart, 190.5, 190.6, cmt. 5, and both cases. Against entering them: the backlog is 34 today (37 when V-5 executes) and the queue is already the project's bottleneck (H22), and if they enter, the fifth-registry-file question (**Q-STAT-5**, still open) is reached again. Against not entering them: this document becomes their only home, and it is not a registry — FE-5 would be built on propositions that live in a spec.
- ⬜ **Q-FE5-8.** TRCP 190 cmt. 3 prints the *Braden* pin cite as **"972-928"**, which is impossible, and the passage sits at 927–28 by both the opinion's star pagination and *Swepi*'s pin cite of the same language. **Nothing was repaired.** **Does the project record known defects in official source documents anywhere durable — this is now the second such finding, after the garbled Bexar county-court sentence reported at #80 — or does each one live only in the memo that found it?** *(Verified to be in the PDF, not the extraction: `-layout` and raw `pdftotext` passes return the identical string.)*
- ⬜ **Q-FE5-9 — THE HEADLINE. A DIVERGENCE INSIDE A *VERIFIED* ENTRY.** The VERIFIED **TRCP 190.3(b)(3)** entry reads *"Level 2 discovery limits **each party** to 25 interrogatories"*; the operative text reads *"Any party may serve on **any other party** no more than 25."* Those are different budgets and the gap scales with the number of opposing parties — 25 total versus 25 × 5 in the trucking posture. **No replacement wording is proposed** (route (c) is a ruled one-off, not standing law) and nothing was edited. **Two questions ride here: does the entry's wording need your verification act, given that verification attaches to wording; and should FE-5 be built against the entry as verified, or against the rule text, if the two are not reconciled before the engine is authorized?** *(Why this row is different in kind from the 08-14 statute pass's eighteen divergences: **this one sits in an entry you VERIFIED**, on 2026-08-11, annotated "Load-bearing for: FE-5." An engine built to the entry's wording would refuse discovery the rule permits, silently. `legal-rule-registry-discovery-and-carrier-duties.md` was NOT touched — the packet's §6 bars it in terms.)*
- ⬜ **Q-FE6-1.** **Is FE-6's disposition explicit or inferred?** `fe-d1-build-slice.md` says *"The §13 item model itself — slice 2's core; nothing in FE-D1 creates items,"* and FE-9, FE-11 and FE-17 are all homed at "the discovery slice." Read together the record places FE-6 at the discovery slice — but there is no FE-6 row in either the IN or the OUT list, and the same is true of FE-4, FE-5 and FE-7. **Should the four already-ruled items get explicit dispositions, and is "slice 2" the same thing as "the discovery slice" (the two names are used in the same document and never equated)?**
- ⬜ **Q-FE6-2.** **Does the item table's `role:<tag>` scope draw its vocabulary from CD-1's party registry** — the derived-so-it-cannot-drift pattern BUILD-STATE records — **or does it carry its own enumeration? If its own, what keeps the two in step when a role tag is added, and who notices when they diverge?**
- ⬜ **Q-FE6-3.** In separate mode one render produces three served documents. **Is each its own generated-document row — three rows, three certificates of service, one verification page on exactly one of them — and if so, what ties the three together as one render of one set to one target?** A render/wave identity would do it, and nothing like that exists. Related: the `generated_documents` table at HEAD constrains `doc_type` to a single permitted value and stores content inline, so instrument rendering does not fit that table as written in either mode.
- ⬜ **Q-FE6-4 — the sharpest unmodelled case.** **How does the item model carry an RFA whose phrasing flips with the target's relationship to a named party** — the capture's example, "the tractor was owned by X," rendered one way to X and another way to X's sibling? The three ruled scope values (`common`, `role:<tag>`, `entity-specific`) describe *which targets get an item*, not *which wording a given target gets*. **Is variant selection (a) an item-level variant table keyed by role, (b) a merge-field expression resolved at render, or (c) genuinely two entity-specific items, accepting the duplication the enter-once principle otherwise rules out?**
- ⬜ **Q-FE6-5.** **Does the packaging mode belong to the case, the wave, or the individual render?** A case-level setting matches how a firm actually serves; a per-render choice matches §13.3's words (*"packaging is a render-time choice"*); a per-wave setting sits between. The practical test is whether a later supplemental instrument in the same case may be served in a different mode than the original, and whether the engine should let that happen silently.
- ⬜ **Q-FE6-6.** *"Set = ordered filtered list."* **Where does the order live?** If ordinals sit on items, every filtered set inherits gaps (item 7 is a `role:carrier` item absent from the driver's set) and the visible numbering must be recomputed per target — which is exactly the numbering pass FE-10 builds, and exactly the defect class its live evidence describes (a chained start value producing a silent duplicate). If ordinals sit on the set-target pair, the ordering is stored per target and the item table is order-free. **Which, and does the FE-10 numbering lint run over separate-mode documents independently or across the family?**
- ⬜ **Q-FE6-7.** §13.3 fixes one certificate of service per document. In separate mode three documents go to the same party on the same day under three certificates. **Is that three service events on the record — with three dates that could, through a technical-failure re-transmission, actually differ — or one service event evidenced three times?** The Task 7 memo's finding that the filing moment is contested (LR-LOOK-1) makes this less theoretical than it looks, and the deadline engine would consume whichever answer holds.
- **Three concrete looks, all Michael's, none a queue ruling:** **FE5-LOOK-1** — a commercial citator on *Braden v. Downey*, 811 S.W.2d 922. **FLP is not a citator; nothing in the spec represents it as good law.** · **FE5-LOOK-2** — a commercial citator on *In re Swepi L.P.*, 103 S.W.3d 578, **which carries the load in the spec's §3.1 and is a 2003 intermediate-appellate mandamus.** · **FE5-LOOK-3** — four further Texas opinions containing "discrete subpart" were located and **deliberately not retrieved or characterized**: *In re Novartis Pharms. Corp. v. Texas* (Tex. App.—15th Dist. 2025), *Affirmation Holdings, LLC v. Windsor at Barton Creek, LP* (Tex. App.—Austin 2025), *In re Estate of Flarity* (Tex. App.—Beaumont 2020), and *In re Manhattan Vaughn, JVP* (Tex. App.—Houston [1st Dist.] 2015). **Five Texas opinions in total use the phrase** — the case law on this rule is thin, which is itself a design fact. Reading the four is the next increment if Q-FE5-3 turns on how courts actually apply the test.
- ⬜ **RUNNER-FOUND DEFECT, 2026-08-15 (#81), thirty-sixth invocation — `fe-5-interrogatory-budget-spec-2026-08-15.md` IS MISSING ITS HEAD, AND IT WAS LANDED THAT WAY ON PURPOSE.** The file as staged **begins at line 1 with `## 2. Legal substrate`** — it has **no title line, no `Status: PROPOSED. Nothing here is authorized to build.` line, no canonical-path line, and no §1 RECONCILE FIRST section.** FE-4 and FE-6 carry all four. **This contradicts the packet's own §5**, which states that *"Every one of these three specs states on its first line that it is PROPOSED and authorized to build nothing"* — true of two of the three. **It is not a transmission truncation introduced by this runner:** the packet's stated line counts (311 non-blank / 389 raw) match the truncated file **exactly**, so the packet was authored and counted against the same head-less text, and the three landed files are byte-identical to the staged ones (`cmp` clean). **Nothing was reconstructed.** §4.2 says "verbatim … do not normalize, strip, or reflow," and inventing a status line into a spec is the fabrication class `K-6/K-7` exists to forbid. **The question: do you want the head restored from the design side (title + status + canonical path + §1), or is the spec fine standing on its §8 provenance section, which does say in terms "Nothing is verified. No status moved."?** **Until it is restored, FE-5's spec is the one form-engine spec with no PROPOSED marker on its face** — a real hazard if a future session reads only its first screen. **Michael's, or the next design session's.**

*(**THE TASK 7 MEMO'S SIX QUESTIONS — ADDED 2026-08-15, thirty-fifth queue-runner invocation, with NO DURABLE IDs**, from `docs/specs/bexar-local-rules-and-efiling-2026-08-15.md` (CHAT-DISPATCH Task 7), PROPOSED design input, research support only, **nothing verified**. **The `LR-<n>`, `EF-<n>` and `BX-<n>` namespaces were checked repo-wide and are FREE — and were deliberately NOT minted**: the packet's §6 bars it and routes the series decision to **`ID-DL-1`**, which now governs **two** packets' packet-local questions (the Task 6 deadline memo's Q1–Q5 and these). Text below is **verbatim from the memo's §11** per QR-1. **The memo's provenance convention is load-bearing: TIER A = clean-authority PDFs read locally; TIER B = reached through a summarizing fetch layer and NOT quotable as rule text.** Both headline findings rest on TIER B strings and each carries a named look instead of a conclusion.)*

- ⬜ **[Task 7 memo Q1] — The filing moment is contested, and it is a CANDIDATE, not a finding.** Part 2 of the Bexar statutory county courts at law local rules appears to state that *"Documents filed with the court are considered filed when marked accepted and file marked by the Bexar County Clerk,"* while TRCP 21(f)(5) and Statewide Criminal E-Filing Rule 2.3(a) both fix filing at transmission to the filer's EFSP, and TRCP 3a(b) provides that a local rule must not be inconsistent with a Supreme Court rule and that the requirement *"extends to any time period provided by these rules."* **The local sentence reached this memo only through a summarizing fetch layer, and that same layer garbled a different sentence in the same document (§6.3). Will you make the concrete look (LR-LOOK-1) — read that section in a clean copy and confirm the sentence, its number, and its scope? If it reads as reported, do you want it opened as a registry entry, and does the filing module display the transmission date, the acceptance date, or both?** *(The exposure named in the memo: a filing transmitted late on a limitations date and accepted two days later.)*
- ⬜ **[Task 7 memo Q2] — A LIVE PRACTICE ITEM, not a design question, and the fastest-moving thing in the packet.** Bexar Civil District Court Local Rule 3.O.1 requires that **all pleadings** include a court-approved A.I. certificate attesting that generative-A.I.-produced language, quotations, sources, citations, arguments, and legal analyses will be verified before submission by a Texas-licensed lawyer through traditional non-A.I. sources. **This is a live filing requirement in your primary civil forum, not a design question. Do your current Bexar civil filings carry that certificate on the Presiding Court's approved form? And should the drafting side treat it as a court-keyed required attachment on the FE-15 render path, alongside the conferral certificate?**
- ⬜ **[Task 7 memo Q3] — Currency is a different check than feature-intake item F specified.** TRCP 3a(c) makes publication on the OCA's website a condition of a local rule's effectiveness, the 2023 amendment removed Supreme Court approval, and the Bexar county-court rules set no calendar date and instead take effect *"upon their publication on the website of the Office of Court Administration."* The county's own index page currently hosts several vintages, and TOPICs could not be searched through this session's fetch layer. **Do you want the court-profile layer's currency check (feature-intake item F) re-specified to compare against the OCA publication record rather than a county PDF's date — storing the publication reference as the authority and the PDF as a convenience copy? And will you make LR-LOOK-2 (one TOPICs pass filtered to Bexar) the first exercise of that feature?**
- ⬜ **[Task 7 memo Q4] — The rule text bearing on H43 is now supplied; the question is not answered.** H43 asked whether a Bexar party whose opponent will not confer is genuinely trapped. Local Rule 4.E supplies an express alternative certificate — *"has made reasonable efforts to confer … but has been unable to do so"* — while reserving the court's discretion to strike the setting; the 225th requires three documented attempts; the 285th bars any setting until a certificate of conference is filed. **Does that change your answer to H43, and does the heartbeat's Bexar conferral gate become a soft gate with a two-branch certificate rather than the hard block it is currently modeled as — differently per court?**
- ⬜ **[Task 7 memo Q5] — A CLE qualifier that the rule text does not carry.** `apil-2025-course-book-mining-pass1.md` §1.4 carries a `[VERIFY]` on the CLE assertion that a Bexar case cannot reach trial without mediation unless the parties agree not to mediate. Local Rule 9.A requires mediation no later than 45 days before trial for all cases on the jury docket and 9.D requires the mediator's report 30 days before trial, but **no "unless the parties agree" qualifier appears in the rule text retrieved**, and the 285th states its own discretionary version. **Do you verify the 45/30 pair, and do you want the CLE's agreement qualifier recorded as unsupported by the rule text, corrected in the mining doc, or left alone pending your own read?** *(The mining doc was NOT edited — that edit is yours.)*
- ⬜ **[Task 7 memo Q6] — A standing constraint on the filing layer, same class as Q-6.** Filing *through* the application requires the OCA-certified EFSP relationship described in the memo's §7.4 — an ECF SOAP interface reached only by becoming or contracting with a certified provider, with no published certification process and no public API documentation. That is the same class of decision as Q-6, which bars wiring the CourtListener API into the app until you resolve terms with FLP. **Do you want a standing constraint recorded that no filing-submission integration is designed until you rule on the EFSP question — so that a future filing module is scoped from the start to envelope tracking and document preparation, not submission?**
- ⬜ **H43 — Can a hearing be set unilaterally in Bexar if the opponent will not confer? CARRIED AND OPEN; given a row here for the FIRST time, 2026-08-15 (#80).** It lives in the heartbeat register (H1–H83) and had **no row in this file at all** — the same gap the Q-3 sync-picker item had at #46 — so it is registered here **cross-linked, not renamed**; the heartbeat register keeps H43. **Task 7 supplies the rule text and expressly does not answer it** — see the memo's §5.2 and Q4 above.
- **Two concrete looks, both Michael's, neither a queue ruling:** **LR-LOOK-1** — read the "considered filed" section of the Bexar county-courts-at-law local rules in a clean copy; confirm the sentence, its section number, and its scope. **Settles the memo's §2.** · **LR-LOOK-2** — on TOPICs, filter County = Bexar and record, per court group, what is published and its publication date. **Settles §8 items 1–4 for every Bexar court at once.**

*(**OPEN-1 – OPEN-5 ADDED 2026-08-15, thirty-fourth queue-runner invocation**, from the chartered record-integrity audit — `docs/specs/record-integrity-audit-2026-08-15.md`, PROPOSED design input, **audit findings only: nothing verified, nothing ruled, no file altered by the audit and none altered by this merge**. **Collision check run repo-wide before entry: ZERO `OPEN-<n>` IDs existed anywhere in `docs/`**, so the packet's series was carried as given. **Question text below is copied VERBATIM from the audit doc's §7 per QR-1.** **Nothing in the audit was applied** — the packet's §6 bars it in terms: the mechanical recounts *"look like typo fixes; they are Michael's call (OPEN-5) because applying auditor findings without a ruling converts the auditor into an editor."* The stale figures it names are therefore still in BUILD-STATE, each now carrying a pointer to the audit doc.)*

- ⬜ **OPEN-1 (from AUD-2):** GL-1 item 5 reads "instructions v15" in the canonical gates doc, "v17" in BUILD-STATE, and v18 is in force. **Do you append a one-line clarification to GL-1 reading item 5 as "the instructions current at the re-check, per trigger 1," or rule that the gates doc's frozen version number is fine because trigger 1 governs anyway?** *(On the launch path. `Go_Live_Gates.md` is canonical and append-only and was NOT edited.)*
- ⬜ **OPEN-2 (from AUD-3):** The carrier-duties file carries 22 VERIFIED entry headings deriving from 13 ruled capture bullets plus 7 entry-by-entry rulings. **(a) Does verification of the combined bullet "TRCP 197.2(a), 196.2, 198.2 — 30-day response period…" extend to the three separately-worded entries it became? (b) What should the header's count say — "22 entries (20 ruled units: 13 bullets + 7 entries)" or a re-ruling that makes it 22 everywhere?** Wording and count are yours; nothing was touched. *(Runner corroboration 2026-08-15: 22 per-entry `**Status:** VERIFIED` lines counted at HEAD against the header's "ALL TWENTY PROPOSITIONS VERIFIED"; the file's own Structure line names the mechanism — "fifteen numbered lines, thirteen propositions — the three 30-day rules were one captured line, split here." **No registry file, header count, or status was altered.**)*
- ⬜ **OPEN-3 (from AUD-4):** **Is mdb-pllc the P15 or the P1? If the P15: where is Probate Corpus.zip, and do you want it re-parked outside the packet inbox either way?** *(The audit flags the hazard independently of the answer: a non-packet zip parked where the runner collects zips is adversarially parked. `inbox/` on this machine is empty.)*
- ⬜ **OPEN-4 (§12 of the charter, raised not decided):** This audit's charter and CHAT-DISPATCH are design-chat prompts with no durable home; Q-2 makes `docs/prompts/` canonical only for prompts executed by a CODE session. **Does Q-2 extend to cross-interface prompts executed by DESIGN chats (`docs/prompts/` with an "executed by: design chat" line), or does a parallel home belong somewhere else?** CHAT-DISPATCH's loss would make Tasks 7–19 unrecoverable except by re-derivation; it has now survived five sessions on the clipboard. **ANNOTATED 2026-08-15 (#81), NOT CLOSED — a SIXTH session, and Tasks 9–19 are still unwritten.** Task 8 completed from the same clipboard copy; the resume point is Task 9 (IN-1 and IN-3 spec drafts). **Eleven tasks now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#83), STILL NOT CLOSED — a SEVENTH session.** Task 9 completed from the same clipboard copy; the resume point is **Task 10** (IN-2 crash-report field extraction). **Ten tasks (10–19) now ride on a clipboard**, and every one of the seven sessions so far has re-pasted it by hand. **ANNOTATED AGAIN 2026-08-15 (#84), STILL NOT CLOSED — an EIGHTH session.** Task 10 completed from the same clipboard copy, which Michael re-pasted mid-session and which **matched the preserved knowledge copy verbatim**; the resume point is **Task 11**. **Nine tasks (11–19) now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#85), STILL NOT CLOSED — a NINTH session.** Task 11 completed from the same clipboard copy; the resume point is **Task 12** (the PR-3 re-parenting migration proposal). **Eight tasks (12–19) now ride on a clipboard**, and nine consecutive sessions have re-pasted it by hand. **ANNOTATED AGAIN 2026-08-15 (#86), STILL NOT CLOSED — a TENTH session.** Task 12 completed from the same clipboard copy, and the design session again reported that the **preserved knowledge copy at `claude/claude_CHAT-DISPATCH_v1_PRESERVED_2026-08-15.md` matched the pasted text** — a second consecutive corroboration, which narrows the loss risk without removing it, since the preserved copy is design-side and the prompt still has no repo home. The resume point is **Task 13** (the QBO integration research memo). **Seven tasks (13–19) now ride on a clipboard.** **ANNOTATED AGAIN 2026-08-15 (#87), STILL NOT CLOSED — an ELEVENTH session.** Task 13 completed from the same clipboard copy, and the design session again reported that the **preserved knowledge copy matched the pasted text** — a **third consecutive** corroboration. The resume point is **Task 14** (the RE-1 inputs memo). **Six tasks (14–19) now ride on a clipboard.**
- ⬜ **OPEN-5 (from §4-P1/P2, only if you want the fixes standing):** **Do you want (a) the refresh recompute-list added to QUEUE-RUNNER's BUILD-STATE step, and (b) the instructions-version comparison added to the design-side start-of-session steps?** Both are one-line convention changes; neither is adopted by this audit. **This row also carries the one-word "apply" for the AUD-5..9 recounts** — the five stale figures are carried in BUILD-STATE unchanged until you give it.

*(**Q-STAT-1 – Q-STAT-6 ADDED 2026-08-14, thirtieth queue-runner invocation**, from the statute pass — `docs/specs/statute-pass-registry-retrieval-2026-08-14.md`, PROPOSED design input, **retrieval only, nothing verified**. **Collision check run repo-wide before entry: no `Q-STAT` ID existed anywhere in `docs/`**, so the packet's own IDs were carried as given rather than reassigned. Full question text below is copied from the staged file's **§8**, per QR-1, because the packet is deleted after processing and these rows are the only place the questions survive. **The session's one ruling — route (c) on divergence handling — is already recorded and is NOT re-asked here**; what remains open is everything below. **No registry file was altered, and the §9 replacement wording is adopted for nothing.**)*

- ✅ **Q-STAT-1 — RULED 2026-08-14 (#77): ADOPT AS DRAFTED. CLOSED.** The sourcing convention for statutes, rules and regulations is **binding**, sitting under Binding conventions beneath TOOLING, which it parallels for non-case law. Six sub-clauses as ruled: Texas statutes from the official bulk corpus at `Documents\Knowledge Repo\Statutes <download-date>\`, cited by code and chapter with the corpus download date (`/Docs/<CODE>/htm/<CODE>.<CHAPTER>.htm` the named fallback, `?link=<CODE>` never used); Texas rules from the clean-authority PDFs, cited with the PDF's own effective date; federal regulations from the eCFR API with request date and the title's `up_to_date_as_of` per item; **currency never inferred from a document**; **extraction artifacts characterized, not guessed** — transform only characterized contexts, REPORT the rest, spot-check every quotation against raw extraction; and **retrieval is not verification**. **Each source named per item. Trigger #3 fired and was resolved the same day: the full text lives in v18**, drafted and delivered, **Michael's hand to paste** — it is not repo content and was not written into any repo file. *(Original question preserved verbatim per QR-1 below.)*
- ✅ **Meter reading — STATED 2026-08-15 at 00:02 Central. The request, open since #68, CLOSES.** All-models **51% used**, Fable **78% used**, both resetting **Tue 2026-08-18 4:00 PM Central**; session 9%. **A timestamped snapshot, not a standing figure** — it moves continuously, and a stale percentage carried in the record would be worse than none. **Allocation read, design input and not a ruling: Fable is SCARCE, not idle** (~22% across 88 hours, realistically one substantial session), so the **ration-it** branch of the ruled economics clause applies this week rather than spend-it-before-it-expires; all-models at 51% is comfortable, so **Opus and Sonnet work is unconstrained**. Strongest claims on the Fable remainder per §7.2: the **gates re-check** (GL-1 item 5), an **adversarial audit of the record**, and **V-9**. **Chain execution, FE-D1, the CD-1 migration and packet assembly stay OFF Fable. No allocation is ordered.** *(One observation, flagged and NOT corrected: the meter displays the reset as **4:00 PM** where the 2026-08-13 observation recorded **3:59 PM Central**. Not grounds for a version bump; v18 keeps the older figure as delivered.)*
- ✅ **Paste v18 into the Claude project's instructions — RULED 2026-08-15 (#82): v18 IS IN FORCE. CLOSED (A-1, from the audit's AUD-1).** Michael ruled it item by item in the Fable disposition session. Evidence: **two independent design sessions read their own governing instructions and found them headed `v18 — 2026-08-14` with all v18 markers** — the 08-15 auditing session and the disposition session itself. **Paste date not stated; the row stays undated.** **BUILD-STATE's two v18 lines were rewritten at the seventy-first refresh and "paste v18" came OFF the your-hand list.** *(Recorded plainly because it bears on how much this closure is worth: **a Code session structurally cannot see a paste.** This row closes on Michael's ruling and on a design-side inference, not on anything the runner verified — which is precisely why #79's packet forbade Code closing it and why it took a ruling to close.)* *(Original question preserved verbatim per QR-1 below.)*
- *(The paste-v18 row's original text, preserved verbatim per QR-1 — the ruling above is what governs.)* **NEW 2026-08-14 (#77) — Paste v18 into the Claude project's instructions.** **ANNOTATED 2026-08-15 (#79), DELIBERATELY NOT CLOSED (AUD-1): the auditing session read its own governing instructions and they are headed `v18 — 2026-08-14`, with all v18 markers — so v18 appears to be IN FORCE already and this row may have been stale when written.** It is the **v17 correction one version later**: a design-side-only fact a Code session structurally cannot confirm, which is exactly why the packet's §6 forbids Code closing it. **Michael or the next design session clears it, sourced to AUD-1.** The paste is undated, so the row may have been true at the 10:24 refresh. Supersedes the revised v17, **which was in fact pasted** (see the correction at #77 — BUILD-STATE had said otherwise since 08-13 and was corrected in place). v18 carries the SOURCING convention, two device-bridge operational notes, and that correction; verified against v17 before delivery — all 49 marker phrases survive, no section dropped, 429 non-blank lines. **His hand; a Code session cannot see or perform the paste.**
- ⬜ **NEW 2026-08-14 (#77) — Should route (c) become STANDING LAW, or stay the one-off it was ruled as?** Route (c) is the divergence-handling route ruled 08-14: **a session drafts PROPOSED replacement wording for materially divergent registry propositions**, the non-material ones staying as flags. It was **deliberately NOT elevated into v18**, at Michael's direction to choose the safest option and review later with Fable 5, **because a standing rule licensing sessions to draft registry wording pushes against the discipline that wording acts are Michael's alone.** **The question: does route (c) become a standing convention, stay a per-pass ruling you give each time, or get narrowed further?** **Recorded as a choice, not an omission — it is the first item for the Fable review pass.** Nothing is elevated anywhere in the meantime: not CLAUDE.md, not BUILD-STATE, not the spec. **ANNOTATED add-only 2026-08-15 (#79), ROW NOT CLOSED — the Fable audit recommends KEEPING IT A ONE-OFF (audit doc §5).** Its reasoning: the audit's own AUD-3 shows registry wording drifting at the *transcription* layer where no drafting license exists at all, so widening the licensed surface is the wrong direction; and no flag-rot evidence has appeared to argue the other way. **A recommendation, not a ruling — the ruling is yours.** **ANNOTATED add-only 2026-08-15 (#81), ROW STILL NOT CLOSED — the strongest test case yet arrived and the restraint held.** Task 8 found a divergence **inside a VERIFIED entry** (Q-FE5-9) — the situation where drafting replacement wording is most tempting and most dangerous, because verification attaches to wording and a redraft would silently detach it. **The session drafted no replacement wording and edited no registry file**, citing route (c) as a ruled one-off. **Recorded as evidence for the ruling, not as an argument either way:** the one-off posture produced the right behavior without a standing licence.
- *(Q-STAT-1's original question, preserved verbatim per QR-1 — the ruling above is what governs.)* **Rule the statute-sourcing convention, or decline it.** Proposed 2026-08-14 and still PROPOSED. The rule would name: **Texas statutes** from the official bulk corpus in the Knowledge Repo, cited by code and chapter with the corpus download date; **TRCP/TRE/TRAP** from the clean-authority PDFs in the same repo; **CFR** from the eCFR API with the request date and `up_to_date_as_of` recorded per item; **each source named per item**, exactly as the 2026-08-13 TOOLING ruling requires for case law. **If you rule it, project-instructions trigger #3 fires and v18 is owed the same day.** This pass was run as if the rule were already in force, so nothing in it needs retrofitting either way.
- ⬜ **Q-STAT-2 — The fourth registry file.** `legal-rule-registry-draft-entries-medical-billing.md` is headed "ALL ENTRIES UNVERIFIED" and sits outside the backlog of 34, while BUILD-STATE says the registry is three files. **Is that deliberate — draft entries not yet adopted into the registry proper — or a counting gap?** If deliberate, the three-files line would be clearer as "three registry files plus a draft-entry file, not counted in the backlog." If not, the backlog figure moves. *(Runner note: BUILD-STATE now states four-not-three and that the backlog was **not** changed; no other file's count moved.)* **ANNOTATED 2026-08-15 (#78), NOT ANSWERED — the question is sharper than when it was written.** That file holds **ten numbered entries plus one expressly UNADOPTED draft entry**, uses its own `[READ]`/`[STATUTE]`/`[EXT]` source-flag scheme, and — unlike the registry files proper — **its entries are subject-scoped and can carry multiple propositions each** (ENTRY 2 has a "Propositions:" block). **That is an argument that its unit is not the backlog's unit**, which would make the exclusion deliberate rather than a counting gap. It also **holds live Insurance Code material** (ENTRY 8, ch. 1467), which the #78 correction surfaced.
- ⬜ **Q-STAT-3 — Annotate the 2026-08-14 capture, or leave it.** `claude_Authority_Corpus_and_eCFR_Method_2026-08-14.md` §2 publishes a normalizer this pass proved wrong (staged file §3). It is a RAW CAPTURE in project knowledge, and captures are reference-only records — **the append-only instinct says leave it as written; the practical risk says a future session RAG-hits the capture, not this file, and silently corrupts a quotation of primary law.** **Do you want the capture's §2 annotated as superseded, or left untouched with this file carrying the correction?** Design sessions can write project knowledge, so either is executable; the choice is yours because it trades a record convention against a live failure mode. **ANNOTATED add-only 2026-08-15 (#79), ROW NOT CLOSED — the Fable audit recommends ANNOTATE (audit doc §6)**, covering the capture's §2 normalizer **and its §3 Insurance observation**, with the original text preserved beneath the banner and proposed banner wording drafted in that §6. Its reasoning: the corrected-in-place branch already governs non-log documents, and the silent-normalizer failure mode is live in this project. **A recommendation, not a ruling.** *(Note from #78: that capture is also where the agent-sourced Insurance observation lives — the one whose over-broad closure the #78 correction fixed.)*
- ⬜ **Q-STAT-4 — TRCP 193 comment 6 as a registry candidate.** The comment states that "the party seeking to avoid discovery has the burden of proving the objection or privilege." Registry entries 14, 20 and 21 currently rest that burden proposition on case law alone. **Do you want the comment entered as its own proposition, or added as a second observation to the existing entries under the one-proposition-one-home rule?** Raised because a rule comment is a different kind of authority from a case and the registry has no precedent for one.
- ⬜ **Q-STAT-5 — How far should the Insurance line go before anything is ruled?** The pass's §7 offers twenty candidates (C-1…C-20, chs. 541–542). **Do you want them ruled in as registry entries now (which starts a fifth registry file, since none of the four covers insurance), held as design input until the UIM/UDJA module is actually scoped, or narrowed to the handful that CD-3 and CL-3 actually touch?** The two deadline candidates — **C-19's 61-day pre-suit notice** and **C-20's two-year limitations, extendable 180 days** — are the ones with a build consequence independent of the registry. **ANNOTATED 2026-08-15 (#78), NOT ANSWERED — it is now also a PLACEMENT question.** The premise that "none of the four covers insurance" was **over-broad**: `legal-rule-registry-draft-entries-medical-billing.md` **ENTRY 8 already occupies Insurance Code ground** (SB 1264 / ch. 1467 balance billing, `[EXT]`, low priority). **Different subject** — third-party provider billing, not first-party prompt payment or unfair settlement practices, **so no candidate is withdrawn and chs. 541/542 really are absent from all four files** — but ruling C-1…C-20 in now raises a placement and one-proposition-one-home question that did not exist when this row was written. **REACHED AGAIN 2026-08-15 (#81), still not answered: `Q-FE5-7` puts twelve TRCP 190/197 propositions to the same placement question** — the FE-5 spec says in terms that if they enter, the fifth-registry-file question is reached again. **Two research passes now stack behind this one ruling.** **REACHED A THIRD TIME 2026-08-15 (#87), still not answered: `Q-QBO-6` puts a Texas client-property / trust-accounting layer to the same placement question** — absent from all four files on a re-verified sweep, and the QBO memo names this row's fifth file as one of the three possible homes. **Nothing was drafted; the backlog stays 34.**
- ⬜ **Q-STAT-6 — The divergence findings and what happens to them.** The pass flags divergences in **entries 1, 2, 4, 5, 6, 7, 8, 9, 11, 12, 13, 23, 26, 27, 29, 32, 33, 34** — eighteen of the twenty-one. Some are omissions that narrow a proposition safely; **at least four change what the entry means** (5's reversed waiver limb, 6's non-existent third response, 11's "may" for "shall unless", 27's mis-attributed 47(d) half). **Correcting the wording is your act.** **RULED 2026-08-14: route (c)** — replacement wording drafted for the four material entries only (staged file §9), the other fourteen left as flags. **What stays open is the fourteen: they are resolved in your hand at verification, entry by entry.** **Nothing is adopted; verification attaches to wording.**
- ⬜ **Delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`** — left behind when the HS chapter had to be unzipped on the device (`device_stage_files` timed out four times on the 56 MB zip). **A session cannot remove it: `device_bash` has no `rm`.** Michael's hand. Entered 2026-08-14 (#76).

*(**The deadline-engine memo's five questions — ADDED 2026-08-14, thirtieth queue-runner invocation, with NO durable IDs**, from `docs/specs/deadline-engine-service-and-response-2026-08-14.md`, PROPOSED design input, research support only, **nothing verified**. The packet asked for durable IDs after a collision check **and** forbade minting a series; the check found no series that can take them without one or the other — **DL** holds only `DL-INPUT` and the packet's §6 names minting `DL-1` as an unruled act, **V-10…V-14 would collide** with the unhyphenated residuals V10–V17 already live in §§1–2 of this file, and **DE** is the deficiency series (DE-1, DE-2), a different subject. So they keep their packet-local numbers, namespaced below as `[DL-memo Q1]`…`[Q5]`, with full question text per QR-1. **Precedent for filing without durable IDs: #59, the Uvalde capture, later reconciled into the ruled CR series.** **`trcp-deadline-skeleton-2026-03-01.md` was NOT edited** — the 50-day contradiction is left visible on purpose, because resolving it is Michael's verification act.)*

- ⬜ **ID-DL-1 — What series do the five questions below belong to?** The runner declined to choose: minting `DL-1` is forbidden by the packet, `V-1x` collides, and `DE` means deficiency. **Do you want a numbered `DL` series ruled into existence (making `DL-INPUT` its worded first member), do these fold into an existing series, or do they stay packet-local until the deadline layer is scoped?** Entered 2026-08-14 (#75). **Series creation is a ruling, not a formatting choice** — that sentence is the packet's, and it is why this row exists rather than a silent assignment. Cheap to close; two words do it. **SHARPER 2026-08-15 (#80): it now governs TWO packets.** The Task 7 Bexar memo arrived with its own **Q1–Q6 plus eleven `LR-`/`EF-` propositions**, and its §6 routes the series decision here rather than minting — so this row decides the naming for both memos at once. **The `LR-<n>`, `EF-<n>` and `BX-<n>` namespaces were checked repo-wide and are free**, if you want any of them. **SHARPER AGAIN 2026-08-15 (#81): it now governs THREE packets.** The Task 8 form-engine specs arrived with **twenty-two questions plus three named looks** under `Q-FE4-`, `Q-FE5-`, `Q-FE6-` and `FE5-LOOK-` — **all four namespaces re-checked repo-wide and FREE, and again deliberately not minted**, because `FE-` is a live durable series and minting into it is your act. **Three packets' worth of questions now depend on this one ruling**, and the newest set is the largest. **SHARPER A THIRD TIME 2026-08-15 (#83): it now governs FOUR packets.** The Task 9 intake specs arrived with **eighteen questions** under `Q-IN1-` and `Q-IN3-` — **both namespaces re-checked repo-wide at HEAD and FREE, and again deliberately not minted**, because `IN-` is a live durable series and minting into it is your act. **Four packets, and this one carries a second edge:** Q-IN1-8 asks whether the `IN` series stands at all (the elicitation caveat's one-word veto), so a decision to rename `IN` would land on the same namespace this row is deciding how to join. **SHARPER A FOURTH TIME 2026-08-15 (#84): it now governs FIVE packets** (CHAT-DISPATCH Tasks 6, 7, 8, 9, 10). The Task 10 IN-2 spec arrived with **nine questions** under `Q-IN2-` — **that namespace and `IN2-LOOK-` re-checked repo-wide at HEAD and FREE, and again deliberately not minted**, for the same reason. **IN-2 is now the third spec making the `IN` IDs load-bearing**, which sharpens Q-IN1-8's veto rather than this row's naming question: the longer the packet-local IDs accumulate, the more of them a rename would have to re-point. **SHARPER A FIFTH TIME 2026-08-15 (#85): it now governs SIX packets** (CHAT-DISPATCH Tasks 6, 7, 8, 9, 10, 11). The Task 11 WF spec arrived with **ten questions and two named looks** under `Q-WF-` and `WF-LOOK-` — **both namespaces re-checked repo-wide at HEAD and FREE, and again deliberately not minted**, because `WF` is a live durable series and minting into it is your act. **This packet adds a second naming question rather than only more dependents:** **Q-WF-2** asks whether the `T` series gets the H → HK treatment for a genuine collision, which is the same *class* of act as this row but a different series — so the record now holds **two open naming rulings at once**, one about where packet-local questions land and one about a collision that gates five WF rows either open or shut. **SHARPER A SIXTH TIME 2026-08-15 (#86): it now governs SEVEN packets** (CHAT-DISPATCH Tasks 6, 7, 8, 9, 10, 11, 12). The Task 12 PR-3 proposal arrived with **seven questions and one named look** under `Q-PR3-` and `PR3-LOOK-` — **both namespaces re-checked repo-wide at HEAD and FREE, and again deliberately not minted**, because `PR` is a live durable series (PR-1, PR-2, PR-3) and minting into it is your act. **This packet's edge is proximity rather than volume:** its questions are not about a new subject but about **PR-3 itself**, so `Q-PR3-2`, `Q-PR3-3` and `Q-PR3-4` sit one row away from the durable ID they cannot use — and if this row rules that packet-local sets fold into their nearest durable series, this is the set where that produces the least friction and the most immediate renumbering. **Seven packets, and the cost of the ruling has not changed: two words.** **SHARPER A SEVENTH TIME 2026-08-15 (#87): it now governs EIGHT packets** (CHAT-DISPATCH Tasks 6, 7, 8, 9, 10, 11, 12, 13). The Task 13 QBO memo arrived with **eight questions and four named looks** under `Q-QBO-` and `QBO-LOOK-` — **both namespaces checked repo-wide at HEAD, both returned ZERO, and again deliberately not minted.** **This packet's edge is that its questions have no durable series to fold into at all:** there is no `QBO` series, no money-module series, and the module they belong to is unruled by a standing BUILD-STATE line (*"NO MONEY MACHINERY"*) — so unlike the PR-3 set, folding-into-the-nearest-durable-series has no nearest. **Eight packets; the cost of the ruling is unchanged.**
- ⬜ **[DL-memo Q1] — The 50-day discovery-response extension is FAMILY-CODE-ONLY on the July 2026 text, and the skeleton has it backwards.** Four provisions carry the Family Code limitation (196.2(a), 196.7(c)(1), 197.2(a), 198.2(a)), and the **unqualified** 50-day rule appears exactly once, at **194a.3(a)**, inside the Family-Code-only rule where it needs no qualifier. `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite relationship — 30/50 as the general structure with 194a as the Family Code parallel. **Do you verify that reading, and do you want the three skeleton rows edited to match? This is a live practice question before it is a design question: on this reading a PI or civil-litigation defendant served with discovery before its answer is due has a FLAT 30 DAYS**, and Rule 198.2(c) makes the consequence automatic — *"If a response is not timely served, the request is considered admitted without the necessity of a court order."* **OPEN — practice-relevant now.** **No verified registry entry is contradicted** (the three verified entries state the 30-day period and carry no 50-day proposition), so **no correction entry is owed**; what conflicts is an UNVERIFIED skeleton candidate.
- ⬜ **[DL-memo Q2] — Rule 99(c)'s citation notice and Rule 194.2(a) give different dates to every defendant but the first.** The notice tells the defendant that disclosures *"generally must be made no later than 30 days after **you file your answer**,"* while 194.2(a) sets the deadline at 30 days after **the first** answer or general appearance. **Does this change your answer to V2 (the staggered-answer anchor gap), and should the engine surface the discrepancy to the defendant-side user rather than silently applying 194.2(a)?** Entered 2026-08-14. **Evidence for V2, not a resolution of it** — cross-links **V2** in §2 above.
- ⬜ **[DL-memo Q3] — No registry entry for TRCP 21a exists anywhere in the repo** — no proposition, no ruling, no queue item — yet its added-days provision is per-party **and** per-method and therefore load-bearing for every response clock. **Do you want a 21a entry opened, and at what scope: the added-days provision alone (21a(c)), or 21a(b)'s completion rules as well, given that fax carries a time-of-day rule (after 5:00 p.m. recipient-local rolls to the next day) and commercial delivery is deposit-complete but uncushioned?** Entered 2026-08-14. **A genuine gap, not a duplicate.** Only mail moves a deadline: 21a(c) adds three days for mail and names no other method; electronic service gets none.
- ⬜ **[DL-memo Q4] — Rule 195.2's "later of … 30 days after the request is served" floor is GONE from the July 2026 text.** The rule now states expert designation as fixed dates — 90 days (party seeking affirmative relief) and 60 days (all others) before the end of the discovery period. The 2021 text carried the floor. **Do you verify that the floor is gone? A designation function built on the 2021 formulation would compute a LATER date than the current rule allows.** Entered 2026-08-14. **Cross-links the TRCP 195.2(a)/(b) sign-off row already on this queue.**
- ⬜ **[DL-memo Q5] — Rule 190.4(b)(2) now permits a Level 3 plan to set a period during which *"either all discovery must be conducted or all discovery requests must be sent, for the entire case or an appropriate phase of it."*** Neither the send-by alternative nor phase scoping appears in the 1998 text this project had been working from. That is a different computation **shape**, not merely a different date. **Should the engine model a Level 3 period as a typed choice between complete-by and send-by, with phase scoping, or should Level 3 remain wholly document-derived with no structured representation?** Entered 2026-08-14.

- ⬜ **HK-7 — NEW 2026-08-13 (#72). Connect the repo checkout via the device bridge for the rest of
  the CHAT-DISPATCH chain.** Requested in the 2026-08-13 design session; **did not land** (no
  folder was connected), so that session's two deliverables are sourced to RAG rather than to HEAD
  and say so throughout. **The question: will you connect the checkout for the remaining chain?**
  Tasks 5 (Supabase permissions matrix), 17 (session-log TOC), and 18 (go-live runbook) each need
  full-file reads that RAG cannot reliably supply. **Michael's hand — Code cannot connect it.**
  *(ID assigned by the runner: the packet carried this as `HK-*` with no number. HK-1..HK-6 are
  taken by the re-lettered T3/Phase 0 advisory series (#66), so this is **HK-7**, the next free
  number. Collision-checked against this file. **K-6/K-7 remain RETIRED and are not reused** — the
  K and HK series are distinct.)*
  **SATISFIED FOR ONE SESSION, NOT CLOSED — 2026-08-13 (#74).** Michael granted the checkout folder
  for the CD-2 mining pass, and all 96 synced files were read full-text at `aa033dc` — the first
  packet in the chain whose sourcing is not qualified by "RAG absence is not evidence of absence."
  **The row stays open on purpose:** the grant is session-scoped and the dispatch's own resume rule
  says re-grant at each resume session, so a later session inherits the item, not the access. The
  packet's §6 says in terms: do not mark HK-7 closed. **Two bridge findings that bear on the queue
  runner, both new and both recorded here because they change how QR-3 must be run:** (a) through
  the bridge mount `git status` reports 199 modified files (37,271 insertions against 37,271
  deletions) while `git diff --ignore-cr-at-eol` is EMPTY — pure CRLF, zero content difference — so
  **a QR-3 Step 0 gate run through `device_bash` reads DIRTY and stops the session on a false
  alarm**; #68 recorded the CRLF caveat as not materializing, but that runner ran natively on
  Windows, and through the bridge it materializes every time; (b) **`git fetch` fails through the
  bridge (`HTTP 403 from proxy`)** — the device VM has no network, so **QR-3's fetch-and-compare
  cannot run through the bridge at all and must stay native.** Local refs reading `HEAD ==
  origin/master`, 0/0, is an UNFETCHED ref and is not a QR-3 pass.
  **SATISFIED FOR ONE SESSION AGAIN, AND AGAIN NOT CLOSED — 2026-08-14 (#75, #76).** Both grants
  were live for the statute pass (`Knowledge Repo\Statutes 26-08-14\` and `Knowledge Repo\Civil\`),
  and the deadline memo's rule text came from the same `Civil\` PDF — **the first project work whose
  rule quotations are sourced to clean consolidated authority rather than to redlines or RAG.**
  **The row stays open for the same reason as last time: the grant is session-scoped**, and Tasks 5,
  17 and 18 of the CHAT-DISPATCH chain still need it re-granted. **New operational finding, recorded
  because it will recur:** `device_stage_files` times out on the larger corpus zips — the 51 MB
  Insurance zip landed on retry 2 and the **56 MB Health & Safety zip failed four times and never
  landed**; the workaround that worked is `device_bash` unzip of a single chapter on the device, then
  stage the small PDF. That workaround is what left `_claude_extract\` behind for Michael to delete.
  **SATISFIED FOR ONE SESSION A THIRD TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#80).** Both the repo
  checkout and `Documents\Knowledge Repo` were granted at the Task 7 session's start, which is what put
  the TRCP and statewide criminal e-filing PDFs in TIER A rather than behind a fetch layer. **The row
  stays open for the standing reason: the grant is session-scoped**, and the remaining chain tasks that
  need full-file reads — 17 and 18 among them — need it re-granted each time.
  **SATISFIED FOR ONE SESSION A FOURTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#81).** The repo
  checkout and `Documents\Knowledge Repo` were both granted at the Task 8 session's start, which is
  what put `Civil\texas-rules-of-civil-procedure July 2026.pdf` in **TIER A** and made the FE-5
  spec's headline finding possible at all — the divergence was found by reading the operative rule
  text against a registry entry, which RAG could not have supported. **The row stays open for the
  standing reason: the grant is session-scoped.** Tasks 9–19 remain; 17 and 18 still need it.
  **SATISFIED FOR ONE SESSION A FIFTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#83).** The repo
  checkout was granted at the Task 9 session's start, which is why every repo fact in both intake
  specs is read **at HEAD** rather than from RAG. **The row stays open for the standing reason: the
  grant is session-scoped**; Tasks 10–19 remain and 17 and 18 still need it. **A THIRD bridge finding,
  new and operational — the bridge is the probable source of the stale `.git/index.lock` the
  thirty-seventh invocation found.** Every git command run through the bridge emits `warning: unable
  to unlink '.git/index.lock': Operation not permitted`, and the Task 9 session observed a 0-byte
  `.git/index.lock` in the checkout stamped to the minute a read-only `git status --porcelain` ran;
  `device_bash` cannot delete it because the mount denies `unlink`. **Stated as a hypothesis with its
  evidence, not a settled cause** — that session did not test it by other means. *(Runner
  corroboration, thirty-eighth invocation: the lock WAS present natively at session start — 0 bytes,
  no git process running — and native `unlink` removed it cleanly. **Second independent occurrence
  after a bridge session, which strengthens the hypothesis without proving it.** Countervailing datum
  from the same check: the CRLF false-DIRTY did **not** reproduce natively — `git status --porcelain`
  returned zero lines — which is consistent with finding (a) above being bridge-only.)*
  **SATISFIED FOR ONE SESSION A SIXTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#84).** The repo
  checkout was granted at the Task 10 session's start, and it is also what let that session read the
  **CR-3 code-sheet binary** in `docs/reference/` by text extraction — which is where both of its
  corrections came from, and neither was reachable from RAG. **The row stays open for the standing
  reason: the grant is session-scoped**; Tasks 11–19 remain and 17 and 18 still need it. *(Lock
  hypothesis, THIRD independent occurrence — thirty-ninth invocation: a 0-byte `.git/index.lock`
  was again present natively at session start, stamped to the minute the packet landed, with no git
  process running; native `unlink` cleared it. Three-for-three after bridge sessions. The CRLF
  false-DIRTY again did **not** reproduce natively.)*
  **SATISFIED FOR ONE SESSION A SEVENTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#85).** The repo
  checkout was granted at the Task 11 session's start, which is why the WF spec's auth-surface
  finding rests on `docs/outlook-setup.md` read at HEAD rather than on RAG. **The row stays open for
  the standing reason: the grant is session-scoped**; Tasks 12–19 remain and 17 and 18 still need it.
  *(**Lock hypothesis — THE STREAK BREAKS, and it is recorded because a confirming-only record would
  be worth less.** The packet's §1 predicted a stale lock and told the runner to clear it natively;
  the fortieth invocation found **NO `.git/index.lock` at all** at session start, after a bridge
  session of the same shape as the three that produced one. The count is **three-for-four**, so
  whatever produces the lock is **not** produced by every bridge session — the hypothesis survives as
  a tendency, not a rule. The CRLF false-DIRTY again did **not** reproduce natively: `git status
  --porcelain` returned **zero** lines.)*
  **SATISFIED FOR ONE SESSION AN EIGHTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#86).** The repo
  checkout was granted at the Task 12 session's start, and it is the whole reason that packet has
  anything to say: **every Class-A/B/C finding in the PR-3 proposal comes from reading
  `src/domain/{caseTypes,types,roster}.ts` full-text at HEAD** — including the `sideSetFor()`
  fall-through, which RAG could not have surfaced. **That same read is why `Q-PR3-1` exists**: the
  session raised, against itself, whether a design-side `src/` read is sanctioned at all. **The row
  stays open for the standing reason: the grant is session-scoped**; Tasks 13–19 remain and 17 and
  18 still need it. *(**Lock hypothesis — ABSENT a second consecutive time, so the count is
  three-for-five.** The forty-first invocation found **no `.git/index.lock`**; the check was run
  mid-session rather than at session start, but the session-start `git status` completed cleanly
  with no lock complaint. **The tendency is now weaker than a coin flip's worth of confirmation and
  should stop being predicted in packets as though it were reliable.** The CRLF false-DIRTY again
  did **not** reproduce natively — `git status --porcelain` returned only this batch's own three
  files — while the **design side reported 200 EOL-only modified files through the bridge**, a
  fourth bridge session showing the split. Bridge-only remains the best reading.)*
  **SATISFIED FOR ONE SESSION A NINTH TIME, AND AGAIN NOT CLOSED — 2026-08-15 (#87).** The repo
  checkout was granted at the Task 13 session's start and every repo fact in the QBO memo — the
  auth-surface lines, the schema sweep, the registry-absence sweep — is read **at HEAD** rather than
  from RAG. **What this session shows is the limit of a repo-only grant:** `QBO-LOOK-4` was **NOT
  RUN** because `Documents\Knowledge Repo` was not granted alongside the checkout, so no Texas
  client-property or trust-accounting authority could be retrieved. **The row stays open for the
  standing reason: the grant is session-scoped**; Tasks 14–19 remain, 17 and 18 still need it, and
  **Task 14's research is registry-style and will hit the same wall unless BOTH folders are granted.**
  *(Lock hypothesis — **ABSENT a third consecutive time; the count is three-for-six.** The
  forty-second invocation found no `.git/index.lock` and the session-start `git status`/`git fetch`
  both completed cleanly. The CRLF false-DIRTY again did **not** reproduce natively — `git status
  --porcelain` returned only this batch's own files. **The lock should no longer be predicted in
  packets at all.**)*
- ⬜ **KICK-1 — NEW 2026-08-09. LOCATE `KICKOFF-phase0-t3-p1-session_2026-08-08.md`.** Absent from
  the P1 inbox; **never tracked by git** (`git log --all -- 'inbox/*'` returns zero commits, and
  `inbox/` is gitignored), so **the loss cannot be dated or recovered from history**. #42 recorded
  the Phase 0/T3 authorization as **PARTLY SPENT**, and **this document is the authoritative text of
  that authorization and its stage definitions.** **Until it is located — or Michael re-issues the
  remaining authorization in his own words — further T3 work is UNAUTHORIZED:** §5 discipline
  requires quoting Michael's explicit authorization, and the quoted source is missing. Likely
  locations: Michael's Downloads on either machine; the 2026-08-08 design chats; partial
  reconstruction from `docs/specs/rulings-capture-2026-08-08*.md` and #42's log entry. **It is not a
  packet and must never be processed as one.** **Michael's.** *(ID is new and collision-checked —
  **not** a K-series number; **K-6/K-7 are RETIRED and must not be reused.**)*
- ✅ **K-5** — Uvalde docket worksheet instructions: home undecided. **CLOSED 2026-08-11 (RULED): home is the CRIM DEFENSE practice project's knowledge, uploaded by Michael's hand — never the repo. Ruled in the same session that created the CRIM DEFENSE practice project (all criminal-side practice work; REQ-1 extended; instructions v15). See #54.**
- ✅ **GH-1 — The two real prior-case identifying strings generalized out of `form-engine.md` §8 by the FE-3 ruling (#55) survive in git history: prior commits of the file still contain them. Rewrite history to purge them, or accept?** **RULED 2026-08-12: ACCEPTED — history stands.** Reason: a rewrite changes every commit sha (invalidating the shas the session log cites as record), forces re-clones on both machines and a design-side re-sync, with MM-1 coordination throughout; the repo is private and the strings are of modest sensitivity, absent from the working tree since `cfa1692`. **TRIPWIRE, standing: REVISIT before repo visibility widens** — multi-user phase (gate 2 / instructions trigger 2), any outside collaborator, or the repo going non-private. Pointer at gate 2 per same-day ruling. See #56; dialogue trail in the project-knowledge capture. *(New ID 2026-08-12; collision-checked at reconciliation per the K-6/K-7 rule — no GH series existed anywhere in this file or the session log.)*
- ✅ **QR-1** — QUEUE-RUNNER merges packet open items down to ID + label and then deletes the packet, **losing the question itself** (this destroyed Q-5's original wording). **CLOSED 2026-08-07: ruled yes.** The runner carries FULL question text into this file when merging packet open items; ID+label-only merges are barred. `QUEUE-RUNNER.md` and `.claude/commands/queue-runner.md` went to **v3** in the same batch. Fires instructions trigger #3 → v6 drafted.
- ✅ **QR-2 — PROPOSED, unruled (2026-08-07). Should the machine-local queue-runner skill/command copies stop carrying the runner's full text and become POINTERS — "read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it" — so that a stale local copy is structurally impossible?** Evidence: the machine-local copies have now loaded **stale twice in two consecutive runs** (#35 loaded v1 against repo v2; #36 loaded v1 against repo v3), and **both times only the runner's own vigilance caught it.** The repo copy is already canonical; this makes the invocation defer to it. **Cost:** one extra file read per invocation. **Rejected alternative, and the reason it is rejected:** keeping full text locally and re-syncing by hand IS the observed failure mode. **Scope note (verified from Code 2026-08-07):** `.claude/commands/queue-runner.md` is **tracked and current at v3** — it updates with the repo and is not the problem; the lag lives in the **untracked machine-local skill copies**, of which there are now at least two (P15, P1). **Michael's.** **CLOSED 2026-08-08: ruled YES** ("QR-2 - yes"). Copies become pointers — *"read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it"* — and that file is now the ONLY full copy. Decisive evidence: #38 was the **third consecutive stale-skill run** (local v1 against repo v3) and cost nothing only because the packet and the design reply independently supplied both deltas — luck standing in for process. `.claude/commands/queue-runner.md` was rewritten to pointer form the same day; **the P15/P1 machine-local copies are Michael's hand.** Fires instructions trigger #3 → v7 drafted. Full reasoning: `rulings-capture-2026-08-08b.md` Part 1.
- ✅ **QR-3 — the runner's pointer reads `docs/prompts/QUEUE-RUNNER.md` at HEAD, but HEAD is whatever the local checkout happens to be; a stale tree serves stale runner text with no signal (demonstrated 2026-08-08: v1 read as current, 14 commits behind). Should Step 0 gain a gate — fetch origin and confirm the checkout is at origin HEAD before reading the runner; if behind, sync or stop?** CLOSED 2026-08-08: ruled yes, sync-and-proceed shape — behind-but-clean-on-master fast-forwards and continues; dirty/diverged/off-master stops. Runner to v4. See `rulings-capture-2026-08-08c.md`. **AMENDED TO v7, 2026-08-13 (#67): the gate ALSO STOPS WHEN HEAD IS AHEAD of `origin/master`** (unpushed local commits) — tell Michael, proceed only on his word, never build on an unpushed tree silently. **Why it was needed:** the twenty-fifth invocation found that a gate stopping only on behind / dirty / diverged / off-master lets *ahead* sail through — so the one state that means "the design side has not seen these commits" was the one state with no guard, and the packet running at the time had explicitly relied on the gate to enforce push-ordering. Runner text amended the same batch; the clause was also folded into the unpasted v17. *(No standalone row ever existed for the gap — it was flagged in BUILD-STATE and the session log by the twenty-fifth invocation and is annotated here rather than given a row that would exist only to be closed: the #57/#60 disposition.)*
- ✅ **QR-4 — PROPOSED, unruled (2026-08-10). Step 1 of QUEUE-RUNNER.md orders packets by file modification time, but inbox mtimes are SAVE times, not session times, and can invert authoring order — demonstrated 2026-08-09/10, when the later-authored t3-kickoff packet (filename 2026-08-10) carried an EARLIER mtime than the earlier-authored slack-claude-tag packet (filename 2026-08-09), because it was saved into `inbox/` first. Order is semantic, not cosmetic: the conflict rule ("later packet wins") and the session-log append order both turn on it. Should Step 1 be amended to: sort by filename date, oldest first, with mtime as tiebreak and as fallback for undated filenames (flagged explicitly); ALWAYS compute both orders and print both whenever they disagree — never silently pick one; and, after unzipping, cross-check the confirmed order against each manifest's §3 entry date, stopping on contradiction? The Michael-confirm gate stays exactly as is — this changes the inferred order and makes disagreement visible; it does not remove the stop. Cost: a few lines of runner text. Rejected alternative — keep mtime primary: rejected because the 2026-08-10 inversion was caught only by the running session noticing the disagreement unprompted; the gate showed one inferred order and nothing forced the conflict into view. If ruled yes: runner v5 → v6; fires instructions trigger #3 → v10 draft. Michael's.** **CLOSED 2026-08-10: RULED ADOPTED** as a standing amendment to Step 1 — filename date primary (oldest first), mtime as tiebreak and fallback for undated filenames, both orders computed and printed whenever they disagree, and the confirmed order cross-checked against each manifest's §3 date. Ruled during the NINTH queue invocation, where the same mtime/filename-date inversion recurred — the second consecutive batch requiring a hand ordering call. `docs/prompts/QUEUE-RUNNER.md` went to **v6** the same day; per QR-2 the tracked command file is a pointer and carries no version text, so it was not edited (see the runner entry's flag). Fires instructions trigger #3 → v10 drafted design-side 2026-08-10. Reasoning: `rulings-capture-2026-08-10.md` §1.
- ✅ **MM-1 — with both machines (P15 + P1 Gen 8) in concurrent two-workspace use, what does concurrency itself require beyond the standing cross-machine rules?** CLOSED 2026-08-08, ruled yes to all four parts: one-runner-at-a-time with the non-FF-stop backstop; inbox-bound destinations name the machine; user-level runner copies deleted (not converted — shadowing risk); bootstrap addendum (git identity + deletion step). Runner to v5. See `rulings-capture-2026-08-08d.md`.
- ⬜ **Interim mitigation, regardless of how QR-2 is ruled — Michael's hand, outside the repo:** re-sync the P15's local skill copy to v3, and **check the P1's copy before its first runner session** (same lag class, third copy). **SUPERSEDED 2026-08-08 by QR-2's ruling: CONVERT each machine-local copy to pointer form, do not re-sync full text anywhere.** **SHARPENED AGAIN 2026-08-08 by MM-1(3) — the operative instruction is now DELETE, not convert:** delete the user-level `%USERPROFILE%\.claude\commands\queue-runner.md` on BOTH machines — a user-level copy can *shadow* the repo pointer and feed stale runner text, and the repo-tracked pointer travels with every clone, so it suffices. QR-3's checkout gate travels to each machine automatically once that machine relies on the repo pointer. **NARROWED 2026-08-09 (verified from the P1, log #42, re-confirmed by the 08-10 packet): the P1 half needs NO ACTION — that machine has no user-level copy. ONLY THE P15 DELETION REMAINS.** Still Michael's hand, still outstanding.
- ⬜ **Q-3 RE-CHECK — Michael's hand, one click, after the 2026-08-10 push: does the GitHub sync picker include `docs/skills/`?** That directory is NEW as of 2026-08-10 (it holds `drafting-disclosures/SKILL.md`, landed this batch). Per Q-3, the project-knowledge selective sync may not auto-include newly created nested directories, even though the sync scope nominally carries all of `docs/`. **If it is excluded, the skill never reaches the design side** — and the design side is exactly where the skill runs, since disclosure drafting happens in design-space chats until the form engine exists as software. Verifiable by Michael only: Code cannot see the sync picker. *(Entered 2026-08-10 with full text per QR-1 — it had never been registered here, only in the 08-06 packet's §7 table.)* **EVIDENCE 2026-08-10, and it points toward CLOSABLE:** the drafting-disclosures skill was read and executed from the HEAD copy in a design session that same day — meaning `docs/skills/` reached the design side. That is evidence, not a ruling; **Michael closes it.** **PAIRED RE-CHECK ADDED 2026-08-11: `docs/templates/`** is new this batch (holds `discovery/template_definitions-instructions_requests.md`) and needs the same one-click confirmation in the sync picker; if the picker excludes newly created nested directories, the house templates never reach the design side either.
- ⬜ **SK-v2 — the `drafting-disclosures` skill's v2 revision.** Per the ruled upgrade protocol the spec fold-in comes FIRST (landed 2026-08-11 at `form-engine.md` §§12.7–12.13); **a design session then revises `docs/skills/drafting-disclosures/SKILL.md` to v2 citing the spec at HEAD, with a version bump.** Sequenced, not blocked — needs a design session. *(Code must not edit the skill; the 08-10 packet's §6 bars it.)* **v2 AUTHORED AND LANDED 2026-08-11** — the revision is a design-session deliverable per the ruled upgrade protocol (spec fold-in first, at `form-engine.md` §§12.7–12.13; then the design-side revision citing the spec at HEAD `13f736a`), so the queue runner landing it is routing, not editing, and the Code-must-not-edit bar is intact. v2 folds attribution verification + the imaging-entity question (§12.7), operator-local service dates (§12.8), style-by-role assertion with the step-1 style harvest (§12.9), caption label style (§12.10), block keep-together in the ship gate (§12.11), served-final-as-next-shell (§12.12); the provider-data block's purpose line now names the contact directory as its consumer (FE-1 scout → CD-1 seed evidence). **REMAINING, and why this entry stays open — Michael's hand, now actionable:** **upload `docs/skills/drafting-disclosures/SKILL.md` (v2) as the claude.ai skill copy now that the revision is at HEAD.** *(This absorbs the standalone "Hand-upload of `drafting-disclosures` as a claude.ai skill" item carried from #45 — still open, a `.skill` packaging was offered in-session and not acted on — consolidated here per QR-1's one-home rule; text moved, not deleted.)*
- ✅ **v14 INSTRUCTIONS PASTE — Michael's hand, same day per instructions trigger #3: paste project instructions v14 into the project's instructions field.** v14 carries **DT-1**, the date-stamp convention ruled 2026-08-11 Central and registered in log #50: *every date a design-side session stamps — session dates, ruling and verification dates, "Entered" lines, filename dates on packets and captures — uses Michael's wall-clock Central date, never the container's UTC date. The container rolls to the next date at 19:00 Central (daylight) / 18:00 (standard); sessions running past that hour check before stamping.* The draft was handed to Michael the evening of 2026-08-11. **Until the paste happens the convention is recorded but not in force for future design sessions** — the log entry binds nothing on its own. *(Entered 2026-08-11 with full text per QR-1. Code verifies nothing about the paste; instructions are Michael's hand entirely.)* Context: DT-1 removes the cause that QR-4 handles as a symptom — the same container-UTC failure class produced QR-4's filename/mtime inversions and #47's certificate-of-service date defect. **CLOSED: paste VERIFIED design-side 2026-08-11** — the CD-1 schema session read the live instructions field at v14 at session start and recorded it in its entry (log #51); **DT-1 is in force.** First in-force application caught its own target failure class live in that session: chat confirmations stamped 08-12 from container UTC until the pre-authoring clock check (2026-08-11 21:38 CDT); no artifact was mis-stamped. *(Code verifies nothing about the paste itself — this closure rests on the design-side session's own verification, cited per this entry.)*
- ⬜ **v17 INSTRUCTIONS PASTE — NEW 2026-08-13 (#66). Michael's hand, same day per instructions trigger #3: paste project instructions v17 into the project's instructions field.** The file was **delivered to Michael, deliberately NOT routed to the repo** (project instructions are his, and Code must not edit them). v17 carries: the **Descrybe-out** tooling convention (Descrybe is not used in this project at all until further notice; revisit at go-live); **SAT-1** (satellite-project pattern, both parts); the **§7.2 model-routing clause** folded into MODEL USAGE with economics refreshed to the observed 2026-08-13 Max terms — which is what closes **Q-5** and **HK-2**; the **HK-5** rule (preflight questions about Michael-supplied material are answered by Michael first, never by sweeping his machine unprompted; he may direct a search per instance); the "BS-1 provenance" phrase corrected to the ledger-pointer line; and the pre-Cowork read-only wording updated. **Until the paste happens, these conventions are recorded but not in force for future design sessions** — a log entry binds nothing on its own (the v14 precedent directly below). **The next fresh design session verifies v17 in force** (#54 precedent). *(Code verifies nothing about the paste; instructions are Michael's hand entirely.)* **REVISED SAME DAY, 2026-08-13 (#67) — same version number, new file: the QR-3 v7 ahead-stop clause and the line-count convention were folded in. PASTE THE REVISED FILE delivered 2026-08-13 evening; the earlier same-day download is SUPERSEDED.**
- ✅ **NEXT BUILD SLICE — which slice does Michael name as the next build target?** The build queue has been empty since CL-2, and the question has ridden in session-log §7 tables since #31 ("Next build slice — OPEN, Michael names it") **without ever being registered in this file** — entered here 2026-08-11 with full text per QR-1 when the CD-1 packet's routing looked for it and found nothing. **OPEN BY RULING 2026-08-11: naming is DEFERRED** — Michael ruled that the next slice is named in a later design session **citing `docs/specs/contact-directory.md` at HEAD**, not in the schema session itself. What changed with that spec landing (statuses, not rulings): **the form engine is again nameable** — its identity source is resolved, FE-4–FE-7 unblock for spec completion, and FE-2's parked build home can be raced honestly. Code must not name a slice; nothing enters the build queue without an explicit authorization packet in Michael's words. **Michael's, next design session.** **CLOSED — RULED 2026-08-11: the CD-1 directory build, scoped and authorized; text at `docs/specs/cd1-build-slice.md`.** The deferral's own condition (name the slice in a later session citing `contact-directory.md` at HEAD) was satisfied the same evening, after the thirteenth queue invocation landed the spec at `13f736a` and the design side verified the sync. Reason recorded: substrate first — the form engine consumes the directory and its own spec is incomplete (FE-4–FE-7 are spec-completion work). Anti-resurrection check run deliberately and on the record: **NOT Slice A returning** — Slice A was the provider-level framing, withdrawn 2026-08-08 for baking the wrong level; this builds the ruled level. Michael fires the build session; see log #52.
- ✅ **Queue-wide duplication sweep — does any OTHER item live twice?** Raised 2026-08-08 by the CL2-AC-1 finding (that item lived in two places in this file until the same-day dedup ruling consolidated it). *(Wording carried verbatim from the 08-08b packet's §7 per QR-1 — it had never been entered in this register, only in the packet table.)* **RULED AND RUN 2026-08-13 (#68), by widget, full-text against the checkout at `9ac12c8`: CLOSED — NO DUPLICATE.** No question lives as two full rows anywhere in the register. Three glyph-bearing pointer rows found (PR-3, RE-1, registry 1–10 sign-off) and ruled the same session: glyphs stripped (this batch). Cross-register observation, not queue duplication: the "Itemized-bill ingest" capture row overlaps billing Phase 1b scope — OBS-1, Michael's.
- ✅ **Cross-document status-drift sweep — do any design-doc status rows or tables state a ruling status that lags the queue/log record?** Raised 2026-08-08 by the §10 CL2-AC-1 row found stale (still "PROPOSED, unruled" after the DIRECTION-CONFIRMED ruling). Distinct from the queue-duplication sweep item. **FINDING #1 LOGGED 2026-08-13 (#66): the Go_Live_Gates paste-ask was stale** (see that row, now closed as already-done). **RUN 2026-08-13 (#68), same session and method: CLOSED AS A PASS — findings #2–#9** (seven stale locations across four design docs + the model-routing status header, plus three CLAUDE.md paragraphs; six lag the #66 ruling run, #9 lagged BUILD-STATE since 2026-07-28). All fixed this batch by add-only annotation per ruling. A pass, not a standing guarantee — re-running after future ruling batches is cheap and is Michael's to order.
- ✅ **RR-1** — re-read documents authored earlier in a session against rulings made later in it, before the packet ships. **CLOSED 2026-08-07: ruled yes; now a binding convention** (sending-side mirror of RECONCILE FIRST). Live exhibit: FE-3's shell content, which the rule would have caught. Binds Claude, not Michael. Fires instructions trigger #3 → v6 drafted.
- ✅ **SAT-1 — RULED 2026-08-13 (#66): ADOPTED, BOTH PARTS. Rides instructions v17.** The
  satellite-inventory instructions update fires at the first NON-Domser satellite. The adopted text,
  preserved: Two parts. **(a) The criteria:** a piece gets its own satellite project when it needs a large specialized corpus, confidentiality-segregated material, or a sustained observation/research function; a satellite HOLDS knowledge and produces index/synthesis/captures that travel **by Michael's hand**. **(b) The ONE-RULING-SPACE principle:** satellites never rule; nothing enters the build queue from a satellite; the log, the queue, and packet issuance stay singular in the design project; ordinary module design stays in the design project. Each satellite gets Domser-template instructions at birth.
- ✅ **CORPUS-HOME — PROPOSED, unruled (2026-08-07). Final home of `probate_knowledge_corpus.md`: the DOMSER PROJECT?** This **supersedes the #34 ARCHIVE recommendation**. Rationale offered: Domser is the one place Part III privilege is load-bearing, the corpus is core knowledge there rather than dilution, and licensed material stays inside one privileged workspace. **Until ruled, the ARCHIVE recommendation stands and `Probate Corpus.zip` stays UNTOUCHED in `inbox/` per the #31 ruling** — not deleted, not extracted, not moved, not diffed. **Michael's.** **CLOSED 2026-08-08: the ARCHIVE project.** (Michael: "Yes." — the DOMSER proposal above was not adopted; the #34 ARCHIVE recommendation became the ruling.) Execution Michael's hand — an upload into that project's knowledge, not a repo action; the corpus never enters the repo regardless (licensed James Publishing / LexisNexis; Part III privileged). `Probate Corpus.zip` stays untouched in the P15's gitignored `inbox/` until that upload; deleting it afterward is Michael's step. See `rulings-capture-2026-08-08.md` Part 6.
- ✅ **O1 (transcript sort & route) — RULED 2026-08-13 (#66): D1 CONFIRMED — confirm-only STANDS for v1; auto-file stays deferred.** *(Original question: confirm or veto D1, confirm-only in v1 with auto-file deferred until the decision log shows real precision. It gated nothing — the build is confirm-only either way — and had been waiting on the Phase 0 scorecard as its evidence base. Ruled without waiting for it.)*
- ✅ **O3 (transcript sort & route) — RULED 2026-08-13 (#66): non-case recordings go to a PERSONAL STORE. Nothing is destroyed.** *(Original question: where do NOT-case-related recordings go — a personal store, or discard-with-log? The discard half is rejected.)*
- ⬜ **O4 (transcript sort & route) — phone→PC sync channel at go-live: OneDrive Shortcut, iCloud folder, or stay-manual?** **DEFERRED AGAIN 2026-08-13 (#66) — the channel stays open; carried, still open**; also deferred by D2 (interim capture is manual attach from phone). *(ID note: §2 above uses "O1" for a closed Prop. Code ch. 28 fee question — different series, same letter. Disambiguated here rather than renumbered.)*
- ✅ **O2 (transcript sort & route) — P1 OS at purchase: native Ubuntu vs. Windows+WSL2? CLOSED 2026-08-07 (Michael: "windows only")** — Windows host, WSL2 runtime. Native Ubuntu and dual-boot rejected; NeMo has no native Windows support, so WSL2 with CUDA passthrough is the runtime *inside* the Windows-only ruling. Escape hatch on record: a dedicated headless native-Ubuntu box if volume outgrows the laptop. Full text at `transcript-sort-and-route-design.md` §10 and §12.
- ⬜ **Domser project setup — Michael's hand, outside the repo.** Paste the **v2 governance instructions** into that project's instructions field; move the **Rev. 2 case instructions** into that project's knowledge if they are currently sitting in the instructions field; start a fresh chat after the changes. *(The instructions document is NOT repo material and is not reproduced here.)*
- ⬜ **Tascam DR-05XP + SmartLav+ purchase** — standing recommendation, **not gating** anything. Carried.
- *(**Registry entries 1–10 sign-off** — Michael's, one at a time; detail and status at §5 above. Glyph stripped 2026-08-13 (#68).)*

### K-6 and K-7 — RETIRED 2026-07-28 (Michael's ruling)

BUILD-STATE carried the range "K-5–K-7", but **K-6 and K-7 had no question text anywhere in the repo** — no log entry, no doc, only the IDs. Michael does not remember what they asked. **Both are RETIRED.**

**Nothing was reconstructed** — not by Code, not by the design side, not from plausibility. A fabricated open item is worse than a lost one. Michael's reasoning, recorded because it is a standing rule and not just this disposition: *if either question mattered, the work that raised it will raise it again, and it gets a new ID with its text intact.*

Failure class: the same one that destroyed Q-5's original wording — an ID carried forward without its question (**QR-1**, still PROPOSED and unruled). Actor for the loss: **unknown** — it predates attribution or was lost in the queue. **Do not resurrect these IDs.** See `anti-resurrection-ledger.md`.

### Captured 2026-07-28 — IDs issued by design session as they come

Raised by Michael during the CL-2 live walkthrough. **PROPOSED and unruled — captured, not built.** IDs are issued by a design session, not here; the first was issued 2026-08-07 (**CL2-AC-1**, below). The rest still await one.

- *(**CL2-AC-1** — consolidated 2026-08-08 into the CL2-AC-1 entry under "Client model" above — text moved, not deleted; see `rulings-capture-2026-08-08b.md` Part 2.)*
- ⬜ **Itemized-bill ingest.** Drop an itemized medical bill into a client's bill workspace and auto-extract the line items — description, date of service, revenue code, quantity, unit price, CPT — populating the ledger automatically; Michael renames/labels the bill and the treatment lines fill themselves. His rationale: hand-keying every line of every itemized bill *"would take insanely too much time"* — **a viability issue for the medical spine, not a convenience.** The OAA criminal-intake OCR/extract layer already proves the document-in → structured-rows-out shape; **reuse, don't reinvent.** Michael recalls this being discussed before and a transcript search found no record — **check the ARCHIVE project before designing from scratch.**
- ⬜ **OBS-1 — the "Itemized-bill ingest" capture row overlaps billing Phase 1b's scope (local-AI PDF ingestion of bills, gated on the GPU arm) in `medical-billing-analysis-module-synthesis.md`. Cross-register overlap, not queue duplication (sweep A, 2026-08-13 #68). Fold the capture row into the Phase 1b scope when 1b's design pass runs, annotate it as 1b-adjacent now, or leave both standing?** PROPOSED, unruled. Michael's.
- ⬜ **Party-credibility watch.** A fact about a party (indictment, credibility damage, evidence mishandling) should be able to raise a flag on **every case where that party appears in a testimony-bearing role** — a cousin of the registry watch flags, attached to people instead of rules. Capture only; no design pass, no ID.
- ⬜ **Bill label should default to the provider name**, editable only to disambiguate ("ProCare — initial eval" vs "— follow-up"). **Code answered the blocking question 2026-07-28: the label is NOT load-bearing** — display-only in all five uses, and the report generator already prefers the provider (`providerName ?? bill.label`). Safe to pre-fill; not built, as it sits outside CL-2's six pieces.
