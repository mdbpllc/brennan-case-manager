# ATTORNEY REVIEW QUEUE — everything awaiting Michael's personal read or ruling

**Status:** WORKING CHECKLIST. Compiled 2026-07-26 in the design space, then updated the same day as items closed. **Reconciled to session-log #28 on 2026-07-28 (BS-1a item 10)** — §7 was added in that pass, and `BUILD-STATE.md` now points here instead of carrying its own roster. **Reconciled again to session-log #34 on 2026-08-07** (rulings batch: FE-1, FE-2, PR-3, QR-1, RR-1, D-CL2-3, CL2-AC-1; the FE series was absent from this file entirely and was added). **Reconciled again to session-log #38 on 2026-08-08** (contact-directory reframe: CD series added and CD-1 issued, FE-1 annotated superseded, FE-2 build home parked, CL2-AC-1 to direction-confirmed, CORPUS-HOME closed). **Reconciled again to session-log #39 on 2026-08-08** (QR-2 CLOSED; CL2-AC-1's two locations consolidated into the ID-bearing entry; two sweep items entered). **Reconciled again to session-log #44 on 2026-08-09** (queue-runner batch, eighth invocation: the T3/Phase 0 advisory H series added with full question text per QR-1, its H1–H6 collision with the heartbeat register flagged rather than renumbered; the P15/P1 runner-copy item narrowed to P15 only). **Reconciled again to session-log #46 on 2026-08-10** (queue-runner batch, ninth invocation: QR-4 entered with full text and CLOSED the same day it was proposed; the Q-3 `docs/skills/` sync-picker re-check entered with full text, having never been registered here; FE-1 annotated with the provider-data evidence channel, which now feeds CD-1). **Reconciled again to session-log #48 on 2026-08-11** (first REQ-1 REQ-CAPTURE: CD-2 framing ruled, FE-4–FE-7 and new IN series entered, §4 registry batch VERIFIED, templates home ruled, CD-1 path ruled as bounded-evidence-pass-then-session). **Reconciled again to session-log #58 on 2026-08-12** (queue-runner batch, twentieth invocation: REQ-CAPTUREs 4 and 5 — the **DE series added**, ruled into existence by Michael; CL-3, CD-3, FE-13–FE-17, IN-6, IN-7 entered with full question text per QR-1; FE-10, FE-11, FE-12 amended in place; CD-2 annotated; the FE-13–FE-17 gating question and the registry-verification backlog entered). **Reconciled again to session-log #59 on 2026-08-12** (the Uvalde docket-worksheet capture filed UNRECONCILED at §5 — sixth REQ-1 capture, first from CRIM DEFENSE, no durable IDs assigned and no registry entries opened, because it arrived by Michael's hand rather than through a packet). **Reconciled again to session-log #60 on 2026-08-12** (queue-runner batch, twenty-first invocation: the registry-placement flag CLOSED as confirmed, the REQ-10 placeholder-discipline carry given the queue row it never had, and the registry count reconciled at 27). **Reconciled again to session-log #62–#63 on 2026-08-12** (queue-runner batch, twenty-second invocation, two packets: the Uvalde capture RECONCILED and its row replaced by the new **CR series** CR-1–CR-11 plus the binding B&W constraint; registry backlog 27 → **33**; `party_type` retention CONFIRMED; **FE-gating CLOSED** and FE-8–FE-17 each annotated with its FE-D1 disposition; distillation candidates CLOSED into §13.4; **WF-2–WF-8** entered with gates; three form-engine §11 items registered that had no home; the GPU-telemetry item registered and ruled). **This file is the single register of what awaits Michael's ruling; keep it current or the pointer lies.**

**Canonical repo path:** `docs/specs/attorney-review-queue.md` — filed 2026-07-26. Duplicate-routing check run at filing: no equivalent review-queue document existed; the only prior partial is `trcp-deadline-skeleton-2026-03-01.md` §9 (TRCP entries only), which now points here and remains the TRCP-priority detail.

**Why this exists.** The mining has outrun the ruling. Research has produced propositions faster than they can be verified, and the queue — not the research — is now the project's bottleneck (H22). This document is the whole queue in one place, so the bottleneck is at least visible.

**Convention:** ✅ = closed (2026-07-26 unless a later date is stated) · ⬜ = open. Each open item carries **the actual question in bold**.

---

## 1 — COURT OPINIONS

- ✅ **In re Allstate Indem. Co., 622 S.W.3d 870 (Tex. 2021)** — read 2026-07-26. Docket No. 20-0071 confirmed; two-*Allstate* question closed. §18.001(f) qualification holding; reasonable-notice content standard; reasonable notice ≠ Rule 702/*Robinson* reliability.
  - ⬜ **Residual (V1): are the p.876 and p.882 statements load-bearing or dicta?** Targeted re-read of those two pages. Interacts with Entry 2(j) and H77.
- ✅ **In re Chefs' Produce of Houston, Inc., 667 S.W.3d 297 (Tex. 2023)** — read 2026-07-26. Both holdings confirmed; record as limited-by-*Ortiz*.
- ✅ **In re Sherwin-Williams Co., 668 S.W.3d 368 (Tex. 2023)** — read 2026-07-26. Entry 10(a)'s three-element test confirmed verbatim.
- ✅ **In re Auburn Creek Ltd. P'ship, 655 S.W.3d 837 (Tex. 2022)** — read 2026-07-26. Nexus element has independent content; state it separately.
- ✅ **In re H.E.B. Grocery Co., 492 S.W.3d 300 (Tex. 2016)** — read 2026-07-26. Less-intrusive-means analysis captured.
  - ⬜ **Residual (V3): does Entry 10(c)'s "trend" framing survive?** *H.E.B.* (2016) already compelled an exam on unremarkable facts using the same test, which suggests the line is settled rather than shifting.
- ✅ **Tex. State Univ. v. Tanner, 689 S.W.3d 292 (Tex. 2024)** — read 2026-07-26. No freestanding diligence requirement; relation-back; jurisdictional against governmental defendants.
  - ⬜ **Residual (V6): do you adopt *Tanner* as the H21 cite, and does the draft entry live in the registry file or the TRCP skeleton doc?** Asked and not answered.
- ✅ **Rohrmoos Venture v. UTSW DVA Healthcare (Tex. 2019)** — read 2026-07-26. Four proof elements (not five); contemporaneity ruled a badge.
  - ⬜ **Residual (V7): is *Arthur Andersen & Co. v. Perry Equip. Corp.*, 945 S.W.2d 812, a 1997 case?** Stated as 1977 in session.
- ✅ **Tony Gullo Motors I, L.P. v. Chapa, 212 S.W.3d 299 (Tex. 2006)** — read 2026-07-26. All three propositions confirmed; join table survives.

---

## 2 — STATUTES AND RULES IN CURRENT TEXT

- ✅ **CPRC §18.001(d)–(g)** — official text pulled 2026-07-26. All four deadline formulations recorded, including the (e-1) inversion.
  - ⬜ **Residual (V2): what governs "the date the defendant files an answer" in a multi-defendant case with staggered answers?** Same gap as TRCP 194.2(a).
- ✅ **Tex. Prop. Code Ch. 55** — full chapter pulled 2026-07-26. **EMS threshold = counties of 800,000 or less** (a ceiling). Fatal/amount divide sorted.
  - ⬜ **Residual (V4): are notice defects fatal to the lien?** §55.005(g) saves only non-receipt of a properly mailed notice; it says nothing about failure to send or a defective §55.005(e) EMS form notice. **Statute cannot close this — case law must. This still gates the disbursement checklist.**
  - ⬜ **Residual (V5): does a lien attach where the tortfeasor is a county or ISD?** §55.001(4) excludes them from "person"; §55.002(a) requires negligence of "another person."
- ✅ **H.B. 4145 enrolled bill** — read 2026-07-26. Applicability keyed to **date of service**; per-bill exposure flag survives.
- ✅ **CPRC §38.001 + H.B. 1578** — codified text and enrolled bill read 2026-07-26. 87th Leg. 2021, Ch. 665; applies to actions **commenced** on or after 9/1/2021. Four carve-outs and §38.006 newly surfaced.
  - ⬜ **Residual (V8): is the commenced-date capture still worth building?** Any pre-9/1/2021 action is now ~5 years old, and the date is derivable from the filing date.
- ✅ **Tex. Bus. & Com. Code §17.50(d)** — read 2026-07-26. "Shall be awarded," unqualified; `basis: mandatory` holds. Three date-anchored fee cuts surfaced.
- ✅ **Tex. Prop. Code §53.156** — full chapter read 2026-07-26 (session 2); enrolled S.B. 539 retrieved. **Date corrected: 2011, not 2021.** May→shall was the only change; applies to proceedings commenced on/after 9/1/2011. Bidirectional. Residential carve-out is a one-directional downgrade as to the owner-as-payor.
  - ⬜ **Residual (V10): does "equitable and just" permit a ZERO award despite "shall"?** Michael's working position 2026-07-26: yes, given extenuating circumstances. **Not verified — no case law read.** Needs the citator pass.
  - ⬜ **Residual (V13): is the bidirectional exposure live?** Has a Servpro lien ever been attacked with fees sought against the client?
- ✅ **Tex. Prop. Code ch. 28** — full chapter read 2026-07-26 (session 2). **O1 closed: fees are DISCRETIONARY** (§28.005(b)). Three corrections to the drafted entry; §28.0091 was missing entirely.
  - ⬜ **Residual (V11): 1½%/month — simple or compound?** Statute silent. Affects any annualized figure.
  - ⬜ **Residual (V12): public-entity prompt payment** is outside ch. 28 and outside this project. Gap, not error.

- ✅ **Tex. Est. Code ch. 352** — **full chapter read 2026-07-26.** Both queue questions answered: "on proof satisfactory to the court" **IS** the operative standard (the 2026-07-25 voice note was right), and the two-lane separation survives **more strongly than framed** — the lanes are separate **subchapters** (A compensation/commission; B expenses and attorney's fees), an architectural feature, not a practice caution. **But the profile classification FAILED:** `discretionary-equitable` is wrong on both halves — §352.051 says the representative **"is entitled to"** (entitlement, not court permission), and **"equitable and just" appears nowhere in ch. 352**. Second consecutive session in which a voice-session classification failed against primary text (cf. O1). **§352.052 was missing from our docs entirely** — will-contest allowance, with the defend-vs-contest asymmetry. Corrected classification and full treatment: `statutes-pass-est352-cprc71-2026-07-26.md`.
  - ⬜ **Residual (V14a): what procedural vehicle carries a §352.051(2) fee request?** V14 ruled that fees sit outside §352.053, and §352.053 is the only procedure ch. 352 states — so the chapter is silent on the vehicle, and the vehicle is what the export must be shaped against.
  - ⬜ **Residual (V16, narrowed): does "is entitled to" bind the court**, or is it looser than "shall award"? The schema half dissolved into the O6 decomposition; this legal half stands.
  - ⬜ **Residual (V17): are a §71.004(c)-compelled representative's fees reimbursable under §352.051(2)** where the WD recovery is not the estate's? **Decides whether the probate fee profile touches PI at all.**
- ✅ **Tex. Civ. Prac. & Rem. Code ch. 71** — **full chapter read 2026-07-26.** **WD proceeds are not estate funds, on three independent hooks:** §71.004(a) exclusive benefit of surviving spouse, children, parents; §71.010(b) apportionment in the jury's verdict among those alive at that time; §71.011 not subject to the decedent's debts. §352.002's commission base therefore has nothing to attach to on a WD-only file — **including** the §71.004(c) case where the representative is compelled to prosecute. **WD half of V15 closed against the commission, pending sign-off.**
  - ⬜ **Residual (V15, survival half): OPEN and load-bearing.** **CLAUDE CORRECTION, recorded:** Claude stated in-session that a survival recovery is estate property. **§71.021(b) does not say that** — the action survives "to and in favor of the **heirs, legal representatives, and estate**," three unranked takers. Whether funds pass through the representative in administration cannot be closed from ch. 71; needs case law, same shape as V4. Even where they flow, §352.002(b)(2)(C) excludes cash paid to an heir or legatee as such, so the commission would compute on the receipt side only.

### Still to pull — the session stopped here

- ⬜ ~~**Tex. Fam. Code §§106.002, 6.708, 6.502(a)(4), 156.005**~~ — **REMOVED FROM THE QUEUE 2026-07-26. Family law is out of scope by ruling**; no family case type will ever exist, so there is no family profile to encode a mandatory branch into. The §156.005 stress-test lesson is already baked into the ruled O6 decomposition; **the probate chapters replace it as the stress test.**
- ⬜ **TDRPC 1.04** — **← RESUME POINT (moved 2026-07-26; was §352.051).** **RETAINED notwithstanding the family removal** — this is the fee-*reasonableness* rule and is core to PI contingency work; only its divorce-contingency subpart is mooted. Narrowed question: the reasonableness factors and the unconscionability ceiling as they bear on PI contingency work.
- ⬜ **Tex. R. Civ. P. 204.1** (the rule text; the case line is now done) — **should the IME task hook fire on serving a damages expert report, or on an earlier signal?**
- ⬜ **The Estates Code territory the probate line needs** (new 2026-07-26, per S-1/PR-1/PR-2 — the line's spine is **independent, uncontested administration**): will admission and letters; heirship; notices to beneficiaries and creditors; inventory, appraisement, and list of claims; claims presentation and allowance; independent administration. Also referenced but not pulled: Est. Code §404.003 and Subch. B, ch. 361 (removal, via §352.004); ch. 503 / §501.006 (foreign representatives, via §71.012/§71.022).
- ⬜ **Probate research targets — FROM MEMORY, NOT READ, NOT ASSERTED.** A starting list for a future pull, deliberately carrying **no cites**. Nothing here opens a registry entry, and nothing here is authority until the text is in front of us.

### Items ruled 2026-07-26 (recorded so the queue's status lines stay truthful)

- ✅ **S-1 — probate a real practice line?** **CLOSED: yes** — a mapped line, full build-out, notwithstanding low volume.
- ✅ **PR-1 — independent vs. dependent administration?** **CLOSED: independent administration is in scope**; the deadline machinery matters on this line, and §352.003(b) (county-court jurisdiction over independent executors' alternate-compensation applications) is live.
- ✅ **PR-2 — contested vs. uncontested?** **CLOSED: will contests are a rare branch**; §352.052 builds as secondary, not core.
- ✅ **D3 / H8 — shared touch substrate?** **CLOSED: the case-event core (CE), shape (c)** — shared spine plus per-consumer facets, with an operational/evidentiary boundary. **Unblocks CE1; does NOT authorize it.**
- ⬜ **PR-3 — the `Probate companion` case type is MIS-PARENTED. NEW, OPEN.** Source verification 2026-07-26 found it at `src/domain/caseTypes.ts` **under Personal Injury**, inheriting `_piDefault` — a ladder the code's own comment concedes does not fit its arc. **S-1 rules probate a practice line in its own right, which that structure contradicts.** Re-parenting touches the case-type tree and the ladder assignment, so it is your call; it converges with the pending probate-ladder design pass (`spec-feedback` item 1) rather than pre-empting it. **Nothing re-parented, no ladder changed, nothing built.**
  - **Direction CONFIRMED 2026-08-07 per V17** (own practice area, own ladder(s), companion concept gone); **EXECUTION HELD** until the probate-ladder design pass produces the destination ladder(s) — a placeholder ladder is how the current wrong one happened. That pass is now **schedulable**: the Texas probate corpus arrived design-side. **New sub-question the pass must answer: proceeding-driven case types/ladders** (independent administration vs. muniment vs. heirship vs. small estate have different arcs — one "Probate" ladder may recreate the `_piDefault` mistake one level up). D-CL1-3 stays gated on PR-3. **Still open; still nothing built.**
  - **Annotation updated 2026-08-07 (evening session).** The execution hold is **unchanged**, but the gating design pass is now **DEFERRED pending the Domser matter** — Michael has not run a probate matter end to end, so a ladder drafted tonight would be treatise-derived rather than practice-validated. **Concrete unblock condition** (this is what the hold now waits on, replacing "schedulable"): the **letters-issued arc capture** from the Domser matter, plus a rescheduled pass. Checkpoints are at LETTERS and CLOSING. The PL-1..PL-4 series below carries the questions the pass must answer. See `rulings-capture-2026-08-07b.md`.
- ⬜ **RE-1 — referral engine.** **NEW, OPEN.** Ruled yes-eventually; triggers, logging, referral letter, and conflicts interaction all open. Not family-specific — conflicts, out-of-area matters, and overflow are all inputs.
- ✅ **FAM-1 — family furniture in `src`/`db`?** **CLOSED by Code verification 2026-07-26: none exists** — no family practice area or case type, no family-specific roles, no family tables, columns, or enum values. Nothing to remove; the removal was doc-only in fact as well as in authorization.
- ✅ **FLP / CourtListener account + connector.** **CLOSED 2026-07-26** — signed up, connector live. The 30-day window doubled access for members and non-members alike (a usage promotion, not a signup window), so the 8/6 date carried on nine consecutive entries was never a cliff. **The V10 citator pass is now RUNNABLE**, subject to the majority-opinion rule.

---

## 3 — TRCP DEADLINE SKELETON (none touched today)

- ⬜ **TRCP 194.2(a)** — the anchor; everything downstream derives from it. **Is the trigger 30 days after the first answer or appearance, exactly as the skeleton states?**
- ⬜ **TRCP 190.3(b)(1)** — the cascade rule. **Is the discovery-period end the earlier of 30 days before trial or nine months after the anchor?**
- ⬜ **TRCP 195.2(a) and (b)** — the most expensive thing on this list to miss. **Are the 90-day and 60-day offsets from the discovery-period end correct?**
- ⬜ **TRCP 166a(d-1), (e-1), (g-1)** — a **current-practice risk today**, independent of the build. **What changed in the 2026 edition, and does it alter anything you're doing right now?** Confirm you are reading the 2026 edition — a 2020 PDF was uploaded by mistake once.
- ⬜ **TRCP 99(b)** — the answer date is 10:00 a.m. on the Monday next after twenty days expire. **Is that formulation exact enough to write a tested function against?**

---

## 4 — STATUS CHECKS (none done today)

- ⬜ **Sheppard v. Martinez subsequent history** — **has it drawn any subsequent history that changes Entry 1 or Entry 5?** One of seven cases in Entry 1's citator pass.
- ⬜ **In re Greystar · In re Brenham Nursing · In re Pinnergy** — all pending as of the course book's June 2025 window. **Have any been decided?** *Ortiz* is the cautionary example: it sat on this list as "pending" and had in fact been decided in May.
- ⬜ **Kostura's 30-day CPN rule vs the 2024 CMP rules** — the paper itself flags possible supersession. **Which text governs**, before any conditional-payment thread template is drafted?
- ⬜ **AMA CPT license terms** — **can the module store descriptor text at all, or is it numeric codes only?** Entry 7's only real blocker.
- ⬜ **NEW: *Ortiz* citator pass** — the opinion is ~12 weeks old; rehearing window and early treatment unknown.

---

## 5 — PROJECT DOCUMENTS AWAITING REVIEW (none opened today)

- ⬜ **Entry 2 redraft v2** (now the Entry 2 section of `legal-rule-registry-draft-entries-medical-billing.md`, replaced in place 2026-07-26) — **does (e) state the claimant-offers-the-counteraffidavit move correctly, is (f)'s uncontroverted floor worth building as a computed figure, and how should (j) record the contested *Allstate* dicta?**
- ⬜ **`legal-rule-registry-draft-entries-medical-billing.md`** — Entries 1, 3, 6, 7, 8, 9 (Entries 2, 4, 5, 10 advanced today). **Entry 1(c-3) first: does a reasonableness-conditioned LOP cap the "incurred" amount?** The billing module cannot encode either reading until you rule.
- ⬜ **`time-tracker-fee-basis-profiles-design.md`** — **do you adopt it as canonical, and do you accept the §3 call putting claims in their own table?** Still marked DRAFT; written in a Code session, never given a design-side read.
- ✅ ~~**`case-heartbeat-design.md` §10 — D3: one touch substrate or two?**~~ — **RULED 2026-07-26: the case-event core (CE), shape (c)** — a shared spine plus per-consumer facets, owned design-side, with an operational/evidentiary boundary (only evidentiary facets feed a sworn fee affidavit). Ruled against four consumers, not two. **The former "blocks T1" phrasing is retired with it** — the substrate slice is now **CE1** (N-1 rename; the transcript T-series is unchanged), and **CE1 is unblocked but NOT authorized.**
- ⬜ **`case-heartbeat-walkthrough-capture-2026-07-25e.md`** — H35–H41. **H40, asked twice without an answer: is a deposition thread dead on notice served, on the depo being calendared, or on the transcript being in the file?**
- ⬜ **`case-heartbeat-walkthrough-capture-2026-07-25f.md`** — H42–H58. **H50: is the supplementation opt-out a hard kill, or dormant with wake events?** The only opt-out whose penalty is automatic exclusion.
- ⬜ **`apil-2025-course-book-mining-pass2.md` §8** — H59–H67. **H60: do payer threads arm at intake off the coverage inventory, rather than at settlement?**
- ⬜ **`apil-2025-course-book-mining-pass3.md` §10** — H68–H83. **H77: does the registry get a `precedential_status` field separate from verification status?** *Gregory v. Chohan* is a plurality treated as binding by nearly every court citing it. **Now has a second live exhibit** — the contested *Allstate* dicta in Entry 2(j).
- ⬜ **`trcp-deadline-skeleton-2026-03-01.md` §9** — **H22: do the TRCP entries queue behind entries 1–10, or interleave by build urgency?** The queue arithmetic is the project's bottleneck.
- ⬜ **`registry-courtlistener-integration-design.md`** — **webhook or email given current hosting posture, and does cite-validation pull forward?** *(FLP account + Tier 1 + connector: CLOSED 2026-07-26 — see §2. The "promo ends 8/6" clause is struck; it was never a cliff.)*
- ⬜ **`criminal-appointment-intake-and-docket-enhancements.md`** — **which appointee names count as yours, and does that vary by county?** The Hill Country Regional Public Defender Office would currently hard-stop.
- ✅ ~~**`REQ-CAPTURE_uvalde-docket-worksheet_2026-08-12.md` — FILED UNRECONCILED**~~ — **RESOLVED 2026-08-12 by Michael's group ruling** on the presented disposition table (the roster-capture precedent). The sixth REQ-1 capture, and the only one that reached the repo *before* reconciliation — that difference is preserved at #59, not erased. Reconciled against BUILD-STATE's fifty-fourth refresh and the log through #61, i.e. **after the CD-1 build**, so its directory dispositions cite built structure rather than planned structure. Full disposition table in the capture's appended addendum. Its open questions now live in the CR rows below; **the capture is no longer an open item.**

### Criminal-side requirements (CR series) — ADDED 2026-08-12

*(**Series CREATED by Michael's ruling 2026-08-12**, the WF-1/DE precedent: criminal practice items fit neither FE nor IN nor DE. **The #48 elicitation caveat applies to CR as it does to IN and DE — renaming is a one-word veto until these IDs get load-bearing.** **COLLISION FLAGGED, DELIBERATELY NOT RENAMED (the H-series disposition):** `CR-3` already means the **Texas crash-report form** in this file at IN-2 and in `contact-directory.md` §7. That is a form designation, not an ID series, so the packet's collision test did not fire — but the string now carries two meanings in this file. Michael was asked before these rows landed and chose to proceed as CR with the clash flagged; renaming an attorney's ID series is not Code's call. **When citing one, say "CR-3 (criminal requirement)" or "the CR-3 crash report."**)*

- ⬜ **CR-1 — Hearing-date extraction with attorney confirmation at matter creation.** From the appointment paperwork: when a matter is created from an OAA, prompt as to whether a hearing is already set — it may be in the paperwork — and if so pull it from the document. Priority: now. **Dedupe instruction carried:** check against `criminal-appointment-intake-and-docket-enhancements.md` at HEAD; if the intake spec already covers it, this records as a second observation on the covering section rather than a standalone item.
- ⬜ **CR-2 — Per-cause prosecutor assignment, by-prosecutor grouping of the day's docket, and a county prosecutor roster with collision-safe short codes.** Prosecutors ask off-hand "what cases do we have together today?" and the answer must be readable from page 1. **Question riding here (§5 Q1): should the prosecutor assignment live at the CAUSE level or at the DOCKET-ENTRY level**, given that one defendant's multiple causes were split between two prosecutors on the same docket, and that the assignment observed at court differed from the printed docket on roughly three of eight defendants? Priority: now. The roster half is **CD-1 directory substrate** (contacts with roles) — annotated onto the CD entries too.
- ⬜ **CR-3 — At-court correction capture** (prosecutor, attorney-of-record, setting type) that persists to the matter and reconciles against the next published docket, with a **court-confirmed flag distinguishing observed fact from imported data**. Treat printed docket data as unreliable: live evidence includes a blank attorney field resolving on the next docket and a clerk attorney-code error flagged in the docket's own error log. Cross-links CR-2. Priority: soon. *(This is CR-3 the criminal requirement — not the CR-3 crash-report form cited at IN-2.)*
- ⬜ **CR-4 — Front-page expected-disposition marker** (plea / contest / status) **plus per-setting appearance outcome** (appeared, no-show, in custody elsewhere) feeding the follow-up list and the next worksheet's banners. Live evidence: plea-expected cases marked by hand with a "P"; no-shows recorded in the margins; one no-show client on bond; a matter needing a reset after a bench-out did not happen. Priority: soon.
- ⬜ **CR-5 — Custody status per client** — facility, county/state, and the **source and date of the information** — **including out-of-state facilities**, plus **bench-warrant/writ status**, both surfaced in the client-page banner. Live evidence: a revocation client confined in another state's correctional facility, with handwritten notes tracking that he did not get benched out here and needed a reset. Priority: soon.
- ⬜ **CR-6 — Designated scratch and quick-intake blocks on the worksheet, plus a post-court reconciliation step** that routes captured notes to the correct matters and creates new matters from intake blocks. Live evidence of what happens without them: one client's negotiation numbers written on a different client's page; a new case for an existing client written upside-down on a blank back; a full new-client intake handwritten on the front page. **Question riding here (§5 Q7): when handwritten court notes are transcribed and routed to matters after court, what marks the resulting record as ATTORNEY-REVIEWED rather than raw OCR, so downstream worksheets don't print unverified transcription as fact?** Priority: soon.
- ⬜ **CR-7 — Three-way number mapping: defense cause number ↔ docket entry number ↔ DA file/incident number.** Stored per cause, printed on the worksheet, optionally imported from the DA's charge-table document. Live evidence: the DA's own charge-table printout hand-annotated — indicted rows circled, numbered, and mapped to docket entries — because the worksheet carries only cause and docket numbers while the DA's office talks in its own file numbers. **Question riding here (§5 Q5): manual entry at court, OCR-assisted import of the DA charge table, or both — and can the mapping print as a front-page column without crowding the handwriting space?** Priority: now.
- ⬜ **CR-8 — Offer lifecycle tracking: offer versions as first-class records** (date, source, full terms, option sets), with the worksheet rendering history, a changed/unchanged flag, and option sets. Live evidence: one offer tracked through three versions with dates and sources (email vs. offer sheet vs. revised sheet); another flagged "UNCHANGED from the sheet you had before the last setting"; a two-option offer laid out as a client choice; a probation option disappearing and returning. **The email-workflow doc's criminal plea-offer observation is its unruled relative — cross-referenced, never merged.** Priority: now.
- ⬜ **CR-9 — Notes attach to the MATTER, not the worksheet, and print forward automatically** — including transcribed handwritten notes from CR-6's reconciliation step. Live evidence: the later worksheet reproduced the earlier one's typed notes in CARRY-OVER blocks so nothing was lost, but handwritten content did not carry and had to be re-copied by hand; a phone number was handwritten twice and never entered any system. Absorbs the print-forward half of the contacts-at-capture requirement. Priority: now.
- ⬜ **CR-10 — Plea-paperwork audit rendered as a signing checklist:** degree of offense vs. charged statute, jail-credit presence for in-custody/bench-warrant defendants, completeness of required certifications, and non-concurrent cost totals. **The capture's own highest-value item.** Live evidence, all caught on paper before the judge signed: zero days' jail credit printed for a defendant who had been sitting on a bench warrant; the degree of offense misprinted on two causes; a blank, unsigned certification of right to appeal on every cause; and court costs that do not run concurrent. **HARD GATE, restated from CLAUDE.md rule 1: no computed check runs while the underlying propositions are UNVERIFIED.** Those six propositions now live in `docs/specs/legal-rule-registry-criminal-plea-and-costs.md` (all UNVERIFIED), and **CR-10 is gated on their verification.** **Question riding here (§5 Q6): can these checks be encoded as rules run against drafted judgments, and which verified-authority registry do they check against? — ANSWERED IN PART by the ruling: that registry file, once verified.** Priority: soon, but highest-value.
- ⬜ **CR-11 — Rule-generated banners from matter state** (prior FTA with a transportation concern, bench-warrant history, clerk data error, new appointment, changed offer) **plus a "FOLLOW-UPS — not on tomorrow's docket" closing page** fed by open tasks, **plus quick task capture from court notes** (a records subpoena to issue, mitigation to gather, items to run down with the court coordinator). Priority: soon.
- ⬜ **CR-CONSTRAINT (no ID — BINDING DESIGN CONSTRAINT, not a feature): all worksheet semantics must survive BLACK-AND-WHITE printing.** Borders, shading density, weight, and icons — **never color-coded meaning.** Michael prints in black and white only; the highlighting observed on one worksheet was a one-time conversation aid and was expressly **not** a feature request. **This governs ALL worksheet rendering, including every CR item above.** **Question riding here (§5 Q4): which black-and-white-safe typographic conventions should carry banner and flag semantics?** FE-10's format profiles are the civil-side sibling — **cross-referenced, never merged.**
- ⬜ **`spec-feedback.md`, 2026-07-25 OAA entry** — **do you want a cause-number-pending reminder, and should gender and race be captured at all?** **WIDENED 2026-08-12 (#64), NOT resolved: the CR-3 intake path raises the identical question.** The crash report carries ethnicity and sex fields, so if extraction is ever built it faces the same choice the OAA path already put to you. **One answer should govern both intake paths** — recorded here so they cannot drift into two different defaults. Still yours; nothing decided.
- ⬜ **`BUILD-SESSION-NOTES.md`** — 2026-07-21 audit, unreviewed across ~10 log entries. **Should case classification stay frozen at creation, or become editable?**
- ⬜ **`Go_Live_Gates.md`** — **can you paste gates 1–5 verbatim so the repo copy stops being a stub?** Gate 3 also needs its amendment appended, pointing at gate 6.
- ⬜ **Texas Ethics Opinions 532 and 533** — **where is the Op. 532 line on case-level detail leaving the system to a third party — does it reach benchmark pooling and multi-user deployment, and should a go-live gate say so?**

---

## 6 — THE TWO HIGHEST-LEVERAGE LEVERS

Unchanged by today's work, and worth stating plainly at the top of any session:

1. **D3** — the shared touch substrate. Blocks every schema build in the project.
2. **Entry 1(c-3) and Entry 4** — qualified LOP and the fatal-defect conflict. Together they block the disbursement checklist and the billing module's LOP math. *Entry 4 advanced today but did not close: the surviving question (V4) is a case-law question the statute cannot answer.*

*(The former "hardest date on the board: FLP by 8/6" line is struck — closed 2026-07-26. There is no dated deadline on this board.)*

---

## 7 — PROJECT AND BUILD OPEN ITEMS

**Added 2026-07-28 (BS-1a item 10).** These were carried in `BUILD-STATE.md`'s own roster until that ruling replaced it with a pointer to this file. They are Michael's, same as everything above; they are separated only because they are build/process items rather than legal-research items.

### Closed by ruling, recorded so the status lines stay truthful

- ✅ **AUTH-1 — sign-in method for the single-user auth slice?** **CLOSED 2026-07-28: magic link** ("Let's go with Magic Link"). Built and exercised the same day; see log #28.
- ✅ **BS-1a — BUILD-STATE was at cap with the ledger split delivering one line: designate more content for the ledger, split live state differently, or accept the cap pressure?** **CLOSED 2026-07-28 by eleven item-by-item dispositions** — deletion, compression, and pointers rather than ledger moves. Post-cut length 139 of 150. See log #29.
- ✅ **D-CL2-8** — parallel, not promotion. **Adopted 2026-07-28 as Michael's own ruling**; the "Claude's call" asterisk retired.
- ✅ **D-CL2-2a** — earliest limitations derives from **unresolved clients only**, resolved meaning disbursement (D-CL2-4a). Closed 2026-07-26.

### Client model — the design doc's §10 is authoritative for all of these

`docs/specs/claimant-dimension-and-case-links-design.md` §10 carries the full table. Listed here by ID so the queue is complete.

**Amended 2026-08-07 by QR-1:** the former instruction here — *"do not maintain a second copy of the question text"* — is **superseded for the question text itself**. QR-1 rules that every open item carries its FULL question text in this file, because this file is the register that survives when packets are deleted. The design doc §10 remains authoritative for the *ruling detail and reasoning*; what must not be duplicated is analysis, not the question. Where the two disagree on wording, §10 governs the substance and this file governs completeness.

- ✅ **D-CL2-3** — **does the time tracker's "one rate per case, uniform" rule survive per-client fee arrangements, or become one rate per client?** **CLOSED 2026-08-07: one rate per CLIENT.** Michael supplied the fact pattern that withdrew Claude's keep-it-case-level recommendation — some cases have two clients, one at a discounted rate and one at standard. The rate moves onto the client record beside `fee_arrangement`; single-client cases render exactly as today (D-CL2-7 principle). **Mid-case changes are PROSPECTIVE** via an effective-dated rate history: hours are valued at the rate in force when incurred, nothing silently revalues, and a retroactive courtesy is a visible invoice write-down rather than a recompute. Full ruling and quotes: design doc §10 and `rulings-capture-2026-08-07.md` Part 5. **Authorizes nothing** — the time tracker stays parked behind CE1.
- ⬜ **D-CL2-3a — NEW 2026-08-07.** **Fee-affidavit export under per-client rates — the export assumed one uniform case rate (lodestar-shaped). Which rate does the affidavit carry when clients differ — the rate of the client whose claim carries the fee demand (Claude's lean, PROPOSED), or something else?** Michael's.
- ⬜ **CL2-AC-1 — Auto-create the client record when a party is linked with the Client role on a PI case — a PI client with no damages-scope record is an impossible real-world state.** *(ID issued 2026-08-07 for the 2026-07-28 walkthrough capture, so the question cannot die the K-6/K-7 death.)* **Michael's own framing, from the CL-2 live walkthrough (moved here 2026-08-08, not shrunk):** linking a party with the Client role on a PI case and having no damages-scope record is an **impossible real-world state** — a PI client without damages isn't a client. Michael: don't make me build it by hand; assigning the Client role on PI case types should auto-create the damages-scope record, posture defaulting from case type, ready to populate. Data model stays parallel (D-CL2-8); **UI workflow only**. Brushes directly against deferred **CL2-CHECK-1** — "auto-create on PI" and "flag Client-role-without-record" are two answers to the same gap, and Michael fell into that gap himself on 2026-07-28. **DIRECTION-CONFIRMED 2026-08-08** (was PROPOSED, unruled): auto-create on PI client-role link is the policy. **UNRULED, doc-on-screen required:** link-removal behavior (persist/orphan/prompt), mixed-posture cases, backfill vs. new-links-only. **Not closed until the edges are ruled.** *(The morning's "yes" was withdrawn to direction-confirmed by Michael's own choice of option (b) after Claude flagged its own bad elicitation — `rulings-capture-2026-08-08.md` Part 5. **Consolidated here 2026-08-08** from the "Captured 2026-07-28" block per the CL2-AC-1 dedup ruling — one register, one home per question; see `rulings-capture-2026-08-08b.md` Part 2.)*
- ⬜ **CL2-CHECK-1** — advisory client-role ↔ client-record consistency check (flag/review-log, never auto-fix). **DEFERRED 2026-07-28 — do not build.** Adopt later or drop?
- ⬜ **D-CL1-1 · D-CL1-2 · D-CL1-3** — CL-1 / `case_links` shape. **D-CL1-3 is gated on PR-3 alone.**
- ⬜ **CL-3 — Follow-on matter creation: create a linked follow-on matter that inherits selected entities and facts from a source matter, with explicit role transforms, instead of starting blank (live evidence: a settled third-party motor-vehicle case spawned a first-party UIM/UDJA suit sharing plaintiff, occurrence facts, and counsel, with the former defendants recast — the defendant driver became the non-party "underinsured motorist," the co-defendant vehicle owner a non-party settling party, and the client's own insurer entered the defendant slot). Sub-question (UIM capture §5 Q1): should a follow-on matter inherit the source matter's evidence and discovery products (crash report, records indices, deposition transcripts) as linked references, or as copies — and does privilege/work-product tracking differ between the two?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now; ID RULED by Michael the same day as part of the group ruling on the capture's disposition table). CL-series continuation: the follow-on edge carries role-transform mappings ({{party}} was {{role_a}} in source, is {{role_b}} in target) as its payload. **CL-1's non-cascading rule stands** — its spec says anything that should propagate is a separate, explicit ruling, and **CL-3 is that ruling's designated home, scoped to the follow-on link type only.** Cross-reference `docs/specs/claimant-dimension-and-case-links-design.md` §7. Companion finding, no separate ID: the capture's cross-matter party-role-transform requirement is already satisfied by the ruled directory — one global row, roster entry = contact × capacity × role per case, and `contact-directory.md` §4.2's worked example is literally the UIM at-fault driver. **NOT authorized for build.**
- ⬜ **UM-1 · UM-2 · PR-GATE-1 · MIN-1** — the four unruled proposals; none blocks any authorized slice.
- ⬜ **CIV-1** — **civil-litigation damages are entirely UNSPECIFIED.** Needs its own design session; breach of contract is the workhorse of that line.
- ⬜ **PROB-1** — probate client profile unwritten. ⬜ **PA-1** — practice-area edit vs. derived profiles.

### Form engine (FE series) — ADDED 2026-08-07

**This series was absent from this file entirely** until the 2026-08-07 rulings batch; it had lived only in `form-engine.md` §12.6. Added here so the register is complete. `form-engine.md` §12.6 carries the finding text and the ruling detail.

- ✅ **FE-1 — chronology sources carry provider identities but NOT addresses, phones, or billed charges; the engine cannot ship a servable draft from a chronology alone. Where does provider data come from — §4 interview cards, a persistent provider-directory table, or another source?** **CLOSED 2026-08-07: the persistent provider-directory table**, with the §4 interview card surviving as the fallback for a provider not yet in the directory, writing back into it (enter-once). Three sub-rulings, all confirmed: **(a) identity in the directory, dollars on the case** — billed charges are per-case facts, and storing them per-provider would recreate the wrong-level defect class CL-2 was built to kill; **(b) Option A pointer model** — case-level provider records point at the directory entry and read live, with the served .docx files as the historical record of what went out; **(c) silent trust** — directory fills carry no confirm flags, since the data is attorney-entered and the proofread every filing gets already catches staleness. Michael's own load-bearing requirement, recorded: editing a provider **from within a case** must propagate to every linked case, which only the pointer model delivers. Build-facing consequences for whenever the engine is authorized: the directory entry must be editable in case context, and that edit surface must state firm-wide scope with a linked-case count (labeling, not a confirm click). **Spec ruling only — authorizes no table, migration, or UI.** **SUPERSEDED 2026-08-08 by CD-1** (contact-directory reframe): the ruling stands for the provider question as framed; the live question is CD-1. Surviving mechanics carry into CD-1 as PROPOSED. See `rulings-capture-2026-08-08.md` Parts 1–2. **Evidence channel opened 2026-08-10:** the `drafting-disclosures` skill (`docs/skills/drafting-disclosures/SKILL.md`) emits a mandatory provider-data block every run — designed as the FE-1 scout, to accumulate a real provider corpus so the directory-vs-interview-cards question could be ruled on evidence. Since FE-1 closed and was superseded, **that evidence now feeds CD-1**, not FE-1. Blocks are saved by Michael's hand outside the repo; the skill never writes them here.
- ✅ **FE-2 — one billing entity existed ONLY in the chronology's billing-record document names, with no chronology row of its own; a human caught it and §4 intake would have walked past it. Should intake sweep document-name columns for entities not otherwise listed?** **CLOSED 2026-08-07: ruled yes.** Finds surface as flags for Michael — a catch-net, never an auto-add, since nothing enters a disclosure without his call. Because document names are messy (abbreviations, records vendors, misspellings), dismissal is one click and **dismissals are remembered per case** so the engine doesn't re-ask every draft. **Spec ruling only.** **Build home PARKED 2026-08-08**, unattached: ruled, awaiting a build home; likely lands with the CD-1 build or the intake pipeline, whichever comes first. (Slice A, its loose prior attachment, was withdrawn.) **RE-PARKED 2026-08-11: FE-2 lands with the INTAKE PIPELINE**, superseding "whichever comes first" — reason: the sweep operates on document-name columns and nothing in the CD-1 slice (or the app today) ingests documents, so the parked ruling's premise does not hold here. **The 2026-08-07 ruling itself is untouched** — this moves the home only, it does not reopen the ruling. See `docs/specs/cd1-build-slice.md`.
- ✅ **FE-3 — `form-engine.md` §8 carries real-case-flavored shell content predating the packet rules; needs a design-session read of §8 on screen before ruling what example content ships in the spec.** Michael's. *(Design-side searches on 2026-08-07 could not surface an FE-3 row in this file; verification confirmed it was genuinely absent, not a search miss. Added here with full text per QR-1.)* **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** **CLOSED 2026-08-12 (RULED): the §8 on-screen read is complete (full text; design session with Michael supplying the section from the P1 checkout). The five formatting bullets and the contamination bullet's ruling content ship as written; the bullet's two real prior-case example strings are GENERALIZED to generic descriptions by in-place amend with a dated note (never silent rewrite). The strings are not reproduced in the record. See #55.**
- ⬜ **FE-4 — Definitions as case-level objects: one definitions list (incident, vehicles by VIN, party set, ESI terms) written once per case with its own identity, referenced by every instrument rather than baked into each document, so an amendment propagates; the "You/Your" addressee definition substitutes per target. How do definition objects attach to the case and version across a discovery wave?** Entered 2026-08-11 (REQ-CAPTURE). Exemplar: `docs/templates/discovery/template_definitions-instructions_requests.md` (¶2 = per-target substitution; ¶12+ = case-specific slot; optional-ESI brackets = conditional blocks). Gated behind CD-1 with the rest of the engine. **RULED 2026-08-11 — design at `form-engine.md` §13.1** (case-level versioned definitions objects: immutable versions, every rendered instrument stamps the version used, prospective propagation; the You/Your addressee definition is render-time per-target substitution, NOT versioned content). Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.**
- ⬜ **FE-5 — Interrogatory count budget: live count per set against a configurable cap by discovery level; document-identification-only interrogatories excluded from the count; soft warning where an interrogatory has facially severable subparts. How aggressive should subpart detection be — attempt facial detection (multiple sentences, enumerated clauses) and show a worst-case count beside the numbered count, accepting false positives on compound-but-unified interrogatories, or warn only on request?** Entered 2026-08-11 (REQ-CAPTURE §5.2). Legal substrate — TRCP 190.3(b)(3), 190.4 — VERIFIED by Michael 2026-08-11; counts may display as facts. Design risk on record: warning fatigue. **RULED 2026-08-11 — design at `form-engine.md` §13.2:** the middle path between this entry's two poles — subpart detection is always-on and purely facial, the worst-case count displays **passively** beside the numbered count, and it escalates to a warning **only when the worst-case crosses the cap while the numbered count does not**, which honors the recorded warning-fatigue risk. Severability judgment stays the attorney's. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.**
- ⬜ **FE-6 — Instrument packaging modes: render the same request content as one combined served document or three separate documents without re-entry, from one item model (item = id, text, scope common|role:<tag>|entity-specific, instrument type; set = ordered filtered list). Sub-question never asked in the capture: does each standalone instrument repeat the definitions block or incorporate it by reference?** Entered 2026-08-11 (REQ-CAPTURE). Combined-mode layout fully evidenced (caption → title → TO → preamble → definitions → instructions → interrogatories → RFPs → RFAs → signature → COS, plus the template's page-break structure); separate mode needs its own layout ruling. **RULED 2026-08-11 — design at `form-engine.md` §13.3:** packaging is a render-time choice from one item model, and the never-asked sub-question is answered — **each standalone instrument REPEATS the definitions block in full, no incorporation by reference** (served documents self-prove; FE-4's versioning removes repetition's drift cost). Separate-mode layout derives from the evidenced combined layout, one COS per document, verification page rides the interrogatories instrument only. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.**
- ⬜ **FE-7 — Template distillation queue: mark a produced document "distill to template" so a later session generates the client-clean template with merge fields. Guardrails (in from birth): distillation output is client-clean BY CONSTRUCTION — the no-real-client-data rule is why distillation is a deliberate separate step, never "save as template" — and distilled templates are PROPOSED artifacts until Michael adopts them as house standard. What does the queue look like pre-engine, and what does adoption look like?** Entered 2026-08-11 (REQ-CAPTURE). Pre-engine the queue is a list in `form-engine.md`; first three candidates: entity trucking set, driver set, nonsubscriber battery (from the 2026-08-11 drafting session). **RULED 2026-08-11 — design at `form-engine.md` §13.4**, where the pre-engine queue is now a LIVE table seeded with those three candidates (all `queued`, none distilled, none format-authoritative). Route respects REQ-1: marking happens in practice space, the candidate travels by Michael's hand, distillation is a design-session act producing a client-clean template into `docs/templates/<category>/`. **Adoption is Michael's explicit ruling and is what flips the template's FE-12 provenance to format-authoritative**; until then it is distilled-PROPOSED and nothing drafts from it as authority. Question text retained above; **build remains gated on the form engine being named and authorized as a slice, after the CD-1 build per `docs/specs/cd1-build-slice.md`.**
- ⬜ **FE-8 — Attorney-edit round-trip diffing: retain the as-generated version of every rendered instrument so an attorney-edited return can be diffed precisely — what the attorney changed is both the finalization worklist and a signal about house conventions (live evidence: a five-set family came back edited with no stored as-delivered copy to diff against; edits had to be inferred). Sub-question (capture §5.6): should the as-generated retained copy be immutable and content-addressed so "what did we deliver" is provable later, and how long are superseded generations retained?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Extends form-engine §10's generated-document record and FE-1(b)'s served-docx history. Lineage note: the capture cites a prior packet's "REQ-12 (artifact provenance)" — the cite resolves to nothing on record (design side searched 2026-08-12; Michael searched CIVIL LIT the same day); recorded as a probable citation defect, nothing reconstructed per the K-6/K-7 rule; this entry's text is complete on its own. Gated behind CD-1 with the rest of the engine. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status.
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, retention half only.** As-generated retention IS the §10 generated-document record (.docx + metadata) — that record is the retention design, so FE-D1 builds it. The DIFF half is DEFERRED to the slice with the first real consumer (the transform / discovery work), where FE-13 needs it anyway.**
- ⬜ **FE-9 — Document-family consistency: model a request-set family as shared core + per-recipient overlays so a checker can distinguish intentional per-recipient tailoring from drift and report ONLY the drift (live evidence: attorney edits introduced an "ADMISSION"/"ADMISSIONS" section-header divergence in one set of five, and small-caps character formatting lost in one retyped paragraph). Sub-questions: should character-level formatting (small caps, underline) inside body text be enforced or only surfaced as advisory, given attorneys legitimately retype text in Word (capture §5.2)? Does the drift report belong in the finalization UI (blocking, pre-service) or a review log (non-blocking) — is drift ever acceptable to serve (capture §5.5)?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Cross-link: FE-6's item model (scope common|role:<tag>|entity-specific) is the substrate — FE-6 is packaging, FE-9 is drift detection; not merged. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status.
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1, home named: the DISCOVERY slice.** Family-drift detection needs FE-6's packaging families, which FE-D1 does not build.**
- ⬜ **FE-10 — Render-time format lint: format profiles carrying paragraph-level properties (indent, hanging indent, spacing) per element class — item paragraph, prompt label (ANSWER: / RESPONSE: / ADMIT OR DENY:), section header, certificate block, verification block — enforced at render time, plus a lint pass flagging formatting outliers among elements that should be identical (a 20-of-21 paragraph-property histogram makes the 1 obvious; live evidence: the final response-prompt label carried different indentation than the other 20/31/19 identical labels in every set — a generation-side glitch invisible until rendered). Sub-question (capture §5.1): when an attorney's edit conflicts with the stored profile, is it a one-off deviation preserved in that document only, or a profile update propagating to future renders — and who confirms which?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE, priority soon). Extends form-engine §§12.7–12.13 (style-by-role, caption label style) from documented method to enforcement. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; UIM-UDJA REQ-CAPTURE REQ-05, ruled — no new ID, one home per question): the lint gains a NUMBERING pass. Generated instruments use continuous logical numbering and never hard-coded restart values, and a render-time lint verifies the visible numbering sequence is gapless and duplicate-free (live evidence: an exemplar's second numbered list was defined with a start value chained to the first list's final count, so inserting one fact paragraph silently produced a duplicate visible number until the start value was manually bumped). Additional sub-question riding here (UIM capture §5 Q6): should the numbering lint run automatically on every rendered instrument, or also exist as a standalone document-check tool that can be pointed at exemplars and incoming documents?** Format profiles also own the footer, per FE-15's cross-link.
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, FROM BIRTH — including the numbering pass.** The render-time format lint ships with the engine's first slice rather than being retrofitted.**
- ⬜ **FE-11 — Caption-body integrity: captions, titles, and pronouns render from roster data. Fork (roster capture §5.4): hard requirement — they cannot be hand-edited out of sync — or a lint/validation layer over free-form editing? The observed error rate argues the former; drafting flexibility argues the latter. Live evidence (roster REQ-16): caption-body drift is endemic across both banks — caption ≠ body plaintiff, filename ≠ caption party, singular/plural and pronoun drift, venue mismatches, store-number mismatches, parties in body missing from caption. The capture calls this error class the single strongest practice argument in the bank for a roster-driven engine: if the party list is data, every one of these defects becomes unrepresentable.** Entered 2026-08-12 (roster-mining REQ-CAPTURE). Distinct from FE-9: FE-9 compares family documents to each other; FE-11 compares documents to the roster. Interacts with FE-8's diff model — attorney edits are expected input, not defects. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; UIM-UDJA REQ-CAPTURE REQ-04, ruled — no new ID, one home per question per CL2-AC-1): the roster-driven consistency sweep covers EVERY STORY of the document — headers, footers, and footnotes — not caption-vs-body alone (live evidence: one exemplar carried THREE different insurer identities — one in the caption, a second in the body, and a third in the running page footer; caption-vs-body checking alone would have missed the footer). Two further sub-questions ride here — (UIM capture §5 Q2) when the consistency checker sweeps headers and footers for party identifiers, how should it distinguish a deliberate short-form (a footer that intentionally uses a shortened case style) from genuine drift against the roster? (§5 Q3) should "non-party tortfeasor" be a formal roster role in first-party coverage suits — the person appears throughout the declarations and facts but is not a party — and if so, does the checker verify their name consistency with the same rigor as parties?** Footer merge fields ({{case_short_style}}, {{instrument_name}}, page X of Y) route to the form engine as format-profile evidence, not an afterthought — REQ-04's practice moment is the proof.
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1, home named: the DISCOVERY slice.** Stated explicitly because the two look alike: **FE-D1's lint does NOT absorb FE-11.** FE-10 compares a document to its format profile; FE-11 compares a document to the ROSTER.**
- ⬜ **FE-12 — Template/exemplar provenance attribute: format-authoritative / content-only / known-bad-kept-for-reference. ATTORNEY INSTRUCTION OF RECORD (2026-08-11, standing, binding on drafting regardless of how this item resolves): the current-practice bank is format authority; the prior-firm bank is content-only, its formats expressly non-authoritative. Sub-question (roster capture §5.6): should source-trust level become an explicit attribute in the template library design, and does "known-bad, kept for reference" deserve first-class status, given a defective exemplar ("BAD DO NOT USE") was found filed alongside good ones?** Entered 2026-08-12 (roster-mining REQ-CAPTURE). Cross-links: FE-7 (distilled templates arrive born format-authoritative); the docs/templates/ home ruled in #48. Gated behind CD-1. **GATING RULED 2026-08-11 (see #54): this item does NOT gate the form engine's slice naming — FE-8–FE-12 are ruled at the engine's slice-scoping session (the CD-1-scoping pattern); FE-3 rides its own track. The engine is nameable once the CD-1 build lands.** Item remains OPEN; the ruling changes its gating status, not its status. **Extended 2026-08-12 (see #58; deficiency-handling REQ-CAPTURE REQ-05, ruled — no new ID): the provenance attribute gains RENDER-TIME ENFORCEMENT. Per-source authority levels (format-authoritative / content-only / known-bad-kept-for-reference) are enforced when an instrument renders, generalizing the two-bank ATTORNEY INSTRUCTION OF RECORD to a per-instrument rule (live evidence from one letter: the house letterhead asset was the formatting authority, a third-party CLE treatise form was the content model, and language was mined from a prior-firm exemplar whose header was expressly forbidden — three sources, three different authority levels, one document).**
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, FROM BIRTH.** Provenance tiers are enforced at render time from the engine's first slice.**
- ⬜ **FE-§11.1 — Document production / Bates-stamping module** (auto-stamping pipeline, numbering scheme, prefix conventions, output formats). **BANKED — needs its own design session.** *(Registered 2026-08-12 (#63) by a commissioned check of `form-engine.md` §11 against this file: it had no row here. The only "Bates" text in this file was DE-1's `evidence_refs[]`, which is a different thing — item-level pins to produced documents, not a stamping pipeline.)*
- ⬜ **FE-§11.2 — Remaining template conversions beyond disclosures** — designation of lead counsel, motion to withdraw, discovery requests and responses, criminal forms. **Later slices; explicitly OUT of FE-D1.** *(Registered 2026-08-12 (#63), same commissioned check — no row existed.)*
- ⬜ **FE-§11.4 — In-app template editor UX pass.** FE-D1 builds a **MINIMAL** editor (edit → new `template_version`, no styling UI) because §1's Michael-owns-the-wording principle requires *some* editor; **the UX pass itself stays open as a later item.** *(Registered 2026-08-12 (#63), same commissioned check — no row existed.)*
- ⬜ **FE-13 — Instrument transform as a first-class named operation: "draft instrument for matter B using exemplar E for structure and matter A for facts," with the field mapping (who maps to whom) surfaced for attorney confirmation (live evidence: the drafting instruction was literally "redraft document X using document Y as a guide" — same plaintiff and occurrence, new defendant, new instrument posture, the former defendants recast as non-parties).** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). Cross-links: FE-7 (distillation queue), FE-12 (exemplar provenance — E must be format-authoritative under the two-bank ATTORNEY INSTRUCTION OF RECORD), FE-8 (attorney-edit diffing on the transform output). Template candidate flagged by the capture for FE-7's §13.4 queue: `template_petition_udja-uim-carrier.md`. *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1.** Instrument transform needs FE-8's diff half plus the exemplar bank; later slice.**
- ⬜ **FE-14 — Relief bracket as a per-matter picklist among the fixed statutory options, rendered into the pleading template (live evidence: the rule-mandated relief statement had to be swapped between fixed statutory brackets, and the exemplar's bracket did not match the new case).** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). **Gated on the TRCP 47(b)–(c) registry entry, which is UNVERIFIED — the option list's wording is not encoded until Michael verifies.** *(That is a REGISTRY gate under CLAUDE.md's rule 1, not the #54 slice-scoping treatment; see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1** — pleading-side, and separately **gated on the UNVERIFIED TRCP 47(b)–(c) registry entry**, so it could not ship regardless.**
- ⬜ **FE-15 — Instrument posture (original / amended / supplemental) as a render parameter driving title, certificate-of-service inclusion, and footer instrument name TOGETHER (live evidence: converting a "first amended petition" exemplar into an "original petition" changed the title, required deleting the certificate of service — an original petition is served with citation, not by certificate — and changed the footer's instrument name). Sub-question (UIM capture §5 Q4): should instrument posture automatically include or exclude the certificate of service at render time, or only warn the attorney that the combination looks wrong?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority now). Cross-links: **IN-4** — its certificate-of-service logic must be posture-aware, because an original petition carries no certificate at all — and **FE-10** (format profiles own the footer). *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **IN FE-D1, SCOPED TO DISCLOSURES POSTURE ONLY.** The full original/amended/supplemental parameter across instrument families is not in this slice.**
- ⬜ **FE-16 — Citation bank per instrument class: reusable citation blocks tagged UNVERIFIED until the attorney verifies, with verified status persisting across drafts, backed by the legal rule registry (live evidence: every rule and case citation in the deficiency letter and motion came from a third-party CLE treatise and is unverified; drafts carry a delete-before-service banner saying so — that banner is the interim practice).** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority soon). Registry discipline governs: automation flags, only Michael verifies. **EXTENDED the same day by the UIM capture's REQ-09** — the bank stores the canonical current cite with verification state and flags slip cites (docket number + Westlaw) that likely have reporter cites by now; live evidence on both sides (an exemplar carried a slip-opinion cite for a case long since published in the reporter; the deficiency capture's own §4 carries five WL/slip cites, six by Code's count — see the registry note). That currency flag also routes onto `docs/specs/registry-courtlistener-integration-design.md` as evidence (Layer B cite validation; banked feature #13 citation-currency). **Q-6 posture UNCHANGED — nothing here authorizes CourtListener integration.** *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1.** Citation bank is registry/CourtListener-adjacent; **Q-6 posture untouched**; later slice.**
- ⬜ **FE-17 — Internal/outbound channel with render-time exclusion: a hard flag on every item, enforced by the renderer at the schema level and never by convention, so attorney-only rows physically cannot emit into an outbound instrument (live evidence: the deficiency grid contains impeachment material, a defense-favorable fact, and a verifying-officer credibility overlap that must NEVER render into an outbound letter or motion). Sub-question (deficiency capture §5 Q4): should the internal/outbound flag be enforced at the schema level — a renderer physically cannot emit internal items into an outbound instrument — and should the same mechanism serve the privilege firewall's genericization gate elsewhere? Second question riding here (§5 Q5): when a defined term in a propounded set draws a definitional dodge (the responder denies the term describes anything at its facility), should the software support a "stipulated definition" object that, once offered in a conferral letter, propagates into amended requests, the motion, and the proposed order automatically?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). §5 Q4 makes this **bigger than deficiency work** — the genericization gate is a privilege-firewall question. §5 Q5 lands here as the nearest render-layer home, **flagged for re-homing if the DE series grows a definitions object.** *(Gating deliberately unannotated — see the FE-gating item below.)*
  **#63 DISPOSITION (2026-08-12): **OUT of FE-D1 — but with a BINDING FROM-BIRTH ANNOTATION: it rides whichever slice creates the item table.** Recorded so the internal/outbound flag can never be retrofitted onto a table that already exists without it.**
- ✅ **FE-gating — CLOSED 2026-08-12 (#63): FE-13–FE-17 TAKE THE #54 TREATMENT**, ruled at the form engine's slice-scoping session exactly as FE-8–FE-12 were. The question was: do the five new FE items inherit the #54 gating, given that #54 was ruled the day before they existed? Answer: yes. All ten open FE items were dispositioned in that same session; the full table is in `docs/specs/fe-d1-build-slice.md`.
- ✅ **Distillation candidates — CLOSED 2026-08-12 (#63): all four ENTERED `form-engine.md` §13.4** — `template_petition_udja-uim-carrier.md`, `template_deficiency-letter_written-discovery.md`, `template_motion-to-compel_written-discovery.md`, `template_deficiency-grid_columns.md`. **Entering is not adopting:** each is `queued`, none distilled, none format-authoritative, and FE-7's guardrails hold — adoption is a separate explicit ruling of yours, and adoption is what flips a template's FE-12 provenance to format-authoritative.
- ⬜ **Placeholder discipline — a CARRY, not an open question (entered 2026-08-12, #60). The deficiency capture's REQ-10 dispositioned to a content route: placeholder inventory per document plus a fill checklist at delivery, into the form-engine spec as documented method** (live evidence: service dates of the propounded sets, conferral details, and the bar number were unknown to the drafting session, so drafts carried `{{snake_case}} `placeholders for the attorney to fill and file — already house practice in both practice-project sessions, now written down). **CONFIRMED AS CARRY by Michael 2026-08-12:** it rides whichever future session next amends `form-engine.md` — likely the engine's slice-scoping session. **Nothing is owed now, and the edit is design's, never Code's.** *(Registered here by the twenty-first invocation. The twentieth recorded the carry in the session log and BUILD-STATE but never gave it a queue row, so this is the row — reconciled, not duplicated.)*

### Contact directory (CD series) — ADDED 2026-08-08

**New heading, Code's call per the packet's §4.2** — the file already sections §7 by series (FE, PL), so a CD heading fits the structure rather than fighting it. Placed directly beneath the FE series because CD-1 supersedes FE-1's framing.

- ⬜ **CD-1 — One global contact directory as the identity source for the form engine and all case-linked people/entities; provider is a role on it. How does it relate to `case_parties` and `case_clients` — views over one directory, or separate tables with links?** Living spec — revisited as modules surface new field needs. Needs its own design session with schema on screen. NOT authorized for build. Issued 2026-08-08, superseding FE-1's provider framing. **Path RULED 2026-08-11:** the session fires after a BOUNDED evidence pass — opportunistic REQ-CAPTUREs continue, plus ONE deliberate roster-mining pass over the document bank (run in PI DISCOVERY, Michael's hand) — then the typed session with schema on screen. Prep scaffold: `docs/specs/cd-1-session-prep.md`. **CONSTRAINT (roster capture REQ-14, ruled 2026-08-12): sides are a property of the case type, not a constant — any roster model with exactly two sides fails on documents the practice demonstrably files (interpleader, certificates of interested parties, scire facias, ex parte petitions, ad litem joinder by court action). GATE MET 2026-08-12: the bounded evidence pass of #48 ruling 4 is complete — the roster-mining capture is reconciled and ruled; the schema session fires at Michael's choosing. SESSION QUESTIONS added from the roster capture, full text in `docs/specs/REQ-CAPTURE_roster-mining-pass_2026-08-11.md` §5.1 (probate roster modeling: defer entirely, or reserve now for the estate-representative/heirs pattern the PI documents already prove), §5.2 (non-party actors with roles in scope from day one, or parties-only first — the mined evidence says parties-only misses the people drafting revolves around; the gate finding, REQ-15), §5.3 (attorneys-of-record in the same directory with a role, or a separate structure), §5.5 (identity resolution across roles/cases/aliases at CD-1, or per-case identity for the first consumer).** **RULED 2026-08-11 — the schema session ran; every CD-1 question was ruled item by item. Spec: `docs/specs/contact-directory.md` (LIVING SPEC — revisited as modules surface new field needs; additions to its §5/§6 controlled vocabularies are spec-level acts). The core fork resolves as SEPARATE TABLES WITH LINKS, the shape already built: `parties` IS the directory, `case_parties` stays the roster link, `case_clients` stays parallel, D-CL2-8 untouched. Session questions are answered in the spec: §2 scope (non-parties in from day one; attorneys in the same directory, attorney is a role; probate reserves the PI-proven estate-adjacent pattern only), §3.3 identity resolution (none at CD-1 — cross-case identity is inherent in the one-global-row shape; merge-two-contacts is a named future need). BUILD STILL NOT AUTHORIZED — no table, no migration, no UI, no `party_type` migration. This entry stays open as the living-spec pointer.** *(The prep scaffold `docs/specs/cd-1-session-prep.md` was absorbed by the spec and deleted the same day, per its own delete-or-absorb line.)* **BUILD SLICE AUTHORIZED 2026-08-11 within the scope of `docs/specs/cd1-build-slice.md`** — seven IN items (parties evolution; `case_parties` evolution with flags-never-guesses backfill per the CL-2 pattern; roster definitions as data, seeded only for bank-evidenced case types; the contact-edges table; directory UI including the scope-label edit surface; RLS + probe extension from birth; live migration by Michael's hand). Explicit OUTs: form engine, IN-2 fact table, merge tooling, service-story fields, probate beyond the reserved pattern, the `/rules` seed. **The spec remains the design authority; living-spec posture unchanged; anything outside that slice's scope remains unauthorized.** The build is a separate Code session, not the queue runner.
- ⬜ **CD-2 — Case-type party rosters + typed contact relationships (the "family" layer). Each case type carries an expected roster of party roles presented as slots at intake — never auto-created records (trucking PI: carrier / driver / lessor / trainer / parent; premises: owner / occupier / management; probate: decedent / personal representative / heirs / creditors; criminal: defendant / complainant / prosecution) — and directory contacts carry typed edges (parent/subsidiary, employer/employee, spouse, heir-of). Role tags drive downstream content selection: the same RFA flips phrasing depending on whether the target IS the tractor owner or a sibling of it, so tags are template selectors, not labels. How do rosters attach to the case-type tree, and how do contact edges relate to the directory schema?** FRAMING RULED 2026-08-11 (general across practice areas — trucking corporate edges and probate family edges are one structure; roster mechanism belongs to the case-type model, not a trucking feature later abstracted). Design UNRULED — input to the CD-1 session. NOT authorized for build. Distinct from CL-1: `case_links` links cases; this links contacts — a CD-1 session names both so nobody merges them later. Evidence: REQ-CAPTURE 2026-08-11 (trucking first sets); probate is the second real consumer. **EVIDENCE LANDED 2026-08-12: the roster-mining capture (`docs/specs/REQ-CAPTURE_roster-mining-pass_2026-08-11.md`) — per-type constellations REQ-01–10, typed-relationship inventory REQ-11, capacity multiplicity REQ-12 (party = person × capacity), expected/optional/rare slot frequencies REQ-13, service-path-as-party-attribute REQ-19 (cross-link IN-4). TTCA roster default (attorney practice note, 2026-08-12): governmental body alone in nearly all cases; employee joinder deliberate and exceptional.** **RULED 2026-08-11 into the spec — BOTH layers. This entry becomes a pointer; the evidence annotations above stay.** Roster layer → `docs/specs/contact-directory.md` §4: slot definitions are data on the case-type tree with inheritance and expectancy tiers (expected / optional / rare-with-procedure), never auto-creating records; the roster entry decomposes into role / caption alignment (drawn from the case type's defined side set — REQ-14 satisfied by construction, "Plaintiff" is an alignment, not a role) / party status / firm perspective (the existing `side` column's true meaning); entries are history, not snapshot (joined-by + active state, because FE-8 and IN-4 both need roster-as-of). Relationship layer → §5: one directional typed edge structure with optional case scope (null = world fact), a controlled extensible vocabulary seeded from REQ-11, the one-home rule (capacity references stay on the roster entry and never auto-create edges), and the **CL-1 firewall named** — contact edges and `case_links` never merge, never share a structure, and neither ever holds the other's kind of link (the D-CL1 items stay untouched and unruled). **NOT authorized for build.** **EVIDENCE ADDED 2026-08-12 (#58), CD-1/CD-2 both, full text per QR-1 — two annotations, no new IDs:** (a) *registered agent as entity-level data* (UIM capture REQ-07, same home as roster REQ-19's service-path-as-party-attribute) — the service paragraph's agent name and address were inherited from an exemplar and had to be independently checked, so registered-agent name and address are stored per entity with a **verify-before-service state and a staleness date, never copied as prose**; (b) *verification-officer tracking* (deficiency capture REQ-11, priority someday) — **track who verified which response set (contact × role) and cross-reference against fact-witness roles**, live evidence being that the same officer verified interrogatory answers and personally conducted the investigation those answers denied, a deposition/credibility lever. (b) is carried at full length deliberately: it is the K-6/K-7 cheap insurance. **EVIDENCE ADDED 2026-08-12 (#62), from the Uvalde capture, no new IDs:** (c) *the county prosecutor roster* — contacts with roles, with collision-safe short codes, is directory substrate; the requirement itself is **CR-2**; (d) *contacts-at-capture is SATISFIED BY CONSTRUCTION* — "contacts with roles attach to the matter at capture" is the built directory plus roster link (log #61, items 1–2), so it took no ID at all; its print-forward half rides **CR-9**. **(e) ADDED 2026-08-12 (#64): carrier-ID and financial-responsibility TYPE codes from the CR-3 code sheet are directory-vocabulary CANDIDATES** — PROPOSED only, pointer at `docs/specs/cr3-field-code-map.md`. **Adding directory vocabulary is a spec-level act on the living spec (the CD-3 precedent), so nothing was added.**
- ✅ **`party_type` RETENTION — CONFIRMED by Michael 2026-08-12 (#62).** The CD-1 build **retained** `party_type` where the slice text said "retiring" it, and disclosed the deviation in its #61 handback. **Confirmed, not merely accepted:** dropping the column meant rebuilding registry field rendering, which is outside the slice's scope; role tags supersede it as the thing the app filters on; the tag vocabulary is **derived** from the party-type registry so the two cannot drift; and `role_tags[0]` mirrors it. `docs/specs/cd1-build-slice.md` carries a dated qualification note — **the original slice text stands; nothing was silently rewritten.**
- ⬜ **CD-3 — Insurer entity subtype (county mutual / foreign insurer / domestic company) as directory-level data, with a flag raised whenever the defendant slot changes and citizenship/diversity implications follow (live evidence: the exemplar's defendant paragraph pleaded Texas citizenship — accurate for its county-mutual defendant, inaccurate and removal-relevant for a foreign insurer in the new defendant slot). Sub-question (UIM capture §5 Q5): is entity-type-driven removal-exposure flagging — a foreign insurer in the defendant slot plus a relief bracket above the federal jurisdictional threshold — in scope for the software's deadline/strategy engine, or is that attorney-only analysis the software should merely annotate?** Entered 2026-08-12 (UIM-UDJA REQ-CAPTURE, priority soon). **Flag-only per registry discipline — the software annotates, the attorney analyzes.** Additions to `contact-directory.md`'s §5/§6 controlled vocabularies are spec-level acts under its living-spec posture, so the subtype vocabulary is not written by a Code session. **NOT authorized for build.**

### Intake pipeline (IN series) — ADDED 2026-08-11

*(New heading by the same structural logic as the CD heading, #39. FE-2's parked build home already names "the intake pipeline" as a candidate landing zone; IN-1 and IN-2 arriving makes it look like a real module — drift named, not decided. **Elicitation caveat, kept on the record: the IN-series naming rode inside ruling 1's "Yes on 1"; if Michael meant FE numbers instead, renaming is a one-word veto before these IDs get load-bearing.** Placement note — the fold-in said "after the FE series," but the file documents at the CD heading that CD sits directly beneath FE deliberately, because CD-1 supersedes FE-1's framing; the IN heading therefore lands after the CD series rather than splitting that documented adjacency.)*

- ⬜ **IN-1 — Answer-mining for discovery hooks: when a defendant's filed answer is ingested, flag affirmative pleadings and capacity denials that support contention discovery and suggest request stubs (live evidence: an alternative-benefits-policy assertion and a not-proper-party denial each generated a contention interrogatory + RFP + RFAs). Is answer ingestion a distinct pipeline step, and where in the UI do suggestions surface?** Entered 2026-08-11 (REQ-CAPTURE §5.3). FE-2's rulings carry as PROPOSED defaults: flags for Michael, never auto-add; one-click dismissal; dismissals remembered per case. Honest capability note: recognizing "this paragraph pleads an affirmative defense" is document understanding, not string matching — the Phase-1b local-LLM capability class, and the spec should say so.
- ⬜ **IN-2 — Crash-report field extraction + discrepancy flags: structured extraction of CR-3 fields (VINs, carrier identity, DOT number, owner/lessee, driver-license class codes) into the case record, plus automatic flags when petition allegations and crash-report fields diverge (live evidence: two real conflicts — incident time; pleaded vehicle ownership). Implies per-field SOURCE ATTRIBUTION on the case record ("this VIN came from the crash report; the petition says otherwise") — a data-model decision touching CD-1 (vehicle/instrumentality identity) and the intake spec, not a feature bolt-on. Where does source attribution live?** Entered 2026-08-11 (REQ-CAPTURE). Extraction half is Phase-1b-class ingestion (easiest possible target — fixed state form — but still PDF ingestion, GPU-arm gated). **EXTENDED 2026-08-12 (attorney-edit REQ-CAPTURE, ruled — no new ID, one home per question per the CL2-AC-1 dedup principle): generalized to source-fact pinning. Case facts used in any instrument are first-class data — {fact_id, value, source_document, source_field, extraction_method, verified_by_attorney} — referenced by merge fields, so every instrument can be re-verified mechanically and a corrected fact propagates to every instrument that uses it. Image-only sources (scanned forms with no text layer) take OCR or vision extraction with a confidence flag. Sub-question (capture §5.3): what confidence threshold or human-confirmation step gates a "fact verified" state, and should an unverifiable fact block the finalized state (interacts with IN-4's lifecycle)?** **HOME QUESTION RULED 2026-08-11 — `docs/specs/contact-directory.md` §7: source attribution lives on the CASE-SCOPED FACT TABLE ({fact_id, value, source_document, source_field, extraction_method, verified_by_attorney}), not on directory entries and not in a sidecar (FE-1c's silent-trust posture is preserved; a sidecar would erect a second provenance regime over data ruled to need none). Identity-class facts (a carrier's DOT number, a facility's legal name) stage on the fact table and promote into the directory entry ONLY by attorney verification — one direction of flow, no copies. Confidence scores order the review queue and drive display flags; NO threshold ever auto-verifies (registry discipline transposed to facts: automation flags, only Michael verifies), which answers the confidence half of the §5.3 sub-question. Discrepancy flags fall out of the shape — two sources asserting different values for one fact row IS the flag.** **STILL OPEN: the EXTRACTION half** — structured CR-3 field extraction remains Phase-1b-class ingestion, GPU-arm gated, and the question text above is retained in full because none of it was ruled. **The block-finalized sub-question ("should an unverifiable fact block the finalized instrument state") now RIDES IN-4** — deliberately not ruled in the CD-1 session. **EVIDENCE ADDED 2026-08-12 (#64): the authoritative CR-3 field/code inventory is filed at `docs/specs/cr3-field-code-map.md`** (synthesis, PROPOSED design input) **with the raw code sheet at `docs/reference/CR3-code-sheet-2023.pdf`** (Form CR-3CS 4/1/2023, governs wrecks 2023-forward). **The load-bearing requirement it carries: crash-report code tables are VERSIONED EXTERNAL VOCABULARIES.** The 2023+ sheet supersedes earlier sheets, so extraction must store **(code, decoded label, code-sheet version)** and the tables must carry effective dates — otherwise a 2022 wreck's codes decode against 2023 meanings and nothing signals it. The extraction design consumes this; **IN-2 stays queued and unauthorized.**
- ⬜ **IN-3 — Held sets with service triggers: a document/set status of HOLD with a defined trigger event (answer filed / appearance) that surfaces a finalize-and-serve prompt (live evidence: a full driver set drafted pre-appearance with placeholder COS). Trigger source PRE-ANSWERED for v1: MANUAL attorney input only, per the standing Q-6 posture — docket-watch is CourtListener integration, which is UNAUTHORIZED; the upgrade path routes through Q-6 whenever that is revisited, so this never becomes a second unguarded door into the same integration. What does the HOLD lifecycle look like in the document model?** Entered 2026-08-11 (REQ-CAPTURE §5.4). Cross-link 2026-08-12: IN-4 now holds the full instrument lifecycle; IN-3's HOLD/trigger stands separate as the pre-service fragment.
- ⬜ **IN-4 — Instrument lifecycle states: generated → delivered → attorney-edited → finalized → served, each with timestamp and file provenance; the certificate-of-service date is bound to the SERVICE EVENT — stamped at service time, never typed into the text — and drives downstream deadline computation (live evidence: a hand-filled certificate date across five documents goes silently stale if service slips a day). Sub-question (attorney-edit capture §5.4): how does the system handle mixed service across a document family (some recipients served one day, others later) — one certificate per recipient with per-recipient dates?** Entered 2026-08-12 (attorney-edit REQ-CAPTURE). Cross-links: IN-3 (HOLD + service trigger — the pre-service lifecycle fragment; stands separate); the queued deadline-engine input (per-defendant service dates within one wave, each response deadline computed independently); roster REQ-19 (service path as a party attribute).
- ⬜ **IN-5 — Disclosure-mining: when the other side's disclosures are ingested, extract the four categories the attorney reads them for — identities of responsible third parties alleged by the other side; identities of witnesses or persons with knowledge of the claims and defenses; insuring agreements; identities and/or production of documents that side intends to use in the prosecution/defense of their case — feeding the contact roster and the case record. Where do the extracted hooks surface, and how do they interact with IN-1's answer-mining suggestions?** Entered 2026-08-12, sourced to Michael's direct statement in the design session (not a REQ-CAPTURE). Sibling of IN-1 (same flags-only, never-auto-add defaults carry as PROPOSED). Cross-links: CD-1's non-party scope question (three of the four categories are non-party actors); the "who is whose insurer" relationship (roster REQ-06). Legal substrate: the expanded TRCP 194 registry entry (VERIFIED 2026-08-12). Document-understanding capability class, Phase-1b-adjacent, like IN-1.
- ⬜ **IN-6 — Response-set ingestion: parse a served response set into structured items (instrument type, number, request text, objections[], answer text) — the mirror image of the incoming-request ingestion requirement already observed on the responding side (live evidence: the opponent's response documents restate each request followed by objections and an answer, and building the deficiency grid required manually decomposing about 60 items).** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). This is **the substrate IN-1's answer-mining runs on** — without structured response items there is nothing for answer-mining to mine. Cross-links IN-1, IN-5. Entity shape observed by the capture: `response_set` {set_number, instrument_mix, served_date, verifier} and `response_item` {instrument_type: rog|rfp|rfa, number, request_text, objections[], answer_text, categories[], evidence_refs[], cure_demand, outbound: bool, cluster}.
- ⬜ **IN-7 — Cross-document contradiction detection: link answer assertions to contrary evidence in the produced record; even manual flagging with structured links would capture most of the value. Live evidence — the highest-value deficiencies were contradictions between the opponent's answers and its OWN production and disclosures: an answer denying any investigation while produced statement forms recite an investigation on their face; an admission denial contradicted by a same-day, same-declarant interrogatory answer; a persons-with-knowledge disclosure omitting people named in the opponent's own documents.** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority soon). **Manual structured links first**, per the capture's own instinct. Document-understanding capability class, Phase-1b-adjacent, like IN-1/IN-5; **flags only, never conclusions.**

### Deficiency enforcement (DE series) — ADDED 2026-08-12

*(**Series RULED into existence by Michael, 2026-08-12**, in the same group ruling that assigned the deficiency capture's durable IDs — the anchor item fits neither FE (not rendering) nor IN (not intake), which is the **WF-1 structural precedent**; a WF home was offered and declined. Placement is Code's call and is stated rather than buried: the file sections §7 by series, and DE lands directly after the IN series because **its own substrate is an IN item** — DE-1's item store is what IN-6's response-set ingestion feeds — so the reading order FE → CD → IN → DE follows the data, with WF/DL/PL/H after. **The #48 elicitation caveat applies to DE exactly as it does to IN: renaming is a one-word veto until these IDs get load-bearing.**)*

- ⬜ **DE-1 — Deficiency taxonomy as first-class data: a classification vocabulary applied per item — boilerplate objection (BP); answer given "subject to" objections (STO); objection-only with no substantive response (OO); evasive or incomplete (EV); contradicted by the responder's own documents (CONTRA); privilege asserted without the required withholding procedure (PRIV); and RFP responses stating none of the rule's permitted response forms — driving downstream rendering. It is the enforcement-side sibling of the objection library already observed on the responding side. ABSORBS the module's shape (capture REQ-04): one item store with renderers per instrument — internal working grid (all items), deficiency letter (outbound items, per-item cure paragraphs, doctrinal preamble, conferral designation and fuse), motion chart and proposed order (outbound items grouped by cluster, sustain/overrule column left blank for the court) — with clusters grouped by subject matter rather than seriatim, per the treatise's judge-facing advice. ALSO ABSORBS evidence pinning (capture REQ-08): item-level `evidence_refs[]` resolving to produced-document identifiers and rendered inline in letters and motions, because deficiency assertions gain force by citing the opponent's own production by Bates range. Three questions ride here — (§5 Q1) should the deficiency taxonomy (enforcement side) and the objection library (responding side) be modeled as ONE shared vocabulary viewed from two postures, since an objection pattern we assert when responding is the same object we attack when enforcing, or as two separate libraries that merely cross-reference each other? (§5 Q3) how should motion-chart subject-matter clusters be assigned — manually at enforcement time, or derived from topic tags attached to each request when the set was originally propounded, which would argue for tagging requests at drafting time in the propounding module? (§5 Q6) the treatise recommends multiple small motions to compel, one subject area each, over one omnibus motion — should the motion renderer support splitting one deficiency data set into several instrument instances, and how should the conferral certificate and exhibit lists be shared across them?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). **Series anchor.** Cross-links: **FE-6** (same one-item-model, many-renderings principle on the propounding side — **cross-linked, deliberately NOT merged**: different instrument classes); **IN-2** (source attribution, same family as `evidence_refs[]`). **Design substrate, reconciled as CONFIRMATION rather than collision: the parked discovery-deficiency engine (case-heartbeat walkthrough capture, Part 6 — the Bright treatise closed list, the TRCP 193.2(e) waiver linchpin, the thread-shaped workflow). The CLE treatise this capture's instruments used IS that same paper, and REQ-02's taxonomy is that closed list made data. DE-1/DE-2 are the parked buildout's durable HOME, not its green light — nothing here is authorized for build.**
- ⬜ **DE-2 — Conferral/escalation timeline engine: letter → cure deadline → motion → hearing → compliance deadline, computed as PROPOSALS for attorney verification and integrated with docket-control-order deadlines (live evidence: the letter carries a cure fuse of N days from the letter date; the motion's conference certificate must recite the effort; the agreed DCO imposes a discovery cutoff, a serve-by date of cutoff minus 30 days, expert deadlines, and mediation — and the enforcement track must land compelled production before expert workup needs it). Sub-question (§5 Q2): should the software track the opponent's response-service dates and automatically propose a conferral fuse and escalation timeline, as proposals for attorney confirmation, or should enforcement timelines be created only manually?** Entered 2026-08-12 (deficiency-handling REQ-CAPTURE, priority now). Shape observed: `escalation_timeline` {letter_date, cure_days, cure_deadline, motion_filed, hearing, compliance_deadline}, **all as proposals**. Cross-links: **DL-INPUT** (per-defendant service dates) and **IN-3** (HOLD / service trigger). **Q-6 posture untouched — triggers are MANUAL for v1; no docket-watch.** NOT authorized for build.

### Workflow channels (WF series) — ADDED 2026-08-12

*(New heading, Michael's ruling 2026-08-12: the item fits neither FE — not rendering — nor IN — not intake. Same structural logic as the IN heading in #48.)*

- ⬜ **WF-1 — Review-folder as a workflow channel: a per-matter inbox/outbox convention the software understands — where generated drafts land, where attorney-edited files return — with state transitions inferred from file arrival (live evidence: the attorney's working convention is a dedicated "drafts for review" folder, separate from the generated-output location).** Entered 2026-08-12 (attorney-edit REQ-CAPTURE, priority someday). Kin to the design-side email-workflow requirements doc (PROPOSED, un-packetized). Feeds IN-4's delivered → attorney-edited transition.

*(**WF-2–WF-8 ADDED 2026-08-12 (#63)** by Michael's group ruling adopting `docs/specs/email-workflow-requirements.md` into the repo. Adoption makes the catalog part of the record; **it builds, schedules, and authorizes nothing.** Each row carries its GATE — the thing that must exist or be ruled before the pipeline is buildable — so none can silently die, the K-6/K-7 insurance. The standing carry "email-workflow doc, PROPOSED and un-packetized" **CLOSES** with these.)*

- ⬜ **WF-2 — E-filing envelope lifecycle.** GATE: inbox-facing detection is gated on **T3** (KICK-1 governs, so T3 work is itself unauthorized until Michael locates or re-issues the kickoff doc). The envelope-number dedupe key is recorded as a **T3 design constraint**. Module design UNRULED.
- ⬜ **WF-3 — E-signature lifecycle.** GATE: **T3** for inbox detection; final-PDF capture needs the **document-storage model**, which does not exist — form-engine §10's OneDrive-plus-metadata pattern is the nearest precedent, not a decision. Module design UNRULED.
- ⬜ **WF-4 — LOP medical pipeline.** GATE: medical-tab **treatment records, not yet built** (form-engine §10's write-back names them as future); the approval-request queue **gates client care** and needs its own design pass. Cross-link: the medical module.
- ⬜ **WF-5 — Settlement pipeline. DOUBLY GATED.** GATE: **money machinery is deliberately unbuilt and UNRULED** — no settlement ledger, no trust/IOLTA, no liens, a standing BUILD-STATE line. WF-5 records the observed shape only; **nothing proceeds until Michael rules the money-module question first.**
- ⬜ **WF-6 — Criminal appointment pipeline. LARGELY COVERED ALREADY** — OAA intake Tier 1 is BUILT, and the **CR series** carries the rest: custody is **CR-5**, offers are **CR-8**, docket prep is the worksheet items. The intake spec's docket sections cover more. **WF-6's actual residue is the inbox-facing auto-intake half, gated on T3.** Cross-links recorded deliberately **so WF-6 never duplicates CR work.**
- ⬜ **WF-7 — Records retrieval.** Module design UNRULED; the request-aging / stall-alert shape is recorded. Clerk-fee cost tracking **cross-links the future money question — flagged, not assumed.**
- ⬜ **WF-8 — Service of process.** Cross-links the **CD-1-deferred service-story fields** ("land with the first instrument consumer") and IN-3/IN-4's service events. **WF-8's board is EVIDENCE FOR that deferred revisit, not a separate structure.** GATE: T3 for inbox detection of returns.
- ⬜ **Cross-cutting constraints on all future email/intake work (RULED 2026-08-12 as recorded constraints, not buildable items):** deadline extraction is the **highest-value function and leads any T3 scoping**; dedupe keys (envelope number, claim number, document name) are **T3 design constraints**; a **hard ignore layer precedes any triage feature**; and — recorded with rule force — **client emails get human routing, NEVER templated automation.**

### Deadline engine (DL series) — ADDED 2026-08-11

*(Placement is Code's call and non-obvious, so it is stated: §7 carried no deadline-engine design-input home — §3 above holds TRCP **verification** items, which is a different act — and the file sections §7 by series. A DL heading here matches that structure rather than burying a data-model fork among housekeeping items.)*

- ⬜ **DL-INPUT (2026-08-11) — Per-defendant service dates within one discovery wave: six documents in one wave may carry six certificate-of-service dates, and each defendant's response deadline computes independently from its own service date. Is the deadline model per-(case, party), not per-case? A data-model fork the deadline-engine design pass must take deliberately; nothing in the heartbeat/deadline docs rules it either way.** From REQ-CAPTURE §5.5. Response-period substrate (TRCP 197.2(a), 196.2, 198.2, deemed admissions) VERIFIED by Michael 2026-08-11.

### Probate ladder (PL series) — ADDED 2026-08-07 (evening)

**All four ISSUED UNRULED and DEFERRED** with the probate-ladder design pass itself (see the PR-3 annotation at §2). The deferral is deliberate and Michael's: the Domser matter is the validation instrument, and these get answered against a lived arc rather than the treatise. First evidence arrives at the LETTERS capture. Full reasoning: `rulings-capture-2026-08-07b.md`.

- ⬜ **PL-1 — Model shape for probate: does PROCEEDING = CASE TYPE under a Probate practice area (each proceeding its own type + ladder, shared intake front-segment, mid-matter changes via the existing review-logged case-type change), or is it ONE "Decedent's Estate" case type with a proceeding ATTRIBUTE keying the ladder?** Sub-questions: is heirship a conditionally-present phase of intestate independent administration (with standalone Heirship Determination reserved for no-letters title clearing)? Is temporary administration an overlay object rather than a case type? **Claude's recommendation for proceeding-as-case-type is on record as PROPOSED ONLY** — it is not a ruling and must not be built from. **OPEN — deferred pending Domser arc evidence.**
- ⬜ **PL-2 — Which proceedings get DRAFTED ladders when the pass runs, versus stubs?** Independent administration is the ruled spine (PR-1); do muniment, standalone heirship, and small-estate ladders draft in the same pass, or defer until one of them is live? **OPEN — deferred with the pass.**
- ⬜ **PL-3 — Independent-administration ladder content: which of the Ch. 7 arc's stations are LADDER STATUSES versus checklist/tracked-object material?** To be answered against the Domser lived arc, not the treatise alone. **OPEN — deferred; first evidence at the letters capture.**
- ⬜ **PL-4 — Ladder / deadline-engine boundary: the ladder stays phase-shaped while every probate deadline enters the registry UNVERIFIED as its own batch** (planned in the design, per `spec-feedback` item 1) — **confirm that boundary and the registry-batch scope when the pass runs.** **OPEN — deferred with the pass.**

### T3 / Phase 0 advisory (H series — ADDED 2026-08-09)

**ID COLLISION, FLAGGED AND DELIBERATELY NOT RENUMBERED.** These arrived from the 2026-08-09
design-side advisory packet lettered H1–H6, which **collides with the case-heartbeat register's
H1–H83** (H8, H22, H35–H83 are cited throughout this file). Renumbering an attorney's items is not
Code's call, so they keep their packet IDs inside this labeled subsection and the collision is
stated instead — the same disposition as the standing §2-O1 / O-series-O1 collision. **When citing
one, name the series.** **H3 (paste-state of the audio-row exception ruling) is CLOSED** per the
entry's 2026-08-10 amendment — the ruling was confirmed pasted into the running T3 session — and
appears in no table.

- ⬜ **H1 — file the npm-advisories spec-feedback item:** "5 advisories (1 moderate, 4 high) on
  fresh install, P1, 2026-08-08; advisories sit in dev-dependency chains of a lockfile green at 232
  tests on 07-28; triage as a routed task on a machine that can test the result, **not** auto-fixed
  via `npm audit fix`, which can silently desync the machine from the tested lockfile."
  **Does Michael rule to file it?** PROPOSED, unruled.
- ⬜ **H2 — Fable 5 is now standard on Max** (banner 2026-08-09: up to 50% of weekly limit on
  Fable, faster drawdown than Opus). **The MODEL USAGE section's economic assumptions reference the
  old terms — revisit in a design session.** Trigger #3 does not fire (external pricing change, not
  a convention change). OPEN — design session. *(Adjacent but distinct from **Q-5**, below, which is
  the unruled model-usage clause itself.)*
- ⬜ **H4 — stage the 13 pilot recordings into `..\data`** (create the directory at staging time,
  **outside the repo tree**); a session then re-runs the audio preflight row and **Stage 1 proceeds
  under the ORIGINAL 2026-08-07 authorization — no new ruling needed.** OPEN — Michael's hand.
  *(This is the single thing standing between the P1 environment and Stage 1 scoring; the audio row
  stays RED and the preflight stays NOT-passed until it is done.)*
- ⬜ **H5 — adopt as a standing rule: "preflight rows about Michael-supplied material are answered
  by Michael, not by searching his machine"?** Candidate home: a rules doc, Michael's pick.
  PROPOSED, unruled. *(Origin: a proposed recursive `$env:USERPROFILE` sweep for audio files was
  refused design-side — filenames alone can carry client information, so the listing is itself an
  exposure.)*
- ⬜ **H6 — adopt the raw capture (`claude_T3_Kickoff_Day_Capture_2026-08-09.md`) into the repo** at
  `docs/specs/t3-kickoff-day-capture-2026-08-09.md`, status line *"RAW CAPTURE — design-side
  advisory session, 2026-08-08→09; not canonical, not a design doc, not in the build queue"*?
  **If yes it ships in a later packet; if no, the session-log entry is the sole record.** OPEN —
  Michael. *(The file is NOT in the repo and was deliberately not created — see log #44's runner line.)*

### Process, tooling, and housekeeping

- ⬜ **O5 — half-answered, needs a yes/no.** `direction` / `conditionalDowngrade`: **absorbed into the ruled fee-profile shape, but the substance was never ruled — confirm or reject it.** **Deferred 2026-08-07 by a design ruling of scope: not to be ruled from memory; requires the fee-profile doc on screen.** Stays OPEN.
- ⬜ **Q-5** — the model-usage clarifying clause. Drafted at `model-routing-plan.md` §7.2; **the memo does not close it.** Ruling it fires instructions trigger #3.
- ⬜ **Q-6** — **FLP internal-tooling terms.** Research use is within bounds; wiring the CourtListener API into the app is **UNAUTHORIZED** until Michael resolves it with FLP directly.
- ⬜ **PR-3** — the mis-parented `Probate companion` case type (also at §2 above, where the 2026-08-07 direction-confirmed / execution-held annotation lives).
- ⬜ **RE-1** — referral engine (also at §2 above).
- ⬜ **M-3** — Medchron fictional-content check. ⬜ **M-4** — **LegiScan key rotation after the T3 build (firm).**
- 🟡 **GPU telemetry posture — RULED 2026-08-12 (#63), open only for your confirmation. The question was:** NeMo pulled `wandb`, `sentry-sdk`, and OneLogger onto the machine that will process **privileged audio** (smoke test run 08-09, tone only) — what is the posture? **RULED: OFFLINE LOCKDOWN.** The packages stay installed but are **forced offline machine-wide BEFORE any privileged audio is processed.** Recipe and verification steps: `docs/gpu-telemetry-offline.md`. **The variables get set on the GPU machine by your hand — never from a Code session** — and this **closes fully only when you report verification 3 passed.** *(Registered here 2026-08-12 (#63). **FINDING: this item had NO ROW in this file** — it lived only in BUILD-STATE's design-side section, so the packet's "annotate the telemetry row" instruction had no row to annotate. Same class as the REQ-10 finding at #60: an item tracked in BUILD-STATE but never registered in the register. Recorded with full question text per QR-1 so it cannot die the K-6/K-7 death.)*
- ⬜ **Registry verification backlog — NEW 2026-08-12 (#58), WIDENED 27 → 33 on 2026-08-12 (#62).** **The six added entries are criminal**, in a third registry file, `docs/specs/legal-rule-registry-criminal-plea-and-costs.md` (ALL UNVERIFIED), opened because no existing file's subject honestly covers criminal offence classification, plea procedure, and costs — the #50/#58 placement reasoning. **Two of those six carry NO CITE AT ALL**: the capture stated them bare (degree-of-offence/punishment correspondence, and per-cause non-concurrent court costs), and **supplying a cite is a verification act, so it is Michael's, not Code's** — they were copied exactly as stated and nothing was researched or filled in. **These six hard-gate CR-10**, the plea-paperwork signing checklist: no computed check runs until they are verified. Original twenty-seven below. **Twenty-seven entries went into the registry UNVERIFIED in the #58 batch and await Michael's verification, one by one or batched at his direction.** Home: `docs/specs/legal-rule-registry-discovery-enforcement-and-pleading.md` (all entries UNVERIFIED, by construction; the file states why it is a new file rather than a fold-in). **PLACEMENT FLAG CLOSED — CONFIRMED by Michael 2026-08-12** (post-run review of the twentieth invocation; see #60): the new-file deviation stands, for the honest-status-header reason, split later if volume warrants. **That closes the placement question only — the twenty-seven entries themselves remain UNVERIFIED and are still your homework.** *(Note on where this closed: no standalone placement row ever existed in this file — the flag lived in the registry file's header, in BUILD-STATE, and in the twentieth invocation's handback. It is annotated here rather than given a row of its own, since the row would exist only to be closed; the #57 disposition.)* Composition: **twenty-two from the deficiency-handling capture** — the largest single batch since trucking — covering discovery scope, response completeness, objection specificity and waiver, permitted RFP response forms, contention discovery, sanctions and expenses, the conference requirement, and nine supporting case propositions; and **five from the UIM-UDJA capture** — CPRC § 37.001 et seq., § 37.009, § 15.002(a)(1), TRCP 47(b)–(c), and *Allstate Ins. Co. v. Irwin*, 627 S.W.3d 263 (Tex. 2021). *(Count note: the packet estimated "~26 propositions" for the deficiency batch; the capture's §4 carries 24 bullet lines, of which 3 deduped onto existing VERIFIED entries and one line split into two distinct cites — 22 entries. The estimate and the count are both right about different things.)* **Two reads specifically flagged for Michael:** (a) the existing VERIFIED TRCP 194 entry's expanded four-category wording versus the UIM capture's narrower "194.1–194.4" span — same entry, two wordings, and verification attaches to wording; (b) **six** WL/slip cites in the deficiency batch flagged for reporter-cite check at verification time (the packet named five — Volt Power, Sting Soccer, Ochoa, Redman, Collins — and *De Anda v. Jason C. Webster, P.C.*, 2018 WL 3580579, is a sixth carrying the same defect; flagged, not corrected). **Correcting a slip cite to a reporter cite is Michael's verification act, not Code's** — nothing was changed. Blocks **FE-14**, whose picklist wording cannot be encoded until TRCP 47(b)–(c) is verified.
  - **Cite-check memo filed 2026-08-13 (#65)** at `docs/specs/registry-cite-check-2026-08-13.md` —
    the six slip-cite checks and the two cite-less criminal entries each reduced to one look; see
    the memo's §3 priority list. Research support only; every verification act remains Michael's.
- ⬜ **V-2 — NEW 2026-08-13 (#65). The cite-check memo's one-look list — six looks, in priority
  order.** The question: **for each item below, does the flagged check close, and with what text?**
  (1) **Ochoa** — pull 2004 WL 1192444 on Westlaw; the header shows the S.W.3d parallel cite if one
  exists, and the opinion's **"(PUBLISH)" notation says it plausibly does** — the single
  highest-value look of the six. (2) **Collins v. Kappa Sigma Fraternity, 2017 WL 218286** —
  retrieve it (not in FLP, not on the public web); **beware the 2010 sibling appeal**,
  No. 02-09-00305-CV, same litigation. (3) **Sting Soccer** — confirm the Nov. 30, 2017 opinion's
  designation (memorandum vs. published). (4) **Volt Power / Redman / De Anda** — confirm each WL
  number maps to the substantive opinion named in the memo; then **the "no reporter cite exists or
  will exist" closure is yours to record**; for Volt Power and Redman also decide whether the
  dismissed-as-moot subsequent history merits a parenthetical, and for De Anda confirm "pet.
  denied." (5) **Criminal entry 3** — verify Penal Code §§ 12.34/12.35 plus *Mizell v. State*,
  119 S.W.3d 804, 806 (Tex. Crim. App. 2003), as the cite set. (6) **Criminal entry 4** — **rule on
  the wording split under Tex. Code Crim. Proc. art. 102.073 BEFORE any cite is supplied**: the
  statute's verbatim text cuts against the carried "per cause, non-concurrent" proposition for
  causes disposed in a **single criminal action**, where each cost is assessed only once at the
  highest offense category; read *LaPorte* / *Hurlburt* if wanted. Evidence staged at
  `docs/specs/registry-cite-check-2026-08-13.md`. **Nothing in that memo is verified and no
  registry entry was touched** — supplying or upgrading a cite is your verification act alone.
- ⬜ **V-1 — NEW 2026-08-13 (#65). Instructions question arising from the Descrybe ruling.** The
  question: **record the Descrybe exclusion as session-level tool routing only, or bump the
  instructions to v17 now — folding in the two already-queued touch-ups** (the "BS-1 provenance"
  phrase; the pre-Cowork "Claude cannot write project docs" line)? Michael ruled 2026-08-13, in his
  own words, that **Descrybe is not used in this project at all until further notice** (revisit at
  go-live if still useful); FLP/CourtListener plus named public sources is the case-retrieval path.
  **His call, one word.** Code must not draft the revision.
- ⬜ **V-3 — NEW 2026-08-13 (#65). PROBATE practice-project creation + CRIM DEFENSE paste-state.**
  The question: **are both now done — the PROBATE practice project created (v1 instructions + the
  carry snippet into each of the two grandfathered matter workspaces), and the Uvalde instructions
  plus v1 confirmed pasted into CRIM DEFENSE?** Both were reported done on 2026-08-12 by his own
  statement, but **neither is visible to Code**, so the record cannot close itself. **One word in
  any session records both CONFIRMED.**
- ⬜ **KICK-1 — NEW 2026-08-09. LOCATE `KICKOFF-phase0-t3-p1-session_2026-08-08.md`.** Absent from
  the P1 inbox; **never tracked by git** (`git log --all -- 'inbox/*'` returns zero commits, and
  `inbox/` is gitignored), so **the loss cannot be dated or recovered from history**. #42 recorded
  the Phase 0/T3 authorization as **PARTLY SPENT**, and **this document is the authoritative text of
  that authorization and its stage definitions.** **Until it is located — or Michael re-issues the
  remaining authorization in his own words — further T3 work is UNAUTHORIZED:** §5 discipline
  requires quoting Michael's explicit authorization, and the quoted source is missing. Likely
  locations: Michael's Downloads on either machine; the 2026-08-08 design chats; partial
  reconstruction from `docs/specs/rulings-capture-2026-08-08*.md` and #42's log entry. **It is not a
  packet and must never be processed as one.** **Michael's.** *(ID is new and collision-checked —
  **not** a K-series number; **K-6/K-7 are RETIRED and must not be reused.**)*
- ✅ **K-5** — Uvalde docket worksheet instructions: home undecided. **CLOSED 2026-08-11 (RULED): home is the CRIM DEFENSE practice project's knowledge, uploaded by Michael's hand — never the repo. Ruled in the same session that created the CRIM DEFENSE practice project (all criminal-side practice work; REQ-1 extended; instructions v15). See #54.**
- ✅ **GH-1 — The two real prior-case identifying strings generalized out of `form-engine.md` §8 by the FE-3 ruling (#55) survive in git history: prior commits of the file still contain them. Rewrite history to purge them, or accept?** **RULED 2026-08-12: ACCEPTED — history stands.** Reason: a rewrite changes every commit sha (invalidating the shas the session log cites as record), forces re-clones on both machines and a design-side re-sync, with MM-1 coordination throughout; the repo is private and the strings are of modest sensitivity, absent from the working tree since `cfa1692`. **TRIPWIRE, standing: REVISIT before repo visibility widens** — multi-user phase (gate 2 / instructions trigger 2), any outside collaborator, or the repo going non-private. Pointer at gate 2 per same-day ruling. See #56; dialogue trail in the project-knowledge capture. *(New ID 2026-08-12; collision-checked at reconciliation per the K-6/K-7 rule — no GH series existed anywhere in this file or the session log.)*
- ✅ **QR-1** — QUEUE-RUNNER merges packet open items down to ID + label and then deletes the packet, **losing the question itself** (this destroyed Q-5's original wording). **CLOSED 2026-08-07: ruled yes.** The runner carries FULL question text into this file when merging packet open items; ID+label-only merges are barred. `QUEUE-RUNNER.md` and `.claude/commands/queue-runner.md` went to **v3** in the same batch. Fires instructions trigger #3 → v6 drafted.
- ✅ **QR-2 — PROPOSED, unruled (2026-08-07). Should the machine-local queue-runner skill/command copies stop carrying the runner's full text and become POINTERS — "read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it" — so that a stale local copy is structurally impossible?** Evidence: the machine-local copies have now loaded **stale twice in two consecutive runs** (#35 loaded v1 against repo v2; #36 loaded v1 against repo v3), and **both times only the runner's own vigilance caught it.** The repo copy is already canonical; this makes the invocation defer to it. **Cost:** one extra file read per invocation. **Rejected alternative, and the reason it is rejected:** keeping full text locally and re-syncing by hand IS the observed failure mode. **Scope note (verified from Code 2026-08-07):** `.claude/commands/queue-runner.md` is **tracked and current at v3** — it updates with the repo and is not the problem; the lag lives in the **untracked machine-local skill copies**, of which there are now at least two (P15, P1). **Michael's.** **CLOSED 2026-08-08: ruled YES** ("QR-2 - yes"). Copies become pointers — *"read `docs/prompts/QUEUE-RUNNER.md` at HEAD and follow it"* — and that file is now the ONLY full copy. Decisive evidence: #38 was the **third consecutive stale-skill run** (local v1 against repo v3) and cost nothing only because the packet and the design reply independently supplied both deltas — luck standing in for process. `.claude/commands/queue-runner.md` was rewritten to pointer form the same day; **the P15/P1 machine-local copies are Michael's hand.** Fires instructions trigger #3 → v7 drafted. Full reasoning: `rulings-capture-2026-08-08b.md` Part 1.
- ✅ **QR-3 — the runner's pointer reads `docs/prompts/QUEUE-RUNNER.md` at HEAD, but HEAD is whatever the local checkout happens to be; a stale tree serves stale runner text with no signal (demonstrated 2026-08-08: v1 read as current, 14 commits behind). Should Step 0 gain a gate — fetch origin and confirm the checkout is at origin HEAD before reading the runner; if behind, sync or stop?** CLOSED 2026-08-08: ruled yes, sync-and-proceed shape — behind-but-clean-on-master fast-forwards and continues; dirty/diverged/off-master stops. Runner to v4. See `rulings-capture-2026-08-08c.md`.
- ✅ **QR-4 — PROPOSED, unruled (2026-08-10). Step 1 of QUEUE-RUNNER.md orders packets by file modification time, but inbox mtimes are SAVE times, not session times, and can invert authoring order — demonstrated 2026-08-09/10, when the later-authored t3-kickoff packet (filename 2026-08-10) carried an EARLIER mtime than the earlier-authored slack-claude-tag packet (filename 2026-08-09), because it was saved into `inbox/` first. Order is semantic, not cosmetic: the conflict rule ("later packet wins") and the session-log append order both turn on it. Should Step 1 be amended to: sort by filename date, oldest first, with mtime as tiebreak and as fallback for undated filenames (flagged explicitly); ALWAYS compute both orders and print both whenever they disagree — never silently pick one; and, after unzipping, cross-check the confirmed order against each manifest's §3 entry date, stopping on contradiction? The Michael-confirm gate stays exactly as is — this changes the inferred order and makes disagreement visible; it does not remove the stop. Cost: a few lines of runner text. Rejected alternative — keep mtime primary: rejected because the 2026-08-10 inversion was caught only by the running session noticing the disagreement unprompted; the gate showed one inferred order and nothing forced the conflict into view. If ruled yes: runner v5 → v6; fires instructions trigger #3 → v10 draft. Michael's.** **CLOSED 2026-08-10: RULED ADOPTED** as a standing amendment to Step 1 — filename date primary (oldest first), mtime as tiebreak and fallback for undated filenames, both orders computed and printed whenever they disagree, and the confirmed order cross-checked against each manifest's §3 date. Ruled during the NINTH queue invocation, where the same mtime/filename-date inversion recurred — the second consecutive batch requiring a hand ordering call. `docs/prompts/QUEUE-RUNNER.md` went to **v6** the same day; per QR-2 the tracked command file is a pointer and carries no version text, so it was not edited (see the runner entry's flag). Fires instructions trigger #3 → v10 drafted design-side 2026-08-10. Reasoning: `rulings-capture-2026-08-10.md` §1.
- ✅ **MM-1 — with both machines (P15 + P1 Gen 8) in concurrent two-workspace use, what does concurrency itself require beyond the standing cross-machine rules?** CLOSED 2026-08-08, ruled yes to all four parts: one-runner-at-a-time with the non-FF-stop backstop; inbox-bound destinations name the machine; user-level runner copies deleted (not converted — shadowing risk); bootstrap addendum (git identity + deletion step). Runner to v5. See `rulings-capture-2026-08-08d.md`.
- ⬜ **Interim mitigation, regardless of how QR-2 is ruled — Michael's hand, outside the repo:** re-sync the P15's local skill copy to v3, and **check the P1's copy before its first runner session** (same lag class, third copy). **SUPERSEDED 2026-08-08 by QR-2's ruling: CONVERT each machine-local copy to pointer form, do not re-sync full text anywhere.** **SHARPENED AGAIN 2026-08-08 by MM-1(3) — the operative instruction is now DELETE, not convert:** delete the user-level `%USERPROFILE%\.claude\commands\queue-runner.md` on BOTH machines — a user-level copy can *shadow* the repo pointer and feed stale runner text, and the repo-tracked pointer travels with every clone, so it suffices. QR-3's checkout gate travels to each machine automatically once that machine relies on the repo pointer. **NARROWED 2026-08-09 (verified from the P1, log #42, re-confirmed by the 08-10 packet): the P1 half needs NO ACTION — that machine has no user-level copy. ONLY THE P15 DELETION REMAINS.** Still Michael's hand, still outstanding.
- ⬜ **Q-3 RE-CHECK — Michael's hand, one click, after the 2026-08-10 push: does the GitHub sync picker include `docs/skills/`?** That directory is NEW as of 2026-08-10 (it holds `drafting-disclosures/SKILL.md`, landed this batch). Per Q-3, the project-knowledge selective sync may not auto-include newly created nested directories, even though the sync scope nominally carries all of `docs/`. **If it is excluded, the skill never reaches the design side** — and the design side is exactly where the skill runs, since disclosure drafting happens in design-space chats until the form engine exists as software. Verifiable by Michael only: Code cannot see the sync picker. *(Entered 2026-08-10 with full text per QR-1 — it had never been registered here, only in the 08-06 packet's §7 table.)* **EVIDENCE 2026-08-10, and it points toward CLOSABLE:** the drafting-disclosures skill was read and executed from the HEAD copy in a design session that same day — meaning `docs/skills/` reached the design side. That is evidence, not a ruling; **Michael closes it.** **PAIRED RE-CHECK ADDED 2026-08-11: `docs/templates/`** is new this batch (holds `discovery/template_definitions-instructions_requests.md`) and needs the same one-click confirmation in the sync picker; if the picker excludes newly created nested directories, the house templates never reach the design side either.
- ⬜ **SK-v2 — the `drafting-disclosures` skill's v2 revision.** Per the ruled upgrade protocol the spec fold-in comes FIRST (landed 2026-08-11 at `form-engine.md` §§12.7–12.13); **a design session then revises `docs/skills/drafting-disclosures/SKILL.md` to v2 citing the spec at HEAD, with a version bump.** Sequenced, not blocked — needs a design session. *(Code must not edit the skill; the 08-10 packet's §6 bars it.)* **v2 AUTHORED AND LANDED 2026-08-11** — the revision is a design-session deliverable per the ruled upgrade protocol (spec fold-in first, at `form-engine.md` §§12.7–12.13; then the design-side revision citing the spec at HEAD `13f736a`), so the queue runner landing it is routing, not editing, and the Code-must-not-edit bar is intact. v2 folds attribution verification + the imaging-entity question (§12.7), operator-local service dates (§12.8), style-by-role assertion with the step-1 style harvest (§12.9), caption label style (§12.10), block keep-together in the ship gate (§12.11), served-final-as-next-shell (§12.12); the provider-data block's purpose line now names the contact directory as its consumer (FE-1 scout → CD-1 seed evidence). **REMAINING, and why this entry stays open — Michael's hand, now actionable:** **upload `docs/skills/drafting-disclosures/SKILL.md` (v2) as the claude.ai skill copy now that the revision is at HEAD.** *(This absorbs the standalone "Hand-upload of `drafting-disclosures` as a claude.ai skill" item carried from #45 — still open, a `.skill` packaging was offered in-session and not acted on — consolidated here per QR-1's one-home rule; text moved, not deleted.)*
- ✅ **v14 INSTRUCTIONS PASTE — Michael's hand, same day per instructions trigger #3: paste project instructions v14 into the project's instructions field.** v14 carries **DT-1**, the date-stamp convention ruled 2026-08-11 Central and registered in log #50: *every date a design-side session stamps — session dates, ruling and verification dates, "Entered" lines, filename dates on packets and captures — uses Michael's wall-clock Central date, never the container's UTC date. The container rolls to the next date at 19:00 Central (daylight) / 18:00 (standard); sessions running past that hour check before stamping.* The draft was handed to Michael the evening of 2026-08-11. **Until the paste happens the convention is recorded but not in force for future design sessions** — the log entry binds nothing on its own. *(Entered 2026-08-11 with full text per QR-1. Code verifies nothing about the paste; instructions are Michael's hand entirely.)* Context: DT-1 removes the cause that QR-4 handles as a symptom — the same container-UTC failure class produced QR-4's filename/mtime inversions and #47's certificate-of-service date defect. **CLOSED: paste VERIFIED design-side 2026-08-11** — the CD-1 schema session read the live instructions field at v14 at session start and recorded it in its entry (log #51); **DT-1 is in force.** First in-force application caught its own target failure class live in that session: chat confirmations stamped 08-12 from container UTC until the pre-authoring clock check (2026-08-11 21:38 CDT); no artifact was mis-stamped. *(Code verifies nothing about the paste itself — this closure rests on the design-side session's own verification, cited per this entry.)*
- ✅ **NEXT BUILD SLICE — which slice does Michael name as the next build target?** The build queue has been empty since CL-2, and the question has ridden in session-log §7 tables since #31 ("Next build slice — OPEN, Michael names it") **without ever being registered in this file** — entered here 2026-08-11 with full text per QR-1 when the CD-1 packet's routing looked for it and found nothing. **OPEN BY RULING 2026-08-11: naming is DEFERRED** — Michael ruled that the next slice is named in a later design session **citing `docs/specs/contact-directory.md` at HEAD**, not in the schema session itself. What changed with that spec landing (statuses, not rulings): **the form engine is again nameable** — its identity source is resolved, FE-4–FE-7 unblock for spec completion, and FE-2's parked build home can be raced honestly. Code must not name a slice; nothing enters the build queue without an explicit authorization packet in Michael's words. **Michael's, next design session.** **CLOSED — RULED 2026-08-11: the CD-1 directory build, scoped and authorized; text at `docs/specs/cd1-build-slice.md`.** The deferral's own condition (name the slice in a later session citing `contact-directory.md` at HEAD) was satisfied the same evening, after the thirteenth queue invocation landed the spec at `13f736a` and the design side verified the sync. Reason recorded: substrate first — the form engine consumes the directory and its own spec is incomplete (FE-4–FE-7 are spec-completion work). Anti-resurrection check run deliberately and on the record: **NOT Slice A returning** — Slice A was the provider-level framing, withdrawn 2026-08-08 for baking the wrong level; this builds the ruled level. Michael fires the build session; see log #52.
- ⬜ **Queue-wide duplication sweep — does any OTHER item live twice?** Raised 2026-08-08 by the CL2-AC-1 finding (that item lived in two places in this file until the same-day dedup ruling consolidated it). **PROPOSED, unruled. Michael's.** *(Wording carried verbatim from the 08-08b packet's §7 per QR-1 — it had never been entered in this register, only in the packet table.)*
- ⬜ **Cross-document status-drift sweep — do any design-doc status rows or tables state a ruling status that lags the queue/log record?** Raised 2026-08-08 by the §10 CL2-AC-1 row found stale (still "PROPOSED, unruled" after the DIRECTION-CONFIRMED ruling). Distinct from the queue-duplication sweep item; both PROPOSED, unruled. **Michael's.**
- ✅ **RR-1** — re-read documents authored earlier in a session against rulings made later in it, before the packet ships. **CLOSED 2026-08-07: ruled yes; now a binding convention** (sending-side mirror of RECONCILE FIRST). Live exhibit: FE-3's shell content, which the rule would have caught. Binds Claude, not Michael. Fires instructions trigger #3 → v6 drafted.
- ⬜ **SAT-1 — PROPOSED, unruled (2026-08-07). Adopt the SATELLITE-PROJECT PATTERN?** Two parts. **(a) The criteria:** a piece gets its own satellite project when it needs a large specialized corpus, confidentiality-segregated material, or a sustained observation/research function; a satellite HOLDS knowledge and produces index/synthesis/captures that travel **by Michael's hand**. **(b) The ONE-RULING-SPACE principle:** satellites never rule; nothing enters the build queue from a satellite; the log, the queue, and packet issuance stay singular in the design project; ordinary module design stays in the design project. Each satellite gets Domser-template instructions at birth. Adopting this fires the instructions trigger for a satellite inventory at the first NON-Domser satellite. **Michael's — do not treat as ruled.**
- ✅ **CORPUS-HOME — PROPOSED, unruled (2026-08-07). Final home of `probate_knowledge_corpus.md`: the DOMSER PROJECT?** This **supersedes the #34 ARCHIVE recommendation**. Rationale offered: Domser is the one place Part III privilege is load-bearing, the corpus is core knowledge there rather than dilution, and licensed material stays inside one privileged workspace. **Until ruled, the ARCHIVE recommendation stands and `Probate Corpus.zip` stays UNTOUCHED in `inbox/` per the #31 ruling** — not deleted, not extracted, not moved, not diffed. **Michael's.** **CLOSED 2026-08-08: the ARCHIVE project.** (Michael: "Yes." — the DOMSER proposal above was not adopted; the #34 ARCHIVE recommendation became the ruling.) Execution Michael's hand — an upload into that project's knowledge, not a repo action; the corpus never enters the repo regardless (licensed James Publishing / LexisNexis; Part III privileged). `Probate Corpus.zip` stays untouched in the P15's gitignored `inbox/` until that upload; deleting it afterward is Michael's step. See `rulings-capture-2026-08-08.md` Part 6.
- ⬜ **O1 (transcript sort & route) — auto-file posture: confirm or veto D1** (confirm-only in v1; auto-file deferred until the decision log shows real precision). **Gates nothing** — the build is confirm-only either way. Note added 2026-08-07: the **Phase 0 scorecard is the evidence base this decision was waiting on**, so it stays open until that scorecard exists. **Carried.**
- ⬜ **O3 (transcript sort & route) — where do NOT-case-related recordings go — a personal store, or discard-with-log?** **Carried, open.**
- ⬜ **O4 (transcript sort & route) — phone→PC sync channel at go-live: OneDrive Shortcut, iCloud folder, or stay-manual?** **Carried, open; deferred by D2** (interim capture is manual attach from phone). *(ID note: §2 above uses "O1" for a closed Prop. Code ch. 28 fee question — different series, same letter. Disambiguated here rather than renumbered.)*
- ✅ **O2 (transcript sort & route) — P1 OS at purchase: native Ubuntu vs. Windows+WSL2? CLOSED 2026-08-07 (Michael: "windows only")** — Windows host, WSL2 runtime. Native Ubuntu and dual-boot rejected; NeMo has no native Windows support, so WSL2 with CUDA passthrough is the runtime *inside* the Windows-only ruling. Escape hatch on record: a dedicated headless native-Ubuntu box if volume outgrows the laptop. Full text at `transcript-sort-and-route-design.md` §10 and §12.
- ⬜ **Domser project setup — Michael's hand, outside the repo.** Paste the **v2 governance instructions** into that project's instructions field; move the **Rev. 2 case instructions** into that project's knowledge if they are currently sitting in the instructions field; start a fresh chat after the changes. *(The instructions document is NOT repo material and is not reproduced here.)*
- ⬜ **Tascam DR-05XP + SmartLav+ purchase** — standing recommendation, **not gating** anything. Carried.
- ⬜ **Registry entries 1–10 sign-off** — Michael's, one at a time (detail at §5 above).

### K-6 and K-7 — RETIRED 2026-07-28 (Michael's ruling)

BUILD-STATE carried the range "K-5–K-7", but **K-6 and K-7 had no question text anywhere in the repo** — no log entry, no doc, only the IDs. Michael does not remember what they asked. **Both are RETIRED.**

**Nothing was reconstructed** — not by Code, not by the design side, not from plausibility. A fabricated open item is worse than a lost one. Michael's reasoning, recorded because it is a standing rule and not just this disposition: *if either question mattered, the work that raised it will raise it again, and it gets a new ID with its text intact.*

Failure class: the same one that destroyed Q-5's original wording — an ID carried forward without its question (**QR-1**, still PROPOSED and unruled). Actor for the loss: **unknown** — it predates attribution or was lost in the queue. **Do not resurrect these IDs.** See `anti-resurrection-ledger.md`.

### Captured 2026-07-28 — IDs issued by design session as they come

Raised by Michael during the CL-2 live walkthrough. **PROPOSED and unruled — captured, not built.** IDs are issued by a design session, not here; the first was issued 2026-08-07 (**CL2-AC-1**, below). The rest still await one.

- *(**CL2-AC-1** — consolidated 2026-08-08 into the CL2-AC-1 entry under "Client model" above — text moved, not deleted; see `rulings-capture-2026-08-08b.md` Part 2.)*
- ⬜ **Itemized-bill ingest.** Drop an itemized medical bill into a client's bill workspace and auto-extract the line items — description, date of service, revenue code, quantity, unit price, CPT — populating the ledger automatically; Michael renames/labels the bill and the treatment lines fill themselves. His rationale: hand-keying every line of every itemized bill *"would take insanely too much time"* — **a viability issue for the medical spine, not a convenience.** The OAA criminal-intake OCR/extract layer already proves the document-in → structured-rows-out shape; **reuse, don't reinvent.** Michael recalls this being discussed before and a transcript search found no record — **check the ARCHIVE project before designing from scratch.**
- ⬜ **Party-credibility watch.** A fact about a party (indictment, credibility damage, evidence mishandling) should be able to raise a flag on **every case where that party appears in a testimony-bearing role** — a cousin of the registry watch flags, attached to people instead of rules. Capture only; no design pass, no ID.
- ⬜ **Bill label should default to the provider name**, editable only to disambiguate ("ProCare — initial eval" vs "— follow-up"). **Code answered the blocking question 2026-07-28: the label is NOT load-bearing** — display-only in all five uses, and the report generator already prefers the provider (`providerName ?? bill.label`). Safe to pre-fill; not built, as it sits outside CL-2's six pieces.
