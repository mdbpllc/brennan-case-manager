# Post-Execution Sign-Off + WS-1/WS-3 Adjudication Record — 2026-08-18 (#109)

**Canonical repo path (proposed):** `docs/specs/post-execution-signoff-record-2026-08-18.md` — NEW file, rides this packet.

**Status: RECORD.** Every ruling and verification below is **Michael's word, recorded per item
before any file was touched**. The directed edits at §3 are executed by the queue runner; every
proposition touched carries the status Michael's answer gives it, nothing more.

**Session:** #109, design side, **Fable 5**, Cowork, 2026-08-18 late evening (Central) — **the same
conversation as #108, continuing after the SIXTY-SECOND runner batch executed the #108 packet.**
Ceiling at HEAD: **#108** → this entry is **#109** (design-minted; TOC-6 honored).
**Reconcile basis:** HEAD `62455f5` on `master`, bridge lock-free reads (`cat .git/HEAD` +
`cat .git/refs/heads/master`); `inbox/` empty at this packet's authoring; `.git/index.lock` absent.
Local-only evidence, nothing about origin. The sixty-second runner line and its execution of the
#108 packet were read at the top of the log before anything was asserted; the enforcement file's
38 status lines (22 V / 16 UNV), the criminal file's 7 (4 V), and the carrier-duties 22 V were
re-derived by anchored grep at this HEAD.
**Instructions v22 in force. Runner v11.**
**DT-1:** wall clock `Tue Aug 18 21:4x CDT 2026` at the re-reconcile (container reading 08-19 UTC —
the same evening window, fourth catch of the day). All stamps 2026-08-18. **The same slip recurred
in-session:** two option labels again displayed "2026-08-19" before the wall-clock re-check;
caught before any stamp, corrected in chat, recorded here (actor: Fable 5; failure class: DT-1
evening-window UTC drift, same-session correction, the #108 §4 pattern).
**PF-1 ran design-side, pre-`inbox/`, on this packet's full draft** — result and the corrections
it forced at §5 (this sentence finalized after the run, not before it).

---

## §1 — FOUR ONE-LOOK VERIFICATIONS, ALL MICHAEL'S WORD, DATED 2026-08-18

The four looks were put with the executed entry text at HEAD `62455f5` beside the official
extractions (same sources and methods as the #108 record §5; extraction artifacts characterized
identically).

1. **Entry 12 — TRCP 215.4(b): "VERIFIED."** The executed Rule line is the adopted verbatim text;
   programmatic character comparison against the July 2026 TRCP PDF extraction (whitespace-
   normalized): **exact match**. The successor act the sixty-second batch's queue row created is
   satisfied as to this entry.
2. **Entry 32 — CCP art. 102.073 (criminal 4b): "VERIFIED."** Exact match against the official
   corpus extraction after the two characterized artifacts are normalized (the `AA` space encoding;
   `defendant ’s` → `defendant's` per the #108 character note). Entry 4a untouched, still
   UNVERIFIED and cite-less.
3. **Entry D — TRCP 194.2(b)(9): "VERIFIED."** The operative quote ("any witness statements
   described in Rule 192.3(h)") is exact; the lead-in accurately restates the 194.2(b) intro
   ("Without awaiting a discovery request, a party must provide to the other parties:"); the
   second sentence is characterization, put as such.
4. **Entry E — TRCP 194.5: "VERIFIED."** The quoted sentence is the rule's entire text, exact; the
   characterization sentences and the Q-WS2-3 scope note (asserting nothing on attorney-client)
   were put as such; the #108 cross-reference to 192.5(c)(1) is in place.

**Entries A, B, F were put the same way and went to CONFORMATION instead — §2.** His words at the
put, verbatim: on entry A, **"Let's dial in on this one a bit more."**; on the B/F pair, **"Split —
tell me in chat"** (the split question was then put and answered **"Both verbatim after all"** —
§2 item 3). Their divergence
lists were named in full in chat before the choice (A's 215.1(e) limb: two-sentences-to-one, "the
amount of" dropped, the request clause compressed; B: "support it" for "support the objection or
privilege," the merged evidence sentence, "in camera review" for "an in camera review," the
labeled Rule-193 gloss; F: the proviso recast, "either by" → "— by," the condensed in-camera
clause, "answers" for "answers to the deposition questions," "if" for "in the event").

---

## §2 — RULINGS, PICKS VERBATIM

0. **Session scope — his pick: "Yes — all of it (Recommended)."** After entries 12 and 32 verified,
   Michael authorized the rest of the sitting in one pick: the five inserted-entry looks AND the
   two WS-3 gate questions. Everything below proceeded under that authorization.
1. **Q-WS1-1 — RULED: "Own entry (Recommended)."** TRCP 215.1(e) takes its own entry (matching the
   file's existing 215.1(c)/(d) pattern), Rule line the rule's exact two sentences; entry A keeps
   the 192.3(h) right with its Rule line reduced to the exact rule sentence and an enforcement
   cross-reference. **Both texts adopted, his pick: "Adopt both."** The old conservative default
   (215.1(e) inside A) is superseded; A's Dedupe note is replaced by the cross-reference.
2. **The 215.1 preamble tension — RULED: "Keep flagged (Recommended)."** The "A party" (preamble)
   vs "the person who made the request" ((e)) tension stays flagged, asserting nothing — no
   located authority resolves it. The flag moves with the enforcement limb to the new 215.1(e)
   entry.
3. **Entry B — RULED: "Both verbatim after all" / "Adopt."** Rule line conformed to 193.4(a)'s
   exact text; the "this rule = Rule 193" construction moves to a labeled note line.
4. **Entry F — same ruling, "Adopt."** Rule line conformed to 199.6's first paragraph exact; the
   corrected no-asymmetry passage (with its exact 193.4(b) quote) stays.
5. **Q-WS3-1 — RULED: "Cross-reference (Recommended)."** The WS-3 entry carries an express
   cross-reference line to the VERIFIED 192.5(c)(1) entry (the two-propositions-two-homes pattern
   ruled at #108 for 194.5). CLOSED.
6. **Q-WS3-2 — RULED: "Comment-9 (Recommended)."** The entry's comment-9 derivation stands as
   drafted, including its Derivation note stating the shorthand honestly. CLOSED.
7. **WS-3 insertion — RULED: "Insert (Recommended)."** The drafted entry — with the #108-adopted
   C-XL-1 boundary and C-ARD-1 condition — inserts into the enforcement file, entering UNVERIFIED.
8. **Stale-clause conformation — RULED: "Conform (Recommended)."** The Rule line's
   "which this pass located but did not read" (now false — read at #104; the sixty-second batch's
   staleness finding (1)) conforms at insertion to: "read and V-9-identified 2026-08-18 (#104);
   its adopted boundary is stated below (C-XL-1)."
9. **ws3 doc §6 table — RULED: "Annotate (Recommended)."** The dated document's open-items rows
   for Q-WS3-5 and Q-WS3-6 (staleness finding (2)) are annotated add-only as CLOSED — ruled
   2026-08-18 (#108); the rows for Q-WS3-1 and Q-WS3-2 take the same add-only annotation citing
   this session (#109). Originals stand as written.
10. **Sequence note:** all conformations and the split follow the #109-applied standing verbatim
    direction (ruled at #108, §2 item 20) — applied here per entry by his picks, never
    retroactively by default.
11. **Entry 4b cite parenthetical — RULED: "Update it (Recommended)"** (put as its own act per
    ROUTE-C after PF-1 caught it directed without a ruling): the parenthetical becomes
    `(text read verbatim 2026-08-13 from a public source; verified against the official bulk corpus, Statutes 26-08-14, at #109)`.

---

## §3 — DIRECTED EDITS (queue runner; exact scope; counts re-derived at HEAD, never copied)

**File names, canonical (PF-1 fix — never inferred): "the enforcement file" =
`docs/specs/legal-rule-registry-discovery-enforcement-and-pleading.md`; "the criminal file" =
`docs/specs/legal-rule-registry-criminal-plea-and-costs.md`. The carrier-duties file
(`docs/specs/legal-rule-registry-discovery-and-carrier-duties.md`) is NOT touched this batch.**

1. **Four Status moves** (and only these four). For each: **REPLACE the existing
   `**Status:** UNVERIFIED.` line** with the new Status line, and insert the `**Verified:**` line
   **immediately after the Status line** — never a second Status line:
   - Enforcement `## TRCP 215.4(b)`: `**Status:** VERIFIED — Michael, 2026-08-18 (#109).` Then:
     `**Verified:** 2026-08-18 one-look (#109): executed verbatim Rule line compared character-level against the July 2026 TRCP PDF extraction — exact match. Record: post-execution-signoff-record-2026-08-18.md §1.`
   - Criminal `### 4b`: `**Status:** VERIFIED — Michael, 2026-08-18 (#109).` Then:
     `**Verified:** 2026-08-18 one-look (#109): exact match against the official corpus (Statutes 26-08-14), characterized artifacts normalized. Record: post-execution-signoff-record-2026-08-18.md §1.`
     Also update 4b's cite parenthetical to: `(text read verbatim 2026-08-13 from a public source; verified against the official bulk corpus, Statutes 26-08-14, at #109)` — **RULED as its own act, §2 item 11.**
   - Enforcement `## TRCP 194.2(b)(9)`: `**Status:** VERIFIED — Michael, 2026-08-18 (#109).` Then:
     `**Verified:** 2026-08-18 look (#109) against the July 2026 TRCP PDF; operative quote exact; lead-in and characterization put as such. Record: post-execution-signoff-record-2026-08-18.md §1.`
   - Enforcement `## TRCP 194.5`: `**Status:** VERIFIED — Michael, 2026-08-18 (#109).` Then:
     `**Verified:** 2026-08-18 look (#109); quoted sentence is the rule's entire text, exact; scope note asserts nothing on Q-WS2-3, which stays open. Record: post-execution-signoff-record-2026-08-18.md §1.`
2. **Replace the `## TRCP 192.3(h) — a person's right to his or her own statement` entry IN FULL**
   (its Status line at HEAD is the bare `**Status:** UNVERIFIED.` — confirmed design-side, so the
   replacement loses no trailing status text; the prior Dedupe note is replaced by the
   cross-reference per §2 item 1) with:
   ```markdown
   ## TRCP 192.3(h) — a person's right to his or her own statement

   **Cite:** Tex. R. Civ. P. 192.3(h); enforcement at Tex. R. Civ. P. 215.1(e).
   **Rule.** Any person may obtain, upon written request, his or her own statement concerning the lawsuit, which is in the possession, custody or control of any party.
   **Characterization note (labeled, not rule text).** The right runs to any person — the rule's own word — not only to a party to the suit, and the rule states no condition of a pending discovery request.
   **Enforcement cross-reference (ruled 2026-08-18, #109, Q-WS1-1):** enforcement is TRCP 215.1(e)'s own entry in this file — the person who made the request may move to compel and recover expenses; the "A party" preamble tension is flagged there.
   **Status:** UNVERIFIED.
   **Relied on for:** the witness-statement conversion boundary (the T-22 memo's §3 territory) — a person recorded by this firm may demand the recording or its substantially verbatim transcript back, on written request alone; and the intake/adjuster-call posture, where the recorded person is typically not a party.
   **Wording:** adopted 2026-08-18 per #109 (ROUTE-C — Rule line conformed to the rule's exact sentence; the 215.1(e) limb split to its own entry per Q-WS1-1, replacing the prior Dedupe note); verification attaches to this wording.
   **Source:** clean-authority PDF, `Documents\Knowledge Repo\Civil\texas-rules-of-civil-procedure July 2026.pdf` ("July 2026" is the filename's designation; no separate effective-date line — caveat accepted at WP-1/2/3). Retrieval: RUN 2026-08-17, raw pdftotext in the device VM, quotations spot-checked in two extraction modes.
   ```
3. **Insert a NEW entry immediately after the `## TRCP 215.1(d)` entry:**
   ```markdown
   ## TRCP 215.1(e) — compelling compliance with a person's own-statement request

   **Cite:** Tex. R. Civ. P. 215.1(e); the right enforced is Tex. R. Civ. P. 192.3(h).
   **Rule.** If a party fails to comply with any person's written request for the person's own statement as provided in Rule 192.3(h), the person who made the request may move for an order compelling compliance. If the motion is granted, the movant may recover the expenses incurred in obtaining the order, including attorney fees, which are reasonable in relation to the amount of work reasonably expended in obtaining the order.
   **FLAGGED, NOT RESOLVED (carried from the WS-1 draft; kept by ruling 2026-08-18, #109):** Rule 215.1's preamble grants the application right to "A party, upon reasonable notice to other parties and all other persons affected thereby," while (e) grants the motion to "the person who made the request," who under 192.3(h) need not be a party. No located authority resolves whether a non-party may move under 215.1; this entry asserts no answer.
   **Status:** UNVERIFIED.
   **Relied on for:** the enforcement limb of the own-statement right — the intake/adjuster-call posture, where the recorded person is typically not a party.
   **Cross-reference:** the right itself is the `## TRCP 192.3(h) — a person's right to his or her own statement` entry above.
   **Wording:** the Rule line is Rule 215.1(e)'s exact two sentences (subdivision caption omitted, the file's convention); adopted 2026-08-18 per #109 (Q-WS1-1); verification attaches to this wording.
   **Source:** clean-authority PDF, `Documents\Knowledge Repo\Civil\texas-rules-of-civil-procedure July 2026.pdf` (currency caveat as at WP-1/2/3). Retrieval: RUN 2026-08-18, raw pdftotext in the device VM, spot-checked against raw extraction.
   ```
4. **Entry B (`## TRCP 193.4(a)`):** replace its Rule line with exactly:
   `**Rule.** Any party may at any reasonable time request a hearing on an objection or claim of privilege asserted under this rule. The party making the objection or asserting the privilege must present any evidence necessary to support the objection or privilege. The evidence may be testimony presented at the hearing or affidavits served at least seven days before the hearing or at such other reasonable time as the court permits. If the court determines that an in camera review of some or all of the requested discovery or required disclosure is necessary, that material or information must be segregated and produced to the court in a sealed wrapper within a reasonable time following the hearing.`
   Insert after the Rule line: `**Construction note (labeled):** "this rule" in 193.4(a) is Rule 193 — stated here, not substituted into the quoted text.`
   Neither entry B nor entry F carries a `**Wording:**` line at HEAD (confirmed design-side). Add
   one to B, **inserted immediately after its "Relied on for" line** (the 215.4(b)/4b pattern):
   `**Wording:** Rule line conformed VERBATIM to 193.4(a), adopted 2026-08-18 per #109; verification attaches to this wording.`
   If a Wording-type line unexpectedly exists at execution, update it instead and say so. Status
   stays UNVERIFIED; all other lines untouched.
5. **Entry F (`## TRCP 199.6`):** replace its Rule line with exactly:
   `**Rule.** Any party may, at any reasonable time, request a hearing on an objection or privilege asserted by an instruction not to answer or suspension of the deposition; provided the failure of a party to obtain a ruling prior to trial does not waive any objection or privilege. The party seeking to avoid discovery must present any evidence necessary to support the objection or privilege either by testimony at the hearing or by affidavits served on opposing parties at least seven days before the hearing. If the court determines that an in camera review of some or all of the requested discovery is necessary to rule, answers to the deposition questions may be made in camera, to be transcribed and sealed in the event the privilege is sustained, or made in an affidavit produced to the court in a sealed wrapper.`
   Add, **inserted immediately after its "Relied on for" block** (same pattern and same
   exists-at-execution caveat as item 4): `**Wording:** Rule line conformed VERBATIM to 199.6, adopted 2026-08-18 per #109; verification attaches to this wording.` The corrected no-asymmetry passage (exact 193.4(b) quote) stays; Status stays UNVERIFIED.
6. **Insert the WS-3 entry:** take the fenced entry text from
   `docs/specs/ws3-privilege-authority-read-2026-08-17.md` §6 **at HEAD** (it already carries the
   #108-adopted C-XL-1 and C-ARD-1 lines), apply exactly two changes:
   (a) in the Rule line, replace `which this pass located but did not read.` with
   `read and V-9-identified 2026-08-18 (#104); its adopted boundary is stated below (C-XL-1).`
   (b) insert **immediately after the SCOPE paragraph and BEFORE the `**Adopted boundary
   (C-XL-1…` line** (the intra-entry order is: SCOPE → this cross-reference → C-XL-1 → C-ARD-1 →
   Source):
   `**Cross-reference (ruled 2026-08-18, #109, Q-WS3-1):** see the VERIFIED entry ## TRCP 192.5(c)(1) — witness statements excepted from work-product protection in this file — the work-product side is settled there; this entry states the attorney-client limit only.`
   The fenced draft carries its own `**Status:** UNVERIFIED.` line (confirmed design-side) — no
   third change is needed or permitted.
   Then insert at the destination the draft itself states: **immediately after the
   `## TRCP 192.3(h) — witness statements discoverable` entry** (i.e., before
   `## TRCP 192.3(h) — what a witness statement is`). Status enters UNVERIFIED. If the placement
   is ambiguous at HEAD, STOP and tell Michael. Annotate the ws3 doc's §6 fenced draft add-only,
   **placed OUTSIDE and immediately BELOW the closing code fence** (never inside it — the fence is
   the copy source and must stay clean):
   `*(INSERTED 2026-08-18, #109 — with the two ruled changes recorded in post-execution-signoff-record-2026-08-18.md §3 item 6.)*`
7. **ws3 doc §6 open-items table:** annotate add-only the rows for `Q-WS3-1` and `Q-WS3-2`
   (`RULED AND CLOSED 2026-08-18 (#109) — see the queue`), `Q-WS3-6`
   (`RULED AND CLOSED 2026-08-18 (#108) — see the queue`), and `Q-WS3-5`
   (`RULED AND CLOSED 2026-08-18 (#108) — adoption executed; its second limb, the five-lead read, is AUTHORIZED and unrun — see the queue`).
   Originals stand as written.
8. **Queue merge, two acts (QR-6(b)):** close `Q-WS1-1`, `Q-WS3-1`, `Q-WS3-2` (rulings appended,
   original text preserved per QR-1). Update the sixty-second batch's ID-less post-execution row:
   entries 12, 32, D, E verified (§1); A, B, F conformed-adopted; WS-3 inserted — and enter its
   successor: **one-look verifications of the five UNVERIFIED ruled-wording entries (A, B, F, the
   new 215.1(e), and WS-3), Michael's act, next sitting.** If the ID-less row cannot be located
   unambiguously at HEAD, enter a NEW row carrying the successor act instead and say so — never
   guess at which row was meant. Status-header reconcile sentence updated to #109.
9. **BUILD-STATE rewrite + TOC regeneration** per runner Step 4. Orientation only, re-derive at
   HEAD: expected enforcement file 40 status lines (25 V / 15 UNV), criminal 7 (5 V / 2 UNV) —
   backlog 47, 30 VERIFIED / 17 UNVERIFIED.

---

## §4 — SOURCES (per item; unchanged methods)

*(Section numbering note: this record runs §1–§5; the #108 record's sources sat at its §5 — cite
each record by its own headings.)*

Same table as the #108 record §5, plus: TRCP 215.1 (preamble and (e)), 193.4(a), 194.2(a)/(b),
194.5, 199.6 — all from `texas-rules-of-civil-procedure July 2026.pdf`, raw `pdftotext -layout` in
the device VM, quotes spot-checked against raw extraction; comparisons for entries 12/32 run
programmatically (whitespace-normalized, characterized artifacts normalized, method stated in
chat). Retrieval is not verification; the four verifications at §1 are Michael's.

---

## §5 — PF-1 PREFLIGHT RESULT (completed, and the corrections applied, before the zip was saved)

Two independent read-only adversarial agents ran against this packet's full draft plus the raw
extracts and the session's recorded picks. **Asserted defects: 22 (13 + 9, overlapping; each
agent one HIGH).** The HIGHs: the enforcement and criminal registry files were never named by
canonical path anywhere in the packet (now named in §3's preamble); and the record omitted
Michael's session-scope pick ("Yes — all of it") from a record claiming per-item completeness
(now §2 item 0, with his entry-A and B/F responses quoted verbatim in §1). One agent finding
required a ruling rather than an edit: the 4b cite-parenthetical update had been directed with no
pick behind it — **it was put to Michael as its own ROUTE-C act and RULED "Update it
(Recommended)" (§2 item 11).** The rest, corrected: Status "moves" made replace-not-add with
placement anchors; the WS-3 intra-entry cross-reference anchor fixed to before the C-XL-1 line;
the fence annotation placed outside the fence; the B/F Wording-line instructions made symmetric
and anchored (neither carries one at HEAD, confirmed design-side); the Q-WS3-5 annotation carries
its unrun-second-limb caveat; the ID-less queue row gained a no-guess fallback; bare "Record: §1"
pointers given the full document name; entry A's bare Status line confirmed so the full
replacement loses nothing; the §-numbering gap closed (§4 note); the work order's orientation
figures relabeled as arithmetic expectations, its DO-NOT counts clause scoped, and §8 given "at
the TOP." **If this section ever reads as an unfilled bracket, the run was skipped — STOP and
tell Michael.**
