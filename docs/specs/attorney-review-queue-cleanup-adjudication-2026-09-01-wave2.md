# ATTORNEY REVIEW QUEUE — CLEANUP ADJUDICATION, WAVE-2 SUPPLEMENT (2026-09-01)

> **STATUS: PROPOSED — RULING-READY, ADJUDICATED, NOT RULED. THIS DOCUMENT CLOSES NOTHING.**
> Companion to `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01.md` (THE SHEET), whose
> §6 this file discharges. Same rules: Michael rules — row by row, or block by block in words; nothing
> closes on silence; every instrument executes on a row's full first line as an exact string at the
> executing HEAD, never on a line number.

**Canonical repo path:** `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01-wave2.md`
**Produced by:** the same 2026-09-01 typed Fable 5 design session, DT-1 Central, adjudicating the wave-2
evidence returned by the OPUS-SWEEP session (Opus 5, execution class; its evidence file rides this
packet to `docs/record/queue-cleanup-evidence-2026-09-01/wave2_opus_sweep_evidence.md`, 435,976 B,
verbatim as returned). Evidence pinned to `7f02131`; this supplement authored while runner batch 86 was
in flight, which is why its own instruments anchor on strings and IDs only.

## §0 — WHAT WAVE 2 ESTABLISHED

**The sweep of the 247 open rows no closure-class audit ever tested returned `CLOSE` = 0.** Eight
independent Opus sweepers (247/247 accounted, coverage diffed programmatically, no dead agent), and not
one found a row a later ruling closes outright. Read with wave 1, the shape of the register is now
measured end to end: **the closable backlog lived almost entirely inside the audit's 108 candidates,
already adjudicated on the sheet; the remaining 247 are genuinely open — but 31 of them recite a build
state that is false at HEAD, and 225 of 247 are the ONLY home of their own text.** The register's
problem is staleness and irreplaceability, not closable clutter — which is QR-1 doing its job, and the
strongest argument on record against any fast disposition pass.

**The finding that governs execution, adopted into the sheet's discipline:** every line number in both
audits and in `id-collision-report.md` is OFF BY ONE against `7f02131` (eight citations probed, eight
drift; the audit's "L1002 = Q-STAT-5" resolves to **`Q-STAT-4`** — a zero-footprint row whose question
exists nowhere else; its "L1260 = O4" lands on a closed `O-3`). **No closure may ever be executed from
an audit line number.** The sheet was already built this way — its anchors were derived at `7f02131`
directly and re-verified, and every WO is a full-line exact string — but the rule is now evidenced, not
just prudent. After batch 86, every line number in every layer is stale again; the strings are the
identity.

## §1 — ADJUDICATED DISPOSITIONS (the wave-2 blocks, joining the sheet's)

### BLOCK W-C — the sweep's one CLOSE-SPLIT, adopted: `Q-COM-4`

Limb (i) — is `Q-WF-4` upstream of the communications log? — **closed on Michael's #94 ruling "Amend
the row,"** executed the same day: `Q-WF-4`'s row now names all three consumers (WF-2–WF-8, `Q-QBO-3`,
`Q-COM-4` "the last the most demanding"), add-only, full text per QR-1. The row's informational purpose
is spent. Limb (ii) — does a third consumer move `Q-WF-4` up the queue — was never put; `#94`'s own
closing words are *"The identity question itself remains open and Michael's."* **Re-minted row text
(QR-1): "Does a third consumer (the communications log, per `#89`/`#94`) move `Q-WF-4` — the
server-side-identity question — up the queue? Never put, never ruled. Provenance preserved from
`Q-COM-4`: `#88` declined to add a third consumer because referral acts are attorney-initiated and
synchronous; `#94` then amended `Q-WF-4`'s row to name all three. (Split from `Q-COM-4`, closed <date>
on #94.)"** The closure retains the row verbatim (append-only), so the `#88` provenance sentence —
register count 1 — survives twice over. Confidence MED (the sweep's own honest note: "Amend the row"
could be read as clerical; the adopted text's substance says otherwise). **Michael's word decides.**

### BLOCK W-G — two RECLASSIFY-ACT rows joining the sheet's Block G inventory: `P-COM-1`, `P-COM-5`

Neither carries a question. Each is an UNVERIFIED proposition whose ONLY copy in the repository is its
register row — `P-COM-1` = TRCP 192.5(a)(2) (`192.5(a)(2)` returns 0 everywhere else; control
`192.5(d)` fires 4×); `P-COM-5` = TRE 503(a)(5) (0 everywhere else; *"the limb an ingest pipeline lives
or dies on"*). `#94` ruled three OTHER TRCP propositions into the registry and these were not among
them; the hardened audit's family classification (*"stale verification status, not ruled-but-open"*)
applies. **The acts are Michael's, twice over: MINT into the registry, then VERIFY. Never closed —
closing `P-COM-5` would delete the repository's only statement of TRE 503(a)(5).** These join G-Q1's
inventory as its 39th and 40th acts (both "Michael's hand"), and the G-Q1 marking question covers them.

### BLOCK W-D — two more rows only his word moves: `Q-T19-1`, `Q-QBO-8`

- **`Q-T19-1`** — numbering **DEFERRED by Michael at the walk (`D-6`, #98)**; BUILD-STATE at HEAD keeps
  it OPEN by name. This confirms the sheet's E-10 pairing (rule it as ONE question with `V5-IDS` when he
  lifts the deferral); the deferral's verbatim wording lives in the unstaged sign-off record —
  UNVERIFIABLE-HERE, reported as the log's and BUILD-STATE's account.
- **`Q-QBO-8`** — the cheapest row in the whole register: *"Does this change anything, or is it filed as
  a fact?"* with the recommended answer ("not a ceiling for a solo practice — filed as a fact") already
  drafted at `#87`, where Michael made no rulings. **One word closes it; a Claude "filed as a fact" is
  not a ruling.** Belongs on the next sitting's list.

### BLOCK W-E — the 68 ANNOTATE-KEEPs, grouped so the sitting rules FIVE times, not sixty-eight

*Every row stays `⬜`; each annotation is a dated sentence the executing packet assembles; the sweep's
per-row blocks (in the evidence annex) carry the quotes. "Adopt W-E" authorizes all five groups.*

- **W-E1 — THE `#130` REVERSAL PROPAGATION (nineteen rows, ONE sentence).** Every row tagged
  `Phase 1b GPU` still recites the pre-2026-08-21 world in which local-GPU inference was the only
  sanctioned path for document understanding. The annotation, once, on each: *"`#130` (2026-08-21)
  reversed `H12` — the app calls the model directly on the firm's own BAA-covered API account, vendor
  open at `H12-v`, BAA a HARD GATE — and `#140`'s AS-Q1 gave the call its server-side shape; the local
  path remains an option, not the premise."* The 2026-08-24 audit itself grouped four intake rows under
  the GPU arm three days AFTER the reversal — the annotation is what stops the next audit inheriting it.
- **W-E2 — STALE COUNTS AND FIGURES (the counts family).** Rows carrying figures BUILD-STATE has since
  recomputed: the registry backlog (rows say 34 / "40, 16 unverified"; HEAD says 47 / 35 / 12); `CR-10`
  (six→seven propositions, two now VERIFIED — the hard gate itself survives); `Q-PR3-1` (the "second
  task shaped by the unruled method question" is the SIXTH at HEAD, verified entry by entry); `ID-DL-1`
  (the row says twelve packets; eighteen at HEAD, flattened grep); `IN-2`'s migration/table counts;
  `TC-OPEN-1`'s figures (and `#138`'s third uncapped grower, `pi-case-playbooks.md`, which its title
  excludes). Annotations state the current figure AND that counts are recomputed at reading, never
  trusted from a row.
- **W-E3 — GATES THAT LIFTED WITHOUT THE ROW NOTICING.** `FE-14` (entry 27 VERIFIED 2026-08-17 — the
  row's stated gate is GONE; plus its cite is now 47(b)–(d) and `#76`'s five-options finding — one of
  the twenty-seven rows no audit ever read); `Q-RL6-1` (*Pharr* WAS read, #117 — the cite supply stays
  his); `G10-4` (seven ch. 521 entries EXIST, drafted-not-inserted — the row's "none was drafted" is
  false at HEAD); `Q-FE5-9` (route (c) is STANDING law since #95 — the row's "ruled one-off"
  parenthetical is false — and its ROUTE-C wording is being drafted TODAY by the v5 chain, so the
  annotation says the wording is in flight and nothing here collides); `Q-QBO-6`'s blocker (FC-14
  reaches it); `Q-WF-7` (both limbs of its disjunction were answered by rulings that PREDATE it —
  `Q-STAT-1` and `HK-5` — the row has the order backwards; the eCFR limb survives, so annotate, never
  close); `Q-COM-4`'s sibling notes; the `CD-1`-gate-spent FE rows (`FE-9`, `FE-11`, `FE-15`).
- **W-E4 — DEFERRED-IN-WORDS RESTATEMENTS.** `O-12` (deferred by his ruling AND physically truncated —
  the sheet's Q-2a restoration covers the text; the annotation records the deferral), `H12-v`
  (architecture refined since by AS-Q1; the vendor question untouched), `TC-OPEN-1`/`TC-OPEN-2` (the
  survey ran at `#138`; the destination question outranks the sweep and has never been put),
  `Q-STAT-5` (his "plain open" choice at #108 stands; count stale).
- **W-E5 — SINGLETONS.** The remaining annotate-keeps as the evidence file's §2 table lists them —
  each one sentence, each preserving text that exists nowhere else (`Q-STAT-4` and `Q-DES-4` above all:
  true zero-footprint rows; the exact rows an audit line number would have destroyed).

### BLOCK W-K — KEEP, 174 rows

Confirmed live, premise intact, greps run on every one. **No action, and that is the finding:** the
un-audited majority of the register is real backlog, not cruft.

## §2 — CROSS-CUTTING ITEMS PUT (one at a time; none decided here)

- **W-Q1 — the `V17` collision is real and settles as TWO subjects** (one opened at archive `#8`,
  §352.051(2)/§71.004(c) fees; a different `V17` — probate practice area — CLOSED at archive `#15` and
  cited by BUILD-STATE and the PR-3 row). The collision report had it "unsure"; the register carries
  both meanings at once. **Flag-not-rename (the `CR-3` treatment)?**
- **W-Q2 — the schema/live-DB divergence on `generated_documents`** while `MIG-1` is unrun (schema 41
  tables, live 37; BUILD-STATE carries the `#83` pre-build claim and the FE-D1 extension claim side by
  side, unreconciled). Not a queue-row defect — the sweep's four-way test of the hardened §5.3 landed
  "premise right, implication over-broad." **Recorded for the next BUILD-STATE rewrite's recompute and
  for the `MIG-1` sitting; the row annotations (W-E1/W-E2) force a re-measure after the migration runs.**
- **W-Q3 — record-only notes, no action proposed:** the audit double-counts `CD-10`; `#138`'s
  process-notes tail contradicts its own body (append-only — stands as written, flagged); thirteen B3
  rows and `L968` appear in neither audit (consistent with the hardened "twenty-seven named nowhere");
  the `WF-1`-inside-`Q-WF-1` and bold-marker grep traps join the register's counting-trap notes.

## §3 — REPAIRS TO THE SHEET ITSELF (two exact-string work orders, staged here)

Found after the sheet's packet was staged; named rather than smoothed (the sheet's own §0 promises its
defects get recorded, not hidden):

- **WO-S1 — the §0.3 mis-pointer.** OLD (unique): `execution); its results fold in at §7.` → NEW:
  `execution); its results fold in at §6.`
- **WO-S2 — the §6 placeholder discharged.** OLD (unique, the placeholder paragraph beginning
  `*Delegated to the background Opus session` and ending `nothing in §2–§5 depends on it.*`) → NEW, one
  paragraph: `*Discharged 2026-09-01: the sweep returned 247/247 (CLOSE = 0; 68 annotate-keeps in five
  groups; one close-split; two reclassify-acts; two his-word rows; 174 keeps). The adjudication is the
  WAVE-2 SUPPLEMENT beside this file; the evidence is at
  docs/record/queue-cleanup-evidence-2026-09-01/wave2_opus_sweep_evidence.md.*` (The header's "~247"
  may keep its tilde — it is now exactly 247 and the supplement says so.)

## §4 — WHAT THIS SUPPLEMENT DOES NOT ESTABLISH

Everything the sheet's §7 disclaims, plus: the sweep's five recurring UNVERIFIABLE-HERE reads (the
`generated_documents` columns; the four registry files; `src/`; the live database; the Task 7/8/9 spec
files) remain unsettled, each tabled in the evidence file's §4 against the read that would settle it —
**and per H5, the off-repo items there are Michael's, never a session's sweep.** PF-1 did not fire (no
new legal characterization, no proposed registry entry; the two propositions W-G names are quoted only
as register-resident text awaiting HIS mint-and-verify — the skip recorded, not silent). Models: the
sweep — Opus 5 (its own header); this adjudication — Fable 5 per the session environment. The evidence
file's project-knowledge copy is SPENT on this supplement's shipping, by its own banner — its deletion
is a one-word item for Michael (§5 of the sheet's sitting), taken only after this packet's batch is
verified at origin.
