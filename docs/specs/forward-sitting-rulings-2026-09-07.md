# THE FORWARD SITTING — RULINGS AND ACTS OF 2026-09-07

> **STATUS: RULING RECORD — every ruling in Michael's words, the acts he performed by his own hand during the sitting, and what each closes.** Canonical repo path: `docs/specs/forward-sitting-rulings-2026-09-07.md` (CAP-2 class: RULING). Filed by packet `push-to-code_forward-sitting_2026-09-07`; session-log entry `#149`. The running ledger it is copied from is filed as EVIDENCE at `docs/record/forward-sitting-2026-09-07/ruling-ledger-2026-09-07.md`.
> **This document RECORDS rulings.** The slice it authorizes is `docs/specs/cc1-rulings-and-address-model-slice.md`; the gates doc, `spec-feedback.md` item 2 and the register are ANNOTATED or APPENDED by the packet — none of their ratified text is edited.
> Cite by heading or quoted sentence.

## §0 — THE SITTING

Typed, Cowork, Fable 5.1 (`claude-fable-5-1` per the environment) throughout; device bridge granted on `C:\Users\Brennan\brennan-case-manager`, then `Documents\Knowledge Repo` and `Downloads`; Claude in Chrome (Michael's own session) for the two TRANSIT extractions. HEAD `f5bbdf9` = local origin ref = `git ls-remote` (the bridge VM had network), 0/0, no lock; `inbox/` empty of zips. Knowledge meter 1,582,076 / 2,000,000 = **79.1%** at open — `Q-CAP-5(a)` did not fire; **77.4%** at close. Newest design entry at the live log `#148`; this sitting is `#149`. Every date Central (DT-1); the sitting ran 16:0x–18:xx CDT. Michael's form instructions, kept: *"Run the confirmations by me one by one."* (the gates); *"Break this down more for me and let's rule on it."* (the slice); *"Run through them one by one."* (housekeeping). Three answers came from outside the offered set (D1(ii); the signup-toggle history; "Firm obligations first" with the CR-3 fact); two recommendations were adopted with the drawing marked Claude's (D1 limb 3, gate 9's close).

## §1 — DECISION 1: THE FACILITY AND PARTY ADDRESS MODEL (`spec-feedback.md` item 2 / 2a) — FIVE RULINGS

Rendered blocks for a fictional three-campus system entered through the app's own party form were shown before each put. Fact read from the model (`src/domain/partyRegistry.ts`, whole): no party type carries `addressLine1`, `addressLine2` or `cityStateZip`; `CONTACT` stores one "Mailing address" textarea; `providerBusiness` stores `locations[]` with one "Physical address" text; the instrument reads two-line keys in three regions and only the fixtures write them.

| ID | The put | Michael's words | The rule |
|---|---|---|---|
| **D1** | (a) primary location · (b) split the fields · (c) the R17 row picks the location · (d) composite (c)+(b) — Claude's lean | ***"d"*** | The case-scoped R17 facility row carries WHICH location treated the client (a selector on the Medical-tab facility card, pre-filled from the last case as the type is); each location's address is stored as street (+ suite) and city/state/ZIP. RULED-BY-SELECTION. |
| **D1(i)** | the phone: formatted / hyphenated / as stored; block only or instrument-wide | ***"formatted"*** | Formatted, `(210) 555-0200`, never bare digits. Scope limb NOT answered → build default `SD-3` (instrument-wide), his veto open. Which phone: the location's, falling back to the facility's main — a default stated and not objected to (`SD-2`). |
| **D1(ii)** | a location entered on one line before the split: render + panel / must-fix / silent | ***"I would like you to render what it has, but split the street and suite to one line and the city state zip on another line."*** | OUTSIDE the offered set: the legacy one-line value renders as two lines. Verified before re-asking: the 2026-08-22 prose-parsing bar (REQ-CAPTURE §8 Q2; the amendment's DO-NOT) is about the CHRONOLOGY, not an address string; the pre-sitting prompt's wider reading was Claude's gloss. |
| **D1(iii)** | where the split happens: (1) once at the record, marked, confirm-or-edit, the render path never parses · (2) at render every time · (3) once, silently | ***"1"*** | Split ONCE, at the record, by the store/DB step, by the rule *last two comma-separated parts = city/state/ZIP; the rest = street (+suite); unsplittable stays whole on the street line*; marked "split by rule"; confirm-or-edit on the Parties page; one panel line per unconfirmed split; **the render path never parses**. |
| **D1(iv)** | persons-with-knowledge and the service list: (1) same treatment instrument-wide · (2) facility only · (3) persons yes, service no | ***"1"*** | ONE address shape in the whole registry; all three regions read the split fields. |
| **D1(v)** | the tokens: (1) rule under the current tokens · (2) hold the block half until RF-2 | ***"1"*** | Build to the current tokens; a later RF-2 hand-in changes the rendering, not the record shape. `RF-2` untouched. |

**Consequences carried into the slice:** panel line 1 rewritten to be true for the shape; the fixture helpers conform; item 2 + 2a annotated to point here. **Trigger 3 does not fire** — module design.

## §2 — DECISION 2: THE LIVE MATTER — ***"test record"***
The one matter on LIVE (`:5173`) is a TEST record. Not opened; nothing from it recorded. Go-live has not happened; trigger 1 had not fired on that ground; BUILD-STATE's "NO REAL CLIENT DATA HAS EVER ENTERED THE APP" remains true on his word (the repo cannot establish it).

## §3 — DECISION 3: THE FLOOR — ***"1"*** (run it now) — GATE 9 CLOSED, THE RE-CHECK COMPLETED
- **Step 1, the SMTP settings, his hand:** *"1. Custom SMTP on; 2. signin@brennanstx.com; 3. smtp.postmarkapp.com"*.
- **Step 2a, Saturday's emails:** *"Saturday emails are coming from signin@brennanstx.com."* — a later-day repeat (§4.5).
- **Step 2b, a finding:** *"I tried sending an email to an outside address […], and it tells me 'Signups are not allowed for the otp.'"* — the signup toggle is OFF (BUILD-STATE's open act (1), measured ON at `#125`, is closed on live evidence). His recollection, recorded with the discrepancy named and not resolved: *"I believe that we turned that off on Claude's instruction in about July."*
- **Step 2c, the outside mailbox by invite (option (1), Claude's lean):** *"(1) Sent invite and email arrived from signin@brennanstx.com. I then deleted the user."* · *"1. Landed in inbox; 2. seconds"* — Proton Mail, Inbox, seconds, From `signin@brennanstx.com`, temporary user deleted.
- **The credential:** *"(a) No, it is nowhere else."* — the Postmark token lives only in Postmark's and Supabase's dashboards.
- **GATE 9 CLOSED — *"(b) sure."*** The invite-for-magic-link substitution RULED TO COUNT (identical path, different template). Edge recorded: the magic-link template itself has only ever reached the firm's own tenant. GL-1 item (3) DONE. The stale sign-in sentence *"the sending limit is low"* (`SignInPage.tsx`) is a text act in the slice (`SD-11`).
- **The re-check, one gate at a time at his instruction:** gate 1 *"Still on Pro."* · gate 2 *"Yes"* (multi-user-only reading stands; the paralegal-in-the-software design is the named second-user event) · gate 4 *"I do not believe that there has been any other exposure. When it comes to API keys, I have taken great care to follow your directions exactly."* · gates 7/8 *"Confirmed"* (billing analysis on real data deferred until they pass) · gates 3, 5, 6, 10 confirmed at HEAD (3's edge (3) now met by the signup toggle OFF).
- **GL-1: (1) (2) (3) (4) DONE; (5) this pass + instructions v30, drafted by this sitting, completing at the paste.** TRIGGER 1 FIRED. Go-live is Michael's act on his day, in GL-1's own words.

## §4 — DECISION 8 (pulled forward): THE FIX MIGRATION — RUN BY HIS HAND
*"I don't believe it has been run. If you point me to it I will go and run it."* → `db/migrations/2026-09-03-fe-d1-amendment-fix.sql` ran in the Supabase SQL editor: *"Success. No rows returned."* on the file; **check 1** exactly one CHECK, `contact_edges_edge_type_check`, twenty values ending `'renders-care-at'` (his paste of the grid); **check 2 = the amendment's check 8, BOTH halves** — the trailing-space insert refused by `contact_edges_edge_type_check` (23514), which proves the first insert was accepted; sanity `count(*)` of `renders-care-at%` = 0; **check 3** constraints / indexes / policies each *"Success. No rows returned"* — run singly after the three-together paste was shown to display only the last result. **NO MIGRATION IS PENDING on the live database as of 2026-09-07.** The migration-run record is EVIDENCE in this packet.

## §5 — DECISION 4: `CCS-1` — THE CC-1 RULINGS AND ADDRESS-MODEL SLICE — RULED YES
The slice and its kickoff prompt were drafted BEFORE the question and delivered as files. *"Break this down more for me and let's rule on it."* Seven limbs: (1) one slice, all four groups — ***"1"***; (2) the address model's seven build parts (a)–(g) — ***"Confirmed"***; (3) two migrations written and not run, schema then data, his hand, before go-live — *"What do you recommend and why?"* → ***"Confirm the recommendation."*** (marked: the drawing Claude's, the adoption his); (4) the eleven text acts provisional now, ruled at the walk — ***"1"***; (5) `HS-3` — ***"IN"***; (6) the session shape: fresh Opus Code session, runner barred, fixture-only — ***"Confirmed."***; (7) **the YES — *"yes"*, 2026-09-07 17:11 CDT**, on the one text: *"`CCS-1` — Does Michael authorize the CC-1 RULINGS AND ADDRESS-MODEL SLICE at `docs/specs/cc1-rulings-and-address-model-slice.md`, scope IN (§3) and OUT (§4) as written, defaults (§6) as named, to be built by a fresh Opus Code session fired from `docs/prompts/PROMPT-cc1-rulings-and-address-model-slice-build-session.md`, the queue runner BARRED, fixture-only, the two migrations written and not run — with `HS-3` IN?"* **The prompt's Step 0 test is satisfied by `#149` at HEAD and by nothing else.** The YES neither accepts nor declines the seven remaining proposed items; touches neither `RF-2`, `TFI-1` nor `H12-v`; authorizes no vendor; runs no migration; leaves the engine outside the GL-1 floor.

## §6 — DECISION 5: `H12-v` — ***"1"*** — BOTH ASKS THIS WEEK
(a) Michael reads the AWS BAA in AWS Artifact himself (the memo's §4 / §8 gap 4 — self-service acceptance by any account's admin; no page states a size condition; none affirms a solo may accept); if unclear, an email to AWS support, drafted; (b) an email to the malpractice carrier or broker carrying the memo §7's corrected frame and thirteen questions verbatim plus the in-writing request, drafted. Both are DRAFTS in project knowledge (`claude_Draft_Email_Malpractice_Carrier_AI_Drafting_2026-09-07.md`, `claude_Draft_Email_AWS_BAA_Solo_Firm_2026-09-07.md`), never sent by Claude, addressee placeholders (not answered). Nothing about a credential's home is decided; the BAA stays a hard gate.

## §7 — DECISION 6: THE NEXT MODULE — ***"Firm obligations first - also, the cr-3 and versioned manual are in the knowledge repo."***
FIRM OBLIGATIONS is next (order beyond "first" not stated, not inferred). The next act on that track is a firm-obligations DESIGN sitting — an open design pass whose only prior art is the 2026-08-22 voice rulings (`#137`) and the roomless `FO-1`–`FO-7`; nothing is authorized to build. **`Q-IN2-1`'s "Yes — both" has LANDED:** the blank CR-3 form and its versioned manual are in `Documents\Knowledge Repo` (his hand; bridge-reachable; not the repo) — the crash-report module is buildable at the design level whenever called.

## §8 — DECISION 7: HOUSEKEEPING, SEVEN ROWS, ONE AT A TIME
1. `spec-feedback.md` renumbering — ***"leave and cite by heading"*** (operational note, v30).
2. TRANSIT, the `#148` pair — ***"(a), and create the Session Captures folder in the knowledge repo folder."*** → `Documents\Knowledge Repo\Session Captures\` created and named the permanent home.
3. TRANSIT, the `#147` pair — ***"Put them in the same zip if its not a problem."***
4. `HS-8` — ***"Deliberate"*** (held on PR-3; the row closes).
5. `HS-5` into v30 — ***"Yes"***.
6. The leftover `inbox/` folder — ***"Deleted"*** (verified empty over the bridge).
7. The seven remaining proposed items — ***"Name the sitting."*** → **THE POST-`CCS-1` WALK**.

**TRANSIT executed, six files:** zip 1 `brennan-case-manager_transit-0903-and-0905-pairs_2026-09-07.zip` (80,074 B, sha256 `0cd420cc3dd320d41b87b4cee83cbd8b2884d35213a6d016f2262b96173669ba`; the `#147` and `#148` pairs, 76,445 B, 23,012 units) — *"permission granted."* / *"go"*; zip 2 `brennan-case-manager_transit-0902-late-pair_2026-09-07.zip` (39,746 B, sha256 `95fb51082734f1ac0a78f5a6c44da0d283aa2293d6853760db218c27e42ac9b0`; the `#146` pair found eligible on the post-delete listing, 37,140 B, 11,246 units) — *"Yes, relocate."* / *"Permission granted."* / *"Go"*. Both built in his Chrome from the docs API, downloaded on a fresh tab's first attempt, verified in `Downloads\` and again in `Session Captures\`, then deleted; the meter moved by exactly 23,012 and then 11,246 (calibration confirmations four and five). Nine docs remain; the `#137` Voice2 pair stays (its FO/BR/DA content has no design-doc home — and is the firm-obligations sitting's prior art).

## §9 — WHAT THIS SITTING DID NOT DO
Built nothing. Authorized nothing except `CCS-1`, in his quoted word. Proposed no registry entry and characterized no law — **PF-1 did not fire**, recorded so the skip is not silent (the AWS and carrier drafts are questions, not propositions; TRCP 195.5(a)(1) appears only as FE-18's ratified basis). Edited no ratified text — the gates doc takes an APPEND, item 2 an ANNOTATION, the register rows annotations and flips. Reopened nothing closed at `#148`; did not re-propose the withdrawn review. Did not open the live matter; swept nothing on his machine (the `Knowledge Repo` top level was listed once to create the folder; nothing below it was read). Did not act on BUILD-STATE's stale NEXT-ACTS bullet (flagged for the runner). **Corrections owed by this sitting: none** — one Claude gloss was caught before it bound (D1(ii), the prose-parsing bar's scope, named in-session and never asserted as a rule).

## §10 — TRIGGERS
**1 FIRED** (the floor's completing re-check; v30 is the trigger-1 edition). **3 did not fire** (module design; operational notes are not conventions). **4 FIRED** (six files left; a permanent home named). **5 read as not firing** (the SMTP token's home recorded for the first time; the arrangement dates from 2026-08-20 — his read if otherwise). **6 did not fire** (BUILD-STATE-vs-itself, flagged).
