# Five-Look Sign-Off Record — 2026-08-18 (#110)

**Canonical repo path (proposed):** `docs/specs/five-look-signoff-record-2026-08-18.md` — NEW file, rides this packet.

**Status: RECORD.** Every verification below is **Michael's word, recorded per item before any file
was touched**. Directed edits at §3; executed by the queue runner.

**Session:** #110, design side, **Fable 5**, Cowork — the third entry from the one 2026-08-18
evening conversation (#108 → #109 → this), resumed after the SIXTY-THIRD runner batch executed the
#109 packet. Ceiling at HEAD: **#109** → this entry is **#110** (design-minted; TOC-6).
**Reconcile basis:** HEAD `c9cfc94` on `master`, bridge lock-free reads; `inbox/` empty;
`.git/index.lock` absent; counts re-derived by anchored grep at this HEAD — enforcement
`docs/specs/legal-rule-registry-discovery-enforcement-and-pleading.md` **40 status lines,
25 VERIFIED / 15 UNVERIFIED**; criminal `docs/specs/legal-rule-registry-criminal-plea-and-costs.md`
**7 lines, 5 VERIFIED** — the #109 arithmetic expectations landed exactly. **The carrier-duties
file is NOT touched this batch.**
**DT-1:** wall clock `Tue Aug 18 22:44 CDT 2026` at re-reconcile (container on 08-19 UTC — the
evening window, still). All stamps 2026-08-18.
**Instructions v22 in force. Runner v11.**
**Model routing, recorded:** Michael asked mid-session "Should I just run this on Opus?" — answer
given and followed: the five looks finished here (setup hot, one picker from done, Fable bar
fresh from the afternoon reset); the two authorized Opus-lane runs ship as the copy-paste prompt
in this packet (§4). No convention relaxes on either model.
**PF-1's run and result are recorded at §5 — this header sentence and §5 were both finalized
AFTER the run completed and before the zip was saved,** so neither is a prediction.

---

## §1 — FIVE VERIFICATIONS, ALL MICHAEL'S WORD, DATED 2026-08-18

All five entries live in the enforcement file. The four verbatim entries were compared
**programmatically, character-level (whitespace-normalized), against the raw extractions** from
`Documents\Knowledge Repo\Civil\texas-rules-of-civil-procedure July 2026.pdf` — commands run in
the device VM, quotes spot-checked against raw; **all four returned exact matches, zero flags**:

1. **Entry A — `## TRCP 192.3(h) — a person's right to his or her own statement`: "VERIFIED."**
   Rule line exact against the rule's final sentence; the characterization note is labeled; the
   #109 enforcement cross-reference in place.
2. **New entry — `## TRCP 215.1(e) — compelling compliance with a person's own-statement
   request`: "VERIFIED."** Rule line exact (the rule's two sentences, caption omitted). **The
   preamble-tension flag is now verified text that asserts no answer** — "A party" vs "the person
   who made the request" stays open on the rule's face.
3. **Entry B — `## TRCP 193.4(a)`: "VERIFIED."** Exact; construction note labeled.
4. **Entry F — `## TRCP 199.6`: "VERIFIED."** Exact; the corrected no-asymmetry passage (exact
   193.4(b) quote) stands inside the verified entry.
5. **The WS-3 entry — `## TRCP 192.3(h) — the privilege limit on witness-statement discovery`:
   "VERIFIED."** Put with its layered basis stated, not smoothed: **mechanically verified
   tonight** — the quoted 192.3(h) clause and the full comment-9 quotation, both exact against
   the PDF, and the Cite line's comment-to-Rule-192 correction consistent with the PDF's
   structure; **resting on the record's reads, not re-checked tonight** — the *Fontenot* and
   *ExxonMobil* characterizations (#102 full reads, V-9 authorship on the face: Livingston, J.;
   Seymore, J.), the *XL Specialty* boundary and *Arden* condition (#104 reads, #108 adoptions),
   and the burden/waiver conditions as described. **The first characterization-bearing entry to
   reach verification since the V-9 amendment; his judgment, not a text match.** **The Pinpoint
   caveat SURVIVES verification as verified text:** star-pagination pinpoints must be verified
   against the reporter before any quotation enters a filing.

**Backlog effect once §3 executes — arithmetic expectation, re-derived at HEAD by the executing
batch:** enforcement 40 lines, 25 → **30 VERIFIED / 10 UNVERIFIED**; criminal unchanged 5/2;
**backlog 47 entries, 35 VERIFIED / 12 UNVERIFIED.** The ruled-wording verification tail is now
EMPTY — no entry with adopted-but-unverified wording remains.

---

## §2 — THE ONE ADDITIONAL PICK

**Opus handoff — his pick: "Yes — cut it (Recommended)."** The copy-paste prompt for the two
authorized-and-unrun Opus-lane acts (FC-13's five entry drafts; the Q-WS3-5 five-lead read) rides
this packet at §4 and routes to `docs/prompts/`. Cutting the prompt authorizes nothing new — both
acts were authorized at #108; the prompt restates their ruled terms.

---

## §3 — DIRECTED EDITS (queue runner; exact scope)

**File named canonically: all five edits are in
`docs/specs/legal-rule-registry-discovery-enforcement-and-pleading.md`. No other registry file is
touched.**

1. **Five Status moves** (and only these five). For each: **REPLACE the entry's existing
   `**Status:**` line** (confirmed design-side at HEAD to read the bare `**Status:** UNVERIFIED.`
   on all five; if it carries variant or trailing text at execution, replace the WHOLE Status
   line and report the variant; **if no `**Status:**` line is found in the entry, STOP**) with
   `**Status:** VERIFIED — Michael, 2026-08-18 (#110).` and insert the entry's `**Verified:**`
   line **immediately after the Status line** — never a second Status line. **Pre-state check
   first: if re-derivation at HEAD disagrees with the stated pre-state (40 lines, 25 V / 15 UNV)
   for any reason other than this packet's own prior execution, STOP and report before moving any
   line.**
   - `## TRCP 192.3(h) — a person's right to his or her own statement`:
     `**Verified:** 2026-08-18 one-look (#110): Rule line character-exact against the July 2026 TRCP PDF extraction; zero flags. Record: five-look-signoff-record-2026-08-18.md §1.`
   - `## TRCP 215.1(e) — compelling compliance with a person's own-statement request`:
     `**Verified:** 2026-08-18 one-look (#110): exact two-sentence match; the preamble-tension flag survives as verified text asserting no answer. Record: five-look-signoff-record-2026-08-18.md §1.`
   - `## TRCP 193.4(a) — hearing on objections; the objecting party carries the evidentiary burden`:
     `**Verified:** 2026-08-18 one-look (#110): character-exact; zero flags. Record: five-look-signoff-record-2026-08-18.md §1.`
   - `## TRCP 199.6 — hearing on deposition objections; the party avoiding discovery carries the burden`:
     `**Verified:** 2026-08-18 one-look (#110): character-exact; zero flags; the no-asymmetry passage stands. Record: five-look-signoff-record-2026-08-18.md §1.`
   - `## TRCP 192.3(h) — the privilege limit on witness-statement discovery`:
     `**Verified:** 2026-08-18 look (#110): quotations mechanically verified against the July 2026 TRCP PDF; case characterizations verified on the #102/#104 reads as recorded; the Pinpoint caveat survives — reporter check required before any filing quotation. Record: five-look-signoff-record-2026-08-18.md §1.`
2. **New file:** route this packet's `OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md` to
   `docs/prompts/OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md`, verbatim. It is a
   chat-dispatch prompt (design-chat execution, the CHAT-DISPATCH class) — a Code session never
   executes it.
3. **Queue merge, two acts (QR-6(b)):** the sixty-third batch's successor row (the five-entry
   one-look verification) is **CLOSED — completed at #110, all five VERIFIED**; if that row cannot
   be located unambiguously — or no carrying row is identifiable at all — **STOP and tell Michael;
   never guess.** Note on
   the row (or its successor line) that the FC-13 drafting + Q-WS3-5 read now travel via the
   routed prompt at `docs/prompts/OPUS-RUN_…md`. `Q-WS2-3`, `Q-WS3-3`, `Q-WS3-4`, the `WS-P`
   rows, `Q-AUTH-1`, `Q-IN2-1`, and the v23 row are untouched. Status-header reconcile sentence
   updated to #110.
4. **BUILD-STATE rewrite + TOC regeneration** per runner Step 4 — re-derive everything at HEAD.

---

## §4 — THE OPUS PROMPT (shipped as its own file in this packet)

See `OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md` — self-contained, one paste into an Opus 5
design chat. It restates the ruled terms: the five FC-13 entries drafted under the standing
verbatim direction with SOURCING channels named per item (statutes corpus for Prop. Code ch. 55,
CPRC ch. 146, §18.001/§41.0105; the TRCP PDF for Rule 47(c); the State Bar TDRPC PDF, FC-14's
fourth channel, for 1.04(f)); the Q-WS3-5 five-lead read via FLP/CourtListener under V-9
discipline, retrieval only; everything entering UNVERIFIED; PF-1 before the packet ships; IDs
packet-local, minting Michael's.

---

## §5 — PF-1 PREFLIGHT RESULT (completed, and the corrections applied, before the zip was saved)

Two independent read-only adversarial agents ran against the packet's full draft (record, work
order, and the OPUS-RUN prompt) plus the raw extracts and this turn's recorded picks. **Asserted
defects: 14 (6 + 8, overlapping; none HIGH; none requiring a ruling — no unruled act was directed
this time; the one authorization leak ran the OTHER way and was removed).** Corrected before
shipping: the OPUS-RUN prompt's "case-law-side" mislabel of the four statutory/rule candidates and
its "picks verbatim" gloss; **its 1.04(g) note-allowance struck** — nothing about 1.04(g) was
ruled, and a prompt claiming to authorize nothing new may not grant even a restrictive permission
beyond the #108 terms; the hard-coded month in both staging-doc filename templates (now DT-1
Central date, open); Task 1's done-check re-aimed at the drafts doc (the registry can never show
FC-13 entries pre-insertion); the resume rule re-anchored to staging docs at HEAD; the Status-move
REPLACE given variant-text and no-line branches plus a pre-state STOP; the queue-merge fallback
given a terminal STOP; the log entry's pick quote completed ("(Recommended)"), its
verbatim-direction attribution fixed to #108, its heading figure labeled as expected, and §0's
enumeration given the queue merge; the record's PF-1 header sentence finalized after the run.
**If this section ever reads as an unfilled bracket, the run was skipped — STOP and tell
Michael.**
