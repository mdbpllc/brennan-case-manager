# THE FIRM-OBLIGATIONS RULING SITTING — RULINGS OF 2026-09-10

> **STATUS: RULING RECORD — every ruling in Michael's words (his pick's label where he selected among option texts Claude wrote; his sentence verbatim where he wrote one), and what each closes.** Canonical repo path: `docs/specs/firm-obligations-rulings-2026-09-10.md` (CAP-2 class: RULING). Filed by packet `push-to-code_firm-obligations-ruling-sitting_2026-09-10`; session-log entry `#155`. The running ledger it is copied from is filed as EVIDENCE at `docs/record/firm-obligations-sitting-2026-09-10/ruling-ledger-2026-09-10.md`.
> **This document RECORDS rulings.** The slice it authorizes is `docs/specs/firm-obligations-build-slice.md`; the SPEC (`docs/specs/firm-obligations-module-spec.md`) takes a status line and an appended §16 by the packet; the sheet takes one RULED line; neither's authored text is edited.
> Cite by heading or quoted sentence.

## §0 — THE SITTING

Typed, Cowork, Fable 5.1 (`claude-fable-5-1` per the environment) throughout; device bridge granted on `C:\Users\Brennan\brennan-case-manager` at 21:4x CDT — **the bridge VM's shell could not mount the folder (*"no Plan9 drive shares mounted"*), so every repo read was a staged read-only copy via `device_stage_files`, and the packet is delivered by `device_commit_files`; no `git` command ran on his machine and no lock could be stranded.** HEAD `357ca8e` = the local `origin/master` tracking ref (`.git/refs/heads/master` = `.git/refs/remotes/origin/master`; `357ca8e` committed 2026-09-09 12:23:28 CDT per the reflog; `FETCH_HEAD` mtime 12:23:39 CDT by `device_list_dir`) — a local read, not a QR-3 pass; `inbox/` EMPTY; no `.git/index.lock`. Batch 95 (`#154`) at HEAD; a follow-on Opus commit `357ca8e` (spec-feedback's counting-method section; a bracketed correction inside batch 95's runner line). Knowledge meter **1,628,404 / 2,000,000 = 81.4%** at open, thirteen docs — `Q-CAP-5(a)` did not fire. Instructions v31 in force by a live read of the field. Michael's Fable usage meter, his reading: **42%**. Newest design entry at the live log `#154` (heading-anchored grep); this sitting is `#155`; `#155` at 0 hits in the staged files (the runner re-checks repo-wide). Every date Central (DT-1; the container read 2026-09-11 UTC throughout; the sitting opened 21:34 CDT, its rulings closed 23:05 CDT, and the entry is stamped the Central date of the work whether or not the packaging crosses midnight — the `#153` precedent). His opening words: *"Let's keep working. It's Thursday evening and we are only at 42% usage on Fable. Do what ever you suggest we do next and don't worry about tokens."* His form instruction for the sitting is the sheet's: one decision at a time; the mock beside it; dates never asked. **Fourteen findings were adopted as proposed; one (`FOM-12`) came back as a composite outside the offered set and was re-asked twice, narrower** — the CC-1(a) exhibit of this sitting, and the one place the module's design moved.

**The two picks that opened the sitting** (starting points, not a menu): *"FO ruling sitting now (Recommended)"*; **`Q-API-20`: *"After (Recommended)"*** — the split-store slice is drafted after the FO sitting and the post-`CCS-1` walk, once the Azure facts (`Q-API-18`) are in hand. `Q-API-20` CLOSES on that pick.

## §1 — THE ELEVEN DECISIONS, IN THE SHEET'S ORDER

| Sheet | `Q-FO` | Michael's words | The rule, and what it closes |
|---|---|---|---|
| **DECISION 0** — the word | `Q-FO-0` | ***"Firm obligations (Recommended)"*** | "Firm obligation" is the word: nav "Obligations" at `/firm/obligations`, card "Firm obligations", tables `firm_obligations` / `firm_obligation_occurrences`; "firm deadline" stays `FC-7`'s matter-scoped term. |
| **DECISION 1** — the closes | `Q-FO-2`(a),(c) | ***"As drafted (Recommended)"*** | Two closes only — Done; Not-applicable only where the duty can lapse for a period, reason required; Undo; **no third "delegated" state**; no snooze, later, dismiss or bulk action. `FOD-18`, `FOD-19` stand. |
| **DECISION 2** — a missed period | `Q-FO-2`(b) | ***"As drafted (Recommended)"*** | Filings serial; cadences collapse; per-obligation override. `FOD-5` stands (as amended by `FOM-1`, `FOM-4`). |
| **DECISION 3** — where it lives | `Q-FO-1` | ***"Two tables (Recommended)"*** | `firm_obligations` + `firm_obligation_occurrences`; `calendar_events` untouched; `FO-1`'s "new SCOPE, not a new type" is read as the behaviour, not the table. An engineering call, confirmed; no migration authorized by the answer. |
| **DECISION 4** — `FO-5` | `Q-FO-3` | ***"Owner-scope field now (Recommended)"*** | `ownerScope` firm/attorney from the first migration; nothing on screen at solo; fan-out one occurrence per licensed user, and the assignee axis, at multi-user (gate 2). **`FO-5` CLOSES.** |
| **DECISION 5** — `FO-7` | `Q-FO-4` | ***"Thin, no hook; FO-7 closes (Recommended)"*** | A research memo is not "spec'd" in `FO-3`'s sense; the conditional limb does NOT fire; no `externalRef`; `doneNote` + `filedAt` are the only money-adjacent fields; a QuickBooks slice, if ever authorized, adds a nullable `ledgerRef` then. **`FO-7` CLOSES.** |
| **DECISION 6** — weight | `Q-FO-6` | ***"Two classes; routine on the register only (Recommended)"*** | `weight` hard/routine; the `/cases` card shows `hard` only; order and emphasis only, never behaviour. `FOD-11` stands. |
| **DECISION 7** — Outlook | `Q-FO-5` | ***"All-day event, separate "MDBP Firm" calendar, reminder at the lead (Recommended)"*** | An all-day event on the TARGET date (per `FOM-12`) in a separate "MDBP Firm" calendar, subject *"Firm obligation: <name> (<period>)"*, `isReminderOn` with the reminder at the lead window; no new consent. Keep-vs-delete on Done HELD for his Outlook (hands-on; `FOD-22` names the interim). |
| **DECISION 8** — the registry boundary | `Q-FO-7` | ***"Registry entries, new file (Recommended)"*** | A FIFTH registry file `docs/specs/legal-rule-registry-firm-obligations.md`, drafts by a LATER Opus act on the `FC-13` precedent, PF-1 running on that packet; every entry UNVERIFIED until he verifies; the module's `sourceNote` points at the entry; `Q-STAT-5`'s practice-side pass folds in; the `FOM-12` roll defaults are entries in the same file. **The registry's cases-vs-practice boundary — UNDECIDED since `FC-13` — is DECIDED BY ACT: the registry covers the firm's PRACTICE too.** Nothing drafted or inserted by this sitting. |
| **DECISION 9, Part A** — acquisitions and channels | `Q-FO-9` | ***"All five; (4) and (5) as named channels (Recommended)"*** | His hand acquires (1) the State Bar Rules + MCLE Regulations, (2) the Rules Governing the Operation of the Texas Access to Justice Foundation, (3) the Family Code zip; **(4) TIDC-published county indigent-defense plans and (5) the U.S. Code via the Office of the Law Revision Counsel / govinfo.gov become the FIFTH and SIXTH SOURCING channels — TRIGGER 3 FIRES; instructions v32 is the trigger-3 edition.** Which counties' lists he is on stays his fact (`H5`). |
| **DECISION 9, Part B** — the catalog | `Q-FO-8` | ***"Offer all 35; activate the core at go-live (Recommended)"*** | All 35 `FOT-` templates OFFERED; his intended go-live activations are twelve rows — `FOT-1`, `FOT-2`, `FOT-4`, `FOT-6`, `FOT-8`, `FOT-9`, `FOT-19`, `FOT-22`, `FOT-23`, `FOT-24`, `FOT-25`, `FOT-27` (the BOI note seeds inactive as the spec has it); everything else activated when its fact holds; every activation takes his date in the product (`FOD-9`). Weights as the spec drafted them — no change named. |
| **DECISION 10** — rows | `Q-FO-10` | ***"Mint all, and carry the three definitions (Recommended)"*** | The packet mints register rows: `FO-1`, `FO-2`, `FO-3` born ✅ with their `#137` ruling text; `FO-4` ✅ (the design pass — spec and sheet exist, DECISIONS 0–9 ruled); `FO-5` ✅ on DECISION 4; `FO-7` ✅ on DECISION 5; `FO-6` ⬜ (the firm-wide calendar page; hands-on); `BR-3` ⬜ pointing at `FOT-22`; and the definitions of `BR-2`, `BR-4`, `DA-2` — and `FO-4`, `FO-5`, `BR-3` — copied verbatim from the `#137` Voice2 capture into EVIDENCE so that pair becomes transit-eligible (the packet carries the capture's whole PART 12 with them, `INS-1` included — a packet act on `docs/specs/id-collision-report.md`'s finding, not part of his ruling). **The `FO-`, `BR-` and `DA-` ids are thereby MINTED by his act** (the closed register's own header said the minting was his). |

## §2 — THE FIFTEEN FINDINGS, EACH PUT RIGHT AFTER ITS DECISION

| Finding | Michael's words | Disposition |
|---|---|---|
| `FOM-11` (DECISION 1) | ***"Adopt (Recommended)"*** | §4.2 drops "within the same period"; `FOD-7`'s "while the next occurrence is untouched" is the only undo test. |
| `FOM-12` (DECISION 1) | ***"I am ruling with option 1, but with a caveat: If the deadline falls on the weekend and hence is due on the following business day, I want it to be made clear that the due date is the following business day, but also want strong encouragement that we satisfy and meet the deadline by the business day prior to the weekend/holiday."*** | Option 1 adopted (never "overdue" or a day count on a weekend-dated occurrence as such) AND the caveat changes `FOD-1`: the roll IS shown as the due date where it applies; the business day before the weekend/holiday is the TARGET, strongly encouraged. Re-asked twice, narrower: **(a)** *how the module knows a given obligation rolls* — ***"Per-obligation setting, confirmed at activation (Recommended)"*** (a `weekendRule` on every obligation: rolls-forward / no-roll / unknown; template defaults PROPOSED and registry-backed under DECISION 8; confirmed at activation; unknown = the `FOD-1` note + the target line); **(b)** *holidays* — ***"Weekends now; holidays flagged, not computed (Recommended)"*** (weekends computed; a holiday calendar comes once, with `FC-9`'s). And, at the slice question: ***"Ship as unknown; defaults come with the registry act (Recommended)"*** — no template carries a roll default; PF-1 does not fire on this packet. The three-state synthesis (rolls-forward → due = next business day; no-roll → due = the rule date; unknown → the note and never "overdue"; the target = the business day before, in all three) is Claude's reading, named in the `FOS-1` question and confirmed by his YES. |
| `FOM-1` (DECISION 2) | ***"Adopt both limbs (Recommended)"*** | A cadence completion closes the period it falls in — next = the first rule date in a LATER period; a dated kind under collapse never re-materializes its own date — next = the first rule date after the later of its due date and the completion. `FOD-5`'s collapse wording and `materializeNext` change; interval-from-completion unaffected. |
| `FOM-4` (DECISION 2) | ***"Adopt (Recommended)"*** | First occurrence = the first rule date on or after today, stated for the fixed kinds too; a `serial` obligation takes an OPTIONAL "last period completed" at activation; if given, the first occurrence is the period after it, possibly already overdue. `FOD-9` unchanged. |
| `FOM-10` (DECISION 2) | ***"Adopt (Recommended)"*** | One plain sentence in §4.1: each Done materializes the next already-overdue period — two years behind is two clicks. |
| `FOM-15` (DECISION 4) | ***"Adopt (Recommended)"*** | §5.2's parenthetical becomes "only once more than one licensed user exists"; no owner badge at solo. |
| `FOM-9` (DECISION 6) | ***"Hard first, then most-overdue (Recommended)"*** | Inside the Overdue pin: every hard item above every routine item; within each class, most days overdue first. §4.3 states it. |
| `FOM-13` (DECISION 6) | ***"Adopt the conjunctive reading (Recommended)"*** | `FOD-14`'s second limb becomes conjunctive: the card shows a hard item if it is OVERDUE, or if it is LIT and its target is within 14 days — the card never shows what the register has not lit, and never drops an overdue item (`FO-2`). The option text's *"lit (or overdue) AND its target is within 14 days"* is read as the mock's rendering, which `FOM-13` describes in those words; his eye at the hands-on sitting. |
| `FOM-14` (DECISION 6) | ***"Adopt (Recommended)"*** | §4.3 reworded: the register shows every open occurrence; lit and overdue are the emphasis. |
| `FOM-8` (DECISION 7) | ***"Adopt (Recommended)"*** | The long reminder is verified against live Graph as a named build item; fallback = a second all-day "lights today" event on the `lightsOn` day (the lead day). No To Do consent. |
| `FOM-2` (DECISION 9) | ***"Adopt (Recommended)"*** | "Applies if" split from "Can lapse per period"; `conditionalPerPeriod` on eight rows: `FOT-8`, `FOT-10`, `FOT-11`, `FOT-13`, `FOT-14`, `FOT-15`, `FOT-16`, `FOT-17`. |
| `FOM-3` (DECISION 9) | ***"Adopt (Recommended)"*** | A "Later" group at the foot; Done allowed there; the date in full. |
| `FOM-5` (DECISION 9) | ***"Adopt (Recommended)"*** | An Inactive section at the foot, collapsed, Activate… per row; never-added templates live only in the catalog. |
| `FOM-6` (DECISION 9) | ***"Adopt (Recommended)"*** | A month-precision occurrence lights at the EARLIER of the 1st and target − lead. `FOD-17` amended. |
| `FOM-7` (DECISION 9) | ***"Adopt (Recommended)"*** | `periodLabel`: "by <due date>" for interval kinds; the due year for one-time. |

**Tally:** fifteen of fifteen disposed; fourteen adopted as proposed; `FOM-12` adopted as a composite with two narrowings and a slice-level pick; none rejected, none edited.

## §3 — `FOS-1` — THE SLICE AUTHORIZATION — RULED YES

The slice (`docs/specs/firm-obligations-build-slice.md` — 49,838 B as put to him; the filed size is in the packet's `CHECKSUMS.txt`, the difference being the status line and §11 taking his word and the audit's fixes, each listed in the audit record) and its kickoff prompt (`docs/prompts/PROMPT-firm-obligations-slice-build-session.md`) were drafted AFTER DECISION 10 and delivered as files BEFORE the question, on the `CCS-1` pattern. Put WHOLE, on the one text: *"`FOS-1` — Does Michael authorize the FIRM-OBLIGATIONS BUILD SLICE at `docs/specs/firm-obligations-build-slice.md`, scope IN (§3) and OUT (§4) as written, the weekend rule as synthesized in §2.3, defaults (§6, `FOD-20`–`FOD-33`, and `FOD-1`–`FOD-19` as amended) as named, to be built by a fresh Opus Code session fired from `docs/prompts/PROMPT-firm-obligations-slice-build-session.md`, the queue runner BARRED, fixture-only in demo mode, the one migration written and not run — with `FOD-20` (the `cancelled` value in the `review_log` CHECK) IN?"* The option set: *Yes* / *Yes, `FOD-20` OUT* / *Break it down into limbs* / *Not tonight* (the labels as the widget put them). **RULED YES — Michael, verbatim, 2026-09-10 ~23:03 CDT: *"Yes"*.** So `FOD-20` is IN and the §2.3 synthesis, named in the question's text, is confirmed. **The prompt's Step 0 test is satisfied by `#155` at HEAD and by nothing else.** The YES authorizes no registry entry and no legal characterization (DECISION 8's drafting act is separate), runs no migration, consents to no scope, seeds nothing active in Supabase mode, and touches neither `FO-6`, `DA-*`, `BR-1`/`BR-2`/`BR-4`/`BR-5`, `H12-v` nor `Q-API-20`'s sequencing.

## §4 — THE HANDS-ON QUEUE — ***"Accept all five (Recommended)"***

The five spec §11 items — the card's wording, position, line count and 14-day horizon; the register's twelve-month shape and the Done / Not-applicable affordances; Outlook as seen in his Outlook (keep-vs-delete on Done, the subject, the reminder at the lead, the To Do route only if he ever grants the scope); whether a firm-wide calendar page should exist (`FO-6`'s real content) and visible-but-filtered on a case tab; lead-window defaults once he has felt one — are ACCEPTED onto the CC-1 hands-on queue, gated on this build in demo mode, and named **THE FIRM-OBLIGATIONS HANDS-ON SITTING**. Claude calls it when the build lands (CC-1(c)). The queue after this sitting: twelve accepted-gated (unchanged) + **five accepted-gated (these)** + seven proposed (unchanged) + two held 2026-09-09 (unchanged) + the post-`CCS-1` walk's agenda (unchanged).

## §5 — WHAT THIS SITTING DID NOT DO

Built nothing. Authorized nothing except `FOS-1`, in his quoted word. Proposed no registry entry and characterized no law — **PF-1 did not fire**, recorded so the skip is not silent (the roll defaults were expressly kept OUT of this packet by his pick; every catalog cite string in the slice is a copy of a SPEC §7 cell). Edited no authored text — the spec takes a status line and an appended §16, the sheet one RULED line, the register rows and flips. Asked for no date (`FOD-9`; CC-1(b)). Swept nothing on his machine (the repo checkout's own listings only; no folder outside it was requested or read). Ran no `git` command on his machine (the VM shell could not mount the folder). Did not open the live app. Did not put `Q-STAT-7` (offered as optional at the close; not taken as of this record). **Corrections owed by this sitting: one Claude miscount caught before it bound** — the ledger first wrote DECISION 9B's activation set as "thirteen rows"; a recount before the slice was written made it twelve, and the ledger says so beside the correction. The DECISION 0 prose was first sent without an answer widget and Michael asked whether the session was stuck; it was re-put as a widget — a form slip, not a record error.

## §6 — TRIGGERS

**3 FIRES** — DECISION 9A adds two named SOURCING channels (TIDC county indigent-defense plans; the U.S. Code via OLRC / govinfo.gov): **v32 is the trigger-3 edition**, delivered by this sitting and filed as delivered; its paste is Michael's and a later live read is the evidence. **1 does not fire** (no real record; the module is outside the floor). **2 does not fire.** **4 does not fire tonight** — two capture files ENTER project knowledge as TRANSIT files (this sitting's pair; the `#151`, `#152` and `#154` pairs entered the same way without a firing — the standing practice since v26; the 2026-08-07 firing was an addition of RESIDENTS, the probate index set) and none leaves; the `#137` Voice2 pair becomes ELIGIBLE once this packet is at HEAD and its definitions are in the repo, and its departure fires trigger 4 then. **5 does not fire** (no credential changes; DECISION 7 needs no new consent). **6 does not fire** — BUILD-STATE at `357ca8e` was read against v31 and no contradiction found; its FIRM OBLIGATIONS bullet and NEXT ACTS line are overtaken by this sitting, which is staleness for the runner's refresh.
