# ATTORNEY REVIEW QUEUE — CLEANUP ADJUDICATION (2026-09-01)

> **STATUS: PROPOSED — RULING-READY, ADJUDICATED, NOT RULED. THIS DOCUMENT CLOSES NOTHING.**
> Every entry below is the adjudicated disposition of one register row, with the decisive evidence
> quoted and the instrument staged. **No row is closed, no glyph flipped, no ID minted, renamed or
> merged, and nothing enters or leaves the build queue until Michael rules — row by row, or block by
> block where he says so in words.** Where this sheet contradicts the 2026-08-24 audit or the
> 2026-08-25 hardening, all readings are given and the sheet says which it adopts and why — that IS
> the adjudication the hardening said was owed; the ruling stays his.

**Canonical repo path:** `docs/specs/attorney-review-queue-cleanup-adjudication-2026-09-01.md`
**Produced by:** the 2026-09-01 typed design session (Cowork, **Fable 5** per the environment; DEVICE
BRIDGE granted on the checkout), DT-1 Central. **Pinned to HEAD `7f02131f215605b3062a8351c2b7ec96f46c004c`**
(batch 85), `origin/master` == `HEAD` by `git ls-remote`, `inbox/` empty of zips. Register at that HEAD:
**487 marked rows = 356 `⬜` + 126 `✅` + 5 `🟡`** (programmatic count, `^\s*- (⬜|✅|🟡)`).
**CITE-STABILITY:** register line numbers below are AT `7f02131` and are finding aids only; every staged
instrument anchors on the row's full first line as an exact string, re-verified unique at execution HEAD.

## §0 — THE THREE LAYERS, AND WHAT THIS ONE ADDS

1. **The 2026-08-24 audit** (11-agent Fable fleet + Opus consolidation, at `edea20b`): classified all 362
   then-open rows; found 108 not open rulings. **UNTRACKED at HEAD — it was never filed** (Michael ruled
   DO NOTHING, batch 83); read here from the working tree.
2. **The 2026-08-25 HARDENING** (Opus, five checkers, at `edea20b`): the EVIDENTIARY half of the verify
   pass — of the 58 closure-class candidates, **19 verify clean, 39 need Michael's eye, 11 would destroy
   an open question if acted on as written; not one of the ten "duplicates" is one question in two rows.**
   Its own §7: *"No adversarial verify pass ran on THIS file either… the adjudicative half is still owed
   and is Fable-class."*
3. **THIS SHEET** — the owed adjudicative half. Wave 1 (this morning, **Fable 5**, eight read-only
   verifiers, all eight returned content — the `#104` dead-fleet check passed): every one of the 108
   candidates re-verified at `7f02131` against the live log, the closed archive and BUILD-STATE, each
   with a disconfirmation attempt and a unique-text destruction test — **115 register rows covered: 114
   open (109 `⬜` + 5 `🟡`) and one `✅` (`SK-v2`, verified closed at batch 83), spanning the audit's 108
   candidate items (two share a line; one, `FO-7`, has no row and was verified as such) plus the
   satellite rows verified beside them.** Wave 2 (the remaining **247** open rows; 114 + 247 = 361, the
   register's full open count) **launched on Fable and DIED AT THE WEEKLY LIMIT — eight agents, zero
   output; rerouted the same hour to an Opus session per §7.2** (mechanical evidence-gathering is
   execution); its results fold in at §6. **Three batches landed between the hardening and this sheet
   (`#138`–`#140`, runners 83–85), and they change verdicts** — most visibly the v23-paste row, the
   hardening's own headline PREMISE-INTACT finding, which v28 (pasted 2026-08-31) has since dissolved.

**Evidence annexes:** the eight wave-1 verifier reports travel with this sheet's packet to
`docs/record/queue-cleanup-evidence-2026-09-01/` — **repo-only, bridge-reachable, OUTSIDE the design-side
sync by the standing `/docs/record/` exclusion (TC-4's rule: what would outgrow the sync belongs there)**.
Every quote below is copied from a staged byte-identical file and re-verified character-exact before this
sheet shipped (three independent Opus verifiers; their findings are fixed into this text and the fix pass
re-swept); the annex block for a row carries the fuller record. **Annexes ship exactly as the verifiers
returned them** — one annex cite typo found at verification (A1 cites the `#63` distillation ruling at
"ARCH L1303–1304"; the text is at ARCH L303–304) is recorded here rather than edited into the annex. One
source discrepancy likewise recorded, not resolved: the 2026-08-24 audit calls its fleet ELEVEN agents;
the hardening says NINE auditors — this sheet quotes each file's own words where it cites them.

## §1 — HOW TO RULE FROM THIS SHEET (CC-1(a): the blocks are a way of asking, never a menu)

Each row below is put INDEPENDENTLY. The blocks bundle rows whose evidence has the same shape so that one
word from Michael can move many — **"Adopt Block A" closes its rows on the quoted rulings; naming
exceptions ("Block A except FE-4") is expected, and an answer outside the blocks entirely is CC-1(a)'s
normal case and will be re-asked narrower.** Nothing closes on silence. Three vocabulary notes:
- **CLOSE** flips `- ⬜` to `- ✅` and APPENDS a dated closure sentence quoting the ruling cite — row text
  is never deleted, so nothing unique is destroyed (the `SK-v2` / `RECON-1` closure shape, batches 83/84).
- **CLOSE-SPLIT** closes the answered question and RE-MINTS the surviving limb as its own narrow row with
  full text (QR-1). The surviving limb's text is staged verbatim in Block C's own cells.
- **ANNOTATE-KEEP** stays `⬜` and takes a dated annotation so the row stops asserting a stale world.
Executing ANY of this is a packet through the queue, assembled ONLY from the rows Michael rules, each OLD
string re-verified unique at the executing HEAD (`QR-6(c)` discipline) — **with whitespace normalized when
re-verifying quotes against the log, because the log hard-wraps mid-quote and a plain literal grep reports
false NOT-FOUNDs** (at least eight of this sheet's verified quotes are wrap-split at HEAD; the wrap trap's known class).

---

## §2 — THE ADJUDICATED DISPOSITIONS, BLOCK BY BLOCK

### BLOCK A — CLEAN CLOSES: THE QUESTION IS ANSWERED, OR THE CONDITION IS MET, ON THE RECORD (HIGH confidence; nothing unique destroyed)

*Two shapes share this block, stated so its label over-claims nothing (the verification pass's finding).
**Closed on HIS RULING** — the row's whole question answered in Michael's words, every residue already
rowed: A-1…A-7, A-9, A-10 (and A-8's "his word" verification). **Closed on a DONE ACT OR EVENT** — the
row carries no open interrogative; its own closing condition is met on the record: A-11…A-20 (A-13/A-14
by mootness). The register's #114 scope line — "They await Michael's ACTION, not his RULING — and that is
the line this register is drawn on" — is why the second shape closes: a row whose act is DONE awaits
nothing. One word adopts the block; each row still stands alone.*

| # | Row (line@7f02131) | The question, and the ruling that answered it |
|---|---|---|
| A-1 | `FE-7` (L188) | "What does the queue look like pre-engine, and what does adoption look like?" — **ruled #53** (§13.4 live table, three candidates seeded) and **#63** (*"all four candidates ENTER §13.4 … Entering ≠ adopting; FE-7 guardrails hold"*). The guardrails survive in substance in the ✅ row at L213 (its restatement: *"Entering is not adopting … FE-7's guardrails hold"*), and FE-7's own guardrail sentence is retained by the append-only closure. Zero distillation activity since. |
| A-2 | `DL-INPUT` (L288) | Per-(case, party) deadline model? — **ruled #66** in the ruling run (*"all rulings Michael's … by widget or direct words"*): *"the deadline model is PER-(CASE, PARTY) — each party's response clock computes from its own service date."* `#75`'s refinement is PROPOSED elaboration, not contradiction; the pass's open questions live at `[DL-memo Q1]–[Q5]`/`ID-DL-1`. Closure records "ruled ≠ authorized: no deadline engine is in the build queue." |
| A-3 | `H22` heartbeat (L112) | Do TRCP entries queue behind entries 1–10 or interleave? — **ruled #66**: verification interleaves BY BUILD URGENCY. Collision noted in the closure: the OTHER `H22` (disclosures, warn-never-block, `#130`) is now `HD-22` and rowless — an ID, not this row. |
| A-4 | OAA demographics row (L133) | Cause-number-pending reminder; sex and race/ethnicity capture — **ruled #66** (*"DEMOGRAPHICS RULED … CAPTURES BOTH … Cause-number-pending reminder RULED YES"*); the row's own text says *"the question CLOSES."* |
| A-5 | `PF-2` (L518) | Adopt the `#118` re-sweep-after-fix proposal as standing? — **ruled #126** (*"Adopt as standing"*), now the RE-SWEEP AFTER FIX convention in instructions v24+/v28. Runner 79's own words: *"one word closes it in the next batch."* This is that word's landing place. |
| A-6 | `V-5` (L650, 🟡) | Split the three two-case entries? — **ruled #73** (*"V-5 — SPLIT ALL THREE two-case entries, one entry per case"*), EXECUTED (51st invocation; entries 19a/19b/20a/20b/21a/21b exist). The audit quoted a runner heading, not the ruling — the hardening caught that; the REAL #73 ruling is quoted here and in the annex. Residue has rows: `V5-IDS` (L606), `V5-ATTRIB` (L607-area). |
| A-7 | `V-6` (L667, 🟡) | — **ruled #73** (*"BOTH STAY, REWORDED to state their operative tests"*), executed via ROUTE-C adoptions, entries 33/34 VERIFIED 2026-08-17. Residue at `Q-T19-4`. |
| A-8 | `T-26` (L745) | — **ran and closed at #108** (*"T-26 RAN, LIVE, AND CLOSED … ENTRIES 13, 2 AND 29 VERIFIED — his word, dated 2026-08-18"*); successor act had its own row, closed #110. The hardening: "the cleanest close in the set." |
| A-9 | `Q-AUTH-1` (L777) | The Pharr/Hurlburt authority read — **authorized #106, EXECUTED #117** (*"BOTH READ IN FULL, BOTH POSITIVELY IDENTIFIED UNDER V-9's SECOND LIMB"*). Residue rowed at `Q-RL6-1`/`-2`; `Q-PH-1..5` rowless BY his ruling. |
| A-10 | Tex. Fam. Code row (L59, struck) | — **ruled #9** in his words (*"I am not going to be practicing family law… Delete it."*). The row has been struck-through-but-⬜ since July. |
| A-11 | Placeholder-discipline carry (L214) | The carry's own condition (*"it rides whichever future session next amends `form-engine.md`"*) — **executed #63** (*"REQ-10 FOLD EXECUTED … placeholder discipline enters form-engine.md as new §14"*). Closure names BUILD-STATE's stale "CONFIRMED AS CARRY" line for the next rewrite. |
| A-12 | `WS-P1` (L388) | — its own closing condition (*"STAYS OPEN until a design session verifies it from the live config"*) **met at #123**: the sync filters, read via the API after his click, carry `/docs/reference/`: "exclude". The row's 2026-08-20 annotation — *"THE ROW IS NOT CLOSED AND THAT IS DELIBERATE"*, protecting the record that the click might never have been made — is SPENT by #123's later live-config verification (log order checked: #123 postdates it). |
| A-13 | `O-8` (L409) | — row's own text: *"~~OPEN~~ CLOSED 2026-08-19: Michael upgraded the database the same night."* Glyph lag only. |
| A-14 | `O-9` (L410) | — the row reads MOOT in its own words (*"the split never had to be decided"*; the 65th runner line's phrasing: *"answered by never having to be decided"*); glyph lag only. |
| A-15 | `TC-OPEN-5` (L467) | The `/docs/record/` picker exclusion — **done and measured** (#133 correction 2 records the mis-ordered click and the 72.6% landing; the rule's own origin exhibit). |
| A-16 | `TC-OPEN-6` (L472) | The fifteen captures' relocation — **done 2026-08-21**; closure records #133 correction 4 (destination = his machine, not ARCHIVE). *(Wave-1 confidence MED, not HIGH: the removal step is UNVERIFIABLE from the staged files and rests on #133's corrections plus the meter arithmetic — stated in the closure rather than smoothed.)* |
| A-17 | `TC-OPEN-7` (L473) | The v26 paste — **done and superseded** (v27, then v28 in force per `#140`). |
| A-18 | `RE-LOOK-3` (L882) | Acquire the conduct-rules PDFs — **done by his hand 2026-08-18** (#106: six documents acquired mid-session; `FC-14` ruled the channel that makes them citable; `#112` read the TDRPC over the bridge). |
| A-19 | 2026-08-16 privilege-tier migration row (L1249) | — **run clean, THIRD, 2026-08-19 (#113)**, his hand; closure carries the two honest limits (C2 trivially satisfied on empty tables; check 4 not run). |
| A-20 | v23 INSTRUCTIONS row (L1248) | **The hardening's headline PREMISE-INTACT — now DISSOLVED BY EVENTS.** The row asked for one act: the instructions field to carry `PF-1` and FC-14's SOURCING fourth channel. **`#139`: trigger #6 fired on the v23 loss, Michael's "Yes" to v28, assembled from the same verbatim fragments; `#140`: *"the v28 instructions IN FORCE (read live in the project-instructions field …"*; BUILD-STATE at HEAD: "~~PASTE v28~~ — REPORTED DONE" and "THE HAND ITEM IS THEREFORE OFF THE LIST."** Closure records the design-side attributed read (a Code session cannot see the field) and names BUILD-STATE's one stale contrary line ("rides the v23 paste") for the next rewrite. |

**Also in Block A on their own evidence, from the SUPERSEDED set:** *(each verified premise-dissolved by
the hardening AND re-verified here with nothing since disturbing it)* — none beyond the rows above; the
remaining superseded candidates sit in Blocks B–E below because something in them needs his eye.

### BLOCK B — CLOSES THAT NEED HIS TIEBREAK, NAMED PER ROW (MED confidence)

*The question is answered in his ruling, but a considered Claude/runner posture (never his word) holds
the glyph, or one clause needs his nod. Each row names the tiebreak. "Adopt Block B" takes the sheet's
reading; rejecting any row leaves it ANNOTATE-KEEP with the same evidence.*

| # | Row | Ruled where | The tiebreak he is deciding |
|---|---|---|---|
| B-1 | `FE-4` (L185) | **#53** (*"definitions are case-level versioned objects — immutable versions … prospective propagation"*; §13.1) | The record's ruled-but-unbuilt keep-open posture (the annex's shorthand; the glyph half is BUILD-STATE's *"All three rows stay ⬜"*) is **runner 36's and #81's (Claude), never his** — wave 1 re-attributed both sentences (annex A1 §S-2; the hardening had swapped the cites). Residue all rowed (`Q-FE4-1..6`; build-home at `Q-FE4-2`). Does the register's #114 scope line ("the complete inventory of open RULINGS") govern, closing the row? |
| B-2 | `FE-6` (L187) | **#53** (§13.3: standalone instruments REPEAT the definitions block; separate-mode layout derived; one COS per document) | Same tiebreak as B-1; seven `Q-FE6-1..7` rows hold every residue; no item table exists (BS: "NO ITEM TABLE") — a build fact, not a ruling. |
| B-3 | `CD-1` (L228) | **#51** (the fork: *"`parties` IS the contact directory"*), built #61, **COMPLETE #113** | The sentence *"This entry stays open as the living-spec pointer"* exists ONLY in the register row — **not in #51** (wave 1: 0 hits both logs; the hardening's "part of the same #51 ruling" is literally false at HEAD, its substance a design annotation). Living-spec revisits demonstrably take their own IDs (`CD-3`…`CD-13`, `CD-14`). Keep a pointer row open, or close with the pointer homed in the spec header? |
| B-4 | `P-COM-2/-3/-4` (L840–842) | **#94** (entries created), **VERIFIED by him #98** (2026-08-17) | The rows carry NO question — they are stale verification-status lines (the propositions live in the registry as `WP-1/2/3`-shaped entries, VERIFIED). The row-level cite the audit gave was wrong (zero `P-COM-2` hits in either log at the hardening's HEAD; at `7f02131` exactly ONE — `#138` quoting the hardening's own zero-finding, its counter-example); the SUBSTANCE (verified entries exist) is right. Close as stale-status records naming the registry entries? *(Registry files unstaged — the WP↔P-COM wording identity is UNVERIFIABLE-HERE and the closure sentence says so.)* |
| B-5 | `TOC-5` (L809) | never ruled by name — **superseded by events** (TC-3/TC-4 at #132: the index split and moved to the stable `docs/record/session-log-toc.md` path) | Supersession-by-events, not a ruling on TOC-5 (the hardening's point, confirmed). **CO-REQUISITE, staged with the closure:** the toc's own footer says twice at HEAD that TOC-5 *"remains Michael's and unruled"*, and the runner REGENERATES that file every batch — so the closure rides ONLY with a same-batch footer reconcile in the executing packet's routing, or the file re-asserts "unruled" against a ✅ row forever. Closure rides only if he says the events answered it. |
| B-6 | `Q-PR3-3` (L888) | spent by events **his own act made** (#113: CD-1 ran first; backfill step (c) matched ZERO rows; *"the failure did not occur"*) | The reservation *"Whether anything remains in it is Michael's; the row is annotated, not closed"* is the **#113 session's sentence, not his words** (wave 1 checked) — so this is a ONE-WORD CONFIRM, not a supersession he must be protected from. Residue = `Q-PR3-2` (its own row). |
| B-7 | `Q-STAT-6` (L1004) | **all eighteen dispositioned** (BS: *"Q-STAT-6's EIGHTEEN ARE NOW NONE LIVE, AND THE ROW STILL STAYS OPEN"*) | The "STAYS OPEN" is a runner posture; the general residue (verbatim-drafting direction) is `READ-A`'s and the `#108` direction's territory. One-word confirm. |
| B-8 | `Q-STAT-3` (L1001) | premise dissolved (#107 retired the capture; the correction lives in the spec at HEAD) | The capture is RELOCATED, not deleted — bridge-reachable on his machine (v27/v28 rule 4) — so the stated hazard is narrowed, not zero. Closure sentence says exactly that. |

### BLOCK C — CLOSE-SPLIT: the answered limb closes; the surviving limb is re-minted narrowly. **The re-mint question texts are IN the cells below, verbatim (QR-1) — five texts (C-6 re-mints nothing)**

| # | Row | Closes on | The surviving limb (re-minted with full text, QR-1) |
|---|---|---|---|
| C-1 | `FE-5` (L186) | **#53** (subpart detection: the ruled middle path — *"escalates to a warning ONLY when worst-case crosses the cap **while the numbered count does not**"* — the clause the 08-24 audit truncated, as the hardening caught) | The `#81` scope findings with NO other row. **Re-minted row text (QR-1): "Does §13.2's budget key on the (propounding party, responding party) PAIR and accumulate across waves — every Rule 190 cap is worded 'Any party may serve on any other party no more than N' — and how does the budget source a Level 3 cap that is an ORDER value rather than a level constant (190.4(b)(2) permits phase scoping), where `cap = f(level)` is structurally wrong? PROPOSED at #81; never put. (Split from `FE-5`, closed <date> on #53.)"** (`Q-FE5-1`, `Q-FE5-9` already have rows and stay.) |
| C-2 | `CD-2` (L229) | **#51** (both layers: roster-as-data; typed directional edges), built #61, migration #113 | **Re-minted row text (QR-1): "Do the CR-3 code sheet's carrier-ID and financial-responsibility TYPE codes enter `contact-directory.md` controlled vocabulary — §5 is the record's only sectioned vocabulary fact; the spec is unstaged, so the receiving section is confirmed at execution (a spec-level act on the living spec, the CD-3 precedent)? PROPOSED #64, never put; pointer `docs/specs/cr3-field-code-map.md`. BANKED beneath this row, preserved from CD-2 limb (b): verification-officer tracking — someday, the K-6/K-7 cheap insurance (per-response-set 'who verified which response set' capture). (Split from `CD-2`, closed <date> on #51/#61/#113.)"** — the bank rides the re-minted row's FACE, not the ✅ closure: a bank buried in a closed row loses its open-status visibility (the verification pass's finding, adopted). |
| C-3 | `V-7` (L684, 🟡) | **#73** ruled the narrowing (*"V-7 — NARROW ENTRY 23 to what CPRC § 37.001 et seq. actually provides"*); the cite-stays ruling — *"Rejected: `V7-23-CITE` — entry 23's cite stays `§ 37.001 et seq.`"* — is Michael's at the 51st invocation's Step-1 STOP (2026-08-17, one of his eleven rulings there; the 08-24 audit mis-cited it to #73, an inherited defect this sheet corrects rather than repeats); executed; entry 23 VERIFIED | The only open home of: *"entry 24's proposition has never been located inside the Devine opinion"* — re-minted as a narrow ACT row. **Re-minted row text (QR-1): "Entry 24 (Devine): the registry proposition has never been located inside the opinion. ACT — a session's proposition-location read of the opinion against entry 24's text, staging the passage or reporting cannot-locate; verification is Michael's. (Split from `V-7`, closed <date>.)"** |
| C-4 | `OPEN-5` (L990) | limb (a) — **ruled #82 (A-2, anchor form) and #94 (*"Yes — ride the v9 amendment"*)**; the five stale pointers were removed by the 37th invocation | **TWO limbs survive, and the re-mint carries BOTH — the pair the record keeps together.** #94's full sentence, no ellipsis: *"OPEN-5(b) and the AUD-5..9 one-word apply were deliberately NOT put and REMAIN OPEN."* Carried open again at the 51st invocation and in BUILD-STATE's your-hand list at HEAD; the row is the register's only home of the `AUD-5..9` apply. **Re-minted row text (QR-1): "OPEN-5(b) — the start-of-session instructions-version comparison (§4-P2): adopt, adapt, or decline? Absent from v28. AND the `AUD-5..9` one-word apply — recounts staged since #82, deliberately not put (#94), still awaiting the word. (Split from `OPEN-5`, whose limb (a) closed <date> on #82/#94.)"** *(The earlier draft of this sheet closed the AUD-5..9 limb on a compressed quote whose ellipsis sat exactly on it — caught by the adversarial verification pass, HIGH; corrected here and recorded rather than smoothed.)* |
| C-5 | `Q-COM-10-E` (L767) | the fourth-enum-value question is spent by **`Q-COM-10-A` ruled #105** (Option 2: THREE values plus a `witness_statement` boolean — there is no fourth value) | The boolean's TOKEN (`witness_statement` is Claude's option text at #105) — confirm or substitute the name before the Option-2 redraft. **Re-minted row text (QR-1): "The Option-2 boolean's TOKEN: `witness_statement` was Claude's option text at #105, adopted as a shape, its name never separately confirmed — confirm or substitute the column name before the unified-vocabulary redraft executes. (Split from `Q-COM-10-E`, closed <date> on Q-COM-10-A.)"** *(#105's runner expressly declined to close the row — its words: "`Q-COM-10-B`–`F` were annotated add-only as unblocked; none closed"; the hardening's epigram for the gap is "governs ≠ answers." The re-mint honors it: the unanswered token half survives as its own narrow row.)* |
| C-6 | `WESTLAW-5` (L613) | the ACT limb — the five Westlaw pulls **done and over-delivered (#99)** | **Nothing re-mints — stated so the packet's re-mint list expects FIVE texts from this block, not six.** The residual cite supply for entries 30/31 already lives at `Q-RL6-1`/`-2` (cross-referenced in the closure). The row's two unique V-9 provenance facts (entry 17's *"MEMORANDUM OPINION, Nov. 30, 2017, Lang, J."*; entry 16's *"pet. denied"* that no public source could reach) are retained verbatim by the append-only closure — the merge the audit proposed is REJECTED (Block H). |

### BLOCK D — HIS OWN WORDS HOLD IT: only Michael closes these, and only in words

| # | Row | His words, quoted |
|---|---|---|
| D-1 | `WS-P4` (L391) | **"Defer."** (register annotation #108; log #108 *"WS-P4 deferred"*). And its substance is NOT spent: the row wants a version-note line about the 2026-08-18 trigger-4 firing, and **v28's trigger-4 history still omits 2026-08-18** (read from the v28 text in this session, labelled a non-staged observation). Put narrowly: adopt the one-line history note into the next instructions edition, or close the want? |
| D-2 | `READ-A` (L605) | **deferred #95** — and the deferral is CURRENT BINDING LAW: v28's ROUTE-C bullet carries *"Reading A's SCOPE remains expressly DEFERRED … silence is not a ruling on the rest."* The audit's superseding ground (*"the `#108` verbatim-drafting direction now governs every entry"*) is FALSE at HEAD three ways (BS: *"a ruled DIRECTION, not a binding convention … applying it to any EXISTING entry is a per-entry ROUTE-C act and also yours"*). Stake re-stated at today's numbers: backlog 47, 12 unverified. Put narrowly, or leave standing. |
| D-3 | `PR-3` umbrella (L71) | **"execution HELD"** (#34) on the ladder pass **he deferred pending Domser**. Eleven children exist but `D-CL1-3` (L172) and `O-2` (L400) gate on PR-3 BY NAME and the unblock condition exists nowhere else — closing the umbrella orphans them (hardening's PARTIAL + wave-1 confirmed: destroys-unique-text = Y). Stays, annotated with the #113 events (backfill limb SPENT; row ANNOTATED NOT CLOSED). A CLOSE-SPLIT (re-minted execution-hold row) is available but is HIS restructuring to choose, not this sheet's. |

### BLOCK E — ANNOTATE-KEEP: stays ⬜; the annotation stops the row asserting a stale world

*Each annotation's substance is stated in its own entry below (the executing packet assembles the dated
sentence from it — §4's rule); none decides anything. "Adopt Block E" authorizes the annotations as docs
acts in the executing packet.*

- **E-1 `FE-8` (L189)** — retention half BUILT 2026-08-20 (answer snapshot on `generated_documents`; `MIG-1` unrun); diff half DEFERRED #63 and **confirmed still deferred #135** (*"R13 RETIRED … RECON-1 is not its consumer after all"*); the §5.6 sub-question (content-addressed retention; retention period) NEVER PUT (0 hits, both logs); CD-1 gate spent.
- **E-2 `FE-10` (L193)** — the #63 cite ruled SCOPE ("from birth (incl. numbering pass)"), not the row's two sub-questions (profile-conflict propagation; standalone lint tool), both unruled with no other home; render lint BUILT; numbering pass's build state UNSTATED anywhere at HEAD (a Code-side verification item).
- **E-3 `FE-12` (L197)** — ruled IN from birth (#63; render-time enforcement #58) but the build entry never names FE-12 or provenance, and BUILD-STATE names FE-12 nowhere (its seven "provenance" occurrences, on five lines, are five other subjects: the slice's ruling-provenance classes, gate 10's provenance-only ruling, the T3 R/K/I marks, HK-4's pilot-recordings provenance README, and #80's TIER A/B provenance marks) — the attribute's build state is UNSTATED at HEAD; the audit's FE-12 row claimed *"provenance flag built with render-time enforcement"* (its "built and exercised" phrase belonged to its FE-10 row) and the hardening found the claim NOT-FOUND — confirmed; the "known-bad, kept for reference" third-value limb NEVER PUT (0 hits) and unique to this row; the two-bank attorney instruction of record continues to bind regardless.
- **E-4 `Q-DES-5` (L773)** — no ruling reaches it: the #100 sentence the audit quoted rules `V-8` (already ✅; the quoted strings return ZERO — hardening confirmed); narrowed by V-8's execution; asked after #100 and never answered. Annotation records the narrowing.
- **E-5 BUILD-SESSION-NOTES classification row (L134)** — first limb (unreviewed entries) DISCHARGED at #13; the build made classification EDITABLE with review-log + re-evaluate notice, but **no Michael ruling exists anywhere** (hardening: EVIDENCE-IS-A-CLAUDE-ASSERTION; wave-1 grep confirms zero) and it predates the nothing-without-his-ruling convention. Annotation states exactly that; one word from him converts it to a close.
- **E-6 `TOC-3` (L807)** — TC-3 ruled the SPLIT, TC-4 the DIRECTORY; **neither ruled stable-vs-dated naming**; twin `GLR-2` — the record says twice *"one ruling closes both."* Annotation cross-references; the pair is PUT as ONE one-word question (§5 Q-3).
- **E-7 `CR-1` (L121)** — the row prescribes, on a positive finding, **a fold** ("records as a second observation on the covering section"), and the fold has not run; the intake-spec §2 confirmation is UNVERIFIABLE-HERE (spec unstaged). RECLASSIFY-ACT flavor: a design session executes the row's own dedupe-and-fold, then it closes. Annotation says so.
- **E-8 The twelve hands-on rows** (`CL2-AC-1` L170, `CL2-CHECK-1` L171, `FE-§11.4` L201, bill-label L1283, `CR-7` L127, `CR-CONSTRAINT` L132, `Q-FE6-5` L964, `Q-FE4-1` L945, `Q-FE5-3` L953, `Q-IN2-7` L918, `Q-IN1-1` L924, `Q-IN3-6` L938) — **ACCEPTED onto the CC-1 hands-on queue at #139 in his words** (*"Accept all of them onto the queue"*). Annotation per row: "On the CC-1 hands-on queue (accepted #139; full text also at REQ-CAPTURE §18.F). Held for the sitting — CC-1(b)." Ripeness carried: four ripe now (`CL2-AC-1`, `CL2-CHECK-1`, `FE-§11.4`, bill-label), the rest not (their gates named in the annex). **A register-header defect rides here (§3 item 5): the batch-84 header sentence says the twenty-one accepted items are "none of them a row in this register" — false for these twelve.**
- **E-9 `Q-WF-4` (L900)** — LIVE and unruled (confirmed; named nowhere in the 08-24 audit); annotation records **#140's AS-Q1** (*"Yes — server-side function + secret; fixture writer only now; note CLAUDE.md"*) as the question's FIRST INSTANCE — a shape precedent for one synchronous consumer that answers neither the identity limb nor the three async consumers (WF-2–8, `Q-QBO-3`, `Q-COM-4`).
- **E-10 `V5-IDS` (L606) + `Q-T19-1` (L609)** — one policy question, two instances, different option sets; **both deferred via `D-6` at #98** (the deferral names `Q-T19-1`; `V5-IDS` by extension — annex A6 corrects the hardening's overstatement). Cross-annotated; put as ONE question when he takes it; `V5-IDS`'s unique file-shape finding (*"the two registry files are now shaped two different ways"*) survives any answer and is preserved.
- **E-11 `Q-RL6-5` (L761) + `Q-DES-6` (L774)** — reciprocal self-identification verified; the per-entry alternative and the non-CCA/TRAP-47 branch (live via *Whaley*) are unique residue; cross-annotated, ruled together.
- **E-12 `Q-COM-10` umbrella (L832)** — parent of `-A`…`-F`; annotation records `-A` RULED #105 (the umbrella does not say so) and the umbrella's unique second interrogative (*"does the answer come before a third consumer picks one by implementation?"*); closes at list adoption per #100's own terms.
- **E-13 `Q-FE4-2` (L946) · `Q-FE5-2` (L952) · `Q-FE6-1` (L960)** — the disposition trio: one shared limb of three, NOT verbatim duplicates (each carries unique rationale; `Q-FE6-1` adds the "slice 2" naming limb; `FE-7` has no disposition row at all — a gap named, not minted). Cross-annotated to rule together; the amendment slice's IN/OUT (UNVERIFIABLE-HERE — not staged) is read before ruling.
- **E-14 `[Task 7 memo Q4]` (L975) + `H43` (L978)** — both expressly deny being one answered question; the soft-gate/two-branch-certificate limb is unique; cross-annotated, ruled together. **"FC-8 ruled it" is REFUTED: `H43` occurs nowhere in #106; closing the register's H43 on FC-8 would destroy an open item** (H43-α OPEN; H43-β ruled — the collision report cleared it wrongly, as the hardening found).
- **E-15 Cross-cutting email constraints row (L282)** — ruled #63 *as recorded constraints, not buildable items*. **CONDITIONAL — the sheet adopts wave 1's verdict here, correcting its own earlier draft's silent escalation to a plain close.** Not a question at all; but a ✅ reads as SPENT while the four constraints bind UNBUILT T3 work, `#63`'s entry text carries only ONE of the four (*"client emails get human routing, never templated automation"*), and the other three exist nowhere else in the STAGED record (the adopted doc is UNVERIFIABLE-HERE). Two-step disposition: (1) a session's ACT — verify all four constraints stand in the adopted email-workflow doc at HEAD; (2) only then close, the closure sentence carrying all four VERBATIM and naming the doc. Until (1) runs: ANNOTATE-KEEP. The glyph-less record-line conversion (the `#68` precedent) remains his alternative.

### BLOCK F — KEEP: the audit's candidate was WRONG, and the row stands as the only home of its question

- **F-1 `RE-1` (L75)** — the audit's quote was from the WRONG RULING (byte-verified: limb (3) of the family-law deletion); the actual #9 ruling reads *"Ruled yes-eventually; everything else OPEN"*, `#88` adds *"the row stays ⬜ … RE-1 has NO design-doc home"*, and **"overflow" occurs nowhere else in the register.** LIVE. (Annotation only: the #106 `Q-RE-1` ruling and FC-14 closure of `Q-RE-9` are recorded on their own rows.)
- **F-2 `FC13-Q-5` (L432)** — not a duplicate of `Q-RE-8` (neither names the other; not in the `Q-STAT-5` stack; carries the option-(c) hold, the entry-specific anchor and the candidate filename uniquely). LIVE in the `Q-STAT-5` dependency.
- **F-3 `Q-RE-8` (L878)** — not a duplicate of the `Q-STAT-5` stack (shared BLOCKER, not shared question); its boundary interrogative is register-unique. Annotated with #108's TDRPC 1.04(f) IN-ruling; stays LIVE as the stack's widest framing.
- **F-4 `Q-QBO-7` (L862) (+ its twin `[Task 7 memo Q6]` L977)** — the audit's §5 named a *"`Q-QBO-7`-adjacent EFSP bar"* among its two standing bars — a bar ADJACENT to the row, not the row; the row itself is a yes/no RULING question, unruled. LIVE.

### BLOCK G — RECLASSIFY-ACT: awaiting an ACT, not a ruling (the #114 line: this register is "the complete inventory of open RULINGS")

*Wave 1 verified all 38 of the audit's action rows and confirmed: an act awaits in every case, whose actor
is named, and **not one act has been recorded as happened since** (nearest partials: `RE-LOOK-1` half run —
1.04 read #106/#112, 7.03 outstanding; `V-2` items 2–4 researched pending his word, item 6 discharged;
`Q-3` evidenced twice, never clicked-and-closed by him). Two corrections to the audit: `KICK-1`'s
"12,041 B" was an extraction artifact (the row is 1,887 B; the audit's figure swallowed eight following ✅
rows); `Q-QBO-7` is not an action row (Block F). The structural question this block PUTS (one ruling):*

> **G-Q1 — Does the register grow a marked "AWAITING AN ACT — not a ruling" subsection (or per-row ACT
> marker), so the open-RULING count stops carrying these — 38 audit-classed acts on 37 rows (`M-3`/`M-4`
> share L522; the audit's `Q-QBO-7` reclassified to Block F), plus the R11 verification pair?**
> The acts: **his hand (19 items, 18 rows)** — `HK-4`, `HK-6` 🟡, `WS-P3`, `TC-OPEN-3`,
> `TC-OPEN-4`/`RF-2`, `KICK-1`, GPU-telemetry 🟡, `Q-3 RE-CHECK`, `M-3`, `M-4`, `_claude_extract\`
> deletion, P15 runner-copy deletion, Domser setup, `QBO-LOOK-2`, `COM-LOOK-4`, `RE-LOOK-2`,
> `PR3-LOOK-1`, `OPEN-3`, Tascam (gates nothing — the audit is right that it belongs off the register;
> his call); **a session's research act (18)** — Sheppard history, Greystar/Brenham/Pinnergy dockets,
> Kostura-vs-2024-CMP, Ortiz citator, `V-2` (his confirming word + item-6 annotation), `COM-LOOK-1/-2/-3`,
> `QBO-LOOK-1/-3/-4`, `WF-LOOK-1/-2`, `RE-LOOK-1` (7.03), *Arthur Andersen* year check (`V7` label collides
> with V-7 — flagged), TDRPC 1.04(a)–(b) read, Estates Code pulls (gated on the deferred ladder pass), `HK-7` — the recurring per-session bridge grant, its 20,102 B row the register’s largest (condensable only with the ruled gap-note preserved verbatim);
> **standing bar (1)** — `Q-6` (also in instructions v28); **and the R11 pair** — TRCP 195.2(a)/(b)
> (L86) and `[DL-memo Q4]` (L1013), **his VERIFICATION acts, connected by #139's HD-10 ruling** — his
> words: **"Close it by pointing at them"** — the entry recording that the two register rows ARE the
> verification act; annotated as such either way.
> Marking is a format act; MOVING any row off the register is his per-row call.

### BLOCK H — DUPLICATES: NONE MERGED. The hardened headline is CONFIRMED at full adversarial depth.

All ten audit "duplicates" re-verified in both directions: **"not one is a true duplicate"** (the
hardening's words, confirmed at full adversarial depth) — **every proposed fold deletes at least one
register-unique interrogative** (counts with controls in annex A6). Disposition: the two
DIFFERENT-QUESTIONS pairs (`WESTLAW-5`→`Q-RL6-*`; `Q-RE-8`→`Q-STAT-5`) are REJECTED as merges outright
(C-6 and F-3 carry their rows); seven OVERLAPPING pairs take cross-reference annotations and
rule-together notes (E-10…E-14), never folds; the eighth — `FC13-Q-5`→`Q-RE-8` — resolves as two KEEPs
(F-2/F-3). **The audit's merge instruction for `[Task 7 memo Q4]`→`H43` would have destroyed an open item
on a false "FC-8 ruled it" premise — the collision report's own H43 clearance is WRONG (§3 item 8).**

---

## §3 — REGISTER DEFECTS FOUND OR CONFIRMED BY THIS PASS (reported; each fix is PUT, none made)

1. **`O-12`'s question text is TRUNCATED mid-sentence** (QR-1 breach; the deleted packet was the only
   other home). CONFIRMED. Fix is a restoration from `docs/spec-feedback.md` / the Grok review —
   **UNVERIFIABLE-HERE (unstaged); the restoring packet must quote its source at HEAD.** PUT as §5 Q-2a.
2. **The `🟡` glyph has no legend** — five rows carry it (`HK-6` L377, GPU-telemetry L523, `V-5` L650,
   `V-6` L667, `V-7` L684); the Convention line documents two glyphs. **Staged fix (one sentence, PUT as
   §5 Q-2b):** amend the Convention line to read: *"Convention: `✅` = closed · `⬜` = open · `🟡` =
   ruled-but-execution-pending or awaiting Michael's confirming word."* (The Convention line is the
   unrouted act `QR-6(e)` has reserved to him for four batches.)
3. **The Outlook edit/cancel defect has NO ROW** while BUILD-STATE carries it as live-with-no-fix.
   Minting is his (PUT as §5 Q-2c; the row's full text is assembled by the executing packet from
   BUILD-STATE's own words and `outlook-edit-cancel-exercise-2026-08-13.md`).
4. **The 24 no-row items** (`FO-1..3/6/7`, `BR-1..5` — `BR-2/3/4` existing *only inside range/slash
   notation* (the collision report's words), with no item text anywhere — `DA-1..4`, `RC-1..3` [since
   CLOSED at #139], `RF-2/3/5/7`, `INS-1`, `MIG-1`, `R11`): minting any is HIS act (the register header
   says so itself). The hands-on sitting covers `DA-1/3/4`; `FO`/`BR` wait on the design-doc-home ruling
   (`#137`'s residue). PUT as §5 Q-2d (mint now / mint at their sittings / leave header-carried).
5. **The batch-84 header sentence** contradicts the register's own rows (the "none of them a row" clause —
   twelve ARE rows). A header-sentence repair rides the next runner batch's reconcile sentence naturally;
   noted for the packet's manifest.
6. **Line-cite decay** (4 rows, 5 cites into moved/rewritten files) CONFIRMED; **the "nine oversize rows"
   figure is CORRECTED, not carried**: the audit's own nine figures sum to 69,933 B (≈68 KB, not its
   stated ~62 KB), and 59,779 B once `KICK-1`'s extraction artifact is removed (the audit's 12,041 B
   swallowed eight following ✅ rows; the row itself is 1,887 B — re-derived to the byte at verification).
   Both belong to `TC-OPEN-1`'s deferred sitting; no action staged (TC-1 deferral honored); re-derive
   before acting.
7. **BUILD-STATE stale lines found by this pass** (for the next rewrite's recompute, not edits here): the
   hand-list still carries "re-upload SKILL.md v2 (SK-v2)" three refreshes after closure, and still
   carries `Q-STAT-2 – Q-STAT-6` whole; "rides the v23 paste" contradicts its own v28 line; the five 🟡 rows described as *"ruled-but-execution-pending or awaiting your confirmation"* where, for V-5/V-6/V-7, execution is DONE and only the second disjunct can be doing any work; `Q-WF-4` called "ON NO ROW AT
   ALL" beside a cite to its row; "Rule 195.2's 30-day floor is GONE" stated flat where it is a PROPOSED
   memo finding awaiting his verification. **REGISTER stale lines, same class, different file:** `Q-RL6-1`
   (L757) still says *Pharr* "WAS NOT READ" against #117's executed read; the `V-5` row still states
   "16 REMAIN UNVERIFIED" where BUILD-STATE's recomputed figure is 12.
8. **The collision report's `H43` clearance is WRONG** — it cleared `H43` as "same subject, not a
   collision" and omitted it from its H-series list, against the live log's earlier recorded *"one ID,
   two propositions, already in the record"* (the hardening's finding, re-confirmed here: `H43` occurs
   nowhere in #106, so "FC-8 ruled it" is false for the register row). A defect in an UNTRACKED file
   Michael ruled DO NOTHING on — recorded here, nothing edited.

## §4 — HOW THE STAGED TEXTS WORK
*The five CLOSE-SPLIT re-mint question texts are staged VERBATIM in Block C's own cells (QR-1). Closure
sentences and annotations are NOT pre-drafted here: their substance is fully specified per row in §2, and
the executing packet assembles each sentence from its row's cell AFTER Michael rules — duplicating them
now would be copy-forward surface for rows he may decline. Every §2 cell is written to be sufficient for
that assembly: the cite, the quoted basis, and what the sentence must record.*

## §5 — THE QUESTIONS THIS SHEET PUTS BEYOND THE BLOCKS (one at a time, CC-1(a))

- **Q-1 (= G-Q1)** — the AWAITING-AN-ACT marking, above.
- **Q-2 (a–d)** — the four register-defect fixes at §3 items 1–4, each its own word. *(Labelled Q-2a–Q-2d
  and NOT D-4…D-7 as an earlier draft had them: `D-4`, `D-5` and `D-6` are LIVE IDs from the 2026-08-17
  registry walk — the instructions' own "AN ID IS NOT AN IDENTITY UNTIL THE ROW IS READ" class (v28, from `#133`), caught at
  verification. These labels are sheet-local; minting real IDs is his.)*
- **Q-3** — `TOC-3` + `GLR-2` as ONE naming-convention question (stable vs dated pointer-doc paths), per
  the record's own "one ruling closes both."
- **Q-4** — the `Q-STAT-5` stack (five rows, one unmade boundary decision: does the registry take a
  practice-law file?): the audit's finding stands and is NOT collapsed (hardening + wave 1 both confirm
  the rows differ); put as ONE upstream question whose answer unblocks five rows without merging them.
- **Q-5** — the ruling-sitting SHAPE: this sheet is sized for one typed sitting (Blocks A/B/C/D in an
  hour; E/G are nods). Fable is the ruled model class for adjudication sittings; the weekly bar reset at
  3:59 PM Central today.

## §6 — WAVE-2 (the ~247 LIVE rows) — FOLDS IN HERE
*Discharged 2026-09-01: the sweep returned 247/247 (CLOSE = 0; 68 annotate-keeps in five groups; one close-split;
two reclassify-acts; two his-word rows; 174 keeps). The adjudication is the WAVE-2 SUPPLEMENT beside this file;
the evidence is at docs/record/queue-cleanup-evidence-2026-09-01/wave2_opus_sweep_evidence.md.*

## §7 — WHAT THIS SHEET DOES NOT ESTABLISH
No closure (Michael rules); no build authorization; no legal-currency verification (registry Status lines
untouched; the registry files were NOT staged — every claim about them above is marked and carried as
UNVERIFIABLE-HERE); no live-database fact (`MIG-1` carried unverified); no reading of the instructions
FIELD (the v28-in-force statements are the design side's attributed reads at `#140` and this session);
no edit to any file. **Models named per the correction-entry discipline:** this adjudication — Fable 5
per the session environment; the wave-1 fleet — dispatched as Fable subagents by this session (the
session's own dispatch record; the annex reports do not self-state a model); the sheet's verification
fleet — Opus 5 by explicit dispatch after the Fable wall; the 2026-08-24 audit — Fable 5 fleet with Opus 5
consolidation, and the hardening — Opus 5 (each per its own header); wave 2 — Opus 5 (pending).
PF-1 did not fire on this sheet's packet: it carries no legal
characterization and no proposed registry entry — quoted rule text appears only inside already-recorded
register rows and rulings (the QR-6(f)-shaped skip, recorded here rather than silent).
