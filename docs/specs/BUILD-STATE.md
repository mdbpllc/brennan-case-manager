# BUILD STATE — brennan-case-manager
Commit: 3020dfd (session start)  |  Branch: master  |  Generated: 2026-08-20 Central (one hundred seventeenth refresh)  |  **WRITTEN BY THE QUEUE-RUNNER'S SEVENTY-EIGHTH INVOCATION — one docs-only packet carrying ONE routing row, the thinnest the runner has taken. §5 is NONE: NOTHING WAS BUILT and no `src/`, `db/` or `supabase/` path was WRITTEN — several were READ, which is how the counts below are re-derived rather than carried. **THE HEALTH TRIO WAS SKIPPED ON `QR-6(f)`'s RULE AND EVERY HEALTH FIGURE BELOW IS THE GATE-3 SESSION'S, CARRIED AND LABELLED AS CARRIED — a skipped check is not a pass, and this batch changed no code it could have tested.** Every other count RE-DERIVED at HEAD (OPEN-5(a)); sections this batch does not bear on are carried VERBATIM rather than retyped, which is what protects them. **THE FIGURE-CLASS THAT MOVED IS AGAIN THE RECORD'S OWN — the log and its index — AND THIS TIME THE QUEUE DID NOT MOVE WITH THEM, BY DESIGN AND ON TWO INDEPENDENT AUTHORITIES.**
*`3020dfd` is the commit this snapshot was written FROM. **It DESCRIBES the tree as this batch commits it**, so this batch's own commit — the SHA you are told — is the one at which every claim below verifies; `3020dfd` is not.* **AND THE LIVE-DATABASE CLASS DID NOT MOVE THIS BATCH: NOTHING WAS WRITTEN TO, READ FROM, OR CONNECTED TO THE DATABASE.** The gate-3 measurement of 2026-08-20 stands as the newest live reading — all 37 tables probed by INSERT twice each, filed at `docs/specs/gate3-write-path-test-run-2026-08-20.md` (**16,698 B / 208 non-blank, re-derived**), reviewed design-side full-text at `#124` and expressly NOT re-measured, the authorization being single-run and spent. **THE OTHER LIVE FACTS BELOW ARE STILL Michael's OWN REPORTS, NOT re-confirmed here:** gate 10's six checks (2026-08-19), the §5 pre-flip report (2026-08-20), the BACKUP and PRO facts read off the SUPABASE DASHBOARD, gate 9's round trip and its 2026-08-20 header read, **and the 2026-08-20 SIGNUPS-TOGGLE reading (`#125`) — a dashboard reading of LIVE CONFIGURATION, and the day's security finding.** **NO KEY, TOKEN OR SECRET VALUE IS READ, QUOTED OR RECORDED ANYWHERE, and `.env` was not opened by this batch at all — only its gitignore line re-derived, `.gitignore:24`.**
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*
***DO NOT CITE THIS FILE BY LINE NUMBER IN A PERMANENT ENTRY.** RULED PROJECT-WIDE 2026-08-16 (#94, cite-stability, riding v20): line cites are permitted only into **append-only** files; **prepending** files (the session log) and **wholesale-rewritten** files (this one) are cited by heading or quoted sentence.*
*EVERY COUNT BELOW WAS RE-DERIVED THIS REFRESH, NOT CARRIED — runner v11 Step 4 item 3 (OPEN-5(a), ruled #94).*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS COMPLETE — ITEM 7 RAN AND
VERIFIED 2026-08-19 (#113) — AND SO DID GATE 10's MIGRATION, PASTED AND VERIFIED BY YOUR HAND THE SAME DAY, ALL SIX CHECKS ANSWERED. NO MIGRATION IS PENDING.**

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **AUDITED 2026-08-15 (#79): `record-integrity-audit-2026-08-15.md` — 65 claims in this file and the queue: 46 SOUND, 6 stale, 1 CONFIRMED-FALSE, 1 contradicted.** Six findings RULED AND CORRECTED (#82, A-1–A-6); **`AUD-2` and `AUD-3` still carried unchanged** — they sit in **OPEN-1**/**OPEN-2** territory, where correcting them without your ruling makes the auditor an editor
- **WITHDRAWN, NEVER ROUTE: `registry-retrieval-completion-2026-08-16.md` (#93).** Written on a false premise and withdrawn the same session. **It never reached the repo.** If a copy arrives by any path, it is an error
- **THE FIVE CHAT-DISPATCH v4 RESEARCH DOCUMENTS WERE ADVERSARIALLY AUDITED AND ARE CORRECTED IN PLACE (#103).** Twenty-two defects — **3 HIGH, 8 MEDIUM, 11 LOW**. **Nothing propagated: no registry entry was ever created from any of them.** Each opens with a `⚠ CORRECTED IN PLACE 2026-08-18` banner quoting its own retracted text — **do not "restore" or merge any retracted passage**. **THE FOUR AUTHORITIES THAT AUDIT SURFACED ARE READ (#104).** **SIX FABLE ADJUDICATIONS FOLLOWED: #105** (`Q-AUDIT-1` → PF-1 standing; `Q-COM-10-A` → `privilege_tier` purpose-scoped, Option 2), **#106** (the FC form-corpus block, THIRTEEN RULED), **#107** (the project-knowledge prune), **and #108 → #109 → #110, ONE EVENING CONVERSATION IN THREE PACKETS, EXECUTED BY THREE CONSECUTIVE RUNNER BATCHES.** Records, every size RE-DERIVED HERE: `fable-adjudication-record-2026-08-18.md` (26,759 B / 329 non-blank), `fc-adjudication-record-2026-08-18.md` (25,182 B / 299), `t26-signoff-and-adjudication-record-2026-08-18.md` (32,720 B / 321), `post-execution-signoff-record-2026-08-18.md` (21,665 B / 209), `five-look-signoff-record-2026-08-18.md` (10,576 B / 123) and `grok-external-review-2026-08-18.md` (**31,512 B / 262 — GREW THIS BATCH by its own §W annotation; the 29,458 B / 238 carried until now was the pre-annotation file and is corrected, not re-carried. It is the only one of the six authored OUTSIDE this project's models**). **THOSE THREE MOVED THE REGISTRY AND NOTHING ELSE HAS: twelve entries VERIFIED between them, seven inserted, five conformed verbatim**

## THE FC BLOCK — ruled 2026-08-18 (#106), ALL DESIGN DIRECTION, NOTHING AUTHORIZED TO BUILD
- **THE ONE CONVENTION, HIS WORDS: *"My system should stay true to one convention as much as possible."*** **`{token}` — single brace, bare name — IS CANONICAL** (FC-1): the stored form, the editor's display form, and the form every new template is written in. **The importer accepts legacy conventions and emits canonical** — `{{TOKEN|filter}}` (the 15 MDB PLLC production letters) and registered bracket tokens — **and legacy forms are NEVER written back**
- **FC-1–FC-4 ARE FOLDED INTO `form-engine.md` §3** (#106), appended as a closing subsection: per-spot formatting lives in **TEMPLATE SETTINGS**, not token text (FC-2); bracket handling on import is an **ALLOWLIST, never a pattern match** (FC-3 — 147 `[signature block on next page]` notes, 80 `[s]` markers and Texas citation parentheticals would otherwise become data fields); `[s]` **converts** to the grammar engine's party-count flex point (FC-4). **§9 was NOT touched** — a verified pure insertion, 25 lines, zero deletions. **This matters to FE-D1: slice 1 hard-codes a parser, and the spec it reads now states the ruled syntax**
- **THE REST OF THE RULED THIRTEEN, ALL DIRECTION AND NONE OF IT A BUILD ITEM.** **FC-5: fee basis STAYS PER CLIENT** — D-CL2-3 stands, contrary stream-rate evidence recorded as considered and not followed · **FC-6: the reduction model runs FORWARD** — client net is an OUTPUT, ruled on MDB PLLC's own payout templates read at source at his direction; **client-net-as-input worksheets are the prior firm's method** · **FC-7: deadlines are TWO-TRACK**, his definitions · **FC-8: a court-keyed deadline PROFILE plus STRUCTURED GEOGRAPHY on `cases` when a schema act is next authorized — NO MIGRATION AUTHORIZED** · **FC-9: both rule kinds plus a holiday calendar** · **FC-10: consent is a FIRST-CLASS ENTITY, expiry a field** · **FC-11: the breakdown is a LIVING scenario document that FREEZES at client signature**, and a case may carry retainage plus a second breakdown per later recovery · **FC-15: the designation paragraph STAYS, whole, in all 12**
- **FC-12 AND FC-13 ARE BOTH RULED AND CLOSED (#108) — AND NEITHER AUTHORIZES A BUILD.** **FC-12: "(c) Structure only."** The corpus may contribute **skeletons, section orders and clause lists — NEVER body text**; body prose is always written clean. This REPLACES the interim no-imports default and makes prior-content leakage structurally impossible, the evidence being that **nothing in any of the four libraries is safely blank** and **six of the fifteen tokenized letters hard-code prior content**. **NO IMPORT TOOLING IS AUTHORIZED TO BUILD BY THIS RULING** — a separate, later act. **FE-D1 is unaffected** (its §11.3 skeleton works from your own shell). **FC-13: all four picked, plus the fifth.** Registry entries OPEN for **Prop. Code ch. 55**, **CPRC ch. 146 incl. the 2025 (c-1) amendment**, **TRCP 47(c) bands**, **§18.001/§41.0105** — and **TDRPC 1.04(f), the registry's FIRST practice-of-law proposition.** All five were **DRAFTING ACTS FOR A LATER OPUS SESSION — AND THAT SESSION HAS RUN (#112): six texts now exist at `docs/specs/fc13-entry-drafts-2026-08-18.md`, DRAFTED AND NOT INSERTED, each entering UNVERIFIED if and when it is. The reconcile is the finding — not one of the five destinations is free, so `FC13-Q-1..6` ask whether they insert at all and where.** **You chose the plain open over the boundary-ruling variant, so the cases-vs-practice boundary stays UNDECIDED**

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE MEANS: real case / party / client / SOL data hand-entered into the core app — NOTHING MORE.** Floor, in order: **(1) the CD-1 directory build landed and exercised — COMPLETE 2026-08-19 · (2) gate 1 Supabase Pro — COMPLETE 2026-08-19 · (3) gate 9 production SMTP — **ROUND TRIP ONE PASSED END TO END 2026-08-20 (`#122`) AND THE GATE IS STILL OPEN.** Both Postmark DNS records published at Namecheap and **verified at the AUTHORITATIVE servers** (all four server×record checks green), Postmark verified the domain, Supabase **custom SMTP is ON** (`smtp.postmarkapp.com`:587, sender `signin@brennanstx.com`), and a magic link **arriving FROM `signin@brennanstx.com` signed him into the LIVE database** — the From address is itself the proof the custom sender carried the mail, the built-in dev sender being unable to produce it. **WHAT THE §4 EVIDENCE NOW SHOWS AND WHAT IT STILL OWES — `#125`, 2026-08-20, ITEMISED SO THE ROUND TRIP IS NEVER READ AS THE GATE:** **§4.2 HEADERS PASS, ON THE RECEIVER'S OWN VERDICT** — `spf=pass`, `dkim=pass` TWICE including **`header.d=brennanstx.com`** signed as `signin@brennanstx.com`, `dmarc=bestguesspass action=none` (inside §4.2's *pass, or none* band), `compauth=pass`, with the Received chain showing Postmark end-to-end and TLS on every hop · **§4.4 JUNK HALF PASS** — inbox, corroborated by the receiver's own `DIR:INB` stamp rather than by report alone · **§4.4 OUTSIDE HALF: ARRIVAL PROVEN, DETAILS OWED** — an admin invite to a non-tenant mailbox arrived and the invited `auth.users` row was then DELETED by his hand, but **which provider and which folder is still owed**, and the invite-for-magic-link substitution is FLAGGED for the re-check · **§4.5 LATER-DAY REPEATS OWED BY DEFINITION** — tomorrow and once more, from a fresh browser profile, the app being localhost-only so a phone cannot reach it · **optional DMARC `p=none` still deliberately NOT published inside the go-live window.** **THE GATE IS STILL OPEN and closes at the gates re-check on that evidence, not here.** **AND THE DAY'S SECURITY FINDING SITS ON THIS GATE'S OWN SURFACE: “Allow new users to sign up” WAS MEASURED **ON** (dashboard, `#125`)** — with magic-link auth and 36 byte-identical `using (true)` policies, ON means the set of authenticatable users is NOT CLOSED, so anyone reaching the auth endpoint could mint a full-privilege user. **The recommendation — OFF for the solo phase, his existing account unaffected and staff onboarding by admin invite, which ignores the toggle — IS PUT; the RULING AND THE EXECUTION ARE HIS HAND AND WERE PENDING at `#125`'s stamp. Do NOT read it as resolved unless a LATER entry says so** · (4) gate 3 RLS tested INCLUDING the CD-1 slice's tables and `party_pii` — **THE RUN HAPPENED 2026-08-20 AND IT WAS CLEAN, AND THE GATE IS STILL OPEN.** Authorized in session (`Q-G3-1` → *"Live Supabase project"*, `Q-G3-2` → *"run as written"*), driven by a fresh Opus Code session against the LIVE project — the CD-1 pattern, the queue runner barred, and its token taken from round trip one's own signed-in browser session exactly as predicted. **RESULT: 37/37 refused signed out, EVERY ONE at the privilege layer; 36/36 policy-bearing tables ACCEPTED signed in; `file_counters` refused signed in WITH THE PRIVILEGE MESSAGE, as §3 requires; teardown verified by re-read, zero rows surviving anywhere.** Record: `docs/specs/gate3-write-path-test-run-2026-08-20.md`; pre-run audit: `docs/specs/gate3-protocol-preflight-audit-2026-08-20.md`. **WHAT IT DOES NOT DO IS CLOSE THE GATE — `Q-G3-4` IS OPEN AND HIS**, and the run report names its own six limits, of which the live one is `Q-G3-2` as ruled: **UPDATE and DELETE were NOT exercised against `using (true)`.** · (5) the gates re-check + the trigger-1 instructions revision.** **TWO OF THE FIVE ARE DOWN, LEAVING THREE — and GATE 10, which sits AHEAD of the floor, IS BUILT, RUN, CLOSED BY RULING (2026-08-20) AND NOW HAS BOTH ITS RECORDED EDGES RESOLVED OR OWED**, so the launch path is back to the floor's own three. **GL-1's FLOOR IS UNCHANGED AT THREE — discharging gate 10's edge moves it not at all, gate 10 having sat ahead of it.** **THE SEQUENCING NOTE IS SPENT: gate 10 ran BEFORE gate 3, as directed, so gate 3 covers `party_pii` once rather than twice.** **AND GATE 1's STATED RATIONALE IS HALF WRONG — CORRECTED FROM YOUR OWN DASHBOARD, 2026-08-19.** `Go_Live_Gates.md` gate 1 reads *“the free tier has no automatic backups”* and `#114` concluded that every schema act before the upgrade ran *“on a manual-dump-or-nothing footing.”* **The Supabase backup list shows ELEVEN PHYSICAL backups spanning 12–19 August — a week BEFORE the Pro purchase — EACH WITH A RESTORE BUTTON.** **So gate 10's migration was the FOURTH live schema act with a backup artifact behind it and the SEVENTH ever.** **ONE LIMB DOES NOT CLOSE AND IS RECORDED AS UNCLOSEABLE: whether those backups were restorable FROM A FREE ACCOUNT on 18 August.** He reads the button from a Pro account, and the upgrade destroyed the only state that could have answered it. The gate stays closed; it is the REASON that was wrong. **Gate 1's text is append-only, so it is NOT edited here — and whether it takes a correcting append is still the queue row `GL1-1`, OPEN, yours.** **THE CANDIDATE DRAFT WAS ITSELF CORRECTED 2026-08-20 (`#118`): it had said the append is *“Michael's ruling and his hand,”* which reads as a bar on a Code session touching the gates doc at all. **That is false — the rule governs the FORM (append, never rewrite), not the hand**, and `git log -S` shows both prior gate appends were queue-runner Code sessions executing your rulings (`70df10f`, `d6f97e6`). **The RULING requirement is unchanged and `GL1-1` is NOT answered by that correction.** *(Naming flag, carried not renamed: `GL1-1` is GATE 1's row and is NOT a sub-item of `GL-1`, the go-live floor — one hyphen apart.)*
- **GATE 10 IS CLOSED — RULED BY MICHAEL 2026-08-20.** Both halves built and exercised; the closure is appended beneath the gate in `Go_Live_Gates.md`, whose own text stands as written (append-don't-rewrite). **THE CLOSURE'S TWO RECORDED EDGES, because a closed gate is the thing most likely to be read as covering more than it does: (1) AUDITABILITY IS NOT BUILT** — the gate's parenthetical says the columns are *“excludable from API selects, auditable”*; excludable is DELIVERED and demonstrated, while auditable describes what the shape now PERMITS, not machinery that exists. `G10-1` was ruled provenance-only and the audit limb — history, freeze, `REVOKE UPDATE, DELETE`, reveal logging — is expressly owed to **`O-1`, which is OPEN**. **(2) THE §5 PRE-FLIP REPORT IS NOW RUN AND EDGE (2) IS DISCHARGED — 2026-08-20.** Michael ran `docs/specs/gate10-preflip-report-query.sql` unmodified in the Supabase SQL editor: **“Success. No rows returned”**, both labelled lists empty — the ruled EIGHT and the four AS-BUILT keys. **So the gate's exclusion limb now rests on a MEASUREMENT rather than on an expectation**, and the earlier *“a reason to expect it, not a reading of it”* is answered. **THREE THINGS THE ZERO DOES AND DOES NOT CARRY, because it is read neither broader nor narrower than it is: (a) it is clean ON TEN KEYS ONLY** — the query's own header calls the list a heuristic and a value under an unguessed key is invisible to it, which no amount of querying closes; **(b) it is a POST-FLIP reading where §5 specified PRE-FLIP** — the front-end half landed at `47b65b4` and the report ran about half an hour later; **(c) THAT DEVIATION'S CONSEQUENCE WAS MEASURED TO NIL rather than argued away** — the write-guard would have silently stripped a party saved in that window, so `parties.updated_at` under its `parties_touch` BEFORE UPDATE trigger was read against the flip time and returned **`false` on both rows**: no party was updated, the guard cleaned nothing, and **the pre-flip inference is UNCONDITIONAL on all ten keys.** **Do NOT compress this to “the report came back clean,” and do not carry the earlier four-versus-six conditional — it is superseded by that measurement.** **EDGE (1) IS UNTOUCHED AND STILL OWED TO `O-1`, WHICH IS OPEN.** **`G10-2` and `G10-4` are NOT closed by this.** Spec `docs/specs/gate10-pii-slice.md` (replaced verbatim this batch: `G10-3` ANSWERED, §8 marked EXECUTED, §3.3's C-2 comment and `anon` note conformed). **IN `db/schema.sql` AND IN THE LIVE DATABASE: `parties.date_of_birth date` (nullable, NO index) and a `party_pii` child table — `ssn`, `drivers_license`, `drivers_license_state`, PLUS PROVENANCE ONLY — with `_touch` and `set_created_by` triggers, RLS, its `authenticated` policy and its own GRANT, all from birth.** **ALL SIX CHECKS ANSWERED: `date_of_birth` is `date`/nullable · `party_pii` has SEVEN columns and NO `id`, `party_pii_pkey` reading `PRIMARY KEY (party_id)` · `authenticated` true on all four DML · `anon` FALSE on all four · both §5 key reports ZERO ROWS · exactly two triggers.** **The pre-flight did NOT fire.** **THE SHAPE: table-level exclusion works at one role where column-level `REVOKE` does not**, and the CD-1 no-second-identity-table firewall is argued past rather than slipped — `party_pii`'s PRIMARY KEY IS ITS FOREIGN KEY, so it cannot create a person. **`G10-1` HONORED — PROVENANCE ONLY; the audit limb is EXPLICITLY OWED TO `O-1`.** **`G10-2` FLAGGED, NOT SETTLED.** **THE EXCLUSION LIMB IS NOW IN EFFECT IN THE APP — BUILT 2026-08-19 ON `G10-5`.** Registry field definitions carry a **storage destination** (`dob` → `parties.date_of_birth`; `ssn` / `dlNumber` / `dlState` → `party_pii`); **`src/domain/partyPii.ts` owns the routing and a write-guard applied at BOTH adapter seams**, stripping the four keys from every `fields` write whatever the caller hands over; the three party reads moved from `select('*')` to an **explicit column list** — `select('*')` on `parties` now returns **ZERO** occurrences — naming `date_of_birth` and **no `party_pii` column**; `getPartyPii` / `savePartyPii` fetch the child row **on demand, NEVER joined into a list read**, and there is deliberately no `listPartyPii`; SSN and licence are **masked everywhere by default with an explicit per-view reveal and NO reveal log** (§4 as ruled); the demo store migrates **v11 → v12**, moving each value to its home and **REMOVING the blob keys rather than copying them**, with a full pre-migration backup. **`G10-6` was ruled IN the same day, so BOTH false `RlsProbePanel` sentences are repaired, not one.** **WALKED IN A BROWSER, not merely unit-tested — and the walk caught a defect the tests did not (see the RLS section).** **WHAT THIS DOES NOT DO: gate 10 is NOT CLOSED — that is its own ruling — and THE AUDIT LIMB IS STILL EXPLICITLY OWED TO `O-1`. The exclusion limb landing must never be read as auditability delivered.** The historical position, for readers of older entries: `G10-3` is ANSWERED **YES on all three** — the party form renders and saves every registry field including `ssn`/`dlNumber`/`dlState`, the OAA importer writes an extracted `dob`, the seed plants two — and **`listParties()` / `getParty()` / `getParties()` all `select('*')`, so those values ride every party read TODAY.** **THE FRONT-END SLICE NOW EXISTS: `docs/specs/gate10-pii-frontend-slice.md`, PROPOSED, EXPRESSLY NOT BUILD-AUTHORIZED — storage destinations on the four registry fields, a write-guard stripping them from every `fields` write, explicit column lists replacing `select('*')`, `getPartyPii`/`savePartyPii` on the DataAdapter so both modes keep working, and SSN MASKED BY DEFAULT WITH EXPLICIT PER-VIEW REVEAL AND NO REVEAL LOG (ruled 2026-08-19; logging rides `O-1`). Authorizing it is `G10-5`, YOURS, its own session, the CD-1 / FE-D1 pattern — the queue runner is BARRED. **ITS KICKOFF PROMPT IS NOW FILED AND UNFIRED at `docs/prompts/PROMPT-gate10-frontend-build-session.md` — the `#115` pattern, written ahead so authorizing is ONE act; filing it authorizes NOTHING, and its own Step 0 tests “RULED YES” rather than “ruled,” since a ruling of NO satisfies the weaker test.** **AND THE RULED §5 KEY LIST COULD NOT HAVE FOUND A LICENCE NUMBER**: none of its four guesses matches `dlNumber`/`dlState`, so the migration probes BOTH lists, labelled
- **A POINTER CHECKLIST OVER THE GATES EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It restates no gate's substance and carries NO status column, by design** — status is this file's job, and a second status snapshot is the `#13`/`R-3` copy-forward failure. **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect.** **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor**
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. Appending a gate is the ruling.)*
- **THREE PLACES WHERE READING A GATE ALONE GIVES THE WRONG ANSWER (#92 — recorded, NOT corrected).** **Gate 2 read alone OVER-BLOCKS** (the 08-11 clarification is load-bearing). **Gate 3 read alone looks satisfiable and is not** — gate 6 is a hard prerequisite and **the dependency is recorded at gate 6, not at gate 3.** **Gate 9's trigger is a different SHAPE** — the others fire on real *data*, gate 9 on *reliance*, **so a checklist ordered by "when does real data arrive" schedules it too late**
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**
- **ITEM 7 RAN 2026-08-19 AND IS VERIFIED — CD-1 IS COMPLETE (#113).** `db/migrations/2026-08-12-cd1-contact-directory.sql`, run FIRST of the three by your hand, its five checks answered in words: **C1** 0 untagged / 0 mismatched against 2 `parties` rows · **C2** 0/0/0 against 2 `case_parties` rows · **C3 TWO open roster flags against two links both carrying role `Client`** — the high end, which is the RIGHT end · **C4** `authenticated` reaches both new tables · **C5** `to_regclass('public.case_links')` null, the CL-1 firewall holding. **THE TWO-ROW CAVEAT, STATED RATHER THAN GLOSSED: the backfill ran over TWO rows and steps (c) and (d) matched NOTHING** — no Plaintiff, Defendant, Witness, adjuster, provider, expert or judge link exists live — **so the caption-alignment and function-role branches were NOT exercised here.** They were exercised in code on fixtures at #61, and GL-1 item (1) is satisfied by the PAIR; **a two-row backfill proves less than "verified clean" reads**
- **THAT ORDER HAPPENED, AND THE FAILURE IT WARNED OF DID NOT (#86 → #113; `Q-PR3-3` ANNOTATED, STILL OPEN).** CD-1 ran with **PR-3 unexecuted** — the sequence in which probate rows are still PI at that moment, take the plaintiff-defendant derivation, and are never revisited. **Backfill step (c) matched ZERO rows**: both live `case_parties` links carry role `Client`, which step (c) does not target. **The backfill limb is SPENT. The row is ANNOTATED, NOT CLOSED** — it stays coupled to the `sideSetFor()` defect below, **which is untouched and still fails open for a fourth practice area**
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED**
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **THE PROTOCOL FOR STAGE 1 EXISTS AND AUTHORIZES NOTHING (#90, Task 16): `docs/t3-pilot-recording-protocol.md`** — 534 non-blank / 688 raw, **PROPOSED — PREPARATION ONLY.** `R`/`K`/`I` provenance marks intact, **`I` NOT promoted**; its §5.3 placeholders are **deliberate — no audio file was measured.** **Seven questions `Q-T3P-1..7`, packet-local.** Placement at `docs/` is your **interim** direction, not a ruling (`Q-T3P-7` open)
- **ITS HEADLINE IS THAT HK-4 MAY DESCRIBE AN ACT THAT ALREADY HAPPENED.** HK-4 and this file both read as *"create the directory at staging time"*; the **2026-07-25** pilot-fixtures entry records the thirteen as **already archived at `..\data\pilot-recordings\` WITH A PROVENANCE README no design session has ever read.** **So HK-4 is probably a P15 → P1 TRANSFER — cheaper and more dangerous, since a transfer can silently overwrite that README** (`Q-T3P-1`, `Q-T3P-2`). **The bundle also held ground-truth scripts, scorecard and findings, while HK-4 names only audio** (`Q-T3P-3`)
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **YOUR HAND (HK-4), NOT RE-WORDED.** **Re-checked natively this refresh against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** A negative about **this** machine that **does not answer the preflight row**, which is yours
- **THE MEASURE ITSELF IS SPLIT, WHICH NOTHING ELSE STATED (`Q-T3P-4`).** The authorization reruns both batches *"against the existing ground truth"* — but ground truth exists for **five of thirteen**. Batch 2 is scripted → an **ACCURACY** measure; batch 1's "existing" artifact is the int8/CPU-floor transcript, a prior **OUTPUT** → a **CHANGE** measure. One figure reports two different things. **D1 auto-file and the confidence thresholds wait on this scorecard**
- **COVERAGE, PLAINLY: the thirteen exercise ROUTING thoroughly and the ASR stack NARROWLY.** No long-form audio (max ~71 s; deposition length is the Statement Bank's real use case, untested — Phase 0's 79 MiB / 20 MiB activation figures are **3-second datapoints, not extrapolable**). The **4-speaker cap is untested**. **No Spanish.** **Nothing from the capture kit, which is not purchased. Word boosting has never run on any recording.** All thirteen are iPhone / Voice Memos / **AAC ~70 kbps mono** and can never meet the kit standard — **preserve bit-exact, no re-encode/up-sample/normalize/trim**, because an up-converted file *looks* like kit-standard material in its header
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (unauthorized). A **second, unrelated `T1`–`T4`** is the LegiScan poller tier in `statute-text-and-bill-tracking-design.md` — **substantially BUILT.** **Five WF gates say "gated on T3" without naming which**; re-lettering is yours (the H → HK precedent)
- **THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until you locate it or re-issue, T3 WORK IS UNAUTHORIZED.** **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6`**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher
- **INTAKE IS EMPTY IN THE DATABASE AND NOT EMPTY IN THE FRONT END (#88, `Q-RE-4`).** The master spec **designs** the PNC funnel and calls it SETTLED — PNC → Client / Declined / **Referred out** — and the db-side claim holds: word-bounded `PNC` returns **ZERO** across `db/` and `supabase/`, `parties` has **no status column**, and three separate places record the missing PNC → Client promotion path. **But "it is not built" was too wide: `src/domain/partyRegistry.ts` carries a `pncStatus` select on the `person` party type whose options are exactly `— / PNC / Client / Declined / Referred out`, stored in `parties.fields`.** So the funnel's CAPTURE exists as free-form jsonb while nothing in the database models it — **the same wrong-level pattern gate 10 was ruled to fix, one entity over.** Flagged, not built on

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)**. **SSN and licence inputs render here TODAY and save into `parties.fields`** — the front-end slice fixes it, behind `G10-5` |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** | bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients.** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab.** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT — RE-AFFIRMED 2026-08-18 (FC-5).** **D-CL2-3a: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. The time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter interface — every feature works in both modes. Seeds fictional; **store v12** (gate 10 PII promotion), v9→v10→v11→v12 chains **FORWARD in place**, backing up at each step. **A LATENT DEFECT WAS ACTIVATED AND FIXED BY THIS BUMP, AND IT IS THE MOST REUSABLE THING IN THE SLICE: `migrateV10ToV11` stamped `STORE_VERSION` rather than a literal 11** — correct only while that constant happened to BE 11. At 12 it would have stamped a v11 store as v12, so **the v10 chain would have SKIPPED the v11→v12 migration entirely and left SSNs in the blob.** This file's own `v9→v10` step already carried a comment warning of exactly this class; the `v10→v11` step did not. **All three steps are now pinned to literals**, and the CL-2 test that asserted `STORE_VERSION` was pinned with them — **that assertion breaking is what surfaced it.** **`npm run dev:demo`** runs demo mode past a real `.env`; **demo state does NOT travel**. **AND THE APP ON `mdb-pllc` SAT IN DEMO MODE UNTIL 2026-08-20 FOR A REASON NOBODY HAD INSTRUMENTED FOR (`#122`): `.env`'s `VITE_SUPABASE_URL` AND `VITE_SUPABASE_ANON_KEY` WERE EMPTY — the KEYS present, the VALUES never filled**, the file having been copied from the template on 2026-08-14 with only the two MSAL values entered. `usingSupabase = Boolean(url && key)` did exactly what it says. **Michael filled both by hand 2026-08-20 and the footer flipped to “Connected: central database.”** **The design side's own earlier claim that `.env` “holds” the two was corrected in the same entry, and the correction is the reusable part: its check redacted values with `=<redacted>`, WHICH RENDERS A FILLED VALUE AND AN EMPTY ONE IDENTICALLY** — a value-LENGTH check is what it should have been, the `#119` normalizer class again, *the instrument built so it cannot disconfirm.* **`.env` is gitignored at `.gitignore:24`, re-derived here; ITS VALUES WERE NOT READ BY THIS BATCH and nothing about them is asserted beyond `#122`**
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **37 `create table` statements in schema.sql AND THE LIVE DATABASE IS AT 37.** **THE SERVER IS PostgreSQL `17.6`.** *(Re-derived here every refresh: 37 tables, 37 `enable row level security`, **36 policies** — the gap is `file_counters`, deliberately policy-free as the probe's deny control, so never "correct" 36 to 37 for symmetry. **The live 37 is now BETTER THAN INFERRED ON ITS FLOOR, and still not a census.** The prior basis was the live 36 (#113) plus gate 10's one new table, corroborated by a privilege sweep. **The 2026-08-20 gate 3 run adds a POSITIVE, PER-TABLE existence reading: each of the 37 named tables answered an insert BY NAME — 36 accepting and `file_counters` refusing on privilege — and a table absent from the API would have returned `PGRST205` instead. None did.** That raises the floor from a privilege inference to a per-name confirmation; **it still cannot see a 38th table, because nothing enumerated the catalog. NO TABLE CENSUS WAS RUN.**)*
- **NINE SCHEMA RULINGS CAME OUT OF THE GROK EXTERNAL REVIEW (#111) AND EIGHT ARE IN `db/schema.sql`; THE LIVE HALF RAN 2026-08-19 AND ALL TEN CHECKS CAME BACK CLEAN (#113).** **F-1 WAS A LIVE EXPOSURE AND IS NOW CLOSED:** no PUBLIC entry survives on `next_file_number()`, which as a `SECURITY DEFINER` counter-writer was `anon`-callable over PostgREST RPC from 2026-07-28 until 2026-08-19 — **and the PG17 default was checked against the documentation rather than assumed.** **F-3 confirmed live on both limbs.** Landed: **`revoke execute on function next_file_number() from public`** (F-1, ordered AFTER the replace) · **`America/Chicago` on both the file-number year and `cases.date_opened`** (F-3 — a case opened 2026-12-31 18:05 Central would have been issued a `27-` number) · **a BEFORE UPDATE trigger refusing any change to `file_number`** (F-2) · **`created_by uuid references auth.users (id)` on five tables with an `auth.uid()` BEFORE INSERT trigger** (F-25 — **columns only; NO per-user RLS**) · **the "gapless" claim OUT of the schema header** (C-6) · **the stale grant-block counts rewritten COUNT-FREE** (F-27)
- **THE NINTH RULING LANDED THE SAME NIGHT (ruling 4, F-4+F-18).** `case_parties` now carries **`unique nulls not distinct (case_id, party_id, role, capacity_kind, capacity_points_at_party_id)`** plus **`case_parties_capacity_pointer_check`** — `next-friend-of` / `representative-of-estate-of` / `dba` require a pointer, `individually` exempt. **It was STOPPED first** when the database read 14.5. **Michael upgraded, and it landed under the EXISTING ruling — no fresh authorization, only the gate passing.** **IT RAN 2026-08-19:** ONE unique constraint on `case_parties`, ZERO of the old three-column key, and the pointer check at 1. The file carries a `server_version_num` guard **REFUSING to run below 15** and drops the old key **by looking it up in `pg_constraint` by DEFINITION rather than by a guessed name.** Its `raise notice` was treated as decoration — **a check that cannot produce a disconfirmation is not a check.** **O-8 and O-9 both close**
- **F-12's STEP (d) FIX EXECUTED LIVE 2026-08-19.** `party_status = coalesce(party_status, null)` — which would have left every Witness / Adjuster / provider / expert / judge NULL though `'non-party-actor'` exists in the CHECK — now writes the value. **It was edited while the migration was still unrun, on your in-session confirmation. The repo could establish NEITHER fact — both are your word**
- **A CASCADE/RETENTION MAP EXISTS AND NOTHING IN IT IS EXECUTED: `docs/specs/cascade-retention-map-2026-08-18.md`** (**PROPOSED — AWAITING SIGN-OFF, O-7**). **Eleven** FKs across C-1's six named children mapped current→proposed (CASCADE/SET NULL → RESTRICT), **four component FKs argued for KEEPING cascade**, **six adjacent FKs listed as UNRULED**. **Read its §6 first: RESTRICT without app-layer soft-delete handling means matters can no longer be deleted at all**
- **GRANTS ARE PART OF THE SCHEMA — load-bearing, AND `C-2` IS NOW RESTATED ON THE TRUE WARRANT (RULED 2026-08-19, queue row `O-13`).** **`anon` HOLDS `TRUNCATE`, `REFERENCES`, `TRIGGER` AND `MAINTAIN` ON ALL 37 TABLES, AND SO DOES `service_role`.** **THE SOURCE IS IDENTIFIED, READ FROM THE CATALOG TWICE: `ALTER DEFAULT PRIVILEGES` IS SET ON THIS DATABASE** — `pg_default_acl` carries `postgres | public | r | anon=Dxtm/postgres, …`, so every table `postgres` creates in `public` takes those four automatically. **Nothing in this repo granted them: ZERO executable lines across `db/schema.sql` and all six migrations name `anon` or `service_role`.** **THERE IS NO READ EXPOSURE:** `anon` holds none of the four DML privileges, and a `relacl` sweep for an empty-grantee entry returns **ZERO ROWS**. **The one limb still open is REACHABILITY.** **AND `MAINTAIN` WAS INVISIBLE TO THE FIRST INSTRUMENT** — a PG17 privilege `information_schema` has no column for. **SO THE CONCLUSION SURVIVES ON A DIFFERENT WARRANT: "a new table without its own GRANT is unreachable" holds because THE VENDOR'S DEFAULT EXCLUDES THE FOUR DML PRIVILEGES, not because no default exists — the posture is held by Supabase, not by this project's discipline, and would change silently if that default changed.** **A SHARPER CONDITION: a SECOND rule, `supabase_admin | public | r`, grants `anon=arwdDxtm` — SELECT INCLUDED — to tables created by THAT role. None of the 37 is. Whether Supabase tooling ever creates a `public` table as `supabase_admin` is OPEN (`O-12`, DEFERRED BY RULING pending four unrun diagnostics; the remedy is `O-11`, yours).**
- **THE `anon gets nothing` REPAIR IS NOW COMPLETE IN THE DOCS, AND THE LAST FIVE SITES WERE FOUND BY RE-DERIVATION RATHER THAN BY THE PACKET.** Repaired 2026-08-19 under the packet's routing table: `db/schema.sql` (3 comment sites, executable lines proven identical), the spent kickoff prompt (2), and three forward-looking specs (in-1, in-3, wf-2-wf-8). **The five RUN migrations were ANNOTATED, NOT REWRITTEN — dated correction blocks appended, originals standing visible as the record of the text that ran (your express ruling, twice put).** **THEN THE RUNNER'S OWN SWEEP FOUND FIVE MORE CARRIERS IN NO ROUTING ROW — `fe-4`, `fe-5`, `fe-6`, `qbo-integration-research-memo` and `re-1-referral-engine-inputs` (six sites) — WHICH YOU AUTHORIZED IN-SESSION AND WHICH ARE NOW REPAIRED**, each carrying a dated conformance note quoting what it used to say. **`fe-4`/`fe-5`/`fe-6` were TRIPLY stale: each also said "probe covers 36 tables," now **37** — verified independently against `src/auth/rlsProbe.ts`, whose list is 37 entries, contains `party_pii`, and is sequence-identical to `db/schema.sql`.** **THE METHOD POINT IS THE DURABLE ONE: `fe-4` escaped the first sweep because it writes `is **not** set` with the emphasis INSIDE the phrase, so the pattern could not span it — the tally moves with the pattern, and no single sweep can be trusted.** **WHAT REMAINS OF THE `anon gets nothing` FAMILY IS ONE SITE: `src/components/RlsProbePanel.tsx`, knowingly left — it is item 6 of the front-end slice's checklist, behind `G10-5`.** **BUT THE ADP FAMILY WAS NOT COMPLETE, AND A WHOLE-TREE RE-SWEEP AT `82be555` SETTLED IT: THREE CARRIERS STAND, NOT ONE** — 116 hits over all 313 tracked files including `src/`, emphasis-stripped and wrap-aware, every hit classified BY READING. **The two nobody had listed are `docs/spec-feedback.md`:591** — the phrase WRAPS across the 591/592 line ending, so a line-anchored grep of that file returns four hits and NOT this one (**the wrap trap, a third time**), and it sat under a retraction-class exemption this file grants BY FILENAME, true of that file's 2026-08-19 entry and false of its 2026-07-28 one — **and `docs/specs/attorney-review-queue.md`:793**, the same sentence its source memo carried at `re-1-referral-engine-inputs`:726, **which WAS repaired**: not a narrower pattern this time but a routing table that named the file the text came from and not the file it had been copied into. **BOTH WERE REPAIRED 2026-08-19 ON YOUR RULING**, each taking a dated conformance note that quotes what it used to say, and the queue's copy is back in step with the `re-1` memo it was copied from. **The `src/` carrier still stands, behind `G10-5`.** Full classified table, all 116 hits and the 36-hit `canonical` false-positive class: `docs/specs/anon-adp-sweep-verification-2026-08-19.md` (PROPOSED, one run).** **The retraction class is NOT counted and must never be "fixed": `spec-feedback.md`, this file, the gate 10 run entry, the TOC row and every conformance note above quote the sentence in order to deny it, and the log's entries are append-only.** **AND THE CLASS TOOK A SECOND VICTIM THIS BATCH, IN `src/` THIS TIME: a sweep for `select('*')` on `parties` returned ONE against this file's published ZERO** — the hit being `src/data/__tests__/gate10Pii.test.ts`'s GUARD TEST, whose whole purpose is to assert the absence, the string present in order to deny it. **The ZERO stands unamended; the fix was CLASSIFICATION, never a narrower pattern.** **The live-privilege remedy is still YOURS and none was run (`O-11`)**
- **`privilege_tier` NO LONGER ASSERTS A STANCE NOBODY CHOSE — Q-COM-11 RULED (A) AND THE SCHEMA HALF IS EXECUTED (#94).** Both columns (`generated_documents`, `transcripts`) **lost `default 'work-product'` and `not null`**; **NULL now means unclassified-must-classify.** **THE MIGRATION RAN 2026-08-19 (#113): both columns nullable, NO default, and BOTH CHECKs untouched — the two vocabularies still disagree, which is the CORRECT result and the named failure the file was written to avoid.** **No existing row was re-characterized** (all fiction; re-characterizing is a legal act and yours). Two honest limits: its C2 check is satisfied **TRIVIALLY** (both tables are empty), and **check 4 — insert-a-null inside a rolled-back transaction — was NOT run.** **NO creation-time classification UI was built**
- **THE TWO `privilege_tier` VOCABULARIES STILL DISAGREE IN THE SCHEMA, AND THE DIVERGENCE IS DUPLICATED IN TYPESCRIPT (#85 → #89 `Q-COM-10`, extended #94) — BUT THE SHAPE THEY MUST CONVERGE ON IS RULED (#105, `Q-COM-10-A`).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')`. **NEITHER CHECK WAS TOUCHED.** **`src/domain/billing.ts` and `src/domain/transcripts.ts` each `export type PrivilegeTier` with DIFFERENT members** — **so the question has FOUR homes and any ruling touches `src/` as well as `db/`.** **THE RULING: `privilege_tier` is NOT exhaustive** — purpose-scoped under a stated precedence rule — **and the target is Option 2: THREE values plus a `witness_statement` boolean.** **`Q-COM-10-B`–`F` are unblocked**
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view**, and a word-bounded sweep returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **THERE IS STILL NO GEOGRAPHY ON `cases` — BUT THE QUESTION ABOVE IT IS RULED (`Q-RE-1`, CLOSED 2026-08-18, "Both, separate triggers").** **`cases` ALREADY CARRIES `county text`**, added by the OAA criminal-intake block along with `in_custody`, `custody_location` and `appointment_date`. `court_name` and `cause_number` remain free text, and **`venue`, `district` and `jurisdiction` are genuinely absent.** The old blanket claim was **partly** true, which is worse than wholly wrong: **FC-8 would have authorized adding a column that already exists.** The out-of-area referral trigger tests **geography AND subject-matter INDEPENDENTLY.** **No migration is authorized by either ruling**
- **A WRITTEN-CHANNEL COMMUNICATION STILL HAS NOWHERE TO BE (`Q-COM-2`, CLOSED 2026-08-18, "Yes — adopt the list").** `transcripts.source` and `staging_items.source` **both** still admit exactly `('recorder','phone','manual')`. The ruled list to add: **`e-service`, `email`, `first-class mail`, `certified-mail-RRR` (tracking number as its own field), `fax`, `portal`.** **NOTHING WAS WIDENED — THE MIGRATION IS A SEPARATE LATER ACT AND IS NOT AUTHORIZED.** **AND IDENTITY RESOLUTION HAS NO SUBSTRATE (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series.** **Nothing for a deadline engine** (FC-7/FC-9 are direction only), **and no discovery-level field on `cases`** (FE-5's hard prerequisite). **Nothing for a consent entity** (FC-10). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all ZERO (#85). AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — none of the 37 tables is a money table. NOR FOR RE-1: `referral`, `conflict_check`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC`, `prospective` all ZERO (#88)**. *(Basis stated: word-bounded over `db/` these are exactly zero. Widened to `db/` + `supabase/`, two stop being literally zero — `message` ×3, every one a JavaScript `error.message`, and `case_links` ×4, every one a COMMENT saying it is not built. **A future sweep that counts the schema's own disclaimers will "find" the thing they deny.**)*
- **HEALTH WAS *NOT* RUN THIS BATCH, AND THE FIGURES BELOW ARE CARRIED AND LABELLED RATHER THAN RE-STATED AS THIS TREE'S — `QR-6(f)` APPLIED.** The skip needs `§5` NONE **and** no `src/`, `db/`, `supabase/` or build-tooling path routed; **both limbs hold**, this batch's routing table reaching exactly one path, `docs/specs/session-log.md`. **CARRIED FROM THE GATE-3 SESSION AT `dcc9db2`, WHICH DID RUN THEM AT ITS STEP 0: `npm test` — 322 tests pass across 26 files** · **`npm run build` (`tsc -b` + vite) — exit 0** · **`npm run lint` (oxlint) — exit 0**, the >500 kB chunk warning pre-existing. **THOSE FIGURES ARE ABOUT THAT TREE AT THAT COMMIT.** This batch wrote no code, so they are not stale — but **a carried figure can never be evidence about the tree that carries it, and no health claim of this batch's own is asserted anywhere.** **The build session's lesson still stands: `npm test` passed through two build failures once, because vitest does not type-check — a green suite is not a green build, which is why all three are run together when they are run at all.**

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED, AND THE LIST IS NO LONGER THE SHORT ONE:** authenticated SELECT on all API tables; **INSERT ON ALL 37 TABLES, SIGNED OUT AND SIGNED IN, MEASURED 2026-08-20** (`docs/specs/gate3-write-path-test-run-2026-08-20.md`); the app's write path; CL-2's live paths. **THE "PRESENT BUT UNTESTED — an inference" POSITION IS RETIRED FOR INSERT: 36 of 36 policy-bearing write paths were exercised against the live database and all 36 ALLOWED.** **GL-1 item 4 is the gate and it is NOT closed by that** — `Q-G3-4` is Michael's at the gates re-check, and **UPDATE and DELETE against `using (true)` remain unexercised by ruling** (`Q-G3-2`, *"run as written"*), which the run report names rather than resolves. **AND `#125` ADDS A CONDITION THE RUN RECORD COULD NOT HAVE STATED, WHICH IS CONTEXT FOR `Q-G3-4` RATHER THAN A CORRECTION OF IT:** that record's §10 framing — *“`using (true)` grants every authenticated user every row, by design, in a solo practice”* — is sound only while the set of authenticatable users is CLOSED, **and the signup toggle was measured ON.** The record stated its own scope accurately; what changed is a live-configuration fact outside it
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is NOT an RLS result, **and the 2026-08-20 run CONFIRMED THAT BY MESSAGE TEXT rather than by code**: signed in it refused with *"permission denied for table file_counters"*, never the RLS wording. **Both denials share SQLSTATE `42501`, so a test reading only the code would have scored a privilege refusal as proof that RLS works.** Probe READS all **37** tables (re-derived here: `SCHEMA_TABLES` is 37 entries, contains `party_pii`, `file_counters` its sole `policy:false`, and the list is SEQUENCE-IDENTICAL to `db/schema.sql`'s own `create table` order) but WRITE-probes only **5** — `parties`, `legal_rules`, `glossary_terms`, `watch_targets`, `file_counters`. **THE PROBE IS UNCHANGED; what moved is what has been MEASURED — the gate 3 protocol run covered 37, so the exercised figure is 37 while the STANDING INSTRUMENT still covers 5.** Whether the probe grows to 37 is `Q-G3-5`, a build act needing its own authorization. **The protocol's structural finding stands and limits what the gate can claim: ALL 36 POLICIES ARE BYTE-IDENTICAL — `for all to authenticated using (true) with check (true)` — so the run exercised ONE RULE IN FORCE ON 36 TABLES, not 36 rule sets.** **AND THE DEEPER LIMIT IS UNTOUCHED: THE PROBE STRUCTURALLY CANNOT SEE THE CLASS OF DEFECT FOUND 2026-08-19.** It drives PostgREST, so `REFERENCES`, `TRIGGER` and `TRUNCATE` are invisible to it on every role — **an UNEXPECTED grant hides from it exactly as well as a MISSING one**, and the gate 3 run inherits that blindness whole. **BOTH FALSE SENTENCES IN `RlsProbePanel.tsx` ARE REPAIRED, 2026-08-19** — `G10-6` ruled IN and the `G10-5` build executed it. The signed-out clause now reads *"anon holds none of the four DML privileges"* (adopted wording); the privilege-wall sentence two lines above it now reads *"The role holds none of the four DML privileges on these tables"* — **wording PROPOSED, NOT separately adopted, and yours to override. The 2026-08-20 run is direct evidence for the first: 37 of 37 signed-out inserts were refused for want of a grant.** **Normalized sweeps return ZERO for both old sentences and for "granted nothing": the app renders no false privilege claim.** The second sentence WRAPPED across a source line break, so an exact-match edit anchored on the whole sentence returns zero on correct input — the wrap trap's FIFTH appearance. **The panel is CRLF while the rest of the tree is LF; the convention was detected by RAW BYTE READ before the edit and is unchanged at 144 CRLF after it** — the guard refused an LF-authored block rather than writing one

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. **INDICATED, NOT MEASURED, AND THE DIFFERENCE IS KEPT: `service_role` SHOWS NO DML ON ANY OF THE 37 TABLES** (only `REFERENCES`/`TRIGGER`/`TRUNCATE`, same as `anon`) — **but that rests on the `information_schema` listing alone, a WEAKER instrument than `anon`'s `has_table_privilege` test.** Diagnostic 6 in `spec-feedback.md` is staged and UNRUN. On the evidence the poller would fail on PRIVILEGE rather than on auth the moment it is deployed — and bypassing RLS does not bypass table privileges. **The conclusion holds; its stated GROUND does not: *"`service_role` was never granted either"* is true of THIS PROJECT'S SQL and false of the DATABASE.** **F-14 IS LATENT, NOT A GL-1 BLOCKER (#113)** — undeployed code has no endpoint; it rides `O-3`. **/statutes in demo mode never touches Supabase**
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY THIS REFRESH BY EXISTENCE TEST ON DECLARED PATHS ONLY (HK-5; nothing enumerated) AND STILL ABSENT**, as are `C:\Users\Brennan\data` and `...\data\pilot-recordings\`. **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (your hand, your browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently.** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** Repro: `outlook-edit-cancel-exercise-2026-08-13.md`. **THE AUTH SURFACE IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on your registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (yours) — **and OBS-1 asks whether that capture row folds into Phase 1b's scope; unruled.** Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**
- **A MIGRATION PROPOSAL EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (Task 12; you did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately a QUESTION.** **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`). **`Q-PR3-1..7` and `PR3-LOOK-1` are yours; nothing was re-parented**
- **A DEFECT IN A RULED CONSTRAINT, CONFIRMED AT HEAD, CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts` returns the plaintiff-defendant side set for **every practice area that is not `'Criminal'`** — while that file's header states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant alignments. **Live today, unreachable only because no fourth practice area exists.** **The codebase carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by design, while `sideSetFor()` **silently supplies a wrong-but-plausible answer.** **Flagged, not fixed**
- **FORM ENGINE — FE-D1 (DISCLOSURES ENGINE) NAMED, SCOPED, AND AUTHORIZED 08-12 (#63); NOT BUILT.** Scope `docs/specs/fe-d1-build-slice.md`; kickoff `docs/prompts/PROMPT-fe-d1-build-session.md`; **yours to fire, Opus, its own session** (the CD-1 pattern — the queue runner is barred). Slice 1 = §§1–6 wizard + §10 substrate + §11.3 skeleton; **NOT "FE-1"** (retired scout). **IN:** FE-10 + FE-12 from birth, FE-8 retention half, FE-15, a MINIMAL template editor. **OUT with homes:** FE-9 · FE-11 · FE-13 · **FE-14 (TRCP 47 gate)** · FE-16 · FE-17. **FE-4/FE-5/FE-6/FE-7 have NO FE-D1 disposition at all.** **`form-engine.md` §3 now carries the FC-1–FC-4 token-syntax rulings, so slice 1's parser has a ruled spec rather than an assumption.** `docs/skills/drafting-disclosures/SKILL.md` is **a DOCUMENT, not code — now v2** (08-11, design-side; **Code must never edit it**)
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, backlog 47, THIRTY-FIVE VERIFIED — and the ruled-wording tail is EMPTY
- **BACKLOG RE-DERIVED AT HEAD THIS REFRESH, NEVER CARRIED — and it is UNMOVED for a second consecutive batch:** `legal-rule-registry-discovery-enforcement-and-pleading.md` **30 VERIFIED / 10 UNVERIFIED against 40 `**Status:**` lines**; `legal-rule-registry-criminal-plea-and-costs.md` **5 / 2 against 7.** **Backlog 47 entries; 35 verified, 12 not.** **The unit is STATUS LINES, not headings.** **Counting trap, carried: `grep 'VERIFIED —'` also matches `UNVERIFIED —`; anchor on `^**Status:** VERIFIED`.** **SECOND TRAP, CARRIED AND STILL LIVE — THE BASIS IS TWO NAMED FILES, NOT A GLOB.** A `legal-rule-registry*` sum returns **74 / 57 / 12** by folding in `legal-rule-registry-discovery-and-carrier-duties.md` (22, all VERIFIED) and the medical-billing drafts; `registry-new-entry-drafts-2026-08-17.md` carries **six `**Status:** UNVERIFIED` lines of its own** — drafted entry TEXTS, not registry entries — and also matches
- **THE `WS-3` ENTRY IS INSERTED (#109) AND VERIFIED (#110) — THE FIRST CHARACTERIZATION-BEARING ENTRY TO REACH VERIFICATION SINCE THE V-9 AMENDMENT.** Gates ruled at #109: `Q-WS3-1` → **"Cross-reference"**; `Q-WS3-2` → **"Comment-9"** (the derivation stands as drafted, its note stating honestly that both opinions reach the result **through comment 9** and that **no located case uses the 192.3(a) formulation**). **Verification rested on a LAYERED basis:** the 192.3(h) clause and the full comment-9 quotation **mechanically verified**; the *Fontenot* / *ExxonMobil* characterizations **resting on the #102/#104 reads as recorded**; the `C-XL-1` boundary and `C-ARD-1` condition as adopted at #108. **Michael's judgment, not a text match.** **THE PINPOINT CAVEAT SURVIVES AS VERIFIED TEXT: star-pagination pinpoints must be checked against the reporter before any quotation enters a filing**
- **THE ELEVEN THAT REMAIN, AND THE RECORDS LOOKS BEHIND THEM RAN (#99, T-20/T-21).** **Group A is fully closed** — 13, 2 and 29 VERIFIED at #108; 12 and 32 VERIFIED at #109. **Seven records/cite looks** — 15, 16, 17, 18, 19a, 20a, 21a — have **candidates staged per entry, source named per item**, none adopted. **Formerly four blocked** — **20b is LOCATED and 24's majority is IDENTIFIED**; 30 and 31 remain cite-less and their cites are yours. **The eleven are not "the enforcement backlog PLUS the criminal two": 30 and 31 ARE the criminal file's two. The enforcement file holds TEN unverified** — 15, 16, 17, 18, 19a, 20a, 21a, 20b, 24 **plus 19b**, whose verification detached on rewording — **and the criminal file two. Ten plus two is the 12**
- **THE ROUTE-C UNVERIFIED-BY-DESIGN CLASS IS DOWN TO THE PAIR THAT WAS ALWAYS GOING TO BE LAST.** Entries **12** and **32** left it at #109, and **A** (192.3(h) own-statement), the new **215.1(e)**, **B** (193.4(a)), **F** (199.6) and **WS-3** all left it at #110 — **four of those five on programmatic character-exact matches with ZERO flags.** **What remains is 19a** (*Castillo*) **and 19b** (*Able Supply*). **No entry with adopted-but-unverified wording remains anywhere in the registry.** Adoption is not verification — **execute-then-verify (#95)**
- **ROUTE (c) IS STANDING LAW (#95) AND HAS RUN THREE TIMES.** A divergence that **changes what an entry means** gets proposed conforming wording, **drafted and queued for your adopt/reject/edit — never adopted silently**, and never as a package. #96 produced its one **rejection** (`V7-23-CITE`), the evidence it is not a rubber stamp
- **`Q-STAT-6`'s EIGHTEEN ARE NOW NONE LIVE, AND THE ROW STILL STAYS OPEN.** Adopted at #95 and **verified 08-17: 5, 6, 11, 27**. Flags **CURED**: **1, 23, 33, 34**. Divergences **RESOLVED as-is**: **4, 7, 8, 9, 26**. **The last two, 12 and 32, are VERIFIED as of #109** — and their divergence lists produced a STANDING DRAFTING DIRECTION (#108, your words: *"Why are we making these language changes in the first place instead of simply staying with the actual language of the rules?"*): **registry Rule lines quote operative text VERBATIM where practicable**, condensing only where the rule is too long or the entry states one limb, **flagged as such**. **It is a ruled DIRECTION, not a binding convention — putting it in the project instructions fires trigger #3, and that is yours.** **Applying it to any EXISTING entry is a per-entry ROUTE-C act and also yours**
- **CR-10 STAYS HARD-GATED, THOUGH ONE OF ITS FOUR IS NOW CLEAR:** **32 is VERIFIED (#109)**; 29 was verified at #108; **30 and 31 are cite-less and their cites are yours.** **THE BRIDGE FOR 31 EXISTS AND IS HELD (#104): *Hurlburt v. State*, 506 S.W.3d 199 (Waco 2016)** — verified independently against the opinion text (`type="majority"`, **Tom Gray, C.J.**, one sub-opinion, every quoted passage confirmed verbatim) — **holds that the CCA's construction of *“in a single criminal action”* governs art. 102.073(a) AND that the “same criminal episode” limb DROPS OUT for costs.** **It routes through *Ex parte Pharr*, 897 S.W.2d 795 (Tex. Crim. App. 1995), NOT *LaPorte*** — **`Q-AUTH-1` IS NOW EXECUTED (2026-08-19, on your in-session “Go”): `docs/specs/pharr-hurlburt-authority-read-2026-08-19.md`, PROPOSED/staging — BOTH opinions read in full and BOTH POSITIVELY IDENTIFIED UNDER V-9's SECOND LIMB** (*Pharr* PER CURIAM, CCA en banc; *Hurlburt* TOM GRAY, C.J., corroborating `#104` from a different source). ***Pharr*'s negative side is SHARPER THAN EXPECTED: two plea proceedings held back-to-back the SAME DAY were still NOT a single criminal action, because each concluded before the next began** — and *Hurlburt* names that as its distinguishing fact. **The criminal-episode limb drops out BY AGREEMENT** (*“We agree with Hurlburt”*), **weaker footing than a contested holding.** **THREE LIMITS RIDE: *Hurlburt* is TENTH COURT (Waco) and Bexar sits in the FOURTH, so persuasive not binding; NO CURRENCY CHECK was run on a 2016 construction of a 2015 statute (`Q-PH-1`); and both copies are vLex — LOCATORS, not the reporter.** **THE NEGATIVE IMPLICATION ENTRY 31 WOULD REST ON IS DELIBERATELY NOT DRAFTED — neither opinion holds it, and drafting it would disguise an inference as a holding (`Q-PH-2`). Five candidate propositions staged, ALL UNVERIFIED; no cite selected; the supply stays YOURS.** **AND THE CCA HAS A SCOPE HOLDING BUT EXPRESSLY RESERVED THE REST: *Anastassov* (2022).** **V-6 still governs — art. 42.0197's finding is NOT automatic**
- **THE 19-PAIR IS UNVERIFIED ON BOTH SIDES, AND THAT WAS THE RULING, NOT AN ACCIDENT.** *Castillo* (19a) and *Able Supply* (19b) carry **the same proposition text** by design; the walk verified 19b only, and `RL-1` then reworded **both** — so **19b's 2026-08-17 verification DETACHED** (verification attaches to wording, #95). **Re-verification is yours.** **`V5-ATTRIB` stays OPEN across all six split entries**
- **THE THREE WORK-PRODUCT ENTRIES ARE VERIFIED AND STILL HAVE NO NUMBER (`REGISTRY-V` CLOSED, `Q-T19-1` OPEN).** 192.5(d), 192.5(c)(1) and 192.3(h)'s definition (notes taken during an interview are **not** a witness statement) verified 2026-08-17, **the effective-date caveat ACCEPTED rather than smoothed.** **Labelled `WP-1/2/3`, packet-local; numbering DEFERRED (`D-6`)**
- **THE WALK'S STAGING LAYER STANDS AND VERIFIES NOTHING: `task-19-signoff-worklist-2026-08-17.md` (#97).** One row per entry across all forty. **Its two arithmetic defects ARE STILL STANDING (`Q-T19-3`)** — §5 Part B says "eleven" over a twelve-row table; §6 says "five of the fourteen" and lists four. **Neither was corrected: §4.1 ordered it verbatim and a change to packet content is yours.** **One line WAS changed, and only because he directed it (E-3): the *Park Cities Bank* pinpoint → 876–77** in rows 14 and 15. **E-3 WAS SCOPED, and the four unconformed carriers of the old `876` are named:** the workbook's rows 14/15 (**expressly barred**), two dated pass records (**conforming a finding falsifies it**), and the log's #65-era line (append-only)
- **#73's `V-4` SECOND HALF IS EXECUTED.** #73 ruled **"keep as two, CROSS-REFERENCED"** and the cross-references had run **one direction only**. `RL-5` ruled both sets: entries **17 ⇄ 18** took reciprocal notes, and `legal-rule-registry-discovery-and-carrier-duties.md` took **three reciprocal dedupe notes** (197.2(a), 196.2, 198.2). **NOTES ONLY — no proposition and no Status line in that ALL-VERIFIED file was touched, and its 22 `VERIFIED` lines were counted before and after and are identical**
- **`legal-rule-registry-discovery-and-carrier-duties.md` IS THE VERIFIED FILE** *(**AUD-3, CONFIRMED FALSE and re-derived again this refresh: 22 `**Status:** VERIFIED` lines against a header reading "ALL TWENTY PROPOSITIONS VERIFIED" — a bullets-vs-entries unit mismatch. NOTHING touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)); **TRCP 190.3(b)(3) is a VERIFIED entry under a wording flag — `Q-FE5-9`.** Criminal file: **entry 3 and 4a carry NO CITE — supplying one is YOUR act.** **Six flagged WL/slip cites survive uncorrected.** **`/rules` SEED ALL UNVERIFIED**
- **THE FOURTH FILE SITS OUTSIDE THE BACKLOG: `legal-rule-registry-draft-entries-medical-billing.md`**, headed ALL ENTRIES UNVERIFIED while its own entry Status lines read **DRAFT** — a second header/entry unit mismatch, re-derived this refresh. Whether that exclusion is deliberate is **`Q-STAT-2`, yours**
- **THE SAME ABSENCE HAS APPEARED FIVE TIMES.** (1) **NOT ONE PRIVACY PROPOSITION** (`Q-WF-6`) · (2) #78's insurance narrowing · (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (`Q-QBO-6`) · (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** — thirteen terms, all zero · (5) work product — **now VERIFIED, and still only three entries.** **The registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** **FOUR research passes still stack behind `Q-STAT-5`'s one ruling.** **AND (1) STILL HAS EXACTLY ONE CALLER: gate 10 (`G10-4`, #115)** — Tex. Bus. & Com. Code ch. 521 keys breach notification on **exactly the two values the slice isolates**, named as a **LOCATOR, UNVERIFIED, with NO entry created and none drafted. The 2026-08-19 front-end packet deliberately added none either.** **AND CANDIDATE TEXTS NOW EXIST WITHOUT BEING ENTERED: `docs/specs/g10-4-ch521-entry-drafts-2026-08-19.md` — SEVEN drafted entries on §§ 521.002(a)(2)(A)/(B), 521.002(b), 521.052(a), 521.053(b)/(c)/(i), read from the OFFICIAL BULK CORPUS, ALL `UNVERIFIED`, DRAFTED AND NOT INSERTED, and no registry file touched.** **Its one finding is a FLAG: the § 521.002(a)(2)(A) definition is CONJUNCTIVE and encryption-conditioned — *“if the name AND the items are not encrypted”* — so encrypting the PII columns while `parties.display_name` stays plaintext does NOT exit the definition, and the definition governs § 521.052's protect and destruction duties as well as § 521.053's notification duties.** **Gate 10 expressly rejected encryption and this does NOT reopen that; nothing in ch. 521 read in that pass MANDATES encryption.** **Currency is NOT established from the corpus files and the entries say so.** Whether any is inserted, and where — the siblings' placement rule points at a NEW file, not a fold-in — is `G10-4` / `Q-WF-6`, **yours**
- **THE RETRIEVAL GAP IS CLOSED (#76)** — the rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20**, and all 21 are retrieved against the official corpus. **RETRIEVAL IS NOT VERIFICATION.** *(Re-confirmed the hard way at #93: a session asserted the pass had never run, wrote a document on that premise, and withdrew it — **do not re-run this pass.**)*
- **THE WORKBOOK IS STALE IN SEVEN IDENTIFIED WAYS AND CARRIES A BANNER SAYING SO (`D-3`).** Its **rows are untouched and deliberately so** — it remains **the numbering authority `V5-IDS` and `Q-T19-1` rest on**. **The staleness is disclosed, not repaired**
- **REGISTRY RESEARCH SUPPORT — FIFTEEN DOCS, ALL PROPOSED OR RECORD-ONLY, NOTHING VERIFIED; every size RE-DERIVED THIS REFRESH:** cite-check · workbook v1 (**29,844 B / 171** — banner'd AND FROZEN as the numbering record) · citator pass (**19,056 B / 222**, untouched by design) · `statute-pass-registry-retrieval-2026-08-14.md` — its **§3** carries the corrected A-for-space normalizer and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess* · deadline-engine memo · V-5/V-6/V-7 execution record · worklist (#97) · sign-off record (#98) · `registry-records-look-staging-2026-08-17.md` (**53,672 B / 650**) · `registry-verification-workbook-v2-2026-08.md` (**22,320 B / 185** — the WORKING LAYER) · `fable-adjudication-record-2026-08-18.md` (**26,759 B / 329**) · `registry-new-entry-drafts-2026-08-17.md` (**32,768 B / 274**) · `criminal-opinions-read-2026-08.md` (**32,415 B / 321**) · `costs-and-privilege-authority-read-2026-08-18.md` (**19,158 B / 200**) · `t26-signoff-and-adjudication-record-2026-08-18.md` (**32,720 B / 321**). **§3's normalizer must NOT be carried to the TRCP/TRE PDFs — raw TRCP `AA` count is 0, a different publisher**
- **`V-8` AND `V-9` ARE BOTH CLOSED — RULED 2026-08-18 (#100).** **`V-9` ADOPTED AS DRAFTED.** **CLAUDE.md rule 5 now carries a named-source fallback chain** — the court's own document; a paginated vendor copy stating on its face who delivered the opinion; or **your own identification, put to you and recorded** — with **cannot-identify-STOP as the floor** and unidentifiable entries **flagged, never staged for verification**. **`V-8` was ruled FRESH**: §0.1 gained **0.1.1 – 0.1.7**. **`V-9-UI` is recorded as PROPOSED design input only; Q-6 still bars wiring the API into the app**
- **EVERY CANDIDATE THE RECORDS LOOK PRODUCED IS RULED (`RL-1`..`RL-6`, all CLOSED), AND NOT ONE VERIFIED ANYTHING.** **`RL-3`, three cite acts:** 20b (*Collins*) took the full parenthetical, and 15 and 21a each took **date + cause number**, with **no pinpoint and no designation notation, because none was available.** **`RL-2`:** the *Alford* supersession inference is retired. **THE REPORTER-CITE FLAG SURVIVED EVERY ONE OF THESE ACTS, AND SO DID EVERY ENTRY'S UNVERIFIED STATUS — a cite act is not a verification.** **`RL-6`: the nine unnamed criminal opinions got a FULL READ (T-27), but the cite supply for entries 30 and 31 stays YOURS**
- **THE WHOLE DRAFTED BLOCK IS EXECUTED AND NOW ENTIRELY VERIFIED — SEVEN ENTRIES EXIST, THE EIGHTH DELIBERATELY DOES NOT.** Inserted UNVERIFIED at #108 — **192.3(h)'s own-statement right** (`WS-1`) · **193.4(a)** and **199.6** (`RL-4`) · **194.2(b)(9)** and **194.5** (`WS-2`) — and at #109 the **NEW 215.1(e)** entry and the **`WS-3`** entry. **All seven are VERIFIED as of #110.** **194.1(a) was NOT inserted** — it became a **second observation** on the VERIFIED "TRCP 194" entry under one-proposition-one-home; **the span flag is RESOLVED.** **The 215.1 preamble tension survives INSIDE a verified entry as verified text asserting no answer.** **`WB2-1`/`WB2-2` stand as ruled**

## Design-input memos — ALL PROPOSED, nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions. **No deadline engine is in the build queue.** **ITS HEADLINE IS A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, with 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) now allows a phase-scoped send-by Level 3 period.** **Five questions, NO durable IDs (ID-DL-1) — all five ARE filed as `[DL-memo Q1]`–`[DL-memo Q5]` and all five still ⬜.** **AN ELABORATION SPEC NOW EXISTS AND AUTHORIZES NOTHING: `docs/specs/deadline-engine-spec.md` (PROPOSED)** — it works out what `FC-7`/`FC-8`/`FC-9` and `DL-INPUT` imply, **proposes NO column, NO table and NO migration** (`FC-8`'s geography limb stays unauthorized), reuses the lien engine's `P1`–`P7` vocabulary rather than minting a parallel one, and **leaves the 50-day conflict FLAGGED with the skeleton UNEDITED.** **Its own correction is the durable bit: `P-2`'s *“only to mail”* is IN DOUBT** — TRCP 21a(c) has long read *“by mail **or by commercial delivery service**”* — **not re-retrieved, and named the highest-value verification target in the section.** Six new questions `Q-DE-1`–`Q-DE-6`, packet-local, no series minted
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions. **PROVENANCE MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED:** TRCP 21(f)(5) and Statewide Criminal E-Filing R. 2.3(a) fix filing at **transmission to the filer's EFSP** (TIER A); a Bexar county-court sentence appears to fix it at **clerk acceptance** (TIER B), and **TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule** — exposure: transmitted late on a limitations date, accepted two days later (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**) — **which FC-8's court-keyed profile answers by ruling: the key is the COURT, not the county.** **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing. All three rows stay ⬜.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9). Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument**. *(**All THREE of FE-4/FE-5/FE-6 carried the falsified `ALTER DEFAULT PRIVILEGES` claim AND a stale "probe covers 36 tables" — found by this batch's re-derivation, in NO routing row, and REPAIRED on your in-session authorization, FE-4 only after an emphasis-tolerant second sweep caught what the first missed.**)*
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** **IN-1 has NO design-doc home anywhere.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED and elaborated; its EXTRACTION half is UNRULED and only mapped.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all**, so a build implementing #64 alone ships with no VIN validation and no promotion gate, silently. **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR.** ***`Q-IN2-1` IS ANSWERED 2026-08-18, "Yes — both": the blank CR-3 form AND the versioned instruction manual, his hand***
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN. All seven rows stay ⬜. SEVEN OF SEVEN GATED, ZERO SATISFIED.** **THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **ITS ROW NAMES ALL THREE CONSUMERS (#94)** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`**. **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5).** *(Its `anon`/ADP paragraph WAS repaired this batch — it was in a routing row.)*
- **PR-3 RE-PARENTING PROPOSAL (#86) · QBO MEMO (#87) · RE-1 MEMO (#88).** QBO's **HEADLINE: THERE IS NO READ-ONLY OAUTH SCOPE** — one accounting scope grants **full read/write**, so **"read-only first" can be a property of the code but never of the grant** (`Q-QBO-2`); **the economics are INVERTED** (Intuit meters data-**OUT**, free tier 500k credits/month, **BLOCKS rather than bills**); and **the constraint with legal teeth is the one the platform helps with least** — QuickBooks records trust/operating separation as a **naming convention and enforces nothing**, so a read integration **can display a per-matter trust figure that is wrong with the authority of a computed number** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED.** RE-1's two headline findings are BOTH ruled: **`Q-RE-1` → "Both, separate triggers"**, and **`Q-RE-9`'s sourcing wall → FC-14's fourth channel.** **THE ONE THAT STILL PAYS FOR THE SECTION: TDRPC 1.04(f)(2) IS THE FIELD LIST** — consent **in writing and PRIOR to the referral**; **1.04(g) makes the failure economic and silent — an unconfirmed arrangement does not void the referral, it STRIPS THE FEE.** **`P-RE-1..12` ALL UNVERIFIED.** *(Both memos carried the falsified ADP claim — three sites between them — and **ALL THREE WERE REPAIRED at `20d1a0f`** on your in-session authorization, each taking a dated conformance note that quotes what it used to say. **This line read "NOT repaired" from `20d1a0f` onward and was FALSE there: that commit edited two other lines of this file and left this one.** Corrected 2026-08-19 by the task C1 re-sweep, which found it while reading for something else.)*
- **COMMUNICATIONS-LOG INGEST MEMO (#89)** — Task 15. **THE DISPATCH'S OWN WORD WAS CONTRADICTED IN TERMS:** it called two constraints **ruled**; the log records the exchange as *"Claude recommendations, unruled."* Both trace to one paragraph of `future-modules-capture-2026-07-28.md` §3 — **the same gate as `Q-QBO-1`, firing a second time** (`Q-COM-1`). **THE TWO RESOLVE DIFFERENTLY:** the scope exclusion is honored and **costs nothing**; the architectural commitment is **priced for the first time** — a second public HTTPS endpoint, private-key custody, an outside vendor's discretionary approval, and an unestablished metering position, **for a channel the firm does not use.** **A NEW GATE CLASS (`Q-COM-5`): a third party's DISCRETIONARY APPROVAL.** **THE SMS HEADLINE LANDS ON THE STACK ALREADY COMMITTED:** Teams SMS needs three licences plus A2P Brand *and* Campaign approval, is **1:1 with no MMS** — so it **cannot receive the scene photo, the insurance card, or the bill** — and **is apparently not exposed to Graph at all** (`COM-LOOK-3`). **THE IDEA WORTH ITS OWN LINE: a communications LOG and a communications ARCHIVE are different products, and only the archive needs content** — `callRecords` gives who/when/how-long with **no audio and no transcript, so no privilege, consent or PHI problem attaches, because nothing was captured.** **AND THE ONLY MICROSOFT-NATIVE PATH TO CALL CONTENT INVERTS RULING 8.3** (`Q-COM-8`). **`Q-COM-10` and `Q-COM-12` REMAIN YOURS**
- **WITNESS-STATEMENT CONVERSION MEMO (#99) + THE `WS-3` AUTHORITY READ (#102, T-28, CORRECTED #103).** The memo's headline stands: **on TRCP 192.3(h)(2) the line is crossed at RECORDING, not at transcription** — the recording is already a witness statement — **so a gate placed only at transcription sits downstream of the event it exists to control**; a witness statement is a Rule 194.2(b)(9) INITIAL disclosure owed WITHOUT a request (194.1(a)), against which 194.5 permits **no work-product assertion**; *Trahan v. Lone Star Title*, 247 S.W.3d 269, is the same failure sanctioned. **THE ATTORNEY-CLIENT LIMB IS DECIDED BY TWO PUBLISHED INTERMEDIATE OPINIONS, majority authorship confirmed on the face of each (V-9)**: *In re Fontenot*, 13 S.W.3d 111 (Livingston, J.) and *In re ExxonMobil*, 97 S.W.3d 353 (Seymore, J.) — a witness statement inside a confidential attorney-client communication **is privileged**, and ordering its production is an abuse of discretion. **BOTH REACH IT THROUGH COMMENT 9** (`Q-WS3-2`). ***CORRECTED #103: the "adjuster or third-party witness is never privileged" paragraph is WITHDRAWN.*** **Every proposition UNVERIFIED; no entry created**
- **T3 PILOT PROTOCOL (#90) · SESSION-LOG TOC (#91) · GO-LIVE RUNBOOK (#92).** The **TOC** (`docs/specs/session-log-toc.md`, **REGENERATED THIS BATCH — 150,639 B / 419 non-blank, re-derived**) carries **the finding that matters to every reader of the record: the log runs TWO independent numbering systems and neither covers every entry** — `#nn` and **runner ordinal** — with **eight runner batches carrying a `#nn` instead of an ordinal** and **one carrying neither**; a reader assuming one system mis-files about a quarter of the log. **NO ENTRY ANYWHERE CARRIES `#1`** (`TOC-1`). *(**TWENTY-NINTH EDITION — a genuine `TOC-4` FIRING, the TWENTY-FOURTH.** Rows **129 → 131**, this batch's two. **THE POINTER-STANZA CORRECTION TERM WAS APPLIED BEFORE THE EDIT AND AFTER FOR A SECOND CONSECUTIVE EDITION:** 130 headings − 1 = 129 entries against 129 rows going in, **132 − 1 = 131 against 131** coming out, so nothing had landed unindexed and nothing did. Buckets re-derived on the POSITION-ANCHORED matcher: **61 numbered (`#65`–`#125`, gapless) + 56 runner ordinals (23–78, gapless) + 0 unnumbered runners + 14 other = 131, overlap measured at ZERO.** All 129 carried rows are byte-exact and not one cell was edited — proved by extracting the row set from `git show HEAD:` and comparing it to the new file's tail. **AND TWO STALE RANGES IN THE INDEX'S OWN *How to read the ID column* SECTION WERE CORRECTED RATHER THAN CARRIED:** it still published `#65`–`#123` and ordinals 23–76 while its own Coverage section two screens below said `#65`–`#124` and 23–77 — that file's most-repeated finding turned on its own header section. **`TOC-5` still accrues without a ruling**, and the archive's frozen index is still a second copy of 141 rows in the repo — but **`Q-CAP-3`, the question of whether THAT path syncs, is CLOSED** (see the capacity line below), and the footer sentence that still called it *not yet done* was corrected in the same pass.)*
- **PRIVILEGE-TIER UNIFIED VOCABULARY (#102, T-30, CORRECTED #103) · DESIGNATION-SOURCE METHOD RESEARCH (#102, T-31, CORRECTED #103).** **T-30's headline SURVIVED THE AUDIT BUT ITS ARGUMENT DID NOT:** the overlap it relied on is a **GENERAL cardinality property**, **so the argument defeats the three-value column already in the schema, and the PRIOR question — should `privilege_tier` be exhaustive at all? — had never been ruled** (`Q-COM-10-A`, since RULED at #105). **The 192.5(d) over-read is corrected** — the rule's *"For purposes of these rules"* qualifier was dropped (`Q-COM-10-B`). **The migration remains AUTHORED, UNRUN AND NOT AUTHORIZED.** T-31 stands on its structural finding — **Texas has TWO independent designation axes** (TRAP 47.2(a) Opinion vs Memorandum Opinion, memorandum being the **default** under 47.4; and 47.2(b) publish/do-not-publish, **criminal only**) — **but its two headline claims are RETRACTED:** the *"174,292 to 1"* measurement **had no control arm**; and **`"full precedential"` appears 0 times in the TRAP PDF** (`Q-DES-1`). **AND TRAP 77, NOT 47, GOVERNS THE CCA — 77.3 BARS citing unpublished opinions as authority** (`Q-DES-6`)
- **CRIMINAL OPINIONS READ (#102, T-27, CORRECTED #103) — AND THE CORRECTION RAN AGAINST THE CLIENT.** The read stands: **eight of the nine never mention art. 102.073**, only *Middleton* touches it and only as the State's argument expressly not decided. **BUT *LaPorte* DOES NOT SPLIT BY PAGE.** Both halves are on **415**, in adjacent paragraphs — so **the instruction "Never cite 415" is WITHDRAWN**, having barred counsel from the very sentence stating the rule. **The corrected remedy is a parenthetical — *"overruled on other grounds by Ex parte Carter"*** (`Q-RL6-3`). Two more: ***Carter*'s reaffirmation is a FOUR-vote plurality and dictum** against **five** votes for the overruling; and **"may be raised at any time" was NARROWED, not overruled**. **And FOUR, not five, are "DO NOT PUBLISH"** (`Q-RL6-5`). **Four of the nine remain author-unidentifiable under V-9 and are flagged, never staged**
- **THE ADJUDICATION AND CORRECTION SERIES: QR-6 (#94) · WORDING (#95) · V-EXEC (#96) · TASK 19 WORKLIST (#97) · SIGN-OFF WALK (#98) · TOC REGENERATION (C-2, unnumbered) · CHAT-DISPATCH v3 (#99) · FABLE (#100) · DT-1 CORRECTION (#101) · CHAT-DISPATCH v4 CHAIN (#102) · AUDIT CORRECTIONS (#103) · AUTHORITY READ (#104) · SPEND-DOWN (#105) · FC BLOCK (#106).** **#98 is the first session to move a registry Status line to VERIFIED**, and it moved twenty-four; **C-2 is the first Code session to decline a `#nn`**, which `TOC-6` has now made the rule; **#100 is the first to move a Status line the OTHER WAY**; **#101 is the first correction entry whose whole subject is a DATE**; **#103 is the first produced by an adversarial audit of a prior session's own output**; **#106 is the first whose ruling had to be folded into a spec a build slice will read before that slice runs.** **`docs/prompts/CHAT-DISPATCH-v3.md` and `-v4.md` were NOT edited at #100–#106; amending a repo-tracked prompt is yours**

## For design side
- **THE THIRD CAPACITY EVENT ON RECORD — MEASURED 2026-08-20 (`#120`), ITS FIRST RULING EXECUTED, AND ITS HEADLINE PROJECTION NOW CORRECTED BY `#124`.** `docs/specs/knowledge-capacity-measurement-2026-08-20.md` (**PROPOSED**, **11,682 B / 139 non-blank, re-derived**): **`knowledge_size` 1,795,567 of 2,000,000 — 89.8%** at that measurement. **`Q-CAP-1` IS RULED AND EXECUTED (`#123`): the log SPLIT at the 2026-08-13 cutoff, positional so nothing reordered.** `docs/specs/session-log.md` is **1,012,084 B / 6,773 non-blank** (re-derived; it was 949,027 B after the split and has taken five entries since), against the closed archive at **`docs/archive/session-log-archive-2026-07-21_2026-08-12.md` (578,892 B / 5,128 non-blank, 141 entries, 2026-07-21–2026-08-12, with its own frozen 141-row index)**. **THE ARCHIVE IS CLOSED AT BIRTH: no entry is ever added to it, and corrections to an archived entry go in the LIVE log naming what they correct.** **`Q-CAP-2`'s CLICK IS MADE — AND `#124` MEASURED THAT IT DID NOT PAY, WHICH IS THE MOST USEFUL CAPACITY FINDING ON RECORD:** three consecutive post-exclusion syncs read **1,837,143 → 1,846,727 → 1,856,462 (91.9% → 92.3% → 92.8%) — monotonically UP**, each rise about the day's own additions. **The promised ~78K-token return never appeared because the figure was BYTE-derived, and a scanned PDF's knowledge cost is its EXTRACTED TEXT, which for the CR3 sheet is evidently ≈ nothing.** The exclusion was still right as working-set hygiene; **its token payoff was misestimated, `#123` stands as written, and the capacity doc's `/docs/reference/` lever row is now KNOWN-OVERSTATED and deliberately left alone** — it is swept at `Q-CAP-4`. **`Q-CAP-3` IS NOW CLOSED, AND BY MEASUREMENT RATHER THAN BY REPORT (`#125`, 2026-08-20).** Michael's hand added the `/docs/archive/` exclusion and re-synced; the project's filters now carry `/docs/archive/": "exclude"` and **`knowledge_size` fell 1,856,462 → 1,701,643 — 92.8% → 85.1%**, about 155K tokens freed. The day's full series: **91.9 → 92.3 → 92.8 → 85.1.** **ALL THREE `Q-CAP` ACTS ARE NOW RULED, EXECUTED AND VERIFIED.** **AND THE CALIBRATION DATUM IS THE DURABLE PART: the archive lever's BYTE-DERIVED estimate (~167K) ran ~7% HIGH against the measured ~155K** — the same instrument whose `/docs/reference/` estimate ran catastrophically high at `#124`, **so its bias is now on the record twice with signs both ways named**, which is worth more than either figure. The `/docs/reference/` lever row in the capacity doc stays KNOWN-OVERSTATED and deliberately untouched; it is swept at `Q-CAP-4`.** **NO SYNC OR PICKER CLAIM IS MADE HERE: a Code session structurally cannot read or change that setting.** Still yours: **`Q-CAP-4`** · **`Q-CAP-5`** (a standing policy — the split buys weeks, not a solution). *(Carried: the SECOND event, #107, retired 44 captures, meter 81% → 73.3%, proving the meter counts TOKENS not bytes; the first was 2026-07-26. `docs/project-knowledge-working-set-policy.md` carries both amendments.)*
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built. *(Live tension, a queue item: **`Q-PR3-1`** asks whether a design-side `src/` read through the device bridge is sanctioned at all — the working-set policy says design sessions do not read source, that sentence predates the bridge, and **every finding in the #86 proposal is undrivable without such a read**. **The 2026-08-19 front-end packet honored it: it read no `src/`, quoting the BUILD session's `G10-3` findings from the record instead.**)*
- **INSTRUCTIONS: `v22` IS IN FORCE — OBSERVED 2026-08-17 (#102) IN THE RUNNING SESSION'S OWN LIVE INSTRUCTIONS FIELD**, the #82/A-1 mechanism for the **EIGHTH consecutive time.** v22 carries **two trigger-#3 firings from one session** (`V-9` and the `TOC-4` runner amendment). **v23 IS ASSEMBLED — REPORTED BY #106 AND CARRIED ON THE PACKET'S WORD, WHICH IS THE ONLY EVIDENCE A CODE SESSION CAN HAVE.** It folds FOUR same-day trigger-#3 firings (V-9, TOC-4, **PF-1**, **the FC-14 SOURCING fourth channel**) into ONE document. **THE HAND ITEM IS ONE ACT: PASTE.** *(Your 2026-08-18 correction — no v23 document existed, and a Code session structurally cannot assemble one — was TRUE WHEN MADE and is not withdrawn. **The assembled text is NOT in the repo and never will be.**)* DT-1 remains in force
- **DT-1 WAS READ AND THE DIVERGENT WINDOW DID NOT APPLY, FOR A FOURTH CONSECUTIVE BATCH — and reporting that is the whole point of measuring rather than carrying.** PowerShell, the authority, read **`2026-08-20 17:11 −05:00`** (`Central Standard Time`) at this close-out against a UTC **`2026-08-20 22:11`** — **the SAME calendar day.** This batch ran late afternoon, OUTSIDE the ~19:00-to-midnight window where the two dates split, **so a batch that had ASSUMED the last batch's finding would have published a true date on a false basis** — which is the class, not the date. Every date written this batch is the Central **2026-08-20**, which is also what the packet's §0 stamped. **Every batch run between ~19:00 Central and midnight reads a UTC date one day ahead.** **Any execution point DT-1 gains must read the wall clock, not a shell** — and “the Code session is local, therefore safe” stays false
- **SOURCING IS A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED), AND IT GAINS A FOURTH NAMED CHANNEL BY RULING (FC-14, 2026-08-18).** The three were the official bulk statutes corpus, the clean-authority TRCP/TRE/TRAP PDFs, and the eCFR API. **The fourth: State Bar / court-published Texas conduct-and-administration rules as clean-authority PDFs in `Documents\Knowledge Repo\`, cited by each PDF's own effective date.** Michael acquired six by hand mid-session. **`Q-RE-9` — the convention's first proven gap — CLOSES with it.** **SOURCING itself is NOT amended in any repo file: the convention lives in the project instructions and the amendment rides the v23 paste.** **Q-STAT-2 through Q-STAT-6 stay OPEN**
- **CHAT-DISPATCH v4 IS COMPLETE AND EVERY CHEAP MOVE IT LEFT IS SPENT.** `docs/prompts/CHAT-DISPATCH-v4.md` (79 non-blank / 94 raw). **`RR-1` ran at #102 and caught two cross-task interactions — and caught NONE of the twenty-two defects the audit found, because RR-1 checks documents against later RULINGS, not against PRIMARY SOURCES. THAT GAP IS CLOSED BY RULE AND HAS BEEN EXERCISED FOUR TIMES:** `PF-1` on the #106 packet (twelve defects, 0 HIGH), the #108 packet (twenty-one, two HIGH — two INCOMPLETE divergence enumerations whose re-put CHANGED THE OUTCOME on entries 12 and 32), the #109 packet (twenty-two, one HIGH each), and the #110 packet (**fourteen, none HIGH — and its one authorization finding ran the OTHER way: the shipped Opus prompt was granting a permission beyond the #108 terms and it was struck**). **CODE-DISPATCH v3 IS COMPLETE**
- **HK-7 STAYS OPEN, AND ITS ANNOTATION GAP IS RECORDED IN THE QUEUE ITSELF — RULED 2026-08-18.** The ruled remedy was a **FORWARD gap-note, not a reconstruction:** the row carries that annotations for **#94–#98 were recorded only in BUILD-STATE** and that **this note records the gap, not reconstructed annotations. Nothing was back-written** (K-6/K-7). **RE-CHECKED NATIVELY THIS BATCH by existence test on DECLARED paths only (HK-5 — nothing enumerated, nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL still ABSENT here.** **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**. **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. *(#86 read `roster.ts` and found the `sideSetFor()` defect there — evidence the gap was load-bearing.)* **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SIX REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*`. First three SPENT as CD-1 input; UIM + deficiency seeded the **DE series**. **No capture in the repo is unreconciled.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2
- **YOUR HAND, THE LAUNCH PATH IN ORDER — **AND THE NON-GATE ITEM THAT OUTRANKED PART OF IT ON COST IS DONE: `/docs/archive/` IS IN THE SYNC-PICKER EXCLUSIONS AND IT PAID — `Q-CAP-3` CLOSED BY MEASUREMENT, 92.8% → 85.1%.** **WHAT NOW SITS AT THE TOP OF YOUR LIST IN ITS PLACE IS A SECURITY ITEM, NOT A CAPACITY ONE: TURN “Allow new users to sign up” OFF** (Supabase dashboard, your hand, no repo act) — **measured ON 2026-08-20**, which with `using (true)` policies leaves the authenticatable set open; your own account is unaffected and staff onboarding is by admin invite, which ignores the toggle.** ~~run the CD-1 migration~~ · ~~gate 1 Supabase Pro~~ · ~~paste gate 10's migration~~ **ALL RUN AND VERIFIED 2026-08-19.** What remains on the floor: **gate 9 production SMTP — ~~`D-SMTP-1` (provider)~~ ~~`D-SMTP-2` (sender identity)~~ **BOTH RULED 2026-08-20 and off your list: Postmark, `signin@` on a send subdomain.** ~~provider signup~~ ~~DNS records~~ ~~Supabase dashboard~~ ~~the runbook's §4 ROUND TRIP ONE~~ **ALL DONE BY YOUR HAND 2026-08-20, AND THE DNS CLOCK IS SPENT — the records verified AUTHORITATIVE in the same sitting rather than after 24–48h.** **MOST OF §4 IS NOW EVIDENCED AND WHAT REMAINS IS SMALL AND NONE OF IT IS A RULING (`#125`): §4.2 headers PASS on the receiver's own verdict · §4.4 junk half PASS · §4.4 outside half ARRIVAL-PROVEN with the provider-and-folder statement still OWED FROM YOU · §4.5 later-day repeats, tomorrow and once more from a fresh browser profile · optional DMARC, still not during the go-live window** (`docs/smtp-setup.md`, **10,907 B / 142 non-blank, re-derived**). **The gate is still OPEN: neither round trip one nor the header read is the gate, and closure is the re-check's act**** → ~~gate 3 RLS write-path test~~ **RUN 2026-08-20 AND CLEAN — OFF YOUR LIST AS AN ACT, ON IT AS A RULING.** A fresh Opus Code session drove `docs/prompts/PROMPT-gate3-write-path-test.md` against the LIVE project with you in the sitting, took the access token from your signed-in browser exactly as its Step 0 anticipated, and returned **37/37 signed-out privilege refusals, 36/36 signed-in allows, the deny control refusing on the PRIVILEGE message, and teardown verified to zero rows.** **WHAT IS LEFT IS YOURS AND IT IS A RULING, NOT A RUN: `Q-G3-4` — does a clean run CLOSE gate 3, or close `GL-1` item (4) with gate 3 open on its wider text?** Read it against the run report's six named limits, the live one being that **UPDATE and DELETE were never exercised against `using (true)`** because you ruled *"run as written."* *(Its Step 0.3 called `.env`'s URL "the address of record" when that value was EMPTY at authoring — recorded rather than repaired, and the premise was true at run time.)* → **gates re-check + the trigger-1 instructions revision.** **AND THE DECISIONS THAT ARE NOT GATES: ~~(a) `G10-5`~~ — RULED IN AND BUILT 2026-08-19**, so the app no longer writes SSN or licence numbers into `parties.fields` and no party read carries the blob wholesale; **(b) `O-11` — the `anon`/`service_role` residual privileges**, the four read-only diagnostics first (`anon`'s schema USAGE is cheapest and decisive), then whether to revoke and whether that becomes a standing migration step; **(c) `O-12` — enforce or inherit "unreachable by default," DEFERRED BY YOUR RULING pending those diagnostics; (d) `GL1-1` — whether `Go_Live_Gates.md` gate 1's backup rationale takes a correcting append; the CANDIDATE TEXT now exists, drafted and NOT appended, at `docs/specs/gl1-1-gate1-append-draft-2026-08-19.md`, so your ruling is ONE act — adopt / reject / edit, with THREE sub-options offered on the un-re-derived FOURTH-and-SEVENTH figures it quotes; and ~~(e) `G10-6`~~ — **RULED IN 2026-08-19 and off your list**: the slice's §7 item 6 now covers the SECOND false `RlsProbePanel` sentence as well, and the kickoff prompt's Step 5 branch is resolved to *repair both*.** *(**`Q-PR3-3` is ANNOTATED, NOT CLOSED**.)*
- **STILL YOURS AND ALL SMALL:** **the OPUS-RUN paste HAS RUN and is off your list** (`#112`) — what it returned is twelve questions, all yours · **`Q-COM-10`**, which closes the moment you adopt the unified `privilege_tier` list — **`Q-COM-10-A` IS RULED AND CLOSED (#105), so `B`–`F` are unblocked** · **the cite supply for entries 30 and 31** (`Q-RL6-1`, `Q-RL6-2`) — `Q-RL6-1` resolves to ***Hurlburt*** and ***Pharr***, **whose read is AUTHORIZED to an Opus session and gated only on your two PDF pulls**; **its *Whaley* option is dead on the DO-NOT-PUBLISH finding**, and **entry 30 still got nothing** · **`V5-ATTRIB`**, which `RL-1` deliberately did not resolve · **`V5-IDS`** — derived IDs were executed on the #66 precedent, **but that precedent supports the NAMING and not the FILE SHAPE**
- **ALSO YOURS, LONGER-CARRIED: PASTE v23** — one act, the assembly done · ~~the *Pharr* + *Hurlburt* PDF pulls, then the authorized Opus read~~ **BOTH DONE 2026-08-19 — what returned is a staged read and `Q-PH-1`–`Q-PH-5`, not a cite** · **the blank CR-3 form + versioned instruction manual** (`Q-IN2-1`) · **`FC-12`** (no corpus content imports meanwhile) and **`FC-13`'s open half** · **re-upload SKILL.md v2** (SK-v2) · **the sync-picker clicks** (Q-3, `WS-P1`/`WS-P2`) — **then a FRESH chat to verify the picker state** · **telemetry lockdown on the P1** · **HK-6** · **HK-7 re-grant each session** · **V-2, Ochoa first** · **OBS-1** · **CD-4 – CD-13** · **Q-STAT-2 – Q-STAT-6** · **the deadline memo's five + ID-DL-1** · **the Task 8 specs' 22 + 3 looks — Q-FE5-9 first, it sits inside a VERIFIED entry** · **the Task 9 specs' 18 — Q-IN1-8 is a one-word veto on the whole IN series** · **the Task 10 spec's nine — Q-IN2-2 is cheapest** · **the Task 11 spec's ten + `WF-LOOK-1`** · **the Task 12 proposal's seven + `PR3-LOOK-1`** · **the Task 13 memo's eight + four looks** · **the Task 14 memo's nine + three looks** · **the Task 15 memo's remaining eleven + four looks** · **the Task 16 protocol's seven — `Q-T3P-1` is the cheapest question in the whole queue** · **the Task 17 index's five, now three** · **`GLR-2` and `GLR-3`** · **`OPEN-5(b)` and the `AUD-5..9` one-word apply** · **`READ-A`** · **the workbook's carried `Q5`** · **the CHAT-DISPATCH v4 chain at TWENTY-FIVE OPEN + SEVEN CLOSED of its 32 rows** · **delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`, reported STILL PRESENT by the #112 design session; **NOT re-checked here — HK-5 forbids this runner sweeping your filesystem to confirm it****
- **TWO PRACTICE ITEMS FROM #106, REPORTED TO YOU IN-SESSION AND TOUCHING NO REPO FILE.** **(1) `Payout Template - Normal.xlsx` carries a LIVE SETTLEMENT in the template body** — the §8 type-over pattern, live in your current library; the PD-Only template is properly tokenized. **(2) The A-4 $150 vs $175 expense-fee conflict is CONFIRMED AT SOURCE.** Both are your files, not the app's
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. NOTHING BUILT — and the T-24 SPEC DRAFT (#99) DOES NOT PUT IT IN THE QUEUE.** **THE DISPATCH'S TAXONOMY LINE WAS ONE CATEGORY SHORT — the ruled list is SEVEN (BP/STO/OO/EV/CONTRA/PRIV/RFP-form).** **CONTRA HAS NO VERIFIED AUTHORITY AT ALL** — it rests on entries 2 and 12, and the conference fuse rests on entry 13's wording. **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` is recorded as sitting untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand.** *(**Re-checked rather than inherited this refresh: this batch's Step 1 inventory of `inbox/` on THIS machine returned exactly ONE file — its own push-to-code packet zip — and no `Probate Corpus.zip`. Whether this machine is the P15 is not determinable from the repo.**)* **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-6 + MM-1 + OPEN-5(a) + TOC-4 ALL CLOSED; runner v11** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy**; the `/queue-runner` command is a pointer to it — **re-read at HEAD again this batch rather than from any cached copy — v11, blob `c75de00d`, 14,038 B / 190 non-blank, identical**). **Your hand: the P15 user-level copy deletion.** They stand as ruled: (a) a stated verification **names the command that produced it** — exercised hard this batch, where `git merge-base --is-ancestor 3020dfd origin/master` plus the fact that `3020dfd` IS batch 77's own commit is what proved its push landed, and an `ls` **on batch 77's explicit zip filename** — returning *No such file or directory* — is what proved its deletion ran, neither being assertable by that batch's own entry and neither inferable from a bare count; (b) the queue merge is **TWO acts** — **NEITHER TAKEN THIS BATCH, AND CORRECTLY SO**, which is the thing that looks like a miss and is not: the packet's §6 bars rows and the queue's own Status header already records the **2026-08-19 scope ruling** keeping gate 9's §4 remainder gates-side, **and the precedent was VERIFIED rather than assumed — the header's reconcile chain runs `…#120, #121, #124`, so `#122` and `#123` carry no sentence either**, batches 75 and 76 having stood in this same posture; (c) **pin identity at Step 1, delete BY EXPLICIT FILENAME** — **exercised on the single packet, `35fff0ea…`, and RE-PINNED BYTE-FOR-BYTE UNCHANGED after the Step 1 STOP**, which is the check that clause exists for; (d) format comes from the runner's own Step 4 rules, **never copied from an entry authored under an earlier version**; (e) **a packet-added act in no row and no Step 4 item needs your in-session authorization**; (f) **a docs-only batch records the health-check skip explicitly — it DID apply this batch and the trio was SKIPPED, not passed off as a pass**; **and `TOC-4`: the index regenerates every runner batch — its TWENTY-THIRD firing here.** **THE PACKET DID NOT AMEND THE RUNNER AND SAID SO IN TERMS** — `QUEUE-RUNNER.md` is byte-identical at HEAD. **QR-3 STILL CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74, #93): the mount reads a false DIRTY of CRLF-only files, and `git fetch`/`ls-remote` fail there with `HTTP 403 from proxy` — **and `LE-1` now carries the cheapest decisive evidence on that, appended this batch: through the bridge, `git -c core.autocrlf=true status` reports the tree CLEAN**
- **`QR-6(e)` DID NOT FIRE THIS BATCH EITHER — SIX FIRINGS IN TWENTY-ONE BATCHES, and the absence is stated rather than assumed, because this batch had to reason about a near-miss to reach it.** #94: a stranded `.git/index.lock`. #95: **a confirmation that did not reach the unrouted act**. #96: three at once. #97: **two arithmetic errors inside the packet's OWN CONTENT.** #98: **a ruling being CLOSED whose second half was never executed.** **#100: the stranded lock again.** **#99, #102–#106, the seventy-first through this batch: nothing.** **AND THIS BATCH DID NOT EVEN CARRY A NEAR-MISS, WHICH IS WORTH SAYING BECAUSE THE LAST ONE DID:** batch 77's packet replaced the runner's Step 4 item 6 close-line with one carrying an extra warning — a change to a MESSAGE inside an existing item, honored rather than escalated. **This packet's §8 restates the runner's own obligations and asks for nothing outside a routing row or a Step 4 item; its close line is the runner's own, unmodified.** **The `O-10` collision stays the exhibit for minted IDs; the seventy-second for minted SCOPE; the seventy-third for WHICH ITEMS EARN A ROW AT ALL — and that split governed this batch directly: **ALL SIX of the packet's §7 items took NO rows**, and this batch VERIFIED each one's filed permanent home by OPENING THE FILE rather than trusting it — `docs/smtp-setup.md` §4.4/§4.5 and its §2 DMARC lines, `Go_Live_Gates.md` floor item (5), and **`#125` itself for the two that had no prior home anywhere (the signups ruling and the 16:47 link's click-or-lapse), which is precisely why filing it mattered more than a row would have**, which is the only thing that makes “no new rows” compliance instead of loss**
- **RUNNER-FOUND AT #104 AND NOT EDITED INTO THE DOCUMENT — TWO DEFECTS, ONE OF THEM HIGH.** **(1) *WHALEY*'S PUBLICATION NOTATION IS IN THE COPY, AND IT SAYS “DO NOT PUBLISH.”** The document asserts it *“could not be found anywhere in the copy I read”* and builds `Q-AUTH-2` on that premise. It is at the foot of the FLP-served opinion — so **under TRAP 47.7(a) *Whaley* has NO precedential value**, `Q-RL6-1`'s option (d) is dead, and **FLP reporting `status: “Published”` for this very opinion is a live proof that the FLP field is not evidence of a Texas designation.** **(2) A V-9 BREACH ON *ANASTASSOV*:** it is characterised at length while authorship is recorded as unchecked; **identification was one query away from a V-9-permitted source** — the court's own document reads *“SLAUGHTER, J., delivered the opinion for a unanimous Court”* — so the holding is **unanimous and stronger than claimed**, but the method failed. **The queue rows carry these; the document and #104 stand verbatim (`Q-T19-3`)**
- **THE MULTI-AGENT READ-ONLY PREFLIGHT IS STANDING LAW — `Q-AUDIT-1` RULED 2026-08-18 (#105), `PF-1`.** **Trigger: any packet carrying a legal characterization or a proposed registry entry. Run by the DESIGN SESSION before shipping, pre-`inbox/`.** **It earned the rule four times out of four and has paid a seventh:** #96, a staged block **reworded a citation flag inside an act ruled verbatim**; #97, **two arithmetic errors**; #103, **twenty-two defects across five documents**; #104, ***Whaley*'s DO-NOT-PUBLISH notation**; #106, twelve including its own §8 draft stating the PF-1 result BEFORE the run; #109, twenty-two; **#110, fourteen and none HIGH — but its most interesting finding ran TOWARD more permission than was ruled.** **OPERATIONAL WARNING THAT SURVIVES: at #104 the agent fleet DIED — seven agents, twice, all on `API Error: 529 Overloaded`, zero tokens — and the harness returned `{"surviving_defects":[]}`, a TOTAL FAILURE WEARING THE COSTUME OF A CLEAN BILL OF HEALTH.** **PF-1 fires on the SENDING side, so no queue-runner batch can satisfy it**
- **THE CLASSIFIER CAN REFUSE AN ALLOWLISTED BARE COMMAND.** The v20 note (#94) recorded the forty-seventh invocation's **bare** allowlisted `git push origin master` refused on **both** shells. **The SIXTIETH invocation then stranded exactly as predicted: it committed and never pushed, and 613 insertions across six files sat invisible to the design side until the SIXTY-FIRST batch's Step 0 ahead-stop caught it.** **The QR-3 ahead-stop is the proven catch and it is the ONLY one — a runner that skipped it would have built a second invisible layer on the first. Never conclude a batch landed without `git ls-remote`**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered.** **`FC`, `D-SMTP` and `GL1` are the newest series and only `GL1` needed a flag**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** — **which `Q-IN2-1`'s "Yes — both" now answers, once the documents land.** Synthesis `cr3-field-code-map.md`, **PROPOSED input — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: the #62/#63 and #64 batches were VERIFIED design-side FULL-TEXT on 08-13 (#65)**; **#67 verified landed at the top of #68**; **`#117` (2026-08-19) CLEARED A BLOCK — ITEMISED, NOT BLANKET**; **`#124` (2026-08-20) CLEARED THE GATE-3 RUN'S FOUR HANDED-BACK ITEMS, ALSO ITEMISED**; **AND `#125` (2026-08-20) SPOT-VERIFIED BATCH 77 — PLACEMENT AND COUNTS ONLY, EXPRESSLY NOT A FULL-TEXT RE-READ**, which is a weaker clearance than `#124`'s and is recorded as the weaker thing it is: `#124` present once under the runner-77 line, the `LE-1` append present once with open rows unmoved at 350, TOC census 129/129, BUILD-STATE at exactly 150 non-blank, `inbox/` empty, no stranded lock. **STILL UNREVIEWED, RECOMPUTED FROM THE LOG AT HEAD THIS REFRESH (A-4, ruled #82; now runner text — NEVER carried from a packet):** the carried **#31–#33** material, **#37–#61's own routing**, **#75 through #125** — **the range grew by one this batch, to 51 numbered entries on the inherited basis** — the runner lines interleaved among them **through the seventy-eighth, this batch's own line included**, the unnumbered C-2 Code-session entry, **and — named because `#117` expressly declines them — task C3's substance, every live-database reading, C1's classification table, the health figures, anything about origin, and the seventy-first packet's twelve §1 pins, which are PERMANENTLY uncheckable because the packet was deleted.** Do not copy #36's clearance forward. **AND THE A-4 RECOMPUTE SPANS TWO FILES: everything below `#65` is in the ARCHIVE** — **archived is not reviewed, and the split cleared nothing.** *(**Honest limit, unchanged: the `#75` floor rests on no explicit clearance** — #65, #66 and #68–#74 were DROPPED out of the round-trip lines rather than cleared. **Re-derived this refresh: inherited basis #75–#125 = 51; STRICT positive-clearance basis #65–#125 less #67 = 60. The nine-entry gap is unchanged and is exactly the undetermined set, and `#117` left it to you.**)*
- **EVERYTHING AWAITING YOUR *RULING* IS IN `docs/specs/attorney-review-queue.md` — AND THAT IS NOT THE SAME AS EVERYTHING AWAITING YOUR *ACTION*, WHICH THE FILE SAYS IN ITS OWN WORDS (RULED #114).** The GO-LIVE GATES deliberately do NOT live there: Gates 1–10, `GL-1`'s floor items and `T#1` live in `Go_Live_Gates.md` (**16,012 B / 148 non-blank, re-derived**), their STATUS lives HERE, and they await your ACTION rather than your ruling. **Reconciled through `#124` — READ FROM THAT FILE'S OWN STATUS HEADER AT HEAD (A-5) AND STATED AS A READ, NOT AS THIS BATCH'S ACT — AND IT DID NOT MOVE THIS BATCH, DELIBERATELY.** **THE SEVENTY-EIGHTH BATCH TOUCHED THE QUEUE NOT AT ALL: no row added, no row closed, and NO Status-header sentence written** — `QR-6(b)`'s conditional half correctly DECLINED on two independent authorities, the packet's §6 and that header's own **2026-08-19 scope ruling** keeping gate 9's §4 remainder gates-side. **The precedent was verified, not assumed: the reconcile chain runs `…#120, #121, #124`, so `#122` and `#123` carry no sentence either** — batches 75 and 76 in this same posture. **So the pointer reading `#124` while the log runs to `#125` is ACCURATE-AND-DELIBERATE, not drift; anything else would be a lie about an act that did not happen.** *(#92 and #93 are still absent from that header's reconcile chain — a known, reported gap.)* **Row-anchored open rows re-derived at HEAD and UNMOVED at 350** on the file's own matcher; **the plausible wrong matchers still disagree — line-anchored 334, raw glyph 389**, both unmoved, as they must be on a batch that wrote nothing there. **THE OPEN-5(a) SWEEP RAN AGAIN AND EVERY FIGURE IT REACHED IS UNMOVED EXCEPT THE RECORD'S OWN.** Re-derived and UNMOVED: backlog **47 / 35 verified / 12 not** on the two-named-file basis (`discovery-enforcement-and-pleading` 30/10 against 40 `**Status:**` lines; `criminal-plea-and-costs` 5/2 against 7); carrier-duties **22 VERIFIED**; `db/schema.sql` **37 tables, 37 RLS-enabled, 36 policies**; `SCHEMA_TABLES` **37 entries**, `file_counters` its sole `policy:false`, **and the WRITE-probe set re-derived FROM EXECUTABLE LINES at exactly the 5 named above — a comment-stripping pass, and it earned its keep again: the naive sweep for `write: true` returns ZERO because the probe encodes the set as `table:` rows inside `probeRows()`, so a figure taken from the obvious pattern would have been wrong in the safe-looking direction**; **the log preamble 2,771 bytes, byte-identical across the prepend**; runner **v11, blob `c75de00d`, 14,038 B / 190 non-blank**; the runbook **81 / 106**; the T3 protocol **534 / 688**; CHAT-DISPATCH v4 **79 / 94**; the capacity measurement **11,682 B / 139**. **ONE CARRIED FIGURE STILL RESISTS RE-DERIVATION AND IS FLAGGED RATHER THAN REPEATED — *“the v4 chain at 25 + 7”*:** `CHAT-DISPATCH-v4.md` carries **ZERO** status glyphs, and the queue's header last records the block at **34 rows** against this file's **32**. **Neither figure is asserted here.** **`TOC-6` IS RULED — `#nn` IS DESIGN-ONLY, and this batch's runner line minted none.** **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f into `case-heartbeat-design.md` §8. Carried: no law-change ledger, Outlook unreviewed
