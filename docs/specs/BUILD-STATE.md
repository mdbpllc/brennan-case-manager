# BUILD STATE — brennan-case-manager
Commit: 212b80c  |  Branch: master  |  Generated: 2026-08-16 Central (eighty-third refresh)
*`212b80c` is the batch commit this snapshot DESCRIBES; the refresh itself rides the commits after it, so the pushed SHA you are told is expected to be higher. Nothing here is stale on that account.*
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*
***DO NOT CITE THIS FILE BY LINE NUMBER IN A PERMANENT ENTRY.** RULED PROJECT-WIDE 2026-08-16 (#94, cite-stability, riding v20): line cites are permitted only into **append-only** files; **prepending** files (the session log) and **wholesale-rewritten** files (this one) are cited by heading or quoted sentence. This file is rewritten in full every batch, so a line citation is stale within one batch.*
*EVERY COUNT BELOW WAS RE-DERIVED THIS REFRESH, NOT CARRIED — runner v9 Step 4 item 3 (OPEN-5(a), ruled #94).*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS BUILT IN CODE 08-12 —
SIX OF SEVEN SLICE ITEMS, FIXTURE DATA ONLY; THE LIVE MIGRATION IS UNRUN AND IS YOURS.**
`archive-project-history-by-day.md`

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **AUDITED 2026-08-15 (#79): `record-integrity-audit-2026-08-15.md` — 65 claims in this file and the queue: 46 SOUND, 6 stale, 1 CONFIRMED-FALSE, 1 contradicted.** Six findings RULED AND CORRECTED (#82, A-1–A-6); **`AUD-2` and `AUD-3` still carried unchanged** — they sit in **OPEN-1**/**OPEN-2** territory, where correcting them without your ruling makes the auditor an editor
- **WITHDRAWN, NEVER ROUTE: `registry-retrieval-completion-2026-08-16.md` (#93).** Written on a false premise and withdrawn the same session. **It never reached the repo.** If a copy arrives by any path, it is an error

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE MEANS: real case / party / client / SOL data hand-entered into the core app — NOTHING MORE.** Floor, in order: **(1) the CD-1 build landed and exercised · (2) gate 1 Supabase Pro · (3) gate 9 production SMTP · (4) gate 3 RLS tested INCLUDING the slice's new tables · (5) gates re-check session + instructions version.** Full text: `Go_Live_Gates.md` *(**AUD-2, CONTRADICTED: the gates doc says "v15" at item 5, and v19 is in force. That doc is canonical and append-only and was NOT edited — the wording act is yours at OPEN-1.** **THE SAME QUESTION IS RECORDED THREE TIMES** — `docs-lint-sweep-2026-08-13.md` §1 item 1, then `AUD-2`/`OPEN-1`, then `GLR-1` at #92 — **and NO fourth row was created.** **The drift is itself the argument for ruling it: v15 → v17 → v18 → v19, each recording silently modernizing the number**)*
- **A POINTER CHECKLIST OVER THE GATES EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It restates no gate's substance and carries NO status column, by design** — status is this file's job, and a second status snapshot is the `#13`/`R-3` copy-forward failure. **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect**
- **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor** — drafting continues via the skill
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. Appending a gate is the ruling.)*
- **THREE PLACES WHERE READING A GATE ALONE GIVES THE WRONG ANSWER (#92 — recorded, NOT corrected).** **Gate 2 read alone OVER-BLOCKS** (the 08-11 clarification is load-bearing). **Gate 3 read alone looks satisfiable and is not** — gate 6 is a hard prerequisite and **the dependency is recorded at gate 6, not at gate 3.** **Gate 9's trigger is a different SHAPE** — the others fire on real *data*, gate 9 on *reliance*, **so a checklist ordered by "when does real data arrive" schedules it too late**
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**
- **ITEM 7 IS YOURS AND IS UNRUN: `db/migrations/2026-08-12-cd1-contact-directory.sql`.** Back up, paste ALONE in an empty buffer, **answer its five verification checks IN WORDS.** One check **expects a HIGH flag count — a LOW number would mean something was guessed**
- **ITS ORDER RELATIVE TO PR-3 HAS A CONSEQUENCE AND NOTHING ON THE RECORD SAID SO (#86, `Q-PR3-3`, UNRULED).** That migration derives caption alignment only for a literal two-value practice-area IN list. **PR-3 first** → re-keyed probate rows fall outside it and are **FLAGGED**, the migration working as designed. **CD-1 first — the order this launch path currently schedules** → probate rows are still PI at that moment, take the plaintiff-defendant derivation, and **nothing later revisits them.** **The quieter failure is the one scheduled**, and it is coupled to the `sideSetFor()` defect below
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED**
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **THE PROTOCOL FOR STAGE 1 EXISTS AND AUTHORIZES NOTHING (#90, Task 16): `docs/t3-pilot-recording-protocol.md`** — 534 non-blank / 688 raw, **PROPOSED — PREPARATION ONLY.** `R`/`K`/`I` provenance marks intact, **`I` NOT promoted**; its §5.3 placeholders are **deliberate — no audio file was measured.** **Seven questions `Q-T3P-1..7`, packet-local.** Placement at `docs/` is your **interim** direction, not a ruling (`Q-T3P-7` open)
- **ITS HEADLINE IS THAT HK-4 MAY DESCRIBE AN ACT THAT ALREADY HAPPENED.** HK-4 and this file both read as *"create the directory at staging time"*; the **2026-07-25** pilot-fixtures entry records the thirteen as **already archived at `..\data\pilot-recordings\` WITH A PROVENANCE README no design session has ever read.** **So HK-4 is probably a P15 → P1 TRANSFER — cheaper and more dangerous, since a transfer can silently overwrite that README** (`Q-T3P-1`, `Q-T3P-2`). **The bundle also held ground-truth scripts, scorecard and findings, while HK-4 names only audio** (`Q-T3P-3`)
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **YOUR HAND (HK-4), NOT RE-WORDED.** **Re-checked natively at #90 against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** A negative about **this** machine that **does not answer the preflight row**, which is yours
- **THE MEASURE ITSELF IS SPLIT, WHICH NOTHING ELSE STATED (`Q-T3P-4`).** The authorization reruns both batches *"against the existing ground truth"* — but ground truth exists for **five of thirteen**. Batch 2 is scripted → an **ACCURACY** measure; batch 1's "existing" artifact is the int8/CPU-floor transcript, a prior **OUTPUT** → a **CHANGE** measure. One figure reports two different things. **D1 auto-file and the confidence thresholds wait on this scorecard**
- **COVERAGE, PLAINLY: the thirteen exercise ROUTING thoroughly and the ASR stack NARROWLY.** No long-form audio (max ~71 s; deposition length is the Statement Bank's real use case, untested — Phase 0's 79 MiB / 20 MiB activation figures are **3-second datapoints, not extrapolable**). The **4-speaker cap is untested**. **No Spanish** — accuracy AND the flagged non-English diarization degradation. **Nothing from the capture kit, which is not purchased. Word boosting has never run on any recording.** All thirteen are iPhone / Voice Memos / **AAC ~70 kbps mono** and can never meet the kit standard — **preserve bit-exact, no re-encode/up-sample/normalize/trim**, because an up-converted file *looks* like kit-standard material in its header
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (unauthorized). A **second, unrelated `T1`–`T4`** is the LegiScan poller tier in `statute-text-and-bill-tracking-design.md` — **substantially BUILT.** **Five WF gates say "gated on T3" without naming which**; re-lettering is yours (the H → HK precedent)
- **THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until you locate it or re-issue, T3 WORK IS UNAUTHORIZED.** **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6`**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher
- **NOTHING EXISTS AT INTAKE, WHICH IS WHERE THREE OF RE-1'S FOUR TRIGGERS FIRE (#88, `Q-RE-4`).** The master spec **designs** the PNC funnel and calls it SETTLED — PNC → Client / Declined / **Referred out** — and **it is not built**: word-bounded `PNC` returns **ZERO** across `db/` and `supabase/`, `parties` has **no status column**, and three separate places already record the missing PNC → Client promotion path

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)** |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** | bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients.** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab, so the rule narrows what is OFFERED, never what is REACHABLE.** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT** — no schema. **D-CL2-3a RULED 08-13: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. The time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter interface — every feature works in both modes. Seeds fictional; **store v11** (CD-1), v9→v10→v11 chains **FORWARD in place**, backing up at each step. **`npm run dev:demo`** runs demo mode past a real `.env`; **demo state does NOT travel**
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **36 `create table` statements in schema.sql, but the live database is still at 34**: CD-1's two are in the file and in the UNRUN migration
- **GRANTS ARE PART OF THE SCHEMA — load-bearing.** `authenticated` ONLY, **`anon` gets nothing by design** (`db/migrations/2026-07-28-api-role-grants.sql`). **`ALTER DEFAULT PRIVILEGES` is NOT set: every new table must carry its own GRANT or it is unreachable.** CD-1's two carry their own
- **`privilege_tier` NO LONGER ASSERTS A STANCE NOBODY CHOSE — Q-COM-11 RULED (A) AND THE SCHEMA HALF IS EXECUTED (#94).** Both columns (`generated_documents`, `transcripts`) **lost `default 'work-product'` and `not null`** in `db/schema.sql`; **NULL now means unclassified-must-classify.** The reason ruled on: writing `'work-product'` is an assertion of privilege under TRCP 192.5(d), and for witness interviews and depositions it asserted the stance the rule excludes. **THE MIGRATION IS AUTHORED AND UNRUN AND IS YOURS: `db/migrations/2026-08-16-privilege-tier-no-default.sql`** — five verification checks in words, one of which reads both CHECK vocabularies and states that **two different lists is the correct result today**, so nobody "fixes" Q-COM-10 while running it. **No existing row was re-characterized** (all fiction; re-characterizing is a legal act and yours). **Until it runs, the live database still defaults new rows while `schema.sql` says otherwise.** **NO creation-time classification UI was built — that is the recorded follow-on act**
- **THE TWO `privilege_tier` VOCABULARIES STILL DISAGREE, AND THE DIVERGENCE IS DUPLICATED IN TYPESCRIPT (#85 → #89 `Q-COM-10`, extended #94).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')`, while a comment in `schema.sql` calls the first *"the shared system-wide set."* **NEITHER CHECK WAS TOUCHED — Q-COM-11's ruling expressly did not settle this.** **RUNNER-FOUND #94: `src/domain/billing.ts` and `src/domain/transcripts.ts` each `export type PrivilegeTier` with DIFFERENT members**, matching their own CHECK. **So the question has FOUR homes, not two, and any ruling touches `src/` as well as `db/`**
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view**, and a word-bounded sweep of `db/` + `supabase/` returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **THERE IS NO GEOGRAPHY ON `cases` AT ALL (#88, `Q-RE-1`).** `court_name` and `cause_number` are free text; **no county, venue, district or jurisdiction column.** The record carries **two readings of the same "out-of-area" trigger from the same session** — subject-matter (answerable from `practice_area`/`case_type`) and geographic (answerable from nothing the schema holds). **Picking one is yours**
- **A WRITTEN-CHANNEL COMMUNICATION HAS NOWHERE TO BE, AND THE TABLE IT WOULD LAND IN SAYS SO (#89, `Q-COM-2`).** `transcripts.source` and `staging_items.source` **both** admit exactly `('recorder','phone','manual')` — no chat, no mail, no SMS — and **`staging_items` is the table an ingest pipeline would land in**; all eleven `context_type` values are audio or meeting contexts. **AND IDENTITY RESOLUTION HAS NO SUBSTRATE (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded across `db/schema.sql` and the migrations — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`. **CD-1 built typed aliases and a multi-match FLAG for NAMES and nothing for addresses.** Remaining CHECK widenings are schema acts and yours
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series** — no response-set, response-item, or escalation-timeline table. **Nothing for a deadline engine, and no discovery-level field on `cases`** (FE-5's hard prerequisite). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all ZERO (#85). AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — no correlation column to hang a link-don't-rebuild integration on, and none of the 36 tables is a money table. NOR FOR RE-1: `referral`, `conflict_check`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC`, `prospective` all ZERO (#88)**
- **TWO CLEARING MECHANISMS FOR ONE SHAPE OF THING, FLAGGED AND CHANGED NOWHERE (#88, `Q-RE-6`).** The 08-07-26 ruling routes a conflicts disposition to the **review log** — but `review_log.action`'s CHECK admits only `('suggested','confirmed','edited','rejected','created','generated')` and **none of those is "decided,"** while every flag built since clears through a nullable `resolved_at`. A **vocabulary gap rather than a live defect**
- **Health MEASURED THIS BATCH (#94) — and this batch was NOT docs-only, so `QR-6(f)` required the check rather than a recorded skip: 278 tests pass (23 files), `npm run build` clean (tsc -b + vite), `npm run lint` exit 0.** The chunk-size warning is pre-existing. **The docs-only run that held from #68 ENDED here** — `db/schema.sql` and a new migration were routed

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED:** authenticated SELECT on all API tables; INSERT+DELETE on `parties`, `legal_rules`, `glossary_terms`, `watch_targets`; the app's write path; **CL-2's live paths. PRESENT BUT UNTESTED: every other policy-bearing write path** — an inference. **GL-1 item 4 makes it a gate**
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is NOT an RLS result. Probe covers **36** tables; **keep it in step or a missing GRANT hides.** *(The #94 migration's check 5 confirms no policy reads `privilege_tier`, so nullability cannot disturb one.)*

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. Called auth-blocked, but **that diagnosis is incomplete — `service_role` was never granted either**; start there. **/statutes in demo mode never touches Supabase** — fixture chapters only
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY AT #90 AND ABSENT on this machine** (declared path only). **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (your hand, your browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently (the app loses its id).** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** Repro: `outlook-edit-cancel-exercise-2026-08-13.md`. **THE AUTH SURFACE BEHIND ALL OF IT IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on your registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (yours) — **and OBS-1 asks whether that capture row folds into Phase 1b's scope; unruled.** Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**
- **A MIGRATION PROPOSAL EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (Task 12; you did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately a QUESTION** — inventing a placeholder ladder is how the current wrong one happened. **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`). **`Q-PR3-1..7` and `PR3-LOOK-1` are yours; nothing was re-parented**
- **A DEFECT IN A RULED CONSTRAINT, CONFIRMED AT HEAD, CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts` returns the plaintiff-defendant side set for **every practice area that is not `'Criminal'`** — while that file's header states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant alignments. **Live today, unreachable only because no fourth practice area exists.** **The codebase carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by design, while `sideSetFor()` **silently supplies a wrong-but-plausible answer.** **Flagged, not fixed**
- **FORM ENGINE — FE-D1 (DISCLOSURES ENGINE) NAMED, SCOPED, AND AUTHORIZED 08-12 (#63); NOT BUILT.** Scope `docs/specs/fe-d1-build-slice.md`; kickoff `docs/prompts/PROMPT-fe-d1-build-session.md`; **yours to fire, Opus, its own session** (the CD-1 pattern — the queue runner is barred). Slice 1 = §§1–6 wizard + §10 substrate + §11.3 skeleton; **NOT "FE-1"** (retired scout). **IN:** FE-10 + FE-12 from birth, FE-8 retention half, FE-15, a MINIMAL template editor. **OUT with homes:** FE-9 · FE-11 · FE-13 · **FE-14 (TRCP 47 gate)** · FE-16 · FE-17. **FE-4/FE-5/FE-6/FE-7 have NO FE-D1 disposition at all.** `docs/skills/drafting-disclosures/SKILL.md` is **a DOCUMENT, not code — now v2** (08-11, design-side; **Code must never edit it**)
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, backlog 37, and NOTHING gated by any entry is built
- **THE BACKLOG MOVED FOR THE FIRST TIME IN A MONTH: 34 → 37 (#94), and it moved by ADDITION, not by verification.** Recomputed this refresh: **30 UNVERIFIED** in `legal-rule-registry-discovery-enforcement-and-pleading.md` + **SEVEN UNVERIFIED** in `legal-rule-registry-criminal-plea-and-costs.md`. **NOTHING WAS VERIFIED. The count of verified entries did not change**
- **THE REGISTRY HAS ITS FIRST WORK-PRODUCT PROPOSITIONS — THE FIFTH ABSENCE ANSWERED IN ITS NARROWEST PART (#94).** Three TRCP entries, **ALL UNVERIFIED**, ruled in by you: **192.5(d)** (an assertion of work product **is** an assertion of privilege), **192.5(c)(1)** (witness statements **excepted** from work-product protection), **192.3(h)** (what a witness statement **is** — including that notes taken during an interview are **not** one). Source: a clean-authority TRCP PDF in your Knowledge Repo, **with the caveat recorded rather than smoothed that its opening page carries no separate effective-date line** — "July 2026" is the filename's designation. **`REGISTRY-V` is your verification and nobody else's.** **PLACEMENT: subject order, not appended** — the packet said "append," that file's own Structure paragraph orders by subject with rules before cases, and the deviation is recorded. **A `192.3(h)` entry already existed on the *discoverability* sentence, so the new one carries a DEDUPE NOTE** under that file's own TRCP 196.2(b) precedent; **whether the two consolidate is a one-word answer and yours**
- **TASK 19 WAS ATTEMPTED 2026-08-16 AND IS NOT COMPLETE (#93). NO ENTRY WAS VERIFIED — and it REMAINS THE RESUME POINT.** **THE FINDING THAT STOPPED IT GOVERNS EVERY FUTURE SIGN-OFF RUN: verification attaches to WORDING, and TEN of the original thirty-four carry wording ALREADY RULED OR DRAFTED TO CHANGE AND UNEXECUTED** — **V-5** splits entries **19, 20, 21**, **V-6** rewords **33, 34**, **V-7** narrows **23** (all ruled #73, none executed), and route (c) §9 proposes wording for **5, 6, 11, 27**, adopted for none. **Verifying any of the ten would attach your verification to wording ruled to be replaced.** Proposed order: execute the rulings, then verify. **A FINDING, NOT A RULING** — and the alternative (verify the unaffected, leave the ten) is equally open. **None of the three new #94 entries is among the ten**
- **THE HONEST FRAME: verification turns NOTHING on today.** DE-1 (entries 1–3 + the deficiency batch) — nothing exists for the DE series. FE-14 (entry 27) — **OUT** of FE-D1. CR-10 (entries 28–34) — hard-gated. **Sign-off is permission to build later, not an unblocking act.** Where the backlog *does* bite now is your own hand-drafting: **entry 1 as filed omits "not privileged" from the scope preamble**, the doctrinal opener of every DE-1 deficiency letter
- **YOUR TEST IS ON THE RECORD AND WAS APPLIED TO NOTHING (#93).** *"I honestly think that the proposition should match the actual rule text,"* clarified as **READING A — a proposition must not MISSTATE the operative text; deliberate narrowing to the clause a module relies on stays.** (Reading B, that it must *reproduce* the text, was put and **not** taken.) **SCOPE WAS PUT AND NOT ANSWERED**, so the conservative option governs: **Reading A covers entries 1, 2 and 3 only, was applied to none, and route (c) stays the one-off it was ruled**
- **`legal-rule-registry-discovery-and-carrier-duties.md` IS THE VERIFIED FILE** *(**AUD-3, CONFIRMED FALSE and re-derived again this refresh: it carries 22 VERIFIED status lines against a header reading "TWENTY" — 13 ruled bullets + 7 entry rulings, a bullets-vs-entries unit mismatch. NOTHING was touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)). **AND ONE VERIFIED ENTRY IS UNDER FLAG FOR ITS WORDING: TRCP 190.3(b)(3) — Q-FE5-9. Not touched either.** In the criminal file, **former entry 4 was SPLIT by your ruling 08-13 into 4a** (per-cause costs, still CITE-LESS) **and 4b** (art. 102.073); **entry 3 and 4a carry NO CITE — supplying one is YOUR act.** The discovery-enforcement file's **six WL/slip cites are flagged, none corrected.** **`/rules` SEED ALL UNVERIFIED**
- **THE FOURTH FILE SITS OUTSIDE THE BACKLOG: `legal-rule-registry-draft-entries-medical-billing.md`**, headed ALL ENTRIES UNVERIFIED. Whether that exclusion is deliberate is **Q-STAT-2, yours — the backlog figure above counts the two in-backlog files only**
- **THE SAME ABSENCE HAS APPEARED FIVE TIMES; #94 ANSWERED PART OF THE FIFTH AND NONE OF THE OTHER FOUR (`Q-RE-8` extended).** (1) **NOT ONE PRIVACY PROPOSITION** (#85, `Q-WF-6`). (2) #78's insurance narrowing. (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (#87, `Q-QBO-6`): `iolta`/`trust`/`safekeeping`/`commingl`/`client funds`/`escrow` all **0**. (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** (#88): thirteen terms, all zero. (5) **work product / privilege — NOW PARTLY ANSWERED**: the three #94 entries state what work product is and what the witness-statement exception covers. **THE HONEST STATEMENT STILL HOLDS: the registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** **FOUR research passes still stack behind Q-STAT-5's one ruling**
- **THE RETRIEVAL GAP IS CLOSED, AND THE FIGURE WAS WRONG TOO (#76).** The rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20** — and **all 21 are retrieved** against the official corpus, plus entry 32's official-source read: 22 looks owed, 22 run. **RETRIEVAL IS NOT VERIFICATION.** *(Re-confirmed the hard way at #93: a session asserted the pass had never run, wrote a document on that premise, and withdrew it on finding `statute-pass-registry-retrieval-2026-08-14.md` had done it better — **do not re-run this pass.**)*
- **EIGHTEEN OF THE TWENTY-ONE PROPOSITIONS DIVERGE from the operative text; FOUR CHANGE WHAT THE ENTRY MEANS** — **TRCP 193.2(e)** (the waiver limb is stated **backwards**, and that entry is DE-1's named linchpin), **196.2(b)** (a third permitted response that **does not exist in the rule**), **215.1(d)** (**"shall… unless," not "may"**, plus a reciprocal against a denied movant the entry omits), **47(b)–(c)** (the "all other relief" half is **47(d)**, and a clause **barring discovery until a non-complying pleading is amended** was absent). **PROPOSED replacement wording exists for those four ONLY, ADOPTED FOR NONE** — route (c), your ruling 08-14. The other fourteen stay as flags you resolve at verification (Q-STAT-6)
- **ENTRY 27 IS NO LONGER TEXT PARTIAL, so FE-14's blocker CHANGED KIND: a VERIFICATION act now, not a retrieval gap.** The picklist text exists — **five enumerable options, not "fixed brackets"** — and carries a requirement the form engine did not know about: non-compliance with 47(c) **bars discovery** until amendment
- **INSURANCE — THE ABSENCE CLAIM WAS OVER-BROAD AND IS NARROWED (corrected at #78).** Re-verified across **all four** files: **chapters 541 and 542 never appear, nor does "prompt pay"** ("carrier duties" in that filename means **motor** carrier). **What was false: one Insurance Code entry DOES exist — ENTRY 8 of the draft-entries file** (SB 1264 / ch. 1467 balance billing, `[EXT]`, low priority). The sweep produced **20 CANDIDATES (C-1…C-20), NONE RULED, NO FIFTH FILE CREATED** (Q-STAT-5). **C-19 (61-day pre-suit notice) and C-20 (2-year limitations, extendable 180 days) are deadline material with a build consequence independent of the registry**
- **REGISTRY RESEARCH SUPPORT — FIVE DOCS, ALL PROPOSED, NOTHING VERIFIED, NO REGISTRY FILE TOUCHED BY ANY OF THEM:** `registry-cite-check-2026-08-13.md` · `registry-verification-workbook-2026-08-13.md` (**29,012 B**) · `registry-citator-pass-2026-08-13.md` (**19,056 B**) · **`statute-pass-registry-retrieval-2026-08-14.md`** — its **§3** carries the **corrected A-for-space normalizer** and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess*, and its **§10 harness checked 78 quotations against raw extraction, 78 confirmed** · **`deadline-engine-service-and-response-2026-08-14.md`**. **Net from the citator pass: 5 need a look** (Ochoa, Collins, Irwin, Alford, Castillo), **5 need none for cite or majority** (*each still needs a commercial citator; FLP is not one*). **§3's normalizer must NOT be carried to the TRCP/TRE PDFs — raw TRCP `AA` count is 0, a different publisher with a different artifact profile; the #94 TRCP reads honored that limit**
- **V-4–V-8 ALL RULED 08-13 (#73) — AND NONE OF IT IS EXECUTED.** V-4 keep as two; **V-5 split all three two-case entries — that would take the backlog from 37 to 40 when executed** (a different movement from #94's addition; do not conflate them); V-6 both criminal entries stay, reworded; V-7 narrow entry 23; V-8 record all three FLP hazards. **V-9 OPEN and sharp: the *Irwin* class is a state in which CLAUDE.md's BINDING majority-opinion rule CANNOT RUN.** Amending a binding rule is YOURS

## Design-input memos — ALL PROPOSED, nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions. **No deadline engine is in the build queue.** **ITS HEADLINE IS A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, with 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) now allows a phase-scoped send-by Level 3 period.** **Five questions, NO durable IDs (ID-DL-1)**
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions. **PROVENANCE MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED:** TRCP 21(f)(5) and Statewide Criminal E-Filing R. 2.3(a) fix filing at **transmission to the filer's EFSP** (TIER A); a Bexar county-court sentence appears to fix it at **clerk acceptance** (TIER B), and **TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule** — exposure: transmitted late on a limitations date, accepted two days later (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**). **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing. All three rows stay ⬜.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9). Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument** (its `doc_type` CHECK admits one value)
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** **IN-1 has NO design-doc home anywhere.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED and elaborated; its EXTRACTION half is UNRULED and only mapped.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all**, so a build implementing #64 alone ships with no VIN validation and no promotion gate, silently. **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR** — the crash date is itself under extraction
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN. All seven rows stay ⬜. SEVEN OF SEVEN GATED, ZERO SATISFIED.** **THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **ITS ROW NOW NAMES ALL THREE CONSUMERS (#94, at your word "Amend the row")** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`, the most demanding**: even the metadata-only `callRecords` version needs a **scheduled runner to beat a 30-day retention window.** **THE QUESTION ITSELF IS STILL UNAMENDED AND STILL YOURS.** **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5)**
- **PR-3 RE-PARENTING PROPOSAL (#86) · QBO MEMO (#87) · RE-1 MEMO (#88).** QBO's **HEADLINE: THERE IS NO READ-ONLY OAUTH SCOPE** — one accounting scope grants **full read/write**, so **"read-only first" can be a property of the code but never of the grant** (`Q-QBO-2`); **the economics are INVERTED** (Intuit meters data-**OUT**, free tier 500k credits/month, **BLOCKS rather than bills**); and **the constraint with legal teeth is the one the platform helps with least** — QuickBooks records trust/operating separation as a **naming convention and enforces nothing**, so a read integration **can display a per-matter trust figure that is wrong with the authority of a computed number** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED.** RE-1's: **ONE OF THE THREE NAMED TRIGGERS MEANS TWO DIFFERENT THINGS IN THE RECORD, FROM THE SAME SESSION** (`Q-RE-1`), and **SOURCING NAMES NO SOURCE FOR THE TEXAS DISCIPLINARY RULES — TESTED, NOT ASSUMED:** the bulk corpus holds **494 chapter files** and **the TDRPC live in a Government Code appendix it does not publish**; not academic, since **Gov't Code §82.065(b), §82.0651 and Penal Code §38.12(c) each incorporate them BY NAME** (`Q-RE-9`). **THE ONE THAT PAYS FOR THE SECTION: TDRPC 1.04(f)(2) IS THE FIELD LIST** — consent **in writing and PRIOR to the referral**; **1.04(g) makes the failure economic and silent — an unconfirmed arrangement does not void the referral, it STRIPS THE FEE.** **`P-RE-1..12` ALL UNVERIFIED**
- **COMMUNICATIONS-LOG INGEST MEMO (#89)** — Task 15. **THE DISPATCH'S OWN WORD WAS CONTRADICTED IN TERMS:** it called two constraints **ruled**; the log records the exchange as *"Claude recommendations, unruled."* Both trace to one paragraph of `future-modules-capture-2026-07-28.md` §3 — **the same gate as `Q-QBO-1`, firing a second time** (`Q-COM-1`). **THE TWO RESOLVE DIFFERENTLY:** the scope exclusion is honored and **costs nothing**; the architectural commitment is **priced for the first time** — a second public HTTPS endpoint, private-key custody, an outside vendor's discretionary approval, and an unestablished metering position, **for a channel the firm does not use.** **A NEW GATE CLASS (`Q-COM-5`): a third party's DISCRETIONARY APPROVAL.** **THE SMS HEADLINE LANDS ON THE STACK ALREADY COMMITTED:** Teams SMS needs three licences plus A2P Brand *and* Campaign approval, is **1:1 with no MMS** — so it **cannot receive the scene photo, the insurance card, or the bill** — and **is apparently not exposed to Graph at all**, on the memo's weakest source, marked `[C]` (`COM-LOOK-3`). **THE IDEA WORTH ITS OWN LINE: a communications LOG and a communications ARCHIVE are different products, and only the archive needs content** — `callRecords` gives who/when/how-long with **no audio and no transcript, so no privilege, consent or PHI problem attaches, because nothing was captured.** **AND THE ONLY MICROSOFT-NATIVE PATH TO CALL CONTENT INVERTS RULING 8.3** — compliance recording is **policy-based and always-on** against 8.3's **opt-in per call** (`Q-COM-8`). **`Q-COM-11` IS NOW RULED AND EXECUTED (#94); `Q-COM-10` and `Q-COM-12` REMAIN YOURS**, along with `Q-COM-1..9`, `COM-LOOK-1..4` and `P-COM-1..5`
- **T3 PILOT PROTOCOL (#90) · SESSION-LOG TOC (#91) · GO-LIVE RUNBOOK (#92).** The **TOC** (`docs/specs/session-log-toc.md`, 284 non-blank / 310 raw, 52,994 bytes) carries **the finding that matters to every reader of the record: the log runs TWO independent numbering systems and neither covers every entry** — `#nn` and **runner ordinal** — with **eight runner batches carrying a `#nn` instead of an ordinal** and **one carrying neither**; a reader assuming one system mis-files about a quarter of the log. **NO ENTRY ANYWHERE CARRIES `#1`** (`TOC-1`). **FIVE DATES SIT OUT OF NEWEST-FIRST ORDER; `runner 23`/`#65` is explained nowhere** (`TOC-2`). **The corrections graph is thinner than it reads: 67 edges, 22 self-corrections, only 14 resolving to another entry.** **`TOC-5` has a consequence that arrives without a ruling** — at ~53 KB in the synced `docs/specs/` it enters project knowledge and spends context budget. *(Now STALE by FIVE entries — #92, #93, #94 and two runner lines — which is `TOC-4`, its regeneration cadence, in the concrete.)* The **runbook**'s gate resolved an eighth way: **DERIVED like #91 but NOT REGENERABLE** — its "who acts" and "done-when evidence" columns are judgement, so an appended gate silently makes it incomplete (`GLR-3`)
- **TASK 19 CORRECTION (#93)** — **NO STAGED FILE. THE FIRST SELF-CORRECTION: the session's headline is its own withdrawn error.** It asserted 22 backlog entries had no retrieval, wrote a document on that premise, delivered it, then found the 08-14 statute pass had **already run exactly that pass and recorded it better. Not one finding was new; it never reached the repo.** **Failure class: incomplete session-start reads** — a `project_read` on this file failed early, the session fell back to RAG and **never returned for the read once the bridge was granted**, while this file stated the answer outright
- **QR-6 ADJUDICATION (#94)** — **NO STAGED FILE; a runner amendment, three registry entries, queue dispositions, one annotation, and a schema act.** **THE GATE RESOLVED A TENTH WAY: it is the first packet that was PURE ADJUDICATION** — Fable framed, you ruled live one item at a time, and every item it carried was already an open queue row rather than new research

## For design side
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built. *(Live tension, now a queue item: **`Q-PR3-1`** asks whether a design-side `src/` read through the device bridge is sanctioned at all — the working-set policy says design sessions do not read source, that sentence predates the bridge, and **every finding in the #86 proposal is undrivable without such a read**. Yours; nothing from `src/` entered project knowledge.)*
- **INSTRUCTIONS: v19 IS IN FORCE — ESTABLISHED 2026-08-16 (#94) BY OBSERVATION, NOT BY RECALL.** The #94 design session **read its own live instructions field and found v19**, dated 2026-08-16 with the QR-5 header present — the #82/A-1 mechanism. **Your own recall was "No / not sure"; the observation is the evidence and the uncertainty is recorded rather than smoothed.** **The standing lesson holds: a running session can read its own instructions; a Code session structurally cannot, so this file must never assert a paste state it did not learn from a design session.** DT-1 remains in force. **TRIGGER #3 FIRED TWICE AT #94 → v20 IS DRAFTED AND DELIVERED SEPARATELY, AND THE PASTE IS YOURS. THIS FILE ASSERTS NOTHING ABOUT WHETHER IT HAPPENED.** v20 carries **the cite-stability convention** (in this file's header above) and **the classifier operational note** (below). **The stale `v17` paste row in the queue was annotated overtaken-by-events and deliberately NOT closed — a one-word ruling and yours**
- **SOURCING IS A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED).** Statutes, rules and regulations get a named-source rule **parallel to TOOLING**: official corpus / clean-authority PDFs / eCFR API, **each source named per item**, currency never inferred from a document, extraction artifacts **characterized rather than guessed**, and **retrieval is not verification**. **Q-STAT-2 through Q-STAT-6 stay OPEN**, and **#88 found the convention's first proven GAP — none of its three layers reaches the disciplinary rules (`Q-RE-9`). SOURCING was NOT amended.** *(#94 applied it to a TRCP PDF and recorded the effective-date caveat rather than smoothing it — the convention working as designed.)*
- **CHAT-DISPATCH: TASKS 1–18 COMPLETE; TASK 19 ATTEMPTED AND NOT COMPLETE — IT REMAINS THE RESUME POINT** — 6 = the deadline memo (#75), 7 = Bexar (#80), 8 = the three form-engine specs (#81), 9 = the two intake specs (#83), 10 = IN-2 (#84), 11 = the WF spec (#85), 12 = the PR-3 proposal (#86), 13 = QBO (#87), 14 = RE-1 (#88), 15 = communications (#89), 16 = the T3 protocol (#90), 17 = the TOC (#91), 18 = the runbook (#92). **19 = ATTEMPTED at #93, NO ENTRY VERIFIED** — it is **INTERACTIVE, registry sign-off, only with you live**, and **#93's sequencing finding should be read before it is begun again.** **RISK ON THE RECORD (#77, sharper at each of #80–#94): the prompt has NO durable home** — now **eighteen** sessions on your clipboard, exposure **ONE task**; **if lost, Task 19 is unrecoverable except by re-derivation.** **OPEN-4** asks whether `docs/prompts/` should take design-chat prompts too
- **HK-7 STAYS OPEN; #94 IS THE FIFTEENTH ANNOTATION AND THE FIRST WHERE A SECOND FOLDER PAID.** **TWO folders were granted (the checkout and `Documents\Knowledge Repo`) and the second was SPENT productively** — the TRCP PDF read that produced the three registry entries. **That is the counter-datum to #93**, where the second folder bought only a withdrawn pass. **Task 19 needs the grant re-issued.** **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**
- **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY. **CD-2 is NOT an open design question** — it is a coverage audit of a ruled taxonomy. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. *(#86 read `roster.ts` and found the `sideSetFor()` defect there — evidence the gap was load-bearing.)* **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SIX REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*`. First three SPENT as CD-1 input; UIM + deficiency seeded the **DE series**. **No capture in the repo is unreconciled.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2
- **YOUR HAND, THE LAUNCH PATH IN ORDER: run the CD-1 migration** (back up, paste alone, answer its five checks in words) → **gate 1 Supabase Pro** → **gate 9 production SMTP** → gate 3 RLS test **including the two new tables** → gates re-check + the instructions version. *(**`Q-PR3-3` touches step 1: the PR-3/CD-1 order has a consequence and is unruled.** `docs/specs/go-live-runbook.md` is the pointer checklist — but it carries NO status, so this file stays the authority on where you are.)*
- **ALSO YOURS, NEW THIS BATCH: paste project-instructions v20** · **run `db/migrations/2026-08-16-privilege-tier-no-default.sql`** · **verify the three new TRCP entries (`REGISTRY-V`)** · **close the stale `v17` paste row** · **one line of runner text for `QR-6(c)`'s missing half (below)** · **whether the two `192.3(h)` entries consolidate**
- **ALSO YOURS, CARRIED: re-upload SKILL.md v2** (SK-v2) · **the sync-picker clicks** (Q-3) · **telemetry lockdown on the P1** · **HK-6** · **HK-7 re-grant each session** · **V-2, Ochoa first** · **V-9** · **OBS-1** · **CD-4 – CD-13** · **Q-STAT-2 – Q-STAT-6** · **the deadline memo's five + ID-DL-1** (still **THIRTEEN** packets — #94 mints none) · **the Task 8 specs' 22 + 3 looks — Q-FE5-9 first, it sits inside a VERIFIED entry** · **the Task 9 specs' 18 — Q-IN1-8 is a one-word veto on the whole IN series** · **the Task 10 spec's nine — Q-IN2-2 is cheapest** · **the Task 11 spec's ten + `WF-LOOK-1` — Q-WF-4 decides whether the WF series is reachable at all** · **the Task 12 proposal's seven + `PR3-LOOK-1` — one query that may delete half of PR-3** · **the Task 13 memo's eight + four looks** · **the Task 14 memo's nine + three looks** · **the Task 15 memo's remaining eleven + four looks + five propositions** · **the Task 16 protocol's seven — `Q-T3P-1` is the cheapest question in the whole queue** · **the Task 17 index's five — `TOC-5` accrues at your next Sync click** · **`GLR-2` and `GLR-3` — `GLR-2` is the SAME question as `TOC-3`, one ruling closes both** · **`OPEN-1`, recorded THREE times, the cheapest high-value ruling on the launch path** · **`OPEN-5(b)` and the `AUD-5..9` one-word apply, deliberately not put at #94** · **#93's FOUR: Task 19 sequencing · Reading A's scope · entries 1 and 3's conforming wording · route (c) as standing law** · **delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`**
- **METER, AS A TIMESTAMPED SNAPSHOT AND NOT A STANDING FIGURE (#94, 2026-08-16): Fable 82% used, all-models comfortable, resetting Tue 2026-08-18 4:00 PM Central.** **The consequence, design input and not a ruling: Fable is SCARCE** — **so ration it. Opus and Sonnet work is unconstrained.** Strongest claims on the Fable remainder: **the gates re-check (GL-1 item 5) and V-9**
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. Nothing built** — and **193.2(e), DE-1's linchpin, is one of the four entries whose wording the statute pass says is backwards**
- **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated, **and the #85 spec confirms every gate is still shut**. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` sits untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand.** **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-6 + MM-1 + OPEN-5(a) ALL CLOSED; runner v9** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy**; the `/queue-runner` command is a pointer to it). **Your hand: the P15 user-level copy deletion.** **QR-6 RULED 2026-08-16, ALL SIX (#94):** (a) a stated verification **names the command that produced it, and a command that cannot produce a disconfirmation is not one**; (b) the queue merge is **TWO acts** — rows **and** the header reconcile sentence; (c) **delete by explicit filename, with packet identity (size/mtime/sha256) pinned at Step 1**; (d) **IN DYNAMIC FORM** — format comes from the runner text's own Step 4 rules and is **never copied from an entry authored under an earlier runner version** (a named static exemplar was proposed and **rejected as itself the QR-5 shape**); (e) **a packet-added act in no routing-table row and no Step 4 item needs your in-session authorization**; (f) **a docs-only batch records the health-check skip explicitly.** **`OPEN-5(a)` rode the same amendment into Step 4 item 3 — every count this file states is re-derived, never carried.** **QR-3 STILL CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74, #93): the mount reads a false DIRTY of CRLF-only files, and `git fetch`/`ls-remote` fail there with `HTTP 403 from proxy`
- **QR-6 FIRED ON ITS OWN FIRST BATCH, AND TWO LIMBS PRODUCED FINDINGS AGAINST THEMSELVES (#94, forty-ninth invocation).** **`QR-6(e)`:** the packet reported a stranded 0-byte `.git/index.lock`, routed its deletion nowhere, and it would have blocked the close-out commit — **authorized in-session and recorded rather than done silently.** **`QR-6(c)` LANDED HALF: the pinning half is in at Step 1 item 4, but no insertion puts "delete by explicit filename" into Step 4 item 5**, where the deletion happens; **not added, because inventing a ninth insertion is exactly the act (e) reserves to you. One line of runner text closes it.** **`QR-6(b)`'s OWN DEFECT HAS HAPPENED THREE TIMES, NOT THE ONCE ITS RULING TEXT RECORDS: the queue header's reconcile high-water was #91, so #92 and #93 are both missing their sentence while their rows landed.** **Reported, not back-written** — the #90 precedent
- **THE CLASSIFIER CAN REFUSE AN ALLOWLISTED BARE COMMAND — RULED AN OPERATIONAL NOTE IN v20 (#94), on evidence that moved under the original framing.** The forty-seventh invocation's **bare** allowlisted `git push origin master` was refused on **both** shells, retiring "prefer bare forms" as sufficiency guidance. **A remotely-driven close-out can strand at the push with permissions fully in place; `QR-5(a)` plus the QR-3 ahead-stop is the designed catch; never conclude a batch landed without `git ls-remote`**
- **QR-5(a) HAS RUN THREE CYCLES AND THE THIRD PROVED IT — A REAL FAILURE, CARRIED AND CLOSED.** Cycle 3: the forty-seventh's push was refused, it **reported the block, left the zip in `inbox/`, and asserted nothing.** **The forty-eighth's Step 0 AHEAD-STOP then fired — the first time since QR-3's v7 amendment — and it was right.** You pushed by hand. **Both the convention and the gate did exactly what they were written to do, on a real failure rather than a hypothetical**
- **THE STALE `.git/index.lock` HAS AN OBSERVED CAUSE RATHER THAN A HYPOTHESIS — #90; SIX-FOR-ELEVEN after #94 found it PRESENT AGAIN.** A design session's bridge `git status` makes git emit `warning: unable to unlink '…/.git/index.lock': Operation not permitted`, and **the 0-byte lock strands: the mount DENIES `unlink`, so git's own cleanup fails.** **Evidence only.** At #91 the lock was **ABSENT**. **A bridge read is not evidence about origin (#88, reconfirmed #93) but it IS evidence about local files**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and now `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** (Q-IN2-1). Synthesis `cr3-field-code-map.md`, **PROPOSED input — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: the #62/#63 and #64 batches were VERIFIED design-side FULL-TEXT on 08-13 (#65)**; **#67 verified landed at the top of #68.** Still UNREVIEWED, **recomputed from the log at HEAD this refresh (A-4, ruled #82; now also runner text):** the carried **#31–#33** material, **#37–#61's own routing**, and **#75 through #94** — do not copy #36's clearance forward
- **Everything awaiting your ruling is in `docs/specs/attorney-review-queue.md`** — reconciled through **#94**, **read from that file's own Status header at HEAD this refresh (A-5), not carried.** *(The previous refresh stated #93 while that header's high-water was #91 — the exact drift A-5 exists to prevent, which is why `OPEN-5(a)` is now runner text.)* **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f into case-heartbeat-design.md §8. Carried: no law-change ledger, Outlook unreviewed
