# BUILD STATE — brennan-case-manager
Commit: fdcc9d8 (session start)  |  Branch: master  |  Generated: 2026-08-18 Central (ninety-fourth refresh)
*`fdcc9d8` is the commit this snapshot was written FROM. **It DESCRIBES the tree as this batch commits it**, so the batch's own commit — the SHA you are told — is the one at which every claim below verifies; `fdcc9d8` is not.*
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*
***DO NOT CITE THIS FILE BY LINE NUMBER IN A PERMANENT ENTRY.** RULED PROJECT-WIDE 2026-08-16 (#94, cite-stability, riding v20): line cites are permitted only into **append-only** files; **prepending** files (the session log) and **wholesale-rewritten** files (this one) are cited by heading or quoted sentence.*
*EVERY COUNT BELOW WAS RE-DERIVED THIS REFRESH, NOT CARRIED — runner v11 Step 4 item 3 (OPEN-5(a), ruled #94).*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS BUILT IN CODE 08-12 —
SIX OF SEVEN SLICE ITEMS, FIXTURE DATA ONLY; THE LIVE MIGRATION IS UNRUN AND IS YOURS.**

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **AUDITED 2026-08-15 (#79): `record-integrity-audit-2026-08-15.md` — 65 claims in this file and the queue: 46 SOUND, 6 stale, 1 CONFIRMED-FALSE, 1 contradicted.** Six findings RULED AND CORRECTED (#82, A-1–A-6); **`AUD-2` and `AUD-3` still carried unchanged** — they sit in **OPEN-1**/**OPEN-2** territory, where correcting them without your ruling makes the auditor an editor
- **WITHDRAWN, NEVER ROUTE: `registry-retrieval-completion-2026-08-16.md` (#93).** Written on a false premise and withdrawn the same session. **It never reached the repo.** If a copy arrives by any path, it is an error
- **THE FIVE CHAT-DISPATCH v4 RESEARCH DOCUMENTS WERE ADVERSARIALLY AUDITED AND ARE CORRECTED IN PLACE (#103).** Twenty-two defects — **3 HIGH, 8 MEDIUM, 11 LOW**. **Nothing propagated: no registry entry was ever created from any of them, and the backlog did not move.** Each opens with a `⚠ CORRECTED IN PLACE 2026-08-18` banner quoting its own retracted text — **do not "restore" or merge any retracted passage**. **THE FOUR AUTHORITIES THAT AUDIT SURFACED ARE READ (#104): `costs-and-privilege-authority-read-2026-08-18.md` (19,158 B / 200 non-blank)** — **the entry-31 bridge exists and is held**, see the registry section; a preflight found TWO DEFECTS in it, recorded below and NOT edited into it. **AND THE FOURTH FABLE ADJUDICATION RULED TWO ITEMS, FOUR LIMBS (#105, this batch): `Q-AUDIT-1` → PF-1, the preflight is STANDING law; `Q-COM-10-A` → `privilege_tier` is purpose-scoped on the Option 2 shape.** Its record is folded into `fable-adjudication-record-2026-08-18.md` (26,759 B / 401 raw) — **fold, not branch; no `-b` sibling exists.** **Still nothing built, nothing verified, backlog unmoved**

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE MEANS: real case / party / client / SOL data hand-entered into the core app — NOTHING MORE.** Floor, in order: **(1) the CD-1 build landed and exercised · (2) gate 1 Supabase Pro · (3) gate 9 production SMTP · (4) gate 3 RLS tested INCLUDING the slice's new tables · (5) gates re-check session + instructions version.** Full text: `Go_Live_Gates.md` *(**AUD-2 / OPEN-1 / GLR-1 ALL CLOSED — RULED 2026-08-18 (#100) at the question's fourth recording.** One line APPENDED to GL-1 (append-only honored, the frozen "v15" untouched): **item (5) reads as "the instructions current at the re-check, per trigger 1."** **The number stops carrying meaning, so it can never drift again** — ending a drift that ran v15 → v17 → v18 → v19 → v20 → v21.)*
- **A POINTER CHECKLIST OVER THE GATES EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It restates no gate's substance and carries NO status column, by design** — status is this file's job, and a second status snapshot is the `#13`/`R-3` copy-forward failure. **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect**
- **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor** — drafting continues via the skill
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. Appending a gate is the ruling.)*
- **THREE PLACES WHERE READING A GATE ALONE GIVES THE WRONG ANSWER (#92 — recorded, NOT corrected).** **Gate 2 read alone OVER-BLOCKS** (the 08-11 clarification is load-bearing). **Gate 3 read alone looks satisfiable and is not** — gate 6 is a hard prerequisite and **the dependency is recorded at gate 6, not at gate 3.** **Gate 9's trigger is a different SHAPE** — the others fire on real *data*, gate 9 on *reliance*, **so a checklist ordered by "when does real data arrive" schedules it too late**
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**
- **ITEM 7 IS YOURS AND IS UNRUN: `db/migrations/2026-08-12-cd1-contact-directory.sql`.** Back up, paste ALONE in an empty buffer, **answer its five verification checks IN WORDS.** One check **expects a HIGH flag count — a LOW number would mean something was guessed**
- **ITS ORDER RELATIVE TO PR-3 HAS A CONSEQUENCE AND NOTHING ON THE RECORD SAID SO (#86, `Q-PR3-3`, UNRULED).** That migration derives caption alignment only for a literal two-value practice-area IN list. **PR-3 first** → re-keyed probate rows fall outside it and are **FLAGGED**, the migration working as designed. **CD-1 first — the order this launch path currently schedules** → probate rows are still PI at that moment, take the plaintiff-defendant derivation, and **nothing later revisits them.** **The quieter failure is the one scheduled**, and it is coupled to the `sideSetFor()` defect below
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED**
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **THE PROTOCOL FOR STAGE 1 EXISTS AND AUTHORIZES NOTHING (#90, Task 16): `docs/t3-pilot-recording-protocol.md`** — 534 non-blank / 688 raw, **PROPOSED — PREPARATION ONLY.** `R`/`K`/`I` provenance marks intact, **`I` NOT promoted**; its §5.3 placeholders are **deliberate — no audio file was measured.** **Seven questions `Q-T3P-1..7`, packet-local.** Placement at `docs/` is your **interim** direction, not a ruling (`Q-T3P-7` open)
- **ITS HEADLINE IS THAT HK-4 MAY DESCRIBE AN ACT THAT ALREADY HAPPENED.** HK-4 and this file both read as *"create the directory at staging time"*; the **2026-07-25** pilot-fixtures entry records the thirteen as **already archived at `..\data\pilot-recordings\` WITH A PROVENANCE README no design session has ever read.** **So HK-4 is probably a P15 → P1 TRANSFER — cheaper and more dangerous, since a transfer can silently overwrite that README** (`Q-T3P-1`, `Q-T3P-2`). **The bundle also held ground-truth scripts, scorecard and findings, while HK-4 names only audio** (`Q-T3P-3`)
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **YOUR HAND (HK-4), NOT RE-WORDED.** **Re-checked natively this refresh against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** A negative about **this** machine that **does not answer the preflight row**, which is yours
- **THE MEASURE ITSELF IS SPLIT, WHICH NOTHING ELSE STATED (`Q-T3P-4`).** The authorization reruns both batches *"against the existing ground truth"* — but ground truth exists for **five of thirteen**. Batch 2 is scripted → an **ACCURACY** measure; batch 1's "existing" artifact is the int8/CPU-floor transcript, a prior **OUTPUT** → a **CHANGE** measure. One figure reports two different things. **D1 auto-file and the confidence thresholds wait on this scorecard**
- **COVERAGE, PLAINLY: the thirteen exercise ROUTING thoroughly and the ASR stack NARROWLY.** No long-form audio (max ~71 s; deposition length is the Statement Bank's real use case, untested — Phase 0's 79 MiB / 20 MiB activation figures are **3-second datapoints, not extrapolable**). The **4-speaker cap is untested**. **No Spanish** — accuracy AND the flagged non-English diarization degradation. **Nothing from the capture kit, which is not purchased. Word boosting has never run on any recording.** All thirteen are iPhone / Voice Memos / **AAC ~70 kbps mono** and can never meet the kit standard — **preserve bit-exact, no re-encode/up-sample/normalize/trim**, because an up-converted file *looks* like kit-standard material in its header
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (unauthorized). A **second, unrelated `T1`–`T4`** is the LegiScan poller tier in `statute-text-and-bill-tracking-design.md` — **substantially BUILT.** **Five WF gates say "gated on T3" without naming which**; re-lettering is yours (the H → HK precedent)
- **THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until you locate it or re-issue, T3 WORK IS UNAUTHORIZED.** **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6`**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher
- **NOTHING EXISTS AT INTAKE, WHICH IS WHERE THREE OF RE-1'S FOUR TRIGGERS FIRE (#88, `Q-RE-4`).** The master spec **designs** the PNC funnel and calls it SETTLED — PNC → Client / Declined / **Referred out** — and **it is not built**: word-bounded `PNC` returns **ZERO** across `db/` and `supabase/`, `parties` has **no status column**, and three separate places already record the missing PNC → Client promotion path

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)** |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** | bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients.** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab, so the rule narrows what is OFFERED, never what is REACHABLE.** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT** — no schema. **D-CL2-3a RULED 08-13: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. The time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter interface — every feature works in both modes. Seeds fictional; **store v11** (CD-1), v9→v10→v11 chains **FORWARD in place**, backing up at each step. **`npm run dev:demo`** runs demo mode past a real `.env`; **demo state does NOT travel**
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **36 `create table` statements in schema.sql, but the live database is still at 34**: CD-1's two are in the file and in the UNRUN migration. *(The 36 is re-derived from `db/schema.sql` each refresh; the 34 is an assertion about a remote Postgres instance that no repo check can reach.)*
- **GRANTS ARE PART OF THE SCHEMA — load-bearing.** `authenticated` ONLY, **`anon` gets nothing by design** (`db/migrations/2026-07-28-api-role-grants.sql`). **`ALTER DEFAULT PRIVILEGES` is NOT set: every new table must carry its own GRANT or it is unreachable.** CD-1's two carry their own
- **`privilege_tier` NO LONGER ASSERTS A STANCE NOBODY CHOSE — Q-COM-11 RULED (A) AND THE SCHEMA HALF IS EXECUTED (#94).** Both columns (`generated_documents`, `transcripts`) **lost `default 'work-product'` and `not null`** in `db/schema.sql`; **NULL now means unclassified-must-classify.** **THE MIGRATION IS AUTHORED, UNRUN AND YOURS: `db/migrations/2026-08-16-privilege-tier-no-default.sql`** — five verification checks in words, one of which reads both CHECK vocabularies and states that **two different lists is the correct result today**, so nobody "fixes" Q-COM-10 while running it. **No existing row was re-characterized** (all fiction; re-characterizing is a legal act and yours). **Until it runs, the live database still defaults new rows while `schema.sql` says otherwise.** **NO creation-time classification UI was built — that is the recorded follow-on act**
- **THE TWO `privilege_tier` VOCABULARIES STILL DISAGREE IN THE SCHEMA, AND THE DIVERGENCE IS DUPLICATED IN TYPESCRIPT (#85 → #89 `Q-COM-10`, extended #94) — BUT THE SHAPE THEY MUST CONVERGE ON IS NOW RULED (#105, `Q-COM-10-A`).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')`, while a comment in `schema.sql` calls the first *"the shared system-wide set."* **NEITHER CHECK WAS TOUCHED and nothing was built.** **`src/domain/billing.ts` and `src/domain/transcripts.ts` each `export type PrivilegeTier` with DIFFERENT members**, matching their own CHECK — **so the question has FOUR homes and any ruling touches `src/` as well as `db/`.** **THE RULING: `privilege_tier` is NOT exhaustive** — a purpose-scoped classification under a stated precedence rule, orthogonal attributes in their own fields (the `discoverable_flag`/`phi_flag` pattern) — **and the target is Option 2: THREE values plus a `witness_statement` boolean**, which satisfies `WS-4`'s fourth state without flattening. **`Q-COM-10-B`–`F` are unblocked; `Q-COM-10` itself closes when you adopt the list**
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view**, and a word-bounded sweep of `db/` + `supabase/` returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **THERE IS NO GEOGRAPHY ON `cases` AT ALL (#88, `Q-RE-1`).** `court_name` and `cause_number` are free text; **no county, venue, district or jurisdiction column.** The record carries **two readings of the same "out-of-area" trigger from the same session** — subject-matter (answerable from `practice_area`/`case_type`) and geographic (answerable from nothing the schema holds). **Picking one is yours**
- **A WRITTEN-CHANNEL COMMUNICATION HAS NOWHERE TO BE, AND THE TABLE IT WOULD LAND IN SAYS SO (#89, `Q-COM-2`).** `transcripts.source` and `staging_items.source` **both** admit exactly `('recorder','phone','manual')` — no chat, no mail, no SMS — and **`staging_items` is the table an ingest pipeline would land in**; all eleven `context_type` values are audio or meeting contexts. **AND IDENTITY RESOLUTION HAS NO SUBSTRATE (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded across `db/schema.sql` and the migrations — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`. **CD-1 built typed aliases and a multi-match FLAG for NAMES and nothing for addresses.** Remaining CHECK widenings are schema acts and yours
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series** — no response-set, response-item, or escalation-timeline table. **Nothing for a deadline engine, and no discovery-level field on `cases`** (FE-5's hard prerequisite). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all ZERO (#85). AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — no correlation column to hang a link-don't-rebuild integration on, and none of the 36 tables is a money table. NOR FOR RE-1: `referral`, `conflict_check`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC`, `prospective` all ZERO (#88)**
- **TWO CLEARING MECHANISMS FOR ONE SHAPE OF THING, FLAGGED AND CHANGED NOWHERE (#88, `Q-RE-6`).** The 08-07-26 ruling routes a conflicts disposition to the **review log** — but `review_log.action`'s CHECK admits only `('suggested','confirmed','edited','rejected','created','generated')` and **none of those is "decided,"** while every flag built since clears through a nullable `resolved_at`. A **vocabulary gap rather than a live defect**
- **HEALTH WAS NOT MEASURED THIS BATCH, AND THE SKIP IS RECORDED RATHER THAN SILENT — `QR-6(f)`'s docs-only limb, a NINTH consecutive firing.** Reason named: **§5 was NONE and no `src/`, `db/`, `supabase/` or build-tooling path appeared in the routing table or in `git status`** — the changed paths are the log, this file, the index, the queue and one new document, all under `docs/specs/`. The last real measurement stands as the current figure and is labelled as carried, not re-run: **2026-08-17, 278 tests pass across 23 files; `npm run build` clean (tsc -b + vite); `npm run lint` exit 0**; the >500 kB chunk-size warning pre-existing. **A docs-only batch cannot confirm or disturb those numbers, which is exactly why it must not pretend to**

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED:** authenticated SELECT on all API tables; INSERT+DELETE on `parties`, `legal_rules`, `glossary_terms`, `watch_targets`; the app's write path; **CL-2's live paths. PRESENT BUT UNTESTED: every other policy-bearing write path** — an inference. **GL-1 item 4 makes it a gate**
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is NOT an RLS result. Probe covers **36** tables; **keep it in step or a missing GRANT hides.** *(The #94 migration's check 5 confirms no policy reads `privilege_tier`, so nullability cannot disturb one.)*

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. Called auth-blocked, but **that diagnosis is incomplete — `service_role` was never granted either**; start there. **/statutes in demo mode never touches Supabase** — fixture chapters only
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY THIS REFRESH BY EXISTENCE TEST ON DECLARED PATHS ONLY (HK-5; nothing enumerated) AND STILL ABSENT**, as are `C:\Users\Brennan\data` and `...\data\pilot-recordings\`. **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (your hand, your browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently (the app loses its id).** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** Repro: `outlook-edit-cancel-exercise-2026-08-13.md`. **THE AUTH SURFACE BEHIND ALL OF IT IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on your registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (yours) — **and OBS-1 asks whether that capture row folds into Phase 1b's scope; unruled.** Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**
- **A MIGRATION PROPOSAL EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (Task 12; you did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately a QUESTION** — inventing a placeholder ladder is how the current wrong one happened. **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`). **`Q-PR3-1..7` and `PR3-LOOK-1` are yours; nothing was re-parented**
- **A DEFECT IN A RULED CONSTRAINT, CONFIRMED AT HEAD, CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts` returns the plaintiff-defendant side set for **every practice area that is not `'Criminal'`** — while that file's header states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant alignments. **Live today, unreachable only because no fourth practice area exists.** **The codebase carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by design, while `sideSetFor()` **silently supplies a wrong-but-plausible answer.** **Flagged, not fixed**
- **FORM ENGINE — FE-D1 (DISCLOSURES ENGINE) NAMED, SCOPED, AND AUTHORIZED 08-12 (#63); NOT BUILT.** Scope `docs/specs/fe-d1-build-slice.md`; kickoff `docs/prompts/PROMPT-fe-d1-build-session.md`; **yours to fire, Opus, its own session** (the CD-1 pattern — the queue runner is barred). Slice 1 = §§1–6 wizard + §10 substrate + §11.3 skeleton; **NOT "FE-1"** (retired scout). **IN:** FE-10 + FE-12 from birth, FE-8 retention half, FE-15, a MINIMAL template editor. **OUT with homes:** FE-9 · FE-11 · FE-13 · **FE-14 (TRCP 47 gate)** · FE-16 · FE-17. **FE-4/FE-5/FE-6/FE-7 have NO FE-D1 disposition at all.** `docs/skills/drafting-disclosures/SKILL.md` is **a DOCUMENT, not code — now v2** (08-11, design-side; **Code must never edit it**)
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, backlog 40, TWENTY-THREE VERIFIED, and seven entry texts drafted of which not one exists
- **BACKLOG RE-DERIVED AT HEAD THIS REFRESH, NEVER CARRIED — and NOTHING moved this batch:** `legal-rule-registry-discovery-enforcement-and-pleading.md` **20 VERIFIED / 13 UNVERIFIED against 33 `**Status:**` lines**; `legal-rule-registry-criminal-plea-and-costs.md` **3 / 4 against 7**. **Backlog 40 entries; 23 verified, 17 not.** **The unit is STATUS LINES, not headings** — the criminal file is 6 `##` + 2 `###`, the #66 container shape. **Counting trap, carried: `grep 'VERIFIED —'` also matches `UNVERIFIED —`; anchor on `^**Status:** VERIFIED`.** **SECOND TRAP, CARRIED AND STILL LIVE: THE BASIS IS TWO NAMED FILES, NOT A GLOB.** `registry-new-entry-drafts-2026-08-17.md` carries **six `**Status:** UNVERIFIED` lines of its own** — drafted entry TEXTS inside a drafting record, not registry entries — and it matches `docs/specs/*registry*`; `legal-rule-registry-discovery-and-carrier-duties.md` (22, all VERIFIED) is outside the basis too
- **THE `WS-3` WORK-PRODUCT "GAP" DOES NOT EXIST, AND THIS FILE'S OWN PRIOR LINE ABOUT IT IS RETIRED (#103).** The prior refresh recorded, from #102, that *"194.5 names only work product, so the Fontenot line survives while the Jimenez/Team Transport work-product line is disturbed and NO located authority says how."* **Rule text answers it on its face.** `TRCP 192.5(c)(1)`: *"the following is **not** work product protected from discovery: (1) information discoverable under Rule 192.3 concerning experts, trial witnesses, **witness statements**, and contentions."* ***Jimenez* and *Team Transport* APPLIED that provision and ORDERED PRODUCTION** — they rejected a work-product assertion rather than resting on one. **And this project ALREADY CARRIES the proposition as `## TRCP 192.5(c)(1)`, Status VERIFIED — Michael, 2026-08-17 — in the very file the new entry was to be inserted into, eight headings above its own insertion point.** **Rule 194.5's work-product bar is a 1999 provision, not a 2021 one.** `Q-WS3-1` is NARROWED to a single question: does the WS-3 entry need an express cross-reference?
- **EVERY ONE OF THE 24 IS MICHAEL'S WORD, RECORDED PER ITEM BEFORE ANY FILE WAS TOUCHED: `docs/specs/task-19-signoff-record-2026-08-17.md` (#98).** Its §1 carries the look put in front of him and his answer for each entry, §2 the five adopted wordings, §3 five directed edits, §4 six housekeeping rulings, §5 the counts. **The runner verified nothing and set no status the record does not carry.** Status lines read `VERIFIED — Michael, 2026-08-17 (Task 19 walk; see task-19-signoff-record-2026-08-17.md)`
- **THE SIXTEEN THAT REMAIN, AND THE RECORDS LOOK BEHIND THEM RAN (#99, T-20/T-21).** **Five pending post-execution verification** — 2, 12, 13, 29, 32, whose ROUTE-C wording executed at #98; one look each, and **`T-26` is the only dispatch task left and is INTERACTIVE**. **Seven records/cite looks** — 15, 16, 17, 18, 19a, 20a, 21a — now have **candidates staged per entry, source named per item**, none adopted. **Formerly four blocked** — **20b is LOCATED and 24's majority is IDENTIFIED**; 30 and 31 remain cite-less and their cites are yours. **All sixteen are still UNVERIFIED: locating is not verifying**
- **THE ROUTE-C UNVERIFIED-BY-DESIGN CLASS IS NOW SEVEN ACROSS BOTH FILES, AND THE ENFORCEMENT FILE'S HEADER NAMES ITS FIVE.** #98's five — entries **2** (192.3(h) discoverability), **12** (215.4(b)), **13** (191.2), **29** (§ 481.115(b)), **32** (art. 102.073) — plus **19a** (*Castillo*) and **19b** (*Able Supply*), whose wording Michael adopted 2026-08-18 under `RL-1`. **No cite line changed on any of the seven.** Adoption is not verification — **execute-then-verify (#95)** — and they verify at a later look. **`Q-T19-2` CLOSED**
- **ROUTE (c) IS STANDING LAW (#95) AND HAS RUN THREE TIMES.** A divergence that **changes what an entry means** gets proposed conforming wording, **drafted and queued for your adopt/reject/edit — never adopted silently**, and never as a package. #96 produced its one **rejection** (`V7-23-CITE`), the evidence it is not a rubber stamp. **The #79 audit's contrary recommendation stays recorded as considered and not followed**
- **`Q-STAT-6`'s EIGHTEEN ARE NOW FIVE, AND THE ROW STAYS OPEN.** Adopted at #95 and **verified 08-17: 5, 6, 11, 27**. Flags **CURED**: **1, 23, 33, 34**. Divergences **RESOLVED as-is**, each entry carrying a note naming the omission he was shown: **4, 7, 8, 9, 26**. **Still live: 2, 12, 13, 29, 32** — they resolve at post-execution verification
- **ENTRY 27's HEADING DIVERGENCE IS CLOSED (E-1) AND FE-14's REGISTRY GATE HAS LIFTED.** The heading now reads `TRCP 47(b)–(d)`, matching the cite line #95 routed. **FE-14's BUILD POSITION IS UNCHANGED: it remains outside FE-D1's slice.** **Sign-off is permission to build later, not an unblocking act, and the walk changed no build status**
- **CR-10 STAYS HARD-GATED and the walk did not loosen it:** 29 and 32 are pending execution-plus-verification; 30 and 31 are cite-less and their cites are yours. **THE BRIDGE FOR 31 NOW EXISTS AND IS HELD, THOUGH NOTHING IS SELECTED (#104): *Hurlburt v. State*, 506 S.W.3d 199 (Waco 2016)** — verified independently this batch against the opinion text (`type="majority"`, a full “OPINION,” **Tom Gray, C.J.**, one sub-opinion, every quoted passage confirmed verbatim) — **holds that the CCA's construction of *“in a single criminal action”* governs art. 102.073(a) AND that the “same criminal episode” limb DROPS OUT for costs, which reaches 4a and 4b.** **It routes through *Ex parte Pharr*, 897 S.W.2d 795 (Tex. Crim. App. 1995), NOT *LaPorte*** — why T-27's bridge test came back empty — and ***Pharr* supplies 31's negative side and IS UNREAD** (`Q-AUTH-1`, the cheapest high-value act left). **AND THE CCA HAS A SCOPE HOLDING NOBODY HAD, BUT EXPRESSLY RESERVED THE REST: *Anastassov* (Tex. Crim. App. 2022) — art. 102.073 *“applies only to court costs and fees,”* a fine is not a court cost — while stating it does *“not consider… the court of appeals' holding with respect to court costs.”* **So there is still NO CCA holding on the bridge, and the CCA declined the chance.** **V-6's sharpening still governs — art. 42.0197's finding is NOT automatic** (it needs the State's motion plus a judicial determination), so a completeness check **must not flag its absence where the State never moved**
- **ONE CONSTRUCTION POINT WAS DELIBERATELY NOT RULED AND STAYS FLAGGED INSIDE A NOW-VERIFIED ENTRY (`Q-T19-4`, #98).** Art. 27.18(a) reads *"may accept … **if**"*; entry 34 states the three conditions conjunctively and **deliberately does not write "only if," because the article does not.** Verifying the proposition as written is not ruling whether the conditions are **exclusive** — and CR-10's completeness check behaves differently under the two readings
- **THE 19-PAIR IS UNVERIFIED ON BOTH SIDES, AND THAT WAS THE RULING, NOT AN ACCIDENT.** *Castillo* (19a) and *Able Supply* (19b) carry **the same proposition text** by design; the walk verified 19b only, and `RL-1` then reworded **both** — so **19b's 2026-08-17 verification DETACHED** (verification attaches to wording, #95) and returned to UNVERIFIED with a note saying exactly that. **Re-verification is yours.** The new text adds the omitted disjunctive limb: *"…or where the denial severely compromises a party's ability to present a viable defense."* **`V5-ATTRIB` stays OPEN across all six split entries**
- **THE THREE WORK-PRODUCT ENTRIES ARE VERIFIED AND STILL HAVE NO NUMBER (`REGISTRY-V` CLOSED, `Q-T19-1` OPEN).** 192.5(d), 192.5(c)(1) and 192.3(h)'s definition (notes taken during an interview are **not** a witness statement) verified 2026-08-17, **the effective-date caveat ACCEPTED rather than smoothed.** **Labelled `WP-1/2/3`, packet-local; numbering DEFERRED (`D-6`).** *(#103's finding lands here: **192.5(c)(1) is the entry that closes the WS-3 work-product question**, and the drafting record missed it in its own destination file — `Q-WS2-1(e)`.)*
- **THE WALK'S STAGING LAYER STANDS AND VERIFIES NOTHING: `task-19-signoff-worklist-2026-08-17.md` (#97).** One row per entry across all forty. **Its two arithmetic defects ARE STILL STANDING (`Q-T19-3`)** — §5 Part B says "eleven" over a twelve-row table; §6 says "five of the fourteen" and lists four. **Neither was corrected: §4.1 ordered it verbatim and a change to packet content is yours.** *(That precedent governed again this batch — see the runner line's open-item count finding.)* **One line WAS changed, and only because he directed it (E-3): the *Park Cities Bank* pinpoint → 876–77** in rows 14 and 15; **868 confirmed and standing**
- **E-3 WAS SCOPED, AND THE FOUR UNCONFORMED CARRIERS OF THE OLD `876` ARE NAMED:** the workbook's rows 14/15 (**expressly barred** by the packet's own DO-NOT), `registry-cite-check-2026-08-13.md` and `registry-citator-pass-2026-08-13.md` (**dated records of what a pass found on a date** — conforming a finding falsifies it), and the log's #65-era line (append-only). **Whether the two pass records should be conformed anyway is yours**
- **#73's `V-4` SECOND HALF IS EXECUTED.** #73 ruled **"keep as two, CROSS-REFERENCED"** and the cross-references had run **one direction only**. `RL-5` ruled both sets: entries **17 ⇄ 18** took reciprocal notes, and `legal-rule-registry-discovery-and-carrier-duties.md` took **three reciprocal dedupe notes** (197.2(a), 196.2, 198.2). **NOTES ONLY — no proposition and no Status line in that ALL-VERIFIED file was touched, and its 22 `VERIFIED` lines were counted before and after and are identical**
- **BOTH FILE STATUS HEADERS READ THE MIXED TRUTH (E-5), RE-DERIVED AT EXECUTION.** Neither file can honestly be described as ALL-UNVERIFIED any longer, and each header names the ROUTE-C entries that are unverified **by design**
- **`legal-rule-registry-discovery-and-carrier-duties.md` IS THE VERIFIED FILE** *(**AUD-3, CONFIRMED FALSE and re-derived again this refresh: 22 `**Status:** VERIFIED` lines against a header reading "ALL TWENTY PROPOSITIONS VERIFIED" — a bullets-vs-entries unit mismatch. NOTHING touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)); **TRCP 190.3(b)(3) is a VERIFIED entry under a wording flag — `Q-FE5-9`.** Criminal file: **entry 3 and 4a carry NO CITE — supplying one is YOUR act.** **Six flagged WL/slip cites survive uncorrected**, on 15/16/17/18/20b/21a. **`/rules` SEED ALL UNVERIFIED**
- **THE FOURTH FILE SITS OUTSIDE THE BACKLOG: `legal-rule-registry-draft-entries-medical-billing.md`**, headed ALL ENTRIES UNVERIFIED while its own entry Status lines read **DRAFT** — a second header/entry unit mismatch, re-derived this refresh. Whether that exclusion is deliberate is **`Q-STAT-2`, yours — the backlog figure above counts the two in-backlog files only**
- **THE SAME ABSENCE HAS APPEARED FIVE TIMES.** (1) **NOT ONE PRIVACY PROPOSITION** (`Q-WF-6`) · (2) #78's insurance narrowing · (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (`Q-QBO-6`) · (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** — thirteen terms, all zero · (5) work product — **now VERIFIED, and still only three entries.** **The registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** **FOUR research passes still stack behind `Q-STAT-5`'s one ruling**
- **THE RETRIEVAL GAP IS CLOSED (#76)** — the rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20**, and all 21 are retrieved against the official corpus. **RETRIEVAL IS NOT VERIFICATION.** *(Re-confirmed the hard way at #93: a session asserted the pass had never run, wrote a document on that premise, and withdrew it — **do not re-run this pass.**)*
- **THE WORKBOOK IS STALE IN SEVEN IDENTIFIED WAYS AND CARRIES A BANNER SAYING SO (`D-3`).** Its **rows are untouched and deliberately so** — it remains **the numbering authority `V5-IDS` and `Q-T19-1` rest on**. **The staleness is disclosed, not repaired**
- **REGISTRY RESEARCH SUPPORT — THIRTEEN DOCS, ALL PROPOSED OR RECORD-ONLY, NOTHING VERIFIED; every size RE-DERIVED THIS REFRESH:** cite-check · workbook v1 (**29,844 B** — banner'd AND FROZEN as the numbering record) · citator pass (**19,056 B**, untouched by design) · `statute-pass-registry-retrieval-2026-08-14.md` — its **§3** carries the corrected A-for-space normalizer and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess* · deadline-engine memo · V-5/V-6/V-7 execution record · worklist (#97) · sign-off record (#98) · `registry-records-look-staging-2026-08-17.md` (**53,672 B**) · `registry-verification-workbook-v2-2026-08.md` (**22,320 B** — the WORKING LAYER) · `fable-adjudication-record-2026-08-18.md` (**20,657 B**) · `registry-new-entry-drafts-2026-08-17.md` (30,728 B / 269) · `criminal-opinions-read-2026-08.md` (32,415 B / 321) · **NEW THIS BATCH: `costs-and-privilege-authority-read-2026-08-18.md` (19,158 B / 200 non-blank)**. **§3's normalizer must NOT be carried to the TRCP/TRE PDFs — raw TRCP `AA` count is 0, a different publisher**
- **`V-8` AND `V-9` ARE BOTH CLOSED — RULED 2026-08-18 (#100).** **`V-9` ADOPTED AS DRAFTED**, his words: *"Adopt as drafted, but also keep my ideas to possibly use when we implement it."* **CLAUDE.md rule 5 now carries a named-source fallback chain** — the court's own document; a paginated vendor copy stating on its face who delivered the opinion; or **your own identification, put to you and recorded** — with **cannot-identify-STOP as the floor** and unidentifiable entries **flagged, never staged for verification**. **`V-8` was ruled FRESH**: §0.1 gained **0.1.1 – 0.1.3** verbatim from the §B candidate text with 0.1.2 conformed to V-9, **and §C's four additions were each put separately and ALL FOUR ADOPTED as 0.1.4 – 0.1.7.** **`V-9-UI` is recorded as PROPOSED design input only; Q-6 still bars wiring the API into the app**
- **EVERY CANDIDATE THE RECORDS LOOK PRODUCED IS RULED (`RL-1`..`RL-6`, all CLOSED), AND NOT ONE VERIFIED ANYTHING.** **`RL-3`, three cite acts:** 20b (*Collins*) took the full parenthetical — **No. 02-14-00294-CV, Jan. 19, 2017, pet. denied, (mem. op.)** — and 15 and 21a each took **date + cause number**, with **no pinpoint and no designation notation, because none was available.** **`RL-2`:** the *Alford* supersession inference is retired for the caption facts, **and `registry-citator-pass-2026-08-13.md` was NOT touched**. **THE REPORTER-CITE FLAG SURVIVED EVERY ONE OF THESE ACTS, AND SO DID EVERY ENTRY'S UNVERIFIED STATUS — a cite act is not a verification.** **`RL-6`: the nine unnamed criminal opinions got a FULL READ (T-27), but the cite supply for entries 30 and 31 stays YOURS**
- **SEVEN NEW REGISTRY ENTRIES ARE RULED INTO EXISTENCE, THEIR TEXTS ARE DRAFTED AND IN THE REPO, AND NOT ONE EXISTS AS AN ENTRY.** `RL-4` (TRCP **193.4(a)**, **199.6**) · `WS-1` (192.3(h)'s **own-statement right**, 215.1(e) cited *within*) · `WS-2` (**194.1(a)**, **194.2(b)(9)**, **194.5**) · plus **`WS-3`'s**. **DRAFTED IS NOT INSERTED: no registry file was touched, the backlog stays 40** (46 on insertion, 47 with WS-3's). **TWO OF THE SIX ARE HELD (`Q-WS2-1`, now FIVE limbs): the carrier-duties file already carries a VERIFIED "TRCP 194" entry under an UNRESOLVED span flag, AND — found by the audit — the DESTINATION file already carries the VERIFIED 192.5(c)(1) entry the drafting record said it had checked for.** **AND THE RULING'S OWN CITE WAS WRONG: "194.2(9)" has no such subdivision** — **conformed to 194.2(b)(9) IN THE DRAFT ONLY and flagged as its own act (`Q-WS2-2`), never silently and nowhere else.** ***One asserted defect was itself withdrawn: entry F's claimed 199.6/193.4(b) asymmetry does not exist — TRCP 193.4(b) says it too.*** **`WB2-1`/`WB2-2` stand as ruled**

## Design-input memos — ALL PROPOSED, nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions. **No deadline engine is in the build queue.** **ITS HEADLINE IS A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, with 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) now allows a phase-scoped send-by Level 3 period.** **Five questions, NO durable IDs (ID-DL-1)**
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions. **PROVENANCE MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED:** TRCP 21(f)(5) and Statewide Criminal E-Filing R. 2.3(a) fix filing at **transmission to the filer's EFSP** (TIER A); a Bexar county-court sentence appears to fix it at **clerk acceptance** (TIER B), and **TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule** — exposure: transmitted late on a limitations date, accepted two days later (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**). **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing. All three rows stay ⬜.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9) — **and note what route (c) does NOT reach: a VERIFIED entry, whose rewording detaches an existing verification**. Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument** (its `doc_type` CHECK admits one value)
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** **IN-1 has NO design-doc home anywhere.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED and elaborated; its EXTRACTION half is UNRULED and only mapped.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all**, so a build implementing #64 alone ships with no VIN validation and no promotion gate, silently. **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR** — the crash date is itself under extraction
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN. All seven rows stay ⬜. SEVEN OF SEVEN GATED, ZERO SATISFIED.** **THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **ITS ROW NAMES ALL THREE CONSUMERS (#94)** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`**: even the metadata-only `callRecords` version needs a **scheduled runner to beat a 30-day retention window.** **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5)**
- **PR-3 RE-PARENTING PROPOSAL (#86) · QBO MEMO (#87) · RE-1 MEMO (#88).** QBO's **HEADLINE: THERE IS NO READ-ONLY OAUTH SCOPE** — one accounting scope grants **full read/write**, so **"read-only first" can be a property of the code but never of the grant** (`Q-QBO-2`); **the economics are INVERTED** (Intuit meters data-**OUT**, free tier 500k credits/month, **BLOCKS rather than bills**); and **the constraint with legal teeth is the one the platform helps with least** — QuickBooks records trust/operating separation as a **naming convention and enforces nothing**, so a read integration **can display a per-matter trust figure that is wrong with the authority of a computed number** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED.** RE-1's: **ONE OF THE THREE NAMED TRIGGERS MEANS TWO DIFFERENT THINGS IN THE RECORD, FROM THE SAME SESSION** (`Q-RE-1`), and **SOURCING NAMES NO SOURCE FOR THE TEXAS DISCIPLINARY RULES — TESTED, NOT ASSUMED:** the bulk corpus holds **494 chapter files** and **the TDRPC live in a Government Code appendix it does not publish**; not academic, since **Gov't Code §82.065(b), §82.0651 and Penal Code §38.12(c) each incorporate them BY NAME** (`Q-RE-9`). **THE ONE THAT PAYS FOR THE SECTION: TDRPC 1.04(f)(2) IS THE FIELD LIST** — consent **in writing and PRIOR to the referral**; **1.04(g) makes the failure economic and silent — an unconfirmed arrangement does not void the referral, it STRIPS THE FEE.** **`P-RE-1..12` ALL UNVERIFIED**
- **COMMUNICATIONS-LOG INGEST MEMO (#89)** — Task 15. **THE DISPATCH'S OWN WORD WAS CONTRADICTED IN TERMS:** it called two constraints **ruled**; the log records the exchange as *"Claude recommendations, unruled."* Both trace to one paragraph of `future-modules-capture-2026-07-28.md` §3 — **the same gate as `Q-QBO-1`, firing a second time** (`Q-COM-1`). **THE TWO RESOLVE DIFFERENTLY:** the scope exclusion is honored and **costs nothing**; the architectural commitment is **priced for the first time** — a second public HTTPS endpoint, private-key custody, an outside vendor's discretionary approval, and an unestablished metering position, **for a channel the firm does not use.** **A NEW GATE CLASS (`Q-COM-5`): a third party's DISCRETIONARY APPROVAL.** **THE SMS HEADLINE LANDS ON THE STACK ALREADY COMMITTED:** Teams SMS needs three licences plus A2P Brand *and* Campaign approval, is **1:1 with no MMS** — so it **cannot receive the scene photo, the insurance card, or the bill** — and **is apparently not exposed to Graph at all**, on the memo's weakest source, marked `[C]` (`COM-LOOK-3`). **THE IDEA WORTH ITS OWN LINE: a communications LOG and a communications ARCHIVE are different products, and only the archive needs content** — `callRecords` gives who/when/how-long with **no audio and no transcript, so no privilege, consent or PHI problem attaches, because nothing was captured.** **AND THE ONLY MICROSOFT-NATIVE PATH TO CALL CONTENT INVERTS RULING 8.3** — compliance recording is **policy-based and always-on** against 8.3's **opt-in per call** (`Q-COM-8`). **`Q-COM-11` IS RULED AND EXECUTED (#94); `Q-COM-10` and `Q-COM-12` REMAIN YOURS**
- **WITNESS-STATEMENT CONVERSION MEMO (#99) + THE `WS-3` AUTHORITY READ (#102, T-28, CORRECTED #103).** The memo's headline stands: **on TRCP 192.3(h)(2) the line is crossed at RECORDING, not at transcription** — the recording is already a witness statement — **so a gate placed only at transcription sits downstream of the event it exists to control**; a witness statement is a Rule 194.2(b)(9) INITIAL disclosure owed WITHOUT a request (194.1(a)), against which 194.5 permits **no work-product assertion**; *Trahan v. Lone Star Title*, 247 S.W.3d 269, is the same failure sanctioned. **THE ATTORNEY-CLIENT LIMB IS DECIDED BY TWO PUBLISHED INTERMEDIATE OPINIONS, majority authorship confirmed on the face of each (V-9)**: *In re Fontenot*, 13 S.W.3d 111 (Livingston, J.) and *In re ExxonMobil*, 97 S.W.3d 353 (Seymore, J.) — a witness statement inside a confidential attorney-client communication **is privileged**, and ordering its production is an abuse of discretion. **BOTH REACH IT THROUGH COMMENT 9, NOT THROUGH 192.3(a) BY NAME** (`Q-WS3-2`). ***CORRECTED #103: the "adjuster or third-party witness is never privileged" paragraph is WITHDRAWN*** — *W&G Trucking*'s declarant was **a party** and the case turned on a **failure of proof**, *ExxonMobil* protected a recounting of a third party's words, and *In re Arden* appears to hold an **adjuster**-recorded statement privileged (`Q-WS3-6`). **Every proposition UNVERIFIED; no entry created**
- **T3 PILOT PROTOCOL (#90) · SESSION-LOG TOC (#91) · GO-LIVE RUNBOOK (#92).** The **TOC** (`docs/specs/session-log-toc.md`, **350 non-blank / 374 raw, 67,682 bytes — RE-DERIVED THIS REFRESH**) carries **the finding that matters to every reader of the record: the log runs TWO independent numbering systems and neither covers every entry** — `#nn` and **runner ordinal** — with **eight runner batches carrying a `#nn` instead of an ordinal** and **one carrying neither**; a reader assuming one system mis-files about a quarter of the log. **NO ENTRY ANYWHERE CARRIES `#1`** (`TOC-1`). **`TOC-5` still accrues without a ruling.** *(**REGENERATED THIS BATCH — SEVENTH EDITION, 220 rows, its FIFTH firing under `TOC-4`.** Byte-proof re-run against the sixth edition's basis: the **218 prior rows' entry text is an exact SUFFIX of the log at HEAD** — purely prepended, 13,103 bytes and two new headings above it — **and the preamble is byte-identical again**, all 2,771 bytes. **NO carried row was edited this refresh, and the absence is stated: the two new entries issue no correction against any earlier one.** **No FIFTH derivation trap appeared; the four carried ones were re-confirmed live.** Buckets re-derived: **104 numbered (#2–#105) + 55 ordinals (5–59) + 1 unnumbered runner + 60 other = 220.** **`TOC-2` GAINS NOTHING.**)* The **runbook**'s gate resolved an eighth way: **DERIVED like #91 but NOT REGENERABLE** — its "who acts" and "done-when evidence" columns are judgement, so an appended gate silently makes it incomplete (`GLR-3`)
- **PRIVILEGE-TIER UNIFIED VOCABULARY (#102, T-30, CORRECTED #103) · DESIGNATION-SOURCE METHOD RESEARCH (#102, T-31, CORRECTED #103).** **T-30's headline SURVIVED THE AUDIT BUT ITS ARGUMENT DID NOT:** `WS-4` ruled a fourth `privilege_tier` value, the column holds ONE value, and a client-interview recording is **both** privileged and a witness statement — **but the overlap is a GENERAL cardinality property, not special to the fourth value** (`'attorney-client'` and `'work-product'` already overlap), **so the argument defeats the three-value column already in the schema, and the PRIOR question — should `privilege_tier` be exhaustive at all? — has never been ruled** (`Q-COM-10-A`, rewritten). **The 192.5(d) over-read is corrected** — the rule's *"For purposes of these rules"* qualifier was dropped — so conforming `'privileged'` → `'attorney-client'` is now framed as **a preference, not a cure** (`Q-COM-10-B`). ***And the migration header's copy-forward is corrected: the registry entry is VERIFIED, not UNVERIFIED*** — the `#13`/`R-3` class, caught before it reached tracked content. **The migration remains AUTHORED, UNRUN AND NOT AUTHORIZED; the `src/` half is specified IN WORDS and deliberately NOT drafted** (`Q-PR3-1`). **T-31 stands on its structural finding — Texas has TWO independent designation axes** (TRAP 47.2(a) Opinion vs Memorandum Opinion, civil AND criminal, memorandum being the **default** under 47.4; and 47.2(b) publish/do-not-publish, **criminal only**) — **but its two headline claims are RETRACTED:** the *"174,292 to 1"* measurement **had no control arm** and the unfiltered query returns the same denominator of 1, so **the phrase did no work**; and **`"full precedential"` appears 0 times in the TRAP PDF** — 47.7(b) is entirely negative and preservative, the affirmative proposition sitting in an **amendment comment whose year is unconfirmed** (`Q-DES-1`, premise corrected). **AND TRAP 77, NOT 47, GOVERNS THE CCA — 77.3 BARS citing unpublished opinions as authority** (`Q-DES-6`, new). **NO METHOD ADOPTED, no SOURCING amendment, no §0.1 edit, nothing built**
- **CRIMINAL OPINIONS READ (#102, T-27, CORRECTED #103) — AND THE CORRECTION RAN AGAINST THE CLIENT, WHICH IS WHY IT MATTERS MOST.** The read stands: **eight of the nine never mention art. 102.073**, only *Middleton* touches it and only as the State's argument expressly not decided. **BUT *LaPorte* DOES NOT SPLIT BY PAGE.** Both halves are on **415**, in adjacent paragraphs — so **the instruction "Never cite 415" is WITHDRAWN**, having barred counsel from the very sentence stating the rule. **The corrected remedy is a parenthetical — *"overruled on other grounds by Ex parte Carter"* — not a pinpoint** (`Q-RL6-3`). Two more: ***Carter*'s reaffirmation is a FOUR-vote plurality and dictum** against **five** votes for the overruling; and **"may be raised at any time" was NARROWED, not overruled** — it does not control habeas cognizability, while on direct appeal § 3.03 remains a Marin waiver-only right. **And FOUR, not five, are "DO NOT PUBLISH"** (`Q-RL6-5`). **Four of the nine remain author-unidentifiable under V-9 and are flagged, never staged**
- **QR-6 ADJUDICATION (#94) · WORDING ADJUDICATION (#95) · V-EXEC (#96) · TASK 19 WORKLIST (#97) · SIGN-OFF WALK (#98) · TOC REGENERATION (C-2, unnumbered) · CHAT-DISPATCH v3 (#99) · FABLE ADJUDICATION (#100) · DT-1 CORRECTION (#101) · CHAT-DISPATCH v4 CHAIN (#102) · AUDIT CORRECTIONS (#103).** **#98 is the first session to move a registry Status line to VERIFIED**, and it moved twenty-four; **C-2 is the first Code session to decline a `#nn`**, which `TOC-6` has now made the rule; **#100 is the first to move a Status line the OTHER WAY**; **#101 is the first correction entry whose whole subject is a DATE, and it changes no file**; **#103 is the first correction entry produced by an adversarial audit of a prior session's own output, and the first to correct five tracked documents in place.** **`docs/prompts/CHAT-DISPATCH-v3.md` and `-v4.md` were NOT edited at #100, #102 or #103; amending a repo-tracked prompt is yours**

## For design side
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built. *(Live tension, now a queue item: **`Q-PR3-1`** asks whether a design-side `src/` read through the device bridge is sanctioned at all — the working-set policy says design sessions do not read source, that sentence predates the bridge, and **every finding in the #86 proposal is undrivable without such a read**. Yours; nothing from `src/` entered project knowledge.)*
- **INSTRUCTIONS: `v22` IS IN FORCE — OBSERVED 2026-08-17 (#102) IN THE RUNNING SESSION'S OWN LIVE INSTRUCTIONS FIELD**, the #82/A-1 mechanism for the **EIGHTH consecutive time. YOUR "paste v22" ITEM IS CLOSED.** v22 carries **two trigger-#3 firings from one session**: the `V-9` amendment to CLAUDE.md's binding rule 5, and the `TOC-4` runner amendment (v10 → v11). **TRIGGER #3 HAS NOW FIRED AGAIN (#105) → v23, WHICH CARRIES `PF-1`, AND THE PASTE IS YOURS AND IS OPEN.** **Verbatim amendment text — header block, the new Binding-conventions bullet, the trigger-#3 history line — is staged at `fable-adjudication-record-2026-08-18.md` §3 of the spend-down section; full-text assembly is Opus lane and Code never edits the instructions.** **The standing lesson is the reason this row exists: a running session can read its own instructions; a Code session structurally cannot, so this file must never assert a paste state it did not learn from a design session — v23 is therefore recorded as PENDING, not as done.** DT-1 remains in force
- **DT-1'S CODE-SIDE ENFORCEMENT PROBLEM INVERTED THIS BATCH, WHICH IS STRONGER EVIDENCE THAN THE ORIGINAL FINDING.** The fifty-sixth invocation's Bash shell **reported GMT and silently ignored `TZ`**, so a bare `date` would have stamped it wrong. **This batch's Bash shell is the other way round: bare `date` → `2026-08-18 09:55:35 CDT`, which is RIGHT, while `TZ=America/Chicago date` → `14:55 GMT`, identical to `date -u`.** PowerShell — the authority — reads **`2026-08-18 09:55:33 −05:00`, `Central Standard Time`. **So neither command is safe by nature; the shell decides which one lies**, and "the Code session is local, therefore safe" stays false. `#101` leaves whether DT-1 gains an execution point to you; this line adds that any such point must read the wall clock, not a shell
- **SOURCING IS A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED).** Statutes, rules and regulations get a named-source rule **parallel to TOOLING**: official corpus / clean-authority PDFs / eCFR API, **each source named per item**, currency never inferred from a document, extraction artifacts **characterized rather than guessed**, and **retrieval is not verification**. **Q-STAT-2 through Q-STAT-6 stay OPEN**, and **#88 found the convention's first proven GAP — none of its three layers reaches the disciplinary rules (`Q-RE-9`). SOURCING was NOT amended**
- **CHAT-DISPATCH v4 IS EXECUTED THROUGH T-31, ITS OUTPUT AUDITED (#103) AND THE AUTHORITIES IT SURFACED NOW READ (#104); ONLY `T-26` REMAINS AND IT IS INTERACTIVE AND NEEDS YOU LIVE.** `docs/prompts/CHAT-DISPATCH-v4.md` (79 non-blank / 94 raw). **`RR-1` ran at #102 and caught two cross-task interactions — and caught NONE of the twenty-two defects the audit found, because RR-1 checks documents against later RULINGS, not against PRIMARY SOURCES.** **THAT GAP IS NOW CLOSED BY RULE: `Q-AUDIT-1` IS RULED (#105) AND `PF-1` IS STANDING** — see the preflight row below. **CODE-DISPATCH v3 IS COMPLETE.** **The cheapest high-value move left is still Group A — entries 2, 12, 13, 29, 32, one look each against the executed text, entry 13 first**
- **HK-7 STAYS OPEN, AND ITS ANNOTATION GAP IS RECORDED IN THE QUEUE ITSELF — RULED 2026-08-18.** The ruled remedy was a **FORWARD gap-note, not a reconstruction:** the row carries that annotations for **#94–#98 were recorded only in BUILD-STATE** and that **this note records the gap, not reconstructed annotations. Nothing was back-written** (K-6/K-7). **The count itself is still not reliable from either side.** **RE-CHECKED NATIVELY THIS BATCH against DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL still ABSENT here.** **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**
- **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY. **CD-2 is NOT an open design question** — it is a coverage audit of a ruled taxonomy. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. *(#86 read `roster.ts` and found the `sideSetFor()` defect there — evidence the gap was load-bearing.)* **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SIX REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*`. First three SPENT as CD-1 input; UIM + deficiency seeded the **DE series**. **No capture in the repo is unreconciled.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2
- **YOUR HAND, THE LAUNCH PATH IN ORDER: run the CD-1 migration** (back up, paste alone, answer its five checks in words) → **gate 1 Supabase Pro** → **gate 9 production SMTP** → gate 3 RLS test **including the two new tables** → gates re-check + the instructions version. *(**`Q-PR3-3` touches step 1: the PR-3/CD-1 order has a consequence and is unruled.**)*
- **STILL YOURS AND ALL SMALL:** **`T-26`**, the Group A looks, **interactive and on no queue row at all** — it survives only in `docs/prompts/CHAT-DISPATCH-v4.md`, and minting a row for it is yours, **now flagged by FIVE consecutive runner batches** · **`Q-COM-10`**, which closes the moment you adopt the unified `privilege_tier` list — **`Q-COM-10-A` IS RULED AND CLOSED (#105), so `B`–`F` are unblocked and are the remainder** · **the cite supply for entries 30 and 31** (`Q-RL6-1`, `Q-RL6-2`) — `Q-RL6-1` now resolves to ***Hurlburt*** (bridge held) and ***Pharr*** (negative side, unread); **its *Whaley* option is dead on the DO-NOT-PUBLISH finding**, and **entry 30 still got nothing** · **`V5-ATTRIB`**, which `RL-1` deliberately did not resolve
- **ALSO YOURS, CARRIED.** **`V5-IDS`** — derived IDs were executed on the #66 precedent, **but that precedent supports the NAMING and not the FILE SHAPE, so the two registry files remain shaped differently** · **`V5-ATTRIB`** — the split could not supply which case gave which half, so **all six entries carry the proposition whole**, and `RL-1`'s adopt-for-both **widened the shared text without attributing any of it**
- **ALSO YOURS, LONGER-CARRIED: paste instructions v23** (trigger #3, same day) · **run `db/migrations/2026-08-16-privilege-tier-no-default.sql`** — *(**untouched by #105**: its only DDL is four `drop default`/`drop not null` statements and it changes no CHECK and no vocabulary; the four-value migration #105 sends back for a redraft is PROPOSAL TEXT in `privilege-tier-unified-vocabulary-proposal-2026-08-17.md` §4, **not a file in `db/migrations/`**)* · **re-upload SKILL.md v2** (SK-v2) · **the sync-picker clicks** (Q-3) · **telemetry lockdown on the P1** · **HK-6** · **HK-7 re-grant each session** · **V-2, Ochoa first** · **OBS-1** · **CD-4 – CD-13** · **Q-STAT-2 – Q-STAT-6** · **the deadline memo's five + ID-DL-1** (**EIGHTEEN** packets; carried from the runner's own prior derivation, this packet stating no ordinal either) · **the Task 8 specs' 22 + 3 looks — Q-FE5-9 first, it sits inside a VERIFIED entry** · **the Task 9 specs' 18 — Q-IN1-8 is a one-word veto on the whole IN series** · **the Task 10 spec's nine — Q-IN2-2 is cheapest** · **the Task 11 spec's ten + `WF-LOOK-1`** · **the Task 12 proposal's seven + `PR3-LOOK-1`** · **the Task 13 memo's eight + four looks** · **the Task 14 memo's nine + three looks** · **the Task 15 memo's remaining eleven + four looks + five propositions** · **the Task 16 protocol's seven — `Q-T3P-1` is the cheapest question in the whole queue** · **the Task 17 index's five, now three** · **`GLR-2` and `GLR-3`** · **`OPEN-5(b)` and the `AUD-5..9` one-word apply** · **`READ-A`** · **the workbook's carried `Q5`** · **STILL THE LARGEST SINGLE BLOCK, AND IT SHRANK FOR THE FIRST TIME: the CHAT-DISPATCH v4 chain's THIRTY-FOUR IS NOW THIRTY-TWO OPEN + TWO CLOSED** — `Q-WS1-1`, `Q-WS2-1`–`3`, `Q-WS3-1`–`6`, `Q-RL6-1`–`6`, `Q-COM-10-B`–`F`, `Q-DES-1`–`7`, **`Q-AUTH-1`–`4`**; **`Q-WS2-1` blocks two drafted entries, `Q-DES-4` is needed before any retrieval test, and `Q-AUTH-1` — authorize the *Ex parte Pharr* read — is now the cheapest item with real leverage, on entries 30/31** · **delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`, CONFIRMED STILL PRESENT this refresh**
- **THOSE THREE ARE NO LONGER LOCATOR-ONLY — ALL READ AT #104, AND TWO CHANGED WHAT THEY WERE THOUGHT TO SAY.** ***In re XL Specialty*, 373 S.W.3d 46 (Tex. 2012)** does NOT undercut *Fontenot*: it runs the OPPOSITE direction (insurer claiming, insured held not its representative), **confirms the TRE 503(a)(2) test** and adds an adverse-posture boundary (`C-XL-1`, `Q-WS3-5`). ***In re Arden*** is **the *Fontenot* pattern, not a third-party rule** — Arden was the DEFENDANT and it was HIS OWN statement — and it **corroborates #103's work-product correction** (`C-ARD-1`, `Q-WS3-6`). ***Whaley*** — see the runner finding below. **Still: reading is not verifying, no cite is selected, and the supply for 30/31 is yours**
- **METER — THE FABLE BAR RESETS TUESDAY 2026-08-18 ~4:00 PM CENTRAL; THIS BATCH CLOSED BEFORE THAT, AND UNSPENT FABLE IS LOST.** #100 spent its bar deliberately on adjudication rather than drafting, putting 24 items; **that trade settled well — the follow-on drafting it routed to Opus landed at #102, and the audit that corrected that drafting also ran on Opus.** **Design input, not a ruling — a snapshot, never a standing figure.** **Strongest remaining claim on Fable time: the CHAT-DISPATCH v4 block, now THIRTY-FOUR questions, which is adjudication work of exactly the shape #100 did well — and `Q-AUDIT-1` inside it is a convention ruling rather than a legal one, now with a fourth data point behind it.** **`T-26` needs you rather than a model tier**
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. NOTHING BUILT — and the T-24 SPEC DRAFT (#99) DOES NOT PUT IT IN THE QUEUE:** it proposes no table, column, endpoint or migration. **THE DISPATCH'S TAXONOMY LINE WAS ONE CATEGORY SHORT — the ruled list is SEVEN (BP/STO/OO/EV/CONTRA/PRIV/RFP-form).** **CONTRA HAS NO VERIFIED AUTHORITY AT ALL** — it rests on entries 2 and 12, both in Group A awaiting one look each; the conference fuse rests on entry 13's ADOPTED-but-UNVERIFIED wording, **so the fuse's whole legal substrate is pending one look**
- **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated, **and the #85 spec confirms every gate is still shut**. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` is recorded as sitting untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand.** *(**Re-checked rather than inherited this refresh: at this batch's Step 1 inventory `ls -la inbox/` on THIS machine returned exactly one file — the batch's own packet zip — and no `Probate Corpus.zip`. Whether this machine is the P15 is not determinable from the repo, so the claim is carried with that annotation rather than deleted.**)* **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-6 + MM-1 + OPEN-5(a) + TOC-4 ALL CLOSED; runner v11** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy**; the `/queue-runner` command is a pointer to it). **Your hand: the P15 user-level copy deletion.** They stand as ruled: (a) a stated verification **names the command that produced it**; (b) the queue merge is **TWO acts**; (c) **pin identity at Step 1, delete BY EXPLICIT FILENAME**; (d) format comes from the runner's own Step 4 rules, **never copied from an entry authored under an earlier version**; (e) **a packet-added act in no row and no Step 4 item needs your in-session authorization**; (f) **a docs-only batch records the health-check skip explicitly — fired a TENTH consecutive time this batch**; **and `TOC-4`: the index regenerates every runner batch — its FIFTH firing here.** **QR-3 STILL CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74, #93): the mount reads a false DIRTY of CRLF-only files, and `git fetch`/`ls-remote` fail there with `HTTP 403 from proxy`
- **`QR-6(e)` DID NOT FIRE THIS BATCH — SIX FIRINGS IN TEN BATCHES, and the absence is stated rather than assumed.** #94: a stranded `.git/index.lock` the packet routed nowhere. #95: **a confirmation that did not reach the unrouted act**. #96: three at once. #97: **two arithmetic errors inside the packet's OWN CONTENT.** #98: **a ruling being CLOSED whose second half was never executed.** #99: nothing. **#100: the stranded lock again.** **#102, #103, #104 and #105: nothing — every act sat in a routing row or a Step 4 item.** **THE #105 PACKET'S ARITHMETIC AND REPO VIEW BOTH HOLD:** its §1 states HEAD `fdcc9d8`, which is exactly what the live gate read — **zero reconcile delta** — and its §7 five rows match what the merge found. **CARRIED AND STILL TRUE OF #103's PACKET: it MISCOUNTED ITS OWN OPEN ITEMS IN TWO DIRECTIONS** — its §7 header says *"Twenty-eight items"* and `#103` says *"26 → 28"* while the shipped table holds **THIRTY**, and its §4.7 amend list named **eight** rows where its own table marks **nine** (`Q-DES-4`). **Neither figure was edited — the entry was ordered verbatim and a change to packet content is yours** (`Q-T19-3`). Also carried: **`T-26` still has no queue row**, FIVE consecutive batches
- **RUNNER-FOUND AT #104 AND NOT EDITED INTO THE DOCUMENT — TWO DEFECTS, ONE OF THEM HIGH.** **(1) *WHALEY*'S PUBLICATION NOTATION IS IN THE COPY, AND IT SAYS “DO NOT PUBLISH.”** The document asserts it *“could not be found anywhere in the copy I read”* and builds `Q-AUTH-2` on that premise. It is at the foot of the FLP-served opinion in the Dallas court's standard form — *“/Bill Whitehill/ … **Do Not Publish** — TEX. R. APP. P. 47.2 (b) — 181255F.U05”* — so **under TRAP 47.7(a) *Whaley* has NO precedential value**, `Q-RL6-1`'s option (d) is dead, and **FLP reporting `status: “Published”` for this very opinion is a live proof of #103's rule that the FLP field is not evidence of a Texas designation.** **(2) A V-9 BREACH ON *ANASTASSOV*:** it is characterised at length while authorship is recorded as unchecked; **PD-0848-20 carries TWO FLP documents, both `010combined` with empty `author_str`**, and neither the one read nor the existence of a separate opinion is named. **Identification was one query away from a V-9-permitted source** — the court's own document reads *“SLAUGHTER, J., delivered the opinion for a unanimous Court. YEARY, J., filed a concurring opinion”* — so the holding is **unanimous and stronger than claimed**, but the method failed. **Also: TRAP 77.3 was never applied to *Anastassov* though the document states the bar itself.** **The queue rows carry these; the document and #104 stand verbatim (`Q-T19-3`)**
- **THE MULTI-AGENT READ-ONLY PREFLIGHT IS NOW STANDING LAW — `Q-AUDIT-1` RULED 2026-08-18 (#105), `PF-1`.** **Trigger: any packet carrying a legal characterization or a proposed registry entry. Run by the DESIGN SESSION before shipping, pre-`inbox/`** — the sending-side sibling of RR-1, and the only house mechanism that checks a document against its PRIMARY SOURCES. Discretionary preflights on other content stay available at your direction, per instance. **It earned the rule four times out of four, each on a different failure class:** #96, a staged block **reworded a citation flag inside an act ruled verbatim**; #97, **two arithmetic errors inside a staged document's prose**; #103, **twenty-two defects across five research documents**, including a manufactured legal gap the project already held the VERIFIED answer to; #104, ***Whaley*'s DO-NOT-PUBLISH notation** sitting in the copy the packet called silent. **None was taken on faith — each time the material findings were re-derived, and at #96 one was REJECTED.** **OPERATIONAL WARNING THAT SURVIVES THE RULING: at #104 the agent fleet DIED — seven agents, twice, all on `API Error: 529 Overloaded`, zero tokens — and the harness returned `{"surviving_defects":[]}`, a TOTAL FAILURE WEARING THE COSTUME OF A CLEAN BILL OF HEALTH.** It was re-run **inline**, which is what found the notation. **PF-1 fires on the SENDING side, so no queue-runner batch can satisfy it**
- **THE CLASSIFIER CAN REFUSE AN ALLOWLISTED BARE COMMAND — RULED AN OPERATIONAL NOTE IN v20 (#94).** The forty-seventh invocation's **bare** allowlisted `git push origin master` was refused on **both** shells, retiring "prefer bare forms" as sufficiency guidance. **A remotely-driven close-out can strand at the push with permissions fully in place; `QR-5(a)` plus the QR-3 ahead-stop is the designed catch; never conclude a batch landed without `git ls-remote`**
- **THE STALE `.git/index.lock` DID NOT RECUR — NINE-FOR-SIXTEEN, AND THE FIX NOW HAS TWO DATA POINTS RATHER THAN ONE.** The observed cause stands: a design session's bridge `git status` makes git emit `warning: unable to unlink '…/.git/index.lock': Operation not permitted`, and **the 0-byte lock strands because the mount DENIES `unlink`.** **#102 was the first design session in the recorded series to leave the checkout clean, and #103 is the second** — both used `GIT_OPTIONAL_LOCKS=0` or plain file reads and said so, **and the runner checked the claim rather than taking it: `ls .git/index.lock` returns no such file.** So the practice, not the mount, is what changed. **A bridge read is not evidence about origin but it IS evidence about local files**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and now `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** (Q-IN2-1). Synthesis `cr3-field-code-map.md`, **PROPOSED input — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: the #62/#63 and #64 batches were VERIFIED design-side FULL-TEXT on 08-13 (#65)**; **#67 verified landed at the top of #68.** Still UNREVIEWED, **recomputed from the log at HEAD this refresh (A-4, ruled #82; now runner text):** the carried **#31–#33** material, **#37–#61's own routing**, and **#75 through #105** together with the runner lines interleaved among them **through the fifty-ninth** and the unnumbered C-2 Code-session entry — do not copy #36's clearance forward. *(**Honest limit, stated rather than smoothed: the `#75` floor rests on no explicit clearance.** The log positively clears #36 at #37, #62–#64 at #65, and #67 at #68 — but **#65, #66 and #68–#74 were DROPPED out of the round-trip lines rather than cleared.** The floor is inherited from the log's own round-trip chain, the derivation A-4/AUD-7 blessed; **"#65, #66 and #68–#74 are reviewed" is UNDETERMINED from the files.**)*
- **Everything awaiting your ruling is in `docs/specs/attorney-review-queue.md`** — reconciled through **#105**, **read from that file's own Status header at HEAD (A-5) after this batch wrote the sentence as its queue merge's second act (`QR-6(b)`), and stated as such rather than as an independent check.** *(#92 and #93 are still absent from that header's reconcile chain — a known, reported gap.)* **This batch CLOSED TWO rows (`Q-AUDIT-1`, `Q-COM-10-A`) — the first closures anywhere in the CHAT-DISPATCH v4 block — ENTERED one (the v23 paste, which had no row anywhere), ANNOTATED `Q-COM-10-B`–`F` add-only as unblocked, and minted NO duplicate row: verified mechanically at 32 open / 2 closed.** **`TOC-4`: regenerated here, SEVENTH edition, 220 rows, riding this commit. `TOC-6` IS RULED — `#nn` IS DESIGN-ONLY.** **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f into case-heartbeat-design.md §8. Carried: no law-change ledger, Outlook unreviewed
