# BUILD STATE — brennan-case-manager
Commit: 357ca8e (every claim below re-checked at this commit)  |  Branch: master  |  Generated: 2026-09-11 Central (one hundred forty-first refresh)  |  **WRITTEN BY THE QUEUE RUNNER (batch 96, the NINETY-SIXTH invocation) on a DOCS-ONLY batch — **no `src/`, `db/` or `supabase/` file was read at all**, so every `src/`- and `db/`-derived figure below is CARRIED from the `ea5675b` CODE refresh and is MARKED as carried (`QR-6(a)`); every count over `docs/` was re-measured here.** ⛔ **`CAP-4` BOUND; SHORTFALL NAMED — entered the refresh at 99,915 B, stands at 99,975 B, 25 B under the 100,000-byte ceiling, 136/150 non-blank (150 raw). 29 PASSAGES REMOVED, 7,609 B — 4 DISPLACED outright, 25 SUPERSEDED in place — NOT ONE AN EXISTENCE CLAIM**; each rides this batch's runner line VERBATIM under `DISPLACED FROM BUILD-STATE (CAP-4)`, the log's only copy, with where its substance still stands. ⛔ **`AS-Q4` IS AMENDED IN ONE LIMB (`#154`) AND THE BUILT COLUMN IS NOW A SPEC-VS-CODE GAP** — extracted chronology text leaves the database for a file in the matter's OneDrive folder; the row keeps drive item id + content hash and NO TEXT. **UNBUILT, SLICE UNDRAFTED — `Q-API-20` RULED *"After"* (`#155`): drafted after the walk, once `Q-API-18`'s facts are in; detail beneath FORM ENGINE. FLOOR CAUTION: NO REAL CHRONOLOGY MAY BE DROPPED until it lands — the built path would put PHI text in Supabase. The drop zone stays FIXTURE-ONLY; every other floor item is unchanged.** ⛔ **DECISION 1 (`#154`): PHI LEAVES SUPABASE** — the five PHI-bearing disclosures tables are to move to a Postgres inside the Microsoft tenant (Azure Database for PostgreSQL); Supabase keeps the case core and pointers on Pro. **NOTHING MOVED; NO AZURE RESOURCE EXISTS.** The HIPAA add-on / Team plan is NOT being bought (≈ $714/month before the add-on's unpublished price). 🟡 **`Q-WF-4` IS RULED IN SHAPE (`#154`, item 7) — THE ROW EXISTS AND IS NOW 🟡**, a composite decided now and built in Wave 3, each build its own authorization. **WF-2–WF-8 STAY ⬜, SEVEN OF SEVEN GATED, ZERO SATISFIED.** ✅ **THE CC-1 RULINGS AND ADDRESS-MODEL SLICE IS BUILT** — **fixture-only, EXCLUDED from the GL-1 floor, and walked by clicking in demo mode.** ✅ **EVERY MIGRATION WRITTEN TO DATE HAS RUN, AND NO MIGRATION IS PENDING.** The two `CCS-1` migrations ran on LIVE by his hand 2026-09-07 (`#150`); record at `docs/record/ccs1-migrations-run-2026-09-07/`. ✅ **THE GO-LIVE FLOOR IS COMPLETE — (1) THROUGH (5).** **GO-LIVE ITSELF REMAINS HIS ACT ON HIS DAY.** Gates 7/8 still gate the first real EOB / AnalysisRun. **The one LIVE matter is a TEST record (his word).** **“Allow new users to sign up” is OFF, measured live.** **NO VENDOR IS WIRED AND NO REAL RECORD CAN REACH A MODEL: the only writer is the FIXTURE writer, in both modes, with zero network calls; `H12-v` and the BAA are untouched.** ⚠ **NOTHING WAS BUILT THIS BATCH — `#155` IS A RULING SITTING, DOCUMENTATION ONLY.** ✅ **`FOS-1` RULED YES (*"Yes"*, 2026-09-10): THE FIRM-OBLIGATIONS BUILD SLICE, `docs/specs/firm-obligations-build-slice.md`, IS BUILD-AUTHORIZED AND NOT YET BUILT** — kickoff prompt `docs/prompts/PROMPT-firm-obligations-slice-build-session.md`; a separate Code session Michael fires, the queue runner BARRED; ONE migration to be WRITTEN AND NOT RUN by that build; no client data, OUTSIDE the GL-1 floor. ⚠ **INSTRUCTIONS `v32` DELIVERED 2026-09-10 (`#155`, trigger 3) — PASTED OR NOT IS HIS WORD; `v31` is the last version confirmed in force by a live read; the repo cannot verify the field and does not.**
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*
***DO NOT CITE THIS FILE BY LINE NUMBER IN A PERMANENT ENTRY.** RULED PROJECT-WIDE 2026-08-16 (#94, cite-stability, riding v20): line cites are permitted only into **append-only** files; **prepending** files (the session log) and **wholesale-rewritten** files (this one) are cited by heading or quoted sentence.*
*EVERY COUNT OVER `docs/` WAS RE-DERIVED THIS REFRESH, NOT CARRIED (`OPEN-5(a)`). **THIS IS A RUNNER REFRESH ON A DOCS-ONLY BATCH THAT READ NO `src/`, `db/` OR `supabase/` FILE, SO THOSE FIGURES ARE CARRIED AND SAID TO BE (`QR-6(a)`):** **28 route paths in `src/App.tsx`, `SCHEMA_TABLES` 46 (45 policy-true, 1 not), `STORE_VERSION = 16`, 11 files in `db/migrations/`, 46 `create table` in `db/schema.sql`, tests 746 across 48 files, 46 seeded templates, and the `ALTER DEFAULT PRIVILEGES` occurrence count** — every one MEASURED at the `ea5675b` CODE refresh or the batch-92 read and NOT re-measured here, because this batch read no file outside `docs/`. **Byte figures come from WORKING-TREE reads (`wc -c`), never `git show` and never a file just restored by `git checkout` — both misreport. Line endings re-checked by BINARY read, not `grep`/`file(1)` — neither is evidence about line endings; a bytes read is.** Every file this batch touched is LF, verified by a bytes read (0 CRLF). **⛔ TWELVE consecutive refreshes have now paid `CAP-4`: the BYTE ceiling binds this file, and every fact this session adds costs a paragraph somewhere else.*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS COMPLETE — ITEM 7 RAN AND
VERIFIED 2026-08-19 (#113) — AND SO DID GATE 10's MIGRATION, PASTED AND VERIFIED BY YOUR HAND THE SAME DAY, ALL SIX CHECKS ANSWERED. ✅ **EVERY MIGRATION WRITTEN TO DATE HAS RUN — INCLUDING THE `CCS-1` BUILD'S TWO, BY MICHAEL'S HAND THE EVENING OF 2026-09-07 (`#150`) — AND NO MIGRATION IS PENDING. `db/migrations/2026-09-03-fe-d1-amendment-fix.sql` RAN 2026-09-07 BY HIS HAND (`#149`)** — all three checks answered. Record: `docs/record/forward-sitting-2026-09-07/migration-run-record-2026-09-07.md`. `MIG-1` (`2026-08-20-fe-d1-form-engine.sql`) and `2026-09-03-fe-d1-amendment.sql` **BOTH RAN BY MICHAEL'S HAND 2026-09-03 (`#147`)** — eight of eight, and nine of ten, `MIG-1` first and unchanged per `HD-18`. **THE §5 WINDOW IS CLOSED AND ITS WARNING IS SPENT** — the live app and the live database now agree on `facility_party_id`. **Check 8 IS PAID:** the fix dropped BY CATALOG LOOKUP the stale `contact_edges_type_check` the amendment's guessed drop silently missed, and `renders-care-at` is accepted live.

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **WITHDRAWN, NEVER ROUTE: `registry-retrieval-completion-2026-08-16.md` (#93).** Written on a false premise and withdrawn the same session. **It never reached the repo.** If a copy arrives by any path, it is an error

## THE FC BLOCK — ruled 2026-08-18 (#106), ALL DESIGN DIRECTION, NOTHING AUTHORIZED TO BUILD
- **THE ONE CONVENTION, HIS WORDS: *"My system should stay true to one convention as much as possible."*** **`{token}` — single brace, bare name — IS CANONICAL** (FC-1): the stored form, the editor's display form, and the form every new template is written in. **The importer accepts legacy conventions and emits canonical** — `{{TOKEN|filter}}` (the 15 MDB PLLC production letters) and registered bracket tokens — **and legacy forms are NEVER written back**
- **FC-1–FC-4 ARE FOLDED INTO `form-engine.md` §3** (#106), appended as a closing subsection: per-spot formatting lives in **TEMPLATE SETTINGS**, not token text (FC-2); bracket handling on import is an **ALLOWLIST, never a pattern match** (FC-3 — 147 `[signature block on next page]` notes, 80 `[s]` markers and Texas citation parentheticals would otherwise become data fields); `[s]` **converts** to the grammar engine's party-count flex point (FC-4). **§9 was NOT touched** — a verified pure insertion, 25 lines, zero deletions. **This matters to FE-D1: slice 1 hard-codes a parser, and the spec it reads now states the ruled syntax**
- **THE REST OF THE RULED THIRTEEN, ALL DIRECTION AND NONE OF IT A BUILD ITEM.** **FC-5: fee basis STAYS PER CLIENT** — D-CL2-3 stands, contrary stream-rate evidence recorded as considered and not followed · **FC-6: the reduction model runs FORWARD** — client net is an OUTPUT, ruled on MDB PLLC's own payout templates read at source at his direction; **client-net-as-input worksheets are the prior firm's method** · **FC-7: deadlines are TWO-TRACK**, his definitions · **FC-8: a court-keyed deadline PROFILE plus STRUCTURED GEOGRAPHY on `cases` when a schema act is next authorized — NO MIGRATION AUTHORIZED** · **FC-9: both rule kinds plus a holiday calendar** · **FC-10: consent is a FIRST-CLASS ENTITY, expiry a field** · **FC-11: the breakdown is a LIVING scenario document that FREEZES at client signature**, and a case may carry retainage plus a second breakdown per later recovery · **FC-15: the designation paragraph STAYS, whole, in all 12**
- **FC-12 AND FC-13 ARE BOTH RULED AND CLOSED (#108) — AND NEITHER AUTHORIZES A BUILD.** **FC-12: "(c) Structure only."** The corpus may contribute **skeletons, section orders and clause lists — NEVER body text**; body prose is always written clean. This REPLACES the interim no-imports default and makes prior-content leakage structurally impossible, the evidence being that **nothing in any of the four libraries is safely blank** and **six of the fifteen tokenized letters hard-code prior content**. **NO IMPORT TOOLING IS AUTHORIZED TO BUILD BY THIS RULING** — a separate, later act. **FE-D1 is unaffected** (its §11.3 skeleton works from your own shell). **FC-13: all four picked, plus the fifth.** Registry entries OPEN for **Prop. Code ch. 55**, **CPRC ch. 146 incl. the 2025 (c-1) amendment**, **TRCP 47(c) bands**, **§18.001/§41.0105** — and **TDRPC 1.04(f), the registry's FIRST practice-of-law proposition.** All five were **DRAFTING ACTS FOR A LATER OPUS SESSION — AND THAT SESSION HAS RUN (#112): six texts now exist at `docs/specs/fc13-entry-drafts-2026-08-18.md`, DRAFTED AND NOT INSERTED, each entering UNVERIFIED if and when it is. The reconcile is the finding — not one of the five destinations is free, so `FC13-Q-1..6` ask whether they insert at all and where.** **You chose the plain open over the boundary-ruling variant; the cases-vs-practice boundary was then DECIDED BY ACT at `#155` (DECISION 8 — a fifth registry file, to be drafted by a later Opus act; NOTHING DRAFTED)**

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE = real case/party/client/SOL data hand-entered into the core app, NOTHING MORE.** Floor: **(1)** CD-1 directory build COMPLETE 2026-08-19 · **(2)** gate 1 Supabase Pro COMPLETE 2026-08-19 · **(3)** gate 9 production SMTP **CLOSED 2026-09-07 (`#149`)** Evidence: `docs/record/forward-sitting-2026-09-07/gate9-evidence-record-2026-09-07.md` · **(4)** gate 3 RLS, incl. CD-1 tables + `party_pii` — **RUN CLEAN AND CLOSED 2026-08-20** · **(5)** the gates re-check **RAN TO COMPLETION 2026-09-07 (`#149`)** at Michael's direction (*“Run the confirmations by me one by one.”*) — gates 1, 2, 4 and 7/8 confirmed one at a time in his own words, 3, 5, 6 and 10 confirmed at HEAD — **TRIGGER 1 FIRED; ITEM (5) IS DONE** — v30 pasted and found in force by `#150`'s live read; **v31 has since superseded it (`#153`)** and NOTHING IN THE REPO VERIFIES EITHER. **✅ THE FLOOR IS COMPLETE — (1) THROUGH (5).** **GO-LIVE ITSELF REMAINS HIS ACT ON HIS DAY, in GL-1's own words.** **`GL1-1` RULED *"Adopt as drafted"* 2026-08-20 — the append is beneath gate 1 in `Go_Live_Gates.md`; row CLOSED.** ⛔ **ONE FLOOR CAUTION ADDED 2026-09-09 (`#154`), STATED WHERE THE LAUNCH PATH IS STATED: NO REAL CHRONOLOGY MAY BE DROPPED until the `AS-Q4` split-store slice lands** (detail beneath FORM ENGINE). **It gates the chronology drop zone alone, not go-live; every other floor item is unchanged.**
- **GATE 10 IS CLOSED — RULED 2026-08-20.** **EDGE (1): AUDITABILITY IS NOT BUILT** — only exclusion is; `G10-1` ruled provenance-only, the audit limb (history, freeze, `REVOKE UPDATE, DELETE`, reveal logging) OWED TO **`O-1`, OPEN**. **EDGE (2) DISCHARGED**: the §5 pre-flip report ran clean — but **ON TEN KEYS ONLY**. **`G10-2` and `G10-4` are NOT closed by this.** **IN `db/schema.sql` AND THE LIVE DB: `parties.date_of_birth date` (nullable, NO index) and a `party_pii` child table — `ssn`, `drivers_license`, `drivers_license_state` + PROVENANCE ONLY, SEVEN columns, NO `id`, `PRIMARY KEY (party_id)` (PK = FK), two triggers (`_touch`, `set_created_by`), RLS + its own `authenticated` policy and GRANT; `anon` FALSE on all four DML.** **`G10-6` ruled IN — both false `RlsProbePanel` sentences repaired.**
- **A POINTER CHECKLIST OVER THE GATES EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It carries NO status column, by design.** **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect.** **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor**
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. Appending a gate is the ruling.)*
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**
- **ITEM 7 RAN 2026-08-19 AND IS VERIFIED — CD-1 IS COMPLETE (#113).** `db/migrations/2026-08-12-cd1-contact-directory.sql`, **THE TWO-ROW CAVEAT, STATED RATHER THAN GLOSSED: the backfill ran over TWO rows and steps (c) and (d) matched NOTHING** — no Plaintiff, Defendant, Witness, adjuster, provider, expert or judge link exists live — **so the caption-alignment and function-role branches were NOT exercised here.** They were exercised in code on fixtures at #61, and GL-1 item (1) is satisfied by the PAIR; **a two-row backfill proves less than "verified clean" reads**
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED**
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **YOUR HAND (HK-4), NOT RE-WORDED.** **Re-checked natively this refresh against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** A negative about **this** machine that **does not answer the preflight row**, which is yours
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (unauthorized). A **second, unrelated `T1`–`T4`** is the LegiScan poller tier in `statute-text-and-bill-tracking-design.md` — **substantially BUILT.** **Five WF gates say "gated on T3" without naming which**; re-lettering is yours (the H → HK precedent)
- **THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until you locate it or re-issue, T3 WORK IS UNAUTHORIZED.** **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6`**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher; **/templates — the FE-D1 template bank**: **46 seeded templates** (the instrument, the twelve §9 approved variants, ten stock answers, **the 22 FIXED-SENTENCE rows the app now places, and the ONE writer-instructions row**), a token palette, and an editor whose Save CREATES A NEW VERSION rather than overwriting one — **`R8`, RULED 2026-09-05 and BUILT: the verb is *“Save as a new version”*, an unknown token WARNS on save and never blocks, each version row has a *“what changed”* expander against the version before it, and the Note autogenerates MECHANICALLY from the diff with NO model call.** **A fixed-sentence row Michael edits is placed AS EDITED**; the drift test compares the GENERATED constant to the spec and never the store's current row (`D-63`), so his edit does not turn the suite red

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)**. **SSN and licence inputs render here TODAY and save into `parties.fields`** — the front-end slice fixes it, behind `G10-5` · **`R5`/`R6`/`R16` BUILT 2026-09-07: a NEW PI Client-role link auto-creates the damages record (posture from the practice area, editable); unlink LEAVES it standing; existing gaps are FLAGGED and never backfilled; `CL2-CHECK-1`'s two states flag and never auto-fix; and the *“no client record”* notice MOVED to the TOP flag area with the resolving control left below.** |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** — **AND IT NOW OPENS WITH `R17`** | **PROVIDERS SECTION (new, 2026-09-03, ABOVE the unchanged ledger — D-29, a default): case-scoped FACILITY rows with a TYPE set per case and pre-filled from the last case where he set it (D-32, the only pre-fill there is); the individuals beneath each with credential, role marker, dates, an expandable summary and their visit rows; hand add; SOFT delete behind a “show removed” toggle (D-55 — his delete survives the next chronology drop); PROMOTE-to-directory, directory-FIRST and pre-searched, creating the contact and a `renders-care-at` edge whose DATES ARE HIS, editable and clearable (D-56); oldest-treatment-first by the D-13 chain, which reads his hand-set date BEFORE anything derived.** **ONE CHRONOLOGY DROP ZONE at the top of it (D-9), per client: PDF, Word, Excel, CSV, JSON, plain text — every other extension REFUSED by name; the FILE IS NEVER STORED, only its text (`AS-Q4`); an unreadable scan is flagged, never sent, and never counts as “newest” (D-62); a mis-dropped version is removable behind a confirm that names the reason, and the people it named keep their rows (D-60).** Then, unchanged: bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients.** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab.** **2026-09-07: each facility card now picks WHICH LOCATION treated the client (`D1`) — one location auto-selects, two or more unpicked is a PANEL line and never a stop; and a new bill's LABEL pre-fills from the provider name, editable (`R7`).** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |
| Forms | **THE AMENDMENT IS BUILT — §13 items 1-14. The wizard NO LONGER SELECTS VARIANTS** | **APP ASSEMBLY (`RC-1`, “Option 1”): a paragraph is LEAD · opening · BASIS · middle · CAUSATION, causation always last; the app places the LEAD from the record and the two fixed sentences keyed by the facility's TYPE, and the writer returns only its named parts. **NOTHING READS INSIDE A PART** — a structural test plus an adversarial one feeding it the app's own sentence, a wrong date, markdown, a live token and raw `& < >`.** **TEN SHAPES: treating single / group / mixed, the radiologist split, the imaging facility, the mid-level rider, pharmacy, custodian-only, other-non-physician, retained.** **NO RIDER WITHOUT A PARAGRAPH TO RIDE (D-16), and the mid-level is then off the block too (D-65).** **FOUR MUST-FIX STOPS and only four (`R1`/`R2`, 2026-09-05)** — no incident date, a selected facility with no name, one with no type, and a billed party TYPED `providerBusiness` with no provider row at all — that fourth one names BOTH its clears in its route line; while one holds NOTHING IS GENERATED AND NOTHING IS TRANSMITTED, proved by a call spy. **NINETEEN PANEL LINES, none of which blocks**; the missing-physician line fires only AFTER an extraction has run for that facility, and before any pull there is ONE line for the client. **The §5 gates are RE-KEYED onto typed columns** — the mental-health pause now fires on a TYPE Michael set and on a MARKER at a facility of another type, not on a substring sniffed out of `party.fields`. **The four §4 interview cards are RETIRED for the treating track and NOT DELETED** (`AS-Q9`): the component is kept at `src/components/RetiredInterviewCard.tsx`, imported by nothing. **Retained track, client selector on a multi-client case (one instrument per plaintiff), supplementation pre-deselecting the designated chain.** **The 195.2 panel is UNCHANGED — states the rule, names its UNVERIFIED status, asserts no date.** PI matters only. **EXCLUDED from the GL-1 floor; the drafting skill remains the live path.** ✅ **THE CC-1 RULINGS AND ADDRESS-MODEL SLICE IS BUILT (2026-09-07), fixture-only, walked by clicking** — `CCS-1` RULED YES (`#149`, *“yes”*, `HS-3` IN), spec `docs/specs/cc1-rulings-and-address-model-slice.md`. **The block now reads the SELECTED location's street, city/state/ZIP and phone through `facilityContactLines()`; the persons-with-knowledge lines read the SAME resolved location; every phone token renders formatted through ONE formatter at the render seam; **THE RENDER PATH NEVER PARSES — the split runs ONCE at the record, and a test proves that as an ABSENCE at the output;** panel line 1 became THREE true lines; the custodian-only gap line carries the facility's billed total inline and the gap lines sort charge-descending, with NO threshold.** **`R4` GROUP FILL always; `R10` a panel line per pronoun-less individual; `R11` the LITERAL *“And/or Custodian(s) of Records”* at every N ≥ 1, no inflection; `R12` DPT stays out; `R13` a mid-level is name + credential suffix and never *“Mr.”*/*“Ms.”*; `HS-3` a trailing generational suffix is not a surname.** **`R15` names the responding plaintiff in the title ONLY on a multi-client case — which reaches the record, the filename, the on-screen label and two new scalars, but NOT the served heading, which is static text in the master `.docx`, which no session has edited (`spec-feedback.md` item 10).** |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT — RE-AFFIRMED 2026-08-18 (FC-5).** **D-CL2-3a: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. The time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter — every feature works in both modes. Seeds fictional; **store v16** (2026-09-03: v14 renamed provider→facility inside stored bills, code mappings and profiles and appended §9.4's ruled edit as a NEW template version rather than overwriting v1 — `D-63`; v15 adds the 23 templates a stale store would never have gained and initialises R17's four collections; **v16, 2026-09-07, splits every one-line address ONCE into `addressLine1` + `cityStateZip`, MARKS what a rule touched, and gives every facility location a stable id — and the SEED PATH runs the same rule, so a browser that never held a store lands in the same shape a migrated one does**), the v9→v16 chain running **FORWARD in place**, backing up at each step. **`db/schema.sql` and the RLS probe both stand at 46 tables, sequence-identical, with 45 policies (`file_counters` is the one deliberate omission).** **The v13→v14 step is not cosmetic: a stored v13 bill carries `providerPartyId` while the renamed code reads `facilityPartyId`, so a store that skipped it would show every bill with NO FACILITY, silently — a regression test pins exactly that.** **All migration steps are PINNED TO LITERALS — `migrateV10ToV11` had stamped `STORE_VERSION`, which at 12 would have SKIPPED v11→v12 and left SSNs in the blob.** **`npm run dev:demo` REACHES DEMO MODE (fixed 2026-08-20)**: the ONE seam is `resolveUsingSupabase(url, key, mode)` in `src/data/supabaseClient.ts`, and `--mode demo` forces the local adapter whatever `.env` holds, so **a fresh clone with a filled `.env` gets demo mode, NO helper file**. Six regression cases pin it (`src/data/__tests__/demoMode.test.ts`); `spec-feedback.md` finding 1 ANSWERED. **demo state does NOT travel**. `.env` on `mdb-pllc` filled 2026-08-20 — footer: “Connected: central database”
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **46 `create table` statements in schema.sql, AND BOTH MIGRATIONS HAVE NOW RUN BY MICHAEL'S HAND (2026-09-03, `#147`) — `2026-08-20-fe-d1-form-engine.sql` eight of eight, `2026-09-03-fe-d1-amendment.sql` nine of ten with its hard gate passed.** **The live database therefore moves 37 → 46 ON THE MIGRATIONS' OWN NAME CHECKS — each of the nine new tables answered BY NAME — and NOT on a catalog census. The 37/46 gap is closed by inference from checks that passed, and must never be restated as an enumeration.** **The amendment's five, measured at the `ea5675b` CODE refresh and CARRIED here: `case_chronology_versions`, `case_providers`, `case_provider_individuals`, `case_provider_visits`, `generated_document_paragraphs` — each with RLS, one policy, a GRANT and a probe entry in the same commit as the table.** **AND THE TWO `CCS-1` MIGRATIONS RAN ON LIVE BY HIS HAND 2026-09-07 EVENING (`#150`) — `2026-09-07-address-model-schema.sql` (three checks) then `2026-09-07-address-model-split.sql` (six checks, STEP 0 first), nine of nine answered in his words. `parties` on LIVE holds **2 rows**, **0 split** because none needed it, and **0 confirm-or-edit notices** raised. EVERY MIGRATION WRITTEN TO DATE HAS NOW RUN; none is pending.** **THE SERVER IS PostgreSQL `17.6`.** *(Re-derived here every refresh: 46 tables, 46 `enable row level security`, **45 policies** — the gap is `file_counters`, deliberately policy-free as the probe's deny control, so never "correct" 36 to 37 for symmetry. **The live 37 is now BETTER THAN INFERRED ON ITS FLOOR, and still not a census.** The live 37 rests on the 2026-08-20 gate 3 run's per-name confirmation, not a census; **it still cannot see a 38th table, because nothing enumerated the catalog. NO TABLE CENSUS WAS RUN.**)*
- **NINE SCHEMA RULINGS CAME OUT OF THE GROK EXTERNAL REVIEW (#111) AND EIGHT ARE IN `db/schema.sql`; THE LIVE HALF RAN 2026-08-19 AND ALL TEN CHECKS CAME BACK CLEAN (#113).** **F-1 WAS A LIVE EXPOSURE AND IS NOW CLOSED:** no PUBLIC entry survives on `next_file_number()`. **F-3 confirmed live on both limbs.** Landed: **`revoke execute on function next_file_number() from public`** (F-1, ordered AFTER the replace) · **`America/Chicago` on both the file-number year and `cases.date_opened`** (F-3) · **a BEFORE UPDATE trigger refusing any change to `file_number`** (F-2) · **`created_by uuid references auth.users (id)` on five tables with an `auth.uid()` BEFORE INSERT trigger** (F-25 — **columns only; NO per-user RLS**) · **the "gapless" claim OUT of the schema header** (C-6) · **the stale grant-block counts rewritten COUNT-FREE** (F-27)
- **THE NINTH RULING LANDED THE SAME NIGHT (ruling 4, F-4+F-18).** `case_parties` now carries **`unique nulls not distinct (case_id, party_id, role, capacity_kind, capacity_points_at_party_id)`** plus **`case_parties_capacity_pointer_check`** — `next-friend-of` / `representative-of-estate-of` / `dba` require a pointer, `individually` exempt. **IT RAN 2026-08-19:** ONE unique constraint on `case_parties`, ZERO of the old three-column key, and the pointer check at 1. The file carries a `server_version_num` guard **REFUSING to run below 15** and drops the old key **by looking it up in `pg_constraint` by DEFINITION rather than by a guessed name.** **O-8 and O-9 both close**
- **F-12's STEP (d) FIX EXECUTED LIVE 2026-08-19.** `party_status = coalesce(party_status, null)` — which would have left every Witness / Adjuster / provider / expert / judge NULL though `'non-party-actor'` exists in the CHECK — now writes the value.
- **A CASCADE/RETENTION MAP EXISTS AND NOTHING IN IT IS EXECUTED: `docs/specs/cascade-retention-map-2026-08-18.md`** (**PROPOSED — AWAITING SIGN-OFF, O-7**). **Eleven** FKs across C-1's six named children mapped current→proposed (CASCADE/SET NULL → RESTRICT), **four component FKs argued for KEEPING cascade**, **six adjacent FKs listed as UNRULED**. **Read its §6 first: RESTRICT without app-layer soft-delete handling means matters can no longer be deleted at all**
- **GRANTs ARE LOAD-BEARING SCHEMA; `C-2` RESTATED ON ITS TRUE WARRANT (ruled 2026-08-19, `O-13`).** `anon`+`service_role` hold `TRUNCATE`/`REFERENCES`/`TRIGGER`/`MAINTAIN` on all **37 tables** from `ALTER DEFAULT PRIVILEGES` (`pg_default_acl`), **not from this repo (zero EXECUTABLE lines in `db/schema.sql` or **all ELEVEN migrations**, re-counted at the batch-92 read over the whole of `db/migrations/` and **CARRIED here, not repeated — this docs-only batch read no `db/` file** — **every one of the 14 occurrences of the phrase is a `--` COMMENT line, checked rather than assumed**, so the count survives each widening of the scope: six → nine → eleven, still zero)**. **NO READ EXPOSURE:** `anon` has no DML. **REACHABILITY STILL OPEN** — unreachability rests on **the vendor default excluding DML: Supabase's posture, silently changeable.** **`supabase_admin | public | r` grants `anon=arwdDxtm` (SELECT) on tables IT creates; none of the 37 is; whether tooling makes one is OPEN (`O-12`, DEFERRED BY RULING; remedy `O-11`, yours).**
- **THE `anon gets nothing` REPAIR IS COMPLETE IN THE DOCS** (2026-08-19; the five RUN migrations were **ANNOTATED, NOT REWRITTEN by your ruling**). **The probe covers 46 tables** **THE ADP FAMILY HAD THREE CARRIERS: `docs/spec-feedback.md`:591 and `docs/specs/attorney-review-queue.md`:793 WERE REPAIRED 2026-08-19 ON YOUR RULING; the `src/` one STILL STANDS — `src/components/RlsProbePanel.tsx`, item 6 of the front-end slice checklist, behind `G10-5`.** **The retraction class — every file quoting the sentence in order to DENY it, `src/data/__tests__/gate10Pii.test.ts` included — is NOT counted and must NEVER be "fixed"**. **The live-privilege remedy is still YOURS and none was run (`O-11`)**
- **`privilege_tier` NO LONGER ASSERTS A STANCE NOBODY CHOSE — Q-COM-11 RULED (A) AND THE SCHEMA HALF IS EXECUTED (#94).** Both columns (`generated_documents`, `transcripts`) **lost `default 'work-product'` and `not null`**; **NULL now means unclassified-must-classify.** **THE MIGRATION RAN 2026-08-19 (#113): both columns nullable, NO default, and BOTH CHECKs untouched — the two vocabularies still disagree, which is the CORRECT result and the named failure the file was written to avoid.** **No existing row was re-characterized** (all fiction; re-characterizing is a legal act and yours). Two honest limits: its C2 check is satisfied **TRIVIALLY** (both tables are empty), and **check 4 — insert-a-null inside a rolled-back transaction — was NOT run.** **NO creation-time classification UI was built**
- **THE TWO `privilege_tier` VOCABULARIES STILL DISAGREE IN THE SCHEMA, AND THE DIVERGENCE IS DUPLICATED IN TYPESCRIPT (#85 → #89 `Q-COM-10`, extended #94) — BUT THE SHAPE THEY MUST CONVERGE ON IS RULED (#105, `Q-COM-10-A`).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')`. **NEITHER CHECK WAS TOUCHED.** **`src/domain/billing.ts` and `src/domain/transcripts.ts` each `export type PrivilegeTier` with DIFFERENT members** — **so the question has FOUR homes and any ruling touches `src/` as well as `db/`.** **THE RULING: `privilege_tier` is NOT exhaustive** — purpose-scoped under a stated precedence rule — **and the target is Option 2: THREE values plus a `witness_statement` boolean.** **`Q-COM-10-B`–`F` are unblocked**
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view**, and a word-bounded sweep returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **THERE IS STILL NO GEOGRAPHY ON `cases` — BUT THE QUESTION ABOVE IT IS RULED (`Q-RE-1`, CLOSED 2026-08-18, "Both, separate triggers").** **`cases` ALREADY CARRIES `county text`**, added by the OAA criminal-intake block along with `in_custody`, `custody_location` and `appointment_date`. `court_name` and `cause_number` remain free text, and **`venue`, `district` and `jurisdiction` are genuinely absent.** The old blanket claim was **partly** true, which is worse than wholly wrong: **FC-8 would have authorized adding a column that already exists.** The out-of-area referral trigger tests **geography AND subject-matter INDEPENDENTLY.** **No migration is authorized by either ruling**
- **A WRITTEN-CHANNEL COMMUNICATION STILL HAS NOWHERE TO BE (`Q-COM-2`, CLOSED 2026-08-18, "Yes — adopt the list").** `transcripts.source` and `staging_items.source` **both** still admit exactly `('recorder','phone','manual')`. The ruled list to add: **`e-service`, `email`, `first-class mail`, `certified-mail-RRR` (tracking number as its own field), `fax`, `portal`.** **NOTHING WAS WIDENED — THE MIGRATION IS A SEPARATE LATER ACT AND IS NOT AUTHORIZED.** **AND IDENTITY RESOLUTION HAS NO SUBSTRATE (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series.** **Nothing for a deadline engine** (FC-7/FC-9 are direction only), **and no discovery-level field on `cases`** (FE-5's hard prerequisite). **Nothing for a consent entity** (FC-10). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all ZERO (#85). AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — none of the 37 tables is a money table. NOR FOR RE-1: `referral`, `conflict_check`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC`, `prospective` all ZERO (#88)**. *(Basis stated: word-bounded over `db/` these are exactly zero. Widened to `db/` + `supabase/`, two stop being literally zero — `message` ×3, every one a JavaScript `error.message`, and `case_links` ×4, every one a COMMENT saying it is not built. **A future sweep that counts the schema's own disclaimers will "find" the thing they deny.**)*

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED, AND THE LIST IS NO LONGER THE SHORT ONE:** authenticated SELECT on all API tables; **INSERT ON ALL 37 TABLES, SIGNED OUT AND SIGNED IN, MEASURED 2026-08-20** (`docs/specs/gate3-write-path-test-run-2026-08-20.md`); the app's write path; CL-2's live paths. **THE "PRESENT BUT UNTESTED — an inference" POSITION IS RETIRED FOR INSERT: 36 of 36 policy-bearing write paths were exercised against the live database and all 36 ALLOWED.** **GL-1 ITEM 4 IS THE GATE AND IT IS NOW CLOSED — `Q-G3-4` RULED 2026-08-20 (`#126`), *"Close gate 3, edges recorded"*, the closure appended beneath the gate.** What the closure does NOT reach is named on its face: **UPDATE and DELETE against `using (true)` remain unexercised by ruling** (`Q-G3-2`, *"run as written"*), which the run report names rather than resolves. **`#125`'s condition on `Q-G3-4` — that the solo-practice framing holds only while the set of authenticatable users is CLOSED — is DISCHARGED: the signup toggle was measured OFF 2026-09-07 (`#149`)**
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is a privilege refusal, not an RLS result, and **both share SQLSTATE `42501`, so a test reading only the code scores it as proof RLS works.** The probe READS all **46** tables (`SCHEMA_TABLES` — includes `party_pii`, FE-D1's four `form_*` tables and the amendment's five, `file_counters` its sole `policy:false`; **a TEST asserts it stays sequence-identical to `db/schema.sql`, so a table added there and forgotten in the probe fails the suite**) but WRITE-probes only **5** — `parties`, `legal_rules`, `glossary_terms`, `watch_targets`, `file_counters`; the gate 3 run MEASURED 37, the standing instrument stays at 5 (**`Q-G3-5` RULED 2026-08-20 (`#126`): stays at 5 until a named trigger — the gate-2 multi-user phase or Michael's earlier call; growing it is a build act needing its own authorization**). **ALL 45 POLICIES ARE BYTE-IDENTICAL — `for all to authenticated using (true) with check (true)` — ONE RULE ON 45 TABLES, not 45 rule sets.** ⚠ **THE AMENDMENT'S FIVE CARRY PHI — a chronology's extracted text above all — AND A PERMISSIVE POLICY IS NOT THE ACCESS CONTROL THAT FACT WILL NEED; the migration says so on its face and the security review before multi-user use is where it is decided.** ⚠ **It drives PostgREST, so non-DML grants (`REFERENCES`, `TRIGGER`, `TRUNCATE`) are INVISIBLE to it on every role — an UNEXPECTED grant hides as well as a MISSING one, the gate 3 run inherits that blindness, and it structurally cannot see the class of defect found 2026-08-19.** **`RlsProbePanel.tsx`'s two false privilege sentences are REPAIRED 2026-08-19 (`G10-6` ruled IN, `G10-5` built it); the two replacement wordings are DISPLACED to batch 88's runner line, and the privilege-wall one is PROPOSED, not separately adopted — Michael's to override.**

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. **INDICATED, NOT MEASURED, AND THE DIFFERENCE IS KEPT: `service_role` SHOWS NO DML ON ANY OF THE 37 TABLES** (only `REFERENCES`/`TRIGGER`/`TRUNCATE`, same as `anon`) — **but that rests on the `information_schema` listing alone, a WEAKER instrument than `anon`'s `has_table_privilege` test.** Diagnostic 6 in `spec-feedback.md` is staged and UNRUN. On the evidence the poller would fail on PRIVILEGE rather than on auth the moment it is deployed — and bypassing RLS does not bypass table privileges. **The conclusion holds; its stated GROUND does not: *"`service_role` was never granted either"* is true of THIS PROJECT'S SQL and false of the DATABASE.** **F-14 IS LATENT, NOT A GL-1 BLOCKER (#113)** — undeployed code has no endpoint; it rides `O-3`. **/statutes in demo mode never touches Supabase** ⛔ **DEPLOYMENT AUTHORIZED 2026-09-09 (`#154`, Wave 0 (a)) AND NOT YET RUN — AND IT IS NOT A QUEUE-RUNNER ACT:** it is a SEPARATE Code session that Michael opens with his own Supabase sign-in (`supabase functions deploy`, secrets, cron), whose kickoff prompt is a design act not yet drafted. When it runs it is the proving step for the Supabase scheduled path and answers `F-14` and `O-11` with evidence; **until then both stay exactly as stated above.**
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY THIS REFRESH BY EXISTENCE TEST ON DECLARED PATHS ONLY (HK-5; nothing enumerated) AND STILL ABSENT**, as are `C:\Users\Brennan\data` and `...\data\pilot-recordings\`. **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (your hand, your browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently.** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** **THE AUTH SURFACE IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on your registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (yours) — **and OBS-1 asks whether that capture row folds into Phase 1b's scope; unruled.** Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**
- **A MIGRATION PROPOSAL EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (Task 12; you did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately a QUESTION.** **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`). **`Q-PR3-1..7` and `PR3-LOOK-1` are yours; nothing was re-parented**
- **A DEFECT IN A RULED CONSTRAINT, CONFIRMED AT HEAD, CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts` returns the plaintiff-defendant side set for **every practice area that is not `'Criminal'`** — while that file's header states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant alignments. **Live today, unreachable only because no fourth practice area exists.** **The codebase carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by design, while `sideSetFor()` **silently supplies a wrong-but-plausible answer.** **Flagged, not fixed**
- **FORM ENGINE — FE-D1 IS BUILT (2026-08-20), CODE-COMPLETE, BROWSER-WALKED ON FIXTURES ONLY, MIGRATION RUN 2026-09-03** (`MIG-1`, eight of eight checks, by Michael's hand and unchanged, as `HD-18` required; outside the `GL-1` floor). `src/forms/`: ZIP/docx layer, renderer, FC-1 tokens, grammar engine, §5 gates, FE-10 lint, write-backs, drift-tested §9 library; both binding invariants tested. **§10 substrate: `form_templates` + `form_template_versions` + `form_token_definitions` + `form_format_profiles`, with RLS, GRANTs, probe entry; `generated_documents` EXTENDED, not forked** (7 nullable cols: FE-8 snapshot, FE-15 posture). **NOT BUILT: one-click PDF, OneDrive storage** (Graph: `Calendars.ReadWrite` only)**, NO ITEM TABLE, NO FE-9/11/13/14/16/17.** 11 findings in `spec-feedback.md`. ⛔ **`RC-1` RULED, FE-18 RATIFIED, `R17` MINTED — ALL THREE ARE BUILT** (`R17` = `ProvidersSection.tsx` + `caseProviders.ts`; sentences = `fixedSentences.ts`; slot = `tiers.ts`). Spec homes unchanged. Those + the rename, parts-return contract and model call = the **FE-D1 AMENDMENT SLICE, AUTHORIZED 2026-09-02 (`#146`) AND PART-BUILT 2026-09-03. THE KICKOFF PROMPT HAS FIRED.** **BOTH TRANCHES ARE BUILT (§13 items 1-14); STORE v15.** **THE SECOND TRANCHE BUILT ALL OF IT (carried from the 2026-09-07 CODE refresh's existence test): `src/forms/tiers.ts`, `assembly.ts`, `chronology/extraction.ts`, `writer/` (a DIRECTORY, not a `writer.ts`), `generate.ts`, the Forms-tab rebuild, `src/data/disclosureFixtures.ts` (D-35) and `__tests__/fixtureWalk.test.ts` all EXIST, `ParagraphWriter` is exported from `writer/types.ts`, and the Edge Function stub is at `supabase/functions/disclosures-writer/`. **`DISCLOSURES_WRITER_CREDENTIAL` EXISTS as a literal at that handler's line 25.** **THE SAFETY HALF IS CARRIED FROM THE `ea5675b` CODE REFRESH, WHICH RE-VERIFIED IT; THIS DOCS-ONLY BATCH READ NO `src/` FILE AND DID NOT RE-RUN THE SWEEP:** a case-insensitive sweep of `src/` for `anthropic|openai|claude-3|claude-opus|gpt-4|api_key` returns nothing outside tests, the Edge Function says on its face that it calls no vendor, and `resolveParagraphWriter()` returns `FixtureParagraphWriter` in BOTH modes with `WRITER_REGISTRY = ['fixture']` — `H12-v` and the BAA gate untouched, as they must be. ⛔ **`HS-2`/F7 IS FIXED (`8625508` + `d46ff15`) AND THE BLOCK'S ADDRESS PROBLEM IS NOT OVER.** The three tokens were hard-coded `''`; they now read the party record through one shared `facilityContactLines()`, eight test cases, each proved by reintroducing its failure. **But `providerBusiness` in `partyRegistry.ts` has NO `addressLine1` and NO `cityStateZip` — only a repeating `locations[]` — and `PartyFormPage` renders only `def.fields`, so NO FACILITY CREATED IN THE APP CAN HOLD THE KEYS THE BLOCK READS.** **That gap is `spec-feedback.md` item 2 + `2a`, and it is NO LONGER an open decision: it was RULED at `#149` in five limbs and is BUILT BY the `CCS-1` slice (§3 item 17), **which is now AUTHORIZED AND BUILT (2026-09-07, fixture-only and walked)**** `docs/skills/drafting-disclosures/SKILL.md` **untouched — the live drafting path** ⛔ **`AS-Q4` WAS AMENDED IN ONE LIMB 2026-09-09 (`#154`) AND THE BUILT COLUMN NOW CONTRADICTS THE RULING:** `case_chronology_versions.extracted_text` stores the text (invariant 29); the amended ruling stores NO text, keeping a drive item id and content hash against a file in the matter's OneDrive folder. **NOTHING WAS CHANGED IN CODE by the sitting or by this batch** — the change is a later, separately authorized slice's (`Q-API-20`), needing a migration, a Graph write path on delegated `Files.ReadWrite` (consent Michael's), a `_removed` move and a demo-mode stand-in. Logged as a SPEC-VS-CODE GAP in `docs/spec-feedback.md` (the 2026-09-09 section). **Until it lands the drop zone is FIXTURE-ONLY and no real chronology may be dropped.**
- **A `review_log` WRITE OUTSIDE ITS CHECK — FLAGGED AT `#151`, NOT FIXED, NOT VERIFIED LIVE; NOW `FOD-20`'s, IN BY `FOS-1` (`#155`), closed by the FO build's migration when it runs.** `src/pages/CalendarTab.tsx`'s cancel writes `action: 'cancelled'`; `db/schema.sql`'s CHECK admits six values WITHOUT it; the Supabase adapter passes the row straight through. On those reads a LIVE cancel should fail its audit insert (23514) and never reach the Outlook deletion. **These are `#151`'s design-side reads over the bridge at `ea5675b`, not this runner's — no `src/` file was read this batch, and nothing was confirmed live.** `docs/spec-feedback.md`, the 2026-09-07 firm-obligations section, item 1; `Q-RE-6` records the sibling *"decided"* gap.
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, backlog 47, THIRTY-FIVE VERIFIED — and the ruled-wording tail is EMPTY
- **BACKLOG RE-DERIVED AT HEAD THIS REFRESH, NEVER CARRIED — and UNMOVED again (its streak ordinal is dropped: it read FOURTH through three refreshes while batch 95's runner line said fifth):** `legal-rule-registry-discovery-enforcement-and-pleading.md` **30 VERIFIED / 10 UNVERIFIED against 40 `**Status:**` lines**; `legal-rule-registry-criminal-plea-and-costs.md` **5 / 2 against 7.** **Backlog 47 entries; 35 verified, 12 not.** **The unit is STATUS LINES, not headings.** **Counting trap, carried: `grep 'VERIFIED —'` also matches `UNVERIFIED —`; anchor on `^**Status:** VERIFIED`.** **SECOND TRAP, CARRIED AND STILL LIVE — THE BASIS IS TWO NAMED FILES, NOT A GLOB** (a `legal-rule-registry*` glob over-counts; the worked example is displaced)
- **THE `WS-3` ENTRY IS INSERTED (#109) AND VERIFIED (#110) — THE FIRST CHARACTERIZATION-BEARING ENTRY TO REACH VERIFICATION SINCE THE V-9 AMENDMENT.** Gates ruled at #109: `Q-WS3-1` → **"Cross-reference"**; `Q-WS3-2` → **"Comment-9"**. **Verification rested on a LAYERED basis:** the 192.3(h) clause and the full comment-9 quotation **mechanically verified**; the *Fontenot* / *ExxonMobil* characterizations **resting on the #102/#104 reads as recorded**; the `C-XL-1` boundary and `C-ARD-1` condition as adopted at #108. **Michael's judgment, not a text match.** **THE PINPOINT CAVEAT SURVIVES AS VERIFIED TEXT: star-pagination pinpoints must be checked against the reporter before any quotation enters a filing**
- **THE ELEVEN THAT REMAIN, AND THE RECORDS LOOKS BEHIND THEM RAN (#99, T-20/T-21).** **Group A is fully closed** — 13, 2 and 29 VERIFIED at #108; 12 and 32 VERIFIED at #109. **Seven records/cite looks** — 15, 16, 17, 18, 19a, 20a, 21a — have **candidates staged per entry, source named per item**, none adopted. **Formerly four blocked** — **20b is LOCATED and 24's majority is IDENTIFIED**; 30 and 31 remain cite-less and their cites are yours. **The eleven are not "the enforcement backlog PLUS the criminal two": 30 and 31 ARE the criminal file's two. The enforcement file holds TEN unverified** — 15, 16, 17, 18, 19a, 20a, 21a, 20b, 24 **plus 19b**, whose verification detached on rewording — **and the criminal file two. Ten plus two is the 12**
- **THE ROUTE-C UNVERIFIED-BY-DESIGN CLASS IS DOWN TO THE PAIR THAT WAS ALWAYS GOING TO BE LAST.** Entries **12** and **32** left it at #109, and **A** (192.3(h) own-statement), the new **215.1(e)**, **B** (193.4(a)), **F** (199.6) and **WS-3** all left it at #110 — **four of those five on programmatic character-exact matches with ZERO flags.** **What remains is 19a** (*Castillo*) **and 19b** (*Able Supply*). **No entry with adopted-but-unverified wording remains anywhere in the registry.** Adoption is not verification — **execute-then-verify (#95)**
- **ROUTE (c) IS STANDING LAW (#95) AND HAS RUN THREE TIMES.** A divergence that **changes what an entry means** gets proposed conforming wording, **drafted and queued for your adopt/reject/edit — never adopted silently**, and never as a package. #96 produced its one **rejection** (`V7-23-CITE`), the evidence it is not a rubber stamp
- **`Q-STAT-6`'s EIGHTEEN ARE NOW NONE LIVE, AND THE ROW STILL STAYS OPEN.** Adopted at #95 and **verified 08-17: 5, 6, 11, 27**. Flags **CURED**: **1, 23, 33, 34**. Divergences **RESOLVED as-is**: **4, 7, 8, 9, 26**. **The last two, 12 and 32, are VERIFIED as of #109** — and their divergence lists produced a STANDING DRAFTING DIRECTION (#108, your words, DISPLACED to batch 88's runner line): **registry Rule lines quote operative text VERBATIM where practicable**, condensing only where the rule is too long or the entry states one limb. **It is a ruled DIRECTION, not a binding convention — putting it in the project instructions fires trigger #3, and that is yours.** **Applying it to any EXISTING entry is a per-entry ROUTE-C act and also yours**
- **CR-10 STAYS HARD-GATED, ONE OF ITS FOUR NOW CLEAR: 32 VERIFIED (#109)**, 29 verified at #108; **30 and 31 are cite-less and their cites are yours.** 31's bridge is held (#104): *Hurlburt*, 506 S.W.3d 199 (Waco 2016) — the CCA's construction of *“in a single criminal action”* governs art. 102.073(a) and the “same criminal episode” limb DROPS OUT for costs — routing through *Ex parte Pharr*, 897 S.W.2d 795 (Tex. Crim. App. 1995), NOT *LaPorte*. **`Q-AUTH-1` EXECUTED 2026-08-19 — `docs/specs/pharr-hurlburt-authority-read-2026-08-19.md`, PROPOSED/staging, both opinions positively identified under V-9's second limb.** Limits: Waco is persuasive, not binding in Bexar; the episode limb drops out BY AGREEMENT, weaker than a contested holding; **no currency check (`Q-PH-1`, OPEN)**; vLex locators, not the reporter. **THE NEGATIVE-IMPLICATION ENTRY 31 WOULD REST ON IS DELIBERATELY NOT DRAFTED (`Q-PH-2`) — five propositions staged, ALL UNVERIFIED, no cite selected, the supply stays YOURS.** **The CCA has a scope holding but EXPRESSLY RESERVED THE REST: *Anastassov* (2022).** **V-6 still governs — art. 42.0197's finding is NOT automatic**
- **THE 19-PAIR IS UNVERIFIED ON BOTH SIDES, AND THAT WAS THE RULING, NOT AN ACCIDENT.** *Castillo* (19a) and *Able Supply* (19b) carry **the same proposition text** by design; the walk verified 19b only, and `RL-1` then reworded **both** — so **19b's 2026-08-17 verification DETACHED** (verification attaches to wording, #95). **Re-verification is yours.** **`V5-ATTRIB` stays OPEN across all six split entries**
- **THE THREE WORK-PRODUCT ENTRIES ARE VERIFIED AND STILL HAVE NO NUMBER (`REGISTRY-V` CLOSED, `Q-T19-1` OPEN).** 192.5(d), 192.5(c)(1) and 192.3(h)'s definition (notes taken during an interview are **not** a witness statement) verified 2026-08-17, **the effective-date caveat ACCEPTED rather than smoothed.** **Labelled `WP-1/2/3`, packet-local; numbering DEFERRED (`D-6`)**
- **#73's `V-4` SECOND HALF IS EXECUTED.** #73 ruled **"keep as two, CROSS-REFERENCED"** and the cross-references had run **one direction only**. `RL-5` ruled both sets: entries **17 ⇄ 18** took reciprocal notes, and `legal-rule-registry-discovery-and-carrier-duties.md` took **three reciprocal dedupe notes** (197.2(a), 196.2, 198.2). **NOTES ONLY — no proposition and no Status line in that ALL-VERIFIED file was touched, and its 22 `VERIFIED` lines were counted before and after and are identical**
- **`legal-rule-registry-discovery-and-carrier-duties.md` IS THE VERIFIED FILE** *(**AUD-3, CONFIRMED FALSE and re-derived again this refresh: 22 `**Status:** VERIFIED` lines against a header reading "ALL TWENTY PROPOSITIONS VERIFIED" — a bullets-vs-entries unit mismatch. NOTHING touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)); **TRCP 190.3(b)(3) is a VERIFIED entry under a wording flag — `Q-FE5-9`.** Criminal file: **entry 3 and 4a carry NO CITE — supplying one is YOUR act.** **Six flagged WL/slip cites survive uncorrected.** **`/rules` SEED ALL UNVERIFIED**
- **THE FOURTH FILE SITS OUTSIDE THE BACKLOG: `legal-rule-registry-draft-entries-medical-billing.md`**, headed ALL ENTRIES UNVERIFIED while its own entry Status lines read **DRAFT** — a second header/entry unit mismatch, re-derived this refresh. Whether that exclusion is deliberate is **`Q-STAT-2`, yours**
- **SAME ABSENCE ×5:** zero propositions on privacy (`Q-WF-6`), client property/trust accounting (`Q-QBO-6`), professional conduct (thirteen terms); #78's insurance narrowing; work product VERIFIED, only three. **Registry content: the firm's CASES only so far — DECISION 8 (`#155`) extends it to the PRACTICE through a fifth file, undrafted. FOUR research passes stack behind `Q-STAT-5`.** Privacy's one caller: gate 10 (`G10-4`, #115) — Tex. Bus. & Com. Code ch. 521 keys breach notification on the slice's two values: a LOCATOR, UNVERIFIED, no entry; `docs/specs/g10-4-ch521-entry-drafts-2026-08-19.md` holds **SEVEN drafts (§§ 521.002(a)(2)(A)/(B), .002(b), .052(a), .053(b)/(c)/(i)), ALL `UNVERIFIED`, NOT INSERTED, currency NOT established.** **FLAG: § 521.002(a)(2)(A) is CONJUNCTIVE/encryption-conditioned — encrypting the PII columns while `parties.display_name` stays plaintext does NOT exit it, and the definition governs § 521.052's protect and destruction duties as well as § 521.053's notification duties. Gate 10's encryption rejection is NOT reopened; nothing in ch. 521 read in that pass MANDATES encryption.** Insertion — the placement rule points at a NEW file, not a fold-in — is `G10-4`/`Q-WF-6`, **yours**
- **THE RETRIEVAL GAP IS CLOSED (#76)** — the rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20**, and all 21 are retrieved against the official corpus. **RETRIEVAL IS NOT VERIFICATION.** **Do not re-run this pass** — the #93 exhibit behind that instruction is DISPLACED to batch 88's runner line.
- **THE WORKBOOK IS STALE IN SEVEN WAYS AND SAYS SO (`D-3`); rows untouched, still the numbering authority `V5-IDS`/`Q-T19-1` rest on. DISCLOSED, NOT REPAIRED**
- **REGISTRY RESEARCH SUPPORT — FIFTEEN DOCS, ALL PROPOSED OR RECORD-ONLY, NOTHING VERIFIED:** cite-check · workbook v1 (banner'd AND FROZEN as the numbering record) · citator pass (untouched by design) · `statute-pass-registry-retrieval-2026-08-14.md` — its **§3** carries the corrected A-for-space normalizer and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess* · deadline-engine memo · V-5/V-6/V-7 execution record · worklist (#97) · sign-off record (#98) · `registry-records-look-staging-2026-08-17.md` · `registry-verification-workbook-v2-2026-08.md` (**the WORKING LAYER**) · `fable-adjudication-record-2026-08-18.md` · `registry-new-entry-drafts-2026-08-17.md` · `criminal-opinions-read-2026-08.md` · `costs-and-privilege-authority-read-2026-08-18.md` · `t26-signoff-and-adjudication-record-2026-08-18.md`. **§3's normalizer must NOT be carried to the TRCP/TRE PDFs — raw TRCP `AA` count is 0, a different publisher**
- **`V-8` AND `V-9` ARE BOTH CLOSED — RULED 2026-08-18 (#100).** **`V-9` ADOPTED AS DRAFTED.** **CLAUDE.md rule 5 now carries a named-source fallback chain** — the court's own document; a paginated vendor copy stating on its face who delivered the opinion; or **your own identification, put to you and recorded** — with **cannot-identify-STOP as the floor** and unidentifiable entries **flagged, never staged for verification**. **`V-8` was ruled FRESH**: §0.1 gained **0.1.1 – 0.1.7**. **`V-9-UI` is recorded as PROPOSED design input only; Q-6 still bars wiring the API into the app**
- **EVERY CANDIDATE THE RECORDS LOOK PRODUCED IS RULED (`RL-1`..`RL-6`, all CLOSED), AND NOT ONE VERIFIED ANYTHING.** **`RL-3` made three cite acts and `RL-2` retired the *Alford* supersession inference.** **THE REPORTER-CITE FLAG SURVIVED EVERY ONE OF THESE ACTS, AND SO DID EVERY ENTRY'S UNVERIFIED STATUS — a cite act is not a verification.** **`RL-6`: the nine unnamed criminal opinions got a FULL READ (T-27), but the cite supply for entries 30 and 31 stays YOURS**
- **THE WHOLE DRAFTED BLOCK IS EXECUTED AND NOW ENTIRELY VERIFIED — SEVEN ENTRIES EXIST, THE EIGHTH DELIBERATELY DOES NOT.** Inserted UNVERIFIED at #108 — **192.3(h)'s own-statement right** (`WS-1`) · **193.4(a)** and **199.6** (`RL-4`) · **194.2(b)(9)** and **194.5** (`WS-2`) — and at #109 the **NEW 215.1(e)** entry and the **`WS-3`** entry. **All seven are VERIFIED as of #110.** **194.1(a) was NOT inserted** — it became a **second observation** on the VERIFIED "TRCP 194" entry under one-proposition-one-home; **the span flag is RESOLVED.** **The 215.1 preamble tension survives INSIDE a verified entry as verified text asserting no answer.** **`WB2-1`/`WB2-2` stand as ruled**

## Design-input memos — ALL PROPOSED BUT FIRM OBLIGATIONS (RULED `#155`), nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions. **No deadline engine is in the build queue.** **HEADLINE, A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) allows a phase-scoped send-by Level 3 period.** **Five questions, NO durable IDs (`ID-DL-1`), filed `[DL-memo Q1]`–`[DL-memo Q5]`, all five still ⬜.** **AN ELABORATION SPEC EXISTS AND AUTHORIZES NOTHING: `docs/specs/deadline-engine-spec.md` (PROPOSED)** — NO column, NO table, NO migration, and the 50-day conflict left FLAGGED with the skeleton UNEDITED. **`P-2`'s *“only to mail”* is IN DOUBT** — TRCP 21a(c) has long read *“by mail **or by commercial delivery service**”* — **not re-retrieved, and the highest-value verification target in the section.** Six questions `Q-DE-1`–`Q-DE-6`, packet-local, no series minted
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions. **PROVENANCE MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED** — the rules fix filing at transmission to the filer's EFSP (TIER A) while a Bexar county-court sentence appears to fix it at clerk acceptance (TIER B), and TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**) — **which FC-8's court-keyed profile answers by ruling: the key is the COURT, not the county.** **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing. All three rows stay ⬜.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9). Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument**. 
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** **IN-1 has NO design-doc home anywhere.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED and elaborated; its EXTRACTION half is UNRULED and only mapped.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all**, so a build implementing #64 alone ships with no VIN validation and no promotion gate, silently. **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR.** ***`Q-IN2-1` IS ANSWERED 2026-08-18, "Yes — both": the blank CR-3 form AND the versioned instruction manual, his hand***
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN. All seven rows stay ⬜. SEVEN OF SEVEN GATED, ZERO SATISFIED.** 🟡 **THAT GATE IS RULED IN SHAPE AND ITS ROW IS 🟡 (`#154`, item 7).** The composite is in the banner; **each build is its own authorization and none is given.** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **ITS ROW NAMES ALL THREE CONSUMERS (#94)** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`**. **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5).**
- **PR-3 RE-PARENTING PROPOSAL (#86) · QBO (#87) · RE-1 (#88) MEMOS.** QBO: **NO READ-ONLY OAUTH SCOPE**, and the economics are INVERTED (`Q-QBO-2`); trust/operating separation is a **naming convention QuickBooks does not enforce**, so a read integration **can show a per-matter trust figure that is confidently wrong** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED.** RE-1: both headline findings RULED — **`Q-RE-1` → "Both, separate triggers"**; **`Q-RE-9`'s sourcing wall → FC-14's fourth channel**; **TDRPC 1.04(f)(2) IS THE FIELD LIST** (consent **in writing, PRIOR to the referral**), **1.04(g) strips the fee rather than voiding the referral. `P-RE-1..12` ALL UNVERIFIED.**
- **COMMUNICATIONS-LOG INGEST MEMO (#89)** — Task 15. **DISPATCH CONTRADICTED:** two constraints it called **ruled** are, in the log, *"Claude recommendations, unruled."* — both from `future-modules-capture-2026-07-28.md` §3, the `Q-QBO-1` gate firing again (`Q-COM-1`). **They resolve differently, and the memo prices the architectural commitment for the first time (`Q-COM-5`) — for a channel the firm does not use.** **Teams SMS, `callRecords` and the Microsoft-native call-content path are detailed in the memo itself (`Q-COM-8`, `COM-LOOK-3`).** **`Q-COM-10`/`Q-COM-12` REMAIN YOURS**
- **WITNESS-STATEMENT CONVERSION MEMO (#99) + THE `WS-3` AUTHORITY READ (#102, T-28, CORRECTED #103).** The memo's headline stands: **on TRCP 192.3(h)(2) the line is crossed at RECORDING, not at transcription** — the recording is already a witness statement — **so a gate placed only at transcription sits downstream of the event it exists to control**; a witness statement is a Rule 194.2(b)(9) INITIAL disclosure owed WITHOUT a request (194.1(a)), against which 194.5 permits **no work-product assertion**; *Trahan v. Lone Star Title*, 247 S.W.3d 269, is the same failure sanctioned. **THE ATTORNEY-CLIENT LIMB IS DECIDED BY TWO PUBLISHED INTERMEDIATE OPINIONS, majority authorship confirmed on the face of each (V-9)**: *In re Fontenot*, 13 S.W.3d 111 (Livingston, J.) and *In re ExxonMobil*, 97 S.W.3d 353 (Seymore, J.) — a witness statement inside a confidential attorney-client communication **is privileged**, and ordering its production is an abuse of discretion. **BOTH REACH IT THROUGH COMMENT 9** (`Q-WS3-2`). ***CORRECTED #103: the "adjuster or third-party witness is never privileged" paragraph is WITHDRAWN.*** **Every proposition UNVERIFIED; no entry created**
- **T3 PILOT PROTOCOL (#90) · SESSION-LOG TOC (#91) · GO-LIVE RUNBOOK (#92).** The **TOC** is **`docs/record/session-log-toc.md`** — **186 rows to 186 dated entries, `#65`–`#155` GAPLESS**, regenerated over the log as THIS batch wrote it, so `TOC-4` FIRED (forty-ninth edition; firing count forty-one → forty-two). *(The log's LAST `## ` line is the `## ARCHIVED:` pointer heading, not an entry and never a row — a bare `grep -c '^## '` returns 187 against 186 entries and invents a missing row.)* Its finding: **the log runs TWO independent numbering systems and neither covers every entry**, and **NO ENTRY ANYWHERE CARRIES `#1`** (`TOC-1`); the systems' detail is DISPLACED to batch 88's runner line. **`TOC-5` unruled**; the archive's frozen index is still a second 141-row copy in the repo; **`Q-CAP-3` (does that path sync) is CLOSED.**
- **FIRM OBLIGATIONS — RULED 2026-09-10 (`#155`); THE BUILD SLICE `docs/specs/firm-obligations-build-slice.md` IS AUTHORIZED (`FOS-1`) AND UNBUILT.** Eleven decisions, his picks: `docs/specs/firm-obligations-rulings-2026-09-10.md`. The spec, as authored plus an add-only **§16** of amendments: `docs/specs/firm-obligations-module-spec.md` (the object model, behaviour, surfaces, **nineteen `FOD-` defaults**, **thirty-five `FOT-` seed templates** each with a named source and a status — UNVERIFIED / TIER B / NOT HELD / NOT READ / HIS FACT / PRACTICE / EVENT-DRIVEN / DO NOT SEED — and **eleven `Q-FO-` questions**) and `docs/specs/firm-obligations-ruling-sheet-2026-09-07.md`, the sheet he ruled from, now carrying a RULED line. **`FO-7`'s two facts as `#151` read them over the bridge at `ea5675b` — NOT this runner's read: no QuickBooks code or table; the research memo exists, PROPOSED — RESEARCH ONLY**; so `FO-3`'s conditional limb is ruled NOT to fire (DECISION 5: thin, no hook) — **`FO-7` CLOSED; `FO-5` CLOSED on DECISION 4 (owner scope now).** *"Firm obligation"* is the word (DECISION 0). **`FO-1`–`FO-7` and `BR-3` are register rows (DECISION 10); `BR-1`, `BR-2`, `BR-4`, `BR-5`, `DA-1`–`DA-4` and `INS-1` are DEFINED at `docs/record/firm-obligations-sitting-2026-09-10/voice2-definitions-carried-2026-09-10.md`, rowless.** **WEEKENDS (`FOM-12`, his composite):** a per-obligation `weekendRule` confirmed at activation, every template shipping `unknown`; weekends computed, holidays flagged, not computed. **DECISION 8:** the cases-vs-practice boundary DECIDED BY ACT — a fifth file, `docs/specs/legal-rule-registry-firm-obligations.md`, to be drafted by a LATER Opus act (PF-1 there), each entry UNVERIFIED; **NOTHING DRAFTED OR INSERTED.** **DECISION 9B:** all 35 templates offered, twelve intended go-live activations, his hand and his dates. **The spec §11 five are ACCEPTED as THE FIRM-OBLIGATIONS HANDS-ON SITTING, gated on the build.** **The rendered examples,** `docs/specs/firm-obligations-rendered-examples-2026-09-08.md` (RULING-class), carry the **FIFTEEN `FOM-` findings, ALL RULED at `#155`** — fourteen as proposed, `FOM-12` as his composite. **The mock is EVIDENCE** — `docs/record/firm-obligations-design-2026-09-07/firm-obligations-mock-2026-09-08.html` with its fourteen-file source tree and a **124-assertion harness**, hosted as the claude.ai artifact “Firm Obligations Mock”; **every date and firm fact in its fixture is INVENTED and must never be read as a fact about the firm.** Audit record: `rendered-examples-audit-2026-09-08.md`. **Twenty-one EVIDENCE files sit at `docs/record/firm-obligations-design-2026-09-07/`, the design pass's audit report among them; the sitting's four at `docs/record/firm-obligations-sitting-2026-09-10/`** — the ruling ledger, the definitions carry, v32 as delivered, the audit.
- **PRIVILEGE-TIER UNIFIED VOCABULARY (#102, T-30, CORRECTED #103) · DESIGNATION-SOURCE METHOD RESEARCH (#102, T-31, CORRECTED #103).** **T-30's headline SURVIVED THE AUDIT BUT ITS ARGUMENT DID NOT** (`Q-COM-10-A`, since RULED at #105), and **the 192.5(d) over-read is corrected** (`Q-COM-10-B`); both arguments are DISPLACED to batch 88's runner line. **The migration remains AUTHORED, UNRUN AND NOT AUTHORIZED.** T-31 stands on its structural finding — **Texas has TWO independent designation axes** (TRAP 47.2(a) Opinion vs Memorandum Opinion, memorandum being the **default** under 47.4; and 47.2(b) publish/do-not-publish, **criminal only**) — **but its two headline claims are RETRACTED:** the *"174,292 to 1"* measurement **had no control arm**; and **`"full precedential"` appears 0 times in the TRAP PDF** (`Q-DES-1`). **AND TRAP 77, NOT 47, GOVERNS THE CCA — 77.3 BARS citing unpublished opinions as authority** (`Q-DES-6`)

## For design side
- **CAPACITY — THE DESIGN SIDE'S WORD, NOT A REPO FACT.** At its open `#155` read the meter at **1,628,404 / 2,000,000 = 81.4%**, thirteen docs; the entry states no closing reading. `Q-CAP-1` RULED + EXECUTED (`#123`): log SPLIT at the 2026-08-13 cutoff; log + index in `docs/record/` (`TC-4`), **TRACKED, NOT gitignored — the sync exclusion is a picker act, `TC-OPEN-5`, Michael's hand.** **AT HEAD, RE-MEASURED BY `wc` THIS BATCH AND NOT CARRIED:** `docs/record/session-log.md` **186 dated entries, 91 design `#nn`, high-water `#155`, GAPLESS from `#65`** — **neither the log's byte size nor the two derived files' is published here, in the runner line, or in their own banners — each of those places sits INSIDE the record it would measure, so take them with `wc -c` at HEAD**; `docs/archive/session-log-archive-2026-07-21_2026-08-12.md` (`TC-6`, unmoved) **CLOSED AT BIRTH — no entry is ever added; corrections go in the LIVE log naming what they correct.** **`Q-CAP-4` RULED moot (`#126`), `WS-P2` CLOSED; `Q-CAP-5` RULED adopt-all-three — a STANDING design-side policy, NO runner change: (a) ≥ 90% at session open stages a capacity pass, (b) the ~700 KB re-split trigger, RETIRED by `TC-10` (2026-08-21), (c) re-measure at trigger-7.** **The `#137` Voice2 pair becomes TRANSIT-ELIGIBLE with this batch at HEAD** (its definitions carried into `docs/record/`, its `FO-`/`BR-3` rows minted) — **to be VERIFIED by a full-text read at a later sitting with his Chrome; nothing moved.** The `#155` pair enters project knowledge with TRANSIT lines. **TRANSIT: the third `TC-8` execution ran 2026-09-08 (`#153`); record at `docs/record/transit-2026-09-08/`.**
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built.
- **INSTRUCTIONS: `v32` DELIVERED 2026-09-10 (`#155`) AS THE TRIGGER-3 EDITION, filed as EVIDENCE at `docs/record/firm-obligations-sitting-2026-09-10/project-instructions-v32-as-delivered-2026-09-10.md` — PASTED OR NOT IS MICHAEL'S WORD; THE REPO CANNOT VERIFY THE INSTRUCTIONS FIELD AND DOES NOT.** **`v31` remains the last version confirmed in force by a live read** (`#155`'s); its as-delivered copy is `docs/record/transit-2026-09-08/project-instructions-v31-as-delivered-2026-09-08.md`. `PF-1`, FC-14's fourth channel and DT-1 remain in force.
- **DT-1: `#155` is stamped 2026-09-10, the Central date of its work (opened 21:34, rulings closed 23:05 CDT); this refresh carries the RUN date, 2026-09-11, read from the wall clock at 00:15 CDT.** **The rule is unchanged and is the class, not the date: every batch run between ~19:00 Central and midnight reads a UTC date one day ahead, any execution point DT-1 gains must read the wall clock rather than a shell, and “the Code session is local, therefore safe” stays false.**
- **SOURCING IS A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED), AND IT GAINS A FOURTH NAMED CHANNEL BY RULING (FC-14, 2026-08-18) — AND A FIFTH AND SIXTH RULED 2026-09-10 (`#155`, DECISION 9A): TIDC-published county indigent-defense plans, and the U.S. Code via OLRC / govinfo.gov, both riding the v32 paste.** The three were the official bulk statutes corpus, the clean-authority TRCP/TRE/TRAP PDFs, and the eCFR API. **The fourth: State Bar / court-published Texas conduct-and-administration rules as clean-authority PDFs in `Documents\Knowledge Repo\`, cited by each PDF's own effective date.** Michael acquired six by hand mid-session. **`Q-RE-9` — the convention's first proven gap — CLOSES with it.** **SOURCING itself is NOT amended in any repo file: the convention lives in the project instructions and the amendment rides the v23 paste.** **Q-STAT-2 through Q-STAT-6 stay OPEN** · **THE NORMALIZER GAP IS NOW CHARACTERIZED AS TWO CONTEXTS AND PROPOSED AS §3.1 — STILL NOT APPLIED, AND §3 IS UNCHANGED (`#151` recorded it, `#152` characterized it):** the statute-pass §3 normalizer's characterized set misses **thirteen** residual glued-`AA` sites — **twelve after a SEMICOLON** (`services;AAand`, in `tx.151`, `tx.171`, `tx.31`) and **one five-`A` run** that is the article `A` between two artifact spaces (`SOCIETY.AAAAAcooperative`). `docs/record/normalizer-semicolon-amendment-2026-09-08/` drafts the §3.1 text verbatim and MEASURES it: **Adoption is `Q-STAT-7`, Michael's — and it is applied by a LATER packet, never by `#152`'s, because two published normalizers were already wrong, both silently.** **The Family Code zip is ABSENT from the corpus**, already the acquisition list's recommended minimum
- **HK-7 STAYS OPEN, AND ITS ANNOTATION GAP IS RECORDED IN THE QUEUE ITSELF — RULED 2026-08-18.** The ruled remedy was a **FORWARD gap-note, not a reconstruction:** the row carries that annotations for **#94–#98 were recorded only in BUILD-STATE** and that **this note records the gap, not reconstructed annotations. Nothing was back-written** (K-6/K-7). **RE-CHECKED NATIVELY THIS BATCH by existence test on DECLARED paths only (HK-5 — nothing enumerated, nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL still ABSENT here.** **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**. **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SEVEN REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*` — count RE-DERIVED at this HEAD and UNMOVED (the amendment slice is a BUILD SLICE, not a capture, and does not join this count), but **the disclosures capture is now in its THIRD EDITION at the same path and filename (**253,299 B / 1,147 non-blank**, re-measured this batch by `wc` at HEAD; unchanged), the `2026-08-20` stamp being the CAPTURE date and deliberately preserved.** Its §18 holds the residue and its §11.6–§11.8 now hold the ruled paragraph schema. **No capture in the repo is unreconciled.** **`RF-2` REMAINS OPEN AND IS YOURS: `REQ-CAPTURE_disclosures-master-skeleton_2026-08-20.md`, the SECOND unfiled disclosures capture, is still unfiled — `H5` bars a machine search for it.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2 **The disclosures capture's §14.1 carries `#154`'s add-only `AS-Q4` annotation; the ruled text was NOT edited.**
- **YOUR HAND — OPEN ACTS ONLY:** (1) **THE POST-`CCS-1` WALK** — the seven proposed hands-on items, the eleven `SD-` text acts, the nineteen `SD-` defaults with `SD-3`'s scope veto, the confirm-or-edit marks, `spec-feedback.md` items 8–11, and the `CAP-2` exemption wording; (2) **THE TWO `H12-v` ASKS** — read the AWS BAA in AWS Artifact yourself, and send the malpractice-carrier email; **both were DRAFTED 2026-09-07 into project knowledge and neither was sent by Claude** (addressees are placeholders); (3) **`O-11`** `anon`/`service_role` residual-privilege diagnostics, **`O-12` DEFERRED** behind them. **ADDED 2026-09-09 (`#154`), ALL HIS:** (5) **Wave 0 (b)** — the two Outlook-defect evidence captures (Network tab on a first edit; the badge before any edit); (6) **Wave 0 (c)** — the CloudLex export rehearsal on the portal, to the existing OneDrive tree (importers later, by a Code session, against a FICTIONAL fixture; the real export never enters the repo); (7) **`Q-API-18`** — two facts: the tenant's subscription type (Billing → Your products) and the covered-status determination he left OPEN; (8) **`Q-API-19`** — does the process server use ServeManager, and job creation or status webhooks only (`H5` — his answer, never a sweep). **(4) and (9) are DROPPED, their numbers not reused: (4) is SUPERSEDED by (12); (9) is DONE — `Q-API-20` RULED *"After"* (`#155`).** **ADDED 2026-09-10 (`#155`), ALL HIS:** (10) **paste v32**; (11) **fire the FO build session** once `#155` is at HEAD — *"run docs/prompts/PROMPT-firm-obligations-slice-build-session.md"*; `FOM-8`'s live check needs his connected Outlook; (12) **the DECISION 9A acquisitions** — the State Bar Rules + MCLE Regulations, the TAJF IOLTA rules, the FA zip, the county plan(s), which counties his fact; (13) **the DECISION 8 drafting act**, an Opus design session; (14) **`Q-STAT-7`**, whenever he reads it. **Wave 0 (a) is AUTHORIZED but is a separate Code session with his sign-in, not his hand; Wave 0 (d) is a design act on request; Wave 0 (e) is DEFERRED to the mail-intake slice.**
- **STILL YOURS AND ALL SMALL:** **`Q-COM-10`**, which closes the moment you adopt the unified `privilege_tier` list — **`Q-COM-10-A` IS RULED AND CLOSED (#105), so `B`–`F` are unblocked** · **the cite supply for entries 30 and 31** (`Q-RL6-1`, `Q-RL6-2`) — `Q-RL6-1` resolves to ***Hurlburt*** and ***Pharr***, **whose read is AUTHORIZED to an Opus session and gated only on your two PDF pulls**; **its *Whaley* option is dead on the DO-NOT-PUBLISH finding**, and **entry 30 still got nothing** · **`V5-ATTRIB`**, which `RL-1` deliberately did not resolve · **`V5-IDS`** — derived IDs were executed on the #66 precedent, **but that precedent supports the NAMING and not the FILE SHAPE** · **and `RC-4`'s `CPRC § 18.001` registry candidate, UNVERIFIED and yours alone — **now a register row under §2, minted 2026-09-09**
**NEXT ACTS, IN ORDER, AS OF 2026-09-10 (`#155`), THE WAVE 0 ACTS RUNNING IN PARALLEL AS HIS TIME ALLOWS:** (1) **THE POST-`CCS-1` WALK**, called at `#150`; (2) **the two `H12-v` asks**, in parallel; (3) **THE FIRM-OBLIGATIONS BUILD** (the Code session he fires) **→ THE FIRM-OBLIGATIONS HANDS-ON SITTING → the migration by his hand → his real activations** — the ruling sitting is DONE (`#155`); (4) **the DECISION 8 drafting act**; (5) **the split-store slice, AFTER the walk** (`Q-API-20`); (6) **go-live, his day** · **CC-1 HANDS-ON SITTING — HELD 2026-09-05 (`#148`) WITH THE RUNNING PRODUCT IN FRONT OF HIM; SIXTEEN RULINGS, AND NINE OF THE TWENTY-ONE ACCEPTED ITEMS CLOSED.** **TWELVE ACCEPTED REMAIN, EVERY ONE GATED ON AN UNBUILT MODULE AND NAMED: `CR-7`, `CR-CONSTRAINT`, `Q-FE6-5`, `Q-FE4-1`, `Q-FE5-3`, `Q-IN2-7`, `Q-IN1-1`, `Q-IN3-6` (behind unbuilt modules per the 2026-08-24 audit's ripeness note); `DA-1`, `DA-3`, `DA-4` (document storage is gate 7, unbuilt); `FO-6` (no firm-obligations concept exists in the calendar code). SEVEN PROPOSED REMAIN, untouched: D-18, D-8, per-paragraph regenerate, `AS-Q14`, `AS-Q15`, `AS-Q16`, `AS-Q17`.** **FIVE MORE ACCEPTED-GATED, ADDED 2026-09-10 (`#155`): the FO spec §11 items, gated on the FO build in demo mode — THE FIRM-OBLIGATIONS HANDS-ON SITTING; its item 4 is `FO-6`'s, already among the twelve.** **AND TWO HELD ITEMS ARE ADDED 2026-09-09 under CC-1(b) — Claude's hold, STATED, not Michael's acceptance: `Q-API-14` (the OneDrive deep link, and the SKU/Intune facts, before any in-app document window; `DA-1`/`DA-3` unchanged) and `Q-API-3`'s filing half (the Graph-PDF fidelity test on his master `.docx`).** ✅ **`Q-IN2-1` HAS LANDED** — the blank CR-3 form and its versioned manual are in `Documents\Knowledge Repo` (his hand; NOT the repo). Record at `docs/specs/cc1-hands-on-sitting-rulings-2026-09-05.md`. Also from the sitting: `HS-3` **BUILT** and `HS-6` **BUILT** (the README's *“Known gap”*); `HS-4` is drafted as `SD-14` for his eye and stays ⬜; `HS-6` is rewritten as `SD-12`, PROVISIONAL, and stays ⬜; `HS-8` closed · **§3's sixteen durable IDs, NOT answered** · CR-3 blank form + versioned manual (`Q-IN2-1`) · `FC-12`, `FC-13`'s open half · SKILL.md v2 re-upload (SK-v2) · sync-picker clicks (Q-3, `WS-P1`/`WS-P2`) then a FRESH chat to verify picker state · P1 telemetry lockdown · HK-6 · HK-7 each session · V-2 (Ochoa first) · OBS-1 · CD-4–CD-13 · Q-STAT-2–Q-STAT-6 · the deadline memo's five + ID-DL-1 · Tasks 8–17: 22+3 (Q-FE5-9 first — inside a VERIFIED entry), 18 (Q-IN1-8 vetoes the series), 9, 10+`WF-LOOK-1`, 7+`PR3-LOOK-1`, 8+4, 9+3, 11+4, 7 (`Q-T3P-1`), 3 · `GLR-2`/`GLR-3` · `OPEN-5(b)` + `AUD-5..9` · `READ-A` · the workbook's `Q5` · the CHAT-DISPATCH v4 chain · delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\` (per #112 STILL PRESENT; NOT re-checked — HK-5)
- **TWO PRACTICE ITEMS FROM #106, REPORTED TO YOU IN-SESSION AND TOUCHING NO REPO FILE.** **(1) `Payout Template - Normal.xlsx` carries a LIVE SETTLEMENT in the template body** — the §8 type-over pattern, live in your current library; the PD-Only template is properly tokenized. **(2) The A-4 $150 vs $175 expense-fee conflict is CONFIRMED AT SOURCE.** Both are your files, not the app's
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. NOTHING BUILT — and the T-24 SPEC DRAFT (#99) DOES NOT PUT IT IN THE QUEUE.** **THE DISPATCH'S TAXONOMY LINE WAS ONE CATEGORY SHORT — the ruled list is SEVEN (BP/STO/OO/EV/CONTRA/PRIV/RFP-form).** **CONTRA HAS NO VERIFIED AUTHORITY AT ALL** — it rests on entries 2 and 12, and the conference fuse rests on entry 13's wording. **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` is recorded as sitting untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand.** **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-6 + MM-1 + OPEN-5(a) + TOC-4 + TC-2..TC-5/TC-12 + CAP-1..CAP-4 ALL CLOSED and in force; runner v13** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy** — `/queue-runner` points to it). **Your hand: the P15 user-level copy deletion.** Each batch: every verification **names the command that produced it**, Step 0 reading `git ls-remote`, not the tracking ref; **the queue merge is THREE acts since `CAP-3` — the rows, the Status paragraph's CURRENT reconcile sentence (its predecessor appended to the closed register's RECONCILE HISTORY), and the MOVE of every row flipped ✅**; **pin packet identity at Step 1, delete BY EXPLICIT FILENAME**; a health-check skip is RECORDED, never passed off as a pass; **`TOC-4`** regenerates the index every batch and **`TC-2`/`TC-5`** regenerate `session-log-head.md` IN FULL, never appending; **`CAP-2`: every routing row carries a CLASS, `EVIDENCE` born under `docs/record/` and never in `docs/specs/`**; **`CAP-4`: this file has a 100,000-byte ceiling beside its 150-line cap, by `wc -c` on the working tree.** **QR-3 CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74, #93)
- **#104'S TWO RUNNER-FOUND DEFECTS STAND UNEDITED AND ARE CARRIED BY THE QUEUE ROWS AND `Q-T19-3`:** *Whaley*'s **DO-NOT-PUBLISH** notation (so it has no precedential value under TRAP 47.7(a), killing `Q-RL6-1`'s option (d), and FLP's `status: “Published”` on that very opinion proves the FLP field is not evidence of a Texas designation) and a **V-9 breach on *Anastassov*** (characterised while authorship went unchecked; the court's own document reads *“SLAUGHTER, J., delivered the opinion for a unanimous Court”*, so the holding is stronger than claimed but the method failed). **The narrative is DISPLACED to batch 93's runner line**
- **THE MULTI-AGENT READ-ONLY PREFLIGHT IS STANDING LAW — `Q-AUDIT-1` RULED 2026-08-18 (#105), `PF-1`.** **Trigger: any packet carrying a legal characterization or a proposed registry entry. Run by the DESIGN SESSION before shipping, pre-`inbox/`.** **OPERATIONAL WARNING THAT SURVIVES: at #104 the agent fleet DIED — seven agents, twice, all on `API Error: 529 Overloaded`, zero tokens — and the harness returned `{"surviving_defects":[]}`, a TOTAL FAILURE WEARING THE COSTUME OF A CLEAN BILL OF HEALTH.** **PF-1 fires on the SENDING side, so no queue-runner batch can satisfy it**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered.** **`FC`, `D-SMTP` and `GL1` are the newest series and only `GL1` needed a flag**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** — **which `Q-IN2-1`'s "Yes — both" now answers, once the documents land.** Synthesis `cr3-field-code-map.md`, **PROPOSED input — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: #62–#64 VERIFIED design-side FULL-TEXT at #65 (08-13); #67 verified at #68; `#117` (08-19) and `#124` (08-20, the GATE-3 run's four handed-back items) CLEARED BLOCKS ITEMISED, NOT BLANKET; `#125` (08-20) SPOT-VERIFIED batch 77 — placement and counts only, NOT a full-text re-read.** **STILL UNREVIEWED, recomputed from the log at head (A-4, #82): the carried #31–#33, #37–#61's routing, #75–#155 — **81 entries on the inherited basis, re-derived at this HEAD (A-4)**, `#155` filed here — the runner lines interleaved through the ninety-sixth, the unnumbered C-2 Code entry, the unnumbered `CCS-1` build entry of 2026-09-07 (`TOC-6`), and — named because `#117` expressly declines them — task C3's substance, every live-database reading, C1's classification table, the health figures, anything about origin, and the seventy-first packet's twelve §1 pins, PERMANENTLY uncheckable (packet deleted).** Do not copy #36's clearance forward. **Everything below `#65` is in the ARCHIVE — archived is not reviewed.** *(**Honest limit: the `#75` floor rests on no explicit clearance** — #65, #66 and #68–#74 were DROPPED, not cleared. Inherited basis #75–#155 = 81; STRICT positive-clearance basis #65–#155 less #67 = 90; the nine-entry gap is exactly the undetermined set, which `#117` left to you.)*
- **EVERYTHING AWAITING YOUR *RULING* IS IN `docs/specs/attorney-review-queue.md`; the go-live gates are NOT there — Gates 1–10, `GL-1`'s floor items and `T#1` live in `Go_Live_Gates.md`, their STATUS lives HERE, and they await ACTION, not a ruling (`#114`). Reconciled through `#155`.** **THIS BATCH FLIPPED THIRTEEN ⬜ → ✅ AND MOVED THEM (the twelve `Q-FO-` rows and `Q-API-20`), BORE TWO ⬜ (`FO-6`, `BR-3`) AND SEVEN ✅ (`FO-1`–`FO-5`, `FO-7`, `FOS-1`), ANNOTATED `Q-STAT-5` add-only AND AMENDED the FIRM OBLIGATIONS heading.** ⬜ 386 → 375. **THE REGISTER IS TWO FILES SINCE `CAP-3` and the counts come from BOTH, by LEADING marker (`^\s*- (⬜|✅|🟡)`), measured this refresh:** `attorney-review-queue.md` (SYNCED, **446,315 B**) holds **375 `⬜`, 7 `🟡`** plus the **11 ✅ parents held back, each having an indented open child (`CAP-OPEN-2`)**; `docs/record/attorney-review-queue-closed.md` (repo-only, bridge-reachable, append-only, **291,131 B**) holds **163 `✅`** — **total ✅ 174 with the eleven held parents; the closed file CONSERVED: 143 + 13 moved + 7 born = 163.** So **375 open, 382 open-in-substance** with the seven `🟡`. **This batch appended the superseded `#154` sentence to that same block.** **OPEN AND ROOMLESS: `DA-3` — the PHI boundary on phone-in-public document access, asked twice in `#137`, never answered — now DEFINED in the carried EVIDENCE file (`#155`) and still rowless; `DA-1`, `DA-2`, `DA-4`, `BR-1`, `BR-2`, `BR-4`, `BR-5` and `INS-1` are defined there too, rowless; `FO-1`–`FO-7` and `BR-3` have rows. `RC-1`–`RC-3` are OFF that list — CLOSED at `#139`. **Minting the rest of the `RF` series, or any of §3's sixteen, is STILL YOURS.** **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f → `case-heartbeat-design.md` §8; no law-change ledger; Outlook unreviewed
