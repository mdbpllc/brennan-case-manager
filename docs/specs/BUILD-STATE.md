# BUILD STATE — brennan-case-manager
Commit: 3773e6d (session start)  |  Branch: master  |  Generated: 2026-08-19 Central (one hundred third refresh)
*`3773e6d` is the commit this snapshot was written FROM. **It DESCRIBES the tree as this batch commits it**, so the batch's own commit — the SHA you are told — is the one at which every claim below verifies; `3773e6d` is not.* **AND ONE CLASS OF CLAIM VERIFIES AT NEITHER: the LIVE-DATABASE facts below (#113) AND SUPABASE PRO STATUS (#114) are Michael's own reports — query output in the first case, a vendor dashboard in the second. No repo check can reach them, they are NOT marked re-derived, and `OPEN-5(a)` does not reach them — asserting otherwise is the failure that rule exists to prevent.**
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*
***DO NOT CITE THIS FILE BY LINE NUMBER IN A PERMANENT ENTRY.** RULED PROJECT-WIDE 2026-08-16 (#94, cite-stability, riding v20): line cites are permitted only into **append-only** files; **prepending** files (the session log) and **wholesale-rewritten** files (this one) are cited by heading or quoted sentence.*
*EVERY COUNT BELOW WAS RE-DERIVED THIS REFRESH, NOT CARRIED — runner v11 Step 4 item 3 (OPEN-5(a), ruled #94).*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS COMPLETE — ITEM 7, THE LIVE
MIGRATION, RAN AND VERIFIED 2026-08-19, AND ALL THREE PENDING MIGRATIONS ARE NOW RUN (#113).**

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **AUDITED 2026-08-15 (#79): `record-integrity-audit-2026-08-15.md` — 65 claims in this file and the queue: 46 SOUND, 6 stale, 1 CONFIRMED-FALSE, 1 contradicted.** Six findings RULED AND CORRECTED (#82, A-1–A-6); **`AUD-2` and `AUD-3` still carried unchanged** — they sit in **OPEN-1**/**OPEN-2** territory, where correcting them without your ruling makes the auditor an editor
- **WITHDRAWN, NEVER ROUTE: `registry-retrieval-completion-2026-08-16.md` (#93).** Written on a false premise and withdrawn the same session. **It never reached the repo.** If a copy arrives by any path, it is an error
- **THE FIVE CHAT-DISPATCH v4 RESEARCH DOCUMENTS WERE ADVERSARIALLY AUDITED AND ARE CORRECTED IN PLACE (#103).** Twenty-two defects — **3 HIGH, 8 MEDIUM, 11 LOW**. **Nothing propagated: no registry entry was ever created from any of them.** Each opens with a `⚠ CORRECTED IN PLACE 2026-08-18` banner quoting its own retracted text — **do not "restore" or merge any retracted passage**. **THE FOUR AUTHORITIES THAT AUDIT SURFACED ARE READ (#104).** **SIX FABLE ADJUDICATIONS FOLLOWED: #105** (`Q-AUDIT-1` → PF-1 standing; `Q-COM-10-A` → `privilege_tier` purpose-scoped, Option 2), **#106** (the FC form-corpus block, THIRTEEN RULED), **#107** (the project-knowledge prune), **and #108 → #109 → #110, ONE EVENING CONVERSATION IN THREE PACKETS, EXECUTED BY THREE CONSECUTIVE RUNNER BATCHES.** Records: `fable-adjudication-record-2026-08-18.md` (26,759 B / 329 non-blank), `fc-adjudication-record-2026-08-18.md` (25,182 B / 299), `t26-signoff-and-adjudication-record-2026-08-18.md` (32,720 B / 321), `post-execution-signoff-record-2026-08-18.md` (21,665 B / 209) `five-look-signoff-record-2026-08-18.md` (10,576 B / 123) and `grok-external-review-2026-08-18.md` (**29,458 B / 238 — the only one of the six authored OUTSIDE this project's models. The "168" it has carried since it landed is WRONG AT BIRTH rather than drift: `git log -S` puts the wrong figure and the file itself in the SAME commit, `d6f97e6`. Corrected at the sixty-seventh and RE-DERIVED AGAIN HERE at 238**). **THOSE THREE MOVED THE REGISTRY AND NOTHING ELSE HAS: twelve entries VERIFIED between them, seven inserted, five conformed verbatim**

## THE FC BLOCK — ruled 2026-08-18 (#106), ALL DESIGN DIRECTION, NOTHING AUTHORIZED TO BUILD
- **THE ONE CONVENTION, HIS WORDS: *"My system should stay true to one convention as much as possible."*** **`{token}` — single brace, bare name — IS CANONICAL** (FC-1): the stored form, the editor's display form, and the form every new template is written in. **The importer accepts legacy conventions and emits canonical** — `{{TOKEN|filter}}` (the 15 MDB PLLC production letters) and registered bracket tokens — **and legacy forms are NEVER written back**
- **FC-1–FC-4 ARE FOLDED INTO `form-engine.md` §3** (#106), appended as a closing subsection: per-spot formatting lives in **TEMPLATE SETTINGS**, not token text (FC-2); bracket handling on import is an **ALLOWLIST, never a pattern match** (FC-3 — 147 `[signature block on next page]` notes, 80 `[s]` markers and Texas citation parentheticals would otherwise become data fields); `[s]` **converts** to the grammar engine's party-count flex point (FC-4). **§9 was NOT touched** — a verified pure insertion, 25 lines, zero deletions. **This matters to FE-D1: slice 1 hard-codes a parser, and the spec it reads now states the ruled syntax**
- **THE REST OF THE RULED THIRTEEN, ALL DIRECTION AND NONE OF IT A BUILD ITEM.** **FC-5: fee basis STAYS PER CLIENT** — D-CL2-3 stands, contrary stream-rate evidence recorded as considered and not followed · **FC-6: the reduction model runs FORWARD** — client net is an OUTPUT, ruled on MDB PLLC's own payout templates read at source at his direction; **client-net-as-input worksheets are the prior firm's method** · **FC-7: deadlines are TWO-TRACK**, his definitions · **FC-8: a court-keyed deadline PROFILE plus STRUCTURED GEOGRAPHY on `cases` when a schema act is next authorized — NO MIGRATION AUTHORIZED** · **FC-9: both rule kinds plus a holiday calendar** · **FC-10: consent is a FIRST-CLASS ENTITY, expiry a field** · **FC-11: the breakdown is a LIVING scenario document that FREEZES at client signature**, and a case may carry retainage plus a second breakdown per later recovery · **FC-15: the designation paragraph STAYS, whole, in all 12**
- **FC-12 AND FC-13 ARE BOTH RULED AND CLOSED (#108) — AND NEITHER AUTHORIZES A BUILD.** **FC-12: "(c) Structure only."** The corpus may contribute **skeletons, section orders and clause lists — NEVER body text**; body prose is always written clean. This REPLACES the interim no-imports default and makes prior-content leakage structurally impossible, the evidence being that **nothing in any of the four libraries is safely blank** and **six of the fifteen tokenized letters hard-code prior content**. **NO IMPORT TOOLING IS AUTHORIZED TO BUILD BY THIS RULING** — a separate, later act. **FE-D1 is unaffected** (its §11.3 skeleton works from your own shell). **FC-13: all four picked, plus the fifth.** Registry entries OPEN for **Prop. Code ch. 55**, **CPRC ch. 146 incl. the 2025 (c-1) amendment**, **TRCP 47(c) bands**, **§18.001/§41.0105** — and **TDRPC 1.04(f), the registry's FIRST practice-of-law proposition.** All five were **DRAFTING ACTS FOR A LATER OPUS SESSION — AND THAT SESSION HAS RUN (#112): six texts now exist at `docs/specs/fc13-entry-drafts-2026-08-18.md`, DRAFTED AND NOT INSERTED, each entering UNVERIFIED if and when it is. The reconcile is the finding — not one of the five destinations is free, so `FC13-Q-1..6` ask whether they insert at all and where.** **You chose the plain open over the boundary-ruling variant, so the cases-vs-practice boundary stays UNDECIDED**

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE MEANS: real case / party / client / SOL data hand-entered into the core app — NOTHING MORE.** Floor, in order: **(1) the CD-1 build landed and exercised — COMPLETE 2026-08-19, the migration RUN and verified · (2) gate 1 Supabase Pro — **COMPLETE 2026-08-19, PRO ACTIVE** · (3) gate 9 production SMTP · (4) gate 3 RLS tested INCLUDING the slice's two new tables, WHICH ARE NOW LIVE · (5) gates re-check session + the trigger-1 instructions revision.** **TWO OF THE FIVE ARE DOWN — items (1) and (2), both cleared 2026-08-19 and both by YOUR hand — LEAVING THREE: gate 9, gate 3 and the item-(5) re-check. Counting GATE 10, which sits AHEAD of the floor, the launch path stands at FOUR.** **WHAT PRO BUYS IS RECORDED BECAUSE THE GATE'S OWN TEXT IS WHY IT WAS ON THE FLOOR: daily backups, no pausing after inactivity, 8 GB — and the consequence FORWARD is the backups. Every schema act until 2026-08-19, the three migrations of that morning included, ran against a database with NO automatic backup; gate 10's migration will be the first schema act with one behind it.** **Pro status is YOUR report from the #114 design session — no repo check reaches a vendor dashboard and none was attempted; it is NOT marked re-derived.** Full text: `Go_Live_Gates.md` *(**GATE 10 ADDED 2026-08-19** — C-4, ruled 2026-08-18: DOB / SSN / driver's-license values promote out of the `parties.fields` jsonb into dedicated columns **before any real client record enters the database**. It sits AHEAD of the floor, not behind it.)* *(**AUD-2 / OPEN-1 / GLR-1 ALL CLOSED — RULED 2026-08-18 (#100).** One line APPENDED to GL-1 (append-only honored, the frozen "v15" untouched): **item (5) reads as "the instructions current at the re-check, per trigger 1"** — **the number stops carrying meaning, so it can never drift again.**)*
- **A POINTER CHECKLIST OVER THE GATES EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It restates no gate's substance and carries NO status column, by design** — status is this file's job, and a second status snapshot is the `#13`/`R-3` copy-forward failure. **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect**
- **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor** — drafting continues via the skill
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. Appending a gate is the ruling.)*
- **THREE PLACES WHERE READING A GATE ALONE GIVES THE WRONG ANSWER (#92 — recorded, NOT corrected).** **Gate 2 read alone OVER-BLOCKS** (the 08-11 clarification is load-bearing). **Gate 3 read alone looks satisfiable and is not** — gate 6 is a hard prerequisite and **the dependency is recorded at gate 6, not at gate 3.** **Gate 9's trigger is a different SHAPE** — the others fire on real *data*, gate 9 on *reliance*, **so a checklist ordered by "when does real data arrive" schedules it too late**
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**
- **ITEM 7 RAN 2026-08-19 AND IS VERIFIED — CD-1 IS COMPLETE (#113).** `db/migrations/2026-08-12-cd1-contact-directory.sql`, run FIRST of the three by your hand, its five checks answered in words: **C1** 0 untagged / 0 mismatched against 2 `parties` rows · **C2** 0/0/0 against 2 `case_parties` rows · **C3 TWO open roster flags against two links both carrying role `Client`** — the high end, which is the RIGHT end, since the file warns that a LOW number would mean something was guessed, and two flags for two Client links is one-for-one · **C4** `authenticated` reaches both new tables · **C5** `to_regclass('public.case_links')` null, the CL-1 firewall holding. **THE TWO-ROW CAVEAT, STATED RATHER THAN GLOSSED: the backfill ran over TWO rows and steps (c) and (d) matched NOTHING** — no Plaintiff, Defendant, Witness, adjuster, provider, expert or judge link exists live — **so the caption-alignment and function-role branches were NOT exercised here.** They were exercised in code on fixtures at #61, and GL-1 item (1) is satisfied by the PAIR; **a two-row backfill proves less than "verified clean" reads.** The PG15+ `unique nulls not distinct` bar it could not have cleared before the upgrade is retired, and **nothing in the file was changed for it**
- **THAT ORDER HAPPENED, AND THE FAILURE IT WARNED OF DID NOT (#86 → #113; `Q-PR3-3` ANNOTATED, STILL OPEN).** CD-1 ran with **PR-3 unexecuted** — the sequence this path scheduled and the row warned about, in which probate rows are still PI at that moment, take the plaintiff-defendant derivation, and are never revisited. **Backfill step (c) matched ZERO rows**: both live `case_parties` links carry role `Client`, which step (c) does not target, **so no row took that derivation and the quieter failure never occurred.** **The backfill limb is SPENT. The row is ANNOTATED, NOT CLOSED** — whether it keeps substance beyond the backfill is yours, and it stays coupled to the `sideSetFor()` defect below, **which is untouched and still fails open for a fourth practice area**
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED**
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **THE PROTOCOL FOR STAGE 1 EXISTS AND AUTHORIZES NOTHING (#90, Task 16): `docs/t3-pilot-recording-protocol.md`** — 534 non-blank / 688 raw, **PROPOSED — PREPARATION ONLY.** `R`/`K`/`I` provenance marks intact, **`I` NOT promoted**; its §5.3 placeholders are **deliberate — no audio file was measured.** **Seven questions `Q-T3P-1..7`, packet-local.** Placement at `docs/` is your **interim** direction, not a ruling (`Q-T3P-7` open)
- **ITS HEADLINE IS THAT HK-4 MAY DESCRIBE AN ACT THAT ALREADY HAPPENED.** HK-4 and this file both read as *"create the directory at staging time"*; the **2026-07-25** pilot-fixtures entry records the thirteen as **already archived at `..\data\pilot-recordings\` WITH A PROVENANCE README no design session has ever read.** **So HK-4 is probably a P15 → P1 TRANSFER — cheaper and more dangerous, since a transfer can silently overwrite that README** (`Q-T3P-1`, `Q-T3P-2`). **The bundle also held ground-truth scripts, scorecard and findings, while HK-4 names only audio** (`Q-T3P-3`)
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **YOUR HAND (HK-4), NOT RE-WORDED.** **Re-checked natively this refresh against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** A negative about **this** machine that **does not answer the preflight row**, which is yours
- **THE MEASURE ITSELF IS SPLIT, WHICH NOTHING ELSE STATED (`Q-T3P-4`).** The authorization reruns both batches *"against the existing ground truth"* — but ground truth exists for **five of thirteen**. Batch 2 is scripted → an **ACCURACY** measure; batch 1's "existing" artifact is the int8/CPU-floor transcript, a prior **OUTPUT** → a **CHANGE** measure. One figure reports two different things. **D1 auto-file and the confidence thresholds wait on this scorecard**
- **COVERAGE, PLAINLY: the thirteen exercise ROUTING thoroughly and the ASR stack NARROWLY.** No long-form audio (max ~71 s; deposition length is the Statement Bank's real use case, untested — Phase 0's 79 MiB / 20 MiB activation figures are **3-second datapoints, not extrapolable**). The **4-speaker cap is untested**. **No Spanish** — accuracy AND the flagged non-English diarization degradation. **Nothing from the capture kit, which is not purchased. Word boosting has never run on any recording.** All thirteen are iPhone / Voice Memos / **AAC ~70 kbps mono** and can never meet the kit standard — **preserve bit-exact, no re-encode/up-sample/normalize/trim**, because an up-converted file *looks* like kit-standard material in its header
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (unauthorized). A **second, unrelated `T1`–`T4`** is the LegiScan poller tier in `statute-text-and-bill-tracking-design.md` — **substantially BUILT.** **Five WF gates say "gated on T3" without naming which**; re-lettering is yours (the H → HK precedent)
- **THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until you locate it or re-issue, T3 WORK IS UNAUTHORIZED.** **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6`**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher
- **NOTHING EXISTS AT INTAKE, WHICH IS WHERE THREE OF RE-1'S FOUR TRIGGERS FIRE (#88, `Q-RE-4`).** The master spec **designs** the PNC funnel and calls it SETTLED — PNC → Client / Declined / **Referred out** — and **it is not built**: word-bounded `PNC` returns **ZERO** across `db/` and `supabase/`, `parties` has **no status column**, and three separate places already record the missing PNC → Client promotion path

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)** |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** | bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients.** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab, so the rule narrows what is OFFERED, never what is REACHABLE.** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT — RE-AFFIRMED 2026-08-18 (FC-5), contrary stream-rate evidence recorded as considered and not followed.** **D-CL2-3a: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. The time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter interface — every feature works in both modes. Seeds fictional; **store v11** (CD-1), v9→v10→v11 chains **FORWARD in place**, backing up at each step. **`npm run dev:demo`** runs demo mode past a real `.env`; **demo state does NOT travel**
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **36 `create table` statements in schema.sql AND THE LIVE DATABASE IS NOW AT 36 — the two figures agree for the first time since 08-12**, CD-1's `case_roster_flags` and `contact_edges` having landed 2026-08-19. **THE SERVER IS PostgreSQL `17.6`** — three majors above the 14.5 that stopped ruling 4, not merely the "15+" the record carried. *(The 36 in `schema.sql` is re-derived here every refresh: 36 tables, 36 `enable row level security`, **35 policies** — the gap is `file_counters`, deliberately policy-free as the probe's deny control, so never "correct" 35 to 36 for symmetry. **The live 36 and the 17.6 are YOUR query output as reported at #113; no repo check reaches them and they are NOT marked re-derived.**)*
- **NINE SCHEMA RULINGS CAME OUT OF THE GROK EXTERNAL REVIEW (#111) AND EIGHT ARE IN `db/schema.sql`; THE LIVE HALF RAN 2026-08-19 AND ALL TEN CHECKS CAME BACK CLEAN — `db/migrations/2026-08-18-grok-review-fixes.sql` (#113).** **F-1 WAS A LIVE EXPOSURE AND IS NOW CLOSED:** no PUBLIC entry survives on `next_file_number()`, which as a `SECURITY DEFINER` counter-writer was `anon`-callable over PostgREST RPC from 2026-07-28 until 2026-08-19 — **and the PG17 default was checked against the documentation rather than assumed, because the server had moved three majors.** **F-3 confirmed live on both limbs**, the function body and `cases.date_opened`'s default each reading `America/Chicago`.** Landed in the file: **`revoke execute on function next_file_number() from public`** (F-1 — Postgres grants EXECUTE to PUBLIC on new functions and `CREATE OR REPLACE` keeps that ACL, so this SECURITY DEFINER counter-writer was callable by `anon` over PostgREST RPC; the REVOKE is ordered AFTER the replace) · **`America/Chicago` on both the file-number year and `cases.date_opened`** (F-3 — `now()` and `current_date` are UTC on Supabase; a case opened 2026-12-31 18:05 Central would have been issued a `27-` number) · **a BEFORE UPDATE trigger refusing any change to `file_number`** (F-2) · **`created_by uuid references auth.users (id)` on `cases`, `parties`, `transcripts`, `medical_bills`, `generated_documents` with an `auth.uid()` BEFORE INSERT trigger** (F-25 — **columns only; NO per-user RLS, no policy touched, and the policy count reads 35 before and after**) · **the "gapless" claim OUT of the schema header** (C-6 — the requirement is now "unique, year-scoped, not client-assigned" and holes are documented as normal) · **the stale 32/31 grant-block counts rewritten COUNT-FREE in both places** (F-27)
- **THE NINTH RULING LANDED THE SAME NIGHT, AFTER THE GATE IT WAS STOPPED ON WAS CLEARED (ruling 4, F-4+F-18).** `case_parties` now carries **`constraint case_parties_roster_identity_key unique nulls not distinct (case_id, party_id, role, capacity_kind, capacity_points_at_party_id)`** plus **`case_parties_capacity_pointer_check`** — `next-friend-of` / `representative-of-estate-of` / `dba` require a pointer, `individually` exempt by design. **It was STOPPED first**, both halves, when the database read 14.5: the ruling makes PG15+ a hard bar, and a COALESCE-based equivalent would have been a different DDL than the one ruled. **Michael upgraded, and it landed under the EXISTING ruling — no fresh authorization, only the gate passing.** **IT RAN 2026-08-19, AND THE PAIR THAT COULD HAVE FAILED SILENTLY DID NOT:** R4 reads **ONE** unique constraint on `case_parties`, **ZERO** of the old three-column key, the new roster identity key present in full, and `case_parties_capacity_pointer_check` at **1**. The file carries a `server_version_num` guard **REFUSING to run below 15** rather than trusting the report, and drops the old key **by looking it up in `pg_constraint` by DEFINITION rather than by a guessed name** (the F-23 failure mode, avoided deliberately) — **and the live constraint was read BEFORE the run and returned a character-exact match, so the lookup was known to be able to fire before it was asked to.** Its `raise notice` was treated as decoration, not evidence: the Supabase editor does not reliably surface NOTICE, and **a check that cannot produce a disconfirmation is not a check.** **O-8 and O-9 both close**
- **F-12's STEP (d) FIX EXECUTED LIVE 2026-08-19.** `party_status = coalesce(party_status, null)` — which would have left every Witness / Adjuster / provider / expert / judge NULL though `'non-party-actor'` exists in the CHECK — now writes the value. **It was edited while the migration was still unrun, on your in-session confirmation; it has since run. The repo could establish NEITHER fact — both are your word, then and now**
- **A CASCADE/RETENTION MAP EXISTS AND NOTHING IN IT IS EXECUTED: `docs/specs/cascade-retention-map-2026-08-18.md`** (**PROPOSED — AWAITING SIGN-OFF, O-7**). C-1's principle is ruled; this is the specific DDL, and it is **yours before anything runs.** Every FK extracted mechanically from the DDL: **eleven** FKs across C-1's six named children mapped current→proposed (CASCADE/SET NULL → RESTRICT), **four component FKs argued for KEEPING cascade**, and **six adjacent FKs listed explicitly as UNRULED** rather than folded in. **Read its §6 first: RESTRICT without app-layer soft-delete handling means matters can no longer be deleted at all, and no app work was proposed or authorized**
- **GRANTS ARE PART OF THE SCHEMA — load-bearing.** `authenticated` ONLY, **`anon` gets nothing by design** (`db/migrations/2026-07-28-api-role-grants.sql`). **`ALTER DEFAULT PRIVILEGES` is NOT set: every new table must carry its own GRANT or it is unreachable.** CD-1's two carry their own
- **`privilege_tier` NO LONGER ASSERTS A STANCE NOBODY CHOSE — Q-COM-11 RULED (A) AND THE SCHEMA HALF IS EXECUTED (#94).** Both columns (`generated_documents`, `transcripts`) **lost `default 'work-product'` and `not null`** in `db/schema.sql`; **NULL now means unclassified-must-classify.** **THE MIGRATION RAN 2026-08-19 (#113): both columns nullable, NO default, and BOTH CHECKs untouched — the two vocabularies still disagree, which is the CORRECT result and the named failure the file was written to avoid** — five verification checks in words, one of which reads both CHECK vocabularies and states that **two different lists is the correct result today**, so nobody "fixes" Q-COM-10 while running it. **No existing row was re-characterized** (all fiction; re-characterizing is a legal act and yours). **That divergence is closed — the default is dropped on both columns live.** Two honest limits: its C2 nothing-re-characterised check is satisfied **TRIVIALLY, not meaningfully** (both tables are empty, so nothing COULD have been), and **check 4 — insert-a-null inside a rolled-back transaction — was NOT run**, C1's catalog read establishing the same fact without creating a fixture row. **NO creation-time classification UI was built — that is the recorded follow-on act**
- **THE TWO `privilege_tier` VOCABULARIES STILL DISAGREE IN THE SCHEMA, AND THE DIVERGENCE IS DUPLICATED IN TYPESCRIPT (#85 → #89 `Q-COM-10`, extended #94) — BUT THE SHAPE THEY MUST CONVERGE ON IS NOW RULED (#105, `Q-COM-10-A`).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')`, while a comment in `schema.sql` calls the first *"the shared system-wide set."* **NEITHER CHECK WAS TOUCHED and nothing was built.** **`src/domain/billing.ts` and `src/domain/transcripts.ts` each `export type PrivilegeTier` with DIFFERENT members** — **so the question has FOUR homes and any ruling touches `src/` as well as `db/`.** **THE RULING: `privilege_tier` is NOT exhaustive** — a purpose-scoped classification under a stated precedence rule, orthogonal attributes in their own fields — **and the target is Option 2: THREE values plus a `witness_statement` boolean**, which satisfies `WS-4`'s fourth state without flattening. **`Q-COM-10-B`–`F` are unblocked; `Q-COM-10` itself closes when you adopt the list**
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view**, and a word-bounded sweep of `db/` + `supabase/` returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **THERE IS STILL NO GEOGRAPHY ON `cases` — BUT THE QUESTION ABOVE IT IS NOW RULED (`Q-RE-1`, CLOSED 2026-08-18, his word: "Both, separate triggers").** **CORRECTED THIS REFRESH — THE FILE HAS BEEN WRONG ABOUT THIS AND IT MATTERS MOST WHERE IT IS WRONG: `cases` ALREADY CARRIES `county text`**, added by the OAA criminal-intake block in `db/schema.sql` (`alter table cases add column if not exists county text;`) along with `in_custody`, `custody_location` and `appointment_date`. `court_name` and `cause_number` remain free text, and **`venue`, `district` and `jurisdiction` are genuinely absent — word-bounded, all three return ZERO.** The old blanket claim was **partly** true, which is worse than wholly wrong: **FC-8 would have authorized adding a column that already exists.** The out-of-area referral trigger tests **geography AND subject-matter INDEPENDENTLY** — two triggers, two data requirements. **FC-8, the same session, puts structured county / court / district fields on `cases` WHEN A SCHEMA ACT IS NEXT AUTHORIZED, and no migration is authorized by either ruling.** So the geographic limb is answerable from `county` and nothing else — a starting point, not a blank
- **A WRITTEN-CHANNEL COMMUNICATION STILL HAS NOWHERE TO BE, AND THE VALUES ARE NOW NAMED (`Q-COM-2`, CLOSED 2026-08-18 as ruled-DIRECTION, his word: "Yes — adopt the list").** `transcripts.source` and `staging_items.source` **both** still admit exactly `('recorder','phone','manual')`. The ruled list to add: **`e-service`, `email`, `first-class mail`, `certified-mail-RRR` (tracking number as its own field), `fax`, `portal`.** **NOTHING WAS WIDENED — THE MIGRATION IS A SEPARATE LATER ACT AND IS NOT AUTHORIZED.** **AND IDENTITY RESOLUTION HAS NO SUBSTRATE (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded across `db/schema.sql` and the migrations — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series** — no response-set, response-item, or escalation-timeline table. **Nothing for a deadline engine** (FC-7/FC-9's two-track model and holiday calendar are direction only), **and no discovery-level field on `cases`** (FE-5's hard prerequisite). **Nothing for a consent entity** (FC-10). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all ZERO (#85). AND NOTHING EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party`, `integration` all ZERO (#87) — none of the 36 tables is a money table. NOR FOR RE-1: `referral`, `conflict_check`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC`, `prospective` all ZERO (#88)**. *(Basis stated because the sweep was re-run: word-bounded over `db/` these are exactly zero. Widened to `db/` + `supabase/`, two of the terms across this bullet stop being literally zero — `message` ×3, every one a JavaScript `error.message` in the legiscan-poller, and `case_links` ×4, every one a COMMENT saying it is not built. **The substance holds; the literal claim needs its basis named, and a future sweep that counts the schema's own disclaimers will "find" the thing they deny.**)*
- **HEALTH WAS SKIPPED THIS BATCH, DELIBERATELY, AND THE SKIP IS RECORDED RATHER THAN SILENT.** `QR-6(f)`'s skip limb needs `§5` NONE **and** no `src/`, `db/`, `supabase/` or build-tooling path routed. **Both limbs are satisfied — the sixty-eighth batch's routing table is `docs/specs/` ONLY** — so `npm test` / `npm run build` / `npm run lint` would have proved nothing about it. **The last MEASURED figures stand, from the sixty-seventh, 2026-08-19: 278 tests pass across 23 files; `npm run build` exit 0 (tsc -b + vite); `npm run lint` exit 0**; the >500 kB chunk warning pre-existing. **A skipped check reported as a skip is honest; a skipped check reported as a pass is the failure the rule exists to prevent**

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED:** authenticated SELECT on all API tables; INSERT+DELETE on `parties`, `legal_rules`, `glossary_terms`, `watch_targets`; the app's write path; **CL-2's live paths. PRESENT BUT UNTESTED: every other policy-bearing write path** — an inference. **GL-1 item 4 makes it a gate**
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is NOT an RLS result. Probe READS all **36** tables but WRITE-probes only **5** — `parties`, `legal_rules`, `glossary_terms`, `watch_targets`, plus `file_counters` as the deny control. **By the probe's own methodology only a WRITE proves a policy grants access (a denied SELECT returns an empty set), so "36 covered" overstates what is exercised sevenfold.** **Keep it in step or a missing GRANT hides.** *(The #94 migration's check 5 confirms no policy reads `privilege_tier`, so nullability cannot disturb one.)*

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. Called auth-blocked, but **that diagnosis is incomplete — `service_role` was never granted either**; start there. **F-14 — the poller being an unauthenticated `service_role` writer — IS LATENT, NOT A GL-1 BLOCKER (#113, accepted from the Grok pass and corrected from its own framing): undeployed code has no endpoint, so the trigger is "before you deploy," and it rides `O-3`.** **/statutes in demo mode never touches Supabase** — fixture chapters only
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY THIS REFRESH BY EXISTENCE TEST ON DECLARED PATHS ONLY (HK-5; nothing enumerated) AND STILL ABSENT**, as are `C:\Users\Brennan\data` and `...\data\pilot-recordings\`. **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (your hand, your browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently (the app loses its id).** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** Repro: `outlook-edit-cancel-exercise-2026-08-13.md`. **THE AUTH SURFACE BEHIND ALL OF IT IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on your registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (yours) — **and OBS-1 asks whether that capture row folds into Phase 1b's scope; unruled.** Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY**
- **A MIGRATION PROPOSAL EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (Task 12; you did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately a QUESTION** — inventing a placeholder ladder is how the current wrong one happened. **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`). **`Q-PR3-1..7` and `PR3-LOOK-1` are yours; nothing was re-parented**
- **A DEFECT IN A RULED CONSTRAINT, CONFIRMED AT HEAD, CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts` returns the plaintiff-defendant side set for **every practice area that is not `'Criminal'`** — while that file's header states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant alignments. **Live today, unreachable only because no fourth practice area exists.** **The codebase carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by design, while `sideSetFor()` **silently supplies a wrong-but-plausible answer.** **Flagged, not fixed**
- **FORM ENGINE — FE-D1 (DISCLOSURES ENGINE) NAMED, SCOPED, AND AUTHORIZED 08-12 (#63); NOT BUILT.** Scope `docs/specs/fe-d1-build-slice.md`; kickoff `docs/prompts/PROMPT-fe-d1-build-session.md`; **yours to fire, Opus, its own session** (the CD-1 pattern — the queue runner is barred). Slice 1 = §§1–6 wizard + §10 substrate + §11.3 skeleton; **NOT "FE-1"** (retired scout). **IN:** FE-10 + FE-12 from birth, FE-8 retention half, FE-15, a MINIMAL template editor. **OUT with homes:** FE-9 · FE-11 · FE-13 · **FE-14 (TRCP 47 gate)** · FE-16 · FE-17. **FE-4/FE-5/FE-6/FE-7 have NO FE-D1 disposition at all.** **NEW 2026-08-18: `form-engine.md` §3 now carries the FC-1–FC-4 token-syntax rulings, so slice 1's parser has a ruled spec rather than an assumption.** `docs/skills/drafting-disclosures/SKILL.md` is **a DOCUMENT, not code — now v2** (08-11, design-side; **Code must never edit it**)
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, backlog 47, THIRTY-FIVE VERIFIED — and the ruled-wording tail is EMPTY
- **BACKLOG RE-DERIVED AT HEAD THIS REFRESH, NEVER CARRIED FROM THE PACKET — and after three consecutive batches of movement it is UNMOVED:** `legal-rule-registry-discovery-enforcement-and-pleading.md` **30 VERIFIED / 10 UNVERIFIED against 40 `**Status:**` lines** (was 25/15); `legal-rule-registry-criminal-plea-and-costs.md` **5 / 2 against 7, unchanged**. **Backlog 47 entries; 35 verified, 12 not.** *(The packet stated the same figures as a labeled arithmetic EXPECTATION; these were derived first and independently, which makes them a check on it rather than a copy of it — and the packet's pre-state STOP was satisfied by that derivation, not by assumption.)* **The unit is STATUS LINES, not headings.** **Counting trap, carried: `grep 'VERIFIED —'` also matches `UNVERIFIED —`; anchor on `^**Status:** VERIFIED`.** **SECOND TRAP, CARRIED AND STILL LIVE — IT FIRED AT THE SIXTY-THIRD BATCH AND WAS AVOIDED AT THE SIXTY-FOURTH BY STARTING HERE: THE BASIS IS TWO NAMED FILES, NOT A GLOB.** A `legal-rule-registry*` sum returns **74 / 57 / 12** by folding in `legal-rule-registry-discovery-and-carrier-duties.md` (22, all VERIFIED) and the medical-billing drafts; `registry-new-entry-drafts-2026-08-17.md` carries **six `**Status:** UNVERIFIED` lines of its own** — drafted entry TEXTS, not registry entries — and also matches
- **THE `WS-3` ENTRY IS INSERTED (#109) AND VERIFIED (#110) — AND IT IS THE FIRST CHARACTERIZATION-BEARING ENTRY TO REACH VERIFICATION SINCE THE V-9 AMENDMENT.** Its gates were ruled at #109: `Q-WS3-1` → **"Cross-reference"** (an express line to the VERIFIED `## TRCP 192.5(c)(1)` entry, two-propositions-two-homes); `Q-WS3-2` → **"Comment-9"** (the derivation stands as drafted, its note stating honestly that both opinions reach the result **through comment 9** and that **no located case uses the 192.3(a) formulation**). **Verification rested on a LAYERED basis, put plainly rather than smoothed:** the 192.3(h) clause and the full comment-9 quotation **mechanically verified**; the *Fontenot* / *ExxonMobil* characterizations **resting on the #102/#104 reads as recorded**, not re-checked that night; the `C-XL-1` boundary (*In re XL Specialty*, 373 S.W.3d 46) and `C-ARD-1` condition (*In re Arden*, mem. op., designation per V-9) as adopted at #108. **Michael's judgment, not a text match.** **THE PINPOINT CAVEAT SURVIVES AS VERIFIED TEXT: star-pagination pinpoints must be checked against the reporter before any quotation enters a filing.** The work-product "gap" never existed (#103)
- **THE ELEVEN THAT REMAIN, AND THE RECORDS LOOK BEHIND THEM RAN (#99, T-20/T-21).** **Group A is fully closed** — 13, 2 and 29 VERIFIED at #108; 12 and 32, which went the other way that night to verbatim conformation, VERIFIED at #109. **Seven records/cite looks** — 15, 16, 17, 18, 19a, 20a, 21a — have **candidates staged per entry, source named per item**, none adopted. **Formerly four blocked** — **20b is LOCATED and 24's majority is IDENTIFIED**; 30 and 31 remain cite-less and their cites are yours. **All eleven are still UNVERIFIED — but the sentence that used to follow was arithmetic that did not close, and it is corrected here. The eleven are not "the enforcement backlog PLUS the criminal two": 30 and 31 ARE the criminal file's two**, so counting them on both sides yielded 13 against the 12 this file derives twice. **Re-derived: the enforcement file holds TEN unverified** — the nine named above (15, 16, 17, 18, 19a, 20a, 21a, 20b, 24) **plus 19b**, whose verification detached on rewording — **and the criminal file two, 30 and 31. Ten plus two is the 12.**
- **THE ROUTE-C UNVERIFIED-BY-DESIGN CLASS IS DOWN TO THE PAIR THAT WAS ALWAYS GOING TO BE LAST.** It turned over completely across one evening: entries **12** and **32** left it by being VERIFIED at #109, and **A** (192.3(h) own-statement), the new **215.1(e)**, **B** (193.4(a)), **F** (199.6) and **WS-3** all left it at #110 — **four of those five on programmatic character-exact matches with ZERO flags**, which is the standing verbatim direction ruled at #108 paying for itself the same night. **What remains is 19a** (*Castillo*) **and 19b** (*Able Supply*) **from `RL-1`** — the deliberate pair below. **No entry with adopted-but-unverified wording remains anywhere in the registry.** **No cite line changed on any of them.** Adoption is not verification — **execute-then-verify (#95)**. **`Q-T19-2` CLOSED**
- **ROUTE (c) IS STANDING LAW (#95) AND HAS RUN THREE TIMES.** A divergence that **changes what an entry means** gets proposed conforming wording, **drafted and queued for your adopt/reject/edit — never adopted silently**, and never as a package. #96 produced its one **rejection** (`V7-23-CITE`), the evidence it is not a rubber stamp
- **`Q-STAT-6`'s EIGHTEEN ARE NOW NONE LIVE, AND THE ROW STILL STAYS OPEN.** Adopted at #95 and **verified 08-17: 5, 6, 11, 27**. Flags **CURED**: **1, 23, 33, 34**. Divergences **RESOLVED as-is**, each carrying a note naming the omission he was shown: **4, 7, 8, 9, 26**. **The last two live ones, 12 and 32, are VERIFIED as of #109** — and their divergence lists were the exhibit that produced a STANDING DRAFTING DIRECTION (#108, your words: *"Why are we making these language changes in the first place instead of simply staying with the actual language of the rules?"*): **registry Rule lines quote operative text VERBATIM where practicable**, condensing only where the rule is too long or the entry states one limb, **flagged as such**. **It is a ruled DIRECTION, not a binding convention — putting it in the project instructions fires trigger #3, and that is yours.** It binds future drafting, including the five FC-13 entries; **applying it to any EXISTING entry is a per-entry ROUTE-C act and also yours — #109 applied it to A, B and F on your per-entry picks, never by default**
- **CR-10 STAYS HARD-GATED, THOUGH ONE OF ITS FOUR IS NOW CLEAR:** **32 is VERIFIED (#109)**; 29 was verified at #108; **30 and 31 are cite-less and their cites are yours.** **THE BRIDGE FOR 31 EXISTS AND IS HELD, THOUGH NOTHING IS SELECTED (#104): *Hurlburt v. State*, 506 S.W.3d 199 (Waco 2016)** — verified independently against the opinion text (`type="majority"`, **Tom Gray, C.J.**, one sub-opinion, every quoted passage confirmed verbatim) — **holds that the CCA's construction of *“in a single criminal action”* governs art. 102.073(a) AND that the “same criminal episode” limb DROPS OUT for costs.** **It routes through *Ex parte Pharr*, 897 S.W.2d 795 (Tex. Crim. App. 1995), NOT *LaPorte***, and ***Pharr* supplies 31's negative side and IS UNREAD — but the read is RULED AND AUTHORIZED (`Q-AUTH-1`, "Yes — both limbs"): you pull the *Pharr* and *Hurlburt* PDFs into `Knowledge Repo\Opinions\`, then an OPUS session reads them.** **AND THE CCA HAS A SCOPE HOLDING BUT EXPRESSLY RESERVED THE REST: *Anastassov* (2022) — art. 102.073 *“applies only to court costs and fees”* — while declining to consider the court of appeals' costs holding. So there is still NO CCA holding on the bridge.** **V-6 still governs — art. 42.0197's finding is NOT automatic**
- **THE 19-PAIR IS UNVERIFIED ON BOTH SIDES, AND THAT WAS THE RULING, NOT AN ACCIDENT.** *Castillo* (19a) and *Able Supply* (19b) carry **the same proposition text** by design; the walk verified 19b only, and `RL-1` then reworded **both** — so **19b's 2026-08-17 verification DETACHED** (verification attaches to wording, #95) and returned to UNVERIFIED with a note saying exactly that. **Re-verification is yours.** **`V5-ATTRIB` stays OPEN across all six split entries**
- **THE THREE WORK-PRODUCT ENTRIES ARE VERIFIED AND STILL HAVE NO NUMBER (`REGISTRY-V` CLOSED, `Q-T19-1` OPEN).** 192.5(d), 192.5(c)(1) and 192.3(h)'s definition (notes taken during an interview are **not** a witness statement) verified 2026-08-17, **the effective-date caveat ACCEPTED rather than smoothed.** **Labelled `WP-1/2/3`, packet-local; numbering DEFERRED (`D-6`)**
- **THE WALK'S STAGING LAYER STANDS AND VERIFIES NOTHING: `task-19-signoff-worklist-2026-08-17.md` (#97).** One row per entry across all forty. **Its two arithmetic defects ARE STILL STANDING (`Q-T19-3`)** — §5 Part B says "eleven" over a twelve-row table; §6 says "five of the fourteen" and lists four. **Neither was corrected: §4.1 ordered it verbatim and a change to packet content is yours.** **One line WAS changed, and only because he directed it (E-3): the *Park Cities Bank* pinpoint → 876–77** in rows 14 and 15; **868 confirmed and standing.** **E-3 WAS SCOPED, and the four unconformed carriers of the old `876` are named:** the workbook's rows 14/15 (**expressly barred**), `registry-cite-check-2026-08-13.md` and `registry-citator-pass-2026-08-13.md` (**dated records — conforming a finding falsifies it**), and the log's #65-era line (append-only). **Whether the two pass records should be conformed anyway is yours**
- **#73's `V-4` SECOND HALF IS EXECUTED.** #73 ruled **"keep as two, CROSS-REFERENCED"** and the cross-references had run **one direction only**. `RL-5` ruled both sets: entries **17 ⇄ 18** took reciprocal notes, and `legal-rule-registry-discovery-and-carrier-duties.md` took **three reciprocal dedupe notes** (197.2(a), 196.2, 198.2). **NOTES ONLY — no proposition and no Status line in that ALL-VERIFIED file was touched, and its 22 `VERIFIED` lines were counted before and after and are identical**
- **`legal-rule-registry-discovery-and-carrier-duties.md` IS THE VERIFIED FILE** *(**AUD-3, CONFIRMED FALSE and re-derived again this refresh: 22 `**Status:** VERIFIED` lines against a header reading "ALL TWENTY PROPOSITIONS VERIFIED" — a bullets-vs-entries unit mismatch. NOTHING touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)); **TRCP 190.3(b)(3) is a VERIFIED entry under a wording flag — `Q-FE5-9`.** Criminal file: **entry 3 and 4a carry NO CITE — supplying one is YOUR act.** **Six flagged WL/slip cites survive uncorrected**, on 15/16/17/18/20b/21a. **`/rules` SEED ALL UNVERIFIED**
- **THE FOURTH FILE SITS OUTSIDE THE BACKLOG: `legal-rule-registry-draft-entries-medical-billing.md`**, headed ALL ENTRIES UNVERIFIED while its own entry Status lines read **DRAFT** — a second header/entry unit mismatch, re-derived this refresh. Whether that exclusion is deliberate is **`Q-STAT-2`, yours**
- **THE SAME ABSENCE HAS APPEARED FIVE TIMES.** (1) **NOT ONE PRIVACY PROPOSITION** (`Q-WF-6`) · (2) #78's insurance narrowing · (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (`Q-QBO-6`) · (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** — thirteen terms, all zero · (5) work product — **now VERIFIED, and still only three entries.** **The registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** *(FC-14's fourth SOURCING channel now makes (4) RETRIEVABLE — the wall was sourcing, not scope — but retrieval is not an entry, and opening one is FC-13's open half.)* **FOUR research passes still stack behind `Q-STAT-5`'s one ruling**
- **THE RETRIEVAL GAP IS CLOSED (#76)** — the rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20**, and all 21 are retrieved against the official corpus. **RETRIEVAL IS NOT VERIFICATION.** *(Re-confirmed the hard way at #93: a session asserted the pass had never run, wrote a document on that premise, and withdrew it — **do not re-run this pass.**)*
- **THE WORKBOOK IS STALE IN SEVEN IDENTIFIED WAYS AND CARRIES A BANNER SAYING SO (`D-3`).** Its **rows are untouched and deliberately so** — it remains **the numbering authority `V5-IDS` and `Q-T19-1` rest on**. **The staleness is disclosed, not repaired**
- **REGISTRY RESEARCH SUPPORT — FIFTEEN DOCS (the label read FOURTEEN over a list of fifteen; the t26 record was added without bumping it, and it is counted here), ALL PROPOSED OR RECORD-ONLY, NOTHING VERIFIED; every size RE-DERIVED THIS REFRESH:** cite-check · workbook v1 (**29,844 B / 171** — banner'd AND FROZEN as the numbering record) · citator pass (**19,056 B / 222**, untouched by design) · `statute-pass-registry-retrieval-2026-08-14.md` — its **§3** carries the corrected A-for-space normalizer and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess* · deadline-engine memo · V-5/V-6/V-7 execution record · worklist (#97) · sign-off record (#98) · `registry-records-look-staging-2026-08-17.md` (**53,672 B / 650**) · `registry-verification-workbook-v2-2026-08.md` (**22,320 B / 185** — the WORKING LAYER) · `fable-adjudication-record-2026-08-18.md` (**26,759 B / 329**) · `registry-new-entry-drafts-2026-08-17.md` (**32,768 B / 274** — grown by add-only resolution annotations, its entry texts untouched) · `criminal-opinions-read-2026-08.md` (**32,415 B / 321**) · `costs-and-privilege-authority-read-2026-08-18.md` (**19,158 B / 200**) · `t26-signoff-and-adjudication-record-2026-08-18.md` (**32,720 B / 321** — the #108 record: the five looks put verbatim, twenty-one rulings, and the directed edits this batch executed). **§3's normalizer must NOT be carried to the TRCP/TRE PDFs — raw TRCP `AA` count is 0, a different publisher**
- **`V-8` AND `V-9` ARE BOTH CLOSED — RULED 2026-08-18 (#100).** **`V-9` ADOPTED AS DRAFTED**, his words: *"Adopt as drafted, but also keep my ideas to possibly use when we implement it."* **CLAUDE.md rule 5 now carries a named-source fallback chain** — the court's own document; a paginated vendor copy stating on its face who delivered the opinion; or **your own identification, put to you and recorded** — with **cannot-identify-STOP as the floor** and unidentifiable entries **flagged, never staged for verification**. **`V-8` was ruled FRESH**: §0.1 gained **0.1.1 – 0.1.7**. **`V-9-UI` is recorded as PROPOSED design input only; Q-6 still bars wiring the API into the app**
- **EVERY CANDIDATE THE RECORDS LOOK PRODUCED IS RULED (`RL-1`..`RL-6`, all CLOSED), AND NOT ONE VERIFIED ANYTHING.** **`RL-3`, three cite acts:** 20b (*Collins*) took the full parenthetical, and 15 and 21a each took **date + cause number**, with **no pinpoint and no designation notation, because none was available.** **`RL-2`:** the *Alford* supersession inference is retired for the caption facts. **THE REPORTER-CITE FLAG SURVIVED EVERY ONE OF THESE ACTS, AND SO DID EVERY ENTRY'S UNVERIFIED STATUS — a cite act is not a verification.** **`RL-6`: the nine unnamed criminal opinions got a FULL READ (T-27), but the cite supply for entries 30 and 31 stays YOURS**
- **THE WHOLE DRAFTED BLOCK IS EXECUTED AND NOW ENTIRELY VERIFIED — SEVEN ENTRIES EXIST, THE EIGHTH DELIBERATELY DOES NOT.** Inserted UNVERIFIED into the enforcement file at #108 — **192.3(h)'s own-statement right** (`WS-1`) · **193.4(a)** and **199.6** (`RL-4`) · **194.2(b)(9)** and **194.5** (`WS-2`) — and at #109 the **NEW 215.1(e)** entry (`Q-WS1-1` → "Own entry") and the **`WS-3`** entry. **All seven are VERIFIED as of #110** (194.2(b)(9) and 194.5 at #109; the other five at #110). **194.1(a) was NOT inserted** — it became a **second observation** on the VERIFIED "TRCP 194" entry in the carrier-duties file, under one-proposition-one-home; **the span flag is RESOLVED (that entry's scope is the WHOLE of Rule 194)** and it took a reciprocal cross-reference. **That file's 22 VERIFIED lines were counted before and after at all three batches and are identical.** **The 215.1 preamble tension survives INSIDE a verified entry as verified text asserting no answer** — no located authority resolves whether a non-party may move under 215.1. **`WB2-1`/`WB2-2` stand as ruled**

## Design-input memos — ALL PROPOSED, nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions. **No deadline engine is in the build queue** — and **FC-7/FC-9's two-track model plus holiday calendar is ruled DIRECTION that does not change that.** **ITS HEADLINE IS A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, with 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) now allows a phase-scoped send-by Level 3 period.** **Five questions, NO durable IDs (ID-DL-1)**
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions. **PROVENANCE MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED:** TRCP 21(f)(5) and Statewide Criminal E-Filing R. 2.3(a) fix filing at **transmission to the filer's EFSP** (TIER A); a Bexar county-court sentence appears to fix it at **clerk acceptance** (TIER B), and **TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule** — exposure: transmitted late on a limitations date, accepted two days later (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**) — **which FC-8's court-keyed profile now answers by ruling: the key is the COURT, not the county.** **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing. All three rows stay ⬜.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9) — **and note what route (c) does NOT reach: a VERIFIED entry, whose rewording detaches an existing verification**. Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument** (its `doc_type` CHECK admits one value)
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** **IN-1 has NO design-doc home anywhere.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED and elaborated; its EXTRACTION half is UNRULED and only mapped.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all**, so a build implementing #64 alone ships with no VIN validation and no promotion gate, silently. **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR** — the crash date is itself under extraction. ***`Q-IN2-1` IS ANSWERED 2026-08-18, his word "Yes — both": the blank CR-3 form AND the versioned instruction manual, his hand. The field inventory will be SPECIFIED rather than discovered at build time; the row stays open until the documents land***
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN. All seven rows stay ⬜. SEVEN OF SEVEN GATED, ZERO SATISFIED.** **THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **ITS ROW NAMES ALL THREE CONSUMERS (#94)** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`**: even the metadata-only `callRecords` version needs a **scheduled runner to beat a 30-day retention window.** **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5)**
- **PR-3 RE-PARENTING PROPOSAL (#86) · QBO MEMO (#87) · RE-1 MEMO (#88).** QBO's **HEADLINE: THERE IS NO READ-ONLY OAUTH SCOPE** — one accounting scope grants **full read/write**, so **"read-only first" can be a property of the code but never of the grant** (`Q-QBO-2`); **the economics are INVERTED** (Intuit meters data-**OUT**, free tier 500k credits/month, **BLOCKS rather than bills**); and **the constraint with legal teeth is the one the platform helps with least** — QuickBooks records trust/operating separation as a **naming convention and enforces nothing**, so a read integration **can display a per-matter trust figure that is wrong with the authority of a computed number** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED.** RE-1's two headline findings are BOTH now ruled: **`Q-RE-1`'s two readings → "Both, separate triggers"**, and **`Q-RE-9`'s sourcing wall → FC-14's fourth channel.** **THE ONE THAT STILL PAYS FOR THE SECTION: TDRPC 1.04(f)(2) IS THE FIELD LIST** — consent **in writing and PRIOR to the referral**; **1.04(g) makes the failure economic and silent — an unconfirmed arrangement does not void the referral, it STRIPS THE FEE.** *(Now checkable against the source's face, per the FC-13 pass — and still **UNVERIFIED**.)* **`P-RE-1..12` ALL UNVERIFIED**
- **COMMUNICATIONS-LOG INGEST MEMO (#89)** — Task 15. **THE DISPATCH'S OWN WORD WAS CONTRADICTED IN TERMS:** it called two constraints **ruled**; the log records the exchange as *"Claude recommendations, unruled."* Both trace to one paragraph of `future-modules-capture-2026-07-28.md` §3 — **the same gate as `Q-QBO-1`, firing a second time** (`Q-COM-1`). **THE TWO RESOLVE DIFFERENTLY:** the scope exclusion is honored and **costs nothing**; the architectural commitment is **priced for the first time** — a second public HTTPS endpoint, private-key custody, an outside vendor's discretionary approval, and an unestablished metering position, **for a channel the firm does not use.** **A NEW GATE CLASS (`Q-COM-5`): a third party's DISCRETIONARY APPROVAL.** **THE SMS HEADLINE LANDS ON THE STACK ALREADY COMMITTED:** Teams SMS needs three licences plus A2P Brand *and* Campaign approval, is **1:1 with no MMS** — so it **cannot receive the scene photo, the insurance card, or the bill** — and **is apparently not exposed to Graph at all**, on the memo's weakest source, marked `[C]` (`COM-LOOK-3`). **THE IDEA WORTH ITS OWN LINE: a communications LOG and a communications ARCHIVE are different products, and only the archive needs content** — `callRecords` gives who/when/how-long with **no audio and no transcript, so no privilege, consent or PHI problem attaches, because nothing was captured.** **AND THE ONLY MICROSOFT-NATIVE PATH TO CALL CONTENT INVERTS RULING 8.3** — compliance recording is **policy-based and always-on** against 8.3's **opt-in per call** (`Q-COM-8`). **`Q-COM-11` IS RULED AND EXECUTED (#94); `Q-COM-2` IS NOW RULED (values named, migration unauthorized); `Q-COM-10` and `Q-COM-12` REMAIN YOURS**
- **WITNESS-STATEMENT CONVERSION MEMO (#99) + THE `WS-3` AUTHORITY READ (#102, T-28, CORRECTED #103).** The memo's headline stands: **on TRCP 192.3(h)(2) the line is crossed at RECORDING, not at transcription** — the recording is already a witness statement — **so a gate placed only at transcription sits downstream of the event it exists to control**; a witness statement is a Rule 194.2(b)(9) INITIAL disclosure owed WITHOUT a request (194.1(a)), against which 194.5 permits **no work-product assertion**; *Trahan v. Lone Star Title*, 247 S.W.3d 269, is the same failure sanctioned. **THE ATTORNEY-CLIENT LIMB IS DECIDED BY TWO PUBLISHED INTERMEDIATE OPINIONS, majority authorship confirmed on the face of each (V-9)**: *In re Fontenot*, 13 S.W.3d 111 (Livingston, J.) and *In re ExxonMobil*, 97 S.W.3d 353 (Seymore, J.) — a witness statement inside a confidential attorney-client communication **is privileged**, and ordering its production is an abuse of discretion. **BOTH REACH IT THROUGH COMMENT 9, NOT THROUGH 192.3(a) BY NAME** (`Q-WS3-2`). ***CORRECTED #103: the "adjuster or third-party witness is never privileged" paragraph is WITHDRAWN*** — *W&G Trucking*'s declarant was **a party** and the case turned on a **failure of proof**, *ExxonMobil* protected a recounting of a third party's words, and *In re Arden* appears to hold an **adjuster**-recorded statement privileged (`Q-WS3-6`). **Every proposition UNVERIFIED; no entry created**
- **T3 PILOT PROTOCOL (#90) · SESSION-LOG TOC (#91) · GO-LIVE RUNBOOK (#92).** The **TOC** (`docs/specs/session-log-toc.md`, **RE-DERIVED THIS REFRESH**) carries **the finding that matters to every reader of the record: the log runs TWO independent numbering systems and neither covers every entry** — `#nn` and **runner ordinal** — with **eight runner batches carrying a `#nn` instead of an ordinal** and **one carrying neither**; a reader assuming one system mis-files about a quarter of the log. **NO ENTRY ANYWHERE CARRIES `#1`** (`TOC-1`). **`TOC-5` still accrues without a ruling.** *(**REGENERATED THIS BATCH — SIXTEENTH EDITION, 238 rows, its FOURTEENTH firing under `TOC-4`.** Byte-proof re-run against the fourteenth edition's basis: the **236 prior rows' entry text is an exact SUFFIX of the log as written** — purely prepended — **and the preamble compares byte-identical.** **THE 2,770 / 2,771 OSCILLATION IS SETTLED AND IT WAS NEVER DRIFT: both measure the same unchanged bytes under different conventions.** The preamble is **2,771 bytes** on the convention this file now fixes — everything before the first `## ` heading, its terminating newline included; 2,770 is the same text measured exclusive of that newline. **The fourteenth edition published 2,771 as a *correction*; it was right about the number and wrong about the cause.** Buckets re-derived: **113 numbered (#2–#114) + 64 ordinals (5–68) + 1 unnumbered runner + 60 other = 238.** **AN EIGHTH TRAP IS ENTERED, AND IT IS IN THE INDEX RATHER THAN THE LOG: counting its rows with `grep -c '^| `'` over the WHOLE file returns two too many — the *Basis of this read* table has two cells that also open with a backtick — so the row count must be taken from below the `## The index` heading.**)* The **runbook**'s gate resolved an eighth way: **DERIVED like #91 but NOT REGENERABLE** — its "who acts" and "done-when evidence" columns are judgement, so an appended gate silently makes it incomplete (`GLR-3`)
- **PRIVILEGE-TIER UNIFIED VOCABULARY (#102, T-30, CORRECTED #103) · DESIGNATION-SOURCE METHOD RESEARCH (#102, T-31, CORRECTED #103).** **T-30's headline SURVIVED THE AUDIT BUT ITS ARGUMENT DID NOT:** the overlap it relied on is a **GENERAL cardinality property, not special to the fourth value**, **so the argument defeats the three-value column already in the schema, and the PRIOR question — should `privilege_tier` be exhaustive at all? — had never been ruled** (`Q-COM-10-A`, since RULED at #105). **The 192.5(d) over-read is corrected** — the rule's *"For purposes of these rules"* qualifier was dropped — so conforming `'privileged'` → `'attorney-client'` is framed as **a preference, not a cure** (`Q-COM-10-B`). ***And the migration header's copy-forward is corrected: the registry entry is VERIFIED, not UNVERIFIED*** — the `#13`/`R-3` class, caught before it reached tracked content. **The migration remains AUTHORED, UNRUN AND NOT AUTHORIZED; the `src/` half is specified IN WORDS and deliberately NOT drafted.** T-31 stands on its structural finding — **Texas has TWO independent designation axes** (TRAP 47.2(a) Opinion vs Memorandum Opinion, memorandum being the **default** under 47.4; and 47.2(b) publish/do-not-publish, **criminal only**) — **but its two headline claims are RETRACTED:** the *"174,292 to 1"* measurement **had no control arm**; and **`"full precedential"` appears 0 times in the TRAP PDF** (`Q-DES-1`). **AND TRAP 77, NOT 47, GOVERNS THE CCA — 77.3 BARS citing unpublished opinions as authority** (`Q-DES-6`). **NO METHOD ADOPTED, no SOURCING amendment, no §0.1 edit, nothing built**
- **CRIMINAL OPINIONS READ (#102, T-27, CORRECTED #103) — AND THE CORRECTION RAN AGAINST THE CLIENT, WHICH IS WHY IT MATTERS MOST.** The read stands: **eight of the nine never mention art. 102.073**, only *Middleton* touches it and only as the State's argument expressly not decided. **BUT *LaPorte* DOES NOT SPLIT BY PAGE.** Both halves are on **415**, in adjacent paragraphs — so **the instruction "Never cite 415" is WITHDRAWN**, having barred counsel from the very sentence stating the rule. **The corrected remedy is a parenthetical — *"overruled on other grounds by Ex parte Carter"* — not a pinpoint** (`Q-RL6-3`). Two more: ***Carter*'s reaffirmation is a FOUR-vote plurality and dictum** against **five** votes for the overruling; and **"may be raised at any time" was NARROWED, not overruled**. **And FOUR, not five, are "DO NOT PUBLISH"** (`Q-RL6-5`). **Four of the nine remain author-unidentifiable under V-9 and are flagged, never staged**
- **THE ADJUDICATION AND CORRECTION SERIES: QR-6 (#94) · WORDING (#95) · V-EXEC (#96) · TASK 19 WORKLIST (#97) · SIGN-OFF WALK (#98) · TOC REGENERATION (C-2, unnumbered) · CHAT-DISPATCH v3 (#99) · FABLE (#100) · DT-1 CORRECTION (#101) · CHAT-DISPATCH v4 CHAIN (#102) · AUDIT CORRECTIONS (#103) · AUTHORITY READ (#104) · SPEND-DOWN (#105) · FC BLOCK (#106).** **#98 is the first session to move a registry Status line to VERIFIED**, and it moved twenty-four; **C-2 is the first Code session to decline a `#nn`**, which `TOC-6` has now made the rule; **#100 is the first to move a Status line the OTHER WAY**; **#101 is the first correction entry whose whole subject is a DATE**; **#103 is the first produced by an adversarial audit of a prior session's own output**; **#106 is the first whose ruling had to be folded into a spec a build slice will read before that slice runs.** **`docs/prompts/CHAT-DISPATCH-v3.md` and `-v4.md` were NOT edited at #100–#106; amending a repo-tracked prompt is yours**

## For design side
- **THE SECOND CAPACITY EVENT ON RECORD (#107, 2026-08-18; first was 2026-07-26) — AND IT MEASURED BEFORE CUTTING.** 61 project docs ≈ 804 KB raw ≈ **~230K tokens (~14%)**; the **GitHub sync ≈ ~1.38M tokens (~86%)** — **the dominant consumer is the SYNC, not the docs**, which is why `WS-P1`/`WS-P2` outrank any further doc pruning. **44 session captures retired by your ruling** — archived losslessly, byte-verified and delivered to you BEFORE deletion; meter 81% → 73.3%. **That delta proved the meter counts TOKENS, not bytes** (509,246 B → 145,829 units), correcting a same-session projection. **A capability changed: a Cowork design session CAN now delete project-knowledge docs** — on your explicit in-session ruling only, archive-before-delete. **`docs/project-knowledge-working-set-policy.md` carries both amendments as of this batch** (39 non-blank): the runbook heading no longer says Claude cannot delete, and the never-prune bullet gains a **capture-class caveat** — **specs and design docs remain never-pruned; CAPTURES whose durable content is on the repo record are prunable by ruling, never by default.** Runbook steps 3–5 are untouched and still **PROPOSED, not ruled**
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built. *(Live tension, now a queue item: **`Q-PR3-1`** asks whether a design-side `src/` read through the device bridge is sanctioned at all — the working-set policy says design sessions do not read source, that sentence predates the bridge, and **every finding in the #86 proposal is undrivable without such a read**. Yours; nothing from `src/` entered project knowledge.)*
- **INSTRUCTIONS: `v22` IS IN FORCE — OBSERVED 2026-08-17 (#102) IN THE RUNNING SESSION'S OWN LIVE INSTRUCTIONS FIELD**, the #82/A-1 mechanism for the **EIGHTH consecutive time.** v22 carries **two trigger-#3 firings from one session** (`V-9` and the `TOC-4` runner amendment). **v23 IS NOW ASSEMBLED — REPORTED BY #106 AND CARRIED ON THE PACKET'S WORD, WHICH IS THE ONLY EVIDENCE A CODE SESSION CAN HAVE.** It folds FOUR same-day trigger-#3 firings (V-9 and TOC-4, resolved in v22; **PF-1** and **the FC-14 SOURCING fourth channel**, resolved in v23) into ONE document rather than a stack of amendments. **THE HAND ITEM IS BACK TO ONE ACT: PASTE.** *(Your 2026-08-18 correction — no v23 document existed, and a Code session structurally cannot assemble one — was TRUE WHEN MADE and is not withdrawn; the design side did the assembly, exactly as that correction said it would have to. **The assembled text is NOT in the repo and never will be** — the versioned instructions are the Claude.ai project-instructions field, so this file must never assert a paste state it did not learn from a design session.)* DT-1 remains in force
- **THE DAY-CROSSING REPRODUCED, WHICH TURNS A DATA POINT INTO A PROPERTY.** Bare `date` read `Tue Aug 18 19:59:32 CDT 2026`, **right**; `date -u` read **`Wed Aug 19 00:59:32 UTC 2026`** — the same inversion the sixtieth batch found half an hour earlier, in a separate session. PowerShell, the authority: `2026-08-18 19:59:29 −05:00`, `Central Standard Time`. **So this is a WINDOW, not an unlucky moment: every batch run between ~19:00 Central and midnight reads a UTC date one day ahead**, and would date its entries 2026-08-19 and invert the index. **Any execution point DT-1 gains must read the wall clock, not a shell** — and "the Code session is local, therefore safe" stays false
- **SOURCING IS A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED), AND IT GAINS A FOURTH NAMED CHANNEL BY RULING (FC-14, 2026-08-18).** The three were the official bulk statutes corpus, the clean-authority TRCP/TRE/TRAP PDFs, and the eCFR API. **The fourth: State Bar / court-published Texas conduct-and-administration rules as clean-authority PDFs in `Documents\Knowledge Repo\`, cited by each PDF's own effective date.** Michael acquired six by hand mid-session (TDRPC eff. 3/7/2025; Disciplinary Procedure amends. eff. 10/1/2024; Judicial Administration amends. eff. 7/1/2026; Code of Judicial Conduct; judicial-candidate disciplinary rules 04/01/2026; judicial education). **`Q-RE-9` — the convention's first proven gap — CLOSES with it.** **SOURCING itself is NOT amended in any repo file: the convention lives in the project instructions and the amendment rides the v23 paste.** **Q-STAT-2 through Q-STAT-6 stay OPEN**
- **CHAT-DISPATCH v4 IS COMPLETE AND EVERY CHEAP MOVE IT LEFT IS NOW SPENT: `T-26` RAN LIVE AT #108, #109 CLOSED THE TWO-ENTRY LOOK, AND #110 CLOSED THE FIVE-ENTRY LOOK.** `docs/prompts/CHAT-DISPATCH-v4.md` (79 non-blank / 94 raw). **`RR-1` ran at #102 and caught two cross-task interactions — and caught NONE of the twenty-two defects the audit found, because RR-1 checks documents against later RULINGS, not against PRIMARY SOURCES. THAT GAP IS CLOSED BY RULE AND HAS NOW BEEN EXERCISED FOUR TIMES:** `PF-1` on the #106 packet (twelve defects, 0 HIGH), the #108 packet (twenty-one, two HIGH — two INCOMPLETE divergence enumerations whose re-put CHANGED THE OUTCOME on entries 12 and 32), the #109 packet (twenty-two, one HIGH each — including a directed edit with no ruling behind it), and the #110 packet (**fourteen, none HIGH, none needing a ruling — and its one authorization finding ran the OTHER way: the shipped Opus prompt was granting a permission beyond the #108 terms and it was struck**). **CODE-DISPATCH v3 IS COMPLETE.** **The cheapest high-value move left is no longer a look at all — it is the OPUS-RUN paste**
- **HK-7 STAYS OPEN, AND ITS ANNOTATION GAP IS RECORDED IN THE QUEUE ITSELF — RULED 2026-08-18.** The ruled remedy was a **FORWARD gap-note, not a reconstruction:** the row carries that annotations for **#94–#98 were recorded only in BUILD-STATE** and that **this note records the gap, not reconstructed annotations. Nothing was back-written** (K-6/K-7). **The count itself is still not reliable from either side.** **RE-CHECKED NATIVELY THIS BATCH by existence test on DECLARED paths only (HK-5 — nothing enumerated, nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL still ABSENT here.** **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**. **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY; **not an open design question** but a coverage audit of a ruled taxonomy. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. *(#86 read `roster.ts` and found the `sideSetFor()` defect there — evidence the gap was load-bearing.)* **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SIX REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*`. First three SPENT as CD-1 input; UIM + deficiency seeded the **DE series**. **No capture in the repo is unreconciled.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2
- **YOUR HAND, THE LAUNCH PATH IN ORDER — AND ITS FIRST STEP IS DONE.** ~~run the CD-1 migration~~ **RAN AND VERIFIED 2026-08-19, together with the other two; all three are off this list.** **GATE 1 IS OFF THIS LIST — Supabase Pro BOUGHT 2026-08-19, your hand, the second floor item cleared that day.** What remains: **gate 10 — PII promotion out of `parties.fields`, the ONLY UNBUILT item on the go-live path, and it sits AHEAD of the floor** → **gate 9 production SMTP** → gate 3 RLS test **including the slice's two new tables, now live** → gates re-check + the trigger-1 instructions revision. *(**`Q-PR3-3` is ANNOTATED, NOT CLOSED**: its backfill limb is spent — step (c) matched zero rows — and what remains in it is yours.)*
- **STILL YOURS AND ALL SMALL:** **the OPUS-RUN paste HAS RUN and is off your list — its output is this batch** (`#112`): `docs/prompts/OPUS-RUN_fc13-drafts-and-ws3-leads_2026-08-18.md` carried the two acts authorized at #108, and both landed as records rather than as registry changes — `fc13-entry-drafts-2026-08-18.md` and `ws3-second-pass-read-2026-08-18.md`. **What it returned is twelve questions, all yours** · **`Q-COM-10`**, which closes the moment you adopt the unified `privilege_tier` list — **`Q-COM-10-A` IS RULED AND CLOSED (#105), so `B`–`F` are unblocked** · **the cite supply for entries 30 and 31** (`Q-RL6-1`, `Q-RL6-2`) — `Q-RL6-1` resolves to ***Hurlburt*** (bridge held) and ***Pharr***, **whose read is AUTHORIZED to an Opus session and gated only on your two PDF pulls**; **its *Whaley* option is dead on the DO-NOT-PUBLISH finding**, and **entry 30 still got nothing** · **`V5-ATTRIB`**, which `RL-1` deliberately did not resolve · **`V5-IDS`** — derived IDs were executed on the #66 precedent, **but that precedent supports the NAMING and not the FILE SHAPE, so the two registry files remain shaped differently**
- **ALSO YOURS, LONGER-CARRIED: PASTE v23** — now **one act**, the assembly done · **the *Pharr* + *Hurlburt* PDF pulls into `Knowledge Repo\Opinions\`, then fire the authorized Opus read** · **the blank CR-3 form + versioned instruction manual** (`Q-IN2-1`, "Yes — both") · **`FC-12`** (corpus import posture — **no corpus content imports** meanwhile) and **`FC-13`'s open half — now DRAFTABLE by the routed prompt above** · ~~run `db/migrations/2026-08-16-privilege-tier-no-default.sql`~~ **— RAN 2026-08-19, off your list** *(the four-value migration #105 sent back for a redraft was never this file: it is PROPOSAL TEXT in `privilege-tier-unified-vocabulary-proposal-2026-08-17.md` §4, **not a file in `db/migrations/`**)* · **re-upload SKILL.md v2** (SK-v2) · **the sync-picker clicks** (Q-3, `WS-P1`/`WS-P2`) — **then a FRESH chat to verify the picker state** · **telemetry lockdown on the P1** · **HK-6** · **HK-7 re-grant each session** · **V-2, Ochoa first** · **OBS-1** · **CD-4 – CD-13** · **Q-STAT-2 – Q-STAT-6** · **the deadline memo's five + ID-DL-1** · **the Task 8 specs' 22 + 3 looks — Q-FE5-9 first, it sits inside a VERIFIED entry** · **the Task 9 specs' 18 — Q-IN1-8 is a one-word veto on the whole IN series** · **the Task 10 spec's nine — Q-IN2-2 is cheapest** · **the Task 11 spec's ten + `WF-LOOK-1`** · **the Task 12 proposal's seven + `PR3-LOOK-1`** · **the Task 13 memo's eight + four looks** · **the Task 14 memo's nine + three looks** · **the Task 15 memo's remaining eleven + four looks** · **the Task 16 protocol's seven — `Q-T3P-1` is the cheapest question in the whole queue** · **the Task 17 index's five, now three** · **`GLR-2` and `GLR-3`** · **`OPEN-5(b)` and the `AUD-5..9` one-word apply** · **`READ-A`** · **the workbook's carried `Q5`** · **STILL THE LARGEST SINGLE BLOCK, AND IT MOVED THREE TIMES IN ONE NIGHT: the CHAT-DISPATCH v4 chain stands at TWENTY-FIVE OPEN + SEVEN CLOSED of its 32 rows, RE-DERIVED FROM THE QUEUE AT HEAD rather than incremented** — closed are `Q-WS1-1`, `Q-WS2-1`, `Q-WS2-2`, `Q-WS3-1`, `Q-WS3-2`, `Q-WS3-5`, `Q-WS3-6`; open are `Q-WS2-3`, `Q-WS3-3`/`4`, `Q-RL6-1`–`6`, `Q-COM-10-B`–`F`, `Q-DES-1`–`7`, `Q-AUTH-1`–`4` · **delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`, reported STILL PRESENT by the #112 design session; **NOT re-checked here — HK-5 forbids this runner sweeping your filesystem to confirm it****
- **TWO PRACTICE ITEMS FROM #106, REPORTED TO YOU IN-SESSION AND TOUCHING NO REPO FILE.** **(1) `Payout Template - Normal.xlsx` carries a LIVE SETTLEMENT in the template body** — the §8 type-over pattern, live in your current library; the PD-Only template is properly tokenized. **(2) The A-4 $150 vs $175 expense-fee conflict is CONFIRMED AT SOURCE.** Both are your files, not the app's
- **THOSE THREE ARE NO LONGER LOCATOR-ONLY — ALL READ AT #104, AND TWO CHANGED WHAT THEY WERE THOUGHT TO SAY.** ***In re XL Specialty*, 373 S.W.3d 46 (Tex. 2012)** does NOT undercut *Fontenot*: it runs the OPPOSITE direction, **confirms the TRE 503(a)(2) test** and adds an adverse-posture boundary (`Q-WS3-5`). ***In re Arden*** is **the *Fontenot* pattern, not a third-party rule** — Arden was the DEFENDANT and it was HIS OWN statement — and it **corroborates #103's work-product correction** (`Q-WS3-6`). **Still: reading is not verifying, no cite is selected, and the supply for 30/31 is yours**
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. NOTHING BUILT — and the T-24 SPEC DRAFT (#99) DOES NOT PUT IT IN THE QUEUE:** it proposes no table, column, endpoint or migration. **THE DISPATCH'S TAXONOMY LINE WAS ONE CATEGORY SHORT — the ruled list is SEVEN (BP/STO/OO/EV/CONTRA/PRIV/RFP-form).** **CONTRA HAS NO VERIFIED AUTHORITY AT ALL** — it rests on entries 2 and 12, both in Group A awaiting one look each; the conference fuse rests on entry 13's ADOPTED-but-UNVERIFIED wording, **so the fuse's whole legal substrate is pending one look** **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated, **and the #85 spec confirms every gate is still shut**. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` is recorded as sitting untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand.** *(**Re-checked rather than inherited this refresh: at the sixty-eighth batch's Step 1 inventory `ls -la inbox/` on THIS machine returned exactly ONE file — that batch's push-to-code packet zip — and no `Probate Corpus.zip`. Whether this machine is the P15 is not determinable from the repo, so the claim is carried with that annotation rather than deleted.**)* **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-6 + MM-1 + OPEN-5(a) + TOC-4 ALL CLOSED; runner v11** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy**; the `/queue-runner` command is a pointer to it — **re-read at HEAD again this batch rather than from any cached copy — v11, blob `c75de00d`, sha256 `38a57e22…`, identical**). **Your hand: the P15 user-level copy deletion.** They stand as ruled: (a) a stated verification **names the command that produced it**; (b) the queue merge is **TWO acts**; (c) **pin identity at Step 1, delete BY EXPLICIT FILENAME**; (d) format comes from the runner's own Step 4 rules, **never copied from an entry authored under an earlier version**; (e) **a packet-added act in no row and no Step 4 item needs your in-session authorization**; (f) **a docs-only batch records the health-check skip explicitly — **it DID fire this batch: `§5` NONE and `docs/specs/` only, so the skip limb applied and the skip is recorded above with its reason**; **and `TOC-4`: the index regenerates every runner batch — its FOURTEENTH firing here.** **QR-3 STILL CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74, #93): the mount reads a false DIRTY of CRLF-only files, and `git fetch`/`ls-remote` fail there with `HTTP 403 from proxy`
- **`QR-6(e)` DID NOT FIRE THIS BATCH EITHER — SIX FIRINGS IN THIRTEEN BATCHES, and the absence is stated rather than assumed.** #94: a stranded `.git/index.lock` the packet routed nowhere. #95: **a confirmation that did not reach the unrouted act**. #96: three at once. #97: **two arithmetic errors inside the packet's OWN CONTENT.** #98: **a ruling being CLOSED whose second half was never executed.** **#100: the stranded lock again.** **#99 and #102–#106: nothing — every act sat in a routing row or a Step 4 item.** **THE #107 PACKET'S REPO VIEW WAS AN UNKNOWN SHA AND SAID SO ITSELF** — it was authored before the sixtieth batch's commit existed — and its §7 rows match what the merge found; `.git/index.lock` is **absent**, the sixth consecutive clean checkout. **CARRIED AND STILL TRUE OF #103's PACKET: it MISCOUNTED ITS OWN OPEN ITEMS IN TWO DIRECTIONS.** **Neither figure was edited — the entry was ordered verbatim and a change to packet content is yours** (`Q-T19-3`)
- **RUNNER-FOUND AT #104 AND NOT EDITED INTO THE DOCUMENT — TWO DEFECTS, ONE OF THEM HIGH.** **(1) *WHALEY*'S PUBLICATION NOTATION IS IN THE COPY, AND IT SAYS “DO NOT PUBLISH.”** The document asserts it *“could not be found anywhere in the copy I read”* and builds `Q-AUTH-2` on that premise. It is at the foot of the FLP-served opinion — *“/Bill Whitehill/ … **Do Not Publish** — TEX. R. APP. P. 47.2 (b) — 181255F.U05”* — so **under TRAP 47.7(a) *Whaley* has NO precedential value**, `Q-RL6-1`'s option (d) is dead, and **FLP reporting `status: “Published”` for this very opinion is a live proof that the FLP field is not evidence of a Texas designation.** **(2) A V-9 BREACH ON *ANASTASSOV*:** it is characterised at length while authorship is recorded as unchecked; **identification was one query away from a V-9-permitted source** — the court's own document reads *“SLAUGHTER, J., delivered the opinion for a unanimous Court”* — so the holding is **unanimous and stronger than claimed**, but the method failed. **The queue rows carry these; the document and #104 stand verbatim (`Q-T19-3`)**
- **THE MULTI-AGENT READ-ONLY PREFLIGHT IS STANDING LAW — `Q-AUDIT-1` RULED 2026-08-18 (#105), `PF-1`.** **Trigger: any packet carrying a legal characterization or a proposed registry entry. Run by the DESIGN SESSION before shipping, pre-`inbox/`.** **It earned the rule four times out of four and has now paid a seventh:** #96, a staged block **reworded a citation flag inside an act ruled verbatim**; #97, **two arithmetic errors**; #103, **twenty-two defects across five documents**; #104, ***Whaley*'s DO-NOT-PUBLISH notation**; #106, twelve defects including its own §8 draft stating the PF-1 result BEFORE the run; #109, twenty-two, including a directed edit with **no ruling behind it**; **#110, fourteen and none HIGH — but its most interesting finding ran TOWARD more permission than was ruled: the shipped Opus prompt carried a 1.04(g) note-allowance nothing had ruled, and it was struck on the principle that a prompt claiming to authorize nothing new may not grant even a restrictive permission beyond the ruled terms.** **OPERATIONAL WARNING THAT SURVIVES: at #104 the agent fleet DIED — seven agents, twice, all on `API Error: 529 Overloaded`, zero tokens — and the harness returned `{"surviving_defects":[]}`, a TOTAL FAILURE WEARING THE COSTUME OF A CLEAN BILL OF HEALTH.** **PF-1 fires on the SENDING side, so no queue-runner batch can satisfy it**
- **THE CLASSIFIER CAN REFUSE AN ALLOWLISTED BARE COMMAND — AND THIS BATCH IS THE PREDICTION COMING TRUE, NOT A RESTATEMENT OF IT.** The v20 note (#94) recorded the forty-seventh invocation's **bare** allowlisted `git push origin master` refused on **both** shells. **The SIXTIETH invocation then stranded exactly as predicted: it committed and never pushed, and 613 insertions across six files sat invisible to the design side until the SIXTY-FIRST batch's Step 0 ahead-stop caught it.** This batch's own retry was refused again. **Michael pushed by hand; the runner verified `83d244b` at origin before proceeding.** **The QR-3 ahead-stop is now the proven catch and it is the ONLY one — a runner that skipped it would have built a second invisible layer on the first. Never conclude a batch landed without `git ls-remote`**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and now `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered.** **A new `FC` series was entered this batch and does NOT collide with anything**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** — **which `Q-IN2-1`'s "Yes — both" now answers, once the documents land.** Synthesis `cr3-field-code-map.md`, **PROPOSED input — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: the #62/#63 and #64 batches were VERIFIED design-side FULL-TEXT on 08-13 (#65)**; **#67 verified landed at the top of #68.** Still UNREVIEWED, **recomputed from the log at HEAD this refresh (A-4, ruled #82; now runner text):** the carried **#31–#33** material, **#37–#61's own routing**, and **#75 through #114** together with the runner lines interleaved among them **through the sixty-eighth, this batch's own line included** and the unnumbered C-2 Code-session entry — do not copy #36's clearance forward. *(**Honest limit, stated rather than smoothed: the `#75` floor rests on no explicit clearance.** The log positively clears #36 at #37, #62–#64 at #65, and #67 at #68 — but **#65, #66 and #68–#74 were DROPPED out of the round-trip lines rather than cleared.** The floor is inherited from the log's own round-trip chain, the derivation A-4/AUD-7 blessed; **"#65, #66 and #68–#74 are reviewed" is UNDETERMINED from the files.** **Re-derived independently this refresh, and the two defensible bases are named rather than one being published bare: on the round-trip chain's inherited basis the unreviewed run is #75–#114 = 40 numbered entries; on a STRICT positive-clearance basis it is #65–#114 less #67 = 49. The nine-entry gap between them is exactly the undetermined set.** The scan also found clearances the log's own summary line omits — #51, #52, #54, #55, #56 and #61, all "REVIEWED (design-side post-sync verification)" — **which move nothing, since all sit below #62, but mean that trusting the summary line alone under-reports what was cleared.**)*
- **EVERYTHING AWAITING YOUR *RULING* IS IN `docs/specs/attorney-review-queue.md` — AND THAT IS NOT THE SAME AS EVERYTHING AWAITING YOUR *ACTION*, WHICH THE FILE NOW SAYS IN ITS OWN WORDS (RULED #114).** The GO-LIVE GATES deliberately do NOT live there: Gates 1–10, `GL-1`'s floor items and `T#1` live in `Go_Live_Gates.md`, their STATUS lives HERE, and they await your ACTION rather than your ruling. **The sixty-seventh batch found five of them absent from the register and put the question rather than minting five rows; #114 ruled *fix the claim, not the register*** — the wrong thing was `#113`'s closing sentence asserting a coverage that file did not have, and the two costs of the alternative are named in the ruling: it would make the register an action tracker as well as a ruling queue, and it would create **a THIRD place gate status is asserted, the `#13`/`R-3` copy-forward class.** Reconciled through **#114**, **read from that file's own Status header at HEAD (A-5) after this batch wrote the sentence as its queue merge's second act (`QR-6(b)`), and stated as such rather than as an independent check.** *(#92 and #93 are still absent from that header's reconcile chain — a known, reported gap.)* **THIS BATCH ADDED NO ROW AND CLOSED NONE — it inserted ONE SCOPE PARAGRAPH and nothing else; `⬜` is unmoved at 354 against the packet's stated absolute.** **AND THE PARAGRAPH DISARMS THE PROBE THAT FOUND THE GAP: `Supabase Pro`, `SMTP`, `PII`, `parties.fields`, `RLS write-path` and `T#1` each returned ZERO in that file at `3773e6d` and each returns non-zero now, ENTIRELY from the new paragraph and header sentence, which quote the probe's own phrase list. No row exists behind any of them — a future runner re-running that check would read rows where none are.** **It VERIFIED NOTHING and moved no Status line anywhere.** The v4 chain was re-derived from the queue at HEAD and is **UNMOVED at 25 OPEN + 7 CLOSED of 32**. **THE OPEN-5(a) SWEEP RAN AGAIN AND THIS TIME NOTHING MOVED — which is a result, not a skip, and it is worth exactly as much as the batches where something did.** Re-derived independently at HEAD: backlog **47 / 35 verified / 12 not** on the two-named-file basis (the `legal-rule-registry*` glob still returns the trap figure **74 / 57 / 12** — the unverified figure COINCIDES, so a checker comparing only that one would not detect the wrong basis); carrier-duties **22 VERIFIED**; `db/schema.sql` **36 tables, 36 RLS-enabled, 35 policies**; the log preamble **2,771 bytes**; **every research-doc size in this file, sixteen of them, including the grok record at 238 non-blank — the sixty-seventh's correction, re-derived here rather than carried**; runner **v11, blob `c75de00d`, sha256 `38a57e22…`**; the v4 chain at 25 + 7. **`TOC-4`: regenerated here, SIXTEENTH edition, 238 rows against 238 log headings, riding this commit. `TOC-6` IS RULED — `#nn` IS DESIGN-ONLY.** **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f into case-heartbeat-design.md §8. Carried: no law-change ledger, Outlook unreviewed
