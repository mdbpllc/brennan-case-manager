# BUILD STATE — brennan-case-manager
Commit: 6ca9794  |  Branch: master  |  Generated: 2026-08-16 Central (eighty-first refresh)
*`6ca9794` is the batch commit this snapshot DESCRIBES; the refresh itself rides the commits after it, so the pushed SHA you are told is expected to be higher. Nothing here is stale on that account.*
*Line counts here are NON-BLANK (ruled 08-13, #67; also in CLAUDE.md); raw counts are labeled "raw". This file's 150-line cap is a non-blank cap.*

**Practice areas: PI / civil litigation / criminal defense / probate.** **CL-2 IS BUILT, MIGRATED
LIVE, AND WALKED (2026-07-28)**; auth (§5A) landed the same day. **CD-1 IS BUILT IN CODE 08-12 —
SIX OF SEVEN SLICE ITEMS, FIXTURE DATA ONLY; THE LIVE MIGRATION IS UNRUN AND IS YOURS.**
`archive-project-history-by-day.md`

- **Anti-resurrection ledger: `docs/specs/anti-resurrection-ledger.md`** — closed, withdrawn, and deliberately-not-built items. **Check it before rebuilding anything absent. Never drop this line**
- **AUDITED 2026-08-15 (#79): `record-integrity-audit-2026-08-15.md` — 65 claims in this file and the queue: 46 SOUND, 6 stale, 1 CONFIRMED-FALSE, 1 contradicted. SIX FINDINGS ARE NOW RULED AND CORRECTED IN PLACE (#82, A-1–A-6); TWO ARE STILL CARRIED UNCHANGED, marked `AUD-2` and `AUD-3`** — they sit in **OPEN-1** and **OPEN-2** territory (the gates doc's version line; registry wording, Michael's alone), where correcting them without his ruling would make the auditor an editor

## THE LAUNCH PATH — GL-1 go-live floor (RULED 2026-08-11) + CD-1 authorized
- **GO-LIVE MEANS: real case / party / client / SOL data hand-entered into the core app — NOTHING MORE.** Floor, in order: **(1) the CD-1 build landed and exercised · (2) gate 1 Supabase Pro · (3) gate 9 production SMTP · (4) gate 3 RLS tested INCLUDING the slice's new tables · (5) gates re-check session + instructions v17.** Full text: `Go_Live_Gates.md` *(**AUD-2, CONTRADICTED: the gates doc says "v15" at item 5, this file says v17, and v18 is in force. That doc is canonical and append-only and was NOT edited — the wording act is yours at OPEN-1.** **THE SAME QUESTION HAS NOW BEEN RECORDED THREE TIMES** — `docs-lint-sweep-2026-08-13.md` §1 item 1, then `AUD-2`/`OPEN-1`, then `GLR-1` at #92 — **and NO fourth row was created**; the Task 18 wording was annotated onto `OPEN-1`. **The drift across the three is itself the argument for ruling it: v17 → v18 → v19, each recording silently modernizing the number**)*
- **AND A POINTER CHECKLIST OVER THE GATES NOW EXISTS — `docs/specs/go-live-runbook.md` (#92, Task 18), PROPOSED.** 81 non-blank / 106 raw. **It restates no gate's substance and carries NO status column, by design** — status is this file's job, and a second status snapshot is the `#13`/`R-3` copy-forward failure. **Where it and `Go_Live_Gates.md` differ, the gates doc governs and the runbook is the defect**
- **Gates 7 and 8 do NOT block the floor** (their triggers are the first real EOB / AnalysisRun) — **billing analysis on real data stays DEFERRED past go-live.** **The form engine is EXCLUDED from the floor** — drafting continues via the skill; the engine upgrades a live system
- **GATE 2 READS AS MULTI-USER ONLY** (ruled 08-11, clarified by appended note, original text untouched): the security review gates the multi-user phase. **Solo live use proceeds without it.** *(**Q-QBO-4, #87, UNRULED: on that reading a live QuickBooks OAuth connection would NOT be gated by gate 2** — a credential-tier third-party connection at one user. `Go_Live_Gates.md` was NOT amended; appending a gate is the ruling.)*
- **THREE PLACES WHERE READING A GATE ALONE GIVES THE WRONG ANSWER (#92, properties of the gates doc — recorded, NOT corrected).** **Gate 2 read alone OVER-BLOCKS** (the 2026-08-11 clarification two lines below is load-bearing). **Gate 3 read alone looks satisfiable and is not** — gate 6 is a hard prerequisite and **the dependency is recorded at gate 6, not at gate 3.** **Gate 9's trigger is a different SHAPE** — the others fire on real *data*, gate 9 on *reliance*, **so a checklist ordered by "when does real data arrive" schedules it too late**
- **CD-1 BUILT 08-12 (log #61), items 1–6 — code complete, exercised, fixture data only.** Role tags (vocabulary DERIVED from the party registry so it cannot drift) · typed aliases + the multi-match FLAG · deceased · the four roster attributes · capacity on the LINK · roster history · roster definitions as data with side sets · contact edges · directory + roster UI · **RLS, GRANTs, and probe extended in the SAME commit as the tables.** `party_type` **RETAINED**, not dropped
- **ITEM 7 IS YOURS AND IS UNRUN: `db/migrations/2026-08-12-cd1-contact-directory.sql`.** Back up, paste ALONE in an empty buffer, **answer its five verification checks IN WORDS.** One check **expects a HIGH flag count — a LOW number would mean something was guessed**
- **ITS ORDER RELATIVE TO PR-3 NOW HAS A CONSEQUENCE, AND NOTHING ON THE RECORD HAD SAID SO (#86, `Q-PR3-3`, UNRULED).** That migration derives caption alignment only `where c.practice_area in ('Personal Injury','General Civil Litigation')` (`:100`) — a literal two-value IN list. **PR-3 first** → re-keyed probate rows fall outside it and are **FLAGGED** by the `case_roster_flags` insert at `:135–146`, the migration working as designed. **CD-1 first — the order this launch path currently schedules** → probate rows are still PI at that moment, take the plaintiff-defendant derivation, and **nothing later revisits them.** **The quieter failure is the one scheduled**, and it is coupled to the `sideSetFor()` defect below
- **THE BACKFILL FLAGS MOST CASES, BY DESIGN.** Plaintiff/Defendant map only where the case type's side set defines them; function roles derive to null = non-party; **'Client' is FLAGGED** (our client is Plaintiff on a civil caption, Accused on a criminal one, and nothing in the record decides it). **Demo mode runs the same backfill**, so what you click is what a migration produces
- **Design authority stays `docs/specs/contact-directory.md`** (LIVING SPEC). **THE FORK RESOLVED TO THE SHAPE ALREADY BUILT:** `parties` **IS** the directory, `case_parties` stays the roster link, `case_clients` stays parallel, **D-CL2-8 UNTOUCHED.** Kickoff `docs/prompts/PROMPT-cd1-build-session.md`
- **OUT, and honored:** form engine · IN-2 fact table · merge tooling · **service-story fields** (slots carry an unconsumed `servicePathHint` label only) · probate beyond the reserved pattern · `/rules` seed. **No registry change of any kind.** Slice A stays WITHDRAWN
- **`case_links` STILL DOES NOT EXIST — the CL-1 firewall held.** D-CL1 items stay unruled
- **TWO SPEC-VS-CODE GAPS, in `docs/spec-feedback.md`, built around under stated assumptions:** there is **no case-type tree to inherit on** (CASE_TYPE_DEFS is flat → inheritance runs practice area → case type), and **trucking and UIM/UM are PI FLAGS, not case types** (PR-3 holds the tree shut) → they seed as flag-keyed **overlays**. Slots are DATA; reversing is cheap. **Yours to redirect**

## Phase 0 / T3 — environment only, still blocked, and now with a written protocol above the gate
- **PARTLY SPENT 2026-08-09 on the P1**: WSL2, torch cu128, NeMo 3.0.0, both checkpoints, CUDA on **sm_120**, **outside the repo** at `~/phase0` — `phase0-environment-standup-2026-08-09.md`. **Stage 1 SCORING IS HELD, NO SCORECARD EXISTS**; Stage 2 untouched; **T4 unauthorized**. Weights fit but that **does NOT retire sequential loading**. The 13 pilot transcripts are `src/routing/__tests__/pilot/` fixtures; Stage 1 owes the full-precision compare
- **THE PROTOCOL FOR STAGE 1 EXISTS AND AUTHORIZES NOTHING (#90, Task 16): `docs/t3-pilot-recording-protocol.md`** — 534 non-blank / 688 raw, **PROPOSED — PREPARATION ONLY**, it *"authorizes nothing, stages nothing, and verifies nothing."* Its `R`/`K`/`I` provenance marks are intact and **`I` (this-session inference) was NOT promoted**; its §5.3 `<sha256>`/`<n>`/`2026-07-2?` placeholders are **deliberate — no audio file was measured**, and filling them would fabricate data. **Seven questions `Q-T3P-1..7`, packet-local, no durable IDs.** Placement at `docs/` is Michael's **interim** direction, not a ruling (`Q-T3P-7` still open)
- **AND ITS HEADLINE IS THAT HK-4 MAY DESCRIBE AN ACT THAT ALREADY HAPPENED.** HK-4 and this file both read as *"create the directory at staging time"*; the **2026-07-25** pilot-fixtures entry records the thirteen as **already archived at `..\data\pilot-recordings\` WITH A PROVENANCE README no design session has ever read.** **So HK-4 is probably a P15 → P1 TRANSFER — cheaper and more dangerous, since a transfer can silently overwrite that README** (`Q-T3P-1`, `Q-T3P-2`). **The bundle also held the ground-truth scripts, scorecard and findings, while HK-4 names only audio** (`Q-T3P-3`)
- **The one RED preflight row is still AUDIO** — no real speech has ever run on this stack; **no substitute audio** (08-09 exception). **HIS HAND (HK-4), UNCHANGED AND NOT RE-WORDED.** **Re-checked natively at #90 against the DECLARED paths only (HK-5 — nothing swept): `C:\Users\Brennan\data`, `...\data\pfs\` and `...\data\pilot-recordings\` are ALL ABSENT here.** That is a negative about **this** machine and **does not answer the preflight row**, which is Michael's to answer
- **AND THE MEASURE ITSELF IS SPLIT, WHICH NOTHING ELSE STATED (`Q-T3P-4`).** The authorization reruns both batches *"against the existing ground truth"* — but ground truth exists for **five of thirteen**. Batch 2 is scripted → an **ACCURACY** measure; batch 1 is unscripted and its "existing" artifact is the int8/CPU-floor transcript, a prior **OUTPUT** → a **CHANGE** measure. One figure across thirteen files reports two different things. **D1 auto-file and the confidence thresholds are explicitly waiting on this scorecard**
- **COVERAGE, STATED PLAINLY: the thirteen exercise ROUTING thoroughly and the ASR stack NARROWLY.** No long-form audio (only duration on record ~71 s; deposition-length is the Statement Bank's real use case, untested — and Phase 0's 79 MiB / 20 MiB activation figures are **3-second datapoints, not extrapolable**). The **4-speaker cap is untested** (max two voices vs Sortformer's four). **No Spanish** — two untested things, accuracy AND the flagged non-English diarization degradation. **Nothing from the capture kit, which is not purchased. Word boosting has never run on any recording, anywhere.** All thirteen are iPhone 14 Pro / Voice Memos / **AAC ~70 kbps mono** and can never meet the kit standard — **preserve bit-exact, no re-encode/up-sample/normalize/trim**, because an up-converted file *looks* like kit-standard material in its header
- **`T3` IS AMBIGUOUS ACROSS DOCS AND THE TWO MEANINGS GATE OPPOSITELY (Q-WF-2, #85, FLAGGED NOT RENAMED).** This section's `T3` is the **transcription** tier (`transcript-sort-and-route-design.md:148`, unauthorized). A **second, unrelated `T1`–`T4`** lives at `statute-text-and-bill-tracking-design.md:107` — the LegiScan poller tier — and is **substantially BUILT**. **Five WF gates say "gated on T3" without naming which**; re-lettering is Michael's act (the H → HK precedent)
- **AND THE KICKOFF DOC IS STILL GONE (KICK-1)**, never git-tracked, and it is the authorization's **authoritative text**: **until he locates it or re-issues, T3 WORK IS UNAUTHORIZED** — which is why the new protocol's §5 is NONE. **TELEMETRY LOCKDOWN IS NOT IN PLACE — CONFIRMED NOT SET 08-13 (#66)**, both machine-scope variables EMPTY; recipe `docs/gpu-telemetry-offline.md`; **no privileged audio before then. Whether that gate reaches an ATTESTED-FICTIONAL batch is `Q-T3P-6` and is genuinely two-sided**

## Screens live (what Michael can click)
- **Sign-in gate — Supabase mode only.** Magic link, no password; demo mode deliberately ungated. /cases — case list; compact statute-worklist card (the de facto dashboard); /cases/:id — case detail, tabs URL-driven (/cases/:id/{parties,medical,calendar,transcripts})
- /cases/new — new-case form (types, auto file numbers); /cases/new/oaa — OAA order upload → draft review → Create Matter, charges as child records
- /inbox — transcript staging: upload/import, routing suggestions, confirm-to-file (auto-file OFF, D1); /notes — office notes, per-transcript detail w/ participants/tags; /parties — **contact directory**: role-tag filtering with live counts, alias display + **multi-match flag**, deceased marker, firm-wide edit scope with linked-case count; /benchmarks — Medicare PFS CSV import
- /diagnostics — database + RLS probe (Supabase only), evidence not gospel; /rules — Legal Rule Registry: entries, attorney-only verify, review log, watch flags, worklist card; /statutes — cite box, browse, keyword search, chapter viewer, Mark-verified pins, refresh-and-diff (A4); /bills — watch targets, tracked bills with B3 lifecycle, statute-ref matcher
- **AND NOTHING EXISTS AT INTAKE, WHICH IS WHERE THREE OF RE-1'S FOUR TRIGGERS FIRE (#88, `Q-RE-4`).** The master spec **designs** the PNC funnel and calls it SETTLED — PNC → Client / Declined / **Referred out**, each with a date and a short reason — and **it is not built**: word-bounded `PNC` returns **ZERO** across `db/` and `supabase/`, `parties` has **no status column**, and three separate places already record the missing PNC → Client promotion path. **A referral engine that does not reach the funnel could only record referrals of matters the firm already signed**

## Case detail tabs
| Tab | Status | Notes |
|---|---|---|
| Overview | LIVE | all core fields editable INCLUDING practice area, case type, PI overlay flags, commercial-policy, representation type; classification changes review-logged + "re-evaluate playbooks" notice. **Limitations date is now CLIENT-scoped** |
| Parties | LIVE + CD-1 | **intake roster panel** (slots by expectancy tier, each labelled with the case type or overlay that contributed it; caption sides shown; "Link a contact" scopes the add form to that slot) · **roster-flag card** with Mark-handled · the four attributes shown separately · link/unlink · **Clients — damages scope card (CL-2)** |
| Medical | LIVE — **PI MATTERS ONLY (ruled 08-12)** | bill ledger → per-bill workspace: manual line items, fuzzy CPT mapping, coding audit, claim-type detection, PFS benchmark ratios with per-run schedule choice + demo-placeholder banners, EOB typed field, analysis runs (only CONFIRMED feed settlement math), report generator. **Per-client ledgers once a case has two clients** **Tab hidden on civil-lit and criminal files; `showsMedicalTab()` is the one enforcement point. §1983 civil-rights matters are the other half of that ruling and are NOT implementable — no such case type exists and PR-3 holds the tree shut. Safety valve: a matter that already HAS bills keeps its tab, so the rule narrows what is OFFERED, never what is REACHABLE.** |
| Calendar | LIVE + Outlook push WORKING, ONE DEFECT | create/edit/cancel all EXERCISED against live Graph 08-13. **Cancel deletes; edit patches — EXCEPT the FIRST edit of a connect-pushed event, which DUPLICATES it in Outlook and orphans the original.** Reproduced on two events; cause undetermined, no fix. `outlook-edit-cancel-exercise-2026-08-13.md` |
| Transcripts | LIVE | filed transcripts for the case; detail view |

## Client dimension (CL-2, built 2026-07-28 — migrated live, walked)
**The case owns the occurrence and liability; the CLIENT owns the damages.**
- `case_clients` is **PARALLEL to `case_parties`, not a promotion** (D-CL2-8); `client_id` sits on `medical_bills` and `analysis_runs`. **`cases.statute_of_limitations` IS DROPPED** — the case shows the earliest across clients **not yet disbursed**. **Single-client files click as before** (D-CL2-7): no selector, limitations **writes THROUGH**; two+ → read-only
- Medicare/Medicaid live on the client; occurrence flags (trucking, product, government defendant, death, **minor/incapacitated**) stay FILE-level **by ruling — do not "fix" minor/incapacitated to the client.** **No client-role party = FLAGGED** (`case_client_flags`), orphaned limitations date **PRESERVED on the flag**. **D-CL2-3: BILLING RATE IS PER CLIENT** — no schema. **D-CL2-3a RULED 08-13: the fee affidavit carries the rate of the client whose claim carries the FEE DEMAND; multi-client demands get per-client exhibits. Design status — the time tracker stays parked**

## Data layer
- Adapters: local (localStorage demo) AND supabase; the UI talks only to the DataAdapter interface — every feature works in both modes. Seeds fictional; **store v11** (CD-1), v9→v10→v11 chains **FORWARD in place**, backing up at each step. **`npm run dev:demo`** runs demo mode past a real `.env`; **demo state does NOT travel**
- **`db/schema.sql` EXECUTED live 2026-07-28**, then CL-2 that night. **36 tables in schema.sql, but the live database is still at 34**: CD-1's two are in the file and in the UNRUN migration
- **GRANTS ARE PART OF THE SCHEMA — load-bearing.** `authenticated` ONLY, **`anon` gets nothing by design** (`db/migrations/2026-07-28-api-role-grants.sql`). **`ALTER DEFAULT PRIVILEGES` is NOT set: every new table must carry its own GRANT or it is unreachable.** CD-1's two carry their own
- **PRACTICE AREA AND CASE TYPE ARE NOT MODELLED IN THE DATABASE AT ALL (#86).** `cases.practice_area` and `cases.case_type` are **free text with no CHECK, no enum, no FK, no index and no view** (`db/schema.sql:38–39`), and a word-bounded sweep of `db/` + `supabase/` returns **exactly five occurrences.** Good for rollback; **bad for anyone assuming the type system or the database will catch a mistake — neither will**
- **AND THERE IS NO GEOGRAPHY ON `cases` AT ALL (#88, `Q-RE-1`).** `court_name` and `cause_number` are free text; there is **no county, venue, district or jurisdiction column**. Load-bearing for RE-1: the record carries **two readings of the same "out-of-area" trigger from the same session** — subject-matter (answerable from `practice_area`/`case_type`) and geographic (answerable from nothing the schema holds). **Two triggers, two data requirements; picking one is Michael's**
- **A WRITTEN-CHANNEL COMMUNICATION HAS NOWHERE TO BE, AND THE TABLE IT WOULD LAND IN SAYS SO (#89, `Q-COM-2`).** `transcripts.source` and `staging_items.source` **both** admit exactly `('recorder','phone','manual')` — no chat, no mail, no SMS — and **`staging_items` is the table an ingest pipeline would land in**; all eleven `context_type` values are audio or meeting contexts. **AND IDENTITY RESOLUTION HAS NO SUBSTRATE AT ALL (`Q-COM-3`):** `email`, `phone_number`, `mobile`, `address`, `handle`, `upn`, `external_id` all **0** word-bounded across `db/schema.sql` and all three migrations — `parties` carries only `display_name`, `role_tags`, `aliases jsonb` and free-form `fields jsonb`. **CD-1 built typed aliases and a multi-match FLAG for NAMES and nothing for addresses.** Four CHECKs and two defaults reported as defects; **NONE fixed — widening a CHECK is a schema act and Michael's**
- **A NOT-NULL DEFAULT HAS BEEN ASSERTING PRIVILEGE ON EVERY ROW SINCE THE TABLE SHIPPED (#89, `Q-COM-11`).** Both `privilege_tier` columns read `default 'work-product'`, and **TRCP 192.5(d) makes that an assertion of privilege** — while 192.5(c)(1) excepts witness statements and 192.3(h) makes any *"substantially verbatim transcription"* of a recording of a witness's oral statement one. **So `'witness_interview'` and `'deposition'` rows default to a characterization the rule excludes**, and `transcript-workflows.md` §1.3 **already says the opposite in prose**. **AND THE PIPELINE SITS ON THAT LINE (`Q-COM-12`):** Canary outputs punctuated, timestamped text — so transcribing can convert what would have been attorney's notes into a **discoverable witness statement, at scale, with no decision point.** **FLAGGED, NOT FIXED; neither file edited — reconciling them is a ruling, not a repair**
- **No case-event/CE table, no time_entries, no claims, no `case_links`. Nothing exists for the DE series** — no response-set, response-item, or escalation-timeline table. **Nothing exists for a deadline engine either, and no discovery-level field exists on `cases`** (FE-5's hard prerequisite). **Nor for IN-3: `generated_documents` has NO status column and NO set/parent column (#83). Nor for IN-2: no fact table, `cases.date_of_incident` is a `date` so it cannot hold a crash TIME, and word-bounded `vin`/`vehicle`/`instrumentality` return ZERO (#84). Nor for ANY WF pipeline: `email`, `message`, `envelope`, `signature`, `attachment`, `mailbox`, `thread`, `records_request`, `custodian`, `invoice`, `referral`, `correspondence`, `communication` all return ZERO word-bounded across `db/schema.sql` and all three migrations (#85) — every WF item would create its first table. AND NOTHING ANYWHERE EXPECTS AN EXTERNAL SYSTEM: `external_id`, `external_ref`, `source_id`, `qb_`, `quickbooks`, `third_party` and `integration` return ZERO across the same four files (#87) — no correlation column to hang a link-don't-rebuild integration on, and none of the 36 tables is a money table. NOR FOR RE-1: `referral`/`referrals`, `conflict_check`, `conflicts`, `disqualif`, `waiver`, `fee_split`, `fee_share`, `PNC` and `prospective` all return ZERO across the same four files (#88)**
- **TWO CLEARING MECHANISMS FOR ONE SHAPE OF THING, FLAGGED AND CHANGED NOWHERE (#88, `Q-RE-6`).** The 08-07-26 ruling routes a conflicts disposition to the **review log** — but `review_log.action`'s CHECK admits only `('suggested','confirmed','edited','rejected','created','generated')` and **none of those is "decided,"** while every flag built since (`case_roster_flags`, `case_client_flags`) clears through a nullable `resolved_at` with the undecided value preserved. Same class as #86's two-opposite-defaults finding, though a **vocabulary gap rather than a live defect**
- Health **MEASURED AT #68: 278 tests pass (23 files), build + lint clean — and the tree has been DOCS-ONLY from that measurement through this refresh** (A-3, ruled #82; re-confirmed mechanically this batch — `git status` shows only `docs/` paths. **The health check was SKIPPED EXPLICITLY this batch per `QR-6(f)`**: §5 was NONE and no `src/` or build-tooling path was routed, so it would prove nothing. **OPEN-5(a) still unruled**)
- **THE TWO `privilege_tier` VOCABULARIES DISAGREE AND A LOG WOULD BE A THIRD CONSUMER (#85, sharpened #89 `Q-COM-10`).** `generated_documents` allows `('attorney-client','work-product','non-privileged')`, `transcripts` allows `('privileged','work-product','non-privileged')` — while `db/schema.sql:514` calls the first *"the shared system-wide set."* **They map cleanly in neither direction; choosing silently would settle it by implementation**

## RLS, exercised — the distinction matters
- **ACTUALLY CONSULTED:** authenticated SELECT on all API tables; INSERT+DELETE on `parties`, `legal_rules`, `glossary_terms`, `watch_targets`; the app's write path; **CL-2's live paths. PRESENT BUT UNTESTED: every other policy-bearing write path** — an inference. **GL-1 item 4 makes it a gate**
- **`file_counters` is protected at the PRIVILEGE layer, not by RLS** — its 403 is NOT an RLS result. Probe covers **36** tables; **keep it in step or a missing GRANT hides**

## Known stubs & fakes
- **NO REAL CLIENT DATA HAS EVER ENTERED THE APP.** Still true after auth AND CL-2 — everything written 2026-07-28, live database included, was fictional. **All of `Go_Live_Gates.md` applies**
- legiscan-poller + statute-fetch edge functions written, **NOT deployed**. Called auth-blocked, but **that diagnosis is incomplete — `service_role` was never granted either**; start there. **/statutes in demo mode never touches Supabase** — fixture chapters only
- **Benchmarks import reads `..\data\pfs\` — RE-CHECKED NATIVELY AT #90 AND ABSENT on this machine** (declared path only; it lives on the other machine). **Outlook push: creation, EDIT and CANCEL all exercised live 08-13** (Michael's hand, his browser, demo events only). **Cancel propagates; edit patches cleanly — but the FIRST edit after a connect-time push CREATES A DUPLICATE and strands the original, permanently (the app loses its id).** Systematic, reproduced on a clean control. **Cause undetermined and NOT guessed; no fix.** Repro: `outlook-edit-cancel-exercise-2026-08-13.md`. **THE AUTH SURFACE BEHIND ALL OF IT IS ONE DELEGATED SCOPE AND NO SERVER-SIDE IDENTITY (#85):** single-tenant public-client browser SPA, MSAL/PKCE, **`Calendars.ReadWrite` only**, per-browser tokens, `localhost:5173` redirect, **no secret**. **No mail scope exists, and adding one is a consent act on Michael's registration**
- Inbox has NO auto ingestion (T3 unbuilt); OAA parses digital Uvalde orders only, scans → manual. Medical has NO PDF/bill ingestion (Phase 1b GPU-gated); no document storage. **Hand-keying itemized bills is a VIABILITY issue** (Michael) — **and OBS-1 now asks whether that capture row folds into Phase 1b's scope; unruled**. Playbook engine NOT built; generated docs and code mappings do NOT survive a reseed. **"Mark disbursed" shows on criminal files — KNOWN, NOT fixed**
- **NOTHING FROM THE 08-13 RULING RUN IS BUILT** — demographics capture, cause-number-pending flag, staged deposition threads, payer threads at intake, per-(case, party) deadlines, `precedential_status`: **DESIGN STATUS ONLY**
- **PROBATE: the only furniture is a `Probate companion` case type MIS-PARENTED under Personal Injury with a knowingly wrong ladder (`_piDefault`).** PR-3 direction CONFIRMED (V17: own practice area, own ladder(s), companion concept gone), **EXECUTION HELD** until the ladder pass names the destination; that pass is **DEFERRED pending the Domser matter** and **PL-1..PL-4 are all UNRULED. Do not touch the case-type tree or ladder.** No probate tables or roles. **NO MONEY MACHINERY:** no settlement ledger, trust/IOLTA, liens — **and none of #87's QBO memo, #88's RE-1 memo or #89's communications memo changes that: all three are RESEARCH/INPUTS ONLY, each ends in a non-goals list, and a build session has nothing to build from any of them**
- **A MIGRATION PROPOSAL NOW EXISTS AND IS PROPOSED — NOT AUTHORIZED TO BUILD (#86).** `pr-3-reparenting-migration-proposal-2026-08-15.md` (CHAT-DISPATCH Task 12; Michael did not participate and made no rulings). **The direction is ruled and the DESTINATION is not, so its target-state section is deliberately left as a QUESTION** — inventing a placeholder ladder is how the current wrong one happened. **The act is a SOURCE CHANGE PLUS A TWO-COLUMN VALUE BACKFILL, not a schema migration.** **Rollback is cheap and its cheapness DECAYS: status values written under a probate ladder do not un-write, so reversibility ends at first probate use** (`Q-PR3-4`) — **and the DDL-free property is shape-dependent, since PL-1's proceeding-ATTRIBUTE branch would need a `cases` column that does not exist.** **`Q-PR3-1..7` and `PR3-LOOK-1` are all Michael's; nothing was re-parented and no schema file was touched**
- **A DEFECT IN A RULED CONSTRAINT, FOUND IN PASSING, CONFIRMED AT HEAD, AND CHANGED NOWHERE (#86, `Q-PR3-2`).** `src/domain/roster.ts:195–202` returns `SIDE_SETS['plaintiff-defendant']` for **every practice area that is not `'Criminal'`** — while that file's header at `:7` states it satisfies **roster capture REQ-14: *"sides are a property of the case type, not a constant"* — "BY CONSTRUCTION."** The construction is exhaustive over the three practice areas that existed when it was written and **fails open for any fourth**, so a probate matter would silently take Plaintiff/Defendant caption alignments. **It is live today and unreachable only because no fourth practice area exists.** **The codebase therefore carries TWO OPPOSITE DEFAULTS for the same question:** `statusesFor()` **throws** on an undeclared pair by deliberate design (`caseTypes.ts:6–9`), while `sideSetFor()` **silently supplies a wrong-but-plausible answer** — the exact behaviour that audit item was written to kill. **Flagged, not fixed**
- **FORM ENGINE — FE-D1 (DISCLOSURES ENGINE) NAMED, SCOPED, AND AUTHORIZED 08-12 (#63); NOT BUILT.** Scope `docs/specs/fe-d1-build-slice.md`; kickoff `docs/prompts/PROMPT-fe-d1-build-session.md`; **yours to fire, Opus, its own session** (the CD-1 pattern — the queue runner is barred). Slice 1 = §§1–6 wizard + §10 substrate + §11.3 skeleton; **NOT "FE-1"** (retired scout). **IN:** FE-10 + FE-12 from birth, FE-8 retention half, FE-15, a MINIMAL template editor. **OUT with homes:** FE-9 · FE-11 · FE-13 · **FE-14 (TRCP 47 gate — blocker CHANGED KIND, see below)** · FE-16 · FE-17. **FE-4/FE-5/FE-6/FE-7 have NO FE-D1 disposition at all**. `docs/skills/drafting-disclosures/SKILL.md` is **a DOCUMENT, not code — now v2** (08-11, design-side; **Code must never edit it**)
- Time tracker: draft only — **its §5 export hold LIFTS on D-CL2-3a, but the tracker stays parked behind CE1**. Servpro deadline engine: DESIGN ONLY. Heartbeat: design docs only (captures e+f NOT folded; register H1–H83). **CE1 still NOT AUTHORIZED** (D-CL2-9) — must be **CLIENT-AWARE from the start**. CourtListener: design doc only, integration UNAUTHORIZED (Q-6). **Descrybe is OUT by ruling 08-13**

## The registry — FOUR files, and the absence pattern is now FIVE
- **THE FILE COUNT WAS WRONG AND IS CORRECTED HERE ONLY: there are FOUR `legal-rule-registry-*` files, not three.** The fourth, `legal-rule-registry-draft-entries-medical-billing.md`, is headed **ALL ENTRIES UNVERIFIED** and its entries are **outside the backlog of 34**. Whether that is deliberate is **Q-STAT-2, Michael's — the BACKLOG STAYS 34**
- **TWENTY VERIFIED** (Michael) in `legal-rule-registry-discovery-and-carrier-duties.md` *(**AUD-3, CONFIRMED FALSE: that file carries 22 VERIFIED status lines, from 13 ruled bullets + 7 entry rulings — a bullets-vs-entries unit mismatch. NOTHING was touched; the count and the wording-attachment question are yours at OPEN-2**)*; **three wording expansions there are FLAGGED, NOT ADOPTED** (TRCP 194's span, 193.3, 192.3(f)) — verification attaches to wording. **AND ONE VERIFIED ENTRY IS UNDER FLAG FOR ITS WORDING: TRCP 190.3(b)(3) — Q-FE5-9. Nothing was touched there either.** **TWENTY-SEVEN UNVERIFIED** in `legal-rule-registry-discovery-enforcement-and-pleading.md` (**six WL/slip cites flagged, none corrected**) — **and IN-1's whole legal substrate is already among them, five entries deduped and NONE added (#83).** **SEVEN UNVERIFIED** in `legal-rule-registry-criminal-plea-and-costs.md` — six as filed, then **former entry 4 SPLIT by your ruling 08-13 into 4a** (per-cause costs, still CITE-LESS) **and 4b** (art. 102.073). Entry 3 and 4a carry NO CITE — supplying one is YOUR act. **`/rules` SEED ALL UNVERIFIED**
- **THE SAME ABSENCE HAS NOW APPEARED FIVE TIMES, AND THE FIFTH IS THE FIRST ALREADY LOAD-BEARING IN SHIPPED CODE (#89, `Q-RE-8` extended).** (1) **NOT ONE PRIVACY PROPOSITION** (#85, `Q-WF-6`). (2) #78's insurance narrowing. (3) **NOT ONE CLIENT-PROPERTY OR TRUST-ACCOUNTING PROPOSITION** (#87, `Q-QBO-6`): `iolta`/`trust`/`safekeeping`/`commingl`/`client funds`/`escrow` all **0**. (4) **NOT ONE PROFESSIONAL-CONDUCT PROPOSITION** (#88): thirteen terms, all zero, re-verified natively at HEAD. (5) **NOT ONE STATEMENT OF WHAT WORK PRODUCT IS OR WHAT THE PRIVILEGE COVERS** (#89): `192.5` **0** · `503` **0** · `work product` **1** across all four files, and **all seven `privilege` hits are about assertion mechanics.** **THE REGISTRY KNOWS HOW TO ASSERT A PRIVILEGE AND NOWHERE SAYS WHAT ONE IS** — while `db/schema.sql` carries two live `not null` `privilege_tier` columns. **The first four were about things not built. THIS ONE HAS BEEN WRITING ROWS.** **THE HONEST STATEMENT STILL HOLDS: the registry carries the law of the firm's CASES and nothing about the law of the firm's PRACTICE; nobody chose that boundary.** **FIVE research passes now stack behind Q-STAT-5's one ruling**
- **THE RETRIEVAL GAP IS CLOSED, AND THE FIGURE WAS WRONG TOO (#76).** The rows reading `RETRIEVAL: NOT RUN` numbered **21, not 20** — and **all 21 are now retrieved** against the official corpus, plus entry 32's official-source read: 22 looks owed, 22 run. **RETRIEVAL IS NOT VERIFICATION. No status moved and the BACKLOG STAYS 34**
- **EIGHTEEN OF THE TWENTY-ONE PROPOSITIONS DIVERGE from the operative text; FOUR CHANGE WHAT THE ENTRY MEANS** — **TRCP 193.2(e)** (the waiver limb is stated **backwards**, and that entry is DE-1's named linchpin), **196.2(b)** (a third permitted response that **does not exist in the rule**), **215.1(d)** (**"shall… unless," not "may"**, plus a reciprocal against a denied movant the entry omits), **47(b)–(c)** (the "all other relief" half is **47(d)**, and a clause **barring discovery until a non-complying pleading is amended** was absent). **PROPOSED replacement wording exists for those four ONLY, ADOPTED FOR NONE** — route (c), your ruling 08-14. The other fourteen stay as flags you resolve at verification (Q-STAT-6)
- **ENTRY 27 IS NO LONGER TEXT PARTIAL, so FE-14's blocker CHANGED KIND: it is a VERIFICATION act now, not a retrieval gap.** The picklist text exists — **five enumerable options, not "fixed brackets"** — and carries a requirement the form engine did not know about: non-compliance with 47(c) **bars discovery** until amendment. **Nothing built, nothing authorized**
- **INSURANCE — THE ABSENCE CLAIM WAS OVER-BROAD AND IS NARROWED HERE (corrected at #78).** What holds, re-verified at HEAD across **all four** files: **chapters 541 and 542 never appear, nor does "prompt pay"** ("carrier duties" in that filename means **motor** carrier). **What was false: one Insurance Code entry DOES exist — ENTRY 8 of the draft-entries file** (SB 1264 / ch. 1467 balance billing, `[EXT]`, low priority). **Different subject**, so nothing else moves. The sweep produced **20 CANDIDATES (C-1…C-20), NONE RULED, NO FIFTH FILE CREATED** (Q-STAT-5). **C-19 (61-day pre-suit notice) and C-20 (2-year limitations, extendable 180 days) are deadline material with a build consequence independent of the registry**
- **REGISTRY RESEARCH SUPPORT — FIVE DOCS, ALL PROPOSED, NOTHING VERIFIED, NO REGISTRY FILE TOUCHED BY ANY OF THEM:** `registry-cite-check-2026-08-13.md` · `registry-verification-workbook-2026-08-13.md` · `registry-citator-pass-2026-08-13.md` · **`statute-pass-registry-retrieval-2026-08-14.md`** — its **§3** carries the **corrected A-for-space normalizer** and the rule that matters more, *transform only what is characterized; REPORT anything else, never guess* · **`deadline-engine-service-and-response-2026-08-14.md`**. **Net from the citator pass: 5 need a look** (Ochoa, Collins, Irwin, Alford, Castillo), **5 need none for cite or majority** (*each still needs a commercial citator; FLP is not one*). **#89 adds a limit to §3's normalizer: it must NOT be carried to the TRCP/TRE PDFs — `AA` count in raw TRCP extraction is 0, a different publisher with a different artifact profile**
- **V-4–V-8 ALL RULED 08-13 (#73) — AND NONE OF IT IS EXECUTED.** V-4 keep as two; V-5 split all three two-case entries — **BACKLOG BECOMES 37 WHEN EXECUTED; it is 34 in the files today**; V-6 both criminal entries stay, reworded; V-7 narrow entry 23; V-8 record all three FLP hazards. **V-9 OPEN and sharp: the *Irwin* class is a state in which CLAUDE.md's BINDING majority-opinion rule CANNOT RUN.** Amending a binding rule is YOURS

## Design-input memos — ALL PROPOSED, nothing built, no registry file touched by any of them
- **DEADLINE ENGINE (#75)** — research support only: seven propositions, a per-defendant computation model, service-method effects, Level 1/2/3 interactions, **rule text from a clean-authority PDF in YOUR Knowledge Repo read locally**. **No deadline engine is in the build queue.** **ITS HEADLINE IS A PRACTICE ITEM FIRST: on the July 2026 text the 50-day discovery-response extension is FAMILY-CODE-ONLY**, and `trcp-deadline-skeleton-2026-03-01.md` §5 states the opposite — **so a PI or civil-lit defendant served before its answer is due has a FLAT 30 DAYS**, with 198.2(c) making deemed admission automatic. **The skeleton was NOT edited; resolving it is your verification act.** Also open: **no TRCP 21a entry exists anywhere**; **Rule 195.2's 30-day floor is GONE**; **190.4(b)(2) now allows a phase-scoped send-by Level 3 period.** **Its five questions carry NO durable IDs (ID-DL-1)**
- **BEXAR LOCAL RULES + E-FILING (#80)** — eleven propositions, packet-local IDs. **PROVENANCE IS MARKED AND MUST NOT BE STRIPPED: TIER A** = clean-authority PDFs read locally · **TIER B** = through a summarizing fetch layer, **not quotable as rule text**. **THE FILING MOMENT IS CONTESTED, NOT SETTLED:** TRCP 21(f)(5) and Statewide Criminal E-Filing R. 2.3(a) fix filing at **transmission to the filer's EFSP** (TIER A); a Bexar county-court sentence appears to fix it at **clerk acceptance** (TIER B), and **TRCP 3a(b) bars a local rule inconsistent with a Supreme Court rule** — exposure: transmitted late on a limitations date, accepted two days later (**LR-LOOK-1**). **LIVE PRACTICE ITEM: Bexar Civil LR 3.O.1 requires an A.I. certificate on ALL pleadings — do your current filings carry it?** **A COUNTY-KEYED COURT PROFILE IS WRONG FOR BEXAR: the 225th and 285th elected OUT** of the Presiding/Monitoring dockets (**H43**). **FILING THROUGH THE APP = becoming or contracting with an OCA-certified EFSP** — same class as Q-6; nothing proposes it
- **FORM-ENGINE SPECS FE-4 / FE-5 / FE-6 (#81)** — they **elaborate the 08-11 rulings and adjudicate nothing**. **All three rows stay ⬜. Twenty-two questions + three looks, packet-local IDs.** **RUNNER-FOUND AND NOT REPAIRED: the FE-5 spec has NO title, NO `Status:` line and no §1 head** — it landed verbatim and was **reconstructed by nobody** (the K-6/K-7 class). **THEIR HEADLINE IS A DIVERGENCE INSIDE A *VERIFIED* ENTRY: the TRCP 190.3(b)(3) entry says "each party," the operative rule says "any other party"** — 25 interrogatories vs. 125 in a five-defendant posture, so an engine built to the entry's wording **would refuse discovery the rule permits, silently. FLAGGED, NOT REWORDED** (Q-FE5-9). Also: the cap is **PAIRWISE and CUMULATIVE**; **Level 3 has no cap to look up**; and **`generated_documents` will not accept an instrument** (its `doc_type` CHECK admits one value)
- **INTAKE SPECS IN-1 / IN-3 (#83)** — **THEY RESOLVE THE OPPOSITE WAY FROM THE FE SPECS: those elaborated RULINGS while the BUILD was gated; IN-1 and IN-3 have NO ruling to elaborate**, so both **MAP the design space and hand the question back. A build session must not read either as a design.** Both rows stay ⬜. **IN-1 has NO design-doc home anywhere**; **IN-3 has exactly one.** **Q-IN1-3 REPORTS AN IRRECONCILABLE CLAIM: IN-6 parses a served RESPONSE SET, IN-1's input is a filed ANSWER**, so no parser is inherited. **IN-3's finding: HOLD cannot be expressed on a document today and its trigger has NO SOURCE.** **Q-IN3-3 IS THE SCHEDULING EDGE: CD-1 deferred the service-story columns to "the first instrument consumer" and that consumer has NEVER BEEN NAMED**
- **IN-2 CRASH-REPORT EXTRACTION SPEC (#84)** — **ITS HOME half is RULED** (`contact-directory.md` §7) **and elaborated; its EXTRACTION half is UNRULED and only mapped.** **The row stays ⬜; nine questions, packet-local IDs.** **THE STRUCTURAL FINDING: IN-2's five named targets are THREE extraction problems and the #64 versioned-code-table requirement governs only ONE** — **class B identifiers (VINs, the DOT number) have NO code list at all.** **A build implementing #64 alone ships with no VIN validation and no promotion gate, silently.** **TWO LINES OF `cr3-field-code-map.md` ARE CORRECTED IN THE SPEC AND THE FILE WAS NOT EDITED:** list 38 has **70** enumerated values, not *"(79 codes)"* — so **range validation is wrong and an unknown code is EVIDENCE of a version mismatch, never an error** (**Q-IN2-2, YOURS**). **VERSION SELECTION IS CIRCULAR** — the crash date is itself under extraction
- **WF-2–WF-8 EMAIL-WORKFLOW SPEC (#85)** — **IT MAPS RATHER THAN ELABORATES: #63's ruling narrowed itself in its own words, three times. AN ADOPTED DOCUMENT IS NOT AN ADOPTED DESIGN.** **All seven rows stay ⬜; ten questions and two looks. SEVEN OF SEVEN GATED, ZERO SATISFIED.** **THE HARDEST GATE IS ON NO ROW AT ALL (Q-WF-4): does this application acquire a server-side identity, and of what shape?** Every WF pipeline is a background watcher measured in weeks; the auth surface has none, webhooks need a public endpoint answering in 3–10 s, and background execution needs app-only credentials this registration deliberately lacks. **IT NOW HAS THREE CONSUMERS** — WF-2–WF-8, **#87's `Q-QBO-3`**, and **#89's `Q-COM-4`, the most demanding**: a communications log wants everything continuously, and even the metadata-only `callRecords` version needs a **scheduled runner to beat a 30-day retention window**. **`Q-WF-4` ITSELF WAS NOT AMENDED at any of the three.** **AND A GATE THAT SHOULD BE THERE IS MISSING: `outlook-email-intake.md` makes HIPAA-by-design and a BAA review a "first-class constraint," and is cited by NEITHER the WF rows NOR #63** — **NOT added as an eighth gate, because adding one is a ruling (Q-WF-5)**
- **PR-3 RE-PARENTING MIGRATION PROPOSAL (#86)** — Task 12. **THE GATE RESOLVED A THIRD WAY:** Task 8 elaborated rulings while the build was gated; Tasks 9–11 mapped design space with no ruling to elaborate; **Task 12 is neither — the DIRECTION is ruled and the DESTINATION is not.** Its §3 target state is **deliberately a question**. **Its three findings are carried above where they bite.** **Treatment of existing records rests on one unknown that costs one query — `PR3-LOOK-1`, yours: if the answer is zero the entire data half drops out and PR-3 becomes source-only.** **THREE SUPERSEDED PASSAGES ARE STILL WRITTEN AS OPEN AND NONE WAS ANNOTATED** — V17 closed all three on 08-07, and annotating them **answers `Q-PR3-5`**
- **QBO INTEGRATION RESEARCH MEMO (#87)** — Task 13. **THE GATE RESOLVED A FOURTH WAY: the session was told "staged constraints govern," and the staged constraints are NOT RULINGS** — they come from `future-modules-capture-2026-07-28.md`, which this file still lists as **UNRULED, adopt nothing** (`Q-QBO-1`). **HEADLINE: THERE IS NO READ-ONLY OAUTH SCOPE.** One accounting scope grants **full read/write** — so **"read-only first" can be a property of the code but never of the grant** (`Q-QBO-2`). **THE ECONOMICS ARE INVERTED:** Intuit meters data-**OUT**, free tier 500k credits/month, **BLOCKS rather than bills** (`Q-QBO-8`). **THE CONSTRAINT WITH LEGAL TEETH IS THE ONE THE PLATFORM HELPS WITH LEAST:** QuickBooks records trust/operating separation as a **naming convention and enforces nothing**, so a read integration **can display a per-matter trust figure that is wrong with the authority of a computed number** (`Q-QBO-5`). **NO INTUIT FIGURE IS VERIFIED; the canonical reference was UNREADABLE across four fetches; two date conflicts FLAGGED NOT RESOLVED.** `QBO-LOOK-1..4` are Michael's
- **RE-1 REFERRAL-ENGINE INPUTS MEMO (#88)** — Task 14. **THE GATE RESOLVED AS TASKS 9–11 DID, AND MORE STARKLY: RE-1 HAS NO DESIGN-DOC HOME AT ALL.** Word-bounded at HEAD it appears **15 times across 7 files** and **no document elaborates it**; its queue row was **un-annotated since the day it was raised**. **HEADLINE: ONE OF THE THREE NAMED TRIGGERS MEANS TWO DIFFERENT THINGS IN THE RECORD, FROM THE SAME SESSION** — geography vs. subject matter (`Q-RE-1`). **AND A FOURTH TRIGGER THE DISPATCH DOES NOT NAME is the only one with a designed mechanism:** the master spec makes *"referral out is first-class behavior"* authoritative, while **overflow is defined nowhere.** **SOURCING NAMES NO SOURCE FOR THE TEXAS DISCIPLINARY RULES — TESTED, NOT ASSUMED:** the bulk `GV.pdf.zip` holds **494 entries, every one a chapter file**, **the TDRPC live in a Government Code appendix the bulk channel does not publish**, the Knowledge Repo has **no disciplinary-rules PDF**, and eCFR is federal. **`QBO-LOOK-4` IS ANSWERED AS TO THE CORPUS HALF: NO.** Not academic — **Gov't Code §82.065(b), §82.0651 and Penal Code §38.12(c) each incorporate the disciplinary rules BY NAME** (`Q-RE-9`). **THE ONE THAT PAYS FOR THE SECTION: TDRPC 1.04(f)(2) IS THE FIELD LIST** — consent **in writing and PRIOR to the referral**, carrying every firm's identity, the division basis and each share; **1.04(g) makes the failure economic and silent — an unconfirmed arrangement does not void the referral, it STRIPS THE FEE.** **`P-RE-1..12` ALL UNVERIFIED** (`RE-LOOK-1`)
- **COMMUNICATIONS-LOG INGEST RESEARCH MEMO (#89)** — Task 15, `communications-log-ingest-research-2026-08-16.md`. **THE GATE RESOLVED A FIFTH WAY AND IT IS THE SHARPEST YET: THE DISPATCH'S OWN WORD IS CONTRADICTED IN TERMS.** It called two constraints **ruled**; `session-log.md` records *"No ruling was made on Slack, Teams, or Claude Tag adoption"* and the whole exchange as *"Claude recommendations, unruled."* Both trace to one paragraph of `future-modules-capture-2026-07-28.md` §3 — **the same gate as `Q-QBO-1`, firing a second time** (`Q-COM-1`). **AND THE TWO RESOLVE DIFFERENTLY:** the **scope exclusion** is honored and **costs nothing**; the **architectural commitment** (platform-agnostic chat ingest) is tested as a hypothesis and **priced for the first time** — relative to mail it costs **a second public HTTPS endpoint, private-key custody, an outside vendor's discretionary approval, and an unestablished metering position, for a channel the firm does not use.** **A NEW GATE CLASS (`Q-COM-5`): a third party's DISCRETIONARY APPROVAL** — protected-API access by request form, reviewed *"every Wednesday"* — **neither Michael's ruling nor another module's construction** (`COM-LOOK-1`). **THE SMS HEADLINE LANDS ON THE STACK ALREADY COMMITTED:** Teams SMS needs three licences plus A2P Brand *and* Campaign approval, is **1:1 with no MMS** — so it **cannot receive the scene photo, the insurance card, or the bill** — and **is apparently not exposed to Graph at all.** **THAT ANSWER IS THE MEMO'S WEAKEST SOURCE, MARKED `[C]` — A FOURTH PROVENANCE TIER IT INTRODUCES AND FLAGS RATHER THAN FOLDING INTO `[B]`** (`Q-COM-9`; `COM-LOOK-3`). **THE ONE IDEA WORTH ITS OWN LINE, PROPOSED: a communications LOG and a communications ARCHIVE are different products, and only the archive needs content** — `callRecords` gives who/when/how-long with **no audio and no transcript, so no privilege, consent or PHI problem attaches, because nothing was captured.** **AND THE ONLY MICROSOFT-NATIVE PATH TO CALL CONTENT INVERTS RULING 8.3** — compliance recording is **policy-based and always-on** against 8.3's deliberate **opt-in per call**, and **partner-held media converts 8.1's architectural guarantee back into a contractual one** (`Q-COM-8`). **`Q-COM-1..12`, `COM-LOOK-1..4` and `P-COM-1..5` are all Michael's**
- **T3 PILOT-RECORDING PROTOCOL (#90)** — Task 16, `docs/t3-pilot-recording-protocol.md`. **THE GATE RESOLVED A SIXTH WAY, AND IT IS THE FIRST THAT IS PURELY OPERATIONAL: there is no design question here at all** — the document is preparation for work that **KICK-1 still forbids**, so it is written to be executable the day the authorization returns and to authorize nothing before then. **Its findings sit above, under Phase 0 / T3, where they bite.** **The two cheapest questions in the whole queue are in this set and neither needs a ruling — `Q-T3P-1` is one file and one look at a README that already exists, and `Q-T3P-3` is one look inside a zip.** **`Q-T3P-1..7` are all Michael's; nothing built, nothing staged, no queue row re-worded, no registry file opened, no schema file touched, `src/` not read**
- **SESSION-LOG TABLE OF CONTENTS (#91)** — Task 17, **`docs/specs/session-log-toc.md`** — 284 non-blank / 310 raw, 52,994 bytes, **FINDING AID — PROPOSED.** One row per entry over all **190** entries of `session-log.md`: `[# | date | type | summary | corrections issued/received]`. **THE GATE RESOLVED A SEVENTH WAY: it is DERIVED rather than designed or prepared** — it corrects nothing, rules nothing, asserts nothing about build status, **and where it and the log disagree the LOG is right and the index is stale.** **THE FINDING THAT MATTERS TO EVERY READER OF THE RECORD: the log runs TWO independent numbering systems and neither covers every entry** — `#nn` (design/Code, **#2–#90**, 89 entries) and **runner ordinal** (**5–45**, 41 entries) — with **eight runner batches carrying a `#nn` instead of an ordinal** (#6, #7, #10, #14, #31, #33, #35, #36) and **one carrying neither**; a reader assuming one system mis-files about a quarter of the log. **NO ENTRY ANYWHERE CARRIES `#1`** and the file cannot distinguish a lost label from a series that began at `#2` (`TOC-1`). **FIVE DATES SIT OUT OF NEWEST-FIRST ORDER; the log explains two and `runner 23`/`#65` is explained nowhere** (`TOC-2`). **THE CORRECTIONS GRAPH IS THINNER THAN IT READS: 67 edges, 22 self-corrections, only 14 resolving to another entry.** **Built from a FULL-TEXT bridge read, not RAG — an index built from fragments is silently partial.** **NOTHING WAS TIDIED: no renumbering, no `#1` backfilled, `#30`'s divergent heading shape not normalized, the five orderings not resequenced. `TOC-1..TOC-5` are all Michael's, and `TOC-5` is the one with a consequence that arrives without a ruling** — at ~53 KB in the synced `docs/specs/` it enters project knowledge and spends context budget against the working-set policy; **flagged, not acted on, no sync-picker change made**
- **GO-LIVE RUNBOOK (#92)** — Task 18, **`docs/specs/go-live-runbook.md`** — 81 non-blank / 106 raw, 9,919 bytes, **POINTER CHECKLIST — PROPOSED.** One row per gate: `[gate | label | who acts | done-when evidence | pointer]`, plus the trigger-1 instructions row and the GL-1 floor. **THE GATE RESOLVED AN EIGHTH WAY: it is DERIVED like #91 but NOT REGENERABLE** — its "who acts" and "done-when evidence" columns are judgement, not derivation, so an appended gate silently makes it incomplete (`GLR-3`). **Its substance sits above, under THE LAUNCH PATH.** **`GLR-2` and `GLR-3` are Michael's; `GLR-1` IS NOT A NEW QUESTION — it is `OPEN-1`/`AUD-2` in its third recording and was annotated onto that row rather than given a fourth ID.** **RUNNER-VERIFIED, because the file's whole value is its pointers: all 21 distinct line numbers check out against `Go_Live_Gates.md`, and the source figures reproduce exactly (106 raw / 84 non-blank).** **AND THE ENTRY ITSELF NEEDED CORRECTING: it says the instructions "are now v19" — see below — and calls v15 "five revisions stale" when it is four on that premise and three on v18. `GLR-1`'s substance survives both.** Nothing built, no gate edited, no status column added

## For design side
- SYNC: selective — docs/, db/, supabase/, CLAUDE.md, README.md, BUILD-SESSION-NOTES.md; **src/ EXCLUDED**, which makes this doc the SOLE authority on what is built. *(Live tension, now a queue item: **`Q-PR3-1`** asks whether a design-side `src/` read through the device bridge is sanctioned at all — the working-set policy says design sessions do not read source, that sentence predates the bridge, and **every finding in the #86 proposal is undrivable without such a read**. Michael's; nothing from `src/` entered project knowledge.)*
- **INSTRUCTIONS: v18 IS IN FORCE, and it is UNDATED — RULED 2026-08-15 (#82, A-1).** **The standing lesson: a running session can read its own instructions; a Code session structurally cannot, so this file must never assert a paste state it did not learn from a design session.** DT-1 remains in force. **TRIGGER #3 HAS FIRED (#88): `QR-5` is a binding convention added 2026-08-16, and a v19 DRAFT was produced design-side. THE PASTE IS MICHAEL'S AND THIS FILE ASSERTS NOTHING ABOUT WHETHER IT HAPPENED** — *(and #92 tested exactly that discipline: its entry states flatly that "the instructions are now v19." **NOT ADOPTED here and not carried into the queue.** A design-side session asserting a paste is the same class of claim this line exists to refuse. The drift is now on the record three times — v17, v18, v19 — which is the strongest argument yet for ruling `OPEN-1`)*
- **SOURCING IS NOW A BINDING CONVENTION (Q-STAT-1 RULED 08-14 — CLOSED).** Statutes, rules and regulations get a named-source rule **parallel to TOOLING**: official corpus / clean-authority PDFs / eCFR API, **each source named per item**, currency never inferred from a document, extraction artifacts **characterized rather than guessed**, and **retrieval is not verification**. **Full text lives in v18, not here.** **Q-STAT-2 through Q-STAT-6 stay OPEN**, and **#88 found the convention's first proven GAP — none of its three layers reaches the disciplinary rules (`Q-RE-9`). SOURCING was NOT amended.** **Route (c) was ruled a ONE-OFF and is deliberately NOT standing law**
- **CHAT-DISPATCH: TASKS 1–18 COMPLETE** — 1+2 at #72, 3 at #74, 4 and 5 design-side, **6 = the deadline memo (#75), 7 = Bexar (#80), 8 = the three form-engine specs (#81), 9 = the two intake specs (#83), 10 = IN-2 (#84), 11 = the WF spec (#85), 12 = the PR-3 proposal (#86), 13 = the QBO memo (#87), 14 = the RE-1 memo (#88), 15 = the communications-log memo (#89), 16 = the T3 pilot-recording protocol (#90), 17 = the session-log TOC (#91), 18 = the go-live runbook (#92). TASK 19 IS THE LAST AND IS NOT STARTED — RESUME AT TASK 19** (**INTERACTIVE, registry sign-off execution, only with you live**; it consumes Task 1+2's workbook and citator output). **RISK ON THE RECORD (#77, sharper at each of #80–#92): the CHAT-DISPATCH prompt has NO durable home** — not in the repo, now **sixteen** sessions on your clipboard, though the exposure is down to **ONE task**; **if lost, Task 19 is unrecoverable except by re-derivation.** *(The paste-fidelity datum STAYS AT SEVEN: the #92 packet does not record the comparison, so this refresh does not claim an eighth.)* **OPEN-4** asks whether `docs/prompts/` should take design-chat prompts too
- **HK-7 STAYS OPEN, AND #92 IS THE THIRTEENTH ANNOTATION.** The count has been sound from #90 forward, counting from a written annotation rather than a header sentence; **it is NOT retroactively sound** — #89's occurrence is recorded only here and was deliberately not back-written. **A THIRD CONSECUTIVE SINGLE-FOLDER GRANT WAS SUFFICIENT, and at #92 it was again LOAD-BEARING:** the runbook's value is its 21 line pointers, and **line numbers cannot be derived from RAG fragments** — the gates doc was read full-text at HEAD through the bridge. **Reading strengthened again, still not ruled: the grant is task-shaped, not a standing two-folder requirement.** Task 19 needs it re-granted. **FLP throttles at 5 req/min**
- **BOTH STANDING SWEEPS ARE CLOSED (#68)** — a pass, not a standing guarantee. Open from them: **OBS-1**
- **CD-2 ROLE MINING PASS FILED (#74)** — PROPOSED, DATA PREP ONLY. **CD-2 is NOT an open design question** — it is a coverage audit of a ruled taxonomy. **The honest gap: `src/domain/partyRegistry.ts` and `src/domain/roster.ts` were NOT read**, so ~60 rows carry `TAG-CHECK: NOT RUN`. *(#86 read `roster.ts` and found the `sideSetFor()` defect there — evidence that the gap was load-bearing.)* **Ten questions became CD-4 – CD-13**
- **PRACTICE-PROJECT CHANNEL IS THREE: CIVIL LIT + CRIM DEFENSE + PROBATE** (V-3, 08-13). New probate matters are CHATS inside PROBATE; the two grandfathered matter workspaces reach the build side by ONE path: **PROBATE's client-clean REQ-CAPTUREs**
- **SIX REQ-1 CAPTURES ARE FILED** at `docs/specs/REQ-CAPTURE_*`. First three SPENT as CD-1 input; UIM + deficiency seeded the **DE series**. **No capture in the repo is unreconciled.** **CR SERIES CR-1–CR-11** plus a **binding black-and-white print constraint**; **CR-10 is HARD-GATED** on the criminal registry entries. **COLLISION FLAGGED, NOT RENAMED:** `CR-3` also means the TxDOT crash-report form at IN-2
- **YOUR HAND, THE LAUNCH PATH IN ORDER: run the CD-1 migration** (back up, paste alone, answer its five checks in words) → **gate 1 Supabase Pro** → **gate 9 production SMTP** → gate 3 RLS test **including the two new tables** → gates re-check + v17. *(**`Q-PR3-3` now touches step 1: the PR-3/CD-1 order has a consequence and is unruled.** **`docs/specs/go-live-runbook.md` is now the pointer checklist for these steps — but it carries NO status, so this file stays the authority on where you are.**)*
- **ALSO YOURS: re-upload SKILL.md v2** (SK-v2) · **the sync-picker clicks** (Q-3) · **telemetry lockdown on the P1** · **HK-6** · **HK-7 re-grant each session** · **V-2, Ochoa first** · **V-9** · **OBS-1** · **CD-4 – CD-13** · **Q-STAT-2 – Q-STAT-6** · **the deadline memo's five questions + ID-DL-1** (now **THIRTEEN** packets) · **the Task 8 specs' 22 + 3 looks — Q-FE5-9 first, it sits inside a VERIFIED entry** · **the Task 9 specs' 18 — Q-IN1-8 is a one-word veto on the whole IN series** · **the Task 10 spec's nine — Q-IN2-2 is cheapest** · **the Task 11 spec's ten + `WF-LOOK-1` — Q-WF-4 decides whether the WF series is reachable at all, now at THREE consumers** · **the Task 12 proposal's seven + `PR3-LOOK-1` — one query that may delete half of PR-3** · **the Task 13 memo's eight + four looks** · **the Task 14 memo's nine + three looks — `RE-LOOK-3` is a PDF only you can fetch** · **the Task 15 memo's twelve + four looks + five propositions — `Q-COM-11` is the one already writing rows** · **the Task 16 protocol's seven — `Q-T3P-1` is the cheapest question in the whole queue, and `Q-T3P-7`'s `docs/` placement is your INTERIM direction, not yet a ruling** · **the Task 17 index's five — `TOC-5` is the only one with a cost that starts accruing at your next Sync click** · **the Task 18 runbook's `GLR-2` and `GLR-3` — `GLR-2` is the SAME question as `TOC-3` and one ruling closes both** · **`OPEN-1`, now recorded THREE times and the cheapest high-value ruling on the launch path** · **`QR-6(a)`–`(f)`, six proposed runner conventions** · **project-instructions v19 (QR-5 fired trigger #3)** · **route (c) as standing law or one-off** · **delete `Knowledge Repo\Statutes 26-08-14\_claude_extract\`**
- **METER, AS A TIMESTAMPED SNAPSHOT AND NOT A STANDING FIGURE (stated 2026-08-15 00:02 Central): all-models 51% used, Fable 78% used, both resetting Tue 2026-08-18 4:00 PM Central.** **The consequence, design input and not a ruling: Fable is SCARCE, not idle** — realistically one substantial session — **so ration it.** All-models is comfortable, so **Opus and Sonnet work is unconstrained**. Strongest claims on the Fable remainder: **the gates re-check (GL-1 item 5), an adversarial audit of the record, and V-9**
- **DE SERIES** — deficiency enforcement. **DE-1** taxonomy-as-data and **DE-2** the conferral/escalation timeline, MANUAL triggers per Q-6. **The parked heartbeat Part-6 engine's HOME, not its green light. Nothing built** — and **193.2(e), DE-1's linchpin, is one of the four entries whose wording the statute pass says is backwards**
- **REQ-10 placeholder discipline: CONFIRMED AS CARRY.** **EMAIL-WORKFLOW DOC ADOPTED** (#63) — seven pipelines take **WF-2–WF-8**, every one gated, **and the #85 spec confirms every gate is still shut**. **CL2-AC-1 is DIRECTION-CONFIRMED**
- **The probate corpus is NOT in the repo and never has been** — `Probate Corpus.zip` sits untouched in the **P15's** gitignored `inbox/`; CORPUS-HOME closed to ARCHIVE, **your hand**. *(Re-checked #92: this machine's `inbox/` held only the batch packet — consistent with mdb-pllc not being the P15, and a candidate datum for **OPEN-3**, not an answer.)* **LADDER PASS DEFERRED** pending Domser; **PL-1..PL-4 UNRULED**
- **RUNNER DISCIPLINE — QR-1..QR-5 + MM-1 CLOSED; runner v8** (`docs/prompts/QUEUE-RUNNER.md`, the **ONLY full copy**; the `/queue-runner` command is a pointer to it). **Your hand: the P15 user-level copy deletion.** **QR-5, RULED 2026-08-16: (a) A SESSION-LOG ENTRY ASSERTS NO POST-COMMIT ACTION** — results go to Michael in-session and any failure **rides into the NEXT runner line.** **(b)** the deletion is **VERIFIED** by re-listing `inbox/`; Step 1 marks a packet whose deliverable already exists as **POSSIBLY ALREADY EXECUTED, distinguishing committed-and-pushed from committed-but-unpushed**; and Step 0 gains the `Bash(rm -f inbox/*)` allowlist precondition. **QR-3 STILL CANNOT RUN THROUGH THE DEVICE BRIDGE and must stay native** (#74): the mount reads a false DIRTY of ~199–200 CRLF-only files, and `git fetch` fails there with `HTTP 403 from proxy`
- **QR-5(a) HAS NOW COMPLETED TWO FULL CYCLES AND THE MECHANISM HOLDS.** Cycle 1: the forty-fifth line asserted nothing, and the forty-sixth's native gate confirmed both limbs. **Cycle 2: the forty-sixth asserted nothing, and the forty-seventh confirmed both** — `HEAD == origin/master == 6ca9794`, which **is** the forty-sixth's own commit, so the push landed; `inbox/` held only the new packet, so the deletion ran. **Nothing was carried forward either time.** *(The standing division of labour: the design side can reach "local refs agree and `inbox/` is empty," which is equally consistent with Michael's hand — **only a networked fetch settles it**, and only the next runner can run one.)*
- **THE STALE `.git/index.lock` HAS AN OBSERVED CAUSE RATHER THAN A HYPOTHESIS — #90; the count is FIVE-FOR-TEN after #91 found it ABSENT.** The Task 16 design session ran `git status` through the bridge, git emitted `warning: unable to unlink '…/.git/index.lock': Operation not permitted`, and **the 0-byte lock stranded: the mount DENIES `unlink`, so git's own cleanup fails.** **Evidence only — expressly NOT a BUILD-STATE amendment beyond the tally.** At #91 the lock was **ABSENT** by `Test-Path`, no `.claude*` residue remained, and `git update-index --refresh` returned only *"needs update"* on that batch's own two edited files — **ordinary stat drift, not a lock failure** — so the lock was proven acquirable rather than assumed. **A bridge read is not evidence about origin (#88) but it IS evidence about local files**
- **SIX PROPOSED RUNNER CONVENTIONS, `QR-6(a)`–`(f)`, NONE RULED (#89, runner-found).** `docs/specs/queue-runner-step1-audit-2026-08-16.md`. (a) a verification names the command that produced it — **a check that cannot produce a disconfirmation is not a verification**; (b) the queue merge is **two acts**, rows **and** the header reconcile sentence; (c) delete by **explicit filename**, not the glob, pinning packet identity at Step 1; (d) name a format exemplar; (e) packet-added acts outside the checklist need in-session authorization; (f) a docs-only batch records the health-check skip explicitly. **Followed as if ruled at #90, #91 and #92; (a), (b) and (f) each did visible work at #92**
- **THE H-STRING COLLISION IS ENDED:** the log-#44 housekeeping series re-lettered **H → HK**. **Heartbeat H1–H83 untouched — never rename them.** The naming caveat covers **IN / DE / CR / HK** — **and now `T` as well, flagged at #85 (Q-WF-2) and NOT re-lettered**
- **`docs/reference/` HOLDS A BINARY** — `CR3-code-sheet-2023.pdf` (blank public TxDOT code list, verified client-clean). It is the **CODE SHEET, not the report form**, so the repo has IN-2's *vocabulary* inventory and **not its field inventory** (Q-IN2-1). Synthesis `cr3-field-code-map.md`, **PROPOSED input for IN-2 — nothing built.** **Two of its lines are corrected by the #84 spec and the file was NOT edited**
- **VERIFICATION STATUS: the #62/#63 and #64 batches were VERIFIED design-side FULL-TEXT on 08-13 (#65)**; **#67 verified landed at the top of #68.** Still UNREVIEWED: the carried **#31–#33** material, **#37–#61's own routing**, and **#75 through #92** — do not copy #36's clearance forward *(A-4, ruled #82: the range is **recomputed from the log at HEAD at each refresh**, never copied from a packet — and it was recomputed this refresh)*
- **Everything awaiting your ruling is in `docs/specs/attorney-review-queue.md`** — reconciled through **#92** *(A-5, ruled #82: recomputed from that file's own header at HEAD)*. **K-6/K-7 RETIRED — reconstruct NOTHING.** **UNRULED, adopt nothing: `future-modules-capture-2026-07-28.md`.** Client model: design doc §10. FOLD PENDING: captures e+f into case-heartbeat-design.md §8. Carried: no law-change ledger, Outlook unreviewed
