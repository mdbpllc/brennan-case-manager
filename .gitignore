# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Queued push-to-code packets (transient freight; never committed)
inbox/

# Claude Code machine-local permissions. Untracked by design: QUEUE-RUNNER Step 0 item 4
# edits this file and forbids staging it. NOT the whole .claude/ directory — commands/
# and launch.json are tracked and must stay tracked.
.claude/settings.local.json

# Environment files (keep .env.example)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
